# Benchmark Evaluation Report — SUS Data RAG

**Model:** ollama  
**Run date:** 2026-08-23 01:53  
**API:** http://127.0.0.1:8000  

## Summary

| Metric | Value |
|---|---|
| Total queries | 118 |
| Correct (EA numerator) | 86 |
| **Execution Accuracy (EA)** | **72.9%** |
| Wilson IC 95% | [64.2%, 80.1%] |
| Correct via flexible match (alias diferente) | 57 |
| API errors / timeouts | 0 |
| Mean Time-to-Answer | 5.7s |

> **Nota metodológica — Scoring:** A Execution Accuracy avalia se o SQL gerado
> retorna os mesmos **valores** que o gold-standard, independente do nome das colunas.
> Quando o LLM usa um alias diferente mas retorna os dados corretos (match posicional),
> a query é contada como correta e sinalizada com `~`. Esta decisão segue a definição
> de EA em [Lee et al., 2022] — comparação de conjuntos de resultado, não de SQL texto.

## EA by Category

| Category | Correct | Total | EA |
|---|---:|---:|---:|
| Epidemiológica Simples | 32 | 33 | 97.0% |
| Epidemiológica Complexa | 14 | 31 | 45.2% |
| Financeira | 19 | 26 | 73.1% |
| Temporal/Comparativa | 21 | 28 | 75.0% |

## Query Results

### ✅ Q01 — Epidemiológica Simples

**Q:** Total de internações ortopédicas (M00-M99 e S00-S99) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=69487 exp=69487`
**TTA:** 11816ms

---

### ✅ Q02 — Epidemiológica Simples

**Q:** Total de procedimentos ambulatoriais ortopédicos em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(*) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=105364 exp=105364`
**TTA:** 4262ms

---

### ✅ Q03 — Epidemiológica Simples

**Q:** Total de internações por fratura de fêmur (S72) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%';
```

**Resultado:** `got=30272 exp=30272`
**TTA:** 3832ms

---

### ✅ Q04 — Epidemiológica Simples

**Q:** Total de internações por osteoartrose (M16, M17) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'M16%' OR cid_principal LIKE 'M17%');
```

**Resultado:** `got=165 exp=165`
**TTA:** 4232ms

---

### ✅ Q05 — Epidemiológica Simples

**Q:** Número de óbitos em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND morte = 1;
```

**Resultado:** `got=2274 exp=2274`
**TTA:** 4794ms

---

### ✅ Q06 — Epidemiológica Simples

**Q:** Total de internações por traumatismos (S00-T98) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND icd_group = 'S00-T98';
```

**Resultado:** `got=62607 exp=62607`
**TTA:** 3942ms

---

### ✅ Q07 — Epidemiológica Simples

**Q:** Total de internações por doenças osteomusculares (M00-M99) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND icd_group = 'M00-M99';
```

**Resultado:** `got=6880 exp=6880`
**TTA:** 3940ms

---

### ✅ Q08 — Epidemiológica Simples

**Q:** Número de mulheres internadas por causa ortopédica em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND sexo_paciente = 'F'
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=27027 exp=27027`
**TTA:** 4870ms

---

### ✅ Q09 — Epidemiológica Simples

**Q:** Número de homens internados por causa ortopédica em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND sexo_paciente = 'M'
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=42460 exp=42460`
**TTA:** 4868ms

---

### ✅ Q10 — Epidemiológica Simples

**Q:** Número de internações ortopédicas de idosos (60 anos ou mais) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND idade_paciente >= 60;
```

**Resultado:** `got=28165 exp=28165`
**TTA:** 4912ms

---

### ✅ Q11 — Epidemiológica Simples

**Q:** Número de municípios distintos com internação ortopédica registrada em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT cod_munic_estabelecimento) AS total_municipios
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=215 exp=215`
**TTA:** 4666ms

---

### ✅ Q12 — Epidemiológica Simples

**Q:** Número de estabelecimentos distintos com internação ortopédica em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT cnes_estabelecimento) AS total_estabelecimentos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=385 exp=385`
**TTA:** 4662ms

---

### ✅ Q13 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com permanência superior a 7 dias em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND dias_perm > 7;
```

**Resultado:** `got=17778 exp=17778`
**TTA:** 4821ms

---

### ✅ Q14 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com uso de UTI em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes_com_uti
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND uti_int_to > 0;
```

**Resultado:** `got=1 exp=1`
**TTA:** 5041ms

---

### ✅ Q15 — Epidemiológica Simples

**Q:** Número de internações por fratura de quadril (S72) em pacientes com 70 anos ou mais em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
  AND idade_paciente >= 70;
```

**Resultado:** `got=15626 exp=15626`
**TTA:** 4203ms

---

### ✅ Q16 — Epidemiológica Complexa

**Q:** Top 10 CIDs ortopédicos por volume de internação em SP em 2023

**SQL gerado:**
```sql
SELECT cid_principal,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cid_principal
ORDER BY internacoes DESC
LIMIT 10;
```

**Resultado:** `10 linhas corretas (flexível)`
**TTA:** 5237ms

---

### ✅ Q17 — Epidemiológica Complexa

**Q:** Top 5 municípios de SP com maior número de internações ortopédicas em 2023

**SQL gerado:**
```sql
SELECT cod_munic_estabelecimento,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cod_munic_estabelecimento
ORDER BY internacoes DESC
LIMIT 5;
```

**Resultado:** `5 linhas corretas (flexível)`
**TTA:** 5597ms

---

### ❌ Q18 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por faixa etária (0-17, 18-59, 60+) em SP em 2023

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN idade_paciente BETWEEN 0 AND 17 THEN '0-17'
    WHEN idade_paciente BETWEEN 18 AND 59 THEN '18-59'
    ELSE '60+'
  END AS faixa_etaria,
  COUNT(DISTINCT n_aih) AS internacoes,
  ROUND(100.0 * COUNT(DISTINCT n_aih) / SUM(COUNT(DISTINCT n_aih)) OVER (), 1) AS pct
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY faixa_etaria
ORDER BY internacoes DESC;
```

**Resultado:** `Linha não encontrada: {'faixa_etaria': '0-17', 'total': 4040}`
**TTA:** 8794ms

---

### ❌ Q19 — Epidemiológica Complexa

**Q:** Taxa de mortalidade hospitalar por CID ortopédico (top 5) em SP em 2023

**SQL gerado:**
```sql
SELECT 
  cid_principal,
  ROUND(100.0 * SUM(CASE WHEN morte = 1 THEN 1 ELSE 0 END) / COUNT(*), 2) AS taxa_mortalidade
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY 
  cid_principal
ORDER BY 
  taxa_mortalidade DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cid_principal': 'T274', 'taxa_mortalidade': 100.0} exp={'cid_principal': 'S062', 'total_internacoes': 58, 'total_obitos': 20.0, 'taxa_mortalidade_pct': 34.48}`
**TTA:** 6703ms

---

### ✅ Q20 — Epidemiológica Complexa

**Q:** Top 10 CIDs ambulatoriais ortopédicos por volume de produção em SP em 2023

**SQL gerado:**
```sql
SELECT 
  cid_principal,
  COUNT(*) AS total_producao
FROM 
  processed
WHERE 
  sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY 
  cid_principal
ORDER BY 
  total_producao DESC
LIMIT 10;
```

**Resultado:** `10 linhas corretas (flexível)`
**TTA:** 5553ms

---

### ✅ Q21 — Epidemiológica Complexa

**Q:** Proporção de internações ortopédicas por sexo em SP em 2023

**SQL gerado:**
```sql
SELECT 
  sexo_paciente,
  COUNT(DISTINCT n_aih) AS total_internacoes,
  ROUND(100.0 * COUNT(DISTINCT n_aih) / SUM(COUNT(DISTINCT n_aih)) OVER (), 1) AS pct
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY sexo_paciente
ORDER BY total_internacoes DESC;
```

**Resultado:** `2 linhas corretas, ordem livre (flexível)`
**TTA:** 6805ms

---

### ❌ Q22 — Epidemiológica Complexa

**Q:** Número de internações por fratura de fêmur (S72) em idosos (60+) comparado ao total em SP em 2023

**SQL gerado:**
```sql
SELECT 
  COUNT(DISTINCT CASE WHEN idade_paciente >= 60 THEN n_aih END) AS internacoes_idosos,
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%';
```

**Resultado:** `Linha 1: got={'internacoes_idosos': 19589, 'total_internacoes': 30272} exp={'total_s72': 30272, 's72_idosos': 19598.0, 'pct_idosos': 64.7}`
**TTA:** 4951ms

---

### ❌ Q23 — Epidemiológica Complexa

**Q:** Top 5 CIDs ortopédicos com maior permanência média hospitalar em SP em 2023

**SQL gerado:**
```sql
SELECT
  cid_principal,
  ROUND(AVG(dias_perm), 1) AS permanencia_media_dias
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cid_principal
ORDER BY permanencia_media_dias DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cid_principal': 'S141', 'permanencia_media_dias': 73.0} exp={'cid_principal': 'S272', 'total_internacoes': 49, 'permanencia_media_dias': 14.3}`
**TTA:** 5603ms

---

### ✅ Q24 — Epidemiológica Complexa

**Q:** Top 5 estabelecimentos com maior volume de internação ortopédica em SP em 2023

**SQL gerado:**
```sql
SELECT cnes_estabelecimento,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cnes_estabelecimento
ORDER BY internacoes DESC
LIMIT 5;
```

**Resultado:** `5 linhas corretas (flexível)`
**TTA:** 5520ms

---

### ✅ Q25 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por raça/cor do paciente em SP em 2023

**SQL gerado:**
```sql
SELECT raca_cor_paciente,
  COUNT(DISTINCT n_aih) AS total_internacoes,
  ROUND(100.0 * COUNT(DISTINCT n_aih) / SUM(COUNT(DISTINCT n_aih)) OVER (), 1) AS pct
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY raca_cor_paciente
ORDER BY total_internacoes DESC;
```

**Resultado:** `6 linhas corretas, ordem livre (flexível)`
**TTA:** 6887ms

---

### ❌ Q26 — Epidemiológica Complexa

**Q:** CIDs ortopédicos com maior número de dias totais de internação em SP em 2023

**SQL gerado:**
```sql
SELECT cid_principal,
  SUM(dias_perm) AS total_dias_internacao
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cid_principal
ORDER BY total_dias_internacao DESC
LIMIT 10;
```

**Resultado:** `Linha 1: got={'cid_principal': 'S720', 'total_dias_internacao': 65515.0} exp={'cid_principal': 'S720', 'internacoes': 9059, 'total_dias_internacao': 65515.0}`
**TTA:** 5446ms

---

### ✅ Q27 — Epidemiológica Complexa

**Q:** Top 5 procedimentos ambulatoriais ortopédicos mais realizados em SP em 2023

**SQL gerado:**
```sql
SELECT cod_procedimento,
  COUNT(*) AS total_realizados
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cod_procedimento
ORDER BY total_realizados DESC
LIMIT 5;
```

**Resultado:** `5 linhas corretas (flexível)`
**TTA:** 5190ms

---

### ❌ Q28 — Epidemiológica Complexa

**Q:** Proporção de internações ortopédicas com paciente de outro município em SP em 2023

**SQL gerado:**
```sql
SELECT 
  ROUND(100.0 * COUNT(CASE WHEN cod_munic_residencia != cod_munic_estabelecimento THEN n_aih END) / COUNT(DISTINCT n_aih), 1) AS proporcao
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `Linha 1: got={'proporcao': 57.0} exp={'total_internacoes': 69487, 'deslocamento_intermunicipal': 39611.0, 'pct_deslocamento': 57.0}`
**TTA:** 5979ms

---

### ❌ Q29 — Epidemiológica Complexa

**Q:** Comparativo de volume mensal SIA vs SIH ortopédico em SP em 2023

**SQL gerado:**
```sql
SELECT
  mes_cmpt,
  sistema,
  COUNT(*) AS volume
FROM processed
WHERE uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY mes_cmpt, sistema
ORDER BY mes_cmpt, sistema;
```

**Resultado:** `Linha 10: got={'mes_cmpt': 5, 'sistema': 'SIH', 'volume': 5918} exp={'mes_cmpt': 5, 'sistema': 'SIH', 'total': 5917}`
**TTA:** 4877ms

---

### ❌ Q30 — Epidemiológica Complexa

**Q:** Idade média dos pacientes internados por fratura de fêmur (S72) em SP em 2023

**SQL gerado:**
```sql
SELECT ROUND(AVG(idade_paciente), 1) AS idade_media
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%';
```

**Resultado:** `Linha 1: got={'idade_media': 62.4} exp={'idade_media': 62.4, 'idade_minima': 0.0, 'idade_maxima': 99.0}`
**TTA:** 3896ms

---

### ✅ Q31 — Financeira

**Q:** Custo total de todas as internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=187220821.79 exp=187220821.79`
**TTA:** 4853ms

---

### ✅ Q32 — Financeira

**Q:** Custo médio por internação ortopédica em SP em 2023

**SQL gerado:**
```sql
SELECT ROUND(AVG(COALESCE(custo_total, 0)), 2) AS custo_medio_por_internacao
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=2693.13 exp=2693.13`
**TTA:** 5043ms

---

### ✅ Q33 — Financeira

**Q:** Custo total de internações por fratura de fêmur (S72) em SP em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%';
```

**Resultado:** `got=82692089.44 exp=82692089.44`
**TTA:** 4163ms

---

### ❌ Q34 — Financeira

**Q:** Top 5 CIDs ortopédicos com maior custo total de internação em SP em 2023

**SQL gerado:**
```sql
SELECT cid_principal,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cid_principal
ORDER BY custo_total DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cid_principal': 'S720', 'custo_total': 30400546.73} exp={'cid_principal': 'S720', 'internacoes': 9059, 'custo_total': 30400546.73}`
**TTA:** 5656ms

---

### ❌ Q35 — Financeira

**Q:** Custo total de internações ortopédicas com uso de OPME (val_ortp > 0) em SP em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND val_ortp > 0;
```

**Resultado:** `Linha 1: got={'custo_total': None} exp={'internacoes_com_opme': 0, 'custo_total_opme': None}`
**TTA:** 5183ms

---

### ✅ Q36 — Financeira

**Q:** Valor total de honorários profissionais (val_sp) em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(val_sp, 0)), 2) AS valor_total_honorarios
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=32810269.18 exp=32810269.18`
**TTA:** 4882ms

---

### ❌ Q37 — Financeira

**Q:** Custo médio por dia de internação ortopédica em SP em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)) / SUM(COALESCE(qt_diarias, 0)), 2) AS custo_medio_por_dia
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=487.64 exp=441.21`
**TTA:** 5584ms

---

### ❌ Q38 — Financeira

**Q:** Top 5 estabelecimentos com maior custo total de internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT cnes_estabelecimento,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cnes_estabelecimento
ORDER BY custo_total DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cnes_estabelecimento': '2078015', 'custo_total': 11295149.42} exp={'cnes_estabelecimento': '2078015', 'internacoes': 2257, 'custo_total': 11295149.42}`
**TTA:** 5982ms

---

### ✅ Q39 — Financeira

**Q:** Custo total de procedimentos ambulatoriais ortopédicos em SP em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=9092221.75 exp=9092221.75`
**TTA:** 4849ms

---

### ❌ Q40 — Financeira

**Q:** Proporção custo serviço hospitalar vs honorários em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT
  ROUND(100.0 * SUM(COALESCE(val_sh, 0)) / SUM(COALESCE(custo_total, 0)), 2) AS pct_val_sh,
  ROUND(100.0 * SUM(COALESCE(val_sp, 0)) / SUM(COALESCE(custo_total, 0)), 2) AS pct_val_sp
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `Linha 1: got={'pct_val_sh': 82.14, 'pct_val_sp': 17.52} exp={'custo_servico_hospitalar': 153782049.46, 'honorarios_profissionais': 32810269.18, 'pct_hospitalar': 82.4}`
**TTA:** 7329ms

---

### ❌ Q41 — Temporal/Comparativa

**Q:** Distribuição mensal de internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT
  mes_cmpt,
  COUNT(DISTINCT n_aih) AS internacoes,
  ROUND(100.0 * COUNT(DISTINCT n_aih) / SUM(COUNT(DISTINCT n_aih)) OVER (), 1) AS pct
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'internacoes': 5667, 'pct': 8.2} exp={'mes_cmpt': 1, 'total_internacoes': 5667}`
**TTA:** 6685ms

---

### ❌ Q42 — Temporal/Comparativa

**Q:** Distribuição mensal de procedimentos ambulatoriais ortopédicos em SP em 2023

**SQL gerado:**
```sql
SELECT 
  mes_cmpt,
  COUNT(*) AS total_procedimentos,
  ROUND(100.0 * COUNT(*) / SUM(COUNT(*)) OVER (), 1) AS pct
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'total_procedimentos': 8415, 'pct': 8.0} exp={'mes_cmpt': 1, 'procedimentos': 8415}`
**TTA:** 6072ms

---

### ✅ Q43 — Temporal/Comparativa

**Q:** Mês com maior número de internações por fratura de fêmur (S72) em SP em 2023

**SQL gerado:**
```sql
SELECT mes_cmpt, COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
GROUP BY mes_cmpt
ORDER BY total_internacoes DESC
LIMIT 1;
```

**Resultado:** `got=8 exp=8`
**TTA:** 4699ms

---

### ✅ Q44 — Temporal/Comparativa

**Q:** Evolução mensal do custo total de internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT
  mes_cmpt,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Resultado:** `12 linhas corretas (exato)`
**TTA:** 5619ms

---

### ❌ Q45 — Temporal/Comparativa

**Q:** Volume mensal de internações por trauma (S00-T98) vs doenças musculoesqueléticas (M00-M99) em SP em 2023

**SQL gerado:**
```sql
SELECT
  mes_cmpt,
  COUNT(CASE WHEN icd_group = 'S00-T98' THEN n_aih END) AS internacoes_trauma,
  COUNT(CASE WHEN icd_group = 'M00-M99' THEN n_aih END) AS internacoes_musculoesquelético
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Resultado:** `Número de linhas: got=12 exp=24`
**TTA:** 6054ms

---

### ✅ Q46 — Temporal/Comparativa

**Q:** Evolução mensal de óbitos em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT
  mes_cmpt,
  COUNT(DISTINCT CASE WHEN morte = 1 THEN n_aih END) AS total_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Resultado:** `12 linhas corretas (exato)`
**TTA:** 5628ms

---

### ✅ Q47 — Temporal/Comparativa

**Q:** Permanência média por trimestre nas internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN mes_cmpt BETWEEN 1 AND 3 THEN 'Q1'
    WHEN mes_cmpt BETWEEN 4 AND 6 THEN 'Q2'
    WHEN mes_cmpt BETWEEN 7 AND 9 THEN 'Q3'
    ELSE 'Q4'
  END AS trimestre,
  ROUND(AVG(dias_perm), 1) AS permanencia_media_dias
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY trimestre
ORDER BY trimestre;
```

**Resultado:** `4 linhas corretas, ordem livre (exato)`
**TTA:** 7683ms

---

### ✅ Q48 — Temporal/Comparativa

**Q:** Mês com maior custo médio por internação ortopédica em SP em 2023

**SQL gerado:**
```sql
SELECT mes_cmpt,
  ROUND(AVG(custo_total), 2) AS custo_medio
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY mes_cmpt
ORDER BY custo_medio DESC
LIMIT 1;
```

**Resultado:** `got=1 exp=1`
**TTA:** 5549ms

---

### ✅ Q49 — Temporal/Comparativa

**Q:** Sazonalidade mensal de fraturas de fêmur (S72) em SP em 2023

**SQL gerado:**
```sql
SELECT
  mes_cmpt,
  COUNT(*) AS total_fraturas
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 4302ms

---

### ✅ Q50 — Temporal/Comparativa

**Q:** Volume mensal de internações ortopédicas de idosos (60 anos ou mais) em SP em 2023

**SQL gerado:**
```sql
SELECT
  mes_cmpt,
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND idade_paciente >= 60
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 5725ms

---

### ✅ Q51 — Epidemiológica Simples

**Q:** Número de internações por fratura de fêmur (S72) em mulheres em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
  AND sexo_paciente = 'F';
```

**Resultado:** `got=15639 exp=15639`
**TTA:** 4177ms

---

### ✅ Q52 — Epidemiológica Simples

**Q:** Número de internações por fratura de fêmur (S72) em homens em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
  AND sexo_paciente = 'M';
```

**Resultado:** `got=14633 exp=14633`
**TTA:** 4191ms

---

### ✅ Q53 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com permanência de 1 dia ou menos em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND dias_perm <= 1;
```

**Resultado:** `got=14718 exp=14718`
**TTA:** 4864ms

---

### ✅ Q54 — Epidemiológica Simples

**Q:** Número de procedimentos ambulatoriais ortopédicos realizados em mulheres em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(*) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND sexo_paciente = 'F'
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=47792 exp=47792`
**TTA:** 4648ms

---

### ✅ Q55 — Epidemiológica Simples

**Q:** Número de procedimentos ambulatoriais ortopédicos realizados por idosos (60 anos ou mais) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(*) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND idade_paciente >= 60
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=55711 exp=55711`
**TTA:** 4689ms

---

### ✅ Q56 — Epidemiológica Simples

**Q:** Número de internações por fratura de tíbia e perônio (S82) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S82%';
```

**Resultado:** `got=6827 exp=6827`
**TTA:** 3852ms

---

### ✅ Q57 — Epidemiológica Simples

**Q:** Número de internações por fratura de antebraço (S52) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S52%';
```

**Resultado:** `got=2264 exp=2264`
**TTA:** 3855ms

---

### ✅ Q58 — Epidemiológica Simples

**Q:** Número de internações por fratura de ombro e úmero (S42) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S42%';
```

**Resultado:** `got=1391 exp=1391`
**TTA:** 3847ms

---

### ✅ Q59 — Epidemiológica Simples

**Q:** Número de internações ortopédicas em crianças (menores de 18 anos) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND idade_paciente < 18
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=4040 exp=4040`
**TTA:** 4948ms

---

### ✅ Q60 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com permanência entre 8 e 30 dias em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND dias_perm BETWEEN 8 AND 30;
```

**Resultado:** `got=16748 exp=16748`
**TTA:** 5016ms

---

### ✅ Q61 — Epidemiológica Simples

**Q:** Número de internações por artrose de quadril e joelho (M16 e M17) em idosos (60 anos ou mais) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'M16%' OR cid_principal LIKE 'M17%')
  AND idade_paciente >= 60;
```

**Resultado:** `got=100 exp=100`
**TTA:** 4674ms

---

### ✅ Q62 — Epidemiológica Simples

**Q:** Número de internações por fratura de fêmur (S72) em mulheres com 70 anos ou mais em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
  AND sexo_paciente = 'F'
  AND idade_paciente >= 70;
```

**Resultado:** `got=11171 exp=11171`
**TTA:** 4595ms

---

### ✅ Q63 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com permanência superior a 30 dias em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND dias_perm > 30;
```

**Resultado:** `got=1037 exp=1037`
**TTA:** 4902ms

---

### ✅ Q64 — Epidemiológica Simples

**Q:** Número de procedimentos ambulatoriais ortopédicos realizados em homens em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(*) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND sexo_paciente = 'M'
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=57572 exp=57572`
**TTA:** 4678ms

---

### ✅ Q65 — Epidemiológica Simples

**Q:** Número de internações por traumatismos (grupo S00-T98) em idosos (60 anos ou mais) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND icd_group = 'S00-T98'
  AND idade_paciente >= 60;
```

**Resultado:** `got=26253 exp=26253`
**TTA:** 4386ms

---

### ✅ Q66 — Epidemiológica Simples

**Q:** Número de internações por doenças osteomusculares (grupo M00-M99) em mulheres em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND icd_group = 'M00-M99'
  AND sexo_paciente = 'F';
```

**Resultado:** `got=2554 exp=2554`
**TTA:** 4338ms

---

### ✅ Q67 — Epidemiológica Simples

**Q:** Número de óbitos em internações por fratura de fêmur (S72) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
  AND morte = 1;
```

**Resultado:** `got=1250 exp=1250`
**TTA:** 4104ms

---

### ❌ Q68 — Epidemiológica Simples

**Q:** Número de municípios distintos com registro de procedimentos ambulatoriais ortopédicos em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT cod_munic_residencia) AS total_municipios
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=641 exp=400`
**TTA:** 4659ms

---

### ❌ Q69 — Epidemiológica Complexa

**Q:** Top 5 municípios por volume de procedimentos ambulatoriais ortopédicos em SP em 2023

**SQL gerado:**
```sql
SELECT cod_munic_residencia,
  COUNT(*) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cod_munic_residencia
ORDER BY total_procedimentos DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cod_munic_residencia': '355030', 'total_procedimentos': 33126} exp={'cod_munic_estabelecimento': '355030', 'total': 35601}`
**TTA:** 5470ms

---

### ❌ Q70 — Epidemiológica Complexa

**Q:** Distribuição de procedimentos ambulatoriais ortopédicos por faixa etária em SP em 2023

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN idade_paciente < 18 THEN '0-17'
    WHEN idade_paciente < 30 THEN '18-29'
    WHEN idade_paciente < 40 THEN '30-39'
    WHEN idade_paciente < 50 THEN '40-49'
    WHEN idade_paciente < 60 THEN '50-59'
    WHEN idade_paciente < 70 THEN '60-69'
    ELSE '70+'
  END AS faixa_etaria,
  COUNT(*) AS total,
  ROUND(100.0 * COUNT(*) / SUM(COUNT(*)) OVER (), 1) AS pct
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY faixa_etaria
ORDER BY total DESC;
```

**Resultado:** `Número de linhas: got=7 exp=3`
**TTA:** 10542ms

---

### ❌ Q71 — Epidemiológica Complexa

**Q:** Top 5 diagnósticos com maior número de óbitos em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT 
  cid_principal,
  COUNT(*) AS total_obitos
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND morte = 1
GROUP BY 
  cid_principal
ORDER BY 
  total_obitos DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cid_principal': 'S720', 'total_obitos': 478} exp={'cid_principal': 'S720', 'total_internacoes': 9059, 'total_obitos': 478.0}`
**TTA:** 5788ms

---

### ✅ Q72 — Epidemiológica Complexa

**Q:** Permanência hospitalar média por sexo em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT 
  sexo_paciente,
  ROUND(AVG(dias_perm), 1) AS permanencia_media_dias
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY 
  sexo_paciente
ORDER BY 
  permanencia_media_dias DESC;
```

**Resultado:** `2 linhas corretas, ordem livre (flexível)`
**TTA:** 5860ms

---

### ✅ Q73 — Epidemiológica Complexa

**Q:** Top 10 estabelecimentos por volume de procedimentos ambulatoriais ortopédicos em SP em 2023

**SQL gerado:**
```sql
SELECT cnes_estabelecimento,
  COUNT(*) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cnes_estabelecimento
ORDER BY total_procedimentos DESC
LIMIT 10;
```

**Resultado:** `10 linhas corretas (flexível)`
**TTA:** 5500ms

---

### ✅ Q74 — Epidemiológica Complexa

**Q:** Taxa de mortalidade por sexo em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT 
  sexo_paciente,
  COUNT(CASE WHEN morte = 1 THEN n_aih END) AS total_obitos,
  COUNT(DISTINCT n_aih) AS total_internacoes,
  ROUND(100.0 * COUNT(CASE WHEN morte = 1 THEN n_aih END) / COUNT(DISTINCT n_aih), 1) AS taxa_mortalidade
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY 
  sexo_paciente
ORDER BY 
  total_internacoes DESC;
```

**Resultado:** `2 linhas corretas, ordem livre (exato)`
**TTA:** 8267ms

---

### ❌ Q75 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por grupo diagnóstico (M e S) e faixa etária em SP em 2023

**SQL gerado:**
```sql
SELECT 
  icd_group,
  CASE 
    WHEN idade_paciente < 18 THEN '0-17'
    WHEN idade_paciente < 30 THEN '18-29'
    WHEN idade_paciente < 40 THEN '30-39'
    WHEN idade_paciente < 50 THEN '40-49'
    WHEN idade_paciente < 60 THEN '50-59'
    WHEN idade_paciente < 70 THEN '60-69'
    WHEN idade_paciente < 80 THEN '70-79'
    ELSE '80+'
  END AS faixa_etaria,
  COUNT(DISTINCT n_aih) AS total_internacoes,
  ROUND(100.0 * COUNT(DISTINCT n_aih) / SUM(COUNT(DISTINCT n_aih)) OVER (), 1) AS pct
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY icd_group, faixa_etaria
ORDER BY icd_group, faixa_etaria;
```

**Resultado:** `Número de linhas: got=16 exp=6`
**TTA:** 12664ms

---

### ❌ Q76 — Epidemiológica Complexa

**Q:** Top 5 diagnósticos de traumatismo com maior permanência média em SP em 2023 (mínimo 40 internações)

**SQL gerado:**
```sql
SELECT
  cid_principal,
  AVG(dias_perm) AS permanencia_media_dias,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND icd_group = 'S00-T98'
GROUP BY cid_principal
HAVING COUNT(DISTINCT n_aih) >= 40
ORDER BY permanencia_media_dias DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cid_principal': 'S272', 'permanencia_media_dias': 14.2653061224, 'internacoes': 49} exp={'cid_principal': 'S272', 'internacoes': 49, 'permanencia_media': 14.3}`
**TTA:** 6002ms

---

### ❌ Q77 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por faixa etária (0-17, 18-39, 40-59, 60-79, 80+) em SP em 2023

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN idade_paciente BETWEEN 0 AND 17 THEN '0-17'
    WHEN idade_paciente BETWEEN 18 AND 39 THEN '18-39'
    WHEN idade_paciente BETWEEN 40 AND 59 THEN '40-59'
    WHEN idade_paciente BETWEEN 60 AND 79 THEN '60-79'
    ELSE '80+'
  END AS faixa_etaria,
  COUNT(DISTINCT n_aih) AS total_internacoes,
  ROUND(100.0 * COUNT(DISTINCT n_aih) / SUM(COUNT(DISTINCT n_aih)) OVER (), 1) AS pct
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY faixa_etaria
ORDER BY total_internacoes DESC;
```

**Resultado:** `Linha não encontrada: {'faixa_etaria': '0-17', 'total': 4040}`
**TTA:** 10726ms

---

### ✅ Q78 — Epidemiológica Complexa

**Q:** Top 5 estabelecimentos com maior volume de internações por fratura de fêmur (S72) em SP em 2023

**SQL gerado:**
```sql
SELECT cnes_estabelecimento,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
GROUP BY cnes_estabelecimento
ORDER BY internacoes DESC
LIMIT 5;
```

**Resultado:** `5 linhas corretas (exato)`
**TTA:** 4812ms

---

### ❌ Q79 — Epidemiológica Complexa

**Q:** Permanência hospitalar média por faixa etária em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN idade_paciente < 18 THEN '0-17'
    WHEN idade_paciente < 30 THEN '18-29'
    WHEN idade_paciente < 40 THEN '30-39'
    WHEN idade_paciente < 50 THEN '40-49'
    WHEN idade_paciente < 60 THEN '50-59'
    WHEN idade_paciente < 70 THEN '60-69'
    ELSE '70+'
  END AS faixa_etaria,
  ROUND(AVG(dias_perm), 1) AS permanencia_media_dias
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY faixa_etaria
ORDER BY faixa_etaria;
```

**Resultado:** `Número de linhas: got=7 exp=3`
**TTA:** 10099ms

---

### ✅ Q80 — Epidemiológica Complexa

**Q:** Top 5 diagnósticos de doença osteomuscular (M00-M99) por número de internações em SP em 2023

**SQL gerado:**
```sql
SELECT cid_principal,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND icd_group = 'M00-M99'
GROUP BY cid_principal
ORDER BY internacoes DESC
LIMIT 5;
```

**Resultado:** `5 linhas corretas (flexível)`
**TTA:** 4653ms

---

### ✅ Q81 — Epidemiológica Complexa

**Q:** Número de internações ortopédicas com permanência entre 3 e 7 dias em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND dias_perm BETWEEN 3 AND 7;
```

**Resultado:** `got=27009 exp=27009`
**TTA:** 4984ms

---

### ✅ Q82 — Epidemiológica Complexa

**Q:** Top 5 diagnósticos ambulatoriais ortopédicos em idosos (60 anos ou mais) em SP em 2023

**SQL gerado:**
```sql
SELECT cid_principal,
  COUNT(*) AS total
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND idade_paciente >= 60
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cid_principal
ORDER BY total DESC
LIMIT 5;
```

**Resultado:** `5 linhas corretas (exato)`
**TTA:** 5301ms

---

### ❌ Q83 — Epidemiológica Complexa

**Q:** Média de internações ortopédicas por estabelecimento em SP em 2023

**SQL gerado:**
```sql
SELECT
  cnes_estabelecimento,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cnes_estabelecimento
ORDER BY internacoes DESC;
```

**Resultado:** `got=2078015 exp=180.5`
**TTA:** 5463ms

---

### ❌ Q84 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por faixa de permanência em SP em 2023

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN dias_perm <= 5 THEN '0-5 dias'
    WHEN dias_perm <= 10 THEN '6-10 dias'
    WHEN dias_perm <= 15 THEN '11-15 dias'
    WHEN dias_perm <= 20 THEN '16-20 dias'
    WHEN dias_perm <= 25 THEN '21-25 dias'
    ELSE '26+ dias'
  END AS faixa_permanencia,
  COUNT(DISTINCT n_aih) AS total_internacoes,
  ROUND(100.0 * COUNT(DISTINCT n_aih) / SUM(COUNT(DISTINCT n_aih)) OVER (), 1) AS pct
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY faixa_permanencia
ORDER BY total_internacoes DESC;
```

**Resultado:** `Número de linhas: got=6 exp=4`
**TTA:** 10816ms

---

### ✅ Q85 — Financeira

**Q:** Custo total de internações ortopédicas de idosos (60 anos ou mais) em SP em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND idade_paciente >= 60;
```

**Resultado:** `got=83044181.8 exp=83044181.8`
**TTA:** 5280ms

---

### ✅ Q86 — Financeira

**Q:** Custo médio por internação por fratura de fêmur (S72) em SP em 2023

**SQL gerado:**
```sql
SELECT ROUND(AVG(COALESCE(custo_total, 0)), 2) AS custo_medio_por_internacao
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%';
```

**Resultado:** `got=2730.82 exp=2730.82`
**TTA:** 4367ms

---

### ✅ Q87 — Financeira

**Q:** Custo total de internações ortopédicas com passagem por UTI em SP em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND uti_int_to > 0;
```

**Resultado:** `got=6792.17 exp=6792.17`
**TTA:** 5259ms

---

### ✅ Q88 — Financeira

**Q:** Custo médio por internação ortopédica por sexo do paciente em SP em 2023

**SQL gerado:**
```sql
SELECT 
  sexo_paciente,
  ROUND(AVG(custo_total), 2) AS custo_medio_por_internacao
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY 
  sexo_paciente
ORDER BY 
  sexo_paciente;
```

**Resultado:** `2 linhas corretas, ordem livre (flexível)`
**TTA:** 5852ms

---

### ✅ Q89 — Financeira

**Q:** Custo total de internações ortopédicas em crianças (menores de 18 anos) em SP em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND idade_paciente < 18
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=7933442.51 exp=7933442.51`
**TTA:** 5258ms

---

### ✅ Q90 — Financeira

**Q:** Custo médio por internação ortopédica com permanência superior a 7 dias em SP em 2023

**SQL gerado:**
```sql
SELECT ROUND(AVG(COALESCE(custo_total, 0)), 2) AS custo_medio
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND dias_perm > 7;
```

**Resultado:** `got=4390.48 exp=4390.48`
**TTA:** 5224ms

---

### ✅ Q91 — Financeira

**Q:** Top 3 municípios com maior custo total em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT cod_munic_estabelecimento,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cod_munic_estabelecimento
ORDER BY custo_total DESC
LIMIT 3;
```

**Resultado:** `3 linhas corretas (exato)`
**TTA:** 6064ms

---

### ✅ Q92 — Financeira

**Q:** Valor total de serviço hospitalar em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(val_sh, 0)), 2) AS valor_total_servico_hospitalar
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=153782049.46 exp=153782049.46`
**TTA:** 4979ms

---

### ❌ Q93 — Financeira

**Q:** Custo médio por internação ortopédica por faixa etária em SP em 2023

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN idade_paciente < 18 THEN '0-17'
    WHEN idade_paciente < 30 THEN '18-29'
    WHEN idade_paciente < 40 THEN '30-39'
    WHEN idade_paciente < 50 THEN '40-49'
    WHEN idade_paciente < 60 THEN '50-59'
    WHEN idade_paciente < 70 THEN '60-69'
    ELSE '70+'
  END AS faixa_etaria,
  ROUND(AVG(COALESCE(custo_total, 0)), 2) AS custo_medio_por_internacao
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY faixa_etaria
ORDER BY faixa_etaria;
```

**Resultado:** `Número de linhas: got=7 exp=3`
**TTA:** 10521ms

---

### ✅ Q94 — Financeira

**Q:** Valor total de UTI em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(val_uti, 0)), 2) AS total_uti
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=40878597.03 exp=40878597.03`
**TTA:** 4856ms

---

### ✅ Q95 — Financeira

**Q:** Custo total de internações por doenças osteomusculares (M00-M99) em SP em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND icd_group = 'M00-M99';
```

**Resultado:** `got=15790831.06 exp=15790831.06`
**TTA:** 4285ms

---

### ✅ Q96 — Financeira

**Q:** Custo total de internações por traumatismos (S00-T98) em SP em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND icd_group = 'S00-T98';
```

**Resultado:** `got=171429990.73 exp=171429990.73`
**TTA:** 4289ms

---

### ✅ Q97 — Financeira

**Q:** Top 5 diagnósticos com maior custo médio por internação ortopédica em SP em 2023 (mínimo 40 internações)

**SQL gerado:**
```sql
SELECT
  cid_principal,
  COUNT(DISTINCT n_aih) AS internacoes,
  ROUND(AVG(custo_total), 2) AS custo_medio
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cid_principal
HAVING internacoes >= 40
ORDER BY custo_medio DESC
LIMIT 5;
```

**Resultado:** `5 linhas corretas (exato)`
**TTA:** 6467ms

---

### ✅ Q98 — Financeira

**Q:** Custo médio por procedimento ambulatorial ortopédico em SP em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)) / COUNT(*), 2) AS custo_medio
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=86.29 exp=86.29`
**TTA:** 5061ms

---

### ❌ Q99 — Financeira

**Q:** Top 5 procedimentos ambulatoriais com maior custo total em SP em 2023

**SQL gerado:**
```sql
SELECT cod_procedimento,
  COUNT(*) AS total_procedimentos,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
GROUP BY cod_procedimento
ORDER BY custo_total DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cod_procedimento': '0702100048', 'total_procedimentos': 15268, 'custo_total': 45189222.96} exp={'cod_procedimento': '0701020369', 'custo_total': 1261008.0}`
**TTA:** 5052ms

---

### ✅ Q100 — Financeira

**Q:** Custo total de internações ortopédicas de idosos (60+) com permanência superior a 7 dias em SP em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND idade_paciente >= 60
  AND dias_perm > 7;
```

**Resultado:** `got=37989386.14 exp=37989386.14`
**TTA:** 5599ms

---

### ✅ Q101 — Temporal/Comparativa

**Q:** Mês com maior número de óbitos em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT 
  mes_cmpt,
  COUNT(DISTINCT n_aih) AS total_obitos
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND morte = 1
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY 
  mes_cmpt
ORDER BY 
  total_obitos DESC
LIMIT 1;
```

**Resultado:** `1 linhas corretas (exato)`
**TTA:** 6131ms

---

### ✅ Q102 — Temporal/Comparativa

**Q:** Volume mensal de internações por fratura de fêmur (S72) em idosos (60 anos ou mais) em SP em 2023

**SQL gerado:**
```sql
SELECT 
  mes_cmpt,
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
  AND idade_paciente >= 60
GROUP BY 
  mes_cmpt
ORDER BY 
  mes_cmpt;
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 5340ms

---

### ✅ Q103 — Temporal/Comparativa

**Q:** Custo mensal de internações ortopédicas de idosos (60 anos ou mais) em SP em 2023

**SQL gerado:**
```sql
SELECT 
  mes_cmpt,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND idade_paciente >= 60
GROUP BY 
  mes_cmpt
ORDER BY 
  mes_cmpt;
```

**Resultado:** `12 linhas corretas (exato)`
**TTA:** 6384ms

---

### ✅ Q104 — Temporal/Comparativa

**Q:** Mês com maior custo total de internações por fratura de fêmur (S72) em SP em 2023

**SQL gerado:**
```sql
SELECT mes_cmpt,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
GROUP BY mes_cmpt
ORDER BY custo_total DESC
LIMIT 1;
```

**Resultado:** `1 linhas corretas (exato)`
**TTA:** 5025ms

---

### ✅ Q105 — Temporal/Comparativa

**Q:** Volume mensal de internações por traumatismos (grupo S00-T98) em SP em 2023

**SQL gerado:**
```sql
SELECT
  mes_cmpt,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND icd_group = 'S00-T98'
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Resultado:** `12 linhas corretas (exato)`
**TTA:** 4680ms

---

### ❌ Q106 — Temporal/Comparativa

**Q:** Volume mensal de procedimentos ambulatoriais ortopédicos por sexo em SP em 2023

**SQL gerado:**
```sql
SELECT
  mes_cmpt,
  sexo_paciente,
  COUNT(*) AS total_procedimentos,
  ROUND(100.0 * COUNT(*) / SUM(COUNT(*)) OVER (PARTITION BY mes_cmpt), 1) AS pct
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY mes_cmpt, sexo_paciente
ORDER BY mes_cmpt, sexo_paciente;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'sexo_paciente': 'F', 'total_procedimentos': 3733, 'pct': 44.4} exp={'mes_cmpt': 1, 'sexo_paciente': 'F', 'total': 3733}`
**TTA:** 7352ms

---

### ✅ Q107 — Temporal/Comparativa

**Q:** Comparativo trimestral de internações ortopédicas por grupo diagnóstico em SP em 2023

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN mes_cmpt BETWEEN 1 AND 3 THEN 'Q1'
    WHEN mes_cmpt BETWEEN 4 AND 6 THEN 'Q2'
    WHEN mes_cmpt BETWEEN 7 AND 9 THEN 'Q3'
    ELSE 'Q4'
  END AS trimestre,
  icd_group,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY trimestre, icd_group
ORDER BY trimestre, icd_group;
```

**Resultado:** `8 linhas corretas, ordem livre (flexível)`
**TTA:** 8025ms

---

### ✅ Q108 — Temporal/Comparativa

**Q:** Mês com maior permanência hospitalar média em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT mes_cmpt,
  ROUND(AVG(dias_perm), 1) AS permanencia_media_dias
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY mes_cmpt
ORDER BY permanencia_media_dias DESC
LIMIT 1;
```

**Resultado:** `1 linhas corretas (flexível)`
**TTA:** 5636ms

---

### ❌ Q109 — Temporal/Comparativa

**Q:** Evolução mensal da taxa de mortalidade em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT
  mes_cmpt,
  ROUND(100.0 * SUM(CASE WHEN morte = 1 THEN 1 ELSE 0 END) / COUNT(n_aih), 2) AS taxa_mortalidade
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'taxa_mortalidade': 3.49} exp={'mes_cmpt': 1, 'internacoes': 5667, 'obitos': 198.0, 'taxa_mortalidade': 3.49}`
**TTA:** 6359ms

---

### ✅ Q110 — Temporal/Comparativa

**Q:** Trimestre com maior volume de internações por fratura de fêmur (S72) em SP em 2023

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN mes_cmpt BETWEEN 1 AND 3 THEN 'Q1'
    WHEN mes_cmpt BETWEEN 4 AND 6 THEN 'Q2'
    WHEN mes_cmpt BETWEEN 7 AND 9 THEN 'Q3'
    ELSE 'Q4'
  END AS trimestre,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
GROUP BY trimestre
ORDER BY internacoes DESC
LIMIT 1;
```

**Resultado:** `1 linhas corretas (flexível)`
**TTA:** 6959ms

---

### ❌ Q111 — Temporal/Comparativa

**Q:** Comparativo semestral de internações ortopédicas (1º vs 2º semestre) em SP em 2023

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN mes_cmpt BETWEEN 1 AND 6 THEN '1º Semestre'
    ELSE '2º Semestre'
  END AS semestre,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY semestre
ORDER BY semestre;
```

**Resultado:** `Linha não encontrada: {'semestre': '1S', 'total': 33739}`
**TTA:** 6363ms

---

### ✅ Q112 — Temporal/Comparativa

**Q:** Custo médio mensal por internação por fratura de fêmur (S72) em SP em 2023

**SQL gerado:**
```sql
SELECT 
  mes_cmpt,
  ROUND(AVG(custo_total), 2) AS custo_medio_mensal
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 4787ms

---

### ✅ Q113 — Temporal/Comparativa

**Q:** Volume mensal de internações ortopédicas por sexo do paciente em SP em 2023

**SQL gerado:**
```sql
SELECT 
  mes_cmpt,
  sexo_paciente,
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY 
  mes_cmpt, sexo_paciente
ORDER BY 
  mes_cmpt, sexo_paciente;
```

**Resultado:** `24 linhas corretas (flexível)`
**TTA:** 6158ms

---

### ✅ Q114 — Temporal/Comparativa

**Q:** Mês com menor volume de procedimentos ambulatoriais ortopédicos em SP em 2023

**SQL gerado:**
```sql
SELECT mes_cmpt,
  COUNT(*) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY mes_cmpt
ORDER BY total_procedimentos ASC
LIMIT 1;
```

**Resultado:** `1 linhas corretas (flexível)`
**TTA:** 5227ms

---

### ✅ Q115 — Temporal/Comparativa

**Q:** Volume trimestral de procedimentos ambulatoriais ortopédicos em SP em 2023

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN mes_cmpt BETWEEN 1 AND 3 THEN 'Q1'
    WHEN mes_cmpt BETWEEN 4 AND 6 THEN 'Q2'
    WHEN mes_cmpt BETWEEN 7 AND 9 THEN 'Q3'
    ELSE 'Q4'
  END AS trimestre,
  COUNT(*) AS volume_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY trimestre
ORDER BY trimestre;
```

**Resultado:** `4 linhas corretas, ordem livre (flexível)`
**TTA:** 7319ms

---

### ✅ Q116 — Temporal/Comparativa

**Q:** Custo mensal de procedimentos ambulatoriais ortopédicos em SP em 2023

**SQL gerado:**
```sql
SELECT
  mes_cmpt,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Resultado:** `12 linhas corretas (exato)`
**TTA:** 5644ms

---

### ❌ Q117 — Temporal/Comparativa

**Q:** Comparativo semestral do custo total de internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN mes_cmpt BETWEEN 1 AND 6 THEN 'Semestre 1'
    ELSE 'Semestre 2'
  END AS semestre,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY semestre
ORDER BY semestre;
```

**Resultado:** `Linha não encontrada: {'semestre': '1S', 'custo_total': 90189950.58}`
**TTA:** 6771ms

---

### ✅ Q118 — Temporal/Comparativa

**Q:** Evolução mensal da permanência média em internações por fratura de fêmur (S72) em SP em 2023

**SQL gerado:**
```sql
SELECT
  mes_cmpt,
  ROUND(AVG(dias_perm), 1) AS permanencia_media_dias
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 4707ms

---

## Failures Detail

- **Q18** (Epidemiológica Complexa): Linha não encontrada: {'faixa_etaria': '0-17', 'total': 4040}
  SQL: `SELECT
  CASE
    WHEN idade_paciente BETWEEN 0 AND 17 THEN '0-17'
    WHEN idade_paciente BETWEEN 18 AND 59 THEN '18-59...`
- **Q19** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'T274', 'taxa_mortalidade': 100.0} exp={'cid_principal': 'S062', 'total_internacoes': 58, 'total_obitos': 20.0, 'taxa_mortalidade_pct': 34.48}
  SQL: `SELECT 
  cid_principal,
  ROUND(100.0 * SUM(CASE WHEN morte = 1 THEN 1 ELSE 0 END) / COUNT(*), 2) AS taxa_mortalidade
F...`
- **Q22** (Epidemiológica Complexa): Linha 1: got={'internacoes_idosos': 19589, 'total_internacoes': 30272} exp={'total_s72': 30272, 's72_idosos': 19598.0, 'pct_idosos': 64.7}
  SQL: `SELECT 
  COUNT(DISTINCT CASE WHEN idade_paciente >= 60 THEN n_aih END) AS internacoes_idosos,
  COUNT(DISTINCT n_aih) A...`
- **Q23** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'S141', 'permanencia_media_dias': 73.0} exp={'cid_principal': 'S272', 'total_internacoes': 49, 'permanencia_media_dias': 14.3}
  SQL: `SELECT
  cid_principal,
  ROUND(AVG(dias_perm), 1) AS permanencia_media_dias
FROM processed
WHERE sistema = 'SIH'
  AND ...`
- **Q26** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'S720', 'total_dias_internacao': 65515.0} exp={'cid_principal': 'S720', 'internacoes': 9059, 'total_dias_internacao': 65515.0}
  SQL: `SELECT cid_principal,
  SUM(dias_perm) AS total_dias_internacao
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = '...`
- **Q28** (Epidemiológica Complexa): Linha 1: got={'proporcao': 57.0} exp={'total_internacoes': 69487, 'deslocamento_intermunicipal': 39611.0, 'pct_deslocamento': 57.0}
  SQL: `SELECT 
  ROUND(100.0 * COUNT(CASE WHEN cod_munic_residencia != cod_munic_estabelecimento THEN n_aih END) / COUNT(DISTIN...`
- **Q29** (Epidemiológica Complexa): Linha 10: got={'mes_cmpt': 5, 'sistema': 'SIH', 'volume': 5918} exp={'mes_cmpt': 5, 'sistema': 'SIH', 'total': 5917}
  SQL: `SELECT
  mes_cmpt,
  sistema,
  COUNT(*) AS volume
FROM processed
WHERE uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (ic...`
- **Q30** (Epidemiológica Complexa): Linha 1: got={'idade_media': 62.4} exp={'idade_media': 62.4, 'idade_minima': 0.0, 'idade_maxima': 99.0}
  SQL: `SELECT ROUND(AVG(idade_paciente), 1) AS idade_media
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND an...`
- **Q34** (Financeira): Linha 1: got={'cid_principal': 'S720', 'custo_total': 30400546.73} exp={'cid_principal': 'S720', 'internacoes': 9059, 'custo_total': 30400546.73}
  SQL: `SELECT cid_principal,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AN...`
- **Q35** (Financeira): Linha 1: got={'custo_total': None} exp={'internacoes_com_opme': 0, 'custo_total_opme': None}
  SQL: `SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP...`
- **Q37** (Financeira): got=487.64 exp=441.21
  SQL: `SELECT ROUND(SUM(COALESCE(custo_total, 0)) / SUM(COALESCE(qt_diarias, 0)), 2) AS custo_medio_por_dia
FROM processed
WHER...`
- **Q38** (Financeira): Linha 1: got={'cnes_estabelecimento': '2078015', 'custo_total': 11295149.42} exp={'cnes_estabelecimento': '2078015', 'internacoes': 2257, 'custo_total': 11295149.42}
  SQL: `SELECT cnes_estabelecimento,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SI...`
- **Q40** (Financeira): Linha 1: got={'pct_val_sh': 82.14, 'pct_val_sp': 17.52} exp={'custo_servico_hospitalar': 153782049.46, 'honorarios_profissionais': 32810269.18, 'pct_hospitalar': 82.4}
  SQL: `SELECT
  ROUND(100.0 * SUM(COALESCE(val_sh, 0)) / SUM(COALESCE(custo_total, 0)), 2) AS pct_val_sh,
  ROUND(100.0 * SUM(C...`
- **Q41** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'internacoes': 5667, 'pct': 8.2} exp={'mes_cmpt': 1, 'total_internacoes': 5667}
  SQL: `SELECT
  mes_cmpt,
  COUNT(DISTINCT n_aih) AS internacoes,
  ROUND(100.0 * COUNT(DISTINCT n_aih) / SUM(COUNT(DISTINCT n_...`
- **Q42** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'total_procedimentos': 8415, 'pct': 8.0} exp={'mes_cmpt': 1, 'procedimentos': 8415}
  SQL: `SELECT 
  mes_cmpt,
  COUNT(*) AS total_procedimentos,
  ROUND(100.0 * COUNT(*) / SUM(COUNT(*)) OVER (), 1) AS pct
FROM ...`
- **Q45** (Temporal/Comparativa): Número de linhas: got=12 exp=24
  SQL: `SELECT
  mes_cmpt,
  COUNT(CASE WHEN icd_group = 'S00-T98' THEN n_aih END) AS internacoes_trauma,
  COUNT(CASE WHEN icd_...`
- **Q68** (Epidemiológica Simples): got=641 exp=400
  SQL: `SELECT COUNT(DISTINCT cod_munic_residencia) AS total_municipios
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = '...`
- **Q69** (Epidemiológica Complexa): Linha 1: got={'cod_munic_residencia': '355030', 'total_procedimentos': 33126} exp={'cod_munic_estabelecimento': '355030', 'total': 35601}
  SQL: `SELECT cod_munic_residencia,
  COUNT(*) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'S...`
- **Q70** (Epidemiológica Complexa): Número de linhas: got=7 exp=3
  SQL: `SELECT
  CASE
    WHEN idade_paciente < 18 THEN '0-17'
    WHEN idade_paciente < 30 THEN '18-29'
    WHEN idade_paciente...`
- **Q71** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'S720', 'total_obitos': 478} exp={'cid_principal': 'S720', 'total_internacoes': 9059, 'total_obitos': 478.0}
  SQL: `SELECT 
  cid_principal,
  COUNT(*) AS total_obitos
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'SP'
  ...`
- **Q75** (Epidemiológica Complexa): Número de linhas: got=16 exp=6
  SQL: `SELECT 
  icd_group,
  CASE 
    WHEN idade_paciente < 18 THEN '0-17'
    WHEN idade_paciente < 30 THEN '18-29'
    WHEN...`
- **Q76** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'S272', 'permanencia_media_dias': 14.2653061224, 'internacoes': 49} exp={'cid_principal': 'S272', 'internacoes': 49, 'permanencia_media': 14.3}
  SQL: `SELECT
  cid_principal,
  AVG(dias_perm) AS permanencia_media_dias,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processe...`
- **Q77** (Epidemiológica Complexa): Linha não encontrada: {'faixa_etaria': '0-17', 'total': 4040}
  SQL: `SELECT
  CASE
    WHEN idade_paciente BETWEEN 0 AND 17 THEN '0-17'
    WHEN idade_paciente BETWEEN 18 AND 39 THEN '18-39...`
- **Q79** (Epidemiológica Complexa): Número de linhas: got=7 exp=3
  SQL: `SELECT
  CASE
    WHEN idade_paciente < 18 THEN '0-17'
    WHEN idade_paciente < 30 THEN '18-29'
    WHEN idade_paciente...`
- **Q83** (Epidemiológica Complexa): got=2078015 exp=180.5
  SQL: `SELECT
  cnes_estabelecimento,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_orig...`
- **Q84** (Epidemiológica Complexa): Número de linhas: got=6 exp=4
  SQL: `SELECT
  CASE
    WHEN dias_perm <= 5 THEN '0-5 dias'
    WHEN dias_perm <= 10 THEN '6-10 dias'
    WHEN dias_perm <= 15...`
- **Q93** (Financeira): Número de linhas: got=7 exp=3
  SQL: `SELECT
  CASE
    WHEN idade_paciente < 18 THEN '0-17'
    WHEN idade_paciente < 30 THEN '18-29'
    WHEN idade_paciente...`
- **Q99** (Financeira): Linha 1: got={'cod_procedimento': '0702100048', 'total_procedimentos': 15268, 'custo_total': 45189222.96} exp={'cod_procedimento': '0701020369', 'custo_total': 1261008.0}
  SQL: `SELECT cod_procedimento,
  COUNT(*) AS total_procedimentos,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FRO...`
- **Q106** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'sexo_paciente': 'F', 'total_procedimentos': 3733, 'pct': 44.4} exp={'mes_cmpt': 1, 'sexo_paciente': 'F', 'total': 3733}
  SQL: `SELECT
  mes_cmpt,
  sexo_paciente,
  COUNT(*) AS total_procedimentos,
  ROUND(100.0 * COUNT(*) / SUM(COUNT(*)) OVER (PA...`
- **Q109** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'taxa_mortalidade': 3.49} exp={'mes_cmpt': 1, 'internacoes': 5667, 'obitos': 198.0, 'taxa_mortalidade': 3.49}
  SQL: `SELECT
  mes_cmpt,
  ROUND(100.0 * SUM(CASE WHEN morte = 1 THEN 1 ELSE 0 END) / COUNT(n_aih), 2) AS taxa_mortalidade
FRO...`
- **Q111** (Temporal/Comparativa): Linha não encontrada: {'semestre': '1S', 'total': 33739}
  SQL: `SELECT
  CASE
    WHEN mes_cmpt BETWEEN 1 AND 6 THEN '1º Semestre'
    ELSE '2º Semestre'
  END AS semestre,
  COUNT(DIS...`
- **Q117** (Temporal/Comparativa): Linha não encontrada: {'semestre': '1S', 'custo_total': 90189950.58}
  SQL: `SELECT
  CASE
    WHEN mes_cmpt BETWEEN 1 AND 6 THEN 'Semestre 1'
    ELSE 'Semestre 2'
  END AS semestre,
  ROUND(SUM(C...`

---
_Gerado por scripts/evaluate_benchmark.py — SUS Data RAG — USF/Mestrado_