# Benchmark Evaluation Report — SUS Data RAG

**Model:** ollama  
**Run date:** 2026-08-22 21:50  
**API:** http://127.0.0.1:8000  

## Summary

| Metric | Value |
|---|---|
| Total queries | 118 |
| Correct (EA numerator) | 17 |
| **Execution Accuracy (EA)** | **14.4%** |
| Wilson IC 95% | [9.2%, 21.9%] |
| Correct via flexible match (alias diferente) | 15 |
| API errors / timeouts | 1 |
| Mean Time-to-Answer | 6.1s |

> **Nota metodológica — Scoring:** A Execution Accuracy avalia se o SQL gerado
> retorna os mesmos **valores** que o gold-standard, independente do nome das colunas.
> Quando o LLM usa um alias diferente mas retorna os dados corretos (match posicional),
> a query é contada como correta e sinalizada com `~`. Esta decisão segue a definição
> de EA em [Lee et al., 2022] — comparação de conjuntos de resultado, não de SQL texto.

## EA by Category

| Category | Correct | Total | EA |
|---|---:|---:|---:|
| Epidemiológica Simples | 8 | 33 | 24.2% |
| Epidemiológica Complexa | 1 | 31 | 3.2% |
| Financeira | 4 | 26 | 15.4% |
| Temporal/Comparativa | 4 | 28 | 14.3% |

## Query Results

### ✅ Q01 — Epidemiológica Simples

**Q:** Total de internações ortopédicas (M00-M99 e S00-S99) em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=65970 exp=65970`
**TTA:** 4450ms

---

### ❌ Q02 — Epidemiológica Simples

**Q:** Total de procedimentos ambulatoriais ortopédicos em SP em 2022

**SQL gerado:**
```sql
SELECT SUM(pa_qtdapr) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND cod_procedimento LIKE '0408%';
```

**Resultado:** `got=None exp=105016`
**TTA:** 3822ms

---

### ❌ Q03 — Epidemiológica Simples

**Q:** Total de internações por fratura de fêmur (S72) em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'S72%' OR e.clinical_interpretacao_clinica = 'lesoes e causas traumaticas');
```

**Resultado:** `got=59668 exp=29356`
**TTA:** 5526ms

---

### ✅ Q04 — Epidemiológica Simples

**Q:** Total de internações por osteoartrose (M16, M17) em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M16%' OR cid_principal LIKE 'M17%');
```

**Resultado:** `got=156 exp=156`
**TTA:** 5057ms

---

### ❌ Q05 — Epidemiológica Simples

**Q:** Número de óbitos em internações ortopédicas em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_obitos
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND morte = 1
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=73 exp=2223`
**TTA:** 5362ms

---

### ✅ Q06 — Epidemiológica Simples

**Q:** Total de internações por traumatismos (S00-T98) em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'S%' OR icd_group = 'S00-T98');
```

**Resultado:** `got=59668 exp=59668`
**TTA:** 5106ms

---

### ✅ Q07 — Epidemiológica Simples

**Q:** Total de internações por doenças osteomusculares (M00-M99) em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND e.clinical_interpretacao_clinica = 'doencas osteomusculares e do tecido conjuntivo';
```

**Resultado:** `got=6302 exp=6302`
**TTA:** 5459ms

---

### ❌ Q08 — Epidemiológica Simples

**Q:** Número de mulheres internadas por causa ortopédica em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS num_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND sexo_paciente = 'F'
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=2327 exp=25847`
**TTA:** 5459ms

---

### ❌ Q09 — Epidemiológica Simples

**Q:** Número de homens internados por causa ortopédica em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND sexo_paciente = 'M'
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=3975 exp=40123`
**TTA:** 5457ms

---

### ❌ Q10 — Epidemiológica Simples

**Q:** Número de internações ortopédicas de idosos (60 anos ou mais) em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND idade_paciente >= 60
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=1685 exp=26666`
**TTA:** 5490ms

---

### ❌ Q11 — Epidemiológica Simples

**Q:** Número de municípios distintos com internação ortopédica registrada em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT cod_munic_residencia) AS num_municipios_distintos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=496 exp=211`
**TTA:** 4446ms

---

### ❌ Q12 — Epidemiológica Simples

**Q:** Número de estabelecimentos distintos com internação ortopédica em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT cod_munic_estabelecimento) AS num_estabelecimentos
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=158 exp=381`
**TTA:** 5288ms

---

### ❌ Q13 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com permanência superior a 7 dias em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
  AND dias_perm > 7;
```

**Resultado:** `got=1383 exp=16451`
**TTA:** 5418ms

---

### ❌ Q14 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com uso de UTI em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS num_internacoes_uti
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
  AND uti_int_to > 0;
```

**Resultado:** `got=0 exp=3`
**TTA:** 5257ms

---

### ❌ Q15 — Epidemiológica Simples

**Q:** Número de internações por fratura de quadril (S72) em pacientes com 70 anos ou mais em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND idade_paciente >= 70
  AND (cid_principal LIKE 'S72%' OR e.clinical_interpretacao_clinica = 'lesoes e causas traumaticas');
```

**Resultado:** `got=17608 exp=15119`
**TTA:** 5917ms

---

### ❌ Q16 — Epidemiológica Complexa

**Q:** Top 10 CIDs ortopédicos por volume de internação em SP em 2022

**SQL gerado:**
```sql
SELECT 
    cid_principal, 
    COUNT(DISTINCT n_aih) AS internacoes_unicas
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2022 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cid_principal
ORDER BY 
    internacoes_unicas DESC
LIMIT 10;
```

**Resultado:** `Linha 1: got={'cid_principal': 'M861', 'internacoes_unicas': 912} exp={'cid_principal': 'S720', 'total': 8980}`
**TTA:** 6652ms

---

### ❌ Q17 — Epidemiológica Complexa

**Q:** Top 5 municípios de SP com maior número de internações ortopédicas em 2022

**SQL gerado:**
```sql
SELECT 
    cod_munic_residencia, 
    COUNT(DISTINCT n_aih) AS num_internacoes
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2022 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cod_munic_residencia
ORDER BY 
    num_internacoes DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cod_munic_residencia': '355030', 'num_internacoes': 1190} exp={'cod_munic_estabelecimento': '350000', 'total': 29824}`
**TTA:** 6831ms

---

### ❌ Q18 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por faixa etária (0-17, 18-59, 60+) em SP em 2022

**SQL gerado:**
```sql
SELECT 
    CASE 
        WHEN idade_paciente BETWEEN 0 AND 17 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 59 THEN '18-59'
        WHEN idade_paciente >= 60 THEN '60+'
    END AS faixa_etaria,
    COUNT(DISTINCT n_aih) AS quantidade_internacoes
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    faixa_etaria
ORDER BY 
    faixa_etaria;
```

**Resultado:** `Linha não encontrada: {'faixa_etaria': '0-17', 'total': 3619}`
**TTA:** 8738ms

---

### ❌ Q19 — Epidemiológica Complexa

**Q:** Taxa de mortalidade hospitalar por CID ortopédico (top 5) em SP em 2022

**SQL gerado:**
```sql
SELECT 
    cid_principal,
    COUNT(DISTINCT n_aih) AS total_internacoes,
    SUM(morte) AS total_obitos,
    (SUM(morte) * 1.0 / COUNT(DISTINCT n_aih)) * 100 AS taxa_mortalidade
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cid_principal
ORDER BY 
    taxa_mortalidade DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cid_principal': 'M895', 'total_internacoes': 2, 'total_obitos': 1.0, 'taxa_mortalidade': 50.0} exp={'cid_principal': 'S062', 'total_internacoes': 70, 'total_obitos': 21, 'taxa_mortalidade_pct': 30.0}`
**TTA:** 8015ms

---

### ❌ Q20 — Epidemiológica Complexa

**Q:** Top 10 CIDs ambulatoriais ortopédicos por volume de produção em SP em 2022

**SQL gerado:**
```sql
SELECT 
    cid_principal, 
    COUNT(*) AS volume_producao
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIA' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2022 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cid_principal
ORDER BY 
    volume_producao DESC
LIMIT 10;
```

**Resultado:** `Linha 1: got={'cid_principal': 'M216', 'volume_producao': 4743} exp={'cid_principal': 'S720', 'total': 16466}`
**TTA:** 6310ms

---

### ❌ Q21 — Epidemiológica Complexa

**Q:** Proporção de internações ortopédicas por sexo em SP em 2022

**SQL gerado:**
```sql
SELECT 
    sexo_paciente,
    COUNT(DISTINCT n_aih) AS internacoes,
    COUNT(DISTINCT n_aih) * 1.0 / SUM(COUNT(DISTINCT n_aih)) OVER () AS proporcao
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    sexo_paciente;
```

**Resultado:** `Linha não encontrada: {'sexo_paciente': 'M', 'total': 40123, 'pct': 60.8}`
**TTA:** 6108ms

---

### ❌ Q22 — Epidemiológica Complexa

**Q:** Número de internações por fratura de fêmur (S72) em idosos (60+) comparado ao total em SP em 2022

**SQL gerado:**
```sql
WITH internacoes_femur AS (
    SELECT
        COUNT(DISTINCT n_aih) AS num_internacoes_femur
    FROM
        processed
    JOIN
        enriched e ON processed.row_id = e.row_id
    WHERE
        sistema = 'SIH'
        AND uf_origem = 'SP'
        AND ano_cmpt = 2022
        AND idade_paciente >= 60
        AND (cid_principal LIKE 'S72%' OR e.clinical_interpretacao_clinica = 'lesoes e causas traumaticas')
),
total_internacoes AS (
    SELECT
        COUNT(DISTINCT n_aih) AS num_internacoes_total
    FROM
        processed
    WHERE
        sistema = 'SIH'
        AND uf_origem = 'SP'
        AND ano_cmpt = 2022
)
SELECT
    num_internacoes_femur,
    num_internacoes_total,
    (num_internacoes_femur * 1.0 / num_internacoes_total) * 100 AS percentual_femur
FROM
    internacoes_femur,
    total_internacoes;
```

**Resultado:** `Linha 1: got={'num_internacoes_femur': 24981, 'num_internacoes_total': 195574, 'percentual_femur': 12.7731702578} exp={'total_s72': 29356, 's72_idosos': 19120, 'pct_idosos': 65.1}`
**TTA:** 12431ms

---

### ❌ Q23 — Epidemiológica Complexa

**Q:** Top 5 CIDs ortopédicos com maior permanência média hospitalar em SP em 2022

**SQL gerado:**
```sql
SELECT 
    cid_principal, 
    AVG(dias_perm) AS media_permanencia
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2022 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cid_principal
ORDER BY 
    media_permanencia DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cid_principal': 'M318', 'media_permanencia': 36.0} exp={'cid_principal': 'S063', 'total_internacoes': 78, 'permanencia_media_dias': 16.4}`
**TTA:** 6413ms

---

### ❌ Q24 — Epidemiológica Complexa

**Q:** Top 5 estabelecimentos com maior volume de internação ortopédica em SP em 2022

**SQL gerado:**
```sql
SELECT 
    cod_munic_estabelecimento, 
    COUNT(DISTINCT n_aih) AS volume_internacao
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2022 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cod_munic_estabelecimento
ORDER BY 
    volume_internacao DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cod_munic_estabelecimento': '350000', 'volume_internacao': 2800} exp={'cnes_estabelecimento': '2078015', 'total_internacoes': 2221}`
**TTA:** 6912ms

---

### ❌ Q25 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por raça/cor do paciente em SP em 2022

**SQL gerado:**
```sql
SELECT 
    raca_cor_paciente,
    COUNT(DISTINCT n_aih) AS internacoes_ortopédicas
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    raca_cor_paciente;
```

**Resultado:** `Linha não encontrada: {'raca_cor_paciente': '01', 'total': 38121, 'pct': 57.8}`
**TTA:** 6208ms

---

### ❌ Q26 — Epidemiológica Complexa

**Q:** CIDs ortopédicos com maior número de dias totais de internação em SP em 2022

**SQL gerado:**
```sql
SELECT 
    cid_principal, 
    SUM(qt_diarias) AS total_dias_internacao
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2022 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cid_principal
ORDER BY 
    total_dias_internacao DESC
LIMIT 1;
```

**Resultado:** `Número de linhas: got=1 exp=10`
**TTA:** 6607ms

---

### ❌ Q27 — Epidemiológica Complexa

**Q:** Top 5 procedimentos ambulatoriais ortopédicos mais realizados em SP em 2022

**SQL gerado:**
```sql
SELECT 
    cod_procedimento, 
    COUNT(*) AS quantidade_realizada
FROM 
    processed
WHERE 
    sistema = 'SIA' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2022
    AND cod_procedimento LIKE '0408%'
GROUP BY 
    cod_procedimento
ORDER BY 
    quantidade_realizada DESC
LIMIT 5;
```

**Resultado:** `Número de linhas: got=0 exp=5`
**TTA:** 5032ms

---

### ❌ Q28 — Epidemiológica Complexa

**Q:** Proporção de internações ortopédicas com paciente de outro município em SP em 2022

**SQL gerado:**
```sql
WITH internacoes_ortopédicas AS (
    SELECT
        p.n_aih,
        p.cod_munic_residencia,
        p.cod_munic_estabelecimento
    FROM
        processed p
    JOIN
        enriched e ON p.row_id = e.row_id
    WHERE
        p.sistema = 'SIH'
        AND p.uf_origem = 'SP'
        AND p.ano_cmpt = 2022
        AND (p.cid_principal LIKE 'M%' OR p.icd_group = 'M00-M99')
        AND p.cod_munic_residencia <> p.cod_munic_estabelecimento
)
SELECT
    COUNT(DISTINCT n_aih) AS total_internacoes,
    COUNT(DISTINCT CASE WHEN cod_munic_residencia <> cod_munic_estabelecimento THEN n_aih END) AS internacoes_outro_municipio,
    (COUNT(DISTINCT CASE WHEN cod_munic_residencia <> cod_munic_estabelecimento THEN n_aih END) * 1.0 / COUNT(DISTINCT n_aih)) AS proporcao
FROM
    internacoes_ortopédicas;
```

**Resultado:** `Linha 1: got={'total_internacoes': 3412, 'internacoes_outro_municipio': 3412, 'proporcao': 1.0} exp={'total_internacoes': 65970, 'deslocamento_intermunicipal': 37219, 'pct_deslocamento': 56.4}`
**TTA:** 11778ms

---

### ❌ Q29 — Epidemiológica Complexa

**Q:** Comparativo de volume mensal SIA vs SIH ortopédico em SP em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    sistema,
    COUNT(*) AS volume_mensal
FROM 
    processed
WHERE 
    uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt, sistema
ORDER BY 
    mes_cmpt, sistema;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'sistema': 'SIA', 'volume_mensal': 521} exp={'mes_cmpt': 1, 'sistema': 'SIA', 'total': 5898}`
**TTA:** 4995ms

---

### ❌ Q30 — Epidemiológica Complexa

**Q:** Idade média dos pacientes internados por fratura de fêmur (S72) em SP em 2022

**SQL gerado:**
```sql
SELECT AVG(idade_paciente) AS idade_media
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND cid_principal LIKE 'S72%'
```

**Resultado:** `Linha 1: got={'idade_media': 62.3961647195} exp={'idade_media': 62.4, 'idade_minima': 0, 'idade_maxima': 99}`
**TTA:** 4453ms

---

### ❌ Q31 — Financeira

**Q:** Custo total de todas as internações ortopédicas em SP em 2022

**SQL gerado:**
```sql
SELECT SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=13990374.039999994 exp=175283428.77`
**TTA:** 4447ms

---

### ❌ Q32 — Financeira

**Q:** Custo médio por internação ortopédica em SP em 2022

**SQL gerado:**
```sql
SELECT 
    COALESCE(SUM(custo_total) / COUNT(DISTINCT n_aih), 0) AS custo_medio_por_internacao
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=2219.9895334814 exp=2655.37`
**TTA:** 5110ms

---

### ✅ Q33 — Financeira

**Q:** Custo total de internações por fratura de fêmur (S72) em SP em 2022

**SQL gerado:**
```sql
SELECT SUM(COALESCE(custo_total, 0)) AS total_custo
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'S72%' OR main_icd LIKE 'S72%');
```

**Resultado:** `got=79277316.95999992 exp=79277316.96`
**TTA:** 5213ms

---

### ❌ Q34 — Financeira

**Q:** Top 5 CIDs ortopédicos com maior custo total de internação em SP em 2022

**SQL gerado:**
```sql
SELECT 
    cid_principal, 
    SUM(custo_total) AS total_custo
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2022 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cid_principal
ORDER BY 
    total_custo DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cid_principal': 'M511', 'total_custo': 878799.22} exp={'cid_principal': 'S720', 'internacoes': 8980, 'custo_total': 29879588.9}`
**TTA:** 6325ms

---

### ❌ Q35 — Financeira

**Q:** Custo total de internações ortopédicas com uso de OPME (val_ortp > 0) em SP em 2022

**SQL gerado:**
```sql
SELECT SUM(COALESCE(val_ortp, 0)) AS custo_total_ortopedia
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND val_ortp > 0
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `Linha 1: got={'custo_total_ortopedia': None} exp={'internacoes_com_opme': 0, 'custo_total_opme': None}`
**TTA:** 5431ms

---

### ❌ Q36 — Financeira

**Q:** Valor total de honorários profissionais (val_sp) em internações ortopédicas em SP em 2022

**SQL gerado:**
```sql
SELECT SUM(COALESCE(val_sp, 0)) AS total_honorarios_profissionais
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=2795569.4899999984 exp=30640001.52`
**TTA:** 4450ms

---

### ❌ Q37 — Financeira

**Q:** Custo médio por dia de internação ortopédica em SP em 2022

**SQL gerado:**
```sql
SELECT 
    COALESCE(SUM(custo_total) / SUM(qt_diarias), 0) AS custo_medio_por_dia
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=391.5581875175 exp=432.54`
**TTA:** 5962ms

---

### ❌ Q38 — Financeira

**Q:** Top 5 estabelecimentos com maior custo total de internações ortopédicas em SP em 2022

**SQL gerado:**
```sql
SELECT 
    cod_munic_estabelecimento, 
    SUM(COALESCE(custo_total, 0)) AS total_custo
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2022 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cod_munic_estabelecimento
ORDER BY 
    total_custo DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cod_munic_estabelecimento': '350000', 'total_custo': 6704942.199999998} exp={'cnes_estabelecimento': '2078015', 'internacoes': 2221, 'custo_total': 11218869.54}`
**TTA:** 6981ms

---

### ❌ Q39 — Financeira

**Q:** Custo total de procedimentos ambulatoriais ortopédicos em SP em 2022

**SQL gerado:**
```sql
SELECT SUM(COALESCE(custo_total, 0)) AS total_custo
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=1945222.8200000036 exp=8721217.91`
**TTA:** 4320ms

---

### ❌ Q40 — Financeira

**Q:** Proporção custo serviço hospitalar vs honorários em internações ortopédicas em SP em 2022

**SQL gerado:**
```sql
SELECT 
    COALESCE(SUM(val_sh), 0) / COALESCE(SUM(val_sp), 1) AS proporcao_custos
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `Linha 1: got={'proporcao_custos': 4.0044808723} exp={'custo_servico_hospitalar': 144636796.74, 'honorarios_profissionais': 30640001.52, 'pct_hospitalar': 82.5}`
**TTA:** 5999ms

---

### ❌ Q41 — Temporal/Comparativa

**Q:** Distribuição mensal de internações ortopédicas em SP em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    COUNT(DISTINCT n_aih) AS internacoes_ortopédicas
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'internacoes_ortopédicas': 476} exp={'mes_cmpt': 1, 'internacoes': 5209}`
**TTA:** 6363ms

---

### ❌ Q42 — Temporal/Comparativa

**Q:** Distribuição mensal de procedimentos ambulatoriais ortopédicos em SP em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    COUNT(*) AS quantidade_procedimentos
FROM 
    processed
WHERE 
    sistema = 'SIA' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2022 
    AND cod_procedimento LIKE '0408%'
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `Número de linhas: got=0 exp=12`
**TTA:** 4755ms

---

### ✅ Q43 — Temporal/Comparativa

**Q:** Mês com maior número de internações por fratura de fêmur (S72) em SP em 2022

**SQL gerado:**
```sql
SELECT mes_cmpt, COUNT(DISTINCT n_aih) AS num_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND cid_principal LIKE 'S72%'
GROUP BY mes_cmpt
ORDER BY num_internacoes DESC
LIMIT 1;
```

**Resultado:** `got=8 exp=8`
**TTA:** 5504ms

---

### ❌ Q44 — Temporal/Comparativa

**Q:** Evolução mensal do custo total de internações ortopédicas em SP em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    SUM(COALESCE(custo_total, 0)) AS custo_total
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'custo_total': 1015180.7699999993} exp={'mes_cmpt': 1, 'custo_total': 12942488.31}`
**TTA:** 5406ms

---

### ❌ Q45 — Temporal/Comparativa

**Q:** Volume mensal de internações por trauma (S00-T98) vs doenças musculoesqueléticas (M00-M99) em SP em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    COUNT(DISTINCT CASE WHEN icd_group = 'S00-T98' THEN n_aih END) AS trauma_count,
    COUNT(DISTINCT CASE WHEN icd_group = 'M00-M99' THEN n_aih END) AS musculoesqueletico_count
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `Número de linhas: got=12 exp=24`
**TTA:** 7298ms

---

### ❌ Q46 — Temporal/Comparativa

**Q:** Evolução mensal de óbitos em internações ortopédicas em SP em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    COUNT(DISTINCT n_aih) AS total_obitos
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND morte = 1
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'total_obitos': 6} exp={'mes_cmpt': 1, 'total_obitos': 179}`
**TTA:** 6535ms

---

### ❌ Q47 — Temporal/Comparativa

**Q:** Permanência média por trimestre nas internações ortopédicas em SP em 2022

**SQL gerado:**
```sql
SELECT 
    AVG(dias_perm) AS media_permanencia,
    mes_cmpt AS trimestre
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2022 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `Número de linhas: got=12 exp=4`
**TTA:** 6319ms

---

### ❌ Q48 — Temporal/Comparativa

**Q:** Mês com maior custo médio por internação ortopédica em SP em 2022

**SQL gerado:**
```sql
WITH orthopedic_internations AS (
    SELECT
        mes_cmpt,
        custo_total
    FROM
        processed
    JOIN
        enriched e ON processed.row_id = e.row_id
    WHERE
        sistema = 'SIH'
        AND uf_origem = 'SP'
        AND ano_cmpt = 2022
        AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
)
SELECT
    mes_cmpt,
    AVG(custo_total) AS avg_cost_per_internation
FROM
    orthopedic_internations
GROUP BY
    mes_cmpt
ORDER BY
    avg_cost_per_internation DESC
LIMIT 1;
```

**Resultado:** `got=12 exp=10`
**TTA:** 7927ms

---

### ✅ Q49 — Temporal/Comparativa

**Q:** Sazonalidade mensal de fraturas de fêmur (S72) em SP em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    COUNT(*) AS quantidade_internacoes
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND cid_principal LIKE 'S72%'
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 5527ms

---

### ❌ Q50 — Temporal/Comparativa

**Q:** Volume mensal de internações ortopédicas de idosos (60 anos ou mais) em SP em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND idade_paciente >= 60
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'volume_internacoes': 118} exp={'mes_cmpt': 1, 'internacoes_idosos': 1987}`
**TTA:** 6664ms

---

### ❌ Q51 — Epidemiológica Simples

**Q:** Número de internações por fratura de fêmur (S72) em mulheres em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND sexo_paciente = 'F'
  AND (cid_principal LIKE 'S72%' OR icd_group = 'S00-T98');
```

**Resultado:** `got=23520 exp=15359`
**TTA:** 5531ms

---

### ❌ Q52 — Epidemiológica Simples

**Q:** Número de internações por fratura de fêmur (S72) em homens em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND sexo_paciente = 'M'
  AND (cid_principal LIKE 'S72%' OR icd_group = 'S00-T98');
```

**Resultado:** `got=36148 exp=13997`
**TTA:** 5548ms

---

### ❌ Q53 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com permanência de 1 dia ou menos em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
  AND dias_perm <= 1;
```

**Resultado:** `got=2330 exp=13928`
**TTA:** 5423ms

---

### ❌ Q54 — Epidemiológica Simples

**Q:** Número de procedimentos ambulatoriais ortopédicos realizados em mulheres em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(*) AS numero_procedimentos
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE processed.sistema = 'SIA'
  AND processed.uf_origem = 'SP'
  AND processed.ano_cmpt = 2022
  AND processed.sexo_paciente = 'F'
  AND processed.cod_procedimento LIKE '0408%'
  AND (processed.cid_principal LIKE 'M%' OR processed.icd_group = 'M00-M99');
```

**Resultado:** `got=0 exp=45579`
**TTA:** 5814ms

---

### ❌ Q55 — Epidemiológica Simples

**Q:** Número de procedimentos ambulatoriais ortopédicos realizados por idosos (60 anos ou mais) em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(*) AS numero_procedimentos
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND idade_paciente >= 60
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
  AND cod_procedimento LIKE '0408%';
```

**Resultado:** `got=0 exp=53666`
**TTA:** 5503ms

---

### ❌ Q56 — Epidemiológica Simples

**Q:** Número de internações por fratura de tíbia e perônio (S82) em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'S82%' OR e.clinical_interpretacao_clinica = 'lesoes e causas traumaticas');
```

**Resultado:** `got=59668 exp=6261`
**TTA:** 5523ms

---

### ❌ Q57 — Epidemiológica Simples

**Q:** Número de internações por fratura de antebraço (S52) em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'S52%' OR e.clinical_interpretacao_clinica = 'lesoes e causas traumaticas');
```

**Resultado:** `got=59668 exp=1970`
**TTA:** 5526ms

---

### ❌ Q58 — Epidemiológica Simples

**Q:** Número de internações por fratura de ombro e úmero (S42) em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'S42%' OR e.clinical_interpretacao_clinica = 'lesoes e causas traumaticas');
```

**Resultado:** `got=59668 exp=1248`
**TTA:** 5539ms

---

### ❌ Q59 — Epidemiológica Simples

**Q:** Número de internações ortopédicas em crianças (menores de 18 anos) em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND idade_paciente < 18
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=481 exp=3619`
**TTA:** 5494ms

---

### ❌ Q60 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com permanência entre 8 e 30 dias em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND dias_perm BETWEEN 8 AND 30
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=1239 exp=15489`
**TTA:** 5561ms

---

### ✅ Q61 — Epidemiológica Simples

**Q:** Número de internações por artrose de quadril e joelho (M16 e M17) em idosos (60 anos ou mais) em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M16%' OR cid_principal LIKE 'M17%')
  AND idade_paciente >= 60;
```

**Resultado:** `got=89 exp=89`
**TTA:** 5461ms

---

### ❌ Q62 — Epidemiológica Simples

**Q:** Número de internações por fratura de fêmur (S72) em mulheres com 70 anos ou mais em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND idade_paciente >= 70
  AND sexo_paciente = 'F'
  AND (cid_principal LIKE 'S72%' OR icd_group = 'S00-T98');
```

**Resultado:** `got=12217 exp=10944`
**TTA:** 5952ms

---

### ❌ Q63 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com permanência superior a 30 dias em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND dias_perm > 30
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=157 exp=975`
**TTA:** 5441ms

---

### ❌ Q64 — Epidemiológica Simples

**Q:** Número de procedimentos ambulatoriais ortopédicos realizados em homens em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(*) AS numero_procedimentos
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND sexo_paciente = 'M'
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
  AND cod_procedimento LIKE '0408%';
```

**Resultado:** `got=0 exp=59437`
**TTA:** 5458ms

---

### ✅ Q65 — Epidemiológica Simples

**Q:** Número de internações por traumatismos (grupo S00-T98) em idosos (60 anos ou mais) em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND idade_paciente >= 60
  AND (cid_principal LIKE 'S%' OR icd_group = 'S00-T98');
```

**Resultado:** `got=24981 exp=24981`
**TTA:** 5498ms

---

### ✅ Q66 — Epidemiológica Simples

**Q:** Número de internações por doenças osteomusculares (grupo M00-M99) em mulheres em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND sexo_paciente = 'F'
  AND icd_group = 'M00-M99';
```

**Resultado:** `got=2327 exp=2327`
**TTA:** 5135ms

---

### ✅ Q67 — Epidemiológica Simples

**Q:** Número de óbitos em internações por fratura de fêmur (S72) em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_obitos
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND cid_principal LIKE 'S72%'
  AND morte = 1;
```

**Resultado:** `got=1216 exp=1216`
**TTA:** 4912ms

---

### ❌ Q68 — Epidemiológica Simples

**Q:** Número de municípios distintos com registro de procedimentos ambulatoriais ortopédicos em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT cod_munic_residencia) AS num_municipios_distintos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND cod_procedimento LIKE '0408%';
```

**Resultado:** `got=0 exp=399`
**TTA:** 4127ms

---

### ❌ Q69 — Epidemiológica Complexa

**Q:** Top 5 municípios por volume de procedimentos ambulatoriais ortopédicos em SP em 2022

**SQL gerado:**
```sql
SELECT 
    cod_munic_residencia,
    COUNT(*) AS volume_procedimentos
FROM 
    processed
WHERE 
    sistema = 'SIA' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2022 
    AND cod_procedimento LIKE '0408%'
GROUP BY 
    cod_munic_residencia
ORDER BY 
    volume_procedimentos DESC
LIMIT 5;
```

**Resultado:** `Número de linhas: got=0 exp=5`
**TTA:** 5231ms

---

### ❌ Q70 — Epidemiológica Complexa

**Q:** Distribuição de procedimentos ambulatoriais ortopédicos por faixa etária em SP em 2022

**SQL gerado:**
```sql
SELECT 
    FLOOR(idade_paciente / 10) * 10 AS faixa_etaria,
    COUNT(*) AS quantidade_procedimentos
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIA'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND cod_procedimento LIKE '0408%'
GROUP BY 
    faixa_etaria
ORDER BY 
    faixa_etaria;
```

**Resultado:** `Número de linhas: got=0 exp=3`
**TTA:** 6085ms

---

### ❌ Q71 — Epidemiológica Complexa

**Q:** Top 5 diagnósticos com maior número de óbitos em internações ortopédicas em SP em 2022

**SQL gerado:**
```sql
SELECT 
    cid_principal,
    COUNT(DISTINCT n_aih) AS num_obitos
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND morte = 1
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cid_principal
ORDER BY 
    num_obitos DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cid_principal': 'M869', 'num_obitos': 13} exp={'cid_principal': 'S720', 'total_internacoes': 8980, 'total_obitos': 455.0}`
**TTA:** 6674ms

---

### ❌ Q72 — Epidemiológica Complexa

**Q:** Permanência hospitalar média por sexo em internações ortopédicas em SP em 2022

**SQL gerado:**
```sql
SELECT 
    sexo_paciente,
    AVG(dias_perm) AS media_permanencia
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    sexo_paciente;
```

**Resultado:** `Linha não encontrada: {'sexo_paciente': 'F', 'permanencia_media': 6.3}`
**TTA:** 5804ms

---

### ❌ Q73 — Epidemiológica Complexa

**Q:** Top 10 estabelecimentos por volume de procedimentos ambulatoriais ortopédicos em SP em 2022

**SQL gerado:**
```sql
SELECT 
    cod_munic_estabelecimento,
    COUNT(*) AS volume_procedimentos
FROM 
    processed
WHERE 
    sistema = 'SIA' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2022 
    AND cod_procedimento LIKE '0408%'
GROUP BY 
    cod_munic_estabelecimento
ORDER BY 
    volume_procedimentos DESC
LIMIT 10;
```

**Resultado:** `Número de linhas: got=0 exp=10`
**TTA:** 5350ms

---

### ❌ Q74 — Epidemiológica Complexa

**Q:** Taxa de mortalidade por sexo em internações ortopédicas em SP em 2022

**SQL gerado:**
```sql
SELECT 
    sexo_paciente,
    COUNT(DISTINCT n_aih) AS total_internacoes,
    SUM(CASE WHEN morte = 1 THEN 1 ELSE 0 END) AS total_obitos,
    SUM(CASE WHEN morte = 1 THEN 1 ELSE 0 END) * 1.0 / COUNT(DISTINCT n_aih) AS taxa_mortalidade
FROM 
    processed
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    sexo_paciente;
```

**Resultado:** `Linha não encontrada: {'sexo_paciente': 'F', 'total_obitos': 1081.0, 'taxa_mortalidade': 4.18}`
**TTA:** 7451ms

---

### ❌ Q75 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por grupo diagnóstico (M e S) e faixa etária em SP em 2022

**SQL gerado:**
```sql
SELECT 
    icd_group,
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente < 30 THEN '18-29'
        WHEN idade_paciente < 40 THEN '30-39'
        WHEN idade_paciente < 50 THEN '40-49'
        WHEN idade_paciente < 60 THEN '50-59'
        WHEN idade_paciente < 70 THEN '60-69'
        WHEN idade_paciente < 80 THEN '70-79'
        ELSE '80+'
    END AS faixa_etaria,
    COUNT(DISTINCT n_aih) AS internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99' OR cid_principal LIKE 'S%' OR icd_group = 'S00-T98')
GROUP BY 
    icd_group, faixa_etaria
ORDER BY 
    icd_group, faixa_etaria;
```

**Resultado:** `Número de linhas: got=16 exp=6`
**TTA:** 11764ms

---

### ❌ Q76 — Epidemiológica Complexa

**Q:** Top 5 diagnósticos de traumatismo com maior permanência média em SP em 2022 (mínimo 40 internações)

**SQL gerado:**
```sql
SELECT 
    cid_principal, 
    AVG(dias_perm) AS media_permanencia, 
    COUNT(DISTINCT n_aih) AS internacoes
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2022 
    AND (cid_principal LIKE 'S%' OR icd_group = 'S00-T98')
GROUP BY 
    cid_principal
HAVING 
    COUNT(DISTINCT n_aih) >= 40
ORDER BY 
    media_permanencia DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cid_principal': 'S063', 'media_permanencia': 16.358974359, 'internacoes': 78} exp={'cid_principal': 'S063', 'internacoes': 78, 'permanencia_media': 16.4}`
**TTA:** 7646ms

---

### ❌ Q77 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por faixa etária (0-17, 18-39, 40-59, 60-79, 80+) em SP em 2022

**SQL gerado:**
```sql
SELECT 
    CASE 
        WHEN idade_paciente BETWEEN 0 AND 17 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 39 THEN '18-39'
        WHEN idade_paciente BETWEEN 40 AND 59 THEN '40-59'
        WHEN idade_paciente BETWEEN 60 AND 79 THEN '60-79'
        WHEN idade_paciente >= 80 THEN '80+'
    END AS faixa_etaria,
    COUNT(DISTINCT n_aih) AS quantidade_internacoes
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    faixa_etaria
ORDER BY 
    faixa_etaria;
```

**Resultado:** `Linha não encontrada: {'faixa_etaria': '0-17', 'total': 3619}`
**TTA:** 10400ms

---

### ❌ Q78 — Epidemiológica Complexa

**Q:** Top 5 estabelecimentos com maior volume de internações por fratura de fêmur (S72) em SP em 2022

**SQL gerado:**
```sql
SELECT 
    cod_munic_estabelecimento,
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'S72%' OR e.clinical_interpretacao_clinica = 'lesoes e causas traumaticas')
GROUP BY 
    cod_munic_estabelecimento
ORDER BY 
    volume_internacoes DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cod_munic_estabelecimento': '350000', 'volume_internacoes': 27024} exp={'cnes_estabelecimento': '2091399', 'internacoes': 816}`
**TTA:** 7239ms

---

### ❌ Q79 — Epidemiológica Complexa

**Q:** Permanência hospitalar média por faixa etária em internações ortopédicas em SP em 2022

**SQL gerado:**
```sql
SELECT 
    FLOOR(idade_paciente / 10) * 10 AS faixa_etaria,
    AVG(dias_perm) AS media_permanencia
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    faixa_etaria
ORDER BY 
    faixa_etaria;
```

**Resultado:** `Número de linhas: got=10 exp=3`
**TTA:** 6875ms

---

### ✅ Q80 — Epidemiológica Complexa

**Q:** Top 5 diagnósticos de doença osteomuscular (M00-M99) por número de internações em SP em 2022

**SQL gerado:**
```sql
SELECT 
    cid_principal, 
    COUNT(DISTINCT n_aih) AS num_internacoes
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2022 
    AND icd_group = 'M00-M99'
GROUP BY 
    cid_principal
ORDER BY 
    num_internacoes DESC
LIMIT 5;
```

**Resultado:** `5 linhas corretas (flexível)`
**TTA:** 6203ms

---

### ❌ Q81 — Epidemiológica Complexa

**Q:** Número de internações ortopédicas com permanência entre 3 e 7 dias em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND dias_perm BETWEEN 3 AND 7
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=1465 exp=25807`
**TTA:** 5539ms

---

### ❌ Q82 — Epidemiológica Complexa

**Q:** Top 5 diagnósticos ambulatoriais ortopédicos em idosos (60 anos ou mais) em SP em 2022

**SQL gerado:**
```sql
SELECT 
    cid_principal, 
    COUNT(*) AS quantidade
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIA' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2022 
    AND idade_paciente >= 60 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cid_principal
ORDER BY 
    quantidade DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cid_principal': 'M216', 'quantidade': 1596} exp={'cid_principal': 'S720', 'total': 11096}`
**TTA:** 6453ms

---

### ❌ Q83 — Epidemiológica Complexa

**Q:** Média de internações ortopédicas por estabelecimento em SP em 2022

**SQL gerado:**
```sql
SELECT 
    cod_munic_estabelecimento,
    COUNT(DISTINCT n_aih) AS internacoes_ortopedicas
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cod_munic_estabelecimento;
```

**Resultado:** `got=353930 exp=173.1`
**TTA:** 6433ms

---

### ❌ Q84 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por faixa de permanência em SP em 2022

**SQL gerado:**
```sql
SELECT 
    CASE 
        WHEN dias_perm < 7 THEN 'Menos de 7 dias'
        WHEN dias_perm BETWEEN 7 AND 14 THEN '7 a 14 dias'
        WHEN dias_perm BETWEEN 15 AND 30 THEN '15 a 30 dias'
        WHEN dias_perm BETWEEN 31 AND 60 THEN '31 a 60 dias'
        ELSE 'Mais de 60 dias'
    END AS faixa_permanencia,
    COUNT(DISTINCT n_aih) AS quantidade_internacoes
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    faixa_permanencia
ORDER BY 
    faixa_permanencia;
```

**Resultado:** `Número de linhas: got=5 exp=4`
**TTA:** 10227ms

---

### ❌ Q85 — Financeira

**Q:** Custo total de internações ortopédicas de idosos (60 anos ou mais) em SP em 2022

**SQL gerado:**
```sql
SELECT SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND idade_paciente >= 60
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=4489529.78 exp=78009817.26`
**TTA:** 5748ms

---

### ✅ Q86 — Financeira

**Q:** Custo médio por internação por fratura de fêmur (S72) em SP em 2022

**SQL gerado:**
```sql
SELECT 
    AVG(custo_total) AS custo_medio_internacao
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'S72%' OR e.clinical_interpretacao_clinica = 'lesoes e causas traumaticas')
```

**Resultado:** `got=2703.0392440214 exp=2700.27`
**TTA:** 5845ms

---

### ❌ Q87 — Financeira

**Q:** Custo total de internações ortopédicas com passagem por UTI em SP em 2022

**SQL gerado:**
```sql
SELECT SUM(COALESCE(val_uti, 0)) AS custo_total_uti
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
  AND uti_int_to > 0;
```

**Resultado:** `got=None exp=15904.56`
**TTA:** 5387ms

---

### ❌ Q88 — Financeira

**Q:** Custo médio por internação ortopédica por sexo do paciente em SP em 2022

**SQL gerado:**
```sql
SELECT 
    sexo_paciente,
    AVG(custo_total) AS custo_medio_internacao
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2022 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    sexo_paciente;
```

**Resultado:** `Linha não encontrada: {'sexo_paciente': 'F', 'custo_medio': 2684.14}`
**TTA:** 6000ms

---

### ❌ Q89 — Financeira

**Q:** Custo total de internações ortopédicas em crianças (menores de 18 anos) em SP em 2022

**SQL gerado:**
```sql
SELECT SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND idade_paciente < 18
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=927790.9100000001 exp=7450877.25`
**TTA:** 5745ms

---

### ❌ Q90 — Financeira

**Q:** Custo médio por internação ortopédica com permanência superior a 7 dias em SP em 2022

**SQL gerado:**
```sql
SELECT 
    AVG(custo_total) AS custo_medio_por_internacao
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND dias_perm > 7
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=3083.8746901408 exp=4423.19`
**TTA:** 4811ms

---

### ❌ Q91 — Financeira

**Q:** Top 3 municípios com maior custo total em internações ortopédicas em SP em 2022

**SQL gerado:**
```sql
SELECT 
    cod_munic_residencia, 
    SUM(custo_total) AS total_custo
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2022 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cod_munic_residencia
ORDER BY 
    total_custo DESC
LIMIT 3;
```

**Resultado:** `Linha 1: got={'cod_munic_residencia': '355030', 'total_custo': 2553035.5} exp={'cod_munic_estabelecimento': '350000', 'custo_total': 91002293.78}`
**TTA:** 6645ms

---

### ❌ Q92 — Financeira

**Q:** Valor total de serviço hospitalar em internações ortopédicas em SP em 2022

**SQL gerado:**
```sql
SELECT SUM(COALESCE(val_sh, 0)) AS total_val_sh
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=11194804.549999993 exp=144636796.74`
**TTA:** 5170ms

---

### ❌ Q93 — Financeira

**Q:** Custo médio por internação ortopédica por faixa etária em SP em 2022

**SQL gerado:**
```sql
SELECT 
    FLOOR(idade_paciente / 10) * 10 AS faixa_etaria,
    AVG(custo_total) AS custo_medio_internacao
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    faixa_etaria
ORDER BY 
    faixa_etaria;
```

**Resultado:** `Número de linhas: got=10 exp=3`
**TTA:** 6993ms

---

### ❌ Q94 — Financeira

**Q:** Valor total de UTI em internações ortopédicas em SP em 2022

**SQL gerado:**
```sql
SELECT SUM(COALESCE(val_uti, 0)) AS total_uti
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=1275788.72 exp=37073666.92`
**TTA:** 5219ms

---

### ✅ Q95 — Financeira

**Q:** Custo total de internações por doenças osteomusculares (M00-M99) em SP em 2022

**SQL gerado:**
```sql
SELECT SUM(custo_total) AS custo_total_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND e.clinical_interpretacao_clinica = 'doencas osteomusculares e do tecido conjuntivo';
```

**Resultado:** `got=13990374.04 exp=13990374.04`
**TTA:** 5414ms

---

### ✅ Q96 — Financeira

**Q:** Custo total de internações por traumatismos (S00-T98) em SP em 2022

**SQL gerado:**
```sql
SELECT SUM(COALESCE(custo_total, 0)) AS custo_total
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND icd_group = 'S00-T98';
```

**Resultado:** `got=161293054.73000056 exp=161293054.73`
**TTA:** 4894ms

---

### ❌ Q97 — Financeira

**Q:** Top 5 diagnósticos com maior custo médio por internação ortopédica em SP em 2022 (mínimo 40 internações)

**SQL gerado:**
```sql
SELECT 
    cid_principal,
    AVG(custo_total) AS custo_medio,
    COUNT(n_aih) AS quantidade_internacoes
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2022 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cid_principal
HAVING 
    COUNT(n_aih) >= 40
ORDER BY 
    custo_medio DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cid_principal': 'M518', 'custo_medio': 7827.0964285714, 'quantidade_internacoes': 112} exp={'cid_principal': 'S221', 'internacoes': 41, 'custo_medio': 14053.88}`
**TTA:** 7382ms

---

### ❌ Q98 — Financeira

**Q:** Custo médio por procedimento ambulatorial ortopédico em SP em 2022

**SQL gerado:**
```sql
SELECT 
    cod_procedimento,
    AVG(custo_total) AS custo_medio
FROM 
    processed
WHERE 
    sistema = 'SIA' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2022
    AND cod_procedimento LIKE '0408%'
GROUP BY 
    cod_procedimento;
```

**Resultado:** `Resultado vazio`
**TTA:** 4635ms

---

### ❌ Q99 — Financeira

**Q:** Top 5 procedimentos ambulatoriais com maior custo total em SP em 2022

**SQL gerado:**
```sql
SELECT 
    cod_procedimento, 
    SUM(custo_total) AS total_custo
FROM 
    processed
WHERE 
    sistema = 'SIA' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2022
GROUP BY 
    cod_procedimento
ORDER BY 
    total_custo DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cod_procedimento': '0702100048', 'total_custo': 46893518.21000075} exp={'cod_procedimento': '0701020369', 'custo_total': 1201460.4}`
**TTA:** 4638ms

---

### ❌ Q100 — Financeira

**Q:** Custo total de internações ortopédicas de idosos (60+) com permanência superior a 7 dias em SP em 2022

**SQL gerado:**
```sql
SELECT SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND idade_paciente >= 60
  AND dias_perm > 7
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=1801433.3399999999 exp=35943333.49`
**TTA:** 6058ms

---

### ❌ Q101 — Temporal/Comparativa

**Q:** Mês com maior número de óbitos em internações ortopédicas em SP em 2022

**SQL gerado:**
```sql
SELECT mes_cmpt, COUNT(DISTINCT n_aih) AS num_obitos
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND cid_principal LIKE 'M%'
  AND morte = 1
GROUP BY mes_cmpt
ORDER BY num_obitos DESC
LIMIT 1;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 8, 'num_obitos': 10} exp={'mes_cmpt': 10, 'total_obitos': 224}`
**TTA:** 5670ms

---

### ❌ Q102 — Temporal/Comparativa

**Q:** Volume mensal de internações por fratura de fêmur (S72) em idosos (60 anos ou mais) em SP em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND idade_paciente >= 60
    AND (cid_principal LIKE 'S72%' OR e.clinical_interpretacao_clinica = 'lesoes e causas traumaticas')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'volume_internacoes': 1869} exp={'mes_cmpt': 1, 'internacoes': 1409}`
**TTA:** 7078ms

---

### ❌ Q103 — Temporal/Comparativa

**Q:** Custo mensal de internações ortopédicas de idosos (60 anos ou mais) em SP em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    SUM(COALESCE(custo_total, 0)) AS custo_total
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND idade_paciente >= 60
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'custo_total': 287024.49} exp={'mes_cmpt': 1, 'custo_total': 5379118.99}`
**TTA:** 6759ms

---

### ✅ Q104 — Temporal/Comparativa

**Q:** Mês com maior custo total de internações por fratura de fêmur (S72) em SP em 2022

**SQL gerado:**
```sql
SELECT mes_cmpt, SUM(COALESCE(custo_total, 0)) AS total_custo
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND cid_principal LIKE 'S72%'
GROUP BY mes_cmpt
ORDER BY total_custo DESC
LIMIT 1;
```

**Resultado:** `1 linhas corretas (flexível)`
**TTA:** 5570ms

---

### ✅ Q105 — Temporal/Comparativa

**Q:** Volume mensal de internações por traumatismos (grupo S00-T98) em SP em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND e.clinical_interpretacao_clinica = 'lesoes e causas traumaticas'
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 6337ms

---

### ❌ Q106 — Temporal/Comparativa

**Q:** Volume mensal de procedimentos ambulatoriais ortopédicos por sexo em SP em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    sexo_paciente,
    COUNT(*) AS volume_procedimentos
FROM 
    processed
WHERE 
    sistema = 'SIA'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND cod_procedimento LIKE '0408%'
GROUP BY 
    mes_cmpt, sexo_paciente
ORDER BY 
    mes_cmpt, sexo_paciente;
```

**Resultado:** `Número de linhas: got=0 exp=24`
**TTA:** 5192ms

---

### ⚠️ Q107 — Temporal/Comparativa

**Q:** Comparativo trimestral de internações ortopédicas por grupo diagnóstico em SP em 2022

**SQL gerado:**
```sql
SELECT 
    icd_group,
    EXTRACT(QUARTER FROM TO_DATE(dt_inter, 'YYYYMMDD')) AS trimestre,
    COUNT(DISTINCT n_aih) AS internacoes
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    icd_group, trimestre
ORDER BY 
    icd_group, trimestre;
```

**Erro:** Falha DuckDB: Catalog Error: Scalar Function with name to_date does not exist!
Did you mean "to_days"?

LINE 3:     EXTRACT(QUARTER FROM TO_DATE(dt_inter, 'YYYYMMDD')) AS trimestre,
                                 ^

---

### ❌ Q108 — Temporal/Comparativa

**Q:** Mês com maior permanência hospitalar média em internações ortopédicas em SP em 2022

**SQL gerado:**
```sql
SELECT mes_cmpt, AVG(dias_perm) AS media_permanencia
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY mes_cmpt
ORDER BY media_permanencia DESC
LIMIT 1;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 4, 'media_permanencia': 7.0149892934} exp={'mes_cmpt': 7, 'permanencia_media': 6.2}`
**TTA:** 5843ms

---

### ❌ Q109 — Temporal/Comparativa

**Q:** Evolução mensal da taxa de mortalidade em internações ortopédicas em SP em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    COUNT(DISTINCT CASE WHEN morte = 1 THEN n_aih END) AS mortes,
    COUNT(DISTINCT n_aih) AS total_internacoes,
    (COUNT(DISTINCT CASE WHEN morte = 1 THEN n_aih END) * 1.0 / COUNT(DISTINCT n_aih)) AS taxa_mortalidade
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'mortes': 6, 'total_internacoes': 476, 'taxa_mortalidade': 0.012605042} exp={'mes_cmpt': 1, 'internacoes': 5209, 'obitos': 179.0, 'taxa_mortalidade': 3.44}`
**TTA:** 8659ms

---

### ❌ Q110 — Temporal/Comparativa

**Q:** Trimestre com maior volume de internações por fratura de fêmur (S72) em SP em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2022 
    AND cid_principal LIKE 'S72%'
GROUP BY 
    mes_cmpt
ORDER BY 
    volume_internacoes DESC
LIMIT 1;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 8, 'volume_internacoes': 2757} exp={'trimestre': 'Q3', 'total': 8047}`
**TTA:** 6101ms

---

### ❌ Q111 — Temporal/Comparativa

**Q:** Comparativo semestral de internações ortopédicas (1º vs 2º semestre) em SP em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    COUNT(DISTINCT n_aih) AS internacoes_ortopedicas
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `Número de linhas: got=12 exp=2`
**TTA:** 6373ms

---

### ❌ Q112 — Temporal/Comparativa

**Q:** Custo médio mensal por internação por fratura de fêmur (S72) em SP em 2022

**SQL gerado:**
```sql
SELECT 
    AVG(COALESCE(custo_total, 0)) AS custo_medio_mensal
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND cid_principal LIKE 'S72%'
```

**Resultado:** `Número de linhas: got=1 exp=12`
**TTA:** 5213ms

---

### ❌ Q113 — Temporal/Comparativa

**Q:** Volume mensal de internações ortopédicas por sexo do paciente em SP em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    sexo_paciente,
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt, sexo_paciente
ORDER BY 
    mes_cmpt, sexo_paciente;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'sexo_paciente': 'F', 'volume_internacoes': 180} exp={'mes_cmpt': 1, 'sexo_paciente': 'F', 'total': 2058}`
**TTA:** 6761ms

---

### ❌ Q114 — Temporal/Comparativa

**Q:** Mês com menor volume de procedimentos ambulatoriais ortopédicos em SP em 2022

**SQL gerado:**
```sql
SELECT mes_cmpt
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY mes_cmpt
ORDER BY COUNT(*) ASC
LIMIT 1;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1} exp={'mes_cmpt': 1, 'total': 5898}`
**TTA:** 5333ms

---

### ❌ Q115 — Temporal/Comparativa

**Q:** Volume trimestral de procedimentos ambulatoriais ortopédicos em SP em 2022

**SQL gerado:**
```sql
SELECT 
    ano_cmpt,
    mes_cmpt,
    COUNT(*) AS volume_procedimentos
FROM 
    processed
WHERE 
    sistema = 'SIA' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2022 
    AND cod_procedimento LIKE '0408%'
GROUP BY 
    ano_cmpt, mes_cmpt
ORDER BY 
    ano_cmpt, mes_cmpt;
```

**Resultado:** `Número de linhas: got=0 exp=4`
**TTA:** 5269ms

---

### ❌ Q116 — Temporal/Comparativa

**Q:** Custo mensal de procedimentos ambulatoriais ortopédicos em SP em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    SUM(COALESCE(custo_total, 0)) AS custo_total
FROM 
    processed
WHERE 
    sistema = 'SIA' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2022 
    AND cod_procedimento LIKE '0408%'
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `Número de linhas: got=0 exp=12`
**TTA:** 5117ms

---

### ❌ Q117 — Temporal/Comparativa

**Q:** Comparativo semestral do custo total de internações ortopédicas em SP em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    SUM(custo_total) AS custo_total
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `Número de linhas: got=12 exp=2`
**TTA:** 5120ms

---

### ❌ Q118 — Temporal/Comparativa

**Q:** Evolução mensal da permanência média em internações por fratura de fêmur (S72) em SP em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    AVG(dias_perm) AS media_permanencia
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'S72%' OR e.clinical_interpretacao_clinica = 'lesoes e causas traumaticas')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'media_permanencia': 5.6021550813} exp={'mes_cmpt': 1, 'permanencia_media': 6.1}`
**TTA:** 6559ms

---

## Failures Detail

- **Q02** (Epidemiológica Simples): got=None exp=105016
  SQL: `SELECT SUM(pa_qtdapr) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt ...`
- **Q03** (Epidemiológica Simples): got=59668 exp=29356
  SQL: `SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE si...`
- **Q05** (Epidemiológica Simples): got=73 exp=2223
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_obitos
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistem...`
- **Q08** (Epidemiológica Simples): got=2327 exp=25847
  SQL: `SELECT COUNT(DISTINCT n_aih) AS num_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sist...`
- **Q09** (Epidemiológica Simples): got=3975 exp=40123
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE s...`
- **Q10** (Epidemiológica Simples): got=1685 exp=26666
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE s...`
- **Q11** (Epidemiológica Simples): got=496 exp=211
  SQL: `SELECT COUNT(DISTINCT cod_munic_residencia) AS num_municipios_distintos
FROM processed
WHERE sistema = 'SIH'
  AND uf_or...`
- **Q12** (Epidemiológica Simples): got=158 exp=381
  SQL: `SELECT COUNT(DISTINCT cod_munic_estabelecimento) AS num_estabelecimentos
FROM processed
JOIN enriched e ON processed.row...`
- **Q13** (Epidemiológica Simples): got=1383 exp=16451
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE s...`
- **Q14** (Epidemiológica Simples): got=0 exp=3
  SQL: `SELECT COUNT(DISTINCT n_aih) AS num_internacoes_uti
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE ...`
- **Q15** (Epidemiológica Simples): got=17608 exp=15119
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE s...`
- **Q16** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'M861', 'internacoes_unicas': 912} exp={'cid_principal': 'S720', 'total': 8980}
  SQL: `SELECT 
    cid_principal, 
    COUNT(DISTINCT n_aih) AS internacoes_unicas
FROM 
    processed
JOIN 
    enriched e ON ...`
- **Q17** (Epidemiológica Complexa): Linha 1: got={'cod_munic_residencia': '355030', 'num_internacoes': 1190} exp={'cod_munic_estabelecimento': '350000', 'total': 29824}
  SQL: `SELECT 
    cod_munic_residencia, 
    COUNT(DISTINCT n_aih) AS num_internacoes
FROM 
    processed
JOIN 
    enriched e...`
- **Q18** (Epidemiológica Complexa): Linha não encontrada: {'faixa_etaria': '0-17', 'total': 3619}
  SQL: `SELECT 
    CASE 
        WHEN idade_paciente BETWEEN 0 AND 17 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 59...`
- **Q19** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'M895', 'total_internacoes': 2, 'total_obitos': 1.0, 'taxa_mortalidade': 50.0} exp={'cid_principal': 'S062', 'total_internacoes': 70, 'total_obitos': 21, 'taxa_mortalidade_pct': 30.0}
  SQL: `SELECT 
    cid_principal,
    COUNT(DISTINCT n_aih) AS total_internacoes,
    SUM(morte) AS total_obitos,
    (SUM(mort...`
- **Q20** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'M216', 'volume_producao': 4743} exp={'cid_principal': 'S720', 'total': 16466}
  SQL: `SELECT 
    cid_principal, 
    COUNT(*) AS volume_producao
FROM 
    processed
JOIN 
    enriched e ON processed.row_id...`
- **Q21** (Epidemiológica Complexa): Linha não encontrada: {'sexo_paciente': 'M', 'total': 40123, 'pct': 60.8}
  SQL: `SELECT 
    sexo_paciente,
    COUNT(DISTINCT n_aih) AS internacoes,
    COUNT(DISTINCT n_aih) * 1.0 / SUM(COUNT(DISTINC...`
- **Q22** (Epidemiológica Complexa): Linha 1: got={'num_internacoes_femur': 24981, 'num_internacoes_total': 195574, 'percentual_femur': 12.7731702578} exp={'total_s72': 29356, 's72_idosos': 19120, 'pct_idosos': 65.1}
  SQL: `WITH internacoes_femur AS (
    SELECT
        COUNT(DISTINCT n_aih) AS num_internacoes_femur
    FROM
        processed...`
- **Q23** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'M318', 'media_permanencia': 36.0} exp={'cid_principal': 'S063', 'total_internacoes': 78, 'permanencia_media_dias': 16.4}
  SQL: `SELECT 
    cid_principal, 
    AVG(dias_perm) AS media_permanencia
FROM 
    processed
JOIN 
    enriched e ON processe...`
- **Q24** (Epidemiológica Complexa): Linha 1: got={'cod_munic_estabelecimento': '350000', 'volume_internacao': 2800} exp={'cnes_estabelecimento': '2078015', 'total_internacoes': 2221}
  SQL: `SELECT 
    cod_munic_estabelecimento, 
    COUNT(DISTINCT n_aih) AS volume_internacao
FROM 
    processed
JOIN 
    enr...`
- **Q25** (Epidemiológica Complexa): Linha não encontrada: {'raca_cor_paciente': '01', 'total': 38121, 'pct': 57.8}
  SQL: `SELECT 
    raca_cor_paciente,
    COUNT(DISTINCT n_aih) AS internacoes_ortopédicas
FROM 
    processed
JOIN 
    enrich...`
- **Q26** (Epidemiológica Complexa): Número de linhas: got=1 exp=10
  SQL: `SELECT 
    cid_principal, 
    SUM(qt_diarias) AS total_dias_internacao
FROM 
    processed
JOIN 
    enriched e ON pro...`
- **Q27** (Epidemiológica Complexa): Número de linhas: got=0 exp=5
  SQL: `SELECT 
    cod_procedimento, 
    COUNT(*) AS quantidade_realizada
FROM 
    processed
WHERE 
    sistema = 'SIA' 
    ...`
- **Q28** (Epidemiológica Complexa): Linha 1: got={'total_internacoes': 3412, 'internacoes_outro_municipio': 3412, 'proporcao': 1.0} exp={'total_internacoes': 65970, 'deslocamento_intermunicipal': 37219, 'pct_deslocamento': 56.4}
  SQL: `WITH internacoes_ortopédicas AS (
    SELECT
        p.n_aih,
        p.cod_munic_residencia,
        p.cod_munic_estabe...`
- **Q29** (Epidemiológica Complexa): Linha 1: got={'mes_cmpt': 1, 'sistema': 'SIA', 'volume_mensal': 521} exp={'mes_cmpt': 1, 'sistema': 'SIA', 'total': 5898}
  SQL: `SELECT 
    mes_cmpt,
    sistema,
    COUNT(*) AS volume_mensal
FROM 
    processed
WHERE 
    uf_origem = 'SP'
    AND...`
- **Q30** (Epidemiológica Complexa): Linha 1: got={'idade_media': 62.3961647195} exp={'idade_media': 62.4, 'idade_minima': 0, 'idade_maxima': 99}
  SQL: `SELECT AVG(idade_paciente) AS idade_media
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = ...`
- **Q31** (Financeira): got=13990374.039999994 exp=175283428.77
  SQL: `SELECT SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = '...`
- **Q32** (Financeira): got=2219.9895334814 exp=2655.37
  SQL: `SELECT 
    COALESCE(SUM(custo_total) / COUNT(DISTINCT n_aih), 0) AS custo_medio_por_internacao
FROM 
    processed
WHER...`
- **Q34** (Financeira): Linha 1: got={'cid_principal': 'M511', 'total_custo': 878799.22} exp={'cid_principal': 'S720', 'internacoes': 8980, 'custo_total': 29879588.9}
  SQL: `SELECT 
    cid_principal, 
    SUM(custo_total) AS total_custo
FROM 
    processed
JOIN 
    enriched e ON processed.ro...`
- **Q35** (Financeira): Linha 1: got={'custo_total_ortopedia': None} exp={'internacoes_com_opme': 0, 'custo_total_opme': None}
  SQL: `SELECT SUM(COALESCE(val_ortp, 0)) AS custo_total_ortopedia
FROM processed
JOIN enriched e ON processed.row_id = e.row_id...`
- **Q36** (Financeira): got=2795569.4899999984 exp=30640001.52
  SQL: `SELECT SUM(COALESCE(val_sp, 0)) AS total_honorarios_profissionais
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem =...`
- **Q37** (Financeira): got=391.5581875175 exp=432.54
  SQL: `SELECT 
    COALESCE(SUM(custo_total) / SUM(qt_diarias), 0) AS custo_medio_por_dia
FROM 
    processed
JOIN 
    enriche...`
- **Q38** (Financeira): Linha 1: got={'cod_munic_estabelecimento': '350000', 'total_custo': 6704942.199999998} exp={'cnes_estabelecimento': '2078015', 'internacoes': 2221, 'custo_total': 11218869.54}
  SQL: `SELECT 
    cod_munic_estabelecimento, 
    SUM(COALESCE(custo_total, 0)) AS total_custo
FROM 
    processed
JOIN 
    e...`
- **Q39** (Financeira): got=1945222.8200000036 exp=8721217.91
  SQL: `SELECT SUM(COALESCE(custo_total, 0)) AS total_custo
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND an...`
- **Q40** (Financeira): Linha 1: got={'proporcao_custos': 4.0044808723} exp={'custo_servico_hospitalar': 144636796.74, 'honorarios_profissionais': 30640001.52, 'pct_hospitalar': 82.5}
  SQL: `SELECT 
    COALESCE(SUM(val_sh), 0) / COALESCE(SUM(val_sp), 1) AS proporcao_custos
FROM 
    processed
JOIN 
    enrich...`
- **Q41** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'internacoes_ortopédicas': 476} exp={'mes_cmpt': 1, 'internacoes': 5209}
  SQL: `SELECT 
    mes_cmpt,
    COUNT(DISTINCT n_aih) AS internacoes_ortopédicas
FROM 
    processed
JOIN 
    enriched e ON p...`
- **Q42** (Temporal/Comparativa): Número de linhas: got=0 exp=12
  SQL: `SELECT 
    mes_cmpt,
    COUNT(*) AS quantidade_procedimentos
FROM 
    processed
WHERE 
    sistema = 'SIA' 
    AND u...`
- **Q44** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'custo_total': 1015180.7699999993} exp={'mes_cmpt': 1, 'custo_total': 12942488.31}
  SQL: `SELECT 
    mes_cmpt,
    SUM(COALESCE(custo_total, 0)) AS custo_total
FROM 
    processed
WHERE 
    sistema = 'SIH'
  ...`
- **Q45** (Temporal/Comparativa): Número de linhas: got=12 exp=24
  SQL: `SELECT 
    mes_cmpt,
    COUNT(DISTINCT CASE WHEN icd_group = 'S00-T98' THEN n_aih END) AS trauma_count,
    COUNT(DIST...`
- **Q46** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'total_obitos': 6} exp={'mes_cmpt': 1, 'total_obitos': 179}
  SQL: `SELECT 
    mes_cmpt,
    COUNT(DISTINCT n_aih) AS total_obitos
FROM 
    processed
JOIN 
    enriched e ON processed.ro...`
- **Q47** (Temporal/Comparativa): Número de linhas: got=12 exp=4
  SQL: `SELECT 
    AVG(dias_perm) AS media_permanencia,
    mes_cmpt AS trimestre
FROM 
    processed
JOIN 
    enriched e ON p...`
- **Q48** (Temporal/Comparativa): got=12 exp=10
  SQL: `WITH orthopedic_internations AS (
    SELECT
        mes_cmpt,
        custo_total
    FROM
        processed
    JOIN
 ...`
- **Q50** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'volume_internacoes': 118} exp={'mes_cmpt': 1, 'internacoes_idosos': 1987}
  SQL: `SELECT 
    mes_cmpt,
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
JOIN 
    enriched e ON proces...`
- **Q51** (Epidemiológica Simples): got=23520 exp=15359
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE s...`
- **Q52** (Epidemiológica Simples): got=36148 exp=13997
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE s...`
- **Q53** (Epidemiológica Simples): got=2330 exp=13928
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE s...`
- **Q54** (Epidemiológica Simples): got=0 exp=45579
  SQL: `SELECT COUNT(*) AS numero_procedimentos
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE processed.si...`
- **Q55** (Epidemiológica Simples): got=0 exp=53666
  SQL: `SELECT COUNT(*) AS numero_procedimentos
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'S...`
- **Q56** (Epidemiológica Simples): got=59668 exp=6261
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE s...`
- **Q57** (Epidemiológica Simples): got=59668 exp=1970
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE s...`
- **Q58** (Epidemiológica Simples): got=59668 exp=1248
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE s...`
- **Q59** (Epidemiológica Simples): got=481 exp=3619
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE s...`
- **Q60** (Epidemiológica Simples): got=1239 exp=15489
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE s...`
- **Q62** (Epidemiológica Simples): got=12217 exp=10944
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE s...`
- **Q63** (Epidemiológica Simples): got=157 exp=975
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE s...`
- **Q64** (Epidemiológica Simples): got=0 exp=59437
  SQL: `SELECT COUNT(*) AS numero_procedimentos
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'S...`
- **Q68** (Epidemiológica Simples): got=0 exp=399
  SQL: `SELECT COUNT(DISTINCT cod_munic_residencia) AS num_municipios_distintos
FROM processed
WHERE sistema = 'SIA'
  AND uf_or...`
- **Q69** (Epidemiológica Complexa): Número de linhas: got=0 exp=5
  SQL: `SELECT 
    cod_munic_residencia,
    COUNT(*) AS volume_procedimentos
FROM 
    processed
WHERE 
    sistema = 'SIA' 
 ...`
- **Q70** (Epidemiológica Complexa): Número de linhas: got=0 exp=3
  SQL: `SELECT 
    FLOOR(idade_paciente / 10) * 10 AS faixa_etaria,
    COUNT(*) AS quantidade_procedimentos
FROM 
    processe...`
- **Q71** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'M869', 'num_obitos': 13} exp={'cid_principal': 'S720', 'total_internacoes': 8980, 'total_obitos': 455.0}
  SQL: `SELECT 
    cid_principal,
    COUNT(DISTINCT n_aih) AS num_obitos
FROM 
    processed
JOIN 
    enriched e ON processed...`
- **Q72** (Epidemiológica Complexa): Linha não encontrada: {'sexo_paciente': 'F', 'permanencia_media': 6.3}
  SQL: `SELECT 
    sexo_paciente,
    AVG(dias_perm) AS media_permanencia
FROM 
    processed
JOIN 
    enriched e ON processed...`
- **Q73** (Epidemiológica Complexa): Número de linhas: got=0 exp=10
  SQL: `SELECT 
    cod_munic_estabelecimento,
    COUNT(*) AS volume_procedimentos
FROM 
    processed
WHERE 
    sistema = 'SI...`
- **Q74** (Epidemiológica Complexa): Linha não encontrada: {'sexo_paciente': 'F', 'total_obitos': 1081.0, 'taxa_mortalidade': 4.18}
  SQL: `SELECT 
    sexo_paciente,
    COUNT(DISTINCT n_aih) AS total_internacoes,
    SUM(CASE WHEN morte = 1 THEN 1 ELSE 0 END...`
- **Q75** (Epidemiológica Complexa): Número de linhas: got=16 exp=6
  SQL: `SELECT 
    icd_group,
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente < 30 THEN '18-...`
- **Q76** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'S063', 'media_permanencia': 16.358974359, 'internacoes': 78} exp={'cid_principal': 'S063', 'internacoes': 78, 'permanencia_media': 16.4}
  SQL: `SELECT 
    cid_principal, 
    AVG(dias_perm) AS media_permanencia, 
    COUNT(DISTINCT n_aih) AS internacoes
FROM 
   ...`
- **Q77** (Epidemiológica Complexa): Linha não encontrada: {'faixa_etaria': '0-17', 'total': 3619}
  SQL: `SELECT 
    CASE 
        WHEN idade_paciente BETWEEN 0 AND 17 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 39...`
- **Q78** (Epidemiológica Complexa): Linha 1: got={'cod_munic_estabelecimento': '350000', 'volume_internacoes': 27024} exp={'cnes_estabelecimento': '2091399', 'internacoes': 816}
  SQL: `SELECT 
    cod_munic_estabelecimento,
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
JOIN 
    enr...`
- **Q79** (Epidemiológica Complexa): Número de linhas: got=10 exp=3
  SQL: `SELECT 
    FLOOR(idade_paciente / 10) * 10 AS faixa_etaria,
    AVG(dias_perm) AS media_permanencia
FROM 
    processed...`
- **Q81** (Epidemiológica Complexa): got=1465 exp=25807
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE s...`
- **Q82** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'M216', 'quantidade': 1596} exp={'cid_principal': 'S720', 'total': 11096}
  SQL: `SELECT 
    cid_principal, 
    COUNT(*) AS quantidade
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e....`
- **Q83** (Epidemiológica Complexa): got=353930 exp=173.1
  SQL: `SELECT 
    cod_munic_estabelecimento,
    COUNT(DISTINCT n_aih) AS internacoes_ortopedicas
FROM 
    processed
JOIN 
  ...`
- **Q84** (Epidemiológica Complexa): Número de linhas: got=5 exp=4
  SQL: `SELECT 
    CASE 
        WHEN dias_perm < 7 THEN 'Menos de 7 dias'
        WHEN dias_perm BETWEEN 7 AND 14 THEN '7 a 14...`
- **Q85** (Financeira): got=4489529.78 exp=78009817.26
  SQL: `SELECT SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.r...`
- **Q87** (Financeira): got=None exp=15904.56
  SQL: `SELECT SUM(COALESCE(val_uti, 0)) AS custo_total_uti
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE ...`
- **Q88** (Financeira): Linha não encontrada: {'sexo_paciente': 'F', 'custo_medio': 2684.14}
  SQL: `SELECT 
    sexo_paciente,
    AVG(custo_total) AS custo_medio_internacao
FROM 
    processed
JOIN 
    enriched e ON pr...`
- **Q89** (Financeira): got=927790.9100000001 exp=7450877.25
  SQL: `SELECT SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.r...`
- **Q90** (Financeira): got=3083.8746901408 exp=4423.19
  SQL: `SELECT 
    AVG(custo_total) AS custo_medio_por_internacao
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_ori...`
- **Q91** (Financeira): Linha 1: got={'cod_munic_residencia': '355030', 'total_custo': 2553035.5} exp={'cod_munic_estabelecimento': '350000', 'custo_total': 91002293.78}
  SQL: `SELECT 
    cod_munic_residencia, 
    SUM(custo_total) AS total_custo
FROM 
    processed
JOIN 
    enriched e ON proce...`
- **Q92** (Financeira): got=11194804.549999993 exp=144636796.74
  SQL: `SELECT SUM(COALESCE(val_sh, 0)) AS total_val_sh
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sist...`
- **Q93** (Financeira): Número de linhas: got=10 exp=3
  SQL: `SELECT 
    FLOOR(idade_paciente / 10) * 10 AS faixa_etaria,
    AVG(custo_total) AS custo_medio_internacao
FROM 
    pr...`
- **Q94** (Financeira): got=1275788.72 exp=37073666.92
  SQL: `SELECT SUM(COALESCE(val_uti, 0)) AS total_uti
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistem...`
- **Q97** (Financeira): Linha 1: got={'cid_principal': 'M518', 'custo_medio': 7827.0964285714, 'quantidade_internacoes': 112} exp={'cid_principal': 'S221', 'internacoes': 41, 'custo_medio': 14053.88}
  SQL: `SELECT 
    cid_principal,
    AVG(custo_total) AS custo_medio,
    COUNT(n_aih) AS quantidade_internacoes
FROM 
    pro...`
- **Q98** (Financeira): Resultado vazio
  SQL: `SELECT 
    cod_procedimento,
    AVG(custo_total) AS custo_medio
FROM 
    processed
WHERE 
    sistema = 'SIA' 
    AN...`
- **Q99** (Financeira): Linha 1: got={'cod_procedimento': '0702100048', 'total_custo': 46893518.21000075} exp={'cod_procedimento': '0701020369', 'custo_total': 1201460.4}
  SQL: `SELECT 
    cod_procedimento, 
    SUM(custo_total) AS total_custo
FROM 
    processed
WHERE 
    sistema = 'SIA' 
    A...`
- **Q100** (Financeira): got=1801433.3399999999 exp=35943333.49
  SQL: `SELECT SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.r...`
- **Q101** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 8, 'num_obitos': 10} exp={'mes_cmpt': 10, 'total_obitos': 224}
  SQL: `SELECT mes_cmpt, COUNT(DISTINCT n_aih) AS num_obitos
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE...`
- **Q102** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'volume_internacoes': 1869} exp={'mes_cmpt': 1, 'internacoes': 1409}
  SQL: `SELECT 
    mes_cmpt,
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
JOIN 
    enriched e ON proces...`
- **Q103** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'custo_total': 287024.49} exp={'mes_cmpt': 1, 'custo_total': 5379118.99}
  SQL: `SELECT 
    mes_cmpt,
    SUM(COALESCE(custo_total, 0)) AS custo_total
FROM 
    processed
JOIN 
    enriched e ON proce...`
- **Q106** (Temporal/Comparativa): Número de linhas: got=0 exp=24
  SQL: `SELECT 
    mes_cmpt,
    sexo_paciente,
    COUNT(*) AS volume_procedimentos
FROM 
    processed
WHERE 
    sistema = '...`
- **Q107** (Temporal/Comparativa): Falha DuckDB: Catalog Error: Scalar Function with name to_date does not exist!
Did you mean "to_days"?

LINE 3:     EXTRACT(QUARTER FROM TO_DATE(dt_inter, 'YYYYMMDD')) AS trimestre,
                                 ^
  SQL: `SELECT 
    icd_group,
    EXTRACT(QUARTER FROM TO_DATE(dt_inter, 'YYYYMMDD')) AS trimestre,
    COUNT(DISTINCT n_aih) A...`
- **Q108** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 4, 'media_permanencia': 7.0149892934} exp={'mes_cmpt': 7, 'permanencia_media': 6.2}
  SQL: `SELECT mes_cmpt, AVG(dias_perm) AS media_permanencia
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE...`
- **Q109** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'mortes': 6, 'total_internacoes': 476, 'taxa_mortalidade': 0.012605042} exp={'mes_cmpt': 1, 'internacoes': 5209, 'obitos': 179.0, 'taxa_mortalidade': 3.44}
  SQL: `SELECT 
    mes_cmpt,
    COUNT(DISTINCT CASE WHEN morte = 1 THEN n_aih END) AS mortes,
    COUNT(DISTINCT n_aih) AS tot...`
- **Q110** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 8, 'volume_internacoes': 2757} exp={'trimestre': 'Q3', 'total': 8047}
  SQL: `SELECT 
    mes_cmpt,
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
JOIN 
    enriched e ON proces...`
- **Q111** (Temporal/Comparativa): Número de linhas: got=12 exp=2
  SQL: `SELECT 
    mes_cmpt,
    COUNT(DISTINCT n_aih) AS internacoes_ortopedicas
FROM 
    processed
JOIN 
    enriched e ON p...`
- **Q112** (Temporal/Comparativa): Número de linhas: got=1 exp=12
  SQL: `SELECT 
    AVG(COALESCE(custo_total, 0)) AS custo_medio_mensal
FROM 
    processed
JOIN 
    enriched e ON processed.ro...`
- **Q113** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'sexo_paciente': 'F', 'volume_internacoes': 180} exp={'mes_cmpt': 1, 'sexo_paciente': 'F', 'total': 2058}
  SQL: `SELECT 
    mes_cmpt,
    sexo_paciente,
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
JOIN 
    e...`
- **Q114** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1} exp={'mes_cmpt': 1, 'total': 5898}
  SQL: `SELECT mes_cmpt
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIA'
  AND uf_origem = 'S...`
- **Q115** (Temporal/Comparativa): Número de linhas: got=0 exp=4
  SQL: `SELECT 
    ano_cmpt,
    mes_cmpt,
    COUNT(*) AS volume_procedimentos
FROM 
    processed
WHERE 
    sistema = 'SIA' ...`
- **Q116** (Temporal/Comparativa): Número de linhas: got=0 exp=12
  SQL: `SELECT 
    mes_cmpt,
    SUM(COALESCE(custo_total, 0)) AS custo_total
FROM 
    processed
WHERE 
    sistema = 'SIA' 
 ...`
- **Q117** (Temporal/Comparativa): Número de linhas: got=12 exp=2
  SQL: `SELECT 
    mes_cmpt,
    SUM(custo_total) AS custo_total
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_orig...`
- **Q118** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'media_permanencia': 5.6021550813} exp={'mes_cmpt': 1, 'permanencia_media': 6.1}
  SQL: `SELECT 
    mes_cmpt,
    AVG(dias_perm) AS media_permanencia
FROM 
    processed
JOIN 
    enriched e ON processed.row_...`

---
_Gerado por scripts/evaluate_benchmark.py — SUS Data RAG — USF/Mestrado_