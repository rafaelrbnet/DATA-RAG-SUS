# Benchmark Evaluation Report — SUS Data RAG

**Model:** openai  
**Run date:** 2026-08-23 00:39  
**API:** http://127.0.0.1:8000  

## Summary

| Metric | Value |
|---|---|
| Total queries | 118 |
| Correct (EA numerator) | 31 |
| **Execution Accuracy (EA)** | **26.3%** |
| Wilson IC 95% | [19.2%, 34.9%] |
| Correct via flexible match (alias diferente) | 25 |
| API errors / timeouts | 0 |
| Mean Time-to-Answer | 3.6s |

> **Nota metodológica — Scoring:** A Execution Accuracy avalia se o SQL gerado
> retorna os mesmos **valores** que o gold-standard, independente do nome das colunas.
> Quando o LLM usa um alias diferente mas retorna os dados corretos (match posicional),
> a query é contada como correta e sinalizada com `~`. Esta decisão segue a definição
> de EA em [Lee et al., 2022] — comparação de conjuntos de resultado, não de SQL texto.

## EA by Category

| Category | Correct | Total | EA |
|---|---:|---:|---:|
| Epidemiológica Simples | 17 | 33 | 51.5% |
| Epidemiológica Complexa | 2 | 31 | 6.5% |
| Financeira | 5 | 26 | 19.2% |
| Temporal/Comparativa | 7 | 28 | 25.0% |

## Query Results

### ✅ Q01 — Epidemiológica Simples

**Q:** Total de internações ortopédicas (M00-M99 e S00-S99) em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes_ortopedicas
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
```

**Resultado:** `got=35658 exp=35658`
**TTA:** 3639ms

---

### ❌ Q02 — Epidemiológica Simples

**Q:** Total de procedimentos ambulatoriais ortopédicos em RJ em 2023

**SQL gerado:**
```sql
SELECT SUM(pa_qtdapr) AS total_procedimentos_ambulatoriais_ortopedicos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=4423.0 exp=33877`
**TTA:** 2163ms

---

### ✅ Q03 — Epidemiológica Simples

**Q:** Total de internações por fratura de fêmur (S72) em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
```

**Resultado:** `got=10668 exp=10668`
**TTA:** 1874ms

---

### ✅ Q04 — Epidemiológica Simples

**Q:** Total de internações por osteoartrose (M16, M17) em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'M16%' OR cid_principal LIKE 'M17%')
```

**Resultado:** `got=348 exp=348`
**TTA:** 2018ms

---

### ❌ Q05 — Epidemiológica Simples

**Q:** Número de óbitos em internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(*) AS numero_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND morte = 1
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=23 exp=889`
**TTA:** 2035ms

---

### ✅ Q06 — Epidemiológica Simples

**Q:** Total de internações por traumatismos (S00-T98) em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'S%' OR icd_group = 'S00-T98')
```

**Resultado:** `got=31703 exp=31703`
**TTA:** 1866ms

---

### ✅ Q07 — Epidemiológica Simples

**Q:** Total de internações por doenças osteomusculares (M00-M99) em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND icd_group = 'M00-M99'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
```

**Resultado:** `got=3955 exp=3955`
**TTA:** 1946ms

---

### ❌ Q08 — Epidemiológica Simples

**Q:** Número de mulheres internadas por causa ortopédica em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND sexo_paciente = 'F'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=1648 exp=13285`
**TTA:** 2068ms

---

### ❌ Q09 — Epidemiológica Simples

**Q:** Número de homens internados por causa ortopédica em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS num_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND sexo_paciente = 'M'
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=2307 exp=22373`
**TTA:** 1957ms

---

### ❌ Q10 — Epidemiológica Simples

**Q:** Número de internações ortopédicas de idosos (60 anos ou mais) em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS num_internacoes_ortopedicas_idosos
FROM processed
WHERE sistema = 'SIH'
  AND ano_cmpt = 2023
  AND uf_origem = 'RJ'
  AND idade_paciente >= 60
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=1151 exp=12432`
**TTA:** 2218ms

---

### ❌ Q11 — Epidemiológica Simples

**Q:** Número de municípios distintos com internação ortopédica registrada em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT cod_munic_estabelecimento) 
FROM processed 
WHERE sistema = 'SIH' 
  AND uf_origem = 'RJ' 
  AND ano_cmpt = 2023 
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=45 exp=64`
**TTA:** 1887ms

---

### ❌ Q12 — Epidemiológica Simples

**Q:** Número de estabelecimentos distintos com internação ortopédica em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT cnes_estabelecimento) 
FROM processed 
WHERE sistema = 'SIH' 
  AND uf_origem = 'RJ' 
  AND ano_cmpt = 2023 
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=106 exp=140`
**TTA:** 1863ms

---

### ❌ Q13 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com permanência superior a 7 dias em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) 
FROM processed 
WHERE sistema = 'SIH' 
  AND uf_origem = 'RJ' 
  AND ano_cmpt = 2023 
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99') 
  AND dias_perm > 7
```

**Resultado:** `got=959 exp=10614`
**TTA:** 1908ms

---

### ✅ Q14 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com uso de UTI em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
  AND uti_int_to > 0
```

**Resultado:** `got=1 exp=1`
**TTA:** 1850ms

---

### ✅ Q15 — Epidemiológica Simples

**Q:** Número de internações por fratura de quadril (S72) em pacientes com 70 anos ou mais em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS num_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
  AND idade_paciente >= 70
```

**Resultado:** `got=5480 exp=5480`
**TTA:** 2149ms

---

### ❌ Q16 — Epidemiológica Complexa

**Q:** Top 10 CIDs ortopédicos por volume de internação em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    cid_principal, 
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cid_principal
ORDER BY 
    volume_internacoes DESC
LIMIT 10
```

**Resultado:** `Linha 1: got={'cid_principal': 'M861', 'volume_internacoes': 272} exp={'cid_principal': 'T813', 'total_internacoes': 3702}`
**TTA:** 2413ms

---

### ❌ Q17 — Epidemiológica Complexa

**Q:** Top 5 municípios de RJ com maior número de internações ortopédicas em 2023

**SQL gerado:**
```sql
SELECT 
  p.cod_munic_estabelecimento, 
  COUNT(DISTINCT p.n_aih) AS num_internacoes
FROM 
  processed p
JOIN 
  enriched e ON p.row_id = e.row_id
WHERE 
  p.sistema = 'SIH'
  AND p.uf_origem = 'RJ'
  AND p.ano_cmpt = 2023
  AND (p.cid_principal LIKE 'M%' OR p.icd_group = 'M00-M99')
GROUP BY 
  p.cod_munic_estabelecimento
ORDER BY 
  num_internacoes DESC
LIMIT 5
```

**Resultado:** `Linha 1: got={'cod_munic_estabelecimento': '330000', 'num_internacoes': 2287} exp={'cod_munic_estabelecimento': '330000', 'internacoes': 14154}`
**TTA:** 2995ms

---

### ❌ Q18 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por faixa etária (0-17, 18-59, 60+) em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 59 THEN '18-59'
        ELSE '60+'
    END AS faixa_etaria,
    COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    faixa_etaria
ORDER BY 
    faixa_etaria
```

**Resultado:** `Linha não encontrada: {'faixa_etaria': '0-17', 'total': 3554}`
**TTA:** 2368ms

---

### ❌ Q19 — Epidemiológica Complexa

**Q:** Taxa de mortalidade hospitalar por CID ortopédico (top 5) em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    p.cid_principal,
    COUNT(DISTINCT p.n_aih) AS total_internacoes,
    SUM(p.morte) AS total_obitos,
    (SUM(p.morte) * 100.0 / COUNT(DISTINCT p.n_aih)) AS taxa_mortalidade
FROM 
    processed p
JOIN 
    enriched e ON p.row_id = e.row_id
WHERE 
    p.sistema = 'SIH'
    AND p.uf_origem = 'RJ'
    AND p.ano_cmpt = 2023
    AND (p.cid_principal LIKE 'M%' OR p.icd_group = 'M00-M99')
GROUP BY 
    p.cid_principal
ORDER BY 
    taxa_mortalidade DESC
LIMIT 5
```

**Resultado:** `Linha 1: got={'cid_principal': 'M142', 'total_internacoes': 1, 'total_obitos': 1.0, 'taxa_mortalidade': 100.0} exp={'cid_principal': 'S062', 'total_internacoes': 40, 'total_obitos': 16.0, 'taxa_mortalidade_pct': 40.0}`
**TTA:** 3008ms

---

### ❌ Q20 — Epidemiológica Complexa

**Q:** Top 10 CIDs ambulatoriais ortopédicos por volume de produção em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    cid_principal, 
    COUNT(*) AS volume_producao
FROM 
    processed
WHERE 
    sistema = 'SIA'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cid_principal
ORDER BY 
    volume_producao DESC
LIMIT 10
```

**Resultado:** `Linha 1: got={'cid_principal': 'M216', 'volume_producao': 2453} exp={'cid_principal': 'S781', 'total_procedimentos': 6614}`
**TTA:** 2129ms

---

### ❌ Q21 — Epidemiológica Complexa

**Q:** Proporção de internações ortopédicas por sexo em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    sexo_paciente, 
    COUNT(DISTINCT n_aih) AS total_internacoes,
    COUNT(DISTINCT n_aih) * 1.0 / SUM(COUNT(DISTINCT n_aih)) OVER () AS proporcao
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    sexo_paciente
```

**Resultado:** `Linha não encontrada: {'sexo_paciente': 'M', 'total_internacoes': 22373, 'pct': 62.7}`
**TTA:** 2160ms

---

### ❌ Q22 — Epidemiológica Complexa

**Q:** Número de internações por fratura de fêmur (S72) em idosos (60+) comparado ao total em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    COUNT(DISTINCT CASE WHEN idade_paciente >= 60 THEN n_aih END) AS internacoes_idosos,
    COUNT(DISTINCT n_aih) AS internacoes_totais
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
    AND cid_principal LIKE 'S72%'
```

**Resultado:** `Linha 1: got={'internacoes_idosos': 7134, 'internacoes_totais': 10668} exp={'total_s72': 10668, 's72_idosos': 7134.0, 'pct_idosos': 66.9}`
**TTA:** 2123ms

---

### ❌ Q23 — Epidemiológica Complexa

**Q:** Top 5 CIDs ortopédicos com maior permanência média hospitalar em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    cid_principal, 
    AVG(dias_perm) AS permanencia_media
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cid_principal
ORDER BY 
    permanencia_media DESC
LIMIT 5
```

**Resultado:** `Linha 1: got={'cid_principal': 'M850', 'permanencia_media': 65.0} exp={'cid_principal': 'T845', 'total_internacoes': 76, 'permanencia_media_dias': 19.4}`
**TTA:** 1948ms

---

### ❌ Q24 — Epidemiológica Complexa

**Q:** Top 5 estabelecimentos com maior volume de internação ortopédica em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    p.cnes_estabelecimento, 
    COUNT(DISTINCT p.n_aih) AS volume_internacoes
FROM 
    processed p
JOIN 
    enriched e ON p.row_id = e.row_id
WHERE 
    p.sistema = 'SIH'
    AND p.uf_origem = 'RJ'
    AND p.ano_cmpt = 2023
    AND (p.cid_principal LIKE 'M%' OR p.icd_group = 'M00-M99')
GROUP BY 
    p.cnes_estabelecimento
ORDER BY 
    volume_internacoes DESC
LIMIT 5
```

**Resultado:** `Linha 1: got={'cnes_estabelecimento': '5478898', 'volume_internacoes': 980} exp={'cnes_estabelecimento': '2290227', 'internacoes': 5986}`
**TTA:** 2642ms

---

### ❌ Q25 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por raça/cor do paciente em RJ em 2023

**SQL gerado:**
```sql
SELECT 
  raca_cor_paciente, 
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
  raca_cor_paciente
ORDER BY 
  total_internacoes DESC
```

**Resultado:** `Linha não encontrada: {'raca_cor_paciente': '03', 'total': 21334, 'pct': 59.8}`
**TTA:** 2011ms

---

### ❌ Q26 — Epidemiológica Complexa

**Q:** CIDs ortopédicos com maior número de dias totais de internação em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    cid_principal, 
    SUM(dias_perm) AS total_dias_internacao
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cid_principal
ORDER BY 
    total_dias_internacao DESC
```

**Resultado:** `Número de linhas: got=240 exp=10`
**TTA:** 2209ms

---

### ❌ Q27 — Epidemiológica Complexa

**Q:** Top 5 procedimentos ambulatoriais ortopédicos mais realizados em RJ em 2023

**SQL gerado:**
```sql
SELECT cod_procedimento, SUM(pa_qtdapr) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY cod_procedimento
ORDER BY total_procedimentos DESC
LIMIT 5
```

**Resultado:** `Linha 1: got={'cod_procedimento': '0701010053', 'total_procedimentos': 1341.0} exp={'cod_procedimento': '0302050019', 'total_procedimentos': 15319}`
**TTA:** 2031ms

---

### ❌ Q28 — Epidemiológica Complexa

**Q:** Proporção de internações ortopédicas com paciente de outro município em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    COUNT(DISTINCT p.n_aih) AS total_internacoes,
    COUNT(DISTINCT CASE WHEN e.clinical_deslocamento_territorial = 'deslocamento intermunicipal' THEN p.n_aih END) AS internacoes_deslocamento_intermunicipal,
    COUNT(DISTINCT CASE WHEN e.clinical_deslocamento_territorial = 'deslocamento intermunicipal' THEN p.n_aih END) * 1.0 / COUNT(DISTINCT p.n_aih) AS proporcao_deslocamento_intermunicipal
FROM 
    processed p
JOIN 
    enriched e ON p.row_id = e.row_id
WHERE 
    p.sistema = 'SIH'
    AND p.uf_origem = 'RJ'
    AND p.ano_cmpt = 2023
    AND (p.cid_principal LIKE 'M%' OR p.icd_group = 'M00-M99')
    AND e.clinical_interpretacao_clinica = 'doencas osteomusculares e do tecido conjuntivo'
```

**Resultado:** `Linha 1: got={'total_internacoes': 3955, 'internacoes_deslocamento_intermunicipal': 2778, 'proporcao_deslocamento_intermunicipal': 0.7024020228} exp={'total_internacoes': 35658, 'deslocamento_intermunicipal': 19572.0, 'pct_deslocamento': 54.9}`
**TTA:** 3312ms

---

### ❌ Q29 — Epidemiológica Complexa

**Q:** Comparativo de volume mensal SIA vs SIH ortopédico em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    p.mes_cmpt,
    p.sistema,
    COUNT(*) AS volume
FROM 
    processed p
WHERE 
    p.uf_origem = 'RJ'
    AND p.ano_cmpt = 2023
    AND (p.cid_principal LIKE 'M%' OR p.icd_group = 'M00-M99')
GROUP BY 
    p.mes_cmpt, p.sistema
ORDER BY 
    p.mes_cmpt, p.sistema;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'sistema': 'SIA', 'volume': 270} exp={'mes_cmpt': 1, 'sistema': 'SIA', 'total': 2678}`
**TTA:** 2350ms

---

### ❌ Q30 — Epidemiológica Complexa

**Q:** Idade média dos pacientes internados por fratura de fêmur (S72) em RJ em 2023

**SQL gerado:**
```sql
SELECT AVG(idade_paciente) AS idade_media
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
```

**Resultado:** `Linha 1: got={'idade_media': 63.0353393326} exp={'idade_media': 63.0, 'idade_minima': 0.0, 'idade_maxima': 99.0}`
**TTA:** 1742ms

---

### ❌ Q31 — Financeira

**Q:** Custo total de todas as internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes_ortopedicas
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=13641307.23 exp=92092851.42`
**TTA:** 3708ms

---

### ❌ Q32 — Financeira

**Q:** Custo médio por internação ortopédica em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    COALESCE(SUM(custo_total) / COUNT(DISTINCT n_aih), 0) AS custo_medio_por_internacao
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=3449.1295145386 exp=2581.87`
**TTA:** 4036ms

---

### ✅ Q33 — Financeira

**Q:** Custo total de internações por fratura de fêmur (S72) em RJ em 2023

**SQL gerado:**
```sql
SELECT SUM(COALESCE(custo_total, 0)) AS total_custo_fratura_femur
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
```

**Resultado:** `got=30987118.460000016 exp=30987118.46`
**TTA:** 4611ms

---

### ❌ Q34 — Financeira

**Q:** Top 5 CIDs ortopédicos com maior custo total de internação em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    cid_principal, 
    SUM(custo_total) AS total_custo
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cid_principal
ORDER BY 
    total_custo DESC
LIMIT 5
```

**Resultado:** `Linha 1: got={'cid_principal': 'M511', 'total_custo': 1271678.81} exp={'cid_principal': 'S720', 'internacoes': 3541, 'custo_total': 12139401.8}`
**TTA:** 3551ms

---

### ❌ Q35 — Financeira

**Q:** Custo total de internações ortopédicas com uso de OPME (val_ortp > 0) em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_ortopedicas_opme
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
    AND val_ortp > 0
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `Linha 1: got={'custo_total_ortopedicas_opme': None} exp={'internacoes_com_opme': 0, 'custo_total_opme': None}`
**TTA:** 3927ms

---

### ❌ Q36 — Financeira

**Q:** Valor total de honorários profissionais (val_sp) em internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    COALESCE(SUM(val_sp), 0) AS total_val_sp
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=2288304.3599999994 exp=17470900.35`
**TTA:** 4254ms

---

### ❌ Q37 — Financeira

**Q:** Custo médio por dia de internação ortopédica em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    SUM(custo_total) / SUM(dias_perm) AS custo_medio_por_dia
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
    AND dias_perm > 0
```

**Resultado:** `got=510.3706223176 exp=370.15`
**TTA:** 3915ms

---

### ❌ Q38 — Financeira

**Q:** Top 5 estabelecimentos com maior custo total de internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    p.cnes_estabelecimento, 
    SUM(p.custo_total) AS total_custo
FROM 
    processed p
JOIN 
    enriched e ON p.row_id = e.row_id
WHERE 
    p.sistema = 'SIH' 
    AND p.uf_origem = 'RJ' 
    AND p.ano_cmpt = 2023
    AND (p.cid_principal LIKE 'M%' OR p.icd_group = 'M00-M99')
GROUP BY 
    p.cnes_estabelecimento
ORDER BY 
    total_custo DESC
LIMIT 5
```

**Resultado:** `Linha 1: got={'cnes_estabelecimento': '5478898', 'total_custo': 2805211.840000001} exp={'cnes_estabelecimento': '2290227', 'internacoes': 5986, 'custo_total': 10961414.37}`
**TTA:** 4772ms

---

### ❌ Q39 — Financeira

**Q:** Custo total de procedimentos ambulatoriais ortopédicos em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_ambulatorial_ortopedico
FROM 
    processed
WHERE 
    sistema = 'SIA'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=1188967.7999999982 exp=7729430.04`
**TTA:** 3407ms

---

### ❌ Q40 — Financeira

**Q:** Proporção custo serviço hospitalar vs honorários em internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    SUM(COALESCE(val_sh, 0)) / NULLIF(SUM(COALESCE(val_sp, 0)), 0) AS proporcao_custo_sh_vs_honorarios
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `Linha 1: got={'proporcao_custo_sh_vs_honorarios': 4.470362474} exp={'custo_servico_hospitalar': 73139419.49, 'honorarios_profissionais': 17470900.35, 'pct_hospitalar': 80.7}`
**TTA:** 4463ms

---

### ❌ Q41 — Temporal/Comparativa

**Q:** Distribuição mensal de internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt, 
    COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'total_internacoes': 226} exp={'mes_cmpt': 1, 'total_internacoes': 2581}`
**TTA:** 3921ms

---

### ❌ Q42 — Temporal/Comparativa

**Q:** Distribuição mensal de procedimentos ambulatoriais ortopédicos em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt, 
    COUNT(*) AS total_procedimentos
FROM 
    processed
WHERE 
    sistema = 'SIA'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'total_procedimentos': 270} exp={'mes_cmpt': 1, 'procedimentos': 2678}`
**TTA:** 4175ms

---

### ✅ Q43 — Temporal/Comparativa

**Q:** Mês com maior número de internações por fratura de fêmur (S72) em RJ em 2023

**SQL gerado:**
```sql
SELECT mes_cmpt, COUNT(DISTINCT n_aih) AS num_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
GROUP BY mes_cmpt
ORDER BY num_internacoes DESC
LIMIT 1
```

**Resultado:** `got=7 exp=7`
**TTA:** 3913ms

---

### ❌ Q44 — Temporal/Comparativa

**Q:** Evolução mensal do custo total de internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt, 
    SUM(COALESCE(custo_total, 0)) AS total_custo
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'total_custo': 692777.4} exp={'mes_cmpt': 1, 'custo_total': 6644572.4}`
**TTA:** 4315ms

---

### ❌ Q45 — Temporal/Comparativa

**Q:** Volume mensal de internações por trauma (S00-T98) vs doenças musculoesqueléticas (M00-M99) em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    SUM(CASE WHEN icd_group = 'S00-T98' THEN 1 ELSE 0 END) AS internacoes_trauma,
    SUM(CASE WHEN icd_group = 'M00-M99' THEN 1 ELSE 0 END) AS internacoes_musculoesqueleticas
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt
```

**Resultado:** `Número de linhas: got=12 exp=24`
**TTA:** 4219ms

---

### ❌ Q46 — Temporal/Comparativa

**Q:** Evolução mensal de óbitos em internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt, 
    COUNT(DISTINCT n_aih) AS total_obitos
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
    AND morte = 1
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt
```

**Resultado:** `Número de linhas: got=9 exp=12`
**TTA:** 3963ms

---

### ❌ Q47 — Temporal/Comparativa

**Q:** Permanência média por trimestre nas internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    CEIL(mes_cmpt / 3.0) AS trimestre,
    AVG(dias_perm) AS permanencia_media
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    trimestre
ORDER BY 
    trimestre
```

**Resultado:** `Linha não encontrada: {'trimestre': 'Q1', 'permanencia_media_dias': 6.9}`
**TTA:** 4316ms

---

### ❌ Q48 — Temporal/Comparativa

**Q:** Mês com maior custo médio por internação ortopédica em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt, 
    AVG(custo_total) AS custo_medio
FROM 
    processed
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'RJ' 
    AND ano_cmpt = 2023 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    custo_medio DESC
LIMIT 1
```

**Resultado:** `got=11 exp=12`
**TTA:** 4005ms

---

### ✅ Q49 — Temporal/Comparativa

**Q:** Sazonalidade mensal de fraturas de fêmur (S72) em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt, 
    COUNT(DISTINCT n_aih) AS total_fraturas_femur
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
    AND cid_principal LIKE 'S72%'
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 4131ms

---

### ❌ Q50 — Temporal/Comparativa

**Q:** Volume mensal de internações ortopédicas de idosos (60 anos ou mais) em RJ em 2023

**SQL gerado:**
```sql
SELECT 
  mes_cmpt, 
  COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND ano_cmpt = 2023
  AND uf_origem = 'RJ'
  AND idade_paciente >= 60
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
  mes_cmpt
ORDER BY 
  mes_cmpt
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'volume_internacoes': 65} exp={'mes_cmpt': 1, 'total_internacoes': 888}`
**TTA:** 4101ms

---

### ✅ Q51 — Epidemiológica Simples

**Q:** Número de internações por fratura de fêmur (S72) em mulheres em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND cid_principal LIKE 'S72%'
  AND sexo_paciente = 'F'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
```

**Resultado:** `got=5697 exp=5697`
**TTA:** 3927ms

---

### ✅ Q52 — Epidemiológica Simples

**Q:** Número de internações por fratura de fêmur (S72) em homens em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS num_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND cid_principal LIKE 'S72%'
  AND sexo_paciente = 'M'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
```

**Resultado:** `got=4971 exp=4971`
**TTA:** 3989ms

---

### ❌ Q53 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com permanência de 1 dia ou menos em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS num_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
  AND dias_perm <= 1
```

**Resultado:** `got=558 exp=7210`
**TTA:** 3934ms

---

### ❌ Q54 — Epidemiológica Simples

**Q:** Número de procedimentos ambulatoriais ortopédicos realizados em mulheres em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(*)
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND sexo_paciente = 'F'
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=2089 exp=15625`
**TTA:** 4051ms

---

### ❌ Q55 — Epidemiológica Simples

**Q:** Número de procedimentos ambulatoriais ortopédicos realizados por idosos (60 anos ou mais) em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(*)
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND idade_paciente >= 60
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=1701 exp=17399`
**TTA:** 4474ms

---

### ✅ Q56 — Epidemiológica Simples

**Q:** Número de internações por fratura de tíbia e perônio (S82) em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS num_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S82%'
```

**Resultado:** `got=4271 exp=4271`
**TTA:** 5909ms

---

### ✅ Q57 — Epidemiológica Simples

**Q:** Número de internações por fratura de antebraço (S52) em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS num_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S52%'
```

**Resultado:** `got=2610 exp=2610`
**TTA:** 1744ms

---

### ✅ Q58 — Epidemiológica Simples

**Q:** Número de internações por fratura de ombro e úmero (S42) em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S42%'
```

**Resultado:** `got=1266 exp=1266`
**TTA:** 4072ms

---

### ❌ Q59 — Epidemiológica Simples

**Q:** Número de internações ortopédicas em crianças (menores de 18 anos) em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND idade_paciente < 18
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=581 exp=3554`
**TTA:** 4426ms

---

### ❌ Q60 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com permanência entre 8 e 30 dias em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) 
FROM processed 
WHERE sistema = 'SIH' 
  AND uf_origem = 'RJ' 
  AND ano_cmpt = 2023 
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99') 
  AND dias_perm BETWEEN 8 AND 30
```

**Resultado:** `got=841 exp=9782`
**TTA:** 3767ms

---

### ✅ Q61 — Epidemiológica Simples

**Q:** Número de internações por artrose de quadril e joelho (M16 e M17) em idosos (60 anos ou mais) em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND idade_paciente >= 60
  AND (cid_principal LIKE 'M16%' OR cid_principal LIKE 'M17%')
```

**Resultado:** `got=244 exp=244`
**TTA:** 4342ms

---

### ✅ Q62 — Epidemiológica Simples

**Q:** Número de internações por fratura de fêmur (S72) em mulheres com 70 anos ou mais em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND cid_principal LIKE 'S72%'
  AND sexo_paciente = 'F'
  AND idade_paciente >= 70
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
```

**Resultado:** `got=4000 exp=4000`
**TTA:** 3913ms

---

### ❌ Q63 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com permanência superior a 30 dias em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) 
FROM processed 
WHERE sistema = 'SIH' 
  AND uf_origem = 'RJ' 
  AND ano_cmpt = 2023 
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99') 
  AND dias_perm > 30
```

**Resultado:** `got=121 exp=835`
**TTA:** 4119ms

---

### ❌ Q64 — Epidemiológica Simples

**Q:** Número de procedimentos ambulatoriais ortopédicos realizados em homens em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(*)
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND sexo_paciente = 'M'
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=1851 exp=18252`
**TTA:** 3899ms

---

### ✅ Q65 — Epidemiológica Simples

**Q:** Número de internações por traumatismos (grupo S00-T98) em idosos (60 anos ou mais) em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND icd_group = 'S00-T98'
  AND idade_paciente >= 60
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
```

**Resultado:** `got=11281 exp=11281`
**TTA:** 4107ms

---

### ✅ Q66 — Epidemiológica Simples

**Q:** Número de internações por doenças osteomusculares (grupo M00-M99) em mulheres em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND e.clinical_interpretacao_clinica = 'doencas osteomusculares e do tecido conjuntivo'
  AND sexo_paciente = 'F'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
```

**Resultado:** `got=1648 exp=1648`
**TTA:** 4862ms

---

### ✅ Q67 — Epidemiológica Simples

**Q:** Número de óbitos em internações por fratura de fêmur (S72) em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(*) AS numero_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
  AND morte = 1
```

**Resultado:** `got=345 exp=345`
**TTA:** 3357ms

---

### ❌ Q68 — Epidemiológica Simples

**Q:** Número de municípios distintos com registro de procedimentos ambulatoriais ortopédicos em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT cod_munic_estabelecimento) AS num_municipios
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=25 exp=80`
**TTA:** 4086ms

---

### ❌ Q69 — Epidemiológica Complexa

**Q:** Top 5 municípios por volume de procedimentos ambulatoriais ortopédicos em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    p.cod_munic_estabelecimento, 
    COUNT(*) AS volume_procedimentos
FROM 
    processed p
WHERE 
    p.sistema = 'SIA'
    AND p.uf_origem = 'RJ'
    AND p.ano_cmpt = 2023
    AND (p.cid_principal LIKE 'M%' OR p.icd_group = 'M00-M99')
GROUP BY 
    p.cod_munic_estabelecimento
ORDER BY 
    volume_procedimentos DESC
LIMIT 5
```

**Resultado:** `Linha 1: got={'cod_munic_estabelecimento': '330330', 'volume_procedimentos': 2243} exp={'cod_munic_estabelecimento': '330455', 'total': 9168}`
**TTA:** 4261ms

---

### ❌ Q70 — Epidemiológica Complexa

**Q:** Distribuição de procedimentos ambulatoriais ortopédicos por faixa etária em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 34 THEN '18-34'
        WHEN idade_paciente BETWEEN 35 AND 49 THEN '35-49'
        WHEN idade_paciente BETWEEN 50 AND 64 THEN '50-64'
        ELSE '65+' 
    END AS faixa_etaria,
    COUNT(*) AS quantidade_procedimentos
FROM 
    processed
WHERE 
    sistema = 'SIA'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    faixa_etaria
ORDER BY 
    faixa_etaria;
```

**Resultado:** `Número de linhas: got=5 exp=3`
**TTA:** 4371ms

---

### ❌ Q71 — Epidemiológica Complexa

**Q:** Top 5 diagnósticos com maior número de óbitos em internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    p.cid_principal, 
    COUNT(*) AS numero_obitos
FROM 
    processed p
JOIN 
    enriched e ON p.row_id = e.row_id
WHERE 
    p.sistema = 'SIH'
    AND p.uf_origem = 'RJ'
    AND p.ano_cmpt = 2023
    AND (p.cid_principal LIKE 'M%' OR p.icd_group = 'M00-M99')
    AND p.morte = 1
GROUP BY 
    p.cid_principal
ORDER BY 
    numero_obitos DESC
LIMIT 5
```

**Resultado:** `Linha 1: got={'cid_principal': 'M869', 'numero_obitos': 5} exp={'cid_principal': 'S720', 'total_internacoes': 3541, 'total_obitos': 123.0}`
**TTA:** 5453ms

---

### ❌ Q72 — Epidemiológica Complexa

**Q:** Permanência hospitalar média por sexo em internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    sexo_paciente, 
    AVG(dias_perm) AS permanencia_media
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    sexo_paciente
```

**Resultado:** `Linha não encontrada: {'sexo_paciente': 'F', 'permanencia_media': 7.6}`
**TTA:** 2757ms

---

### ❌ Q73 — Epidemiológica Complexa

**Q:** Top 10 estabelecimentos por volume de procedimentos ambulatoriais ortopédicos em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    p.cnes_estabelecimento, 
    COUNT(*) AS volume_procedimentos
FROM 
    processed p
WHERE 
    p.sistema = 'SIA'
    AND p.uf_origem = 'RJ'
    AND p.ano_cmpt = 2023
    AND (p.cid_principal LIKE 'M%' OR p.icd_group = 'M00-M99')
GROUP BY 
    p.cnes_estabelecimento
ORDER BY 
    volume_procedimentos DESC
LIMIT 10
```

**Resultado:** `Linha 1: got={'cnes_estabelecimento': '2272997', 'volume_procedimentos': 1144} exp={'cnes_estabelecimento': '2270048', 'total': 6165}`
**TTA:** 6526ms

---

### ❌ Q74 — Epidemiológica Complexa

**Q:** Taxa de mortalidade por sexo em internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    sexo_paciente,
    COUNT(DISTINCT CASE WHEN morte = 1 THEN n_aih END) * 100.0 / COUNT(DISTINCT n_aih) AS taxa_mortalidade
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    sexo_paciente
```

**Resultado:** `Linha não encontrada: {'sexo_paciente': 'F', 'total_obitos': 357.0, 'taxa_mortalidade': 2.69}`
**TTA:** 2139ms

---

### ❌ Q75 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por grupo diagnóstico (M e S) e faixa etária em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 34 THEN '18-34'
        WHEN idade_paciente BETWEEN 35 AND 49 THEN '35-49'
        WHEN idade_paciente BETWEEN 50 AND 64 THEN '50-64'
        ELSE '65+' 
    END AS faixa_etaria,
    icd_group,
    COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
    AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY 
    faixa_etaria, icd_group
ORDER BY 
    faixa_etaria, icd_group;
```

**Resultado:** `Número de linhas: got=10 exp=6`
**TTA:** 4995ms

---

### ❌ Q76 — Epidemiológica Complexa

**Q:** Top 5 diagnósticos de traumatismo com maior permanência média em RJ em 2023 (mínimo 40 internações)

**SQL gerado:**
```sql
SELECT 
    p.cid_principal, 
    COUNT(DISTINCT p.n_aih) AS num_internacoes,
    AVG(p.dias_perm) AS permanencia_media
FROM 
    processed p
JOIN 
    enriched e ON p.row_id = e.row_id
WHERE 
    p.sistema = 'SIH'
    AND p.uf_origem = 'RJ'
    AND p.ano_cmpt = 2023
    AND e.clinical_interpretacao_clinica = 'lesoes e causas traumaticas'
GROUP BY 
    p.cid_principal
HAVING 
    COUNT(DISTINCT p.n_aih) >= 40
ORDER BY 
    permanencia_media DESC
LIMIT 5
```

**Resultado:** `Linha 4: got={'cid_principal': 'T847', 'num_internacoes': 71, 'permanencia_media': 12.2816901408} exp={'cid_principal': 'S062', 'internacoes': 40, 'permanencia_media': 12.3}`
**TTA:** 3754ms

---

### ❌ Q77 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por faixa etária (0-17, 18-39, 40-59, 60-79, 80+) em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 39 THEN '18-39'
        WHEN idade_paciente BETWEEN 40 AND 59 THEN '40-59'
        WHEN idade_paciente BETWEEN 60 AND 79 THEN '60-79'
        ELSE '80+'
    END AS faixa_etaria,
    COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    faixa_etaria
ORDER BY 
    faixa_etaria
```

**Resultado:** `Linha não encontrada: {'faixa_etaria': '0-17', 'total': 3554}`
**TTA:** 4227ms

---

### ✅ Q78 — Epidemiológica Complexa

**Q:** Top 5 estabelecimentos com maior volume de internações por fratura de fêmur (S72) em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    p.cnes_estabelecimento, 
    COUNT(DISTINCT p.n_aih) AS total_internacoes
FROM 
    processed p
JOIN 
    enriched e ON p.row_id = e.row_id
WHERE 
    p.sistema = 'SIH'
    AND p.uf_origem = 'RJ'
    AND p.ano_cmpt = 2023
    AND p.cid_principal LIKE 'S72%'
GROUP BY 
    p.cnes_estabelecimento
ORDER BY 
    total_internacoes DESC
LIMIT 5
```

**Resultado:** `5 linhas corretas (flexível)`
**TTA:** 4080ms

---

### ❌ Q79 — Epidemiológica Complexa

**Q:** Permanência hospitalar média por faixa etária em internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 34 THEN '18-34'
        WHEN idade_paciente BETWEEN 35 AND 49 THEN '35-49'
        WHEN idade_paciente BETWEEN 50 AND 64 THEN '50-64'
        ELSE '65+' 
    END AS faixa_etaria,
    AVG(dias_perm) AS permanencia_media
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    faixa_etaria
ORDER BY 
    faixa_etaria;
```

**Resultado:** `Número de linhas: got=5 exp=3`
**TTA:** 3941ms

---

### ✅ Q80 — Epidemiológica Complexa

**Q:** Top 5 diagnósticos de doença osteomuscular (M00-M99) por número de internações em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    main_icd AS diagnostico,
    COUNT(DISTINCT n_aih) AS numero_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
    AND icd_group = 'M00-M99'
GROUP BY 
    main_icd
ORDER BY 
    numero_internacoes DESC
LIMIT 5
```

**Resultado:** `5 linhas corretas (flexível)`
**TTA:** 3841ms

---

### ❌ Q81 — Epidemiológica Complexa

**Q:** Número de internações ortopédicas com permanência entre 3 e 7 dias em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) 
FROM processed 
WHERE sistema = 'SIH' 
  AND uf_origem = 'RJ' 
  AND ano_cmpt = 2023 
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99') 
  AND dias_perm BETWEEN 3 AND 7
```

**Resultado:** `got=1233 exp=12572`
**TTA:** 4047ms

---

### ❌ Q82 — Epidemiológica Complexa

**Q:** Top 5 diagnósticos ambulatoriais ortopédicos em idosos (60 anos ou mais) em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    main_icd, 
    COUNT(*) AS total
FROM 
    processed
WHERE 
    sistema = 'SIA'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
    AND idade_paciente >= 60
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    main_icd
ORDER BY 
    total DESC
LIMIT 5
```

**Resultado:** `Linha 1: got={'main_icd': 'M216', 'total': 1004} exp={'cid_principal': 'S781', 'total': 3863}`
**TTA:** 4228ms

---

### ❌ Q83 — Epidemiológica Complexa

**Q:** Média de internações ortopédicas por estabelecimento em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    cod_munic_estabelecimento,
    cnes_estabelecimento,
    AVG(internacoes) AS media_internacoes
FROM (
    SELECT 
        cod_munic_estabelecimento,
        cnes_estabelecimento,
        COUNT(DISTINCT n_aih) AS internacoes
    FROM 
        processed
    WHERE 
        sistema = 'SIH'
        AND uf_origem = 'RJ'
        AND ano_cmpt = 2023
        AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
    GROUP BY 
        cod_munic_estabelecimento,
        cnes_estabelecimento
) subquery
GROUP BY 
    cod_munic_estabelecimento,
    cnes_estabelecimento
```

**Resultado:** `got=330330 exp=254.7`
**TTA:** 4958ms

---

### ❌ Q84 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por faixa de permanência em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    CASE 
        WHEN dias_perm <= 3 THEN '1-3 dias'
        WHEN dias_perm BETWEEN 4 AND 7 THEN '4-7 dias'
        WHEN dias_perm BETWEEN 8 AND 14 THEN '8-14 dias'
        WHEN dias_perm BETWEEN 15 AND 30 THEN '15-30 dias'
        ELSE '31+ dias'
    END AS faixa_permanencia,
    COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    faixa_permanencia
ORDER BY 
    faixa_permanencia
```

**Resultado:** `Número de linhas: got=5 exp=4`
**TTA:** 3888ms

---

### ❌ Q85 — Financeira

**Q:** Custo total de internações ortopédicas de idosos (60 anos ou mais) em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes_ortopedicas_idosos
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
    AND idade_paciente >= 60
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=4927266.290000001 exp=37226129.45`
**TTA:** 3829ms

---

### ✅ Q86 — Financeira

**Q:** Custo médio por internação por fratura de fêmur (S72) em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    COALESCE(SUM(custo_total) / COUNT(DISTINCT n_aih), 0) AS custo_medio_por_internacao
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
    AND cid_principal LIKE 'S72%'
```

**Resultado:** `got=2904.6792707162 exp=2904.68`
**TTA:** 4091ms

---

### ✅ Q87 — Financeira

**Q:** Custo total de internações ortopédicas com passagem por UTI em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_ortopedicas_uti
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
    AND uti_int_to > 0
```

**Resultado:** `got=18598.76 exp=18598.76`
**TTA:** 4211ms

---

### ❌ Q88 — Financeira

**Q:** Custo médio por internação ortopédica por sexo do paciente em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    sexo_paciente, 
    AVG(custo_total) AS custo_medio
FROM 
    processed
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'RJ' 
    AND ano_cmpt = 2023 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    sexo_paciente
```

**Resultado:** `Linha não encontrada: {'sexo_paciente': 'F', 'custo_medio': 2677.23}`
**TTA:** 3999ms

---

### ❌ Q89 — Financeira

**Q:** Custo total de internações ortopédicas em crianças (menores de 18 anos) em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes_ortopedicas
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
    AND idade_paciente < 18
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=2572882.8799999994 exp=7918775.81`
**TTA:** 3814ms

---

### ❌ Q90 — Financeira

**Q:** Custo médio por internação ortopédica com permanência superior a 7 dias em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    COALESCE(SUM(custo_total) / COUNT(DISTINCT n_aih), 0) AS custo_medio
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
    AND dias_perm > 7
```

**Resultado:** `got=4850.9691553702 exp=3944.5`
**TTA:** 4747ms

---

### ❌ Q91 — Financeira

**Q:** Top 3 municípios com maior custo total em internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    p.cod_munic_estabelecimento, 
    SUM(p.custo_total) AS total_custo
FROM 
    processed p
JOIN 
    enriched e ON p.row_id = e.row_id
WHERE 
    p.sistema = 'SIH' 
    AND p.uf_origem = 'RJ' 
    AND p.ano_cmpt = 2023
    AND (p.cid_principal LIKE 'M%' OR p.icd_group = 'M00-M99')
GROUP BY 
    p.cod_munic_estabelecimento
ORDER BY 
    total_custo DESC
LIMIT 3
```

**Resultado:** `Linha 1: got={'cod_munic_estabelecimento': '330000', 'total_custo': 7541506.2700000005} exp={'cod_munic_estabelecimento': '330000', 'custo_total': 38810304.49}`
**TTA:** 4376ms

---

### ❌ Q92 — Financeira

**Q:** Valor total de serviço hospitalar em internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT COALESCE(SUM(val_sh), 0) AS total_valor_servico_hospitalar
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=10229549.940000001 exp=73139419.49`
**TTA:** 3304ms

---

### ❌ Q93 — Financeira

**Q:** Custo médio por internação ortopédica por faixa etária em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 34 THEN '18-34'
        WHEN idade_paciente BETWEEN 35 AND 49 THEN '35-49'
        WHEN idade_paciente BETWEEN 50 AND 64 THEN '50-64'
        ELSE '65+' 
    END AS faixa_etaria,
    COALESCE(SUM(custo_total) / COUNT(DISTINCT n_aih), 0) AS custo_medio
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    faixa_etaria
ORDER BY 
    faixa_etaria
```

**Resultado:** `Número de linhas: got=5 exp=3`
**TTA:** 4833ms

---

### ❌ Q94 — Financeira

**Q:** Valor total de UTI em internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    COALESCE(SUM(val_uti), 0) AS total_valor_uti
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=1201622.28 exp=16618123.58`
**TTA:** 3600ms

---

### ✅ Q95 — Financeira

**Q:** Custo total de internações por doenças osteomusculares (M00-M99) em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND icd_group = 'M00-M99'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
```

**Resultado:** `got=13641307.230000002 exp=13641307.23`
**TTA:** 5472ms

---

### ✅ Q96 — Financeira

**Q:** Custo total de internações por traumatismos (S00-T98) em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    SUM(COALESCE(custo_total, 0)) AS total_custo_traumatismos
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'S%' OR icd_group = 'S00-T98')
```

**Resultado:** `got=78451544.19000013 exp=78451544.19`
**TTA:** 2625ms

---

### ❌ Q97 — Financeira

**Q:** Top 5 diagnósticos com maior custo médio por internação ortopédica em RJ em 2023 (mínimo 40 internações)

**SQL gerado:**
```sql
SELECT 
    p.cid_principal, 
    COUNT(DISTINCT p.n_aih) AS num_internacoes,
    SUM(p.custo_total) / COUNT(DISTINCT p.n_aih) AS custo_medio
FROM 
    processed p
WHERE 
    p.sistema = 'SIH'
    AND p.uf_origem = 'RJ'
    AND p.ano_cmpt = 2023
    AND (p.cid_principal LIKE 'M%' OR p.icd_group = 'M00-M99')
GROUP BY 
    p.cid_principal
HAVING 
    COUNT(DISTINCT p.n_aih) >= 40
ORDER BY 
    custo_medio DESC
LIMIT 5
```

**Resultado:** `Linha 3: got={'cid_principal': 'M511', 'num_internacoes': 159, 'custo_medio': 7997.9799371069} exp={'cid_principal': 'S062', 'internacoes': 40, 'custo_medio': 9846.69}`
**TTA:** 4573ms

---

### ❌ Q98 — Financeira

**Q:** Custo médio por procedimento ambulatorial ortopédico em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    AVG(custo_total) AS custo_medio
FROM 
    processed
WHERE 
    sistema = 'SIA'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=301.7684771574 exp=228.16`
**TTA:** 3786ms

---

### ❌ Q99 — Financeira

**Q:** Top 5 procedimentos ambulatoriais com maior custo total em RJ em 2023

**SQL gerado:**
```sql
SELECT cod_procedimento, SUM(custo_total) AS total_custo
FROM processed
WHERE sistema = 'SIA' AND uf_origem = 'RJ' AND ano_cmpt = 2023
GROUP BY cod_procedimento
ORDER BY total_custo DESC
LIMIT 5
```

**Resultado:** `Linha 1: got={'cod_procedimento': '0702100048', 'total_custo': 8007574.480000029} exp={'cod_procedimento': '0701010029', 'custo_total': 2349402.8}`
**TTA:** 3874ms

---

### ❌ Q100 — Financeira

**Q:** Custo total de internações ortopédicas de idosos (60+) com permanência superior a 7 dias em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
    AND idade_paciente >= 60
    AND dias_perm > 7
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=1850483.6900000002 exp=19953660.54`
**TTA:** 4859ms

---

### ❌ Q101 — Temporal/Comparativa

**Q:** Mês com maior número de óbitos em internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT mes_cmpt, COUNT(*) AS num_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND morte = 1
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY mes_cmpt
ORDER BY num_obitos DESC
LIMIT 1
```

**Resultado:** `Linha 1: got={'mes_cmpt': 7, 'num_obitos': 5} exp={'mes_cmpt': 5, 'total_obitos': 88}`
**TTA:** 3587ms

---

### ✅ Q102 — Temporal/Comparativa

**Q:** Volume mensal de internações por fratura de fêmur (S72) em idosos (60 anos ou mais) em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt, 
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND cid_principal LIKE 'S72%'
    AND idade_paciente >= 60
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 3963ms

---

### ❌ Q103 — Temporal/Comparativa

**Q:** Custo mensal de internações ortopédicas de idosos (60 anos ou mais) em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    SUM(COALESCE(custo_total, 0)) AS custo_total_mensal
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
    AND idade_paciente >= 60
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'custo_total_mensal': 239988.57} exp={'mes_cmpt': 1, 'custo_total': 2662554.36}`
**TTA:** 4675ms

---

### ✅ Q104 — Temporal/Comparativa

**Q:** Mês com maior custo total de internações por fratura de fêmur (S72) em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt, 
    SUM(custo_total) AS total_custo
FROM 
    processed
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'RJ' 
    AND ano_cmpt = 2023 
    AND cid_principal LIKE 'S72%'
GROUP BY 
    mes_cmpt
ORDER BY 
    total_custo DESC
LIMIT 1
```

**Resultado:** `1 linhas corretas (flexível)`
**TTA:** 3587ms

---

### ✅ Q105 — Temporal/Comparativa

**Q:** Volume mensal de internações por traumatismos (grupo S00-T98) em RJ em 2023

**SQL gerado:**
```sql
SELECT 
  mes_cmpt, 
  COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND icd_group = 'S00-T98'
GROUP BY 
  mes_cmpt
ORDER BY 
  mes_cmpt
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 5045ms

---

### ❌ Q106 — Temporal/Comparativa

**Q:** Volume mensal de procedimentos ambulatoriais ortopédicos por sexo em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt, 
    sexo_paciente, 
    SUM(pa_qtdapr) AS total_procedimentos
FROM 
    processed
WHERE 
    sistema = 'SIA'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt, 
    sexo_paciente
ORDER BY 
    mes_cmpt, 
    sexo_paciente;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'sexo_paciente': 'F', 'total_procedimentos': 142.0} exp={'mes_cmpt': 1, 'sexo_paciente': 'F', 'total': 1224}`
**TTA:** 3775ms

---

### ❌ Q107 — Temporal/Comparativa

**Q:** Comparativo trimestral de internações ortopédicas por grupo diagnóstico em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    FLOOR((mes_cmpt - 1) / 3) + 1 AS trimestre,
    icd_group,
    COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND ano_cmpt = 2023
    AND uf_origem = 'RJ'
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    trimestre, icd_group
ORDER BY 
    trimestre, icd_group;
```

**Resultado:** `Número de linhas: got=4 exp=8`
**TTA:** 4073ms

---

### ❌ Q108 — Temporal/Comparativa

**Q:** Mês com maior permanência hospitalar média em internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT mes_cmpt, AVG(dias_perm) AS media_permanencia
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY mes_cmpt
ORDER BY media_permanencia DESC
LIMIT 1
```

**Resultado:** `Linha 1: got={'mes_cmpt': 3, 'media_permanencia': 7.8589211618} exp={'mes_cmpt': 10, 'permanencia_media': 7.4}`
**TTA:** 4030ms

---

### ❌ Q109 — Temporal/Comparativa

**Q:** Evolução mensal da taxa de mortalidade em internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    COUNT(DISTINCT CASE WHEN morte = 1 THEN n_aih END) * 100.0 / COUNT(DISTINCT n_aih) AS taxa_mortalidade
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND ano_cmpt = 2023
    AND uf_origem = 'RJ'
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'taxa_mortalidade': 0.0} exp={'mes_cmpt': 1, 'internacoes': 2581, 'obitos': 73.0, 'taxa_mortalidade': 2.83}`
**TTA:** 4316ms

---

### ❌ Q110 — Temporal/Comparativa

**Q:** Trimestre com maior volume de internações por fratura de fêmur (S72) em RJ em 2023

**SQL gerado:**
```sql
SELECT 
  CEIL(mes_cmpt / 3.0) AS trimestre,
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
GROUP BY 
  trimestre
ORDER BY 
  total_internacoes DESC
LIMIT 1
```

**Resultado:** `Linha 1: got={'trimestre': 4.0, 'total_internacoes': 2823} exp={'trimestre': 'Q4', 'total': 2823}`
**TTA:** 4044ms

---

### ❌ Q111 — Temporal/Comparativa

**Q:** Comparativo semestral de internações ortopédicas (1º vs 2º semestre) em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    CASE 
        WHEN mes_cmpt BETWEEN 1 AND 6 THEN '1º Semestre'
        ELSE '2º Semestre'
    END AS semestre,
    COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    semestre
ORDER BY 
    semestre;
```

**Resultado:** `Linha não encontrada: {'semestre': '1S', 'total': 16855}`
**TTA:** 4349ms

---

### ✅ Q112 — Temporal/Comparativa

**Q:** Custo médio mensal por internação por fratura de fêmur (S72) em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    COALESCE(SUM(custo_total) / COUNT(DISTINCT n_aih), 0) AS custo_medio_mensal
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
    AND cid_principal LIKE 'S72%'
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 3795ms

---

### ❌ Q113 — Temporal/Comparativa

**Q:** Volume mensal de internações ortopédicas por sexo do paciente em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    sexo_paciente,
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND ano_cmpt = 2023
    AND uf_origem = 'RJ'
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt, 
    sexo_paciente
ORDER BY 
    mes_cmpt, 
    sexo_paciente;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'sexo_paciente': 'F', 'volume_internacoes': 86} exp={'mes_cmpt': 1, 'sexo_paciente': 'F', 'total': 902}`
**TTA:** 4189ms

---

### ❌ Q114 — Temporal/Comparativa

**Q:** Mês com menor volume de procedimentos ambulatoriais ortopédicos em RJ em 2023

**SQL gerado:**
```sql
SELECT mes_cmpt, SUM(pa_qtdapr) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY mes_cmpt
ORDER BY total_procedimentos ASC
LIMIT 1
```

**Resultado:** `Linha 1: got={'mes_cmpt': 10, 'total_procedimentos': 278.0} exp={'mes_cmpt': 2, 'total': 2297}`
**TTA:** 4409ms

---

### ❌ Q115 — Temporal/Comparativa

**Q:** Volume trimestral de procedimentos ambulatoriais ortopédicos em RJ em 2023

**SQL gerado:**
```sql
SELECT 
  CEIL(mes_cmpt / 3.0) AS trimestre,
  COUNT(*) AS volume_procedimentos
FROM 
  processed
WHERE 
  sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
  trimestre
ORDER BY 
  trimestre
```

**Resultado:** `Linha não encontrada: {'trimestre': 'Q1', 'total': 8006}`
**TTA:** 3646ms

---

### ❌ Q116 — Temporal/Comparativa

**Q:** Custo mensal de procedimentos ambulatoriais ortopédicos em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt, 
    SUM(COALESCE(custo_total, 0)) AS custo_total_mensal
FROM 
    processed
WHERE 
    sistema = 'SIA'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'custo_total_mensal': 76593.4899999998} exp={'mes_cmpt': 1, 'custo_total': 586766.69}`
**TTA:** 4127ms

---

### ❌ Q117 — Temporal/Comparativa

**Q:** Comparativo semestral do custo total de internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    CASE 
        WHEN mes_cmpt BETWEEN 1 AND 6 THEN '1º Semestre'
        WHEN mes_cmpt BETWEEN 7 AND 12 THEN '2º Semestre'
    END AS semestre,
    SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    semestre
ORDER BY 
    semestre
```

**Resultado:** `Linha não encontrada: {'semestre': '1S', 'custo_total': 41433762.95}`
**TTA:** 4265ms

---

### ✅ Q118 — Temporal/Comparativa

**Q:** Evolução mensal da permanência média em internações por fratura de fêmur (S72) em RJ em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    AVG(dias_perm) AS permanencia_media
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2023
    AND cid_principal LIKE 'S72%'
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt
```

**Resultado:** `12 linhas corretas (exato)`
**TTA:** 3811ms

---

## Failures Detail

- **Q02** (Epidemiológica Simples): got=4423.0 exp=33877
  SQL: `SELECT SUM(pa_qtdapr) AS total_procedimentos_ambulatoriais_ortopedicos
FROM processed
WHERE sistema = 'SIA'
  AND uf_ori...`
- **Q05** (Epidemiológica Simples): got=23 exp=889
  SQL: `SELECT COUNT(*) AS numero_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND...`
- **Q08** (Epidemiológica Simples): got=1648 exp=13285
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND sexo_paciente = 'F'
  AND ...`
- **Q09** (Epidemiológica Simples): got=2307 exp=22373
  SQL: `SELECT COUNT(DISTINCT n_aih) AS num_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cm...`
- **Q10** (Epidemiológica Simples): got=1151 exp=12432
  SQL: `SELECT COUNT(DISTINCT n_aih) AS num_internacoes_ortopedicas_idosos
FROM processed
WHERE sistema = 'SIH'
  AND ano_cmpt =...`
- **Q11** (Epidemiológica Simples): got=45 exp=64
  SQL: `SELECT COUNT(DISTINCT cod_munic_estabelecimento) 
FROM processed 
WHERE sistema = 'SIH' 
  AND uf_origem = 'RJ' 
  AND a...`
- **Q12** (Epidemiológica Simples): got=106 exp=140
  SQL: `SELECT COUNT(DISTINCT cnes_estabelecimento) 
FROM processed 
WHERE sistema = 'SIH' 
  AND uf_origem = 'RJ' 
  AND ano_cm...`
- **Q13** (Epidemiológica Simples): got=959 exp=10614
  SQL: `SELECT COUNT(DISTINCT n_aih) 
FROM processed 
WHERE sistema = 'SIH' 
  AND uf_origem = 'RJ' 
  AND ano_cmpt = 2023 
  AN...`
- **Q16** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'M861', 'volume_internacoes': 272} exp={'cid_principal': 'T813', 'total_internacoes': 3702}
  SQL: `SELECT 
    cid_principal, 
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
WHERE 
    sistema = 'SI...`
- **Q17** (Epidemiológica Complexa): Linha 1: got={'cod_munic_estabelecimento': '330000', 'num_internacoes': 2287} exp={'cod_munic_estabelecimento': '330000', 'internacoes': 14154}
  SQL: `SELECT 
  p.cod_munic_estabelecimento, 
  COUNT(DISTINCT p.n_aih) AS num_internacoes
FROM 
  processed p
JOIN 
  enriche...`
- **Q18** (Epidemiológica Complexa): Linha não encontrada: {'faixa_etaria': '0-17', 'total': 3554}
  SQL: `SELECT 
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 59 THEN '18-59...`
- **Q19** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'M142', 'total_internacoes': 1, 'total_obitos': 1.0, 'taxa_mortalidade': 100.0} exp={'cid_principal': 'S062', 'total_internacoes': 40, 'total_obitos': 16.0, 'taxa_mortalidade_pct': 40.0}
  SQL: `SELECT 
    p.cid_principal,
    COUNT(DISTINCT p.n_aih) AS total_internacoes,
    SUM(p.morte) AS total_obitos,
    (SU...`
- **Q20** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'M216', 'volume_producao': 2453} exp={'cid_principal': 'S781', 'total_procedimentos': 6614}
  SQL: `SELECT 
    cid_principal, 
    COUNT(*) AS volume_producao
FROM 
    processed
WHERE 
    sistema = 'SIA'
    AND uf_or...`
- **Q21** (Epidemiológica Complexa): Linha não encontrada: {'sexo_paciente': 'M', 'total_internacoes': 22373, 'pct': 62.7}
  SQL: `SELECT 
    sexo_paciente, 
    COUNT(DISTINCT n_aih) AS total_internacoes,
    COUNT(DISTINCT n_aih) * 1.0 / SUM(COUNT(...`
- **Q22** (Epidemiológica Complexa): Linha 1: got={'internacoes_idosos': 7134, 'internacoes_totais': 10668} exp={'total_s72': 10668, 's72_idosos': 7134.0, 'pct_idosos': 66.9}
  SQL: `SELECT 
    COUNT(DISTINCT CASE WHEN idade_paciente >= 60 THEN n_aih END) AS internacoes_idosos,
    COUNT(DISTINCT n_ai...`
- **Q23** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'M850', 'permanencia_media': 65.0} exp={'cid_principal': 'T845', 'total_internacoes': 76, 'permanencia_media_dias': 19.4}
  SQL: `SELECT 
    cid_principal, 
    AVG(dias_perm) AS permanencia_media
FROM 
    processed
WHERE 
    sistema = 'SIH'
    A...`
- **Q24** (Epidemiológica Complexa): Linha 1: got={'cnes_estabelecimento': '5478898', 'volume_internacoes': 980} exp={'cnes_estabelecimento': '2290227', 'internacoes': 5986}
  SQL: `SELECT 
    p.cnes_estabelecimento, 
    COUNT(DISTINCT p.n_aih) AS volume_internacoes
FROM 
    processed p
JOIN 
    e...`
- **Q25** (Epidemiológica Complexa): Linha não encontrada: {'raca_cor_paciente': '03', 'total': 21334, 'pct': 59.8}
  SQL: `SELECT 
  raca_cor_paciente, 
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
  processed
WHERE 
  sistema = 'SIH'
  ...`
- **Q26** (Epidemiológica Complexa): Número de linhas: got=240 exp=10
  SQL: `SELECT 
    cid_principal, 
    SUM(dias_perm) AS total_dias_internacao
FROM 
    processed
WHERE 
    sistema = 'SIH'
 ...`
- **Q27** (Epidemiológica Complexa): Linha 1: got={'cod_procedimento': '0701010053', 'total_procedimentos': 1341.0} exp={'cod_procedimento': '0302050019', 'total_procedimentos': 15319}
  SQL: `SELECT cod_procedimento, SUM(pa_qtdapr) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'R...`
- **Q28** (Epidemiológica Complexa): Linha 1: got={'total_internacoes': 3955, 'internacoes_deslocamento_intermunicipal': 2778, 'proporcao_deslocamento_intermunicipal': 0.7024020228} exp={'total_internacoes': 35658, 'deslocamento_intermunicipal': 19572.0, 'pct_deslocamento': 54.9}
  SQL: `SELECT 
    COUNT(DISTINCT p.n_aih) AS total_internacoes,
    COUNT(DISTINCT CASE WHEN e.clinical_deslocamento_territori...`
- **Q29** (Epidemiológica Complexa): Linha 1: got={'mes_cmpt': 1, 'sistema': 'SIA', 'volume': 270} exp={'mes_cmpt': 1, 'sistema': 'SIA', 'total': 2678}
  SQL: `SELECT 
    p.mes_cmpt,
    p.sistema,
    COUNT(*) AS volume
FROM 
    processed p
WHERE 
    p.uf_origem = 'RJ'
    AN...`
- **Q30** (Epidemiológica Complexa): Linha 1: got={'idade_media': 63.0353393326} exp={'idade_media': 63.0, 'idade_minima': 0.0, 'idade_maxima': 99.0}
  SQL: `SELECT AVG(idade_paciente) AS idade_media
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2...`
- **Q31** (Financeira): got=13641307.23 exp=92092851.42
  SQL: `SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes_ortopedicas
FROM 
    processed
WHERE 
    sistema ...`
- **Q32** (Financeira): got=3449.1295145386 exp=2581.87
  SQL: `SELECT 
    COALESCE(SUM(custo_total) / COUNT(DISTINCT n_aih), 0) AS custo_medio_por_internacao
FROM 
    processed
WHER...`
- **Q34** (Financeira): Linha 1: got={'cid_principal': 'M511', 'total_custo': 1271678.81} exp={'cid_principal': 'S720', 'internacoes': 3541, 'custo_total': 12139401.8}
  SQL: `SELECT 
    cid_principal, 
    SUM(custo_total) AS total_custo
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND u...`
- **Q35** (Financeira): Linha 1: got={'custo_total_ortopedicas_opme': None} exp={'internacoes_com_opme': 0, 'custo_total_opme': None}
  SQL: `SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_ortopedicas_opme
FROM 
    processed
WHERE 
    sistema = 'SIH'...`
- **Q36** (Financeira): got=2288304.3599999994 exp=17470900.35
  SQL: `SELECT 
    COALESCE(SUM(val_sp), 0) AS total_val_sp
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = ...`
- **Q37** (Financeira): got=510.3706223176 exp=370.15
  SQL: `SELECT 
    SUM(custo_total) / SUM(dias_perm) AS custo_medio_por_dia
FROM 
    processed
WHERE 
    sistema = 'SIH'
    ...`
- **Q38** (Financeira): Linha 1: got={'cnes_estabelecimento': '5478898', 'total_custo': 2805211.840000001} exp={'cnes_estabelecimento': '2290227', 'internacoes': 5986, 'custo_total': 10961414.37}
  SQL: `SELECT 
    p.cnes_estabelecimento, 
    SUM(p.custo_total) AS total_custo
FROM 
    processed p
JOIN 
    enriched e ON...`
- **Q39** (Financeira): got=1188967.7999999982 exp=7729430.04
  SQL: `SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_ambulatorial_ortopedico
FROM 
    processed
WHERE 
    sistema ...`
- **Q40** (Financeira): Linha 1: got={'proporcao_custo_sh_vs_honorarios': 4.470362474} exp={'custo_servico_hospitalar': 73139419.49, 'honorarios_profissionais': 17470900.35, 'pct_hospitalar': 80.7}
  SQL: `SELECT 
    SUM(COALESCE(val_sh, 0)) / NULLIF(SUM(COALESCE(val_sp, 0)), 0) AS proporcao_custo_sh_vs_honorarios
FROM 
   ...`
- **Q41** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'total_internacoes': 226} exp={'mes_cmpt': 1, 'total_internacoes': 2581}
  SQL: `SELECT 
    mes_cmpt, 
    COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
   ...`
- **Q42** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'total_procedimentos': 270} exp={'mes_cmpt': 1, 'procedimentos': 2678}
  SQL: `SELECT 
    mes_cmpt, 
    COUNT(*) AS total_procedimentos
FROM 
    processed
WHERE 
    sistema = 'SIA'
    AND uf_ori...`
- **Q44** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'total_custo': 692777.4} exp={'mes_cmpt': 1, 'custo_total': 6644572.4}
  SQL: `SELECT 
    mes_cmpt, 
    SUM(COALESCE(custo_total, 0)) AS total_custo
FROM 
    processed
WHERE 
    sistema = 'SIH'
 ...`
- **Q45** (Temporal/Comparativa): Número de linhas: got=12 exp=24
  SQL: `SELECT 
    mes_cmpt,
    SUM(CASE WHEN icd_group = 'S00-T98' THEN 1 ELSE 0 END) AS internacoes_trauma,
    SUM(CASE WHE...`
- **Q46** (Temporal/Comparativa): Número de linhas: got=9 exp=12
  SQL: `SELECT 
    mes_cmpt, 
    COUNT(DISTINCT n_aih) AS total_obitos
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND ...`
- **Q47** (Temporal/Comparativa): Linha não encontrada: {'trimestre': 'Q1', 'permanencia_media_dias': 6.9}
  SQL: `SELECT 
    CEIL(mes_cmpt / 3.0) AS trimestre,
    AVG(dias_perm) AS permanencia_media
FROM 
    processed
WHERE 
    si...`
- **Q48** (Temporal/Comparativa): got=11 exp=12
  SQL: `SELECT 
    mes_cmpt, 
    AVG(custo_total) AS custo_medio
FROM 
    processed
WHERE 
    sistema = 'SIH' 
    AND uf_or...`
- **Q50** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'volume_internacoes': 65} exp={'mes_cmpt': 1, 'total_internacoes': 888}
  SQL: `SELECT 
  mes_cmpt, 
  COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND ano_...`
- **Q53** (Epidemiológica Simples): got=558 exp=7210
  SQL: `SELECT COUNT(DISTINCT n_aih) AS num_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cm...`
- **Q54** (Epidemiológica Simples): got=2089 exp=15625
  SQL: `SELECT COUNT(*)
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND sexo_paciente = ...`
- **Q55** (Epidemiológica Simples): got=1701 exp=17399
  SQL: `SELECT COUNT(*)
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND idade_paciente >...`
- **Q59** (Epidemiológica Simples): got=581 exp=3554
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano...`
- **Q60** (Epidemiológica Simples): got=841 exp=9782
  SQL: `SELECT COUNT(DISTINCT n_aih) 
FROM processed 
WHERE sistema = 'SIH' 
  AND uf_origem = 'RJ' 
  AND ano_cmpt = 2023 
  AN...`
- **Q63** (Epidemiológica Simples): got=121 exp=835
  SQL: `SELECT COUNT(DISTINCT n_aih) 
FROM processed 
WHERE sistema = 'SIH' 
  AND uf_origem = 'RJ' 
  AND ano_cmpt = 2023 
  AN...`
- **Q64** (Epidemiológica Simples): got=1851 exp=18252
  SQL: `SELECT COUNT(*)
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND sexo_paciente = ...`
- **Q68** (Epidemiológica Simples): got=25 exp=80
  SQL: `SELECT COUNT(DISTINCT cod_munic_estabelecimento) AS num_municipios
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem ...`
- **Q69** (Epidemiológica Complexa): Linha 1: got={'cod_munic_estabelecimento': '330330', 'volume_procedimentos': 2243} exp={'cod_munic_estabelecimento': '330455', 'total': 9168}
  SQL: `SELECT 
    p.cod_munic_estabelecimento, 
    COUNT(*) AS volume_procedimentos
FROM 
    processed p
WHERE 
    p.sistem...`
- **Q70** (Epidemiológica Complexa): Número de linhas: got=5 exp=3
  SQL: `SELECT 
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 34 THEN '18-34...`
- **Q71** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'M869', 'numero_obitos': 5} exp={'cid_principal': 'S720', 'total_internacoes': 3541, 'total_obitos': 123.0}
  SQL: `SELECT 
    p.cid_principal, 
    COUNT(*) AS numero_obitos
FROM 
    processed p
JOIN 
    enriched e ON p.row_id = e.r...`
- **Q72** (Epidemiológica Complexa): Linha não encontrada: {'sexo_paciente': 'F', 'permanencia_media': 7.6}
  SQL: `SELECT 
    sexo_paciente, 
    AVG(dias_perm) AS permanencia_media
FROM 
    processed
WHERE 
    sistema = 'SIH'
    A...`
- **Q73** (Epidemiológica Complexa): Linha 1: got={'cnes_estabelecimento': '2272997', 'volume_procedimentos': 1144} exp={'cnes_estabelecimento': '2270048', 'total': 6165}
  SQL: `SELECT 
    p.cnes_estabelecimento, 
    COUNT(*) AS volume_procedimentos
FROM 
    processed p
WHERE 
    p.sistema = '...`
- **Q74** (Epidemiológica Complexa): Linha não encontrada: {'sexo_paciente': 'F', 'total_obitos': 357.0, 'taxa_mortalidade': 2.69}
  SQL: `SELECT 
    sexo_paciente,
    COUNT(DISTINCT CASE WHEN morte = 1 THEN n_aih END) * 100.0 / COUNT(DISTINCT n_aih) AS tax...`
- **Q75** (Epidemiológica Complexa): Número de linhas: got=10 exp=6
  SQL: `SELECT 
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 34 THEN '18-34...`
- **Q76** (Epidemiológica Complexa): Linha 4: got={'cid_principal': 'T847', 'num_internacoes': 71, 'permanencia_media': 12.2816901408} exp={'cid_principal': 'S062', 'internacoes': 40, 'permanencia_media': 12.3}
  SQL: `SELECT 
    p.cid_principal, 
    COUNT(DISTINCT p.n_aih) AS num_internacoes,
    AVG(p.dias_perm) AS permanencia_media
...`
- **Q77** (Epidemiológica Complexa): Linha não encontrada: {'faixa_etaria': '0-17', 'total': 3554}
  SQL: `SELECT 
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 39 THEN '18-39...`
- **Q79** (Epidemiológica Complexa): Número de linhas: got=5 exp=3
  SQL: `SELECT 
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 34 THEN '18-34...`
- **Q81** (Epidemiológica Complexa): got=1233 exp=12572
  SQL: `SELECT COUNT(DISTINCT n_aih) 
FROM processed 
WHERE sistema = 'SIH' 
  AND uf_origem = 'RJ' 
  AND ano_cmpt = 2023 
  AN...`
- **Q82** (Epidemiológica Complexa): Linha 1: got={'main_icd': 'M216', 'total': 1004} exp={'cid_principal': 'S781', 'total': 3863}
  SQL: `SELECT 
    main_icd, 
    COUNT(*) AS total
FROM 
    processed
WHERE 
    sistema = 'SIA'
    AND uf_origem = 'RJ'
   ...`
- **Q83** (Epidemiológica Complexa): got=330330 exp=254.7
  SQL: `SELECT 
    cod_munic_estabelecimento,
    cnes_estabelecimento,
    AVG(internacoes) AS media_internacoes
FROM (
    SE...`
- **Q84** (Epidemiológica Complexa): Número de linhas: got=5 exp=4
  SQL: `SELECT 
    CASE 
        WHEN dias_perm <= 3 THEN '1-3 dias'
        WHEN dias_perm BETWEEN 4 AND 7 THEN '4-7 dias'
   ...`
- **Q85** (Financeira): got=4927266.290000001 exp=37226129.45
  SQL: `SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes_ortopedicas_idosos
FROM 
    processed
WHERE 
    s...`
- **Q88** (Financeira): Linha não encontrada: {'sexo_paciente': 'F', 'custo_medio': 2677.23}
  SQL: `SELECT 
    sexo_paciente, 
    AVG(custo_total) AS custo_medio
FROM 
    processed
WHERE 
    sistema = 'SIH' 
    AND ...`
- **Q89** (Financeira): got=2572882.8799999994 exp=7918775.81
  SQL: `SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes_ortopedicas
FROM 
    processed
WHERE 
    sistema ...`
- **Q90** (Financeira): got=4850.9691553702 exp=3944.5
  SQL: `SELECT 
    COALESCE(SUM(custo_total) / COUNT(DISTINCT n_aih), 0) AS custo_medio
FROM 
    processed
WHERE 
    sistema ...`
- **Q91** (Financeira): Linha 1: got={'cod_munic_estabelecimento': '330000', 'total_custo': 7541506.2700000005} exp={'cod_munic_estabelecimento': '330000', 'custo_total': 38810304.49}
  SQL: `SELECT 
    p.cod_munic_estabelecimento, 
    SUM(p.custo_total) AS total_custo
FROM 
    processed p
JOIN 
    enriched...`
- **Q92** (Financeira): got=10229549.940000001 exp=73139419.49
  SQL: `SELECT COALESCE(SUM(val_sh), 0) AS total_valor_servico_hospitalar
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem =...`
- **Q93** (Financeira): Número de linhas: got=5 exp=3
  SQL: `SELECT 
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 34 THEN '18-34...`
- **Q94** (Financeira): got=1201622.28 exp=16618123.58
  SQL: `SELECT 
    COALESCE(SUM(val_uti), 0) AS total_valor_uti
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_orige...`
- **Q97** (Financeira): Linha 3: got={'cid_principal': 'M511', 'num_internacoes': 159, 'custo_medio': 7997.9799371069} exp={'cid_principal': 'S062', 'internacoes': 40, 'custo_medio': 9846.69}
  SQL: `SELECT 
    p.cid_principal, 
    COUNT(DISTINCT p.n_aih) AS num_internacoes,
    SUM(p.custo_total) / COUNT(DISTINCT p....`
- **Q98** (Financeira): got=301.7684771574 exp=228.16
  SQL: `SELECT 
    AVG(custo_total) AS custo_medio
FROM 
    processed
WHERE 
    sistema = 'SIA'
    AND uf_origem = 'RJ'
    ...`
- **Q99** (Financeira): Linha 1: got={'cod_procedimento': '0702100048', 'total_custo': 8007574.480000029} exp={'cod_procedimento': '0701010029', 'custo_total': 2349402.8}
  SQL: `SELECT cod_procedimento, SUM(custo_total) AS total_custo
FROM processed
WHERE sistema = 'SIA' AND uf_origem = 'RJ' AND a...`
- **Q100** (Financeira): got=1850483.6900000002 exp=19953660.54
  SQL: `SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    ...`
- **Q101** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 7, 'num_obitos': 5} exp={'mes_cmpt': 5, 'total_obitos': 88}
  SQL: `SELECT mes_cmpt, COUNT(*) AS num_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 202...`
- **Q103** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'custo_total_mensal': 239988.57} exp={'mes_cmpt': 1, 'custo_total': 2662554.36}
  SQL: `SELECT 
    mes_cmpt,
    SUM(COALESCE(custo_total, 0)) AS custo_total_mensal
FROM 
    processed
WHERE 
    sistema = '...`
- **Q106** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'sexo_paciente': 'F', 'total_procedimentos': 142.0} exp={'mes_cmpt': 1, 'sexo_paciente': 'F', 'total': 1224}
  SQL: `SELECT 
    mes_cmpt, 
    sexo_paciente, 
    SUM(pa_qtdapr) AS total_procedimentos
FROM 
    processed
WHERE 
    sist...`
- **Q107** (Temporal/Comparativa): Número de linhas: got=4 exp=8
  SQL: `SELECT 
    FLOOR((mes_cmpt - 1) / 3) + 1 AS trimestre,
    icd_group,
    COUNT(DISTINCT n_aih) AS total_internacoes
FR...`
- **Q108** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 3, 'media_permanencia': 7.8589211618} exp={'mes_cmpt': 10, 'permanencia_media': 7.4}
  SQL: `SELECT mes_cmpt, AVG(dias_perm) AS media_permanencia
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND a...`
- **Q109** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'taxa_mortalidade': 0.0} exp={'mes_cmpt': 1, 'internacoes': 2581, 'obitos': 73.0, 'taxa_mortalidade': 2.83}
  SQL: `SELECT 
    mes_cmpt,
    COUNT(DISTINCT CASE WHEN morte = 1 THEN n_aih END) * 100.0 / COUNT(DISTINCT n_aih) AS taxa_mor...`
- **Q110** (Temporal/Comparativa): Linha 1: got={'trimestre': 4.0, 'total_internacoes': 2823} exp={'trimestre': 'Q4', 'total': 2823}
  SQL: `SELECT 
  CEIL(mes_cmpt / 3.0) AS trimestre,
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
  processed
WHERE 
  sis...`
- **Q111** (Temporal/Comparativa): Linha não encontrada: {'semestre': '1S', 'total': 16855}
  SQL: `SELECT 
    CASE 
        WHEN mes_cmpt BETWEEN 1 AND 6 THEN '1º Semestre'
        ELSE '2º Semestre'
    END AS semestr...`
- **Q113** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'sexo_paciente': 'F', 'volume_internacoes': 86} exp={'mes_cmpt': 1, 'sexo_paciente': 'F', 'total': 902}
  SQL: `SELECT 
    mes_cmpt,
    sexo_paciente,
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
WHERE 
    ...`
- **Q114** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 10, 'total_procedimentos': 278.0} exp={'mes_cmpt': 2, 'total': 2297}
  SQL: `SELECT mes_cmpt, SUM(pa_qtdapr) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND...`
- **Q115** (Temporal/Comparativa): Linha não encontrada: {'trimestre': 'Q1', 'total': 8006}
  SQL: `SELECT 
  CEIL(mes_cmpt / 3.0) AS trimestre,
  COUNT(*) AS volume_procedimentos
FROM 
  processed
WHERE 
  sistema = 'SI...`
- **Q116** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'custo_total_mensal': 76593.4899999998} exp={'mes_cmpt': 1, 'custo_total': 586766.69}
  SQL: `SELECT 
    mes_cmpt, 
    SUM(COALESCE(custo_total, 0)) AS custo_total_mensal
FROM 
    processed
WHERE 
    sistema = ...`
- **Q117** (Temporal/Comparativa): Linha não encontrada: {'semestre': '1S', 'custo_total': 41433762.95}
  SQL: `SELECT 
    CASE 
        WHEN mes_cmpt BETWEEN 1 AND 6 THEN '1º Semestre'
        WHEN mes_cmpt BETWEEN 7 AND 12 THEN '...`

---
_Gerado por scripts/evaluate_benchmark.py — SUS Data RAG — USF/Mestrado_