# Benchmark Evaluation Report — SUS Data RAG

**Model:** openai  
**Run date:** 2026-08-22 22:14  
**API:** http://127.0.0.1:8000  

## Summary

| Metric | Value |
|---|---|
| Total queries | 118 |
| Correct (EA numerator) | 31 |
| **Execution Accuracy (EA)** | **26.3%** |
| Wilson IC 95% | [19.2%, 34.9%] |
| Correct via flexible match (alias diferente) | 28 |
| API errors / timeouts | 0 |
| Mean Time-to-Answer | 3.6s |

> **Nota metodológica — Scoring:** A Execution Accuracy avalia se o SQL gerado
> retorna os mesmos **valores** que o gold-standard, independente do nome das colunas.
> Quando o LLM usa um alias diferente mas retorna os dados corretos (match posicional),
> a query é contada como correta e sinalizada com `~`. Esta decisão segue a definição
> de EA em [Lee et al., 2022] — comparação de conjuntos de resultado, não de SQL texto.

## EA by Category

| Category | Correct | Total | EA |
|---|---:|---:|---:|
| Epidemiológica Simples | 16 | 33 | 48.5% |
| Epidemiológica Complexa | 3 | 31 | 9.7% |
| Financeira | 4 | 26 | 15.4% |
| Temporal/Comparativa | 8 | 28 | 28.6% |

## Query Results

### ✅ Q01 — Epidemiológica Simples

**Q:** Total de internações ortopédicas (M00-M99 e S00-S99) em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
```

**Resultado:** `got=65970 exp=65970`
**TTA:** 5273ms

---

### ❌ Q02 — Epidemiológica Simples

**Q:** Total de procedimentos ambulatoriais ortopédicos em SP em 2022

**SQL gerado:**
```sql
SELECT SUM(pa_qtdapr) AS total_procedimentos_ambulatoriais_ortopedicos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=11388.0 exp=105016`
**TTA:** 2097ms

---

### ✅ Q03 — Epidemiológica Simples

**Q:** Total de internações por fratura de fêmur (S72) em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND cid_principal LIKE 'S72%'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
```

**Resultado:** `got=29356 exp=29356`
**TTA:** 2138ms

---

### ✅ Q04 — Epidemiológica Simples

**Q:** Total de internações por osteoartrose (M16, M17) em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M16%' OR cid_principal LIKE 'M17%')
```

**Resultado:** `got=156 exp=156`
**TTA:** 2084ms

---

### ❌ Q05 — Epidemiológica Simples

**Q:** Número de óbitos em internações ortopédicas em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(*) AS numero_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND morte = 1
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=73 exp=2223`
**TTA:** 2161ms

---

### ✅ Q06 — Epidemiológica Simples

**Q:** Total de internações por traumatismos (S00-T98) em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'S%' OR icd_group = 'S00-T98')
```

**Resultado:** `got=59668 exp=59668`
**TTA:** 2335ms

---

### ✅ Q07 — Epidemiológica Simples

**Q:** Total de internações por doenças osteomusculares (M00-M99) em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND icd_group = 'M00-M99'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
```

**Resultado:** `got=6302 exp=6302`
**TTA:** 1762ms

---

### ❌ Q08 — Epidemiológica Simples

**Q:** Número de mulheres internadas por causa ortopédica em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND sexo_paciente = 'F'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=2327 exp=25847`
**TTA:** 2414ms

---

### ❌ Q09 — Epidemiológica Simples

**Q:** Número de homens internados por causa ortopédica em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS num_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND sexo_paciente = 'M'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=3975 exp=40123`
**TTA:** 2150ms

---

### ❌ Q10 — Epidemiológica Simples

**Q:** Número de internações ortopédicas de idosos (60 anos ou mais) em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND ano_cmpt = 2022
  AND uf_origem = 'SP'
  AND idade_paciente >= 60
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=1685 exp=26666`
**TTA:** 2025ms

---

### ❌ Q11 — Epidemiológica Simples

**Q:** Número de municípios distintos com internação ortopédica registrada em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT cod_munic_estabelecimento) 
FROM processed 
WHERE sistema = 'SIH' 
  AND uf_origem = 'SP' 
  AND ano_cmpt = 2022 
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=158 exp=211`
**TTA:** 1816ms

---

### ❌ Q12 — Epidemiológica Simples

**Q:** Número de estabelecimentos distintos com internação ortopédica em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT cnes_estabelecimento) 
FROM processed 
WHERE sistema = 'SIH' 
  AND uf_origem = 'SP' 
  AND ano_cmpt = 2022 
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=293 exp=381`
**TTA:** 2070ms

---

### ❌ Q13 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com permanência superior a 7 dias em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) 
FROM processed 
WHERE sistema = 'SIH' 
  AND uf_origem = 'SP' 
  AND ano_cmpt = 2022 
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99') 
  AND dias_perm > 7
```

**Resultado:** `got=1383 exp=16451`
**TTA:** 2074ms

---

### ❌ Q14 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com uso de UTI em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS num_internacoes_ortopedicas_uti
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
  AND uti_int_to > 0
```

**Resultado:** `got=0 exp=3`
**TTA:** 2005ms

---

### ✅ Q15 — Epidemiológica Simples

**Q:** Número de internações por fratura de quadril (S72) em pacientes com 70 anos ou mais em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND cid_principal LIKE 'S72%'
  AND idade_paciente >= 70
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
```

**Resultado:** `got=15119 exp=15119`
**TTA:** 1952ms

---

### ❌ Q16 — Epidemiológica Complexa

**Q:** Top 10 CIDs ortopédicos por volume de internação em SP em 2022

**SQL gerado:**
```sql
SELECT 
    cid_principal, 
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cid_principal
ORDER BY 
    volume_internacoes DESC
LIMIT 10
```

**Resultado:** `Linha 1: got={'cid_principal': 'M861', 'volume_internacoes': 912} exp={'cid_principal': 'S720', 'total': 8980}`
**TTA:** 2038ms

---

### ❌ Q17 — Epidemiológica Complexa

**Q:** Top 5 municípios de SP com maior número de internações ortopédicas em 2022

**SQL gerado:**
```sql
SELECT 
    p.cod_munic_estabelecimento, 
    COUNT(DISTINCT p.n_aih) AS num_internacoes
FROM 
    processed p
JOIN 
    enriched e ON p.row_id = e.row_id
WHERE 
    p.sistema = 'SIH'
    AND p.uf_origem = 'SP'
    AND p.ano_cmpt = 2022
    AND (p.cid_principal LIKE 'M%' OR p.icd_group = 'M00-M99')
GROUP BY 
    p.cod_munic_estabelecimento
ORDER BY 
    num_internacoes DESC
LIMIT 5
```

**Resultado:** `Linha 1: got={'cod_munic_estabelecimento': '350000', 'num_internacoes': 2800} exp={'cod_munic_estabelecimento': '350000', 'total': 29824}`
**TTA:** 3083ms

---

### ❌ Q18 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por faixa etária (0-17, 18-59, 60+) em SP em 2022

**SQL gerado:**
```sql
SELECT 
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 59 THEN '18-59'
        ELSE '60+'
    END AS faixa_etaria,
    COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    faixa_etaria
ORDER BY 
    faixa_etaria
```

**Resultado:** `Linha não encontrada: {'faixa_etaria': '0-17', 'total': 3619}`
**TTA:** 2882ms

---

### ❌ Q19 — Epidemiológica Complexa

**Q:** Taxa de mortalidade hospitalar por CID ortopédico (top 5) em SP em 2022

**SQL gerado:**
```sql
SELECT 
    cid_principal, 
    COUNT(DISTINCT n_aih) AS total_internacoes, 
    SUM(morte) AS total_obitos, 
    (SUM(morte) * 100.0 / COUNT(DISTINCT n_aih)) AS taxa_mortalidade
FROM 
    processed
WHERE 
    sistema = 'SIH' 
    AND ano_cmpt = 2022 
    AND uf_origem = 'SP' 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cid_principal
ORDER BY 
    taxa_mortalidade DESC
LIMIT 5
```

**Resultado:** `Linha 1: got={'cid_principal': 'M895', 'total_internacoes': 2, 'total_obitos': 1.0, 'taxa_mortalidade': 50.0} exp={'cid_principal': 'S062', 'total_internacoes': 70, 'total_obitos': 21, 'taxa_mortalidade_pct': 30.0}`
**TTA:** 2641ms

---

### ❌ Q20 — Epidemiológica Complexa

**Q:** Top 10 CIDs ambulatoriais ortopédicos por volume de produção em SP em 2022

**SQL gerado:**
```sql
SELECT 
    cid_principal, 
    COUNT(*) AS volume_producao
FROM 
    processed
WHERE 
    sistema = 'SIA'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cid_principal
ORDER BY 
    volume_producao DESC
LIMIT 10
```

**Resultado:** `Linha 1: got={'cid_principal': 'M216', 'volume_producao': 4743} exp={'cid_principal': 'S720', 'total': 16466}`
**TTA:** 2267ms

---

### ❌ Q21 — Epidemiológica Complexa

**Q:** Proporção de internações ortopédicas por sexo em SP em 2022

**SQL gerado:**
```sql
SELECT 
    sexo_paciente, 
    COUNT(DISTINCT n_aih) AS total_internacoes,
    COUNT(DISTINCT n_aih) * 1.0 / SUM(COUNT(DISTINCT n_aih)) OVER () AS proporcao
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    sexo_paciente
```

**Resultado:** `Linha não encontrada: {'sexo_paciente': 'M', 'total': 40123, 'pct': 60.8}`
**TTA:** 2234ms

---

### ❌ Q22 — Epidemiológica Complexa

**Q:** Número de internações por fratura de fêmur (S72) em idosos (60+) comparado ao total em SP em 2022

**SQL gerado:**
```sql
SELECT 
    COUNT(DISTINCT CASE WHEN idade_paciente >= 60 AND cid_principal LIKE 'S72%' THEN n_aih END) AS fraturas_femur_idosos,
    COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
```

**Resultado:** `Linha 1: got={'fraturas_femur_idosos': 19117, 'total_internacoes': 195574} exp={'total_s72': 29356, 's72_idosos': 19120, 'pct_idosos': 65.1}`
**TTA:** 2292ms

---

### ❌ Q23 — Epidemiológica Complexa

**Q:** Top 5 CIDs ortopédicos com maior permanência média hospitalar em SP em 2022

**SQL gerado:**
```sql
SELECT 
    cid_principal, 
    AVG(dias_perm) AS permanencia_media
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cid_principal
ORDER BY 
    permanencia_media DESC
LIMIT 5
```

**Resultado:** `Linha 1: got={'cid_principal': 'M318', 'permanencia_media': 36.0} exp={'cid_principal': 'S063', 'total_internacoes': 78, 'permanencia_media_dias': 16.4}`
**TTA:** 2011ms

---

### ❌ Q24 — Epidemiológica Complexa

**Q:** Top 5 estabelecimentos com maior volume de internação ortopédica em SP em 2022

**SQL gerado:**
```sql
SELECT 
    p.cnes_estabelecimento, 
    COUNT(DISTINCT p.n_aih) AS volume_internacoes
FROM 
    processed p
JOIN 
    enriched e ON p.row_id = e.row_id
WHERE 
    p.sistema = 'SIH'
    AND p.uf_origem = 'SP'
    AND p.ano_cmpt = 2022
    AND (p.cid_principal LIKE 'M%' OR p.icd_group = 'M00-M99')
GROUP BY 
    p.cnes_estabelecimento
ORDER BY 
    volume_internacoes DESC
LIMIT 5
```

**Resultado:** `Linha 1: got={'cnes_estabelecimento': '2078015', 'volume_internacoes': 347} exp={'cnes_estabelecimento': '2078015', 'total_internacoes': 2221}`
**TTA:** 2610ms

---

### ❌ Q25 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por raça/cor do paciente em SP em 2022

**SQL gerado:**
```sql
SELECT 
  raca_cor_paciente, 
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
  raca_cor_paciente
ORDER BY 
  total_internacoes DESC
```

**Resultado:** `Linha não encontrada: {'raca_cor_paciente': '01', 'total': 38121, 'pct': 57.8}`
**TTA:** 2251ms

---

### ❌ Q26 — Epidemiológica Complexa

**Q:** CIDs ortopédicos com maior número de dias totais de internação em SP em 2022

**SQL gerado:**
```sql
SELECT 
    cid_principal, 
    SUM(dias_perm) AS total_dias_internacao
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND ano_cmpt = 2022
    AND uf_origem = 'SP'
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cid_principal
ORDER BY 
    total_dias_internacao DESC
```

**Resultado:** `Número de linhas: got=308 exp=10`
**TTA:** 2373ms

---

### ❌ Q27 — Epidemiológica Complexa

**Q:** Top 5 procedimentos ambulatoriais ortopédicos mais realizados em SP em 2022

**SQL gerado:**
```sql
SELECT cod_procedimento, SUM(pa_qtdapr) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY cod_procedimento
ORDER BY total_procedimentos DESC
LIMIT 5
```

**Resultado:** `Linha 1: got={'cod_procedimento': '0701010142', 'total_procedimentos': 1889.0} exp={'cod_procedimento': '0302050019', 'total': 70536}`
**TTA:** 2032ms

---

### ❌ Q28 — Epidemiológica Complexa

**Q:** Proporção de internações ortopédicas com paciente de outro município em SP em 2022

**SQL gerado:**
```sql
SELECT 
    COUNT(DISTINCT p.n_aih) AS total_internacoes,
    SUM(CASE WHEN e.clinical_deslocamento_territorial = 'deslocamento intermunicipal' THEN 1 ELSE 0 END) AS deslocamento_intermunicipal,
    SUM(CASE WHEN e.clinical_deslocamento_territorial = 'deslocamento intermunicipal' THEN 1 ELSE 0 END) * 1.0 / COUNT(DISTINCT p.n_aih) AS proporcao_deslocamento_intermunicipal
FROM 
    processed p
JOIN 
    enriched e ON p.row_id = e.row_id
WHERE 
    p.sistema = 'SIH'
    AND p.uf_origem = 'SP'
    AND p.ano_cmpt = 2022
    AND (p.cid_principal LIKE 'M%' OR p.icd_group = 'M00-M99')
    AND e.clinical_interpretacao_clinica = 'doencas osteomusculares e do tecido conjuntivo'
```

**Resultado:** `Linha 1: got={'total_internacoes': 6302, 'deslocamento_intermunicipal': 3417.0, 'proporcao_deslocamento_intermunicipal': 0.5422088226} exp={'total_internacoes': 65970, 'deslocamento_intermunicipal': 37219, 'pct_deslocamento': 56.4}`
**TTA:** 3364ms

---

### ❌ Q29 — Epidemiológica Complexa

**Q:** Comparativo de volume mensal SIA vs SIH ortopédico em SP em 2022

**SQL gerado:**
```sql
SELECT 
    ano_cmpt,
    mes_cmpt,
    sistema,
    COUNT(*) AS volume
FROM 
    processed
WHERE 
    ano_cmpt = 2022
    AND uf_origem = 'SP'
    AND (
        (sistema = 'SIA' AND cid_principal LIKE 'M%')
        OR (sistema = 'SIH' AND cid_principal LIKE 'M%')
    )
GROUP BY 
    ano_cmpt,
    mes_cmpt,
    sistema
ORDER BY 
    ano_cmpt,
    mes_cmpt,
    sistema;
```

**Resultado:** `Linha 1: got={'ano_cmpt': 2022, 'mes_cmpt': 1, 'sistema': 'SIA', 'volume': 521} exp={'mes_cmpt': 1, 'sistema': 'SIA', 'total': 5898}`
**TTA:** 3999ms

---

### ❌ Q30 — Epidemiológica Complexa

**Q:** Idade média dos pacientes internados por fratura de fêmur (S72) em SP em 2022

**SQL gerado:**
```sql
SELECT AVG(idade_paciente) AS idade_media
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND cid_principal LIKE 'S72%'
```

**Resultado:** `Linha 1: got={'idade_media': 62.3961647195} exp={'idade_media': 62.4, 'idade_minima': 0, 'idade_maxima': 99}`
**TTA:** 1758ms

---

### ❌ Q31 — Financeira

**Q:** Custo total de todas as internações ortopédicas em SP em 2022

**SQL gerado:**
```sql
SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes_ortopedicas
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=13990374.039999995 exp=175283428.77`
**TTA:** 2090ms

---

### ❌ Q32 — Financeira

**Q:** Custo médio por internação ortopédica em SP em 2022

**SQL gerado:**
```sql
SELECT 
    COALESCE(SUM(custo_total), 0) / COUNT(DISTINCT n_aih) AS custo_medio_por_internacao
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=2219.9895334814 exp=2655.37`
**TTA:** 2101ms

---

### ✅ Q33 — Financeira

**Q:** Custo total de internações por fratura de fêmur (S72) em SP em 2022

**SQL gerado:**
```sql
SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_fratura_femur
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND cid_principal LIKE 'S72%'
```

**Resultado:** `got=79277316.95999993 exp=79277316.96`
**TTA:** 3695ms

---

### ❌ Q34 — Financeira

**Q:** Top 5 CIDs ortopédicos com maior custo total de internação em SP em 2022

**SQL gerado:**
```sql
SELECT 
    cid_principal, 
    SUM(custo_total) AS total_custo
FROM 
    processed
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2022 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cid_principal
ORDER BY 
    total_custo DESC
LIMIT 5
```

**Resultado:** `Linha 1: got={'cid_principal': 'M511', 'total_custo': 878799.22} exp={'cid_principal': 'S720', 'internacoes': 8980, 'custo_total': 29879588.9}`
**TTA:** 4221ms

---

### ❌ Q35 — Financeira

**Q:** Custo total de internações ortopédicas com uso de OPME (val_ortp > 0) em SP em 2022

**SQL gerado:**
```sql
SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_ortopedicas_opme
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND ano_cmpt = 2022
    AND uf_origem = 'SP'
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
    AND val_ortp > 0
```

**Resultado:** `Linha 1: got={'custo_total_ortopedicas_opme': None} exp={'internacoes_com_opme': 0, 'custo_total_opme': None}`
**TTA:** 4105ms

---

### ❌ Q36 — Financeira

**Q:** Valor total de honorários profissionais (val_sp) em internações ortopédicas em SP em 2022

**SQL gerado:**
```sql
SELECT 
    COALESCE(SUM(val_sp), 0) AS total_val_sp
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=2795569.489999999 exp=30640001.52`
**TTA:** 3822ms

---

### ❌ Q37 — Financeira

**Q:** Custo médio por dia de internação ortopédica em SP em 2022

**SQL gerado:**
```sql
SELECT 
    COALESCE(SUM(custo_total) / NULLIF(SUM(dias_perm), 0), 0) AS custo_medio_por_dia
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=384.0449652749 exp=432.54`
**TTA:** 4630ms

---

### ❌ Q38 — Financeira

**Q:** Top 5 estabelecimentos com maior custo total de internações ortopédicas em SP em 2022

**SQL gerado:**
```sql
SELECT 
    p.cnes_estabelecimento, 
    SUM(p.custo_total) AS total_custo
FROM 
    processed p
JOIN 
    enriched e ON p.row_id = e.row_id
WHERE 
    p.sistema = 'SIH' 
    AND p.uf_origem = 'SP' 
    AND p.ano_cmpt = 2022 
    AND (p.cid_principal LIKE 'M%' OR p.icd_group = 'M00-M99')
GROUP BY 
    p.cnes_estabelecimento
ORDER BY 
    total_custo DESC
LIMIT 5
```

**Resultado:** `Linha 1: got={'cnes_estabelecimento': '2078015', 'total_custo': 1319752.55} exp={'cnes_estabelecimento': '2078015', 'internacoes': 2221, 'custo_total': 11218869.54}`
**TTA:** 4653ms

---

### ❌ Q39 — Financeira

**Q:** Custo total de procedimentos ambulatoriais ortopédicos em SP em 2022

**SQL gerado:**
```sql
SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_ambulatorial_ortopedico_2022
FROM 
    processed
WHERE 
    sistema = 'SIA'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=1945222.8200000033 exp=8721217.91`
**TTA:** 3117ms

---

### ❌ Q40 — Financeira

**Q:** Proporção custo serviço hospitalar vs honorários em internações ortopédicas em SP em 2022

**SQL gerado:**
```sql
SELECT 
    SUM(COALESCE(val_sh, 0)) / NULLIF(SUM(COALESCE(val_sp, 0)), 0) AS proporcao_custo_sh_vs_honorarios
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `Linha 1: got={'proporcao_custo_sh_vs_honorarios': 4.0044808723} exp={'custo_servico_hospitalar': 144636796.74, 'honorarios_profissionais': 30640001.52, 'pct_hospitalar': 82.5}`
**TTA:** 4450ms

---

### ❌ Q41 — Temporal/Comparativa

**Q:** Distribuição mensal de internações ortopédicas em SP em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt, 
    COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'total_internacoes': 476} exp={'mes_cmpt': 1, 'internacoes': 5209}`
**TTA:** 4452ms

---

### ❌ Q42 — Temporal/Comparativa

**Q:** Distribuição mensal de procedimentos ambulatoriais ortopédicos em SP em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt, 
    COUNT(*) AS total_procedimentos
FROM 
    processed
WHERE 
    sistema = 'SIA'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'total_procedimentos': 521} exp={'mes_cmpt': 1, 'procedimentos': 5898}`
**TTA:** 3619ms

---

### ✅ Q43 — Temporal/Comparativa

**Q:** Mês com maior número de internações por fratura de fêmur (S72) em SP em 2022

**SQL gerado:**
```sql
SELECT mes_cmpt, COUNT(DISTINCT n_aih) AS num_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND cid_principal LIKE 'S72%'
GROUP BY mes_cmpt
ORDER BY num_internacoes DESC
LIMIT 1
```

**Resultado:** `got=8 exp=8`
**TTA:** 4059ms

---

### ❌ Q44 — Temporal/Comparativa

**Q:** Evolução mensal do custo total de internações ortopédicas em SP em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt, 
    SUM(COALESCE(custo_total, 0)) AS total_custo
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'total_custo': 1015180.7699999993} exp={'mes_cmpt': 1, 'custo_total': 12942488.31}`
**TTA:** 3908ms

---

### ❌ Q45 — Temporal/Comparativa

**Q:** Volume mensal de internações por trauma (S00-T98) vs doenças musculoesqueléticas (M00-M99) em SP em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    SUM(CASE WHEN icd_group = 'S00-T98' THEN 1 ELSE 0 END) AS internacoes_trauma,
    SUM(CASE WHEN icd_group = 'M00-M99' THEN 1 ELSE 0 END) AS internacoes_musculoesqueleticas
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt
```

**Resultado:** `Número de linhas: got=12 exp=24`
**TTA:** 4478ms

---

### ❌ Q46 — Temporal/Comparativa

**Q:** Evolução mensal de óbitos em internações ortopédicas em SP em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt, 
    COUNT(DISTINCT n_aih) AS total_obitos
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND morte = 1
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'total_obitos': 6} exp={'mes_cmpt': 1, 'total_obitos': 179}`
**TTA:** 4289ms

---

### ❌ Q47 — Temporal/Comparativa

**Q:** Permanência média por trimestre nas internações ortopédicas em SP em 2022

**SQL gerado:**
```sql
SELECT 
    CEIL(mes_cmpt / 3.0) AS trimestre,
    AVG(dias_perm) AS permanencia_media
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    trimestre
ORDER BY 
    trimestre
```

**Resultado:** `Linha não encontrada: {'trimestre': 'Q1', 'permanencia_media_dias': 5.8}`
**TTA:** 3873ms

---

### ❌ Q48 — Temporal/Comparativa

**Q:** Mês com maior custo médio por internação ortopédica em SP em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt, 
    AVG(custo_total) AS custo_medio
FROM 
    processed
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2022 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    custo_medio DESC
LIMIT 1
```

**Resultado:** `got=12 exp=10`
**TTA:** 4363ms

---

### ✅ Q49 — Temporal/Comparativa

**Q:** Sazonalidade mensal de fraturas de fêmur (S72) em SP em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt, 
    COUNT(DISTINCT n_aih) AS total_fraturas_femur
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND cid_principal LIKE 'S72%'
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 3849ms

---

### ❌ Q50 — Temporal/Comparativa

**Q:** Volume mensal de internações ortopédicas de idosos (60 anos ou mais) em SP em 2022

**SQL gerado:**
```sql
SELECT 
  mes_cmpt, 
  COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND idade_paciente >= 60
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
  mes_cmpt
ORDER BY 
  mes_cmpt
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'volume_internacoes': 118} exp={'mes_cmpt': 1, 'internacoes_idosos': 1987}`
**TTA:** 4268ms

---

### ✅ Q51 — Epidemiológica Simples

**Q:** Número de internações por fratura de fêmur (S72) em mulheres em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND cid_principal LIKE 'S72%'
  AND sexo_paciente = 'F'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
```

**Resultado:** `got=15359 exp=15359`
**TTA:** 3792ms

---

### ✅ Q52 — Epidemiológica Simples

**Q:** Número de internações por fratura de fêmur (S72) em homens em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS num_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND cid_principal LIKE 'S72%'
  AND sexo_paciente = 'M'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
```

**Resultado:** `got=13997 exp=13997`
**TTA:** 3919ms

---

### ❌ Q53 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com permanência de 1 dia ou menos em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS num_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
  AND dias_perm <= 1
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
```

**Resultado:** `got=2330 exp=13928`
**TTA:** 4085ms

---

### ❌ Q54 — Epidemiológica Simples

**Q:** Número de procedimentos ambulatoriais ortopédicos realizados em mulheres em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(*)
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND sexo_paciente = 'F'
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=5420 exp=45579`
**TTA:** 4046ms

---

### ❌ Q55 — Epidemiológica Simples

**Q:** Número de procedimentos ambulatoriais ortopédicos realizados por idosos (60 anos ou mais) em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(*)
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND idade_paciente >= 60
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=3856 exp=53666`
**TTA:** 4325ms

---

### ✅ Q56 — Epidemiológica Simples

**Q:** Número de internações por fratura de tíbia e perônio (S82) em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND cid_principal LIKE 'S82%'
```

**Resultado:** `got=6261 exp=6261`
**TTA:** 3734ms

---

### ✅ Q57 — Epidemiológica Simples

**Q:** Número de internações por fratura de antebraço (S52) em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS num_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND cid_principal LIKE 'S52%'
```

**Resultado:** `got=1970 exp=1970`
**TTA:** 4001ms

---

### ✅ Q58 — Epidemiológica Simples

**Q:** Número de internações por fratura de ombro e úmero (S42) em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND cid_principal LIKE 'S42%'
```

**Resultado:** `got=1248 exp=1248`
**TTA:** 3910ms

---

### ❌ Q59 — Epidemiológica Simples

**Q:** Número de internações ortopédicas em crianças (menores de 18 anos) em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND idade_paciente < 18
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=481 exp=3619`
**TTA:** 4312ms

---

### ❌ Q60 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com permanência entre 8 e 30 dias em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) 
FROM processed 
WHERE sistema = 'SIH' 
  AND uf_origem = 'SP' 
  AND ano_cmpt = 2022 
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99') 
  AND dias_perm BETWEEN 8 AND 30
```

**Resultado:** `got=1239 exp=15489`
**TTA:** 4056ms

---

### ✅ Q61 — Epidemiológica Simples

**Q:** Número de internações por artrose de quadril e joelho (M16 e M17) em idosos (60 anos ou mais) em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND idade_paciente >= 60
  AND (cid_principal LIKE 'M16%' OR cid_principal LIKE 'M17%')
```

**Resultado:** `got=89 exp=89`
**TTA:** 4126ms

---

### ✅ Q62 — Epidemiológica Simples

**Q:** Número de internações por fratura de fêmur (S72) em mulheres com 70 anos ou mais em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND cid_principal LIKE 'S72%'
  AND sexo_paciente = 'F'
  AND idade_paciente >= 70
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
```

**Resultado:** `got=10944 exp=10944`
**TTA:** 1940ms

---

### ❌ Q63 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com permanência superior a 30 dias em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) 
FROM processed 
WHERE sistema = 'SIH' 
  AND uf_origem = 'SP' 
  AND ano_cmpt = 2022 
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99') 
  AND dias_perm > 30
```

**Resultado:** `got=157 exp=975`
**TTA:** 6149ms

---

### ❌ Q64 — Epidemiológica Simples

**Q:** Número de procedimentos ambulatoriais ortopédicos realizados em homens em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(*)
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND sexo_paciente = 'M'
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=5228 exp=59437`
**TTA:** 4116ms

---

### ✅ Q65 — Epidemiológica Simples

**Q:** Número de internações por traumatismos (grupo S00-T98) em idosos (60 anos ou mais) em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND icd_group = 'S00-T98'
  AND idade_paciente >= 60
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
```

**Resultado:** `got=24981 exp=24981`
**TTA:** 3859ms

---

### ✅ Q66 — Epidemiológica Simples

**Q:** Número de internações por doenças osteomusculares (grupo M00-M99) em mulheres em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND icd_group = 'M00-M99'
  AND sexo_paciente = 'F'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
```

**Resultado:** `got=2327 exp=2327`
**TTA:** 4085ms

---

### ✅ Q67 — Epidemiológica Simples

**Q:** Número de óbitos em internações por fratura de fêmur (S72) em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(*) AS numero_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND cid_principal LIKE 'S72%'
  AND morte = 1
```

**Resultado:** `got=1216 exp=1216`
**TTA:** 4088ms

---

### ❌ Q68 — Epidemiológica Simples

**Q:** Número de municípios distintos com registro de procedimentos ambulatoriais ortopédicos em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT cod_munic_estabelecimento) AS num_municipios
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=85 exp=399`
**TTA:** 4078ms

---

### ❌ Q69 — Epidemiológica Complexa

**Q:** Top 5 municípios por volume de procedimentos ambulatoriais ortopédicos em SP em 2022

**SQL gerado:**
```sql
SELECT 
  p.cod_munic_estabelecimento, 
  COUNT(*) AS volume_procedimentos
FROM 
  processed p
WHERE 
  p.sistema = 'SIA'
  AND p.uf_origem = 'SP'
  AND p.ano_cmpt = 2022
  AND (p.cid_principal LIKE 'M%' OR p.icd_group = 'M00-M99')
GROUP BY 
  p.cod_munic_estabelecimento
ORDER BY 
  volume_procedimentos DESC
LIMIT 5
```

**Resultado:** `Linha 1: got={'cod_munic_estabelecimento': '355030', 'volume_procedimentos': 5094} exp={'cod_munic_estabelecimento': '355030', 'total': 33846}`
**TTA:** 2497ms

---

### ❌ Q70 — Epidemiológica Complexa

**Q:** Distribuição de procedimentos ambulatoriais ortopédicos por faixa etária em SP em 2022

**SQL gerado:**
```sql
SELECT 
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 34 THEN '18-34'
        WHEN idade_paciente BETWEEN 35 AND 49 THEN '35-49'
        WHEN idade_paciente BETWEEN 50 AND 64 THEN '50-64'
        ELSE '65+' 
    END AS faixa_etaria,
    COUNT(*) AS quantidade_procedimentos
FROM 
    processed
WHERE 
    sistema = 'SIA'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    faixa_etaria
ORDER BY 
    faixa_etaria;
```

**Resultado:** `Número de linhas: got=5 exp=3`
**TTA:** 6384ms

---

### ❌ Q71 — Epidemiológica Complexa

**Q:** Top 5 diagnósticos com maior número de óbitos em internações ortopédicas em SP em 2022

**SQL gerado:**
```sql
SELECT 
    p.cid_principal, 
    COUNT(*) AS numero_obitos
FROM 
    processed p
JOIN 
    enriched e ON p.row_id = e.row_id
WHERE 
    p.sistema = 'SIH'
    AND p.uf_origem = 'SP'
    AND p.ano_cmpt = 2022
    AND (p.cid_principal LIKE 'M%' OR p.icd_group = 'M00-M99')
    AND p.morte = 1
GROUP BY 
    p.cid_principal
ORDER BY 
    numero_obitos DESC
LIMIT 5
```

**Resultado:** `Linha 1: got={'cid_principal': 'M869', 'numero_obitos': 13} exp={'cid_principal': 'S720', 'total_internacoes': 8980, 'total_obitos': 455.0}`
**TTA:** 4835ms

---

### ❌ Q72 — Epidemiológica Complexa

**Q:** Permanência hospitalar média por sexo em internações ortopédicas em SP em 2022

**SQL gerado:**
```sql
SELECT 
    sexo_paciente, 
    AVG(dias_perm) AS permanencia_media
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    sexo_paciente
```

**Resultado:** `Linha não encontrada: {'sexo_paciente': 'F', 'permanencia_media': 6.3}`
**TTA:** 3066ms

---

### ❌ Q73 — Epidemiológica Complexa

**Q:** Top 10 estabelecimentos por volume de procedimentos ambulatoriais ortopédicos em SP em 2022

**SQL gerado:**
```sql
SELECT 
    p.cnes_estabelecimento, 
    COUNT(*) AS volume_procedimentos
FROM 
    processed p
WHERE 
    p.sistema = 'SIA'
    AND p.uf_origem = 'SP'
    AND p.ano_cmpt = 2022
    AND (p.cid_principal LIKE 'M%' OR p.icd_group = 'M00-M99')
GROUP BY 
    p.cnes_estabelecimento
ORDER BY 
    volume_procedimentos DESC
LIMIT 10
```

**Resultado:** `Linha 1: got={'cnes_estabelecimento': '2078015', 'volume_procedimentos': 2090} exp={'cnes_estabelecimento': '2068974', 'total': 9214}`
**TTA:** 1926ms

---

### ❌ Q74 — Epidemiológica Complexa

**Q:** Taxa de mortalidade por sexo em internações ortopédicas em SP em 2022

**SQL gerado:**
```sql
SELECT 
    sexo_paciente, 
    SUM(morte) * 100.0 / COUNT(DISTINCT n_aih) AS taxa_mortalidade
FROM 
    processed
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2022 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    sexo_paciente
```

**Resultado:** `Linha não encontrada: {'sexo_paciente': 'F', 'total_obitos': 1081.0, 'taxa_mortalidade': 4.18}`
**TTA:** 6497ms

---

### ❌ Q75 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por grupo diagnóstico (M e S) e faixa etária em SP em 2022

**SQL gerado:**
```sql
SELECT 
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 34 THEN '18-34'
        WHEN idade_paciente BETWEEN 35 AND 49 THEN '35-49'
        WHEN idade_paciente BETWEEN 50 AND 64 THEN '50-64'
        ELSE '65+' 
    END AS faixa_etaria,
    icd_group,
    COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY 
    faixa_etaria, icd_group
ORDER BY 
    faixa_etaria, icd_group;
```

**Resultado:** `Número de linhas: got=10 exp=6`
**TTA:** 2536ms

---

### ✅ Q76 — Epidemiológica Complexa

**Q:** Top 5 diagnósticos de traumatismo com maior permanência média em SP em 2022 (mínimo 40 internações)

**SQL gerado:**
```sql
SELECT 
    p.cid_principal, 
    COUNT(DISTINCT p.n_aih) AS num_internacoes,
    AVG(p.dias_perm) AS permanencia_media
FROM 
    processed p
JOIN 
    enriched e ON p.row_id = e.row_id
WHERE 
    p.sistema = 'SIH'
    AND p.uf_origem = 'SP'
    AND p.ano_cmpt = 2022
    AND e.clinical_interpretacao_clinica = 'lesoes e causas traumaticas'
GROUP BY 
    p.cid_principal
HAVING 
    COUNT(DISTINCT p.n_aih) >= 40
ORDER BY 
    permanencia_media DESC
LIMIT 5
```

**Resultado:** `5 linhas corretas (flexível)`
**TTA:** 6669ms

---

### ❌ Q77 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por faixa etária (0-17, 18-39, 40-59, 60-79, 80+) em SP em 2022

**SQL gerado:**
```sql
SELECT 
    CASE 
        WHEN idade_paciente BETWEEN 0 AND 17 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 39 THEN '18-39'
        WHEN idade_paciente BETWEEN 40 AND 59 THEN '40-59'
        WHEN idade_paciente BETWEEN 60 AND 79 THEN '60-79'
        ELSE '80+'
    END AS faixa_etaria,
    COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    faixa_etaria
ORDER BY 
    faixa_etaria
```

**Resultado:** `Linha não encontrada: {'faixa_etaria': '0-17', 'total': 3619}`
**TTA:** 4102ms

---

### ✅ Q78 — Epidemiológica Complexa

**Q:** Top 5 estabelecimentos com maior volume de internações por fratura de fêmur (S72) em SP em 2022

**SQL gerado:**
```sql
SELECT 
    p.cnes_estabelecimento, 
    COUNT(DISTINCT p.n_aih) AS volume_internacoes
FROM 
    processed p
WHERE 
    p.sistema = 'SIH'
    AND p.uf_origem = 'SP'
    AND p.ano_cmpt = 2022
    AND p.cid_principal LIKE 'S72%'
GROUP BY 
    p.cnes_estabelecimento
ORDER BY 
    volume_internacoes DESC
LIMIT 5
```

**Resultado:** `5 linhas corretas (flexível)`
**TTA:** 3967ms

---

### ❌ Q79 — Epidemiológica Complexa

**Q:** Permanência hospitalar média por faixa etária em internações ortopédicas em SP em 2022

**SQL gerado:**
```sql
SELECT 
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 34 THEN '18-34'
        WHEN idade_paciente BETWEEN 35 AND 49 THEN '35-49'
        WHEN idade_paciente BETWEEN 50 AND 64 THEN '50-64'
        ELSE '65+' 
    END AS faixa_etaria,
    AVG(dias_perm) AS permanencia_media
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    faixa_etaria
ORDER BY 
    faixa_etaria;
```

**Resultado:** `Número de linhas: got=5 exp=3`
**TTA:** 4584ms

---

### ✅ Q80 — Epidemiológica Complexa

**Q:** Top 5 diagnósticos de doença osteomuscular (M00-M99) por número de internações em SP em 2022

**SQL gerado:**
```sql
SELECT 
    main_icd AS diagnostico,
    COUNT(DISTINCT n_aih) AS numero_internacoes
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND e.clinical_interpretacao_clinica = 'doencas osteomusculares e do tecido conjuntivo'
GROUP BY 
    main_icd
ORDER BY 
    numero_internacoes DESC
LIMIT 5
```

**Resultado:** `5 linhas corretas (flexível)`
**TTA:** 4118ms

---

### ❌ Q81 — Epidemiológica Complexa

**Q:** Número de internações ortopédicas com permanência entre 3 e 7 dias em SP em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS num_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
  AND dias_perm BETWEEN 3 AND 7
```

**Resultado:** `got=1465 exp=25807`
**TTA:** 3307ms

---

### ❌ Q82 — Epidemiológica Complexa

**Q:** Top 5 diagnósticos ambulatoriais ortopédicos em idosos (60 anos ou mais) em SP em 2022

**SQL gerado:**
```sql
SELECT 
    main_icd AS diagnostico,
    COUNT(*) AS total
FROM 
    processed
WHERE 
    sistema = 'SIA'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND idade_paciente >= 60
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    main_icd
ORDER BY 
    total DESC
LIMIT 5
```

**Resultado:** `Linha 1: got={'diagnostico': 'M216', 'total': 1596} exp={'cid_principal': 'S720', 'total': 11096}`
**TTA:** 4315ms

---

### ❌ Q83 — Epidemiológica Complexa

**Q:** Média de internações ortopédicas por estabelecimento em SP em 2022

**SQL gerado:**
```sql
SELECT 
    cod_munic_estabelecimento,
    cnes_estabelecimento,
    COUNT(DISTINCT n_aih) / COUNT(DISTINCT cnes_estabelecimento) AS media_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND ano_cmpt = 2022
    AND uf_origem = 'SP'
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cod_munic_estabelecimento, 
    cnes_estabelecimento
```

**Resultado:** `got=355030 exp=173.1`
**TTA:** 4477ms

---

### ❌ Q84 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por faixa de permanência em SP em 2022

**SQL gerado:**
```sql
SELECT 
    CASE 
        WHEN dias_perm <= 3 THEN '1-3 dias'
        WHEN dias_perm BETWEEN 4 AND 7 THEN '4-7 dias'
        WHEN dias_perm BETWEEN 8 AND 14 THEN '8-14 dias'
        WHEN dias_perm BETWEEN 15 AND 30 THEN '15-30 dias'
        ELSE 'mais de 30 dias'
    END AS faixa_permanencia,
    COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    faixa_permanencia
ORDER BY 
    faixa_permanencia;
```

**Resultado:** `Número de linhas: got=5 exp=4`
**TTA:** 4656ms

---

### ❌ Q85 — Financeira

**Q:** Custo total de internações ortopédicas de idosos (60 anos ou mais) em SP em 2022

**SQL gerado:**
```sql
SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes_ortopedicas_idosos_2022
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND ano_cmpt = 2022
    AND uf_origem = 'SP'
    AND idade_paciente >= 60
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=4489529.78 exp=78009817.26`
**TTA:** 3374ms

---

### ✅ Q86 — Financeira

**Q:** Custo médio por internação por fratura de fêmur (S72) em SP em 2022

**SQL gerado:**
```sql
SELECT 
    COALESCE(SUM(custo_total) / COUNT(DISTINCT n_aih), 0) AS custo_medio_por_internacao
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND cid_principal LIKE 'S72%'
```

**Resultado:** `got=2700.5490175773 exp=2700.27`
**TTA:** 4015ms

---

### ❌ Q87 — Financeira

**Q:** Custo total de internações ortopédicas com passagem por UTI em SP em 2022

**SQL gerado:**
```sql
SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_ortopedicas_uti
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND ano_cmpt = 2022
    AND uf_origem = 'SP'
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
    AND uti_int_to > 0
```

**Resultado:** `got=None exp=15904.56`
**TTA:** 4265ms

---

### ❌ Q88 — Financeira

**Q:** Custo médio por internação ortopédica por sexo do paciente em SP em 2022

**SQL gerado:**
```sql
SELECT 
    sexo_paciente, 
    AVG(custo_total) AS custo_medio
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    sexo_paciente
```

**Resultado:** `Linha não encontrada: {'sexo_paciente': 'F', 'custo_medio': 2684.14}`
**TTA:** 3971ms

---

### ❌ Q89 — Financeira

**Q:** Custo total de internações ortopédicas em crianças (menores de 18 anos) em SP em 2022

**SQL gerado:**
```sql
SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes_ortopedicas
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND ano_cmpt = 2022
    AND uf_origem = 'SP'
    AND idade_paciente < 18
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=927790.9099999999 exp=7450877.25`
**TTA:** 3999ms

---

### ❌ Q90 — Financeira

**Q:** Custo médio por internação ortopédica com permanência superior a 7 dias em SP em 2022

**SQL gerado:**
```sql
SELECT 
    AVG(custo_total) AS custo_medio
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
    AND dias_perm > 7
```

**Resultado:** `got=3083.8746901408 exp=4423.19`
**TTA:** 4099ms

---

### ❌ Q91 — Financeira

**Q:** Top 3 municípios com maior custo total em internações ortopédicas em SP em 2022

**SQL gerado:**
```sql
SELECT 
    p.cod_munic_estabelecimento, 
    SUM(p.custo_total) AS total_custo
FROM 
    processed p
JOIN 
    enriched e ON p.row_id = e.row_id
WHERE 
    p.sistema = 'SIH'
    AND p.uf_origem = 'SP'
    AND p.ano_cmpt = 2022
    AND (p.cid_principal LIKE 'M%' OR p.icd_group = 'M00-M99')
GROUP BY 
    p.cod_munic_estabelecimento
ORDER BY 
    total_custo DESC
LIMIT 3
```

**Resultado:** `Linha 1: got={'cod_munic_estabelecimento': '350000', 'total_custo': 6704942.199999999} exp={'cod_munic_estabelecimento': '350000', 'custo_total': 91002293.78}`
**TTA:** 5206ms

---

### ❌ Q92 — Financeira

**Q:** Valor total de serviço hospitalar em internações ortopédicas em SP em 2022

**SQL gerado:**
```sql
SELECT COALESCE(SUM(val_sh), 0) AS total_valor_servico_hospitalar
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=11194804.549999993 exp=144636796.74`
**TTA:** 3009ms

---

### ❌ Q93 — Financeira

**Q:** Custo médio por internação ortopédica por faixa etária em SP em 2022

**SQL gerado:**
```sql
SELECT 
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 34 THEN '18-34'
        WHEN idade_paciente BETWEEN 35 AND 49 THEN '35-49'
        WHEN idade_paciente BETWEEN 50 AND 64 THEN '50-64'
        ELSE '65+' 
    END AS faixa_etaria,
    AVG(custo_total) AS custo_medio
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    faixa_etaria
ORDER BY 
    faixa_etaria
```

**Resultado:** `Número de linhas: got=5 exp=3`
**TTA:** 4891ms

---

### ❌ Q94 — Financeira

**Q:** Valor total de UTI em internações ortopédicas em SP em 2022

**SQL gerado:**
```sql
SELECT 
    COALESCE(SUM(val_uti), 0) AS total_valor_uti
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=1275788.72 exp=37073666.92`
**TTA:** 3437ms

---

### ✅ Q95 — Financeira

**Q:** Custo total de internações por doenças osteomusculares (M00-M99) em SP em 2022

**SQL gerado:**
```sql
SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND icd_group = 'M00-M99'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
```

**Resultado:** `got=13990374.039999995 exp=13990374.04`
**TTA:** 4076ms

---

### ✅ Q96 — Financeira

**Q:** Custo total de internações por traumatismos (S00-T98) em SP em 2022

**SQL gerado:**
```sql
SELECT 
    SUM(COALESCE(custo_total, 0)) AS total_custo_traumatismos
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'S%' OR icd_group = 'S00-T98')
```

**Resultado:** `got=161293054.73000053 exp=161293054.73`
**TTA:** 4085ms

---

### ❌ Q97 — Financeira

**Q:** Top 5 diagnósticos com maior custo médio por internação ortopédica em SP em 2022 (mínimo 40 internações)

**SQL gerado:**
```sql
SELECT 
    p.cid_principal, 
    COUNT(DISTINCT p.n_aih) AS num_internacoes,
    SUM(p.custo_total) / COUNT(DISTINCT p.n_aih) AS custo_medio
FROM 
    processed p
WHERE 
    p.sistema = 'SIH'
    AND p.uf_origem = 'SP'
    AND p.ano_cmpt = 2022
    AND (p.cid_principal LIKE 'M%' OR p.icd_group = 'M00-M99')
GROUP BY 
    p.cid_principal
HAVING 
    COUNT(DISTINCT p.n_aih) >= 40
ORDER BY 
    custo_medio DESC
LIMIT 5
```

**Resultado:** `Linha 1: got={'cid_principal': 'M518', 'num_internacoes': 112, 'custo_medio': 7827.0964285714} exp={'cid_principal': 'S221', 'internacoes': 41, 'custo_medio': 14053.88}`
**TTA:** 4469ms

---

### ❌ Q98 — Financeira

**Q:** Custo médio por procedimento ambulatorial ortopédico em SP em 2022

**SQL gerado:**
```sql
SELECT 
    AVG(custo_total) AS custo_medio
FROM 
    processed
WHERE 
    sistema = 'SIA'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=182.6843369647 exp=83.05`
**TTA:** 3747ms

---

### ❌ Q99 — Financeira

**Q:** Top 5 procedimentos ambulatoriais com maior custo total em SP em 2022

**SQL gerado:**
```sql
SELECT cod_procedimento, SUM(custo_total) AS total_custo
FROM processed
WHERE sistema = 'SIA' AND uf_origem = 'SP' AND ano_cmpt = 2022
GROUP BY cod_procedimento
ORDER BY total_custo DESC
LIMIT 5
```

**Resultado:** `Linha 1: got={'cod_procedimento': '0702100048', 'total_custo': 46893518.21000075} exp={'cod_procedimento': '0701020369', 'custo_total': 1201460.4}`
**TTA:** 3912ms

---

### ❌ Q100 — Financeira

**Q:** Custo total de internações ortopédicas de idosos (60+) com permanência superior a 7 dias em SP em 2022

**SQL gerado:**
```sql
SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND ano_cmpt = 2022
    AND uf_origem = 'SP'
    AND idade_paciente >= 60
    AND dias_perm > 7
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=1801433.3399999999 exp=35943333.49`
**TTA:** 4464ms

---

### ❌ Q101 — Temporal/Comparativa

**Q:** Mês com maior número de óbitos em internações ortopédicas em SP em 2022

**SQL gerado:**
```sql
SELECT mes_cmpt, COUNT(*) AS num_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND morte = 1
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY mes_cmpt
ORDER BY num_obitos DESC
LIMIT 1
```

**Resultado:** `Linha 1: got={'mes_cmpt': 8, 'num_obitos': 10} exp={'mes_cmpt': 10, 'total_obitos': 224}`
**TTA:** 4007ms

---

### ✅ Q102 — Temporal/Comparativa

**Q:** Volume mensal de internações por fratura de fêmur (S72) em idosos (60 anos ou mais) em SP em 2022

**SQL gerado:**
```sql
SELECT 
  mes_cmpt, 
  COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND idade_paciente >= 60
  AND cid_principal LIKE 'S72%'
GROUP BY 
  mes_cmpt
ORDER BY 
  mes_cmpt
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 4189ms

---

### ❌ Q103 — Temporal/Comparativa

**Q:** Custo mensal de internações ortopédicas de idosos (60 anos ou mais) em SP em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt, 
    SUM(COALESCE(custo_total, 0)) AS custo_total_mensal
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND idade_paciente >= 60
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'custo_total_mensal': 287024.49} exp={'mes_cmpt': 1, 'custo_total': 5379118.99}`
**TTA:** 4302ms

---

### ✅ Q104 — Temporal/Comparativa

**Q:** Mês com maior custo total de internações por fratura de fêmur (S72) em SP em 2022

**SQL gerado:**
```sql
SELECT mes_cmpt, SUM(custo_total) AS total_custo
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND cid_principal LIKE 'S72%'
GROUP BY mes_cmpt
ORDER BY total_custo DESC
LIMIT 1
```

**Resultado:** `1 linhas corretas (flexível)`
**TTA:** 3897ms

---

### ✅ Q105 — Temporal/Comparativa

**Q:** Volume mensal de internações por traumatismos (grupo S00-T98) em SP em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt, 
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND icd_group = 'S00-T98'
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 4089ms

---

### ❌ Q106 — Temporal/Comparativa

**Q:** Volume mensal de procedimentos ambulatoriais ortopédicos por sexo em SP em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt, 
    sexo_paciente, 
    SUM(pa_qtdapr) AS total_procedimentos
FROM 
    processed
WHERE 
    sistema = 'SIA'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt, 
    sexo_paciente
ORDER BY 
    mes_cmpt, 
    sexo_paciente
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'sexo_paciente': 'F', 'total_procedimentos': 288.0} exp={'mes_cmpt': 1, 'sexo_paciente': 'F', 'total': 2589}`
**TTA:** 4368ms

---

### ❌ Q107 — Temporal/Comparativa

**Q:** Comparativo trimestral de internações ortopédicas por grupo diagnóstico em SP em 2022

**SQL gerado:**
```sql
SELECT 
    FLOOR((mes_cmpt - 1) / 3) + 1 AS trimestre,
    icd_group,
    COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND ano_cmpt = 2022
    AND uf_origem = 'SP'
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    trimestre, icd_group
ORDER BY 
    trimestre, icd_group;
```

**Resultado:** `Número de linhas: got=4 exp=8`
**TTA:** 3942ms

---

### ❌ Q108 — Temporal/Comparativa

**Q:** Mês com maior permanência hospitalar média em internações ortopédicas em SP em 2022

**SQL gerado:**
```sql
SELECT mes_cmpt, AVG(dias_perm) AS media_permanencia
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY mes_cmpt
ORDER BY media_permanencia DESC
LIMIT 1
```

**Resultado:** `Linha 1: got={'mes_cmpt': 4, 'media_permanencia': 7.0149892934} exp={'mes_cmpt': 7, 'permanencia_media': 6.2}`
**TTA:** 3903ms

---

### ❌ Q109 — Temporal/Comparativa

**Q:** Evolução mensal da taxa de mortalidade em internações ortopédicas em SP em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    COUNT(DISTINCT CASE WHEN morte = 1 THEN n_aih END) * 100.0 / COUNT(DISTINCT n_aih) AS taxa_mortalidade
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'taxa_mortalidade': 1.2605042017} exp={'mes_cmpt': 1, 'internacoes': 5209, 'obitos': 179.0, 'taxa_mortalidade': 3.44}`
**TTA:** 4983ms

---

### ✅ Q110 — Temporal/Comparativa

**Q:** Trimestre com maior volume de internações por fratura de fêmur (S72) em SP em 2022

**SQL gerado:**
```sql
SELECT 
  CASE 
    WHEN mes_cmpt IN (1, 2, 3) THEN 'Q1'
    WHEN mes_cmpt IN (4, 5, 6) THEN 'Q2'
    WHEN mes_cmpt IN (7, 8, 9) THEN 'Q3'
    WHEN mes_cmpt IN (10, 11, 12) THEN 'Q4'
  END AS trimestre,
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND cid_principal LIKE 'S72%'
GROUP BY 
  trimestre
ORDER BY 
  total_internacoes DESC
LIMIT 1
```

**Resultado:** `1 linhas corretas (flexível)`
**TTA:** 4175ms

---

### ❌ Q111 — Temporal/Comparativa

**Q:** Comparativo semestral de internações ortopédicas (1º vs 2º semestre) em SP em 2022

**SQL gerado:**
```sql
SELECT 
    CASE 
        WHEN mes_cmpt BETWEEN 1 AND 6 THEN '1º Semestre'
        ELSE '2º Semestre'
    END AS semestre,
    COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    semestre
ORDER BY 
    semestre
```

**Resultado:** `Linha não encontrada: {'semestre': '1S', 'total': 31575}`
**TTA:** 3903ms

---

### ✅ Q112 — Temporal/Comparativa

**Q:** Custo médio mensal por internação por fratura de fêmur (S72) em SP em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    COALESCE(SUM(custo_total) / COUNT(DISTINCT n_aih), 0) AS custo_medio_mensal
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND cid_principal LIKE 'S72%'
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 4843ms

---

### ❌ Q113 — Temporal/Comparativa

**Q:** Volume mensal de internações ortopédicas por sexo do paciente em SP em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt, 
    sexo_paciente, 
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt, 
    sexo_paciente
ORDER BY 
    mes_cmpt, 
    sexo_paciente;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'sexo_paciente': 'F', 'volume_internacoes': 180} exp={'mes_cmpt': 1, 'sexo_paciente': 'F', 'total': 2058}`
**TTA:** 3867ms

---

### ❌ Q114 — Temporal/Comparativa

**Q:** Mês com menor volume de procedimentos ambulatoriais ortopédicos em SP em 2022

**SQL gerado:**
```sql
SELECT mes_cmpt, SUM(pa_qtdapr) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY mes_cmpt
ORDER BY total_procedimentos ASC
LIMIT 1
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'total_procedimentos': 565.0} exp={'mes_cmpt': 1, 'total': 5898}`
**TTA:** 4239ms

---

### ❌ Q115 — Temporal/Comparativa

**Q:** Volume trimestral de procedimentos ambulatoriais ortopédicos em SP em 2022

**SQL gerado:**
```sql
SELECT 
  CEIL(mes_cmpt / 3.0) AS trimestre,
  SUM(pa_qtdapr) AS total_procedimentos
FROM 
  processed
WHERE 
  sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
  trimestre
ORDER BY 
  trimestre
```

**Resultado:** `Linha não encontrada: {'trimestre': 'Q1', 'total': 21523}`
**TTA:** 3171ms

---

### ❌ Q116 — Temporal/Comparativa

**Q:** Custo mensal de procedimentos ambulatoriais ortopédicos em SP em 2022

**SQL gerado:**
```sql
SELECT 
    ano_cmpt,
    mes_cmpt,
    SUM(COALESCE(custo_total, 0)) AS custo_total_mensal
FROM 
    processed
WHERE 
    sistema = 'SIA'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    ano_cmpt,
    mes_cmpt
ORDER BY 
    ano_cmpt,
    mes_cmpt
```

**Resultado:** `Linha 1: got={'ano_cmpt': 2022, 'mes_cmpt': 1, 'custo_total_mensal': 95929.0399999997} exp={'mes_cmpt': 1, 'custo_total': 512553.88}`
**TTA:** 4877ms

---

### ❌ Q117 — Temporal/Comparativa

**Q:** Comparativo semestral do custo total de internações ortopédicas em SP em 2022

**SQL gerado:**
```sql
SELECT 
    CASE 
        WHEN mes_cmpt BETWEEN 1 AND 6 THEN '1º Semestre'
        WHEN mes_cmpt BETWEEN 7 AND 12 THEN '2º Semestre'
    END AS semestre,
    SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    semestre
ORDER BY 
    semestre;
```

**Resultado:** `Linha não encontrada: {'semestre': '1S', 'custo_total': 82207816.93}`
**TTA:** 3910ms

---

### ✅ Q118 — Temporal/Comparativa

**Q:** Evolução mensal da permanência média em internações por fratura de fêmur (S72) em SP em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    AVG(dias_perm) AS permanencia_media
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2022
    AND cid_principal LIKE 'S72%'
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt
```

**Resultado:** `12 linhas corretas (exato)`
**TTA:** 3564ms

---

## Failures Detail

- **Q02** (Epidemiológica Simples): got=11388.0 exp=105016
  SQL: `SELECT SUM(pa_qtdapr) AS total_procedimentos_ambulatoriais_ortopedicos
FROM processed
WHERE sistema = 'SIA'
  AND uf_ori...`
- **Q05** (Epidemiológica Simples): got=73 exp=2223
  SQL: `SELECT COUNT(*) AS numero_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND...`
- **Q08** (Epidemiológica Simples): got=2327 exp=25847
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND sexo_paciente = 'F'
  AND ...`
- **Q09** (Epidemiológica Simples): got=3975 exp=40123
  SQL: `SELECT COUNT(DISTINCT n_aih) AS num_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND sexo_paciente = 'M'
  AND uf_...`
- **Q10** (Epidemiológica Simples): got=1685 exp=26666
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND ano_cmpt = 2022
  AND uf_o...`
- **Q11** (Epidemiológica Simples): got=158 exp=211
  SQL: `SELECT COUNT(DISTINCT cod_munic_estabelecimento) 
FROM processed 
WHERE sistema = 'SIH' 
  AND uf_origem = 'SP' 
  AND a...`
- **Q12** (Epidemiológica Simples): got=293 exp=381
  SQL: `SELECT COUNT(DISTINCT cnes_estabelecimento) 
FROM processed 
WHERE sistema = 'SIH' 
  AND uf_origem = 'SP' 
  AND ano_cm...`
- **Q13** (Epidemiológica Simples): got=1383 exp=16451
  SQL: `SELECT COUNT(DISTINCT n_aih) 
FROM processed 
WHERE sistema = 'SIH' 
  AND uf_origem = 'SP' 
  AND ano_cmpt = 2022 
  AN...`
- **Q14** (Epidemiológica Simples): got=0 exp=3
  SQL: `SELECT COUNT(DISTINCT n_aih) AS num_internacoes_ortopedicas_uti
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = '...`
- **Q16** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'M861', 'volume_internacoes': 912} exp={'cid_principal': 'S720', 'total': 8980}
  SQL: `SELECT 
    cid_principal, 
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
WHERE 
    sistema = 'SI...`
- **Q17** (Epidemiológica Complexa): Linha 1: got={'cod_munic_estabelecimento': '350000', 'num_internacoes': 2800} exp={'cod_munic_estabelecimento': '350000', 'total': 29824}
  SQL: `SELECT 
    p.cod_munic_estabelecimento, 
    COUNT(DISTINCT p.n_aih) AS num_internacoes
FROM 
    processed p
JOIN 
   ...`
- **Q18** (Epidemiológica Complexa): Linha não encontrada: {'faixa_etaria': '0-17', 'total': 3619}
  SQL: `SELECT 
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 59 THEN '18-59...`
- **Q19** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'M895', 'total_internacoes': 2, 'total_obitos': 1.0, 'taxa_mortalidade': 50.0} exp={'cid_principal': 'S062', 'total_internacoes': 70, 'total_obitos': 21, 'taxa_mortalidade_pct': 30.0}
  SQL: `SELECT 
    cid_principal, 
    COUNT(DISTINCT n_aih) AS total_internacoes, 
    SUM(morte) AS total_obitos, 
    (SUM(m...`
- **Q20** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'M216', 'volume_producao': 4743} exp={'cid_principal': 'S720', 'total': 16466}
  SQL: `SELECT 
    cid_principal, 
    COUNT(*) AS volume_producao
FROM 
    processed
WHERE 
    sistema = 'SIA'
    AND uf_or...`
- **Q21** (Epidemiológica Complexa): Linha não encontrada: {'sexo_paciente': 'M', 'total': 40123, 'pct': 60.8}
  SQL: `SELECT 
    sexo_paciente, 
    COUNT(DISTINCT n_aih) AS total_internacoes,
    COUNT(DISTINCT n_aih) * 1.0 / SUM(COUNT(...`
- **Q22** (Epidemiológica Complexa): Linha 1: got={'fraturas_femur_idosos': 19117, 'total_internacoes': 195574} exp={'total_s72': 29356, 's72_idosos': 19120, 'pct_idosos': 65.1}
  SQL: `SELECT 
    COUNT(DISTINCT CASE WHEN idade_paciente >= 60 AND cid_principal LIKE 'S72%' THEN n_aih END) AS fraturas_femu...`
- **Q23** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'M318', 'permanencia_media': 36.0} exp={'cid_principal': 'S063', 'total_internacoes': 78, 'permanencia_media_dias': 16.4}
  SQL: `SELECT 
    cid_principal, 
    AVG(dias_perm) AS permanencia_media
FROM 
    processed
WHERE 
    sistema = 'SIH'
    A...`
- **Q24** (Epidemiológica Complexa): Linha 1: got={'cnes_estabelecimento': '2078015', 'volume_internacoes': 347} exp={'cnes_estabelecimento': '2078015', 'total_internacoes': 2221}
  SQL: `SELECT 
    p.cnes_estabelecimento, 
    COUNT(DISTINCT p.n_aih) AS volume_internacoes
FROM 
    processed p
JOIN 
    e...`
- **Q25** (Epidemiológica Complexa): Linha não encontrada: {'raca_cor_paciente': '01', 'total': 38121, 'pct': 57.8}
  SQL: `SELECT 
  raca_cor_paciente, 
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
  processed
WHERE 
  sistema = 'SIH'
  ...`
- **Q26** (Epidemiológica Complexa): Número de linhas: got=308 exp=10
  SQL: `SELECT 
    cid_principal, 
    SUM(dias_perm) AS total_dias_internacao
FROM 
    processed
WHERE 
    sistema = 'SIH'
 ...`
- **Q27** (Epidemiológica Complexa): Linha 1: got={'cod_procedimento': '0701010142', 'total_procedimentos': 1889.0} exp={'cod_procedimento': '0302050019', 'total': 70536}
  SQL: `SELECT cod_procedimento, SUM(pa_qtdapr) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'S...`
- **Q28** (Epidemiológica Complexa): Linha 1: got={'total_internacoes': 6302, 'deslocamento_intermunicipal': 3417.0, 'proporcao_deslocamento_intermunicipal': 0.5422088226} exp={'total_internacoes': 65970, 'deslocamento_intermunicipal': 37219, 'pct_deslocamento': 56.4}
  SQL: `SELECT 
    COUNT(DISTINCT p.n_aih) AS total_internacoes,
    SUM(CASE WHEN e.clinical_deslocamento_territorial = 'deslo...`
- **Q29** (Epidemiológica Complexa): Linha 1: got={'ano_cmpt': 2022, 'mes_cmpt': 1, 'sistema': 'SIA', 'volume': 521} exp={'mes_cmpt': 1, 'sistema': 'SIA', 'total': 5898}
  SQL: `SELECT 
    ano_cmpt,
    mes_cmpt,
    sistema,
    COUNT(*) AS volume
FROM 
    processed
WHERE 
    ano_cmpt = 2022
 ...`
- **Q30** (Epidemiológica Complexa): Linha 1: got={'idade_media': 62.3961647195} exp={'idade_media': 62.4, 'idade_minima': 0, 'idade_maxima': 99}
  SQL: `SELECT AVG(idade_paciente) AS idade_media
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2...`
- **Q31** (Financeira): got=13990374.039999995 exp=175283428.77
  SQL: `SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes_ortopedicas
FROM 
    processed
WHERE 
    sistema ...`
- **Q32** (Financeira): got=2219.9895334814 exp=2655.37
  SQL: `SELECT 
    COALESCE(SUM(custo_total), 0) / COUNT(DISTINCT n_aih) AS custo_medio_por_internacao
FROM 
    processed
WHER...`
- **Q34** (Financeira): Linha 1: got={'cid_principal': 'M511', 'total_custo': 878799.22} exp={'cid_principal': 'S720', 'internacoes': 8980, 'custo_total': 29879588.9}
  SQL: `SELECT 
    cid_principal, 
    SUM(custo_total) AS total_custo
FROM 
    processed
WHERE 
    sistema = 'SIH' 
    AND ...`
- **Q35** (Financeira): Linha 1: got={'custo_total_ortopedicas_opme': None} exp={'internacoes_com_opme': 0, 'custo_total_opme': None}
  SQL: `SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_ortopedicas_opme
FROM 
    processed
WHERE 
    sistema = 'SIH'...`
- **Q36** (Financeira): got=2795569.489999999 exp=30640001.52
  SQL: `SELECT 
    COALESCE(SUM(val_sp), 0) AS total_val_sp
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = ...`
- **Q37** (Financeira): got=384.0449652749 exp=432.54
  SQL: `SELECT 
    COALESCE(SUM(custo_total) / NULLIF(SUM(dias_perm), 0), 0) AS custo_medio_por_dia
FROM 
    processed
WHERE 
...`
- **Q38** (Financeira): Linha 1: got={'cnes_estabelecimento': '2078015', 'total_custo': 1319752.55} exp={'cnes_estabelecimento': '2078015', 'internacoes': 2221, 'custo_total': 11218869.54}
  SQL: `SELECT 
    p.cnes_estabelecimento, 
    SUM(p.custo_total) AS total_custo
FROM 
    processed p
JOIN 
    enriched e ON...`
- **Q39** (Financeira): got=1945222.8200000033 exp=8721217.91
  SQL: `SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_ambulatorial_ortopedico_2022
FROM 
    processed
WHERE 
    sis...`
- **Q40** (Financeira): Linha 1: got={'proporcao_custo_sh_vs_honorarios': 4.0044808723} exp={'custo_servico_hospitalar': 144636796.74, 'honorarios_profissionais': 30640001.52, 'pct_hospitalar': 82.5}
  SQL: `SELECT 
    SUM(COALESCE(val_sh, 0)) / NULLIF(SUM(COALESCE(val_sp, 0)), 0) AS proporcao_custo_sh_vs_honorarios
FROM 
   ...`
- **Q41** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'total_internacoes': 476} exp={'mes_cmpt': 1, 'internacoes': 5209}
  SQL: `SELECT 
    mes_cmpt, 
    COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
   ...`
- **Q42** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'total_procedimentos': 521} exp={'mes_cmpt': 1, 'procedimentos': 5898}
  SQL: `SELECT 
    mes_cmpt, 
    COUNT(*) AS total_procedimentos
FROM 
    processed
WHERE 
    sistema = 'SIA'
    AND uf_ori...`
- **Q44** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'total_custo': 1015180.7699999993} exp={'mes_cmpt': 1, 'custo_total': 12942488.31}
  SQL: `SELECT 
    mes_cmpt, 
    SUM(COALESCE(custo_total, 0)) AS total_custo
FROM 
    processed
WHERE 
    sistema = 'SIH'
 ...`
- **Q45** (Temporal/Comparativa): Número de linhas: got=12 exp=24
  SQL: `SELECT 
    mes_cmpt,
    SUM(CASE WHEN icd_group = 'S00-T98' THEN 1 ELSE 0 END) AS internacoes_trauma,
    SUM(CASE WHE...`
- **Q46** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'total_obitos': 6} exp={'mes_cmpt': 1, 'total_obitos': 179}
  SQL: `SELECT 
    mes_cmpt, 
    COUNT(DISTINCT n_aih) AS total_obitos
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND ...`
- **Q47** (Temporal/Comparativa): Linha não encontrada: {'trimestre': 'Q1', 'permanencia_media_dias': 5.8}
  SQL: `SELECT 
    CEIL(mes_cmpt / 3.0) AS trimestre,
    AVG(dias_perm) AS permanencia_media
FROM 
    processed
WHERE 
    si...`
- **Q48** (Temporal/Comparativa): got=12 exp=10
  SQL: `SELECT 
    mes_cmpt, 
    AVG(custo_total) AS custo_medio
FROM 
    processed
WHERE 
    sistema = 'SIH' 
    AND uf_or...`
- **Q50** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'volume_internacoes': 118} exp={'mes_cmpt': 1, 'internacoes_idosos': 1987}
  SQL: `SELECT 
  mes_cmpt, 
  COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_o...`
- **Q53** (Epidemiológica Simples): got=2330 exp=13928
  SQL: `SELECT COUNT(DISTINCT n_aih) AS num_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND (cid_principal LIKE 'M%' OR i...`
- **Q54** (Epidemiológica Simples): got=5420 exp=45579
  SQL: `SELECT COUNT(*)
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND sexo_paciente = ...`
- **Q55** (Epidemiológica Simples): got=3856 exp=53666
  SQL: `SELECT COUNT(*)
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND idade_paciente >...`
- **Q59** (Epidemiológica Simples): got=481 exp=3619
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano...`
- **Q60** (Epidemiológica Simples): got=1239 exp=15489
  SQL: `SELECT COUNT(DISTINCT n_aih) 
FROM processed 
WHERE sistema = 'SIH' 
  AND uf_origem = 'SP' 
  AND ano_cmpt = 2022 
  AN...`
- **Q63** (Epidemiológica Simples): got=157 exp=975
  SQL: `SELECT COUNT(DISTINCT n_aih) 
FROM processed 
WHERE sistema = 'SIH' 
  AND uf_origem = 'SP' 
  AND ano_cmpt = 2022 
  AN...`
- **Q64** (Epidemiológica Simples): got=5228 exp=59437
  SQL: `SELECT COUNT(*)
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2022
  AND sexo_paciente = ...`
- **Q68** (Epidemiológica Simples): got=85 exp=399
  SQL: `SELECT COUNT(DISTINCT cod_munic_estabelecimento) AS num_municipios
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem ...`
- **Q69** (Epidemiológica Complexa): Linha 1: got={'cod_munic_estabelecimento': '355030', 'volume_procedimentos': 5094} exp={'cod_munic_estabelecimento': '355030', 'total': 33846}
  SQL: `SELECT 
  p.cod_munic_estabelecimento, 
  COUNT(*) AS volume_procedimentos
FROM 
  processed p
WHERE 
  p.sistema = 'SIA...`
- **Q70** (Epidemiológica Complexa): Número de linhas: got=5 exp=3
  SQL: `SELECT 
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 34 THEN '18-34...`
- **Q71** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'M869', 'numero_obitos': 13} exp={'cid_principal': 'S720', 'total_internacoes': 8980, 'total_obitos': 455.0}
  SQL: `SELECT 
    p.cid_principal, 
    COUNT(*) AS numero_obitos
FROM 
    processed p
JOIN 
    enriched e ON p.row_id = e.r...`
- **Q72** (Epidemiológica Complexa): Linha não encontrada: {'sexo_paciente': 'F', 'permanencia_media': 6.3}
  SQL: `SELECT 
    sexo_paciente, 
    AVG(dias_perm) AS permanencia_media
FROM 
    processed
WHERE 
    sistema = 'SIH'
    A...`
- **Q73** (Epidemiológica Complexa): Linha 1: got={'cnes_estabelecimento': '2078015', 'volume_procedimentos': 2090} exp={'cnes_estabelecimento': '2068974', 'total': 9214}
  SQL: `SELECT 
    p.cnes_estabelecimento, 
    COUNT(*) AS volume_procedimentos
FROM 
    processed p
WHERE 
    p.sistema = '...`
- **Q74** (Epidemiológica Complexa): Linha não encontrada: {'sexo_paciente': 'F', 'total_obitos': 1081.0, 'taxa_mortalidade': 4.18}
  SQL: `SELECT 
    sexo_paciente, 
    SUM(morte) * 100.0 / COUNT(DISTINCT n_aih) AS taxa_mortalidade
FROM 
    processed
WHERE...`
- **Q75** (Epidemiológica Complexa): Número de linhas: got=10 exp=6
  SQL: `SELECT 
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 34 THEN '18-34...`
- **Q77** (Epidemiológica Complexa): Linha não encontrada: {'faixa_etaria': '0-17', 'total': 3619}
  SQL: `SELECT 
    CASE 
        WHEN idade_paciente BETWEEN 0 AND 17 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 39...`
- **Q79** (Epidemiológica Complexa): Número de linhas: got=5 exp=3
  SQL: `SELECT 
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 34 THEN '18-34...`
- **Q81** (Epidemiológica Complexa): got=1465 exp=25807
  SQL: `SELECT COUNT(DISTINCT n_aih) AS num_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cm...`
- **Q82** (Epidemiológica Complexa): Linha 1: got={'diagnostico': 'M216', 'total': 1596} exp={'cid_principal': 'S720', 'total': 11096}
  SQL: `SELECT 
    main_icd AS diagnostico,
    COUNT(*) AS total
FROM 
    processed
WHERE 
    sistema = 'SIA'
    AND uf_ori...`
- **Q83** (Epidemiológica Complexa): got=355030 exp=173.1
  SQL: `SELECT 
    cod_munic_estabelecimento,
    cnes_estabelecimento,
    COUNT(DISTINCT n_aih) / COUNT(DISTINCT cnes_estabel...`
- **Q84** (Epidemiológica Complexa): Número de linhas: got=5 exp=4
  SQL: `SELECT 
    CASE 
        WHEN dias_perm <= 3 THEN '1-3 dias'
        WHEN dias_perm BETWEEN 4 AND 7 THEN '4-7 dias'
   ...`
- **Q85** (Financeira): got=4489529.78 exp=78009817.26
  SQL: `SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes_ortopedicas_idosos_2022
FROM 
    processed
WHERE 
...`
- **Q87** (Financeira): got=None exp=15904.56
  SQL: `SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_ortopedicas_uti
FROM 
    processed
WHERE 
    sistema = 'SIH'
...`
- **Q88** (Financeira): Linha não encontrada: {'sexo_paciente': 'F', 'custo_medio': 2684.14}
  SQL: `SELECT 
    sexo_paciente, 
    AVG(custo_total) AS custo_medio
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND u...`
- **Q89** (Financeira): got=927790.9099999999 exp=7450877.25
  SQL: `SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes_ortopedicas
FROM 
    processed
WHERE 
    sistema ...`
- **Q90** (Financeira): got=3083.8746901408 exp=4423.19
  SQL: `SELECT 
    AVG(custo_total) AS custo_medio
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    ...`
- **Q91** (Financeira): Linha 1: got={'cod_munic_estabelecimento': '350000', 'total_custo': 6704942.199999999} exp={'cod_munic_estabelecimento': '350000', 'custo_total': 91002293.78}
  SQL: `SELECT 
    p.cod_munic_estabelecimento, 
    SUM(p.custo_total) AS total_custo
FROM 
    processed p
JOIN 
    enriched...`
- **Q92** (Financeira): got=11194804.549999993 exp=144636796.74
  SQL: `SELECT COALESCE(SUM(val_sh), 0) AS total_valor_servico_hospitalar
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem =...`
- **Q93** (Financeira): Número de linhas: got=5 exp=3
  SQL: `SELECT 
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 34 THEN '18-34...`
- **Q94** (Financeira): got=1275788.72 exp=37073666.92
  SQL: `SELECT 
    COALESCE(SUM(val_uti), 0) AS total_valor_uti
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_orige...`
- **Q97** (Financeira): Linha 1: got={'cid_principal': 'M518', 'num_internacoes': 112, 'custo_medio': 7827.0964285714} exp={'cid_principal': 'S221', 'internacoes': 41, 'custo_medio': 14053.88}
  SQL: `SELECT 
    p.cid_principal, 
    COUNT(DISTINCT p.n_aih) AS num_internacoes,
    SUM(p.custo_total) / COUNT(DISTINCT p....`
- **Q98** (Financeira): got=182.6843369647 exp=83.05
  SQL: `SELECT 
    AVG(custo_total) AS custo_medio
FROM 
    processed
WHERE 
    sistema = 'SIA'
    AND uf_origem = 'SP'
    ...`
- **Q99** (Financeira): Linha 1: got={'cod_procedimento': '0702100048', 'total_custo': 46893518.21000075} exp={'cod_procedimento': '0701020369', 'custo_total': 1201460.4}
  SQL: `SELECT cod_procedimento, SUM(custo_total) AS total_custo
FROM processed
WHERE sistema = 'SIA' AND uf_origem = 'SP' AND a...`
- **Q100** (Financeira): got=1801433.3399999999 exp=35943333.49
  SQL: `SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    ...`
- **Q101** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 8, 'num_obitos': 10} exp={'mes_cmpt': 10, 'total_obitos': 224}
  SQL: `SELECT mes_cmpt, COUNT(*) AS num_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 202...`
- **Q103** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'custo_total_mensal': 287024.49} exp={'mes_cmpt': 1, 'custo_total': 5379118.99}
  SQL: `SELECT 
    mes_cmpt, 
    SUM(COALESCE(custo_total, 0)) AS custo_total_mensal
FROM 
    processed
WHERE 
    sistema = ...`
- **Q106** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'sexo_paciente': 'F', 'total_procedimentos': 288.0} exp={'mes_cmpt': 1, 'sexo_paciente': 'F', 'total': 2589}
  SQL: `SELECT 
    mes_cmpt, 
    sexo_paciente, 
    SUM(pa_qtdapr) AS total_procedimentos
FROM 
    processed
WHERE 
    sist...`
- **Q107** (Temporal/Comparativa): Número de linhas: got=4 exp=8
  SQL: `SELECT 
    FLOOR((mes_cmpt - 1) / 3) + 1 AS trimestre,
    icd_group,
    COUNT(DISTINCT n_aih) AS total_internacoes
FR...`
- **Q108** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 4, 'media_permanencia': 7.0149892934} exp={'mes_cmpt': 7, 'permanencia_media': 6.2}
  SQL: `SELECT mes_cmpt, AVG(dias_perm) AS media_permanencia
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND a...`
- **Q109** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'taxa_mortalidade': 1.2605042017} exp={'mes_cmpt': 1, 'internacoes': 5209, 'obitos': 179.0, 'taxa_mortalidade': 3.44}
  SQL: `SELECT 
    mes_cmpt,
    COUNT(DISTINCT CASE WHEN morte = 1 THEN n_aih END) * 100.0 / COUNT(DISTINCT n_aih) AS taxa_mor...`
- **Q111** (Temporal/Comparativa): Linha não encontrada: {'semestre': '1S', 'total': 31575}
  SQL: `SELECT 
    CASE 
        WHEN mes_cmpt BETWEEN 1 AND 6 THEN '1º Semestre'
        ELSE '2º Semestre'
    END AS semestr...`
- **Q113** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'sexo_paciente': 'F', 'volume_internacoes': 180} exp={'mes_cmpt': 1, 'sexo_paciente': 'F', 'total': 2058}
  SQL: `SELECT 
    mes_cmpt, 
    sexo_paciente, 
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
WHERE 
  ...`
- **Q114** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'total_procedimentos': 565.0} exp={'mes_cmpt': 1, 'total': 5898}
  SQL: `SELECT mes_cmpt, SUM(pa_qtdapr) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND...`
- **Q115** (Temporal/Comparativa): Linha não encontrada: {'trimestre': 'Q1', 'total': 21523}
  SQL: `SELECT 
  CEIL(mes_cmpt / 3.0) AS trimestre,
  SUM(pa_qtdapr) AS total_procedimentos
FROM 
  processed
WHERE 
  sistema ...`
- **Q116** (Temporal/Comparativa): Linha 1: got={'ano_cmpt': 2022, 'mes_cmpt': 1, 'custo_total_mensal': 95929.0399999997} exp={'mes_cmpt': 1, 'custo_total': 512553.88}
  SQL: `SELECT 
    ano_cmpt,
    mes_cmpt,
    SUM(COALESCE(custo_total, 0)) AS custo_total_mensal
FROM 
    processed
WHERE 
 ...`
- **Q117** (Temporal/Comparativa): Linha não encontrada: {'semestre': '1S', 'custo_total': 82207816.93}
  SQL: `SELECT 
    CASE 
        WHEN mes_cmpt BETWEEN 1 AND 6 THEN '1º Semestre'
        WHEN mes_cmpt BETWEEN 7 AND 12 THEN '...`

---
_Gerado por scripts/evaluate_benchmark.py — SUS Data RAG — USF/Mestrado_