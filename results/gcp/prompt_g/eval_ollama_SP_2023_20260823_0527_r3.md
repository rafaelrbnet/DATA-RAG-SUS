# Benchmark Evaluation Report — SUS Data RAG

**Model:** ollama  
**Run date:** 2026-08-23 05:50  
**API:** http://127.0.0.1:8000  

## Summary

| Metric | Value |
|---|---|
| Total queries | 118 |
| Correct (EA numerator) | 78 |
| **Execution Accuracy (EA)** | **66.1%** |
| Wilson IC 95% | [57.2%, 74.0%] |
| Correct via flexible match (alias diferente) | 57 |
| API errors / timeouts | 13 |
| Mean Time-to-Answer | 5.8s |

> **Nota metodológica — Scoring:** A Execution Accuracy avalia se o SQL gerado
> retorna os mesmos **valores** que o gold-standard, independente do nome das colunas.
> Quando o LLM usa um alias diferente mas retorna os dados corretos (match posicional),
> a query é contada como correta e sinalizada com `~`. Esta decisão segue a definição
> de EA em [Lee et al., 2022] — comparação de conjuntos de resultado, não de SQL texto.

## EA by Category

| Category | Correct | Total | EA |
|---|---:|---:|---:|
| Epidemiológica Simples | 25 | 33 | 75.8% |
| Epidemiológica Complexa | 14 | 31 | 45.2% |
| Financeira | 18 | 26 | 69.2% |
| Temporal/Comparativa | 21 | 28 | 75.0% |

## Query Results

### ❌ Q01 — Epidemiológica Simples

**Q:** Total de internações ortopédicas (M00-M99 e S00-S99) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T99');
```

**Resultado:** `got=6880 exp=69487`
**TTA:** 4924ms

---

### ✅ Q02 — Epidemiológica Simples

**Q:** Total de procedimentos ambulatoriais ortopédicos em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(*) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=105364 exp=105364`
**TTA:** 5888ms

---

### ✅ Q03 — Epidemiológica Simples

**Q:** Total de internações por fratura de fêmur (S72) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%';
```

**Resultado:** `got=30272 exp=30272`
**TTA:** 4282ms

---

### ❌ Q04 — Epidemiológica Simples

**Q:** Total de internações por osteoartrose (M16, M17) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M16' OR icd_group = 'M17');
```

**Resultado:** `got=0 exp=165`
**TTA:** 4676ms

---

### ⚠️ Q05 — Epidemiológica Simples

**Q:** Número de óbitos em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND obito = 'S';
```

**Erro:** Falha DuckDB: Binder Error: Referenced column "obito" not found in FROM clause!
Candidate bindings: "morte", "gestao", "row_id", "cod_munic_estabelecimento", "sistema"

LINE 7:   AND obito = 'S';
              ^

---

### ✅ Q06 — Epidemiológica Simples

**Q:** Total de internações por traumatismos (S00-T98) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND icd_group = 'S00-T98';
```

**Resultado:** `got=62607 exp=62607`
**TTA:** 4381ms

---

### ✅ Q07 — Epidemiológica Simples

**Q:** Total de internações por doenças osteomusculares (M00-M99) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND icd_group = 'M00-M99';
```

**Resultado:** `got=6880 exp=6880`
**TTA:** 4400ms

---

### ❌ Q08 — Epidemiológica Simples

**Q:** Número de mulheres internadas por causa ortopédica em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(*) AS total_mulheres_internadas
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND sexo_paciente = 'F'
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=27029 exp=27027`
**TTA:** 5150ms

---

### ❌ Q09 — Epidemiológica Simples

**Q:** Número de homens internados por causa ortopédica em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(*) AS total_homens_internados
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND sexo_paciente = 'M'
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=42489 exp=42460`
**TTA:** 5113ms

---

### ✅ Q10 — Epidemiológica Simples

**Q:** Número de internações ortopédicas de idosos (60 anos ou mais) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND idade_paciente >= 60;
```

**Resultado:** `got=28165 exp=28165`
**TTA:** 5322ms

---

### ✅ Q11 — Epidemiológica Simples

**Q:** Número de municípios distintos com internação ortopédica registrada em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT cod_munic_estabelecimento) AS num_municipios_distintos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=215 exp=215`
**TTA:** 5203ms

---

### ✅ Q12 — Epidemiológica Simples

**Q:** Número de estabelecimentos distintos com internação ortopédica em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT cnes_estabelecimento) AS total_estabelecimentos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=385 exp=385`
**TTA:** 5087ms

---

### ✅ Q13 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com permanência superior a 7 dias em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND dias_perm > 7;
```

**Resultado:** `got=17778 exp=17778`
**TTA:** 5256ms

---

### ❌ Q14 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com uso de UTI em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND val_uti > 0;
```

**Resultado:** `got=13764 exp=1`
**TTA:** 5287ms

---

### ✅ Q15 — Epidemiológica Simples

**Q:** Número de internações por fratura de quadril (S72) em pacientes com 70 anos ou mais em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
  AND idade_paciente >= 70;
```

**Resultado:** `got=15626 exp=15626`
**TTA:** 4620ms

---

### ✅ Q16 — Epidemiológica Complexa

**Q:** Top 10 CIDs ortopédicos por volume de internação em SP em 2023

**SQL gerado:**
```sql
SELECT cid_principal,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cid_principal
ORDER BY internacoes DESC
LIMIT 10;
```

**Resultado:** `10 linhas corretas (flexível)`
**TTA:** 5643ms

---

### ✅ Q17 — Epidemiológica Complexa

**Q:** Top 5 municípios de SP com maior número de internações ortopédicas em 2023

**SQL gerado:**
```sql
SELECT cod_munic_estabelecimento,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cod_munic_estabelecimento
ORDER BY internacoes DESC
LIMIT 5;
```

**Resultado:** `5 linhas corretas (flexível)`
**TTA:** 5997ms

---

### ✅ Q18 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por faixa etária (0-17, 18-59, 60+) em SP em 2023

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN idade_paciente BETWEEN 0 AND 17 THEN '0-17'
    WHEN idade_paciente BETWEEN 18 AND 59 THEN '18-59'
    ELSE '60+'
  END AS faixa_etaria,
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY faixa_etaria
ORDER BY faixa_etaria;
```

**Resultado:** `3 linhas corretas, ordem livre (flexível)`
**TTA:** 7848ms

---

### ⚠️ Q19 — Epidemiológica Complexa

**Q:** Taxa de mortalidade hospitalar por CID ortopédico (top 5) em SP em 2023

**SQL gerado:**
```sql
SELECT 
    cid_principal,
    COUNT(CASE WHEN fator_mortalidade = 1 THEN 1 ELSE NULL END) AS mortes,
    COUNT(*) AS total_internacoes,
    ROUND(100.0 * COUNT(CASE WHEN fator_mortalidade = 1 THEN 1 ELSE NULL END) / COUNT(*), 2) AS taxa_mortalidade
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY 
    cid_principal
ORDER BY 
    mortes DESC
LIMIT 5;
```

**Erro:** Falha DuckDB: Binder Error: Referenced column "fator_mortalidade" not found in FROM clause!
Candidate bindings: "custo_total", "pa_motsai", "raca_cor_paciente", "pa_autoriz", "proc_solic"

LINE 3:     COUNT(CASE WHEN fator_mortalidade = 1 THEN 1 ELSE NULL END) AS mortes,
                            ^

---

### ✅ Q20 — Epidemiológica Complexa

**Q:** Top 10 CIDs ambulatoriais ortopédicos por volume de produção em SP em 2023

**SQL gerado:**
```sql
SELECT cid_principal,
  COUNT(*) AS volume_producao
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cid_principal
ORDER BY volume_producao DESC
LIMIT 10;
```

**Resultado:** `10 linhas corretas (flexível)`
**TTA:** 5558ms

---

### ❌ Q21 — Epidemiológica Complexa

**Q:** Proporção de internações ortopédicas por sexo em SP em 2023

**SQL gerado:**
```sql
SELECT 
  sexo_paciente,
  COUNT(*) AS total_internacoes,
  ROUND(100.0 * COUNT(*) / SUM(COUNT(*)) OVER (), 1) AS pct
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY 
  sexo_paciente
ORDER BY 
  total_internacoes DESC;
```

**Resultado:** `Linha não encontrada: {'sexo_paciente': 'M', 'total_internacoes': 42460, 'pct': 61.1}`
**TTA:** 6800ms

---

### ❌ Q22 — Epidemiológica Complexa

**Q:** Número de internações por fratura de fêmur (S72) em idosos (60+) comparado ao total em SP em 2023

**SQL gerado:**
```sql
SELECT
  SUM(CASE WHEN idade_paciente >= 60 THEN 1 ELSE 0 END) AS internacoes_idosos,
  COUNT(DISTINCT n_aih) AS internacoes_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%';
```

**Resultado:** `Linha 1: got={'internacoes_idosos': 19598.0, 'internacoes_total': 30272} exp={'total_s72': 30272, 's72_idosos': 19598.0, 'pct_idosos': 64.7}`
**TTA:** 5319ms

---

### ❌ Q23 — Epidemiológica Complexa

**Q:** Top 5 CIDs ortopédicos com maior permanência média hospitalar em SP em 2023

**SQL gerado:**
```sql
SELECT cid_principal,
  ROUND(AVG(dias_perm), 1) AS permanencia_media_dias
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cid_principal
ORDER BY permanencia_media_dias DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cid_principal': 'S141', 'permanencia_media_dias': 73.0} exp={'cid_principal': 'S272', 'total_internacoes': 49, 'permanencia_media_dias': 14.3}`
**TTA:** 5901ms

---

### ✅ Q24 — Epidemiológica Complexa

**Q:** Top 5 estabelecimentos com maior volume de internação ortopédica em SP em 2023

**SQL gerado:**
```sql
SELECT cnes_estabelecimento,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cnes_estabelecimento
ORDER BY internacoes DESC
LIMIT 5;
```

**Resultado:** `5 linhas corretas (flexível)`
**TTA:** 5460ms

---

### ❌ Q25 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por raça/cor do paciente em SP em 2023

**SQL gerado:**
```sql
SELECT raca_cor_paciente,
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY raca_cor_paciente
ORDER BY total_internacoes DESC;
```

**Resultado:** `Linha não encontrada: {'raca_cor_paciente': '01', 'total': 42626, 'pct': 61.3}`
**TTA:** 5386ms

---

### ⚠️ Q26 — Epidemiológica Complexa

**Q:** CIDs ortopédicos com maior número de dias totais de internação em SP em 2023

**SQL gerado:**
```sql
SELECT cid_principal,
  SUM(dias_totais_internacao) AS total_dias_internacao
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cid_principal
ORDER BY total_dias_internacao DESC
LIMIT 1;
```

**Erro:** Falha DuckDB: Binder Error: Referenced column "dias_totais_internacao" not found in FROM clause!
Candidate bindings: "dt_inter", "dias_perm", "tipo_financiamento", "dt_saida", "cod_munic_estabelecimento"

LINE 2:   SUM(dias_totais_internacao) AS total_dias_internacao
              ^

---

### ✅ Q27 — Epidemiológica Complexa

**Q:** Top 5 procedimentos ambulatoriais ortopédicos mais realizados em SP em 2023

**SQL gerado:**
```sql
SELECT cod_procedimento,
  COUNT(*) AS total_realizacoes
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cod_procedimento
ORDER BY total_realizacoes DESC
LIMIT 5;
```

**Resultado:** `5 linhas corretas (flexível)`
**TTA:** 5674ms

---

### ⚠️ Q28 — Epidemiológica Complexa

**Q:** Proporção de internações ortopédicas com paciente de outro município em SP em 2023

**SQL gerado:**
```sql
SELECT 
  ROUND(100.0 * COUNT(CASE WHEN uf_origem <> uf_destino THEN 1 ELSE NULL END) / COUNT(*), 2) AS pct_internacoes_outro_municipio
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Erro:** Falha DuckDB: Binder Error: Referenced column "uf_destino" not found in FROM clause!
Candidate bindings: "uf", "mun_res_tipo", "uti_mes_to", "mun_res_nome", "gestao"

LINE 2:   ROUND(100.0 * COUNT(CASE WHEN uf_origem <> uf_destino THEN 1 ELSE NULL END) / COUNT(*), 2) AS pct_inte...
                                                     ^

---

### ❌ Q29 — Epidemiológica Complexa

**Q:** Comparativo de volume mensal SIA vs SIH ortopédico em SP em 2023

**SQL gerado:**
```sql
SELECT
  mes_cmpt,
  SUM(CASE WHEN sistema = 'SIA' THEN 1 ELSE 0 END) AS internacoes_sia,
  SUM(CASE WHEN sistema = 'SIH' THEN 1 ELSE 0 END) AS internacoes_sih
FROM processed
WHERE uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Resultado:** `Número de linhas: got=12 exp=24`
**TTA:** 6289ms

---

### ❌ Q30 — Epidemiológica Complexa

**Q:** Idade média dos pacientes internados por fratura de fêmur (S72) em SP em 2023

**SQL gerado:**
```sql
SELECT ROUND(AVG(idade_paciente), 1) AS idade_media
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%';
```

**Resultado:** `Linha 1: got={'idade_media': 62.4} exp={'idade_media': 62.4, 'idade_minima': 0.0, 'idade_maxima': 99.0}`
**TTA:** 4301ms

---

### ✅ Q31 — Financeira

**Q:** Custo total de todas as internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=187220821.79 exp=187220821.79`
**TTA:** 5229ms

---

### ✅ Q32 — Financeira

**Q:** Custo médio por internação ortopédica em SP em 2023

**SQL gerado:**
```sql
SELECT ROUND(AVG(custo_total), 2) AS custo_medio_por_internacao
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=2693.13 exp=2693.13`
**TTA:** 5151ms

---

### ✅ Q33 — Financeira

**Q:** Custo total de internações por fratura de fêmur (S72) em SP em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%';
```

**Resultado:** `got=82692089.44 exp=82692089.44`
**TTA:** 4565ms

---

### ❌ Q34 — Financeira

**Q:** Top 5 CIDs ortopédicos com maior custo total de internação em SP em 2023

**SQL gerado:**
```sql
SELECT cid_principal,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cid_principal
ORDER BY custo_total DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cid_principal': 'S720', 'custo_total': 30400546.73} exp={'cid_principal': 'S720', 'internacoes': 9059, 'custo_total': 30400546.73}`
**TTA:** 6021ms

---

### ❌ Q35 — Financeira

**Q:** Custo total de internações ortopédicas com uso de OPME (val_ortp > 0) em SP em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND val_ortp > 0;
```

**Resultado:** `Linha 1: got={'custo_total': None} exp={'internacoes_com_opme': 0, 'custo_total_opme': None}`
**TTA:** 5566ms

---

### ✅ Q36 — Financeira

**Q:** Valor total de honorários profissionais (val_sp) em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(val_sp, 0)), 2) AS valor_total_honorarios
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=32810269.18 exp=32810269.18`
**TTA:** 5269ms

---

### ✅ Q37 — Financeira

**Q:** Custo médio por dia de internação ortopédica em SP em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)) / SUM(COALESCE(dias_perm, 0)), 2) AS custo_medio_por_dia
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=441.21 exp=441.21`
**TTA:** 5913ms

---

### ❌ Q38 — Financeira

**Q:** Top 5 estabelecimentos com maior custo total de internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT cnes_estabelecimento,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cnes_estabelecimento
ORDER BY custo_total DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cnes_estabelecimento': '2078015', 'custo_total': 11295149.42} exp={'cnes_estabelecimento': '2078015', 'internacoes': 2257, 'custo_total': 11295149.42}`
**TTA:** 6346ms

---

### ✅ Q39 — Financeira

**Q:** Custo total de procedimentos ambulatoriais ortopédicos em SP em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=9092221.75 exp=9092221.75`
**TTA:** 5248ms

---

### ⚠️ Q40 — Financeira

**Q:** Proporção custo serviço hospitalar vs honorários em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT 
  ROUND(100.0 * SUM(COALESCE(val_sh, 0)) / SUM(COALESCE(custo_total, 0)), 2) AS pct_servico_hospitalar,
  ROUND(100.0 * SUM(COALESCE(val_honorarios, 0)) / SUM(COALESCE(custo_total, 0)), 2) AS pct_honorarios
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Erro:** Falha DuckDB: Binder Error: Referenced column "val_honorarios" not found in FROM clause!
Candidate bindings: "val_sh", "val_ortp", "val_uci", "val_uti", "pa_docorig"

LINE 3:   ROUND(100.0 * SUM(COALESCE(val_honorarios, 0)) / SUM(COALESCE(custo_total, 0)), 2)...
                                     ^

---

### ✅ Q41 — Temporal/Comparativa

**Q:** Distribuição mensal de internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT 
  mes_cmpt,
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY 
  mes_cmpt
ORDER BY 
  mes_cmpt;
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 6008ms

---

### ✅ Q42 — Temporal/Comparativa

**Q:** Distribuição mensal de procedimentos ambulatoriais ortopédicos em SP em 2023

**SQL gerado:**
```sql
SELECT 
  mes_cmpt,
  COUNT(*) AS total_procedimentos
FROM 
  processed
WHERE 
  sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY 
  mes_cmpt
ORDER BY 
  mes_cmpt;
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 5749ms

---

### ✅ Q43 — Temporal/Comparativa

**Q:** Mês com maior número de internações por fratura de fêmur (S72) em SP em 2023

**SQL gerado:**
```sql
SELECT mes_cmpt,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
GROUP BY mes_cmpt
ORDER BY internacoes DESC
LIMIT 1;
```

**Resultado:** `got=8 exp=8`
**TTA:** 4972ms

---

### ✅ Q44 — Temporal/Comparativa

**Q:** Evolução mensal do custo total de internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT
  mes_cmpt,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Resultado:** `12 linhas corretas (exato)`
**TTA:** 5974ms

---

### ❌ Q45 — Temporal/Comparativa

**Q:** Volume mensal de internações por trauma (S00-T98) vs doenças musculoesqueléticas (M00-M99) em SP em 2023

**SQL gerado:**
```sql
SELECT
  mes_cmpt,
  COUNT(CASE WHEN icd_group = 'S00-T98' THEN 1 END) AS internacoes_trauma,
  COUNT(CASE WHEN icd_group = 'M00-M99' THEN 1 END) AS internacoes_musculoesqueléticas
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Resultado:** `Número de linhas: got=12 exp=24`
**TTA:** 7577ms

---

### ⚠️ Q46 — Temporal/Comparativa

**Q:** Evolução mensal de óbitos em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT
  mes_cmpt,
  COUNT(DISTINCT n_aih) AS total_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND tipo_internacao = 'ÓBITO'
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Erro:** Falha DuckDB: Binder Error: Referenced column "tipo_internacao" not found in FROM clause!
Candidate bindings: "tipo_financiamento", "dt_inter", "etnia_paciente", "competencia_ano_mes", "cid_princ"

LINE 9:   AND tipo_internacao = 'ÓBITO'
              ^

---

### ✅ Q47 — Temporal/Comparativa

**Q:** Permanência média por trimestre nas internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN mes_cmpt BETWEEN 1 AND 3 THEN 'Q1'
    WHEN mes_cmpt BETWEEN 4 AND 6 THEN 'Q2'
    WHEN mes_cmpt BETWEEN 7 AND 9 THEN 'Q3'
    ELSE 'Q4'
  END AS trimestre,
  ROUND(AVG(dias_perm), 1) AS permanencia_media_dias
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY trimestre
ORDER BY trimestre;
```

**Resultado:** `4 linhas corretas, ordem livre (exato)`
**TTA:** 7532ms

---

### ✅ Q48 — Temporal/Comparativa

**Q:** Mês com maior custo médio por internação ortopédica em SP em 2023

**SQL gerado:**
```sql
SELECT mes_cmpt,
  ROUND(AVG(custo_total), 2) AS custo_medio
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY mes_cmpt
ORDER BY custo_medio DESC
LIMIT 1;
```

**Resultado:** `got=1 exp=1`
**TTA:** 5457ms

---

### ✅ Q49 — Temporal/Comparativa

**Q:** Sazonalidade mensal de fraturas de fêmur (S72) em SP em 2023

**SQL gerado:**
```sql
SELECT
  mes_cmpt,
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 4967ms

---

### ✅ Q50 — Temporal/Comparativa

**Q:** Volume mensal de internações ortopédicas de idosos (60 anos ou mais) em SP em 2023

**SQL gerado:**
```sql
SELECT 
  mes_cmpt,
  COUNT(DISTINCT n_aih) AS internacoes
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND idade_paciente >= 60
GROUP BY 
  mes_cmpt
ORDER BY 
  mes_cmpt;
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 6310ms

---

### ✅ Q51 — Epidemiológica Simples

**Q:** Número de internações por fratura de fêmur (S72) em mulheres em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
  AND sexo_paciente = 'F';
```

**Resultado:** `got=15639 exp=15639`
**TTA:** 4578ms

---

### ✅ Q52 — Epidemiológica Simples

**Q:** Número de internações por fratura de fêmur (S72) em homens em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
  AND sexo_paciente = 'M';
```

**Resultado:** `got=14633 exp=14633`
**TTA:** 4565ms

---

### ✅ Q53 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com permanência de 1 dia ou menos em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND dias_perm <= 1;
```

**Resultado:** `got=14718 exp=14718`
**TTA:** 5246ms

---

### ✅ Q54 — Epidemiológica Simples

**Q:** Número de procedimentos ambulatoriais ortopédicos realizados em mulheres em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(*) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND sexo_paciente = 'F'
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=47792 exp=47792`
**TTA:** 5040ms

---

### ✅ Q55 — Epidemiológica Simples

**Q:** Número de procedimentos ambulatoriais ortopédicos realizados por idosos (60 anos ou mais) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(*) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND idade_paciente >= 60;
```

**Resultado:** `got=55711 exp=55711`
**TTA:** 5087ms

---

### ✅ Q56 — Epidemiológica Simples

**Q:** Número de internações por fratura de tíbia e perônio (S82) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S82%';
```

**Resultado:** `got=6827 exp=6827`
**TTA:** 4262ms

---

### ✅ Q57 — Epidemiológica Simples

**Q:** Número de internações por fratura de antebraço (S52) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S52%';
```

**Resultado:** `got=2264 exp=2264`
**TTA:** 4248ms

---

### ✅ Q58 — Epidemiológica Simples

**Q:** Número de internações por fratura de ombro e úmero (S42) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S42%';
```

**Resultado:** `got=1391 exp=1391`
**TTA:** 4259ms

---

### ✅ Q59 — Epidemiológica Simples

**Q:** Número de internações ortopédicas em crianças (menores de 18 anos) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND idade_paciente < 18;
```

**Resultado:** `got=4040 exp=4040`
**TTA:** 5321ms

---

### ✅ Q60 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com permanência entre 8 e 30 dias em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND dias_perm BETWEEN 8 AND 30;
```

**Resultado:** `got=16748 exp=16748`
**TTA:** 4959ms

---

### ❌ Q61 — Epidemiológica Simples

**Q:** Número de internações por artrose de quadril e joelho (M16 e M17) em idosos (60 anos ou mais) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M16' OR icd_group = 'M17')
  AND idade_paciente >= 60;
```

**Resultado:** `got=0 exp=100`
**TTA:** 5105ms

---

### ✅ Q62 — Epidemiológica Simples

**Q:** Número de internações por fratura de fêmur (S72) em mulheres com 70 anos ou mais em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
  AND sexo_paciente = 'F'
  AND idade_paciente >= 70;
```

**Resultado:** `got=11171 exp=11171`
**TTA:** 4974ms

---

### ✅ Q63 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com permanência superior a 30 dias em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND dias_perm > 30;
```

**Resultado:** `got=1037 exp=1037`
**TTA:** 5276ms

---

### ✅ Q64 — Epidemiológica Simples

**Q:** Número de procedimentos ambulatoriais ortopédicos realizados em homens em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(*) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND sexo_paciente = 'M'
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=57572 exp=57572`
**TTA:** 5028ms

---

### ✅ Q65 — Epidemiológica Simples

**Q:** Número de internações por traumatismos (grupo S00-T98) em idosos (60 anos ou mais) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND icd_group = 'S00-T98'
  AND idade_paciente >= 60;
```

**Resultado:** `got=26253 exp=26253`
**TTA:** 4756ms

---

### ✅ Q66 — Epidemiológica Simples

**Q:** Número de internações por doenças osteomusculares (grupo M00-M99) em mulheres em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND sexo_paciente = 'F'
  AND icd_group = 'M00-M99';
```

**Resultado:** `got=2554 exp=2554`
**TTA:** 4716ms

---

### ⚠️ Q67 — Epidemiológica Simples

**Q:** Número de óbitos em internações por fratura de fêmur (S72) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
  AND obito = 'S';
```

**Erro:** Falha DuckDB: Binder Error: Referenced column "obito" not found in FROM clause!
Candidate bindings: "morte", "gestao", "row_id", "cod_munic_estabelecimento", "sistema"

LINE 7:   AND obito = 'S';
              ^

---

### ✅ Q68 — Epidemiológica Simples

**Q:** Número de municípios distintos com registro de procedimentos ambulatoriais ortopédicos em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT cod_munic_estabelecimento) AS num_municipios
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=400 exp=400`
**TTA:** 5078ms

---

### ✅ Q69 — Epidemiológica Complexa

**Q:** Top 5 municípios por volume de procedimentos ambulatoriais ortopédicos em SP em 2023

**SQL gerado:**
```sql
SELECT cod_munic_estabelecimento,
  COUNT(*) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cod_munic_estabelecimento
ORDER BY total_procedimentos DESC
LIMIT 5;
```

**Resultado:** `5 linhas corretas (flexível)`
**TTA:** 5899ms

---

### ❌ Q70 — Epidemiológica Complexa

**Q:** Distribuição de procedimentos ambulatoriais ortopédicos por faixa etária em SP em 2023

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN idade_paciente BETWEEN 0 AND 17 THEN '0-17'
    WHEN idade_paciente BETWEEN 18 AND 29 THEN '18-29'
    WHEN idade_paciente BETWEEN 30 AND 44 THEN '30-44'
    WHEN idade_paciente BETWEEN 45 AND 59 THEN '45-59'
    ELSE '60+'
  END AS faixa_etaria,
  COUNT(*) AS total
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY faixa_etaria
ORDER BY faixa_etaria;
```

**Resultado:** `Número de linhas: got=5 exp=3`
**TTA:** 8662ms

---

### ⚠️ Q71 — Epidemiológica Complexa

**Q:** Top 5 diagnósticos com maior número de óbitos em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT cid_principal,
  COUNT(DISTINCT n_aih) AS obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND outcome = 'ÓBITO'
GROUP BY cid_principal
ORDER BY obitos DESC
LIMIT 5;
```

**Erro:** Falha DuckDB: Binder Error: Referenced column "outcome" not found in FROM clause!
Candidate bindings: "morte", "uti_mes_to", "custo_total", "ano_cmpt", "tipo_financiamento"

LINE 8:   AND outcome = 'ÓBITO'
              ^

---

### ✅ Q72 — Epidemiológica Complexa

**Q:** Permanência hospitalar média por sexo em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT 
  sexo_paciente,
  ROUND(AVG(dias_perm), 1) AS permanencia_media_dias
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY 
  sexo_paciente
ORDER BY 
  sexo_paciente;
```

**Resultado:** `2 linhas corretas, ordem livre (flexível)`
**TTA:** 6104ms

---

### ✅ Q73 — Epidemiológica Complexa

**Q:** Top 10 estabelecimentos por volume de procedimentos ambulatoriais ortopédicos em SP em 2023

**SQL gerado:**
```sql
SELECT cnes_estabelecimento,
  COUNT(*) AS procedimentos_ortopédicos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cnes_estabelecimento
ORDER BY procedimentos_ortopédicos DESC
LIMIT 10;
```

**Resultado:** `10 linhas corretas (flexível)`
**TTA:** 6109ms

---

### ⚠️ Q74 — Epidemiológica Complexa

**Q:** Taxa de mortalidade por sexo em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT 
  sexo_paciente,
  COUNT(CASE WHEN obito = 'S' THEN 1 END) AS mortes,
  COUNT(*) AS total_internacoes,
  ROUND(100.0 * COUNT(CASE WHEN obito = 'S' THEN 1 END) / COUNT(*), 2) AS taxa_mortalidade
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY 
  sexo_paciente
ORDER BY 
  taxa_mortalidade DESC;
```

**Erro:** Falha DuckDB: Binder Error: Referenced column "obito" not found in FROM clause!
Candidate bindings: "morte", "gestao", "row_id", "cod_munic_estabelecimento", "sistema"

LINE 3:   COUNT(CASE WHEN obito = 'S' THEN 1 END) AS mortes,
                          ^

---

### ⚠️ Q75 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por grupo diagnóstico (M e S) e faixa etária em SP em 2023

**SQL gerado:**
```sql
SELECT 
  icd_group,
  faixa_etaria,
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY 
  icd_group, faixa_etaria
ORDER BY 
  icd_group, faixa_etaria;
```

**Erro:** Falha DuckDB: Binder Error: Referenced column "faixa_etaria" not found in FROM clause!
Candidate bindings: "pa_autoriz", "faec_tp", "raca_cor_paciente", "main_icd", "cid_notif"

LINE 13:   icd_group, faixa_etaria
                      ^

---

### ❌ Q76 — Epidemiológica Complexa

**Q:** Top 5 diagnósticos de traumatismo com maior permanência média em SP em 2023 (mínimo 40 internações)

**SQL gerado:**
```sql
SELECT cid_principal,
  AVG(dias_perm) AS permanencia_media_dias,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND icd_group = 'S00-T98'
GROUP BY cid_principal
HAVING COUNT(DISTINCT n_aih) >= 40
ORDER BY permanencia_media_dias DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cid_principal': 'S272', 'permanencia_media_dias': 14.2653061224, 'internacoes': 49} exp={'cid_principal': 'S272', 'internacoes': 49, 'permanencia_media': 14.3}`
**TTA:** 5818ms

---

### ✅ Q77 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por faixa etária (0-17, 18-39, 40-59, 60-79, 80+) em SP em 2023

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN idade_paciente BETWEEN 0 AND 17 THEN '0-17'
    WHEN idade_paciente BETWEEN 18 AND 39 THEN '18-39'
    WHEN idade_paciente BETWEEN 40 AND 59 THEN '40-59'
    WHEN idade_paciente BETWEEN 60 AND 79 THEN '60-79'
    ELSE '80+'
  END AS faixa_etaria,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY faixa_etaria
ORDER BY faixa_etaria;
```

**Resultado:** `5 linhas corretas, ordem livre (flexível)`
**TTA:** 10624ms

---

### ✅ Q78 — Epidemiológica Complexa

**Q:** Top 5 estabelecimentos com maior volume de internações por fratura de fêmur (S72) em SP em 2023

**SQL gerado:**
```sql
SELECT cnes_estabelecimento,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
GROUP BY cnes_estabelecimento
ORDER BY internacoes DESC
LIMIT 5;
```

**Resultado:** `5 linhas corretas (exato)`
**TTA:** 5234ms

---

### ❌ Q79 — Epidemiológica Complexa

**Q:** Permanência hospitalar média por faixa etária em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN idade_paciente < 18 THEN '0-17'
    WHEN idade_paciente < 30 THEN '18-29'
    WHEN idade_paciente < 40 THEN '30-39'
    WHEN idade_paciente < 50 THEN '40-49'
    WHEN idade_paciente < 60 THEN '50-59'
    WHEN idade_paciente < 70 THEN '60-69'
    ELSE '70+'
  END AS faixa_etaria,
  ROUND(AVG(dias_perm), 1) AS permanencia_media_dias
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY faixa_etaria
ORDER BY faixa_etaria;
```

**Resultado:** `Número de linhas: got=7 exp=3`
**TTA:** 10334ms

---

### ✅ Q80 — Epidemiológica Complexa

**Q:** Top 5 diagnósticos de doença osteomuscular (M00-M99) por número de internações em SP em 2023

**SQL gerado:**
```sql
SELECT cid_principal,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND icd_group = 'M00-M99'
GROUP BY cid_principal
ORDER BY internacoes DESC
LIMIT 5;
```

**Resultado:** `5 linhas corretas (flexível)`
**TTA:** 5083ms

---

### ✅ Q81 — Epidemiológica Complexa

**Q:** Número de internações ortopédicas com permanência entre 3 e 7 dias em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND dias_perm BETWEEN 3 AND 7;
```

**Resultado:** `got=27009 exp=27009`
**TTA:** 5352ms

---

### ✅ Q82 — Epidemiológica Complexa

**Q:** Top 5 diagnósticos ambulatoriais ortopédicos em idosos (60 anos ou mais) em SP em 2023

**SQL gerado:**
```sql
SELECT cid_principal,
  COUNT(*) AS total_internacoes
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND idade_paciente >= 60
GROUP BY cid_principal
ORDER BY total_internacoes DESC
LIMIT 5;
```

**Resultado:** `5 linhas corretas (flexível)`
**TTA:** 5914ms

---

### ⚠️ Q83 — Epidemiológica Complexa

**Q:** Média de internações ortopédicas por estabelecimento em SP em 2023

**SQL gerado:**
```sql
SELECT cnes_estabelecimento,
  ROUND(AVG(COUNT(DISTINCT n_aih)), 1) AS media_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cnes_estabelecimento
ORDER BY media_internacoes DESC;
```

**Erro:** Falha DuckDB: Binder Error: aggregate function calls cannot be nested

LINE 2:   ROUND(AVG(COUNT(DISTINCT n_aih)), 1) AS media_internacoes
                    ^

---

### ❌ Q84 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por faixa de permanência em SP em 2023

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN dias_perm BETWEEN 0 AND 7 THEN '0-7 dias'
    WHEN dias_perm BETWEEN 8 AND 14 THEN '8-14 dias'
    WHEN dias_perm BETWEEN 15 AND 21 THEN '15-21 dias'
    WHEN dias_perm BETWEEN 22 AND 28 THEN '22-28 dias'
    ELSE '29+ dias'
  END AS faixa_permanencia,
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY faixa_permanencia
ORDER BY faixa_permanencia;
```

**Resultado:** `Número de linhas: got=5 exp=4`
**TTA:** 9493ms

---

### ✅ Q85 — Financeira

**Q:** Custo total de internações ortopédicas de idosos (60 anos ou mais) em SP em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND idade_paciente >= 60;
```

**Resultado:** `got=83044181.8 exp=83044181.8`
**TTA:** 5662ms

---

### ✅ Q86 — Financeira

**Q:** Custo médio por internação por fratura de fêmur (S72) em SP em 2023

**SQL gerado:**
```sql
SELECT ROUND(AVG(COALESCE(custo_total, 0)), 2) AS custo_medio_por_internacao
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%';
```

**Resultado:** `got=2730.82 exp=2730.82`
**TTA:** 4771ms

---

### ❌ Q87 — Financeira

**Q:** Custo total de internações ortopédicas com passagem por UTI em SP em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND val_uti > 0;
```

**Resultado:** `got=86143722.88 exp=6792.17`
**TTA:** 5598ms

---

### ✅ Q88 — Financeira

**Q:** Custo médio por internação ortopédica por sexo do paciente em SP em 2023

**SQL gerado:**
```sql
SELECT 
  sexo_paciente,
  ROUND(AVG(custo_total), 2) AS custo_medio_por_internacao
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY 
  sexo_paciente
ORDER BY 
  sexo_paciente;
```

**Resultado:** `2 linhas corretas, ordem livre (flexível)`
**TTA:** 6237ms

---

### ✅ Q89 — Financeira

**Q:** Custo total de internações ortopédicas em crianças (menores de 18 anos) em SP em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND idade_paciente < 18;
```

**Resultado:** `got=7933442.51 exp=7933442.51`
**TTA:** 5648ms

---

### ✅ Q90 — Financeira

**Q:** Custo médio por internação ortopédica com permanência superior a 7 dias em SP em 2023

**SQL gerado:**
```sql
SELECT ROUND(AVG(custo_total), 2) AS custo_medio
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND dias_perm > 7;
```

**Resultado:** `got=4390.48 exp=4390.48`
**TTA:** 5320ms

---

### ✅ Q91 — Financeira

**Q:** Top 3 municípios com maior custo total em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT cod_munic_estabelecimento,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cod_munic_estabelecimento
ORDER BY custo_total DESC
LIMIT 3;
```

**Resultado:** `3 linhas corretas (exato)`
**TTA:** 6421ms

---

### ❌ Q92 — Financeira

**Q:** Valor total de serviço hospitalar em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS valor_total_servico_hospitalar
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=187220821.79 exp=153782049.46`
**TTA:** 5405ms

---

### ❌ Q93 — Financeira

**Q:** Custo médio por internação ortopédica por faixa etária em SP em 2023

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN idade_paciente < 18 THEN '0-17'
    WHEN idade_paciente < 30 THEN '18-29'
    WHEN idade_paciente < 40 THEN '30-39'
    WHEN idade_paciente < 50 THEN '40-49'
    WHEN idade_paciente < 60 THEN '50-59'
    WHEN idade_paciente < 70 THEN '60-69'
    ELSE '70+'
  END AS faixa_etaria,
  ROUND(AVG(custo_total), 2) AS custo_medio_por_internacao
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY faixa_etaria
ORDER BY faixa_etaria;
```

**Resultado:** `Número de linhas: got=7 exp=3`
**TTA:** 10476ms

---

### ✅ Q94 — Financeira

**Q:** Valor total de UTI em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(val_uti, 0)), 2) AS valor_uti_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=40878597.03 exp=40878597.03`
**TTA:** 5292ms

---

### ✅ Q95 — Financeira

**Q:** Custo total de internações por doenças osteomusculares (M00-M99) em SP em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND icd_group = 'M00-M99';
```

**Resultado:** `got=15790831.06 exp=15790831.06`
**TTA:** 4691ms

---

### ✅ Q96 — Financeira

**Q:** Custo total de internações por traumatismos (S00-T98) em SP em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND icd_group = 'S00-T98';
```

**Resultado:** `got=171429990.73 exp=171429990.73`
**TTA:** 4675ms

---

### ✅ Q97 — Financeira

**Q:** Top 5 diagnósticos com maior custo médio por internação ortopédica em SP em 2023 (mínimo 40 internações)

**SQL gerado:**
```sql
SELECT cid_principal,
  AVG(custo_total) AS custo_medio,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cid_principal
HAVING COUNT(DISTINCT n_aih) >= 40
ORDER BY custo_medio DESC
LIMIT 5;
```

**Resultado:** `5 linhas corretas (exato)`
**TTA:** 7948ms

---

### ✅ Q98 — Financeira

**Q:** Custo médio por procedimento ambulatorial ortopédico em SP em 2023

**SQL gerado:**
```sql
SELECT ROUND(AVG(custo_total), 2) AS custo_medio
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=86.29 exp=86.29`
**TTA:** 5054ms

---

### ❌ Q99 — Financeira

**Q:** Top 5 procedimentos ambulatoriais com maior custo total em SP em 2023

**SQL gerado:**
```sql
SELECT cod_procedimento,
  COUNT(*) AS total_procedimentos,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
GROUP BY cod_procedimento
ORDER BY custo_total DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cod_procedimento': '0702100048', 'total_procedimentos': 15268, 'custo_total': 45189222.96} exp={'cod_procedimento': '0701020369', 'custo_total': 1261008.0}`
**TTA:** 5438ms

---

### ✅ Q100 — Financeira

**Q:** Custo total de internações ortopédicas de idosos (60+) com permanência superior a 7 dias em SP em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND idade_paciente >= 60
  AND dias_perm > 7;
```

**Resultado:** `got=37989386.14 exp=37989386.14`
**TTA:** 5963ms

---

### ⚠️ Q101 — Temporal/Comparativa

**Q:** Mês com maior número de óbitos em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT mes_cmpt,
  COUNT(DISTINCT n_aih) AS total_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND obito = 'S'
GROUP BY mes_cmpt
ORDER BY total_obitos DESC
LIMIT 1;
```

**Erro:** Falha DuckDB: Binder Error: Referenced column "obito" not found in FROM clause!
Candidate bindings: "morte", "gestao", "row_id", "cod_munic_estabelecimento", "sistema"

LINE 8:   AND obito = 'S'
              ^

---

### ✅ Q102 — Temporal/Comparativa

**Q:** Volume mensal de internações por fratura de fêmur (S72) em idosos (60 anos ou mais) em SP em 2023

**SQL gerado:**
```sql
SELECT 
  mes_cmpt,
  COUNT(DISTINCT n_aih) AS internacoes
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
  AND idade_paciente >= 60
GROUP BY 
  mes_cmpt
ORDER BY 
  mes_cmpt;
```

**Resultado:** `12 linhas corretas (exato)`
**TTA:** 6827ms

---

### ✅ Q103 — Temporal/Comparativa

**Q:** Custo mensal de internações ortopédicas de idosos (60 anos ou mais) em SP em 2023

**SQL gerado:**
```sql
SELECT 
  mes_cmpt,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_mensal
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND idade_paciente >= 60
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY 
  mes_cmpt
ORDER BY 
  mes_cmpt;
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 6801ms

---

### ✅ Q104 — Temporal/Comparativa

**Q:** Mês com maior custo total de internações por fratura de fêmur (S72) em SP em 2023

**SQL gerado:**
```sql
SELECT mes_cmpt,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
GROUP BY mes_cmpt
ORDER BY custo_total DESC
LIMIT 1;
```

**Resultado:** `1 linhas corretas (exato)`
**TTA:** 5401ms

---

### ✅ Q105 — Temporal/Comparativa

**Q:** Volume mensal de internações por traumatismos (grupo S00-T98) em SP em 2023

**SQL gerado:**
```sql
SELECT 
  mes_cmpt,
  COUNT(DISTINCT n_aih) AS internacoes
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND icd_group = 'S00-T98'
GROUP BY 
  mes_cmpt
ORDER BY 
  mes_cmpt;
```

**Resultado:** `12 linhas corretas (exato)`
**TTA:** 5365ms

---

### ✅ Q106 — Temporal/Comparativa

**Q:** Volume mensal de procedimentos ambulatoriais ortopédicos por sexo em SP em 2023

**SQL gerado:**
```sql
SELECT 
  mes_cmpt,
  sexo_paciente,
  COUNT(*) AS total_procedimentos
FROM 
  processed
WHERE 
  sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY 
  mes_cmpt, sexo_paciente
ORDER BY 
  mes_cmpt, sexo_paciente;
```

**Resultado:** `24 linhas corretas (flexível)`
**TTA:** 6269ms

---

### ✅ Q107 — Temporal/Comparativa

**Q:** Comparativo trimestral de internações ortopédicas por grupo diagnóstico em SP em 2023

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN mes_cmpt BETWEEN 1 AND 3 THEN 'Q1'
    WHEN mes_cmpt BETWEEN 4 AND 6 THEN 'Q2'
    WHEN mes_cmpt BETWEEN 7 AND 9 THEN 'Q3'
    ELSE 'Q4'
  END AS trimestre,
  icd_group AS grupo_diagnostico,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY trimestre, icd_group
ORDER BY trimestre, grupo_diagnostico;
```

**Resultado:** `8 linhas corretas, ordem livre (flexível)`
**TTA:** 8551ms

---

### ✅ Q108 — Temporal/Comparativa

**Q:** Mês com maior permanência hospitalar média em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT
  mes_cmpt,
  ROUND(AVG(dias_perm), 1) AS permanencia_media_dias
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY mes_cmpt
ORDER BY permanencia_media_dias DESC
LIMIT 1;
```

**Resultado:** `1 linhas corretas (flexível)`
**TTA:** 6070ms

---

### ⚠️ Q109 — Temporal/Comparativa

**Q:** Evolução mensal da taxa de mortalidade em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT
  mes_cmpt,
  ROUND(SUM(CASE WHEN obito = 'S' THEN 1 ELSE 0 END) * 100.0 / COUNT(*), 2) AS taxa_mortalidade
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Erro:** Falha DuckDB: Binder Error: Referenced column "obito" not found in FROM clause!
Candidate bindings: "morte", "gestao", "row_id", "cod_munic_estabelecimento", "sistema"

LINE 3:   ROUND(SUM(CASE WHEN obito = 'S' THEN 1 ELSE 0 END) * 100.0 / COUNT(*), 2) AS...
                              ^

---

### ✅ Q110 — Temporal/Comparativa

**Q:** Trimestre com maior volume de internações por fratura de fêmur (S72) em SP em 2023

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN mes_cmpt BETWEEN 1 AND 3 THEN 'Q1'
    WHEN mes_cmpt BETWEEN 4 AND 6 THEN 'Q2'
    WHEN mes_cmpt BETWEEN 7 AND 9 THEN 'Q3'
    ELSE 'Q4'
  END AS trimestre,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
GROUP BY trimestre
ORDER BY internacoes DESC
LIMIT 1;
```

**Resultado:** `1 linhas corretas (flexível)`
**TTA:** 7296ms

---

### ❌ Q111 — Temporal/Comparativa

**Q:** Comparativo semestral de internações ortopédicas (1º vs 2º semestre) em SP em 2023

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN mes_cmpt BETWEEN 1 AND 6 THEN '1º Semestre'
    ELSE '2º Semestre'
  END AS semestre,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY semestre
ORDER BY semestre;
```

**Resultado:** `Linha não encontrada: {'semestre': '1S', 'total': 33739}`
**TTA:** 6707ms

---

### ❌ Q112 — Temporal/Comparativa

**Q:** Custo médio mensal por internação por fratura de fêmur (S72) em SP em 2023

**SQL gerado:**
```sql
SELECT 
  ROUND(SUM(COALESCE(custo_total, 0)) / COUNT(DISTINCT n_aih), 2) AS custo_medio_mensal
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%';
```

**Resultado:** `Número de linhas: got=1 exp=12`
**TTA:** 5322ms

---

### ✅ Q113 — Temporal/Comparativa

**Q:** Volume mensal de internações ortopédicas por sexo do paciente em SP em 2023

**SQL gerado:**
```sql
SELECT 
  mes_cmpt,
  sexo_paciente,
  COUNT(DISTINCT n_aih) AS internacoes
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY 
  mes_cmpt, sexo_paciente
ORDER BY 
  mes_cmpt, sexo_paciente;
```

**Resultado:** `24 linhas corretas (flexível)`
**TTA:** 6437ms

---

### ✅ Q114 — Temporal/Comparativa

**Q:** Mês com menor volume de procedimentos ambulatoriais ortopédicos em SP em 2023

**SQL gerado:**
```sql
SELECT mes_cmpt,
  COUNT(*) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY mes_cmpt
ORDER BY total_procedimentos ASC
LIMIT 1;
```

**Resultado:** `1 linhas corretas (flexível)`
**TTA:** 5617ms

---

### ✅ Q115 — Temporal/Comparativa

**Q:** Volume trimestral de procedimentos ambulatoriais ortopédicos em SP em 2023

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN mes_cmpt BETWEEN 1 AND 3 THEN 'Q1'
    WHEN mes_cmpt BETWEEN 4 AND 6 THEN 'Q2'
    WHEN mes_cmpt BETWEEN 7 AND 9 THEN 'Q3'
    ELSE 'Q4'
  END AS trimestre,
  COUNT(*) AS volume_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY trimestre
ORDER BY trimestre;
```

**Resultado:** `4 linhas corretas, ordem livre (flexível)`
**TTA:** 7637ms

---

### ✅ Q116 — Temporal/Comparativa

**Q:** Custo mensal de procedimentos ambulatoriais ortopédicos em SP em 2023

**SQL gerado:**
```sql
SELECT 
  mes_cmpt,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_mensal
FROM 
  processed
WHERE 
  sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY 
  mes_cmpt
ORDER BY 
  mes_cmpt;
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 6395ms

---

### ❌ Q117 — Temporal/Comparativa

**Q:** Comparativo semestral do custo total de internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN mes_cmpt BETWEEN 1 AND 6 THEN 'Semestre 1'
    ELSE 'Semestre 2'
  END AS semestre,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY semestre
ORDER BY semestre;
```

**Resultado:** `Linha não encontrada: {'semestre': '1S', 'custo_total': 90189950.58}`
**TTA:** 7091ms

---

### ✅ Q118 — Temporal/Comparativa

**Q:** Evolução mensal da permanência média em internações por fratura de fêmur (S72) em SP em 2023

**SQL gerado:**
```sql
SELECT
  mes_cmpt,
  ROUND(AVG(dias_perm), 1) AS permanencia_media_dias
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 5098ms

---

## Failures Detail

- **Q01** (Epidemiológica Simples): got=6880 exp=69487
  SQL: `SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_...`
- **Q04** (Epidemiológica Simples): got=0 exp=165
  SQL: `SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_...`
- **Q05** (Epidemiológica Simples): Falha DuckDB: Binder Error: Referenced column "obito" not found in FROM clause!
Candidate bindings: "morte", "gestao", "row_id", "cod_munic_estabelecimento", "sistema"

LINE 7:   AND obito = 'S';
              ^
  SQL: `SELECT COUNT(DISTINCT n_aih) AS total_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt ...`
- **Q08** (Epidemiológica Simples): got=27029 exp=27027
  SQL: `SELECT COUNT(*) AS total_mulheres_internadas
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt ...`
- **Q09** (Epidemiológica Simples): got=42489 exp=42460
  SQL: `SELECT COUNT(*) AS total_homens_internados
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = ...`
- **Q14** (Epidemiológica Simples): got=13764 exp=1
  SQL: `SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_...`
- **Q19** (Epidemiológica Complexa): Falha DuckDB: Binder Error: Referenced column "fator_mortalidade" not found in FROM clause!
Candidate bindings: "custo_total", "pa_motsai", "raca_cor_paciente", "pa_autoriz", "proc_solic"

LINE 3:     COUNT(CASE WHEN fator_mortalidade = 1 THEN 1 ELSE NULL END) AS mortes,
                            ^
  SQL: `SELECT 
    cid_principal,
    COUNT(CASE WHEN fator_mortalidade = 1 THEN 1 ELSE NULL END) AS mortes,
    COUNT(*) AS to...`
- **Q21** (Epidemiológica Complexa): Linha não encontrada: {'sexo_paciente': 'M', 'total_internacoes': 42460, 'pct': 61.1}
  SQL: `SELECT 
  sexo_paciente,
  COUNT(*) AS total_internacoes,
  ROUND(100.0 * COUNT(*) / SUM(COUNT(*)) OVER (), 1) AS pct
FR...`
- **Q22** (Epidemiológica Complexa): Linha 1: got={'internacoes_idosos': 19598.0, 'internacoes_total': 30272} exp={'total_s72': 30272, 's72_idosos': 19598.0, 'pct_idosos': 64.7}
  SQL: `SELECT
  SUM(CASE WHEN idade_paciente >= 60 THEN 1 ELSE 0 END) AS internacoes_idosos,
  COUNT(DISTINCT n_aih) AS interna...`
- **Q23** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'S141', 'permanencia_media_dias': 73.0} exp={'cid_principal': 'S272', 'total_internacoes': 49, 'permanencia_media_dias': 14.3}
  SQL: `SELECT cid_principal,
  ROUND(AVG(dias_perm), 1) AS permanencia_media_dias
FROM processed
WHERE sistema = 'SIH'
  AND uf...`
- **Q25** (Epidemiológica Complexa): Linha não encontrada: {'raca_cor_paciente': '01', 'total': 42626, 'pct': 61.3}
  SQL: `SELECT raca_cor_paciente,
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_ori...`
- **Q26** (Epidemiológica Complexa): Falha DuckDB: Binder Error: Referenced column "dias_totais_internacao" not found in FROM clause!
Candidate bindings: "dt_inter", "dias_perm", "tipo_financiamento", "dt_saida", "cod_munic_estabelecimento"

LINE 2:   SUM(dias_totais_internacao) AS total_dias_internacao
              ^
  SQL: `SELECT cid_principal,
  SUM(dias_totais_internacao) AS total_dias_internacao
FROM processed
WHERE sistema = 'SIH'
  AND ...`
- **Q28** (Epidemiológica Complexa): Falha DuckDB: Binder Error: Referenced column "uf_destino" not found in FROM clause!
Candidate bindings: "uf", "mun_res_tipo", "uti_mes_to", "mun_res_nome", "gestao"

LINE 2:   ROUND(100.0 * COUNT(CASE WHEN uf_origem <> uf_destino THEN 1 ELSE NULL END) / COUNT(*), 2) AS pct_inte...
                                                     ^
  SQL: `SELECT 
  ROUND(100.0 * COUNT(CASE WHEN uf_origem <> uf_destino THEN 1 ELSE NULL END) / COUNT(*), 2) AS pct_internacoes_...`
- **Q29** (Epidemiológica Complexa): Número de linhas: got=12 exp=24
  SQL: `SELECT
  mes_cmpt,
  SUM(CASE WHEN sistema = 'SIA' THEN 1 ELSE 0 END) AS internacoes_sia,
  SUM(CASE WHEN sistema = 'SIH...`
- **Q30** (Epidemiológica Complexa): Linha 1: got={'idade_media': 62.4} exp={'idade_media': 62.4, 'idade_minima': 0.0, 'idade_maxima': 99.0}
  SQL: `SELECT ROUND(AVG(idade_paciente), 1) AS idade_media
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND an...`
- **Q34** (Financeira): Linha 1: got={'cid_principal': 'S720', 'custo_total': 30400546.73} exp={'cid_principal': 'S720', 'internacoes': 9059, 'custo_total': 30400546.73}
  SQL: `SELECT cid_principal,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AN...`
- **Q35** (Financeira): Linha 1: got={'custo_total': None} exp={'internacoes_com_opme': 0, 'custo_total_opme': None}
  SQL: `SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP...`
- **Q38** (Financeira): Linha 1: got={'cnes_estabelecimento': '2078015', 'custo_total': 11295149.42} exp={'cnes_estabelecimento': '2078015', 'internacoes': 2257, 'custo_total': 11295149.42}
  SQL: `SELECT cnes_estabelecimento,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SI...`
- **Q40** (Financeira): Falha DuckDB: Binder Error: Referenced column "val_honorarios" not found in FROM clause!
Candidate bindings: "val_sh", "val_ortp", "val_uci", "val_uti", "pa_docorig"

LINE 3:   ROUND(100.0 * SUM(COALESCE(val_honorarios, 0)) / SUM(COALESCE(custo_total, 0)), 2)...
                                     ^
  SQL: `SELECT 
  ROUND(100.0 * SUM(COALESCE(val_sh, 0)) / SUM(COALESCE(custo_total, 0)), 2) AS pct_servico_hospitalar,
  ROUND(...`
- **Q45** (Temporal/Comparativa): Número de linhas: got=12 exp=24
  SQL: `SELECT
  mes_cmpt,
  COUNT(CASE WHEN icd_group = 'S00-T98' THEN 1 END) AS internacoes_trauma,
  COUNT(CASE WHEN icd_grou...`
- **Q46** (Temporal/Comparativa): Falha DuckDB: Binder Error: Referenced column "tipo_internacao" not found in FROM clause!
Candidate bindings: "tipo_financiamento", "dt_inter", "etnia_paciente", "competencia_ano_mes", "cid_princ"

LINE 9:   AND tipo_internacao = 'ÓBITO'
              ^
  SQL: `SELECT
  mes_cmpt,
  COUNT(DISTINCT n_aih) AS total_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
 ...`
- **Q61** (Epidemiológica Simples): got=0 exp=100
  SQL: `SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_...`
- **Q67** (Epidemiológica Simples): Falha DuckDB: Binder Error: Referenced column "obito" not found in FROM clause!
Candidate bindings: "morte", "gestao", "row_id", "cod_munic_estabelecimento", "sistema"

LINE 7:   AND obito = 'S';
              ^
  SQL: `SELECT COUNT(DISTINCT n_aih) AS total_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt ...`
- **Q70** (Epidemiológica Complexa): Número de linhas: got=5 exp=3
  SQL: `SELECT
  CASE
    WHEN idade_paciente BETWEEN 0 AND 17 THEN '0-17'
    WHEN idade_paciente BETWEEN 18 AND 29 THEN '18-29...`
- **Q71** (Epidemiológica Complexa): Falha DuckDB: Binder Error: Referenced column "outcome" not found in FROM clause!
Candidate bindings: "morte", "uti_mes_to", "custo_total", "ano_cmpt", "tipo_financiamento"

LINE 8:   AND outcome = 'ÓBITO'
              ^
  SQL: `SELECT cid_principal,
  COUNT(DISTINCT n_aih) AS obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AN...`
- **Q74** (Epidemiológica Complexa): Falha DuckDB: Binder Error: Referenced column "obito" not found in FROM clause!
Candidate bindings: "morte", "gestao", "row_id", "cod_munic_estabelecimento", "sistema"

LINE 3:   COUNT(CASE WHEN obito = 'S' THEN 1 END) AS mortes,
                          ^
  SQL: `SELECT 
  sexo_paciente,
  COUNT(CASE WHEN obito = 'S' THEN 1 END) AS mortes,
  COUNT(*) AS total_internacoes,
  ROUND(1...`
- **Q75** (Epidemiológica Complexa): Falha DuckDB: Binder Error: Referenced column "faixa_etaria" not found in FROM clause!
Candidate bindings: "pa_autoriz", "faec_tp", "raca_cor_paciente", "main_icd", "cid_notif"

LINE 13:   icd_group, faixa_etaria
                      ^
  SQL: `SELECT 
  icd_group,
  faixa_etaria,
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
  processed
WHERE 
  sistema = '...`
- **Q76** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'S272', 'permanencia_media_dias': 14.2653061224, 'internacoes': 49} exp={'cid_principal': 'S272', 'internacoes': 49, 'permanencia_media': 14.3}
  SQL: `SELECT cid_principal,
  AVG(dias_perm) AS permanencia_media_dias,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
...`
- **Q79** (Epidemiológica Complexa): Número de linhas: got=7 exp=3
  SQL: `SELECT
  CASE
    WHEN idade_paciente < 18 THEN '0-17'
    WHEN idade_paciente < 30 THEN '18-29'
    WHEN idade_paciente...`
- **Q83** (Epidemiológica Complexa): Falha DuckDB: Binder Error: aggregate function calls cannot be nested

LINE 2:   ROUND(AVG(COUNT(DISTINCT n_aih)), 1) AS media_internacoes
                    ^
  SQL: `SELECT cnes_estabelecimento,
  ROUND(AVG(COUNT(DISTINCT n_aih)), 1) AS media_internacoes
FROM processed
WHERE sistema = ...`
- **Q84** (Epidemiológica Complexa): Número de linhas: got=5 exp=4
  SQL: `SELECT
  CASE
    WHEN dias_perm BETWEEN 0 AND 7 THEN '0-7 dias'
    WHEN dias_perm BETWEEN 8 AND 14 THEN '8-14 dias'
  ...`
- **Q87** (Financeira): got=86143722.88 exp=6792.17
  SQL: `SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP...`
- **Q92** (Financeira): got=187220821.79 exp=153782049.46
  SQL: `SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS valor_total_servico_hospitalar
FROM processed
WHERE sistema = 'SIH'
  ...`
- **Q93** (Financeira): Número de linhas: got=7 exp=3
  SQL: `SELECT
  CASE
    WHEN idade_paciente < 18 THEN '0-17'
    WHEN idade_paciente < 30 THEN '18-29'
    WHEN idade_paciente...`
- **Q99** (Financeira): Linha 1: got={'cod_procedimento': '0702100048', 'total_procedimentos': 15268, 'custo_total': 45189222.96} exp={'cod_procedimento': '0701020369', 'custo_total': 1261008.0}
  SQL: `SELECT cod_procedimento,
  COUNT(*) AS total_procedimentos,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FRO...`
- **Q101** (Temporal/Comparativa): Falha DuckDB: Binder Error: Referenced column "obito" not found in FROM clause!
Candidate bindings: "morte", "gestao", "row_id", "cod_munic_estabelecimento", "sistema"

LINE 8:   AND obito = 'S'
              ^
  SQL: `SELECT mes_cmpt,
  COUNT(DISTINCT n_aih) AS total_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  A...`
- **Q109** (Temporal/Comparativa): Falha DuckDB: Binder Error: Referenced column "obito" not found in FROM clause!
Candidate bindings: "morte", "gestao", "row_id", "cod_munic_estabelecimento", "sistema"

LINE 3:   ROUND(SUM(CASE WHEN obito = 'S' THEN 1 ELSE 0 END) * 100.0 / COUNT(*), 2) AS...
                              ^
  SQL: `SELECT
  mes_cmpt,
  ROUND(SUM(CASE WHEN obito = 'S' THEN 1 ELSE 0 END) * 100.0 / COUNT(*), 2) AS taxa_mortalidade
FROM ...`
- **Q111** (Temporal/Comparativa): Linha não encontrada: {'semestre': '1S', 'total': 33739}
  SQL: `SELECT
  CASE
    WHEN mes_cmpt BETWEEN 1 AND 6 THEN '1º Semestre'
    ELSE '2º Semestre'
  END AS semestre,
  COUNT(DIS...`
- **Q112** (Temporal/Comparativa): Número de linhas: got=1 exp=12
  SQL: `SELECT 
  ROUND(SUM(COALESCE(custo_total, 0)) / COUNT(DISTINCT n_aih), 2) AS custo_medio_mensal
FROM 
  processed
WHERE ...`
- **Q117** (Temporal/Comparativa): Linha não encontrada: {'semestre': '1S', 'custo_total': 90189950.58}
  SQL: `SELECT
  CASE
    WHEN mes_cmpt BETWEEN 1 AND 6 THEN 'Semestre 1'
    ELSE 'Semestre 2'
  END AS semestre,
  ROUND(SUM(C...`

---
_Gerado por scripts/evaluate_benchmark.py — SUS Data RAG — USF/Mestrado_