# Benchmark Evaluation Report — SUS Data RAG

**Model:** openai  
**Run date:** 2026-08-22 23:14  
**API:** http://127.0.0.1:8000  

## Summary

| Metric | Value |
|---|---|
| Total queries | 118 |
| Correct (EA numerator) | 30 |
| **Execution Accuracy (EA)** | **25.4%** |
| Wilson IC 95% | [18.4%, 34.0%] |
| Correct via flexible match (alias diferente) | 23 |
| API errors / timeouts | 0 |
| Mean Time-to-Answer | 3.6s |

> **Nota metodológica — Scoring:** A Execution Accuracy avalia se o SQL gerado
> retorna os mesmos **valores** que o gold-standard, independente do nome das colunas.
> Quando o LLM usa um alias diferente mas retorna os dados corretos (match posicional),
> a query é contada como correta e sinalizada com `~`. Esta decisão segue a definição
> de EA em [Lee et al., 2022] — comparação de conjuntos de resultado, não de SQL texto.

## EA by Category

| Category | Correct | Total | EA |
|---|---:|---:|---:|
| Epidemiológica Simples | 16 | 33 | 48.5% |
| Epidemiológica Complexa | 3 | 31 | 9.7% |
| Financeira | 4 | 26 | 15.4% |
| Temporal/Comparativa | 7 | 28 | 25.0% |

## Query Results

### ✅ Q01 — Epidemiológica Simples

**Q:** Total de internações ortopédicas (M00-M99 e S00-S99) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
```

**Resultado:** `got=69487 exp=69487`
**TTA:** 3611ms

---

### ❌ Q02 — Epidemiológica Simples

**Q:** Total de procedimentos ambulatoriais ortopédicos em SP em 2023

**SQL gerado:**
```sql
SELECT SUM(pa_qtdapr) AS total_procedimentos_ambulatoriais_ortopedicos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=12485.0 exp=105364`
**TTA:** 2365ms

---

### ✅ Q03 — Epidemiológica Simples

**Q:** Total de internações por fratura de fêmur (S72) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND cid_principal LIKE 'S72%'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
```

**Resultado:** `got=30272 exp=30272`
**TTA:** 2359ms

---

### ✅ Q04 — Epidemiológica Simples

**Q:** Total de internações por osteoartrose (M16, M17) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'M16%' OR cid_principal LIKE 'M17%')
```

**Resultado:** `got=165 exp=165`
**TTA:** 1965ms

---

### ❌ Q05 — Epidemiológica Simples

**Q:** Número de óbitos em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(*) AS numero_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND morte = 1
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=81 exp=2274`
**TTA:** 2029ms

---

### ✅ Q06 — Epidemiológica Simples

**Q:** Total de internações por traumatismos (S00-T98) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'S%' OR icd_group = 'S00-T98')
```

**Resultado:** `got=62607 exp=62607`
**TTA:** 2284ms

---

### ✅ Q07 — Epidemiológica Simples

**Q:** Total de internações por doenças osteomusculares (M00-M99) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND icd_group = 'M00-M99'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
```

**Resultado:** `got=6880 exp=6880`
**TTA:** 1874ms

---

### ❌ Q08 — Epidemiológica Simples

**Q:** Número de mulheres internadas por causa ortopédica em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND sexo_paciente = 'F'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=2554 exp=27027`
**TTA:** 2381ms

---

### ❌ Q09 — Epidemiológica Simples

**Q:** Número de homens internados por causa ortopédica em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS num_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND sexo_paciente = 'M'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=4326 exp=42460`
**TTA:** 2243ms

---

### ❌ Q10 — Epidemiológica Simples

**Q:** Número de internações ortopédicas de idosos (60 anos ou mais) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND ano_cmpt = 2023
  AND uf_origem = 'SP'
  AND idade_paciente >= 60
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=1912 exp=28165`
**TTA:** 2394ms

---

### ❌ Q11 — Epidemiológica Simples

**Q:** Número de municípios distintos com internação ortopédica registrada em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT cod_munic_estabelecimento)
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=166 exp=215`
**TTA:** 2054ms

---

### ❌ Q12 — Epidemiológica Simples

**Q:** Número de estabelecimentos distintos com internação ortopédica em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT cnes_estabelecimento) 
FROM processed 
WHERE sistema = 'SIH' 
  AND uf_origem = 'SP' 
  AND ano_cmpt = 2023 
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=310 exp=385`
**TTA:** 2087ms

---

### ❌ Q13 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com permanência superior a 7 dias em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) 
FROM processed 
WHERE sistema = 'SIH' 
  AND uf_origem = 'SP' 
  AND ano_cmpt = 2023 
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99') 
  AND dias_perm > 7
```

**Resultado:** `got=1521 exp=17778`
**TTA:** 2630ms

---

### ❌ Q14 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com uso de UTI em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS num_internacoes_ortopedicas_uti
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
  AND uti_int_to > 0
```

**Resultado:** `got=0 exp=1`
**TTA:** 2021ms

---

### ✅ Q15 — Epidemiológica Simples

**Q:** Número de internações por fratura de quadril (S72) em pacientes com 70 anos ou mais em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND cid_principal LIKE 'S72%'
  AND idade_paciente >= 70
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
```

**Resultado:** `got=15626 exp=15626`
**TTA:** 1928ms

---

### ❌ Q16 — Epidemiológica Complexa

**Q:** Top 10 CIDs ortopédicos por volume de internação em SP em 2023

**SQL gerado:**
```sql
SELECT 
    cid_principal, 
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND ano_cmpt = 2023
    AND uf_origem = 'SP'
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cid_principal
ORDER BY 
    volume_internacoes DESC
LIMIT 10
```

**Resultado:** `Linha 1: got={'cid_principal': 'M861', 'volume_internacoes': 957} exp={'cid_principal': 'S720', 'total_internacoes': 9059}`
**TTA:** 2057ms

---

### ❌ Q17 — Epidemiológica Complexa

**Q:** Top 5 municípios de SP com maior número de internações ortopédicas em 2023

**SQL gerado:**
```sql
SELECT 
    p.cod_munic_estabelecimento, 
    COUNT(DISTINCT p.n_aih) AS num_internacoes
FROM 
    processed p
JOIN 
    enriched e ON p.row_id = e.row_id
WHERE 
    p.sistema = 'SIH'
    AND p.uf_origem = 'SP'
    AND p.ano_cmpt = 2023
    AND (p.cid_principal LIKE 'M%' OR p.icd_group = 'M00-M99')
GROUP BY 
    p.cod_munic_estabelecimento
ORDER BY 
    num_internacoes DESC
LIMIT 5
```

**Resultado:** `Linha 1: got={'cod_munic_estabelecimento': '350000', 'num_internacoes': 3097} exp={'cod_munic_estabelecimento': '350000', 'internacoes': 32140}`
**TTA:** 2817ms

---

### ❌ Q18 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por faixa etária (0-17, 18-59, 60+) em SP em 2023

**SQL gerado:**
```sql
SELECT 
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 59 THEN '18-59'
        ELSE '60+'
    END AS faixa_etaria,
    COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    faixa_etaria
ORDER BY 
    faixa_etaria
```

**Resultado:** `Linha não encontrada: {'faixa_etaria': '0-17', 'total': 4040}`
**TTA:** 2249ms

---

### ❌ Q19 — Epidemiológica Complexa

**Q:** Taxa de mortalidade hospitalar por CID ortopédico (top 5) em SP em 2023

**SQL gerado:**
```sql
SELECT 
    p.cid_principal,
    COUNT(DISTINCT p.n_aih) AS total_internacoes,
    SUM(CASE WHEN p.morte = 1 THEN 1 ELSE 0 END) AS total_obitos,
    (SUM(CASE WHEN p.morte = 1 THEN 1 ELSE 0 END) * 100.0 / COUNT(DISTINCT p.n_aih)) AS taxa_mortalidade
FROM 
    processed p
WHERE 
    p.sistema = 'SIH'
    AND p.uf_origem = 'SP'
    AND p.ano_cmpt = 2023
    AND (p.cid_principal LIKE 'M%' OR p.icd_group = 'M00-M99')
GROUP BY 
    p.cid_principal
ORDER BY 
    taxa_mortalidade DESC
LIMIT 5
```

**Resultado:** `Linha 1: got={'cid_principal': 'M311', 'total_internacoes': 3, 'total_obitos': 1.0, 'taxa_mortalidade': 33.3333333333} exp={'cid_principal': 'S062', 'total_internacoes': 58, 'total_obitos': 20.0, 'taxa_mortalidade_pct': 34.48}`
**TTA:** 2857ms

---

### ❌ Q20 — Epidemiológica Complexa

**Q:** Top 10 CIDs ambulatoriais ortopédicos por volume de produção em SP em 2023

**SQL gerado:**
```sql
SELECT 
    cid_principal, 
    COUNT(*) AS volume_producao
FROM 
    processed
WHERE 
    sistema = 'SIA'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cid_principal
ORDER BY 
    volume_producao DESC
LIMIT 10
```

**Resultado:** `Linha 1: got={'cid_principal': 'M216', 'volume_producao': 5349} exp={'cid_principal': 'S720', 'total_procedimentos': 17325}`
**TTA:** 2293ms

---

### ❌ Q21 — Epidemiológica Complexa

**Q:** Proporção de internações ortopédicas por sexo em SP em 2023

**SQL gerado:**
```sql
SELECT 
    sexo_paciente, 
    COUNT(DISTINCT n_aih) AS total_internacoes,
    COUNT(DISTINCT n_aih) * 1.0 / SUM(COUNT(DISTINCT n_aih)) OVER () AS proporcao
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    sexo_paciente
```

**Resultado:** `Linha não encontrada: {'sexo_paciente': 'M', 'total_internacoes': 42460, 'pct': 61.1}`
**TTA:** 2345ms

---

### ❌ Q22 — Epidemiológica Complexa

**Q:** Número de internações por fratura de fêmur (S72) em idosos (60+) comparado ao total em SP em 2023

**SQL gerado:**
```sql
SELECT 
    COUNT(DISTINCT CASE WHEN idade_paciente >= 60 AND cid_principal LIKE 'S72%' THEN n_aih END) AS internacoes_idosos_fratura_femur,
    COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
```

**Resultado:** `Linha 1: got={'internacoes_idosos_fratura_femur': 19589, 'total_internacoes': 209808} exp={'total_s72': 30272, 's72_idosos': 19598.0, 'pct_idosos': 64.7}`
**TTA:** 2289ms

---

### ❌ Q23 — Epidemiológica Complexa

**Q:** Top 5 CIDs ortopédicos com maior permanência média hospitalar em SP em 2023

**SQL gerado:**
```sql
SELECT 
    cid_principal, 
    AVG(dias_perm) AS permanencia_media
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cid_principal
ORDER BY 
    permanencia_media DESC
LIMIT 5
```

**Resultado:** `Linha 1: got={'cid_principal': 'M412', 'permanencia_media': 60.0} exp={'cid_principal': 'S272', 'total_internacoes': 49, 'permanencia_media_dias': 14.3}`
**TTA:** 2230ms

---

### ❌ Q24 — Epidemiológica Complexa

**Q:** Top 5 estabelecimentos com maior volume de internação ortopédica em SP em 2023

**SQL gerado:**
```sql
SELECT 
    p.cnes_estabelecimento, 
    COUNT(DISTINCT p.n_aih) AS volume_internacoes
FROM 
    processed p
JOIN 
    enriched e ON p.row_id = e.row_id
WHERE 
    p.sistema = 'SIH'
    AND p.uf_origem = 'SP'
    AND p.ano_cmpt = 2023
    AND (p.cid_principal LIKE 'M%' OR p.icd_group = 'M00-M99')
GROUP BY 
    p.cnes_estabelecimento
ORDER BY 
    volume_internacoes DESC
LIMIT 5
```

**Resultado:** `Linha 1: got={'cnes_estabelecimento': '2078015', 'volume_internacoes': 416} exp={'cnes_estabelecimento': '2078015', 'internacoes': 2257}`
**TTA:** 3184ms

---

### ❌ Q25 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por raça/cor do paciente em SP em 2023

**SQL gerado:**
```sql
SELECT 
  raca_cor_paciente, 
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND ano_cmpt = 2023
  AND uf_origem = 'SP'
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
  raca_cor_paciente
ORDER BY 
  total_internacoes DESC
```

**Resultado:** `Linha não encontrada: {'raca_cor_paciente': '01', 'total': 42626, 'pct': 61.3}`
**TTA:** 2301ms

---

### ❌ Q26 — Epidemiológica Complexa

**Q:** CIDs ortopédicos com maior número de dias totais de internação em SP em 2023

**SQL gerado:**
```sql
SELECT 
    cid_principal, 
    SUM(dias_perm) AS total_dias_internacao
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND ano_cmpt = 2023
    AND uf_origem = 'SP'
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cid_principal
ORDER BY 
    total_dias_internacao DESC
```

**Resultado:** `Número de linhas: got=306 exp=10`
**TTA:** 2385ms

---

### ❌ Q27 — Epidemiológica Complexa

**Q:** Top 5 procedimentos ambulatoriais ortopédicos mais realizados em SP em 2023

**SQL gerado:**
```sql
SELECT 
    cod_procedimento, 
    SUM(pa_qtdapr) AS total_procedures
FROM 
    processed
WHERE 
    sistema = 'SIA'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cod_procedimento
ORDER BY 
    total_procedures DESC
LIMIT 5
```

**Resultado:** `Linha 1: got={'cod_procedimento': '0701010142', 'total_procedures': 2151.0} exp={'cod_procedimento': '0302050019', 'total_procedimentos': 72108}`
**TTA:** 2581ms

---

### ❌ Q28 — Epidemiológica Complexa

**Q:** Proporção de internações ortopédicas com paciente de outro município em SP em 2023

**SQL gerado:**
```sql
SELECT 
    COUNT(DISTINCT p.n_aih) AS total_internacoes,
    COUNT(DISTINCT CASE WHEN e.clinical_deslocamento_territorial = 'deslocamento intermunicipal' THEN p.n_aih END) AS internacoes_deslocamento_intermunicipal,
    COUNT(DISTINCT CASE WHEN e.clinical_deslocamento_territorial = 'deslocamento intermunicipal' THEN p.n_aih END) * 1.0 / COUNT(DISTINCT p.n_aih) AS proporcao_deslocamento_intermunicipal
FROM 
    processed p
JOIN 
    enriched e ON p.row_id = e.row_id
WHERE 
    p.sistema = 'SIH'
    AND p.uf_origem = 'SP'
    AND p.ano_cmpt = 2023
    AND (p.cid_principal LIKE 'M%' OR p.icd_group = 'M00-M99')
    AND e.clinical_deslocamento_territorial = 'deslocamento intermunicipal'
```

**Resultado:** `Linha 1: got={'total_internacoes': 3701, 'internacoes_deslocamento_intermunicipal': 3701, 'proporcao_deslocamento_intermunicipal': 1.0} exp={'total_internacoes': 69487, 'deslocamento_intermunicipal': 39611.0, 'pct_deslocamento': 57.0}`
**TTA:** 3545ms

---

### ❌ Q29 — Epidemiológica Complexa

**Q:** Comparativo de volume mensal SIA vs SIH ortopédico em SP em 2023

**SQL gerado:**
```sql
SELECT 
    p.mes_cmpt,
    p.sistema,
    COUNT(*) AS volume
FROM 
    processed p
WHERE 
    p.ano_cmpt = 2023
    AND p.uf_origem = 'SP'
    AND (p.cid_principal LIKE 'M%' OR p.icd_group = 'M00-M99')
    AND (p.sistema = 'SIA' OR p.sistema = 'SIH')
GROUP BY 
    p.mes_cmpt, p.sistema
ORDER BY 
    p.mes_cmpt, p.sistema;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'sistema': 'SIA', 'volume': 605} exp={'mes_cmpt': 1, 'sistema': 'SIA', 'total': 8415}`
**TTA:** 3444ms

---

### ❌ Q30 — Epidemiológica Complexa

**Q:** Idade média dos pacientes internados por fratura de fêmur (S72) em SP em 2023

**SQL gerado:**
```sql
SELECT AVG(idade_paciente) AS idade_media
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
```

**Resultado:** `Linha 1: got={'idade_media': 62.3639245732} exp={'idade_media': 62.4, 'idade_minima': 0.0, 'idade_maxima': 99.0}`
**TTA:** 1908ms

---

### ❌ Q31 — Financeira

**Q:** Custo total de todas as internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes_ortopedicas
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=15790831.060000004 exp=187220821.79`
**TTA:** 2147ms

---

### ❌ Q32 — Financeira

**Q:** Custo médio por internação ortopédica em SP em 2023

**SQL gerado:**
```sql
SELECT 
    COALESCE(SUM(custo_total) / COUNT(DISTINCT n_aih), 0) AS custo_medio_por_internacao
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=2295.1789331395 exp=2693.13`
**TTA:** 2200ms

---

### ✅ Q33 — Financeira

**Q:** Custo total de internações por fratura de fêmur (S72) em SP em 2023

**SQL gerado:**
```sql
SELECT SUM(COALESCE(custo_total, 0)) AS total_custo_fratura_femur
FROM processed
WHERE sistema = 'SIH'
  AND cid_principal LIKE 'S72%'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
```

**Resultado:** `got=82692089.43999992 exp=82692089.44`
**TTA:** 1932ms

---

### ❌ Q34 — Financeira

**Q:** Top 5 CIDs ortopédicos com maior custo total de internação em SP em 2023

**SQL gerado:**
```sql
SELECT 
    cid_principal, 
    SUM(custo_total) AS total_custo
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cid_principal
ORDER BY 
    total_custo DESC
LIMIT 5
```

**Resultado:** `Linha 1: got={'cid_principal': 'M511', 'total_custo': 1119270.5} exp={'cid_principal': 'S720', 'internacoes': 9059, 'custo_total': 30400546.73}`
**TTA:** 3864ms

---

### ❌ Q35 — Financeira

**Q:** Custo total de internações ortopédicas com uso de OPME (val_ortp > 0) em SP em 2023

**SQL gerado:**
```sql
SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes_ortopedicas_opme
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND ano_cmpt = 2023
    AND uf_origem = 'SP'
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
    AND val_ortp > 0
```

**Resultado:** `Linha 1: got={'custo_total_internacoes_ortopedicas_opme': None} exp={'internacoes_com_opme': 0, 'custo_total_opme': None}`
**TTA:** 3963ms

---

### ❌ Q36 — Financeira

**Q:** Valor total de honorários profissionais (val_sp) em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT 
    COALESCE(SUM(val_sp), 0) AS total_val_sp
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=3059022.389999998 exp=32810269.18`
**TTA:** 4381ms

---

### ❌ Q37 — Financeira

**Q:** Custo médio por dia de internação ortopédica em SP em 2023

**SQL gerado:**
```sql
SELECT 
    SUM(custo_total) / SUM(dias_perm) AS custo_medio_por_dia
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
    AND dias_perm > 0
```

**Resultado:** `got=374.5821970394 exp=441.21`
**TTA:** 4903ms

---

### ❌ Q38 — Financeira

**Q:** Top 5 estabelecimentos com maior custo total de internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT 
    p.cnes_estabelecimento, 
    SUM(p.custo_total) AS total_custo
FROM 
    processed p
JOIN 
    enriched e ON p.row_id = e.row_id
WHERE 
    p.sistema = 'SIH' 
    AND p.uf_origem = 'SP' 
    AND p.ano_cmpt = 2023
    AND (p.cid_principal LIKE 'M%' OR p.icd_group = 'M00-M99')
GROUP BY 
    p.cnes_estabelecimento
ORDER BY 
    total_custo DESC
LIMIT 5
```

**Resultado:** `Linha 1: got={'cnes_estabelecimento': '2078015', 'total_custo': 1470646.2399999998} exp={'cnes_estabelecimento': '2078015', 'internacoes': 2257, 'custo_total': 11295149.42}`
**TTA:** 4161ms

---

### ❌ Q39 — Financeira

**Q:** Custo total de procedimentos ambulatoriais ortopédicos em SP em 2023

**SQL gerado:**
```sql
SELECT SUM(COALESCE(custo_total, 0)) AS custo_total_ambulatorial_ortopedico
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=2109063.4000000055 exp=9092221.75`
**TTA:** 2966ms

---

### ❌ Q40 — Financeira

**Q:** Proporção custo serviço hospitalar vs honorários em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT 
    SUM(COALESCE(val_sh, 0)) / NULLIF(SUM(COALESCE(val_sp, 0)), 0) AS proporcao_custo_sh_vs_honorarios
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND ano_cmpt = 2023
    AND uf_origem = 'SP'
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `Linha 1: got={'proporcao_custo_sh_vs_honorarios': 4.0965245795} exp={'custo_servico_hospitalar': 153782049.46, 'honorarios_profissionais': 32810269.18, 'pct_hospitalar': 82.4}`
**TTA:** 4364ms

---

### ❌ Q41 — Temporal/Comparativa

**Q:** Distribuição mensal de internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt, 
    COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'total_internacoes': 507} exp={'mes_cmpt': 1, 'total_internacoes': 5667}`
**TTA:** 4023ms

---

### ❌ Q42 — Temporal/Comparativa

**Q:** Distribuição mensal de procedimentos ambulatoriais ortopédicos em SP em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt, 
    COUNT(*) AS total_procedimentos
FROM 
    processed
WHERE 
    sistema = 'SIA' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2023 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'total_procedimentos': 605} exp={'mes_cmpt': 1, 'procedimentos': 8415}`
**TTA:** 4135ms

---

### ✅ Q43 — Temporal/Comparativa

**Q:** Mês com maior número de internações por fratura de fêmur (S72) em SP em 2023

**SQL gerado:**
```sql
SELECT mes_cmpt, COUNT(DISTINCT n_aih) AS num_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
GROUP BY mes_cmpt
ORDER BY num_internacoes DESC
LIMIT 1
```

**Resultado:** `got=8 exp=8`
**TTA:** 3974ms

---

### ❌ Q44 — Temporal/Comparativa

**Q:** Evolução mensal do custo total de internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt, 
    SUM(COALESCE(custo_total, 0)) AS total_custo
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND ano_cmpt = 2023
    AND uf_origem = 'SP'
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'total_custo': 1163311.8100000008} exp={'mes_cmpt': 1, 'custo_total': 16004871.37}`
**TTA:** 4203ms

---

### ❌ Q45 — Temporal/Comparativa

**Q:** Volume mensal de internações por trauma (S00-T98) vs doenças musculoesqueléticas (M00-M99) em SP em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    SUM(CASE WHEN icd_group = 'S00-T98' THEN 1 ELSE 0 END) AS trauma_internacoes,
    SUM(CASE WHEN icd_group = 'M00-M99' THEN 1 ELSE 0 END) AS musculo_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt
```

**Resultado:** `Número de linhas: got=12 exp=24`
**TTA:** 4155ms

---

### ❌ Q46 — Temporal/Comparativa

**Q:** Evolução mensal de óbitos em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt, 
    COUNT(DISTINCT n_aih) AS total_obitos
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND ano_cmpt = 2023
    AND uf_origem = 'SP'
    AND morte = 1
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'total_obitos': 6} exp={'mes_cmpt': 1, 'total_obitos': 198}`
**TTA:** 4138ms

---

### ❌ Q47 — Temporal/Comparativa

**Q:** Permanência média por trimestre nas internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT 
    CEIL(mes_cmpt / 3.0) AS trimestre,
    AVG(dias_perm) AS permanencia_media
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    trimestre
ORDER BY 
    trimestre
```

**Resultado:** `Linha não encontrada: {'trimestre': 'Q1', 'permanencia_media_dias': 6.1}`
**TTA:** 4343ms

---

### ❌ Q48 — Temporal/Comparativa

**Q:** Mês com maior custo médio por internação ortopédica em SP em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt, 
    AVG(custo_total) AS custo_medio
FROM 
    processed
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2023 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    custo_medio DESC
LIMIT 1
```

**Resultado:** `got=8 exp=1`
**TTA:** 3707ms

---

### ✅ Q49 — Temporal/Comparativa

**Q:** Sazonalidade mensal de fraturas de fêmur (S72) em SP em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt, 
    COUNT(*) AS total_fraturas_femur
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND cid_principal LIKE 'S72%'
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 3915ms

---

### ❌ Q50 — Temporal/Comparativa

**Q:** Volume mensal de internações ortopédicas de idosos (60 anos ou mais) em SP em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt, 
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND ano_cmpt = 2023
    AND uf_origem = 'SP'
    AND idade_paciente >= 60
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'volume_internacoes': 147} exp={'mes_cmpt': 1, 'total_internacoes': 2318}`
**TTA:** 4120ms

---

### ✅ Q51 — Epidemiológica Simples

**Q:** Número de internações por fratura de fêmur (S72) em mulheres em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND cid_principal LIKE 'S72%'
  AND sexo_paciente = 'F'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
```

**Resultado:** `got=15639 exp=15639`
**TTA:** 4037ms

---

### ✅ Q52 — Epidemiológica Simples

**Q:** Número de internações por fratura de fêmur (S72) em homens em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND cid_principal LIKE 'S72%'
  AND sexo_paciente = 'M'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
```

**Resultado:** `got=14633 exp=14633`
**TTA:** 4171ms

---

### ❌ Q53 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com permanência de 1 dia ou menos em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) 
FROM processed 
WHERE sistema = 'SIH' 
  AND uf_origem = 'SP' 
  AND ano_cmpt = 2023 
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99') 
  AND dias_perm <= 1
```

**Resultado:** `got=2388 exp=14718`
**TTA:** 4301ms

---

### ❌ Q54 — Epidemiológica Simples

**Q:** Número de procedimentos ambulatoriais ortopédicos realizados em mulheres em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(*)
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND sexo_paciente = 'F'
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=6424 exp=47792`
**TTA:** 3747ms

---

### ❌ Q55 — Epidemiológica Simples

**Q:** Número de procedimentos ambulatoriais ortopédicos realizados por idosos (60 anos ou mais) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(*)
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND idade_paciente >= 60
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=4515 exp=55711`
**TTA:** 4007ms

---

### ✅ Q56 — Epidemiológica Simples

**Q:** Número de internações por fratura de tíbia e perônio (S82) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S82%'
```

**Resultado:** `got=6827 exp=6827`
**TTA:** 4262ms

---

### ✅ Q57 — Epidemiológica Simples

**Q:** Número de internações por fratura de antebraço (S52) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS num_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S52%'
```

**Resultado:** `got=2264 exp=2264`
**TTA:** 3880ms

---

### ✅ Q58 — Epidemiológica Simples

**Q:** Número de internações por fratura de ombro e úmero (S42) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S42%'
```

**Resultado:** `got=1391 exp=1391`
**TTA:** 4020ms

---

### ❌ Q59 — Epidemiológica Simples

**Q:** Número de internações ortopédicas em crianças (menores de 18 anos) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND idade_paciente < 18
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=559 exp=4040`
**TTA:** 4258ms

---

### ❌ Q60 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com permanência entre 8 e 30 dias em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS num_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
  AND dias_perm BETWEEN 8 AND 30
```

**Resultado:** `got=1349 exp=16748`
**TTA:** 4469ms

---

### ✅ Q61 — Epidemiológica Simples

**Q:** Número de internações por artrose de quadril e joelho (M16 e M17) em idosos (60 anos ou mais) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND idade_paciente >= 60
  AND (cid_principal LIKE 'M16%' OR cid_principal LIKE 'M17%')
```

**Resultado:** `got=100 exp=100`
**TTA:** 3561ms

---

### ✅ Q62 — Epidemiológica Simples

**Q:** Número de internações por fratura de fêmur (S72) em mulheres com 70 anos ou mais em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND cid_principal LIKE 'S72%'
  AND sexo_paciente = 'F'
  AND idade_paciente >= 70
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
```

**Resultado:** `got=11171 exp=11171`
**TTA:** 5044ms

---

### ❌ Q63 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com permanência superior a 30 dias em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) 
FROM processed 
WHERE sistema = 'SIH' 
  AND uf_origem = 'SP' 
  AND ano_cmpt = 2023 
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99') 
  AND dias_perm > 30
```

**Resultado:** `got=177 exp=1037`
**TTA:** 3283ms

---

### ❌ Q64 — Epidemiológica Simples

**Q:** Número de procedimentos ambulatoriais ortopédicos realizados em homens em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(*)
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND sexo_paciente = 'M'
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=5410 exp=57572`
**TTA:** 3959ms

---

### ✅ Q65 — Epidemiológica Simples

**Q:** Número de internações por traumatismos (grupo S00-T98) em idosos (60 anos ou mais) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND icd_group = 'S00-T98'
  AND idade_paciente >= 60
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
```

**Resultado:** `got=26253 exp=26253`
**TTA:** 3941ms

---

### ✅ Q66 — Epidemiológica Simples

**Q:** Número de internações por doenças osteomusculares (grupo M00-M99) em mulheres em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND e.clinical_interpretacao_clinica = 'doencas osteomusculares e do tecido conjuntivo'
  AND sexo_paciente = 'F'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
```

**Resultado:** `got=2554 exp=2554`
**TTA:** 4648ms

---

### ✅ Q67 — Epidemiológica Simples

**Q:** Número de óbitos em internações por fratura de fêmur (S72) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(*) AS numero_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
  AND morte = 1
```

**Resultado:** `got=1250 exp=1250`
**TTA:** 3454ms

---

### ❌ Q68 — Epidemiológica Simples

**Q:** Número de municípios distintos com registro de procedimentos ambulatoriais ortopédicos em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT cod_munic_estabelecimento) AS num_municipios
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=73 exp=400`
**TTA:** 4719ms

---

### ❌ Q69 — Epidemiológica Complexa

**Q:** Top 5 municípios por volume de procedimentos ambulatoriais ortopédicos em SP em 2023

**SQL gerado:**
```sql
SELECT 
    p.cod_munic_estabelecimento, 
    COUNT(*) AS volume_procedimentos
FROM 
    processed p
WHERE 
    p.sistema = 'SIA'
    AND p.uf_origem = 'SP'
    AND p.ano_cmpt = 2023
    AND (p.cid_principal LIKE 'M%' OR p.icd_group = 'M00-M99')
GROUP BY 
    p.cod_munic_estabelecimento
ORDER BY 
    volume_procedimentos DESC
LIMIT 5
```

**Resultado:** `Linha 1: got={'cod_munic_estabelecimento': '355030', 'volume_procedimentos': 6895} exp={'cod_munic_estabelecimento': '355030', 'total': 35601}`
**TTA:** 3871ms

---

### ❌ Q70 — Epidemiológica Complexa

**Q:** Distribuição de procedimentos ambulatoriais ortopédicos por faixa etária em SP em 2023

**SQL gerado:**
```sql
SELECT 
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 34 THEN '18-34'
        WHEN idade_paciente BETWEEN 35 AND 49 THEN '35-49'
        WHEN idade_paciente BETWEEN 50 AND 64 THEN '50-64'
        ELSE '65+' 
    END AS faixa_etaria,
    COUNT(*) AS quantidade_procedimentos
FROM 
    processed
WHERE 
    sistema = 'SIA'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    faixa_etaria
ORDER BY 
    faixa_etaria
```

**Resultado:** `Número de linhas: got=5 exp=3`
**TTA:** 4608ms

---

### ❌ Q71 — Epidemiológica Complexa

**Q:** Top 5 diagnósticos com maior número de óbitos em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT 
    p.cid_principal, 
    COUNT(*) AS num_obitos
FROM 
    processed p
JOIN 
    enriched e ON p.row_id = e.row_id
WHERE 
    p.sistema = 'SIH'
    AND p.uf_origem = 'SP'
    AND p.ano_cmpt = 2023
    AND p.morte = 1
    AND (p.cid_principal LIKE 'M%' OR p.icd_group = 'M00-M99')
GROUP BY 
    p.cid_principal
ORDER BY 
    num_obitos DESC
LIMIT 5
```

**Resultado:** `Linha 1: got={'cid_principal': 'M869', 'num_obitos': 17} exp={'cid_principal': 'S720', 'total_internacoes': 9059, 'total_obitos': 478.0}`
**TTA:** 4194ms

---

### ❌ Q72 — Epidemiológica Complexa

**Q:** Permanência hospitalar média por sexo em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT 
    sexo_paciente, 
    AVG(dias_perm) AS permanencia_media
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    sexo_paciente
```

**Resultado:** `Linha não encontrada: {'sexo_paciente': 'F', 'permanencia_media': 6.3}`
**TTA:** 3549ms

---

### ❌ Q73 — Epidemiológica Complexa

**Q:** Top 10 estabelecimentos por volume de procedimentos ambulatoriais ortopédicos em SP em 2023

**SQL gerado:**
```sql
SELECT 
    p.cnes_estabelecimento, 
    COUNT(*) AS volume_procedimentos
FROM 
    processed p
WHERE 
    p.sistema = 'SIA'
    AND p.uf_origem = 'SP'
    AND p.ano_cmpt = 2023
    AND (p.cid_principal LIKE 'M%' OR p.icd_group = 'M00-M99')
GROUP BY 
    p.cnes_estabelecimento
ORDER BY 
    volume_procedimentos DESC
LIMIT 10
```

**Resultado:** `Linha 1: got={'cnes_estabelecimento': '2078015', 'volume_procedimentos': 2811} exp={'cnes_estabelecimento': '2068974', 'total': 7838}`
**TTA:** 4226ms

---

### ❌ Q74 — Epidemiológica Complexa

**Q:** Taxa de mortalidade por sexo em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT 
    sexo_paciente,
    COUNT(DISTINCT CASE WHEN morte = 1 THEN n_aih END) * 100.0 / COUNT(DISTINCT n_aih) AS taxa_mortalidade
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND ano_cmpt = 2023
    AND uf_origem = 'SP'
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    sexo_paciente
```

**Resultado:** `Linha não encontrada: {'sexo_paciente': 'F', 'total_obitos': 1050.0, 'taxa_mortalidade': 3.89}`
**TTA:** 4179ms

---

### ❌ Q75 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por grupo diagnóstico (M e S) e faixa etária em SP em 2023

**SQL gerado:**
```sql
SELECT 
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 34 THEN '18-34'
        WHEN idade_paciente BETWEEN 35 AND 49 THEN '35-49'
        WHEN idade_paciente BETWEEN 50 AND 64 THEN '50-64'
        ELSE '65+' 
    END AS faixa_etaria,
    icd_group,
    COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY 
    faixa_etaria, icd_group
ORDER BY 
    faixa_etaria, icd_group;
```

**Resultado:** `Número de linhas: got=10 exp=6`
**TTA:** 4477ms

---

### ✅ Q76 — Epidemiológica Complexa

**Q:** Top 5 diagnósticos de traumatismo com maior permanência média em SP em 2023 (mínimo 40 internações)

**SQL gerado:**
```sql
SELECT 
    p.cid_principal, 
    COUNT(DISTINCT p.n_aih) AS num_internacoes,
    AVG(p.dias_perm) AS permanencia_media
FROM 
    processed p
JOIN 
    enriched e ON p.row_id = e.row_id
WHERE 
    p.sistema = 'SIH'
    AND p.uf_origem = 'SP'
    AND p.ano_cmpt = 2023
    AND e.clinical_interpretacao_clinica = 'lesoes e causas traumaticas'
GROUP BY 
    p.cid_principal
HAVING 
    COUNT(DISTINCT p.n_aih) >= 40
ORDER BY 
    permanencia_media DESC
LIMIT 5
```

**Resultado:** `5 linhas corretas (flexível)`
**TTA:** 4427ms

---

### ❌ Q77 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por faixa etária (0-17, 18-39, 40-59, 60-79, 80+) em SP em 2023

**SQL gerado:**
```sql
SELECT 
    CASE 
        WHEN idade_paciente BETWEEN 0 AND 17 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 39 THEN '18-39'
        WHEN idade_paciente BETWEEN 40 AND 59 THEN '40-59'
        WHEN idade_paciente BETWEEN 60 AND 79 THEN '60-79'
        ELSE '80+'
    END AS faixa_etaria,
    COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    faixa_etaria
ORDER BY 
    faixa_etaria
```

**Resultado:** `Linha não encontrada: {'faixa_etaria': '0-17', 'total': 4040}`
**TTA:** 4011ms

---

### ✅ Q78 — Epidemiológica Complexa

**Q:** Top 5 estabelecimentos com maior volume de internações por fratura de fêmur (S72) em SP em 2023

**SQL gerado:**
```sql
SELECT 
    p.cnes_estabelecimento, 
    COUNT(DISTINCT p.n_aih) AS total_internacoes
FROM 
    processed p
WHERE 
    p.sistema = 'SIH'
    AND p.uf_origem = 'SP'
    AND p.ano_cmpt = 2023
    AND p.cid_principal LIKE 'S72%'
GROUP BY 
    p.cnes_estabelecimento
ORDER BY 
    total_internacoes DESC
LIMIT 5
```

**Resultado:** `5 linhas corretas (flexível)`
**TTA:** 3947ms

---

### ❌ Q79 — Epidemiológica Complexa

**Q:** Permanência hospitalar média por faixa etária em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT 
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 34 THEN '18-34'
        WHEN idade_paciente BETWEEN 35 AND 49 THEN '35-49'
        WHEN idade_paciente BETWEEN 50 AND 64 THEN '50-64'
        ELSE '65+' 
    END AS faixa_etaria,
    AVG(dias_perm) AS permanencia_media
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    faixa_etaria
ORDER BY 
    faixa_etaria
```

**Resultado:** `Número de linhas: got=5 exp=3`
**TTA:** 4388ms

---

### ✅ Q80 — Epidemiológica Complexa

**Q:** Top 5 diagnósticos de doença osteomuscular (M00-M99) por número de internações em SP em 2023

**SQL gerado:**
```sql
SELECT 
    main_icd AS diagnostico,
    COUNT(DISTINCT n_aih) AS numero_internacoes
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND e.clinical_interpretacao_clinica = 'doencas osteomusculares e do tecido conjuntivo'
GROUP BY 
    main_icd
ORDER BY 
    numero_internacoes DESC
LIMIT 5
```

**Resultado:** `5 linhas corretas (flexível)`
**TTA:** 4623ms

---

### ❌ Q81 — Epidemiológica Complexa

**Q:** Número de internações ortopédicas com permanência entre 3 e 7 dias em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) 
FROM processed 
WHERE sistema = 'SIH' 
  AND uf_origem = 'SP' 
  AND ano_cmpt = 2023 
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99') 
  AND dias_perm BETWEEN 3 AND 7
```

**Resultado:** `got=1769 exp=27009`
**TTA:** 3426ms

---

### ❌ Q82 — Epidemiológica Complexa

**Q:** Top 5 diagnósticos ambulatoriais ortopédicos em idosos (60 anos ou mais) em SP em 2023

**SQL gerado:**
```sql
SELECT 
  main_icd, 
  COUNT(*) AS total
FROM 
  processed
WHERE 
  sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND idade_paciente >= 60
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
  main_icd
ORDER BY 
  total DESC
LIMIT 5
```

**Resultado:** `Linha 1: got={'main_icd': 'M216', 'total': 1846} exp={'cid_principal': 'S720', 'total': 11745}`
**TTA:** 4262ms

---

### ❌ Q83 — Epidemiológica Complexa

**Q:** Média de internações ortopédicas por estabelecimento em SP em 2023

**SQL gerado:**
```sql
SELECT 
    cod_munic_estabelecimento,
    cnes_estabelecimento,
    AVG(internacoes) AS media_internacoes
FROM (
    SELECT 
        cod_munic_estabelecimento,
        cnes_estabelecimento,
        COUNT(DISTINCT n_aih) AS internacoes
    FROM 
        processed
    WHERE 
        sistema = 'SIH'
        AND ano_cmpt = 2023
        AND uf_origem = 'SP'
        AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
    GROUP BY 
        cod_munic_estabelecimento,
        cnes_estabelecimento
) AS subquery
GROUP BY 
    cod_munic_estabelecimento,
    cnes_estabelecimento
```

**Resultado:** `got=353470 exp=180.5`
**TTA:** 4598ms

---

### ❌ Q84 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por faixa de permanência em SP em 2023

**SQL gerado:**
```sql
SELECT 
    CASE 
        WHEN dias_perm <= 3 THEN '1-3 dias'
        WHEN dias_perm BETWEEN 4 AND 7 THEN '4-7 dias'
        WHEN dias_perm BETWEEN 8 AND 14 THEN '8-14 dias'
        WHEN dias_perm BETWEEN 15 AND 30 THEN '15-30 dias'
        ELSE 'mais de 30 dias'
    END AS faixa_permanencia,
    COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    faixa_permanencia
ORDER BY 
    faixa_permanencia
```

**Resultado:** `Número de linhas: got=5 exp=4`
**TTA:** 4652ms

---

### ❌ Q85 — Financeira

**Q:** Custo total de internações ortopédicas de idosos (60 anos ou mais) em SP em 2023

**SQL gerado:**
```sql
SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes_ortopedicas_idosos_2023
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND ano_cmpt = 2023
    AND uf_origem = 'SP'
    AND idade_paciente >= 60
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=5568723.949999998 exp=83044181.8`
**TTA:** 3302ms

---

### ✅ Q86 — Financeira

**Q:** Custo médio por internação por fratura de fêmur (S72) em SP em 2023

**SQL gerado:**
```sql
SELECT 
    COALESCE(SUM(custo_total) / COUNT(DISTINCT n_aih), 0) AS custo_medio_por_internacao
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND cid_principal LIKE 'S72%'
```

**Resultado:** `got=2731.6361469345 exp=2730.82`
**TTA:** 4061ms

---

### ❌ Q87 — Financeira

**Q:** Custo total de internações ortopédicas com passagem por UTI em SP em 2023

**SQL gerado:**
```sql
SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes_ortopedicas_uti
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND ano_cmpt = 2023
    AND uf_origem = 'SP'
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
    AND uti_int_to > 0
```

**Resultado:** `got=None exp=6792.17`
**TTA:** 4042ms

---

### ❌ Q88 — Financeira

**Q:** Custo médio por internação ortopédica por sexo do paciente em SP em 2023

**SQL gerado:**
```sql
SELECT 
    sexo_paciente, 
    AVG(custo_total) AS custo_medio
FROM 
    processed
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2023 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    sexo_paciente
```

**Resultado:** `Linha não encontrada: {'sexo_paciente': 'F', 'custo_medio': 2710.26}`
**TTA:** 4098ms

---

### ❌ Q89 — Financeira

**Q:** Custo total de internações ortopédicas em crianças (menores de 18 anos) em SP em 2023

**SQL gerado:**
```sql
SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes_ortopedicas
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND ano_cmpt = 2023
    AND uf_origem = 'SP'
    AND idade_paciente < 18
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=938048.93 exp=7933442.51`
**TTA:** 4250ms

---

### ❌ Q90 — Financeira

**Q:** Custo médio por internação ortopédica com permanência superior a 7 dias em SP em 2023

**SQL gerado:**
```sql
SELECT 
    COALESCE(SUM(custo_total) / COUNT(DISTINCT n_aih), 0) AS custo_medio_por_internacao
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND ano_cmpt = 2023
    AND uf_origem = 'SP'
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
    AND dias_perm > 7
```

**Resultado:** `got=3437.7343523997 exp=4390.48`
**TTA:** 4216ms

---

### ❌ Q91 — Financeira

**Q:** Top 3 municípios com maior custo total em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT 
    p.cod_munic_estabelecimento, 
    SUM(p.custo_total) AS total_custo
FROM 
    processed p
JOIN 
    enriched e ON p.row_id = e.row_id
WHERE 
    p.sistema = 'SIH'
    AND p.uf_origem = 'SP'
    AND p.ano_cmpt = 2023
    AND (p.cid_principal LIKE 'M%' OR p.icd_group = 'M00-M99')
GROUP BY 
    p.cod_munic_estabelecimento
ORDER BY 
    total_custo DESC
LIMIT 3
```

**Resultado:** `Linha 1: got={'cod_munic_estabelecimento': '350000', 'total_custo': 7641321.079999998} exp={'cod_munic_estabelecimento': '350000', 'custo_total': 98503323.97}`
**TTA:** 4820ms

---

### ❌ Q92 — Financeira

**Q:** Valor total de serviço hospitalar em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT COALESCE(SUM(val_sh), 0) AS total_valor_servico_hospitalar
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=12531360.409999983 exp=153782049.46`
**TTA:** 3175ms

---

### ❌ Q93 — Financeira

**Q:** Custo médio por internação ortopédica por faixa etária em SP em 2023

**SQL gerado:**
```sql
SELECT 
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 34 THEN '18-34'
        WHEN idade_paciente BETWEEN 35 AND 49 THEN '35-49'
        WHEN idade_paciente BETWEEN 50 AND 64 THEN '50-64'
        ELSE '65+' 
    END AS faixa_etaria,
    AVG(custo_total) AS custo_medio
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    faixa_etaria
ORDER BY 
    faixa_etaria
```

**Resultado:** `Número de linhas: got=5 exp=3`
**TTA:** 4666ms

---

### ❌ Q94 — Financeira

**Q:** Valor total de UTI em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT 
    COALESCE(SUM(val_uti), 0) AS total_valor_uti
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=1778326.4 exp=40878597.03`
**TTA:** 3954ms

---

### ✅ Q95 — Financeira

**Q:** Custo total de internações por doenças osteomusculares (M00-M99) em SP em 2023

**SQL gerado:**
```sql
SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND icd_group = 'M00-M99'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
```

**Resultado:** `got=15790831.060000002 exp=15790831.06`
**TTA:** 3632ms

---

### ✅ Q96 — Financeira

**Q:** Custo total de internações por traumatismos (S00-T98) em SP em 2023

**SQL gerado:**
```sql
SELECT SUM(COALESCE(custo_total, 0)) AS total_custo_traumatismos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'S%' OR icd_group = 'S00-T98')
```

**Resultado:** `got=171429990.73000064 exp=171429990.73`
**TTA:** 4260ms

---

### ❌ Q97 — Financeira

**Q:** Top 5 diagnósticos com maior custo médio por internação ortopédica em SP em 2023 (mínimo 40 internações)

**SQL gerado:**
```sql
SELECT 
    p.cid_principal, 
    COUNT(DISTINCT p.n_aih) AS num_internacoes,
    SUM(p.custo_total) / COUNT(DISTINCT p.n_aih) AS custo_medio
FROM 
    processed p
WHERE 
    p.sistema = 'SIH'
    AND p.uf_origem = 'SP'
    AND p.ano_cmpt = 2023
    AND (p.cid_principal LIKE 'M%' OR p.icd_group = 'M00-M99')
GROUP BY 
    p.cid_principal
HAVING 
    COUNT(DISTINCT p.n_aih) >= 40
ORDER BY 
    custo_medio DESC
LIMIT 5
```

**Resultado:** `Linha 1: got={'cid_principal': 'M510', 'num_internacoes': 53, 'custo_medio': 8514.0111320755} exp={'cid_principal': 'S221', 'internacoes': 41, 'custo_medio': 14403.66}`
**TTA:** 4534ms

---

### ❌ Q98 — Financeira

**Q:** Custo médio por procedimento ambulatorial ortopédico em SP em 2023

**SQL gerado:**
```sql
SELECT 
    AVG(custo_total) AS custo_medio
FROM 
    processed
WHERE 
    sistema = 'SIA'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=178.2206692581 exp=86.29`
**TTA:** 3861ms

---

### ❌ Q99 — Financeira

**Q:** Top 5 procedimentos ambulatoriais com maior custo total em SP em 2023

**SQL gerado:**
```sql
SELECT cod_procedimento, SUM(custo_total) AS total_custo
FROM processed
WHERE sistema = 'SIA' AND uf_origem = 'SP' AND ano_cmpt = 2023
GROUP BY cod_procedimento
ORDER BY total_custo DESC
LIMIT 5
```

**Resultado:** `Linha 1: got={'cod_procedimento': '0702100048', 'total_custo': 45189222.96000072} exp={'cod_procedimento': '0701020369', 'custo_total': 1261008.0}`
**TTA:** 4076ms

---

### ❌ Q100 — Financeira

**Q:** Custo total de internações ortopédicas de idosos (60+) com permanência superior a 7 dias em SP em 2023

**SQL gerado:**
```sql
SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND ano_cmpt = 2023
    AND uf_origem = 'SP'
    AND idade_paciente >= 60
    AND dias_perm > 7
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=2241104.4899999998 exp=37989386.14`
**TTA:** 4277ms

---

### ❌ Q101 — Temporal/Comparativa

**Q:** Mês com maior número de óbitos em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT mes_cmpt, COUNT(*) AS num_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND morte = 1
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY mes_cmpt
ORDER BY num_obitos DESC
LIMIT 1
```

**Resultado:** `Linha 1: got={'mes_cmpt': 9, 'num_obitos': 12} exp={'mes_cmpt': 11, 'total_obitos': 209}`
**TTA:** 4044ms

---

### ✅ Q102 — Temporal/Comparativa

**Q:** Volume mensal de internações por fratura de fêmur (S72) em idosos (60 anos ou mais) em SP em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt, 
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND idade_paciente >= 60
    AND cid_principal LIKE 'S72%'
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 3919ms

---

### ❌ Q103 — Temporal/Comparativa

**Q:** Custo mensal de internações ortopédicas de idosos (60 anos ou mais) em SP em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    SUM(COALESCE(custo_total, 0)) AS custo_total_mensal
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND ano_cmpt = 2023
    AND uf_origem = 'SP'
    AND idade_paciente >= 60
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'custo_total_mensal': 494055.7599999998} exp={'mes_cmpt': 1, 'custo_total': 7166029.19}`
**TTA:** 4565ms

---

### ✅ Q104 — Temporal/Comparativa

**Q:** Mês com maior custo total de internações por fratura de fêmur (S72) em SP em 2023

**SQL gerado:**
```sql
SELECT 
  mes_cmpt, 
  SUM(custo_total) AS total_custo
FROM 
  processed
WHERE 
  sistema = 'SIH' 
  AND cid_principal LIKE 'S72%' 
  AND uf_origem = 'SP' 
  AND ano_cmpt = 2023
GROUP BY 
  mes_cmpt
ORDER BY 
  total_custo DESC
LIMIT 1
```

**Resultado:** `1 linhas corretas (flexível)`
**TTA:** 4242ms

---

### ✅ Q105 — Temporal/Comparativa

**Q:** Volume mensal de internações por traumatismos (grupo S00-T98) em SP em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt, 
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND icd_group = 'S00-T98'
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 3642ms

---

### ❌ Q106 — Temporal/Comparativa

**Q:** Volume mensal de procedimentos ambulatoriais ortopédicos por sexo em SP em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt, 
    sexo_paciente, 
    SUM(pa_qtdapr) AS total_procedimentos
FROM 
    processed
WHERE 
    sistema = 'SIA'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt, 
    sexo_paciente
ORDER BY 
    mes_cmpt, 
    sexo_paciente
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'sexo_paciente': 'F', 'total_procedimentos': 359.0} exp={'mes_cmpt': 1, 'sexo_paciente': 'F', 'total': 3733}`
**TTA:** 4333ms

---

### ❌ Q107 — Temporal/Comparativa

**Q:** Comparativo trimestral de internações ortopédicas por grupo diagnóstico em SP em 2023

**SQL gerado:**
```sql
SELECT 
    FLOOR((mes_cmpt - 1) / 3) + 1 AS trimestre,
    icd_group,
    COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND ano_cmpt = 2023
    AND uf_origem = 'SP'
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    trimestre, icd_group
ORDER BY 
    trimestre, icd_group;
```

**Resultado:** `Número de linhas: got=4 exp=8`
**TTA:** 4086ms

---

### ❌ Q108 — Temporal/Comparativa

**Q:** Mês com maior permanência hospitalar média em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT mes_cmpt, AVG(dias_perm) AS media_permanencia
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY mes_cmpt
ORDER BY media_permanencia DESC
LIMIT 1
```

**Resultado:** `Linha 1: got={'mes_cmpt': 11, 'media_permanencia': 6.4865900383} exp={'mes_cmpt': 11, 'permanencia_media': 6.4}`
**TTA:** 3982ms

---

### ❌ Q109 — Temporal/Comparativa

**Q:** Evolução mensal da taxa de mortalidade em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    COUNT(DISTINCT CASE WHEN morte = 1 THEN n_aih END) * 100.0 / COUNT(DISTINCT n_aih) AS taxa_mortalidade
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND ano_cmpt = 2023
    AND uf_origem = 'SP'
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'taxa_mortalidade': 1.1834319527} exp={'mes_cmpt': 1, 'internacoes': 5667, 'obitos': 198.0, 'taxa_mortalidade': 3.49}`
**TTA:** 4277ms

---

### ❌ Q110 — Temporal/Comparativa

**Q:** Trimestre com maior volume de internações por fratura de fêmur (S72) em SP em 2023

**SQL gerado:**
```sql
SELECT 
  CEIL(mes_cmpt / 3.0) AS trimestre,
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
GROUP BY 
  trimestre
ORDER BY 
  total_internacoes DESC
LIMIT 1
```

**Resultado:** `Linha 1: got={'trimestre': 3.0, 'total_internacoes': 7947} exp={'trimestre': 'Q3', 'total': 7947}`
**TTA:** 4216ms

---

### ❌ Q111 — Temporal/Comparativa

**Q:** Comparativo semestral de internações ortopédicas (1º vs 2º semestre) em SP em 2023

**SQL gerado:**
```sql
SELECT 
    CASE 
        WHEN mes_cmpt BETWEEN 1 AND 6 THEN '1º Semestre'
        ELSE '2º Semestre'
    END AS semestre,
    COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    semestre
ORDER BY 
    semestre
```

**Resultado:** `Linha não encontrada: {'semestre': '1S', 'total': 33739}`
**TTA:** 4107ms

---

### ✅ Q112 — Temporal/Comparativa

**Q:** Custo médio mensal por internação por fratura de fêmur (S72) em SP em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    COALESCE(SUM(custo_total) / COUNT(DISTINCT n_aih), 0) AS custo_medio_mensal
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND cid_principal LIKE 'S72%'
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 3874ms

---

### ❌ Q113 — Temporal/Comparativa

**Q:** Volume mensal de internações ortopédicas por sexo do paciente em SP em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    sexo_paciente,
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND ano_cmpt = 2023
    AND uf_origem = 'SP'
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt, 
    sexo_paciente
ORDER BY 
    mes_cmpt, 
    sexo_paciente
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'sexo_paciente': 'F', 'volume_internacoes': 207} exp={'mes_cmpt': 1, 'sexo_paciente': 'F', 'total': 2219}`
**TTA:** 4436ms

---

### ❌ Q114 — Temporal/Comparativa

**Q:** Mês com menor volume de procedimentos ambulatoriais ortopédicos em SP em 2023

**SQL gerado:**
```sql
SELECT mes_cmpt, SUM(pa_qtdapr) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY mes_cmpt
ORDER BY total_procedimentos ASC
LIMIT 1
```

**Resultado:** `Linha 1: got={'mes_cmpt': 6, 'total_procedimentos': 603.0} exp={'mes_cmpt': 6, 'total': 3733}`
**TTA:** 3905ms

---

### ❌ Q115 — Temporal/Comparativa

**Q:** Volume trimestral de procedimentos ambulatoriais ortopédicos em SP em 2023

**SQL gerado:**
```sql
SELECT 
  CEIL(mes_cmpt / 3.0) AS trimestre,
  SUM(pa_qtdapr) AS total_procedimentos
FROM 
  processed
WHERE 
  sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
  trimestre
ORDER BY 
  trimestre
```

**Resultado:** `Linha não encontrada: {'trimestre': 'Q1', 'total': 26812}`
**TTA:** 4208ms

---

### ❌ Q116 — Temporal/Comparativa

**Q:** Custo mensal de procedimentos ambulatoriais ortopédicos em SP em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt, 
    SUM(COALESCE(custo_total, 0)) AS custo_total_mensal
FROM 
    processed
WHERE 
    sistema = 'SIA'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'custo_total_mensal': 116813.9299999998} exp={'mes_cmpt': 1, 'custo_total': 598431.72}`
**TTA:** 3986ms

---

### ❌ Q117 — Temporal/Comparativa

**Q:** Comparativo semestral do custo total de internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT 
    CASE 
        WHEN mes_cmpt BETWEEN 1 AND 6 THEN '1º Semestre'
        WHEN mes_cmpt BETWEEN 7 AND 12 THEN '2º Semestre'
    END AS semestre,
    SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    semestre
ORDER BY 
    semestre
```

**Resultado:** `Linha não encontrada: {'semestre': '1S', 'custo_total': 90189950.58}`
**TTA:** 4623ms

---

### ✅ Q118 — Temporal/Comparativa

**Q:** Evolução mensal da permanência média em internações por fratura de fêmur (S72) em SP em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    AVG(dias_perm) AS permanencia_media
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND cid_principal LIKE 'S72%'
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt
```

**Resultado:** `12 linhas corretas (exato)`
**TTA:** 3522ms

---

## Failures Detail

- **Q02** (Epidemiológica Simples): got=12485.0 exp=105364
  SQL: `SELECT SUM(pa_qtdapr) AS total_procedimentos_ambulatoriais_ortopedicos
FROM processed
WHERE sistema = 'SIA'
  AND uf_ori...`
- **Q05** (Epidemiológica Simples): got=81 exp=2274
  SQL: `SELECT COUNT(*) AS numero_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND...`
- **Q08** (Epidemiológica Simples): got=2554 exp=27027
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND sexo_paciente = 'F'
  AND ...`
- **Q09** (Epidemiológica Simples): got=4326 exp=42460
  SQL: `SELECT COUNT(DISTINCT n_aih) AS num_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND sexo_paciente = 'M'
  AND uf_...`
- **Q10** (Epidemiológica Simples): got=1912 exp=28165
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND ano_cmpt = 2023
  AND uf_o...`
- **Q11** (Epidemiológica Simples): got=166 exp=215
  SQL: `SELECT COUNT(DISTINCT cod_munic_estabelecimento)
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_c...`
- **Q12** (Epidemiológica Simples): got=310 exp=385
  SQL: `SELECT COUNT(DISTINCT cnes_estabelecimento) 
FROM processed 
WHERE sistema = 'SIH' 
  AND uf_origem = 'SP' 
  AND ano_cm...`
- **Q13** (Epidemiológica Simples): got=1521 exp=17778
  SQL: `SELECT COUNT(DISTINCT n_aih) 
FROM processed 
WHERE sistema = 'SIH' 
  AND uf_origem = 'SP' 
  AND ano_cmpt = 2023 
  AN...`
- **Q14** (Epidemiológica Simples): got=0 exp=1
  SQL: `SELECT COUNT(DISTINCT n_aih) AS num_internacoes_ortopedicas_uti
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = '...`
- **Q16** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'M861', 'volume_internacoes': 957} exp={'cid_principal': 'S720', 'total_internacoes': 9059}
  SQL: `SELECT 
    cid_principal, 
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
WHERE 
    sistema = 'SI...`
- **Q17** (Epidemiológica Complexa): Linha 1: got={'cod_munic_estabelecimento': '350000', 'num_internacoes': 3097} exp={'cod_munic_estabelecimento': '350000', 'internacoes': 32140}
  SQL: `SELECT 
    p.cod_munic_estabelecimento, 
    COUNT(DISTINCT p.n_aih) AS num_internacoes
FROM 
    processed p
JOIN 
   ...`
- **Q18** (Epidemiológica Complexa): Linha não encontrada: {'faixa_etaria': '0-17', 'total': 4040}
  SQL: `SELECT 
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 59 THEN '18-59...`
- **Q19** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'M311', 'total_internacoes': 3, 'total_obitos': 1.0, 'taxa_mortalidade': 33.3333333333} exp={'cid_principal': 'S062', 'total_internacoes': 58, 'total_obitos': 20.0, 'taxa_mortalidade_pct': 34.48}
  SQL: `SELECT 
    p.cid_principal,
    COUNT(DISTINCT p.n_aih) AS total_internacoes,
    SUM(CASE WHEN p.morte = 1 THEN 1 ELSE...`
- **Q20** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'M216', 'volume_producao': 5349} exp={'cid_principal': 'S720', 'total_procedimentos': 17325}
  SQL: `SELECT 
    cid_principal, 
    COUNT(*) AS volume_producao
FROM 
    processed
WHERE 
    sistema = 'SIA'
    AND uf_or...`
- **Q21** (Epidemiológica Complexa): Linha não encontrada: {'sexo_paciente': 'M', 'total_internacoes': 42460, 'pct': 61.1}
  SQL: `SELECT 
    sexo_paciente, 
    COUNT(DISTINCT n_aih) AS total_internacoes,
    COUNT(DISTINCT n_aih) * 1.0 / SUM(COUNT(...`
- **Q22** (Epidemiológica Complexa): Linha 1: got={'internacoes_idosos_fratura_femur': 19589, 'total_internacoes': 209808} exp={'total_s72': 30272, 's72_idosos': 19598.0, 'pct_idosos': 64.7}
  SQL: `SELECT 
    COUNT(DISTINCT CASE WHEN idade_paciente >= 60 AND cid_principal LIKE 'S72%' THEN n_aih END) AS internacoes_i...`
- **Q23** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'M412', 'permanencia_media': 60.0} exp={'cid_principal': 'S272', 'total_internacoes': 49, 'permanencia_media_dias': 14.3}
  SQL: `SELECT 
    cid_principal, 
    AVG(dias_perm) AS permanencia_media
FROM 
    processed
WHERE 
    sistema = 'SIH'
    A...`
- **Q24** (Epidemiológica Complexa): Linha 1: got={'cnes_estabelecimento': '2078015', 'volume_internacoes': 416} exp={'cnes_estabelecimento': '2078015', 'internacoes': 2257}
  SQL: `SELECT 
    p.cnes_estabelecimento, 
    COUNT(DISTINCT p.n_aih) AS volume_internacoes
FROM 
    processed p
JOIN 
    e...`
- **Q25** (Epidemiológica Complexa): Linha não encontrada: {'raca_cor_paciente': '01', 'total': 42626, 'pct': 61.3}
  SQL: `SELECT 
  raca_cor_paciente, 
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
  processed
WHERE 
  sistema = 'SIH'
  ...`
- **Q26** (Epidemiológica Complexa): Número de linhas: got=306 exp=10
  SQL: `SELECT 
    cid_principal, 
    SUM(dias_perm) AS total_dias_internacao
FROM 
    processed
WHERE 
    sistema = 'SIH'
 ...`
- **Q27** (Epidemiológica Complexa): Linha 1: got={'cod_procedimento': '0701010142', 'total_procedures': 2151.0} exp={'cod_procedimento': '0302050019', 'total_procedimentos': 72108}
  SQL: `SELECT 
    cod_procedimento, 
    SUM(pa_qtdapr) AS total_procedures
FROM 
    processed
WHERE 
    sistema = 'SIA'
   ...`
- **Q28** (Epidemiológica Complexa): Linha 1: got={'total_internacoes': 3701, 'internacoes_deslocamento_intermunicipal': 3701, 'proporcao_deslocamento_intermunicipal': 1.0} exp={'total_internacoes': 69487, 'deslocamento_intermunicipal': 39611.0, 'pct_deslocamento': 57.0}
  SQL: `SELECT 
    COUNT(DISTINCT p.n_aih) AS total_internacoes,
    COUNT(DISTINCT CASE WHEN e.clinical_deslocamento_territori...`
- **Q29** (Epidemiológica Complexa): Linha 1: got={'mes_cmpt': 1, 'sistema': 'SIA', 'volume': 605} exp={'mes_cmpt': 1, 'sistema': 'SIA', 'total': 8415}
  SQL: `SELECT 
    p.mes_cmpt,
    p.sistema,
    COUNT(*) AS volume
FROM 
    processed p
WHERE 
    p.ano_cmpt = 2023
    AND...`
- **Q30** (Epidemiológica Complexa): Linha 1: got={'idade_media': 62.3639245732} exp={'idade_media': 62.4, 'idade_minima': 0.0, 'idade_maxima': 99.0}
  SQL: `SELECT AVG(idade_paciente) AS idade_media
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2...`
- **Q31** (Financeira): got=15790831.060000004 exp=187220821.79
  SQL: `SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes_ortopedicas
FROM 
    processed
WHERE 
    sistema ...`
- **Q32** (Financeira): got=2295.1789331395 exp=2693.13
  SQL: `SELECT 
    COALESCE(SUM(custo_total) / COUNT(DISTINCT n_aih), 0) AS custo_medio_por_internacao
FROM 
    processed
WHER...`
- **Q34** (Financeira): Linha 1: got={'cid_principal': 'M511', 'total_custo': 1119270.5} exp={'cid_principal': 'S720', 'internacoes': 9059, 'custo_total': 30400546.73}
  SQL: `SELECT 
    cid_principal, 
    SUM(custo_total) AS total_custo
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND u...`
- **Q35** (Financeira): Linha 1: got={'custo_total_internacoes_ortopedicas_opme': None} exp={'internacoes_com_opme': 0, 'custo_total_opme': None}
  SQL: `SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes_ortopedicas_opme
FROM 
    processed
WHERE 
    sis...`
- **Q36** (Financeira): got=3059022.389999998 exp=32810269.18
  SQL: `SELECT 
    COALESCE(SUM(val_sp), 0) AS total_val_sp
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = ...`
- **Q37** (Financeira): got=374.5821970394 exp=441.21
  SQL: `SELECT 
    SUM(custo_total) / SUM(dias_perm) AS custo_medio_por_dia
FROM 
    processed
WHERE 
    sistema = 'SIH'
    ...`
- **Q38** (Financeira): Linha 1: got={'cnes_estabelecimento': '2078015', 'total_custo': 1470646.2399999998} exp={'cnes_estabelecimento': '2078015', 'internacoes': 2257, 'custo_total': 11295149.42}
  SQL: `SELECT 
    p.cnes_estabelecimento, 
    SUM(p.custo_total) AS total_custo
FROM 
    processed p
JOIN 
    enriched e ON...`
- **Q39** (Financeira): got=2109063.4000000055 exp=9092221.75
  SQL: `SELECT SUM(COALESCE(custo_total, 0)) AS custo_total_ambulatorial_ortopedico
FROM processed
WHERE sistema = 'SIA'
  AND u...`
- **Q40** (Financeira): Linha 1: got={'proporcao_custo_sh_vs_honorarios': 4.0965245795} exp={'custo_servico_hospitalar': 153782049.46, 'honorarios_profissionais': 32810269.18, 'pct_hospitalar': 82.4}
  SQL: `SELECT 
    SUM(COALESCE(val_sh, 0)) / NULLIF(SUM(COALESCE(val_sp, 0)), 0) AS proporcao_custo_sh_vs_honorarios
FROM 
   ...`
- **Q41** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'total_internacoes': 507} exp={'mes_cmpt': 1, 'total_internacoes': 5667}
  SQL: `SELECT 
    mes_cmpt, 
    COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
   ...`
- **Q42** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'total_procedimentos': 605} exp={'mes_cmpt': 1, 'procedimentos': 8415}
  SQL: `SELECT 
    mes_cmpt, 
    COUNT(*) AS total_procedimentos
FROM 
    processed
WHERE 
    sistema = 'SIA' 
    AND uf_or...`
- **Q44** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'total_custo': 1163311.8100000008} exp={'mes_cmpt': 1, 'custo_total': 16004871.37}
  SQL: `SELECT 
    mes_cmpt, 
    SUM(COALESCE(custo_total, 0)) AS total_custo
FROM 
    processed
WHERE 
    sistema = 'SIH'
 ...`
- **Q45** (Temporal/Comparativa): Número de linhas: got=12 exp=24
  SQL: `SELECT 
    mes_cmpt,
    SUM(CASE WHEN icd_group = 'S00-T98' THEN 1 ELSE 0 END) AS trauma_internacoes,
    SUM(CASE WHE...`
- **Q46** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'total_obitos': 6} exp={'mes_cmpt': 1, 'total_obitos': 198}
  SQL: `SELECT 
    mes_cmpt, 
    COUNT(DISTINCT n_aih) AS total_obitos
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND ...`
- **Q47** (Temporal/Comparativa): Linha não encontrada: {'trimestre': 'Q1', 'permanencia_media_dias': 6.1}
  SQL: `SELECT 
    CEIL(mes_cmpt / 3.0) AS trimestre,
    AVG(dias_perm) AS permanencia_media
FROM 
    processed
WHERE 
    si...`
- **Q48** (Temporal/Comparativa): got=8 exp=1
  SQL: `SELECT 
    mes_cmpt, 
    AVG(custo_total) AS custo_medio
FROM 
    processed
WHERE 
    sistema = 'SIH' 
    AND uf_or...`
- **Q50** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'volume_internacoes': 147} exp={'mes_cmpt': 1, 'total_internacoes': 2318}
  SQL: `SELECT 
    mes_cmpt, 
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
  ...`
- **Q53** (Epidemiológica Simples): got=2388 exp=14718
  SQL: `SELECT COUNT(DISTINCT n_aih) 
FROM processed 
WHERE sistema = 'SIH' 
  AND uf_origem = 'SP' 
  AND ano_cmpt = 2023 
  AN...`
- **Q54** (Epidemiológica Simples): got=6424 exp=47792
  SQL: `SELECT COUNT(*)
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND sexo_paciente = ...`
- **Q55** (Epidemiológica Simples): got=4515 exp=55711
  SQL: `SELECT COUNT(*)
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND idade_paciente >...`
- **Q59** (Epidemiológica Simples): got=559 exp=4040
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano...`
- **Q60** (Epidemiológica Simples): got=1349 exp=16748
  SQL: `SELECT COUNT(DISTINCT n_aih) AS num_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cm...`
- **Q63** (Epidemiológica Simples): got=177 exp=1037
  SQL: `SELECT COUNT(DISTINCT n_aih) 
FROM processed 
WHERE sistema = 'SIH' 
  AND uf_origem = 'SP' 
  AND ano_cmpt = 2023 
  AN...`
- **Q64** (Epidemiológica Simples): got=5410 exp=57572
  SQL: `SELECT COUNT(*)
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND sexo_paciente = ...`
- **Q68** (Epidemiológica Simples): got=73 exp=400
  SQL: `SELECT COUNT(DISTINCT cod_munic_estabelecimento) AS num_municipios
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem ...`
- **Q69** (Epidemiológica Complexa): Linha 1: got={'cod_munic_estabelecimento': '355030', 'volume_procedimentos': 6895} exp={'cod_munic_estabelecimento': '355030', 'total': 35601}
  SQL: `SELECT 
    p.cod_munic_estabelecimento, 
    COUNT(*) AS volume_procedimentos
FROM 
    processed p
WHERE 
    p.sistem...`
- **Q70** (Epidemiológica Complexa): Número de linhas: got=5 exp=3
  SQL: `SELECT 
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 34 THEN '18-34...`
- **Q71** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'M869', 'num_obitos': 17} exp={'cid_principal': 'S720', 'total_internacoes': 9059, 'total_obitos': 478.0}
  SQL: `SELECT 
    p.cid_principal, 
    COUNT(*) AS num_obitos
FROM 
    processed p
JOIN 
    enriched e ON p.row_id = e.row_...`
- **Q72** (Epidemiológica Complexa): Linha não encontrada: {'sexo_paciente': 'F', 'permanencia_media': 6.3}
  SQL: `SELECT 
    sexo_paciente, 
    AVG(dias_perm) AS permanencia_media
FROM 
    processed
WHERE 
    sistema = 'SIH'
    A...`
- **Q73** (Epidemiológica Complexa): Linha 1: got={'cnes_estabelecimento': '2078015', 'volume_procedimentos': 2811} exp={'cnes_estabelecimento': '2068974', 'total': 7838}
  SQL: `SELECT 
    p.cnes_estabelecimento, 
    COUNT(*) AS volume_procedimentos
FROM 
    processed p
WHERE 
    p.sistema = '...`
- **Q74** (Epidemiológica Complexa): Linha não encontrada: {'sexo_paciente': 'F', 'total_obitos': 1050.0, 'taxa_mortalidade': 3.89}
  SQL: `SELECT 
    sexo_paciente,
    COUNT(DISTINCT CASE WHEN morte = 1 THEN n_aih END) * 100.0 / COUNT(DISTINCT n_aih) AS tax...`
- **Q75** (Epidemiológica Complexa): Número de linhas: got=10 exp=6
  SQL: `SELECT 
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 34 THEN '18-34...`
- **Q77** (Epidemiológica Complexa): Linha não encontrada: {'faixa_etaria': '0-17', 'total': 4040}
  SQL: `SELECT 
    CASE 
        WHEN idade_paciente BETWEEN 0 AND 17 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 39...`
- **Q79** (Epidemiológica Complexa): Número de linhas: got=5 exp=3
  SQL: `SELECT 
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 34 THEN '18-34...`
- **Q81** (Epidemiológica Complexa): got=1769 exp=27009
  SQL: `SELECT COUNT(DISTINCT n_aih) 
FROM processed 
WHERE sistema = 'SIH' 
  AND uf_origem = 'SP' 
  AND ano_cmpt = 2023 
  AN...`
- **Q82** (Epidemiológica Complexa): Linha 1: got={'main_icd': 'M216', 'total': 1846} exp={'cid_principal': 'S720', 'total': 11745}
  SQL: `SELECT 
  main_icd, 
  COUNT(*) AS total
FROM 
  processed
WHERE 
  sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmp...`
- **Q83** (Epidemiológica Complexa): got=353470 exp=180.5
  SQL: `SELECT 
    cod_munic_estabelecimento,
    cnes_estabelecimento,
    AVG(internacoes) AS media_internacoes
FROM (
    SE...`
- **Q84** (Epidemiológica Complexa): Número de linhas: got=5 exp=4
  SQL: `SELECT 
    CASE 
        WHEN dias_perm <= 3 THEN '1-3 dias'
        WHEN dias_perm BETWEEN 4 AND 7 THEN '4-7 dias'
   ...`
- **Q85** (Financeira): got=5568723.949999998 exp=83044181.8
  SQL: `SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes_ortopedicas_idosos_2023
FROM 
    processed
WHERE 
...`
- **Q87** (Financeira): got=None exp=6792.17
  SQL: `SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes_ortopedicas_uti
FROM 
    processed
WHERE 
    sist...`
- **Q88** (Financeira): Linha não encontrada: {'sexo_paciente': 'F', 'custo_medio': 2710.26}
  SQL: `SELECT 
    sexo_paciente, 
    AVG(custo_total) AS custo_medio
FROM 
    processed
WHERE 
    sistema = 'SIH' 
    AND ...`
- **Q89** (Financeira): got=938048.93 exp=7933442.51
  SQL: `SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes_ortopedicas
FROM 
    processed
WHERE 
    sistema ...`
- **Q90** (Financeira): got=3437.7343523997 exp=4390.48
  SQL: `SELECT 
    COALESCE(SUM(custo_total) / COUNT(DISTINCT n_aih), 0) AS custo_medio_por_internacao
FROM 
    processed
WHER...`
- **Q91** (Financeira): Linha 1: got={'cod_munic_estabelecimento': '350000', 'total_custo': 7641321.079999998} exp={'cod_munic_estabelecimento': '350000', 'custo_total': 98503323.97}
  SQL: `SELECT 
    p.cod_munic_estabelecimento, 
    SUM(p.custo_total) AS total_custo
FROM 
    processed p
JOIN 
    enriched...`
- **Q92** (Financeira): got=12531360.409999983 exp=153782049.46
  SQL: `SELECT COALESCE(SUM(val_sh), 0) AS total_valor_servico_hospitalar
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem =...`
- **Q93** (Financeira): Número de linhas: got=5 exp=3
  SQL: `SELECT 
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 34 THEN '18-34...`
- **Q94** (Financeira): got=1778326.4 exp=40878597.03
  SQL: `SELECT 
    COALESCE(SUM(val_uti), 0) AS total_valor_uti
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_orige...`
- **Q97** (Financeira): Linha 1: got={'cid_principal': 'M510', 'num_internacoes': 53, 'custo_medio': 8514.0111320755} exp={'cid_principal': 'S221', 'internacoes': 41, 'custo_medio': 14403.66}
  SQL: `SELECT 
    p.cid_principal, 
    COUNT(DISTINCT p.n_aih) AS num_internacoes,
    SUM(p.custo_total) / COUNT(DISTINCT p....`
- **Q98** (Financeira): got=178.2206692581 exp=86.29
  SQL: `SELECT 
    AVG(custo_total) AS custo_medio
FROM 
    processed
WHERE 
    sistema = 'SIA'
    AND uf_origem = 'SP'
    ...`
- **Q99** (Financeira): Linha 1: got={'cod_procedimento': '0702100048', 'total_custo': 45189222.96000072} exp={'cod_procedimento': '0701020369', 'custo_total': 1261008.0}
  SQL: `SELECT cod_procedimento, SUM(custo_total) AS total_custo
FROM processed
WHERE sistema = 'SIA' AND uf_origem = 'SP' AND a...`
- **Q100** (Financeira): got=2241104.4899999998 exp=37989386.14
  SQL: `SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    ...`
- **Q101** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 9, 'num_obitos': 12} exp={'mes_cmpt': 11, 'total_obitos': 209}
  SQL: `SELECT mes_cmpt, COUNT(*) AS num_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 202...`
- **Q103** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'custo_total_mensal': 494055.7599999998} exp={'mes_cmpt': 1, 'custo_total': 7166029.19}
  SQL: `SELECT 
    mes_cmpt,
    SUM(COALESCE(custo_total, 0)) AS custo_total_mensal
FROM 
    processed
WHERE 
    sistema = '...`
- **Q106** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'sexo_paciente': 'F', 'total_procedimentos': 359.0} exp={'mes_cmpt': 1, 'sexo_paciente': 'F', 'total': 3733}
  SQL: `SELECT 
    mes_cmpt, 
    sexo_paciente, 
    SUM(pa_qtdapr) AS total_procedimentos
FROM 
    processed
WHERE 
    sist...`
- **Q107** (Temporal/Comparativa): Número de linhas: got=4 exp=8
  SQL: `SELECT 
    FLOOR((mes_cmpt - 1) / 3) + 1 AS trimestre,
    icd_group,
    COUNT(DISTINCT n_aih) AS total_internacoes
FR...`
- **Q108** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 11, 'media_permanencia': 6.4865900383} exp={'mes_cmpt': 11, 'permanencia_media': 6.4}
  SQL: `SELECT mes_cmpt, AVG(dias_perm) AS media_permanencia
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND a...`
- **Q109** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'taxa_mortalidade': 1.1834319527} exp={'mes_cmpt': 1, 'internacoes': 5667, 'obitos': 198.0, 'taxa_mortalidade': 3.49}
  SQL: `SELECT 
    mes_cmpt,
    COUNT(DISTINCT CASE WHEN morte = 1 THEN n_aih END) * 100.0 / COUNT(DISTINCT n_aih) AS taxa_mor...`
- **Q110** (Temporal/Comparativa): Linha 1: got={'trimestre': 3.0, 'total_internacoes': 7947} exp={'trimestre': 'Q3', 'total': 7947}
  SQL: `SELECT 
  CEIL(mes_cmpt / 3.0) AS trimestre,
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
  processed
WHERE 
  sis...`
- **Q111** (Temporal/Comparativa): Linha não encontrada: {'semestre': '1S', 'total': 33739}
  SQL: `SELECT 
    CASE 
        WHEN mes_cmpt BETWEEN 1 AND 6 THEN '1º Semestre'
        ELSE '2º Semestre'
    END AS semestr...`
- **Q113** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'sexo_paciente': 'F', 'volume_internacoes': 207} exp={'mes_cmpt': 1, 'sexo_paciente': 'F', 'total': 2219}
  SQL: `SELECT 
    mes_cmpt,
    sexo_paciente,
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
WHERE 
    ...`
- **Q114** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 6, 'total_procedimentos': 603.0} exp={'mes_cmpt': 6, 'total': 3733}
  SQL: `SELECT mes_cmpt, SUM(pa_qtdapr) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND...`
- **Q115** (Temporal/Comparativa): Linha não encontrada: {'trimestre': 'Q1', 'total': 26812}
  SQL: `SELECT 
  CEIL(mes_cmpt / 3.0) AS trimestre,
  SUM(pa_qtdapr) AS total_procedimentos
FROM 
  processed
WHERE 
  sistema ...`
- **Q116** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'custo_total_mensal': 116813.9299999998} exp={'mes_cmpt': 1, 'custo_total': 598431.72}
  SQL: `SELECT 
    mes_cmpt, 
    SUM(COALESCE(custo_total, 0)) AS custo_total_mensal
FROM 
    processed
WHERE 
    sistema = ...`
- **Q117** (Temporal/Comparativa): Linha não encontrada: {'semestre': '1S', 'custo_total': 90189950.58}
  SQL: `SELECT 
    CASE 
        WHEN mes_cmpt BETWEEN 1 AND 6 THEN '1º Semestre'
        WHEN mes_cmpt BETWEEN 7 AND 12 THEN '...`

---
_Gerado por scripts/evaluate_benchmark.py — SUS Data RAG — USF/Mestrado_