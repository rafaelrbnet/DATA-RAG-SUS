# Benchmark Evaluation Report — SUS Data RAG

**Model:** ollama  
**Run date:** 2026-08-29 06:40  
**API:** http://127.0.0.1:8000  

## Summary

| Metric | Value |
|---|---|
| Total queries | 118 |
| Correct (EA numerator) | 92 |
| **Execution Accuracy (EA)** | **78.0%** |
| Wilson IC 95% | [69.7%, 84.5%] |
| Correct via flexible match (alias diferente) | 62 |
| API errors / timeouts | 0 |
| Mean Time-to-Answer | 6.3s |

> **Nota metodológica — Scoring:** A Execution Accuracy avalia se o SQL gerado
> retorna os mesmos **valores** que o gold-standard, independente do nome das colunas.
> Quando o LLM usa um alias diferente mas retorna os dados corretos (match posicional),
> a query é contada como correta e sinalizada com `~`. Esta decisão segue a definição
> de EA em [Lee et al., 2022] — comparação de conjuntos de resultado, não de SQL texto.

## EA by Category

| Category | Correct | Total | EA |
|---|---:|---:|---:|
| Epidemiológica Simples | 32 | 33 | 97.0% |
| Epidemiológica Complexa | 17 | 31 | 54.8% |
| Financeira | 19 | 26 | 73.1% |
| Temporal/Comparativa | 24 | 28 | 85.7% |

## Query Results

### ✅ Q01 — Epidemiológica Simples

**Q:** Total de internações ortopédicas (M00-M99 e S00-S99) em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=35658 exp=35658`
**TTA:** 12380ms

---

### ✅ Q02 — Epidemiológica Simples

**Q:** Total de procedimentos ambulatoriais ortopédicos em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(*) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=33877 exp=33877`
**TTA:** 4318ms

---

### ✅ Q03 — Epidemiológica Simples

**Q:** Total de internações por fratura de fêmur (S72) em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%';
```

**Resultado:** `got=10668 exp=10668`
**TTA:** 3891ms

---

### ✅ Q04 — Epidemiológica Simples

**Q:** Total de internações por osteoartrose (M16, M17) em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'M16%' OR cid_principal LIKE 'M17%');
```

**Resultado:** `got=348 exp=348`
**TTA:** 4290ms

---

### ✅ Q05 — Epidemiológica Simples

**Q:** Número de óbitos em internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND morte = 1;
```

**Resultado:** `got=889 exp=889`
**TTA:** 4846ms

---

### ✅ Q06 — Epidemiológica Simples

**Q:** Total de internações por traumatismos (S00-T98) em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND icd_group = 'S00-T98';
```

**Resultado:** `got=31703 exp=31703`
**TTA:** 3998ms

---

### ✅ Q07 — Epidemiológica Simples

**Q:** Total de internações por doenças osteomusculares (M00-M99) em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND icd_group = 'M00-M99';
```

**Resultado:** `got=3955 exp=3955`
**TTA:** 4001ms

---

### ✅ Q08 — Epidemiológica Simples

**Q:** Número de mulheres internadas por causa ortopédica em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND sexo_paciente = 'F'
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=13285 exp=13285`
**TTA:** 4939ms

---

### ✅ Q09 — Epidemiológica Simples

**Q:** Número de homens internados por causa ortopédica em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND sexo_paciente = 'M'
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=22373 exp=22373`
**TTA:** 4930ms

---

### ✅ Q10 — Epidemiológica Simples

**Q:** Número de internações ortopédicas de idosos (60 anos ou mais) em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND idade_paciente >= 60;
```

**Resultado:** `got=12432 exp=12432`
**TTA:** 4982ms

---

### ✅ Q11 — Epidemiológica Simples

**Q:** Número de municípios distintos com internação ortopédica registrada em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT cod_munic_estabelecimento) AS total_municipios
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=64 exp=64`
**TTA:** 4739ms

---

### ✅ Q12 — Epidemiológica Simples

**Q:** Número de estabelecimentos distintos com internação ortopédica em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT cnes_estabelecimento) AS total_estabelecimentos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=140 exp=140`
**TTA:** 4742ms

---

### ✅ Q13 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com permanência superior a 7 dias em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND dias_perm > 7;
```

**Resultado:** `got=10614 exp=10614`
**TTA:** 4912ms

---

### ✅ Q14 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com uso de UTI em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes_com_uti
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND uti_int_to > 0;
```

**Resultado:** `got=1 exp=1`
**TTA:** 5127ms

---

### ✅ Q15 — Epidemiológica Simples

**Q:** Número de internações por fratura de quadril (S72) em pacientes com 70 anos ou mais em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
  AND idade_paciente >= 70;
```

**Resultado:** `got=5480 exp=5480`
**TTA:** 4280ms

---

### ✅ Q16 — Epidemiológica Complexa

**Q:** Top 10 CIDs ortopédicos por volume de internação em RJ em 2023

**SQL gerado:**
```sql
SELECT cid_principal,
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cid_principal
ORDER BY total_internacoes DESC
LIMIT 10;
```

**Resultado:** `10 linhas corretas (flexível)`
**TTA:** 5489ms

---

### ✅ Q17 — Epidemiológica Complexa

**Q:** Top 5 municípios de RJ com maior número de internações ortopédicas em 2023

**SQL gerado:**
```sql
SELECT cod_munic_estabelecimento,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cod_munic_estabelecimento
ORDER BY internacoes DESC
LIMIT 5;
```

**Resultado:** `5 linhas corretas (flexível)`
**TTA:** 5700ms

---

### ❌ Q18 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por faixa etária (0-17, 18-59, 60+) em RJ em 2023

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN idade_paciente BETWEEN 0 AND 17 THEN '0-17'
    WHEN idade_paciente BETWEEN 18 AND 59 THEN '18-59'
    ELSE '60+'
  END AS faixa_etaria,
  COUNT(DISTINCT n_aih) AS total_internacoes,
  ROUND(100.0 * COUNT(DISTINCT n_aih) / SUM(COUNT(DISTINCT n_aih)) OVER (), 1) AS pct
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY faixa_etaria
ORDER BY total_internacoes DESC;
```

**Resultado:** `Linha não encontrada: {'faixa_etaria': '0-17', 'total': 3554}`
**TTA:** 9105ms

---

### ❌ Q19 — Epidemiológica Complexa

**Q:** Taxa de mortalidade hospitalar por CID ortopédico (top 5) em RJ em 2023

**SQL gerado:**
```sql
SELECT 
  cid_principal,
  COUNT(DISTINCT CASE WHEN morte = 1 THEN n_aih END) AS total_obitos,
  COUNT(DISTINCT n_aih) AS total_internacoes,
  ROUND(100.0 * COUNT(DISTINCT CASE WHEN morte = 1 THEN n_aih END) / COUNT(DISTINCT n_aih), 1) AS taxa_mortalidade
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY 
  cid_principal
ORDER BY 
  taxa_mortalidade DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cid_principal': 'S352', 'total_obitos': 1, 'total_internacoes': 1, 'taxa_mortalidade': 100.0} exp={'cid_principal': 'S062', 'total_internacoes': 40, 'total_obitos': 16.0, 'taxa_mortalidade_pct': 40.0}`
**TTA:** 11647ms

---

### ✅ Q20 — Epidemiológica Complexa

**Q:** Top 10 CIDs ambulatoriais ortopédicos por volume de produção em RJ em 2023

**SQL gerado:**
```sql
SELECT cid_principal,
  COUNT(*) AS total_producao
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cid_principal
ORDER BY total_producao DESC
LIMIT 10;
```

**Resultado:** `10 linhas corretas (flexível)`
**TTA:** 8336ms

---

### ✅ Q21 — Epidemiológica Complexa

**Q:** Proporção de internações ortopédicas por sexo em RJ em 2023

**SQL gerado:**
```sql
SELECT sexo_paciente,
  COUNT(DISTINCT n_aih) AS total_internacoes,
  ROUND(100.0 * COUNT(DISTINCT n_aih) / SUM(COUNT(DISTINCT n_aih)) OVER (), 1) AS pct
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY sexo_paciente
ORDER BY total_internacoes DESC;
```

**Resultado:** `2 linhas corretas, ordem livre (flexível)`
**TTA:** 6864ms

---

### ❌ Q22 — Epidemiológica Complexa

**Q:** Número de internações por fratura de fêmur (S72) em idosos (60+) comparado ao total em RJ em 2023

**SQL gerado:**
```sql
SELECT 
  COUNT(DISTINCT n_aih) AS total_internacoes_femur,
  (SELECT COUNT(DISTINCT n_aih) 
   FROM processed 
   WHERE sistema = 'SIH' 
     AND uf_origem = 'RJ' 
     AND ano_cmpt = 2023 
     AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')) AS total_internacoes_rj
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND cid_principal LIKE 'S72%'
  AND idade_paciente >= 60;
```

**Resultado:** `Linha 1: got={'total_internacoes_femur': 7134, 'total_internacoes_rj': 35658} exp={'total_s72': 10668, 's72_idosos': 7134.0, 'pct_idosos': 66.9}`
**TTA:** 9586ms

---

### ❌ Q23 — Epidemiológica Complexa

**Q:** Top 5 CIDs ortopédicos com maior permanência média hospitalar em RJ em 2023

**SQL gerado:**
```sql
SELECT cid_principal,
  COUNT(DISTINCT n_aih) AS total_internacoes,
  ROUND(AVG(dias_perm), 1) AS permanencia_media_dias
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cid_principal
ORDER BY permanencia_media_dias DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cid_principal': 'S158', 'total_internacoes': 1, 'permanencia_media_dias': 67.0} exp={'cid_principal': 'T845', 'total_internacoes': 76, 'permanencia_media_dias': 19.4}`
**TTA:** 9387ms

---

### ✅ Q24 — Epidemiológica Complexa

**Q:** Top 5 estabelecimentos com maior volume de internação ortopédica em RJ em 2023

**SQL gerado:**
```sql
SELECT cnes_estabelecimento,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cnes_estabelecimento
ORDER BY internacoes DESC
LIMIT 5;
```

**Resultado:** `5 linhas corretas (flexível)`
**TTA:** 5658ms

---

### ✅ Q25 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por raça/cor do paciente em RJ em 2023

**SQL gerado:**
```sql
SELECT raca_cor_paciente,
  COUNT(DISTINCT n_aih) AS total_internacoes,
  ROUND(100.0 * COUNT(DISTINCT n_aih) / SUM(COUNT(DISTINCT n_aih)) OVER (), 1) AS pct
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY raca_cor_paciente
ORDER BY total_internacoes DESC;
```

**Resultado:** `5 linhas corretas, ordem livre (flexível)`
**TTA:** 7028ms

---

### ❌ Q26 — Epidemiológica Complexa

**Q:** CIDs ortopédicos com maior número de dias totais de internação em RJ em 2023

**SQL gerado:**
```sql
SELECT cid_principal,
  SUM(dias_perm) AS total_dias_internacao
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cid_principal
ORDER BY total_dias_internacao DESC
LIMIT 5;
```

**Resultado:** `Número de linhas: got=5 exp=10`
**TTA:** 8595ms

---

### ✅ Q27 — Epidemiológica Complexa

**Q:** Top 5 procedimentos ambulatoriais ortopédicos mais realizados em RJ em 2023

**SQL gerado:**
```sql
SELECT cod_procedimento,
  COUNT(*) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cod_procedimento
ORDER BY total_procedimentos DESC
LIMIT 5;
```

**Resultado:** `5 linhas corretas (flexível)`
**TTA:** 5401ms

---

### ❌ Q28 — Epidemiológica Complexa

**Q:** Proporção de internações ortopédicas com paciente de outro município em RJ em 2023

**SQL gerado:**
```sql
SELECT 
  ROUND(100.0 * COUNT(CASE WHEN cod_munic_residencia != cod_munic_estabelecimento THEN 1 END) / COUNT(*), 1) AS proporcao
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `Linha 1: got={'proporcao': 54.9} exp={'total_internacoes': 35658, 'deslocamento_intermunicipal': 19572.0, 'pct_deslocamento': 54.9}`
**TTA:** 5827ms

---

### ❌ Q29 — Epidemiológica Complexa

**Q:** Comparativo de volume mensal SIA vs SIH ortopédico em RJ em 2023

**SQL gerado:**
```sql
SELECT
  mes_cmpt,
  sistema,
  COUNT(*) AS volume_mensal
FROM processed
WHERE uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY mes_cmpt, sistema
ORDER BY mes_cmpt, sistema;
```

**Resultado:** `Linha 2: got={'mes_cmpt': 1, 'sistema': 'SIH', 'volume_mensal': 2582} exp={'mes_cmpt': 1, 'sistema': 'SIH', 'total': 2581}`
**TTA:** 5091ms

---

### ❌ Q30 — Epidemiológica Complexa

**Q:** Idade média dos pacientes internados por fratura de fêmur (S72) em RJ em 2023

**SQL gerado:**
```sql
SELECT ROUND(AVG(idade_paciente), 1) AS idade_media
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%';
```

**Resultado:** `Linha 1: got={'idade_media': 63.0} exp={'idade_media': 63.0, 'idade_minima': 0.0, 'idade_maxima': 99.0}`
**TTA:** 3948ms

---

### ✅ Q31 — Financeira

**Q:** Custo total de todas as internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=92092851.42 exp=92092851.42`
**TTA:** 4936ms

---

### ✅ Q32 — Financeira

**Q:** Custo médio por internação ortopédica em RJ em 2023

**SQL gerado:**
```sql
SELECT ROUND(AVG(COALESCE(custo_total, 0)), 2) AS custo_medio_por_internacao
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=2581.87 exp=2581.87`
**TTA:** 5136ms

---

### ✅ Q33 — Financeira

**Q:** Custo total de internações por fratura de fêmur (S72) em RJ em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%';
```

**Resultado:** `got=30987118.46 exp=30987118.46`
**TTA:** 4243ms

---

### ✅ Q34 — Financeira

**Q:** Top 5 CIDs ortopédicos com maior custo total de internação em RJ em 2023

**SQL gerado:**
```sql
SELECT cid_principal,
  COUNT(DISTINCT n_aih) AS total_internacoes,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cid_principal
ORDER BY custo_total DESC
LIMIT 5;
```

**Resultado:** `5 linhas corretas (flexível)`
**TTA:** 6395ms

---

### ❌ Q35 — Financeira

**Q:** Custo total de internações ortopédicas com uso de OPME (val_ortp > 0) em RJ em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND val_ortp > 0;
```

**Resultado:** `Linha 1: got={'custo_total': None} exp={'internacoes_com_opme': 0, 'custo_total_opme': None}`
**TTA:** 5287ms

---

### ✅ Q36 — Financeira

**Q:** Valor total de honorários profissionais (val_sp) em internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(val_sp, 0)), 2) AS valor_total_honorarios
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=17470900.35 exp=17470900.35`
**TTA:** 4980ms

---

### ❌ Q37 — Financeira

**Q:** Custo médio por dia de internação ortopédica em RJ em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)) / SUM(COALESCE(qt_diarias, 0)), 2) AS custo_medio_por_dia
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=396.63 exp=370.15`
**TTA:** 5692ms

---

### ❌ Q38 — Financeira

**Q:** Top 5 estabelecimentos com maior custo total de internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT cnes_estabelecimento,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cnes_estabelecimento
ORDER BY custo_total DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cnes_estabelecimento': '2290227', 'custo_total': 10961414.37} exp={'cnes_estabelecimento': '2290227', 'internacoes': 5986, 'custo_total': 10961414.37}`
**TTA:** 6112ms

---

### ✅ Q39 — Financeira

**Q:** Custo total de procedimentos ambulatoriais ortopédicos em RJ em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=7729430.04 exp=7729430.04`
**TTA:** 4964ms

---

### ❌ Q40 — Financeira

**Q:** Proporção custo serviço hospitalar vs honorários em internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT 
  ROUND(100.0 * SUM(COALESCE(val_sh, 0)) / SUM(COALESCE(val_sp, 0)), 2) AS pct_val_sh_vs_val_sp
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `Linha 1: got={'pct_val_sh_vs_val_sp': 418.64} exp={'custo_servico_hospitalar': 73139419.49, 'honorarios_profissionais': 17470900.35, 'pct_hospitalar': 80.7}`
**TTA:** 6035ms

---

### ❌ Q41 — Temporal/Comparativa

**Q:** Distribuição mensal de internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT
  mes_cmpt,
  COUNT(DISTINCT n_aih) AS total_internacoes,
  ROUND(100.0 * COUNT(DISTINCT n_aih) / SUM(COUNT(DISTINCT n_aih)) OVER (), 1) AS pct
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'total_internacoes': 2581, 'pct': 7.2} exp={'mes_cmpt': 1, 'total_internacoes': 2581}`
**TTA:** 6894ms

---

### ❌ Q42 — Temporal/Comparativa

**Q:** Distribuição mensal de procedimentos ambulatoriais ortopédicos em RJ em 2023

**SQL gerado:**
```sql
SELECT
  mes_cmpt,
  COUNT(*) AS total_procedimentos,
  ROUND(100.0 * COUNT(*) / SUM(COUNT(*)) OVER (), 1) AS pct
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'total_procedimentos': 2678, 'pct': 7.9} exp={'mes_cmpt': 1, 'procedimentos': 2678}`
**TTA:** 6172ms

---

### ✅ Q43 — Temporal/Comparativa

**Q:** Mês com maior número de internações por fratura de fêmur (S72) em RJ em 2023

**SQL gerado:**
```sql
SELECT mes_cmpt,
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
GROUP BY mes_cmpt
ORDER BY total_internacoes DESC
LIMIT 1;
```

**Resultado:** `got=7 exp=7`
**TTA:** 4823ms

---

### ✅ Q44 — Temporal/Comparativa

**Q:** Evolução mensal do custo total de internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT
  mes_cmpt,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Resultado:** `12 linhas corretas (exato)`
**TTA:** 5735ms

---

### ❌ Q45 — Temporal/Comparativa

**Q:** Volume mensal de internações por trauma (S00-T98) vs doenças musculoesqueléticas (M00-M99) em RJ em 2023

**SQL gerado:**
```sql
SELECT
  mes_cmpt,
  COUNT(DISTINCT CASE WHEN icd_group = 'S00-T98' THEN n_aih END) AS trauma_internacoes,
  COUNT(DISTINCT CASE WHEN icd_group = 'M00-M99' THEN n_aih END) AS musculoesqueletico_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Resultado:** `Número de linhas: got=12 exp=24`
**TTA:** 6383ms

---

### ✅ Q46 — Temporal/Comparativa

**Q:** Evolução mensal de óbitos em internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT
  mes_cmpt,
  COUNT(DISTINCT CASE WHEN morte = 1 THEN n_aih END) AS total_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Resultado:** `12 linhas corretas (exato)`
**TTA:** 8813ms

---

### ✅ Q47 — Temporal/Comparativa

**Q:** Permanência média por trimestre nas internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN mes_cmpt BETWEEN 1 AND 3 THEN 'Q1'
    WHEN mes_cmpt BETWEEN 4 AND 6 THEN 'Q2'
    WHEN mes_cmpt BETWEEN 7 AND 9 THEN 'Q3'
    ELSE 'Q4'
  END AS trimestre,
  ROUND(AVG(dias_perm), 1) AS permanencia_media_dias
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY trimestre
ORDER BY trimestre;
```

**Resultado:** `4 linhas corretas, ordem livre (exato)`
**TTA:** 7813ms

---

### ✅ Q48 — Temporal/Comparativa

**Q:** Mês com maior custo médio por internação ortopédica em RJ em 2023

**SQL gerado:**
```sql
SELECT
  mes_cmpt,
  ROUND(AVG(custo_total), 2) AS custo_medio
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY mes_cmpt
ORDER BY custo_medio DESC
LIMIT 1;
```

**Resultado:** `got=12 exp=12`
**TTA:** 8812ms

---

### ✅ Q49 — Temporal/Comparativa

**Q:** Sazonalidade mensal de fraturas de fêmur (S72) em RJ em 2023

**SQL gerado:**
```sql
SELECT 
  mes_cmpt,
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
GROUP BY 
  mes_cmpt
ORDER BY 
  mes_cmpt;
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 4982ms

---

### ✅ Q50 — Temporal/Comparativa

**Q:** Volume mensal de internações ortopédicas de idosos (60 anos ou mais) em RJ em 2023

**SQL gerado:**
```sql
SELECT
  mes_cmpt,
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND idade_paciente >= 60
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 5823ms

---

### ✅ Q51 — Epidemiológica Simples

**Q:** Número de internações por fratura de fêmur (S72) em mulheres em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
  AND sexo_paciente = 'F';
```

**Resultado:** `got=5697 exp=5697`
**TTA:** 4268ms

---

### ✅ Q52 — Epidemiológica Simples

**Q:** Número de internações por fratura de fêmur (S72) em homens em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
  AND sexo_paciente = 'M';
```

**Resultado:** `got=4971 exp=4971`
**TTA:** 4253ms

---

### ✅ Q53 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com permanência de 1 dia ou menos em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND dias_perm <= 1;
```

**Resultado:** `got=7210 exp=7210`
**TTA:** 4955ms

---

### ✅ Q54 — Epidemiológica Simples

**Q:** Número de procedimentos ambulatoriais ortopédicos realizados em mulheres em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(*) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND sexo_paciente = 'F'
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=15625 exp=15625`
**TTA:** 4747ms

---

### ✅ Q55 — Epidemiológica Simples

**Q:** Número de procedimentos ambulatoriais ortopédicos realizados por idosos (60 anos ou mais) em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(*) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND idade_paciente >= 60;
```

**Resultado:** `got=17399 exp=17399`
**TTA:** 4805ms

---

### ✅ Q56 — Epidemiológica Simples

**Q:** Número de internações por fratura de tíbia e perônio (S82) em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S82%';
```

**Resultado:** `got=4271 exp=4271`
**TTA:** 3918ms

---

### ✅ Q57 — Epidemiológica Simples

**Q:** Número de internações por fratura de antebraço (S52) em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S52%';
```

**Resultado:** `got=2610 exp=2610`
**TTA:** 3919ms

---

### ✅ Q58 — Epidemiológica Simples

**Q:** Número de internações por fratura de ombro e úmero (S42) em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S42%';
```

**Resultado:** `got=1266 exp=1266`
**TTA:** 3928ms

---

### ✅ Q59 — Epidemiológica Simples

**Q:** Número de internações ortopédicas em crianças (menores de 18 anos) em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND idade_paciente < 18
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=3554 exp=3554`
**TTA:** 5036ms

---

### ✅ Q60 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com permanência entre 8 e 30 dias em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND dias_perm BETWEEN 8 AND 30;
```

**Resultado:** `got=9782 exp=9782`
**TTA:** 5108ms

---

### ✅ Q61 — Epidemiológica Simples

**Q:** Número de internações por artrose de quadril e joelho (M16 e M17) em idosos (60 anos ou mais) em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'M16%' OR cid_principal LIKE 'M17%')
  AND idade_paciente >= 60;
```

**Resultado:** `got=244 exp=244`
**TTA:** 4760ms

---

### ✅ Q62 — Epidemiológica Simples

**Q:** Número de internações por fratura de fêmur (S72) em mulheres com 70 anos ou mais em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
  AND sexo_paciente = 'F'
  AND idade_paciente >= 70;
```

**Resultado:** `got=4000 exp=4000`
**TTA:** 4695ms

---

### ✅ Q63 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com permanência superior a 30 dias em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND dias_perm > 30;
```

**Resultado:** `got=835 exp=835`
**TTA:** 4999ms

---

### ✅ Q64 — Epidemiológica Simples

**Q:** Número de procedimentos ambulatoriais ortopédicos realizados em homens em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(*) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND sexo_paciente = 'M'
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=18252 exp=18252`
**TTA:** 4742ms

---

### ✅ Q65 — Epidemiológica Simples

**Q:** Número de internações por traumatismos (grupo S00-T98) em idosos (60 anos ou mais) em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND icd_group = 'S00-T98'
  AND idade_paciente >= 60;
```

**Resultado:** `got=11281 exp=11281`
**TTA:** 4469ms

---

### ✅ Q66 — Epidemiológica Simples

**Q:** Número de internações por doenças osteomusculares (grupo M00-M99) em mulheres em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND icd_group = 'M00-M99'
  AND sexo_paciente = 'F';
```

**Resultado:** `got=1648 exp=1648`
**TTA:** 4428ms

---

### ✅ Q67 — Epidemiológica Simples

**Q:** Número de óbitos em internações por fratura de fêmur (S72) em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
  AND morte = 1;
```

**Resultado:** `got=345 exp=345`
**TTA:** 4187ms

---

### ❌ Q68 — Epidemiológica Simples

**Q:** Número de municípios distintos com registro de procedimentos ambulatoriais ortopédicos em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT cod_munic_residencia) AS total_municipios
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=105 exp=80`
**TTA:** 4768ms

---

### ✅ Q69 — Epidemiológica Complexa

**Q:** Top 5 municípios por volume de procedimentos ambulatoriais ortopédicos em RJ em 2023

**SQL gerado:**
```sql
SELECT cod_munic_estabelecimento,
  COUNT(*) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cod_munic_estabelecimento
ORDER BY total_procedimentos DESC
LIMIT 5;
```

**Resultado:** `5 linhas corretas (flexível)`
**TTA:** 5634ms

---

### ✅ Q70 — Epidemiológica Complexa

**Q:** Distribuição de procedimentos ambulatoriais ortopédicos por faixa etária (0-17, 18-59, 60+) em RJ em 2023

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN idade_paciente BETWEEN 0 AND 17 THEN '0-17'
    WHEN idade_paciente BETWEEN 18 AND 59 THEN '18-59'
    ELSE '60+'
  END AS faixa_etaria,
  COUNT(*) AS total,
  ROUND(100.0 * COUNT(*) / SUM(COUNT(*)) OVER (), 1) AS pct
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY faixa_etaria
ORDER BY total DESC;
```

**Resultado:** `3 linhas corretas, ordem livre (exato)`
**TTA:** 8218ms

---

### ✅ Q71 — Epidemiológica Complexa

**Q:** Top 5 diagnósticos com maior número de óbitos em internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT cid_principal,
  COUNT(DISTINCT n_aih) AS total_internacoes,
  SUM(morte) AS total_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cid_principal
ORDER BY total_obitos DESC
LIMIT 5;
```

**Resultado:** `5 linhas corretas (exato)`
**TTA:** 8943ms

---

### ✅ Q72 — Epidemiológica Complexa

**Q:** Permanência hospitalar média por sexo em internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT
  sexo_paciente,
  ROUND(AVG(dias_perm), 1) AS permanencia_media_dias
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY sexo_paciente
ORDER BY permanencia_media_dias DESC;
```

**Resultado:** `2 linhas corretas, ordem livre (flexível)`
**TTA:** 5641ms

---

### ✅ Q73 — Epidemiológica Complexa

**Q:** Top 10 estabelecimentos por volume de procedimentos ambulatoriais ortopédicos em RJ em 2023

**SQL gerado:**
```sql
SELECT cnes_estabelecimento,
  COUNT(*) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cnes_estabelecimento
ORDER BY total_procedimentos DESC
LIMIT 10;
```

**Resultado:** `10 linhas corretas (flexível)`
**TTA:** 5614ms

---

### ✅ Q74 — Epidemiológica Complexa

**Q:** Taxa de mortalidade por sexo em internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT 
  sexo_paciente,
  COUNT(DISTINCT n_aih) AS total_internacoes,
  SUM(CASE WHEN morte = 1 THEN 1 ELSE 0 END) AS total_obitos,
  ROUND(100.0 * SUM(CASE WHEN morte = 1 THEN 1 ELSE 0 END) / COUNT(DISTINCT n_aih), 1) AS taxa_mortalidade
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY sexo_paciente
ORDER BY total_internacoes DESC;
```

**Resultado:** `2 linhas corretas, ordem livre (exato)`
**TTA:** 8212ms

---

### ❌ Q75 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por grupo diagnóstico (M e S) e faixa etária em RJ em 2023

**SQL gerado:**
```sql
SELECT 
  icd_group,
  CASE 
    WHEN idade_paciente < 18 THEN '0-17'
    WHEN idade_paciente < 30 THEN '18-29'
    WHEN idade_paciente < 40 THEN '30-39'
    WHEN idade_paciente < 50 THEN '40-49'
    WHEN idade_paciente < 60 THEN '50-59'
    WHEN idade_paciente < 70 THEN '60-69'
    WHEN idade_paciente < 80 THEN '70-79'
    ELSE '80+'
  END AS faixa_etaria,
  COUNT(DISTINCT n_aih) AS total_internacoes,
  ROUND(100.0 * COUNT(DISTINCT n_aih) / SUM(COUNT(DISTINCT n_aih)) OVER (), 1) AS pct
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY icd_group, faixa_etaria
ORDER BY total_internacoes DESC;
```

**Resultado:** `Número de linhas: got=16 exp=6`
**TTA:** 15775ms

---

### ❌ Q76 — Epidemiológica Complexa

**Q:** Top 5 diagnósticos de traumatismo com maior permanência média em RJ em 2023 (mínimo 40 internações)

**SQL gerado:**
```sql
SELECT cid_principal,
  COUNT(DISTINCT n_aih) AS total_internacoes,
  ROUND(AVG(dias_perm), 1) AS permanencia_media_dias
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND icd_group = 'S00-T98'
GROUP BY cid_principal
HAVING COUNT(DISTINCT n_aih) >= 40
ORDER BY permanencia_media_dias DESC
LIMIT 5;
```

**Resultado:** `Linha 4: got={'cid_principal': 'T847', 'total_internacoes': 71, 'permanencia_media_dias': 12.3} exp={'cid_principal': 'S062', 'internacoes': 40, 'permanencia_media': 12.3}`
**TTA:** 9399ms

---

### ❌ Q77 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por faixa etária (0-17, 18-39, 40-59, 60-79, 80+) em RJ em 2023

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN idade_paciente BETWEEN 0 AND 17 THEN '0-17'
    WHEN idade_paciente BETWEEN 18 AND 39 THEN '18-39'
    WHEN idade_paciente BETWEEN 40 AND 59 THEN '40-59'
    WHEN idade_paciente BETWEEN 60 AND 79 THEN '60-79'
    ELSE '80+'
  END AS faixa_etaria,
  COUNT(DISTINCT n_aih) AS total_internacoes,
  ROUND(100.0 * COUNT(DISTINCT n_aih) / SUM(COUNT(DISTINCT n_aih)) OVER (), 1) AS pct
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY faixa_etaria
ORDER BY total_internacoes DESC;
```

**Resultado:** `Linha não encontrada: {'faixa_etaria': '0-17', 'total': 3554}`
**TTA:** 10861ms

---

### ✅ Q78 — Epidemiológica Complexa

**Q:** Top 5 estabelecimentos com maior volume de internações por fratura de fêmur (S72) em RJ em 2023

**SQL gerado:**
```sql
SELECT cnes_estabelecimento,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
GROUP BY cnes_estabelecimento
ORDER BY internacoes DESC
LIMIT 5;
```

**Resultado:** `5 linhas corretas (exato)`
**TTA:** 8001ms

---

### ❌ Q79 — Epidemiológica Complexa

**Q:** Permanência hospitalar média por faixa etária em internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN idade_paciente < 18 THEN '0-17 anos'
    WHEN idade_paciente < 30 THEN '18-29 anos'
    WHEN idade_paciente < 40 THEN '30-39 anos'
    WHEN idade_paciente < 50 THEN '40-49 anos'
    WHEN idade_paciente < 60 THEN '50-59 anos'
    WHEN idade_paciente < 70 THEN '60-69 anos'
    ELSE '70 anos ou mais'
  END AS faixa_etaria,
  ROUND(AVG(dias_perm), 1) AS permanencia_media_dias
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY faixa_etaria
ORDER BY faixa_etaria;
```

**Resultado:** `Número de linhas: got=7 exp=3`
**TTA:** 10559ms

---

### ✅ Q80 — Epidemiológica Complexa

**Q:** Top 5 diagnósticos de doença osteomuscular (M00-M99) por número de internações em RJ em 2023

**SQL gerado:**
```sql
SELECT cid_principal,
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND icd_group = 'M00-M99'
GROUP BY cid_principal
ORDER BY total_internacoes DESC
LIMIT 5;
```

**Resultado:** `5 linhas corretas (flexível)`
**TTA:** 7996ms

---

### ✅ Q81 — Epidemiológica Complexa

**Q:** Número de internações ortopédicas com permanência entre 3 e 7 dias em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND dias_perm BETWEEN 3 AND 7;
```

**Resultado:** `got=12572 exp=12572`
**TTA:** 5077ms

---

### ✅ Q82 — Epidemiológica Complexa

**Q:** Top 5 diagnósticos ambulatoriais ortopédicos em idosos (60 anos ou mais) em RJ em 2023

**SQL gerado:**
```sql
SELECT cid_principal,
  COUNT(*) AS total
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND idade_paciente >= 60
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cid_principal
ORDER BY total DESC
LIMIT 5;
```

**Resultado:** `5 linhas corretas (exato)`
**TTA:** 5404ms

---

### ❌ Q83 — Epidemiológica Complexa

**Q:** Média de internações ortopédicas por estabelecimento em RJ em 2023

**SQL gerado:**
```sql
SELECT cnes_estabelecimento,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cnes_estabelecimento
ORDER BY internacoes DESC;
```

**Resultado:** `got=2290227 exp=254.7`
**TTA:** 5487ms

---

### ❌ Q84 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por faixa de permanência (rótulos: '1-3 dias', '4-7 dias', '8-14 dias', '15+ dias') em RJ em 2023

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN dias_perm BETWEEN 1 AND 3 THEN '1-3 dias'
    WHEN dias_perm BETWEEN 4 AND 7 THEN '4-7 dias'
    WHEN dias_perm BETWEEN 8 AND 14 THEN '8-14 dias'
    ELSE '15+ dias'
  END AS faixa_permanencia,
  COUNT(DISTINCT n_aih) AS total_internacoes,
  ROUND(100.0 * COUNT(DISTINCT n_aih) / SUM(COUNT(DISTINCT n_aih)) OVER (), 1) AS pct
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY faixa_permanencia
ORDER BY total_internacoes DESC;
```

**Resultado:** `Linha não encontrada: {'faixa_permanencia': '1-3 dias', 'total': 13855}`
**TTA:** 9813ms

---

### ✅ Q85 — Financeira

**Q:** Custo total de internações ortopédicas de idosos (60 anos ou mais) em RJ em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND idade_paciente >= 60;
```

**Resultado:** `got=37226129.45 exp=37226129.45`
**TTA:** 8465ms

---

### ✅ Q86 — Financeira

**Q:** Custo médio por internação por fratura de fêmur (S72) em RJ em 2023

**SQL gerado:**
```sql
SELECT ROUND(AVG(COALESCE(custo_total, 0)), 2) AS custo_medio_por_internacao
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%';
```

**Resultado:** `got=2904.68 exp=2904.68`
**TTA:** 4447ms

---

### ✅ Q87 — Financeira

**Q:** Custo total de internações ortopédicas com passagem por UTI em RJ em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND uti_int_to > 0;
```

**Resultado:** `got=18598.76 exp=18598.76`
**TTA:** 5350ms

---

### ❌ Q88 — Financeira

**Q:** Custo médio por internação ortopédica por sexo do paciente em RJ em 2023

**SQL gerado:**
```sql
SELECT 
  sexo_paciente,
  COUNT(DISTINCT n_aih) AS total_internacoes,
  ROUND(SUM(COALESCE(custo_total, 0)) / COUNT(DISTINCT n_aih), 2) AS custo_medio_por_internacao
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY 
  sexo_paciente
ORDER BY 
  total_internacoes DESC;
```

**Resultado:** `Linha não encontrada: {'sexo_paciente': 'F', 'custo_medio': 2677.23}`
**TTA:** 7319ms

---

### ✅ Q89 — Financeira

**Q:** Custo total de internações ortopédicas em crianças (menores de 18 anos) em RJ em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND idade_paciente < 18
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=7918775.81 exp=7918775.81`
**TTA:** 8439ms

---

### ✅ Q90 — Financeira

**Q:** Custo médio por internação ortopédica com permanência superior a 7 dias em RJ em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)) / COUNT(DISTINCT n_aih), 2) AS custo_medio_por_internacao
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND dias_perm > 7;
```

**Resultado:** `got=3948.21 exp=3944.5`
**TTA:** 5841ms

---

### ✅ Q91 — Financeira

**Q:** Top 3 municípios com maior custo total em internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT cod_munic_estabelecimento,
  COUNT(DISTINCT n_aih) AS total_internacoes,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cod_munic_estabelecimento
ORDER BY custo_total DESC
LIMIT 3;
```

**Resultado:** `3 linhas corretas (exato)`
**TTA:** 6779ms

---

### ✅ Q92 — Financeira

**Q:** Valor total de serviço hospitalar em internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(val_sh, 0)), 2) AS valor_total_servico_hospitalar
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=73139419.49 exp=73139419.49`
**TTA:** 5052ms

---

### ❌ Q93 — Financeira

**Q:** Custo médio por internação ortopédica por faixa etária em RJ em 2023

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN idade_paciente < 18 THEN '0-17'
    WHEN idade_paciente < 30 THEN '18-29'
    WHEN idade_paciente < 40 THEN '30-39'
    WHEN idade_paciente < 50 THEN '40-49'
    WHEN idade_paciente < 60 THEN '50-59'
    WHEN idade_paciente < 70 THEN '60-69'
    WHEN idade_paciente < 80 THEN '70-79'
    ELSE '80+'
  END AS faixa_etaria,
  COUNT(DISTINCT n_aih) AS total_internacoes,
  ROUND(SUM(COALESCE(custo_total, 0)) / COUNT(DISTINCT n_aih), 2) AS custo_medio_por_internacao
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY faixa_etaria
ORDER BY faixa_etaria;
```

**Resultado:** `Número de linhas: got=8 exp=3`
**TTA:** 12316ms

---

### ✅ Q94 — Financeira

**Q:** Valor total de UTI em internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(val_uti, 0)), 2) AS valor_total_uti
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=16618123.58 exp=16618123.58`
**TTA:** 8094ms

---

### ✅ Q95 — Financeira

**Q:** Custo total de internações por doenças osteomusculares (M00-M99) em RJ em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND icd_group = 'M00-M99';
```

**Resultado:** `got=13641307.23 exp=13641307.23`
**TTA:** 4378ms

---

### ✅ Q96 — Financeira

**Q:** Custo total de internações por traumatismos (S00-T98) em RJ em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND icd_group = 'S00-T98';
```

**Resultado:** `got=78451544.19 exp=78451544.19`
**TTA:** 4365ms

---

### ✅ Q97 — Financeira

**Q:** Top 5 diagnósticos com maior custo médio por internação ortopédica em RJ em 2023 (mínimo 40 internações)

**SQL gerado:**
```sql
SELECT 
  cid_principal,
  COUNT(DISTINCT n_aih) AS total_internacoes,
  ROUND(AVG(custo_total), 2) AS custo_medio
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY 
  cid_principal
HAVING 
  COUNT(DISTINCT n_aih) >= 40
ORDER BY 
  custo_medio DESC
LIMIT 5;
```

**Resultado:** `5 linhas corretas (flexível)`
**TTA:** 7298ms

---

### ✅ Q98 — Financeira

**Q:** Custo médio por procedimento ambulatorial ortopédico em RJ em 2023

**SQL gerado:**
```sql
SELECT ROUND(AVG(COALESCE(custo_total, 0)), 2) AS custo_medio_procedimento
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=228.16 exp=228.16`
**TTA:** 8180ms

---

### ❌ Q99 — Financeira

**Q:** Top 5 procedimentos ambulatoriais com maior custo total em RJ em 2023

**SQL gerado:**
```sql
SELECT cod_procedimento,
  COUNT(*) AS total_procedimentos,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
GROUP BY cod_procedimento
ORDER BY custo_total DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cod_procedimento': '0702100048', 'total_procedimentos': 2713, 'custo_total': 8007574.48} exp={'cod_procedimento': '0701010029', 'custo_total': 2349402.8}`
**TTA:** 5137ms

---

### ✅ Q100 — Financeira

**Q:** Custo total de internações ortopédicas de idosos (60+) com permanência superior a 7 dias em RJ em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND idade_paciente >= 60
  AND dias_perm > 7;
```

**Resultado:** `got=19953660.54 exp=19953660.54`
**TTA:** 5694ms

---

### ✅ Q101 — Temporal/Comparativa

**Q:** Mês com maior número de óbitos em internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT mes_cmpt,
  COUNT(DISTINCT n_aih) AS total_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND morte = 1
GROUP BY mes_cmpt
ORDER BY total_obitos DESC
LIMIT 1;
```

**Resultado:** `1 linhas corretas (exato)`
**TTA:** 5812ms

---

### ✅ Q102 — Temporal/Comparativa

**Q:** Volume mensal de internações por fratura de fêmur (S72) em idosos (60 anos ou mais) em RJ em 2023

**SQL gerado:**
```sql
SELECT
  mes_cmpt,
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
  AND idade_paciente >= 60
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 5077ms

---

### ✅ Q103 — Temporal/Comparativa

**Q:** Custo mensal de internações ortopédicas de idosos (60 anos ou mais) em RJ em 2023

**SQL gerado:**
```sql
SELECT
  mes_cmpt,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND idade_paciente >= 60
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Resultado:** `12 linhas corretas (exato)`
**TTA:** 6131ms

---

### ✅ Q104 — Temporal/Comparativa

**Q:** Mês com maior custo total de internações por fratura de fêmur (S72) em RJ em 2023

**SQL gerado:**
```sql
SELECT mes_cmpt,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
GROUP BY mes_cmpt
ORDER BY custo_total DESC
LIMIT 1;
```

**Resultado:** `1 linhas corretas (exato)`
**TTA:** 5099ms

---

### ✅ Q105 — Temporal/Comparativa

**Q:** Volume mensal de internações por traumatismos (grupo S00-T98) em RJ em 2023

**SQL gerado:**
```sql
SELECT
  mes_cmpt,
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND icd_group = 'S00-T98'
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 4830ms

---

### ✅ Q106 — Temporal/Comparativa

**Q:** Volume mensal de procedimentos ambulatoriais ortopédicos por sexo em RJ em 2023

**SQL gerado:**
```sql
SELECT 
  mes_cmpt,
  sexo_paciente,
  COUNT(*) AS total_procedimentos
FROM 
  processed
WHERE 
  sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY 
  mes_cmpt, sexo_paciente
ORDER BY 
  mes_cmpt, sexo_paciente;
```

**Resultado:** `24 linhas corretas (flexível)`
**TTA:** 6002ms

---

### ✅ Q107 — Temporal/Comparativa

**Q:** Comparativo trimestral de internações ortopédicas por grupo diagnóstico em RJ em 2023

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN mes_cmpt BETWEEN 1 AND 3 THEN 'Q1'
    WHEN mes_cmpt BETWEEN 4 AND 6 THEN 'Q2'
    WHEN mes_cmpt BETWEEN 7 AND 9 THEN 'Q3'
    ELSE 'Q4'
  END AS trimestre,
  icd_group,
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY trimestre, icd_group
ORDER BY trimestre, icd_group;
```

**Resultado:** `8 linhas corretas, ordem livre (flexível)`
**TTA:** 8227ms

---

### ✅ Q108 — Temporal/Comparativa

**Q:** Mês com maior permanência hospitalar média em internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT mes_cmpt,
  ROUND(AVG(dias_perm), 1) AS permanencia_media_dias
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY mes_cmpt
ORDER BY permanencia_media_dias DESC
LIMIT 1;
```

**Resultado:** `1 linhas corretas (flexível)`
**TTA:** 8823ms

---

### ❌ Q109 — Temporal/Comparativa

**Q:** Evolução mensal da taxa de mortalidade em internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT
  mes_cmpt,
  ROUND(100.0 * SUM(CASE WHEN morte = 1 THEN 1 ELSE 0 END) / COUNT(DISTINCT n_aih), 2) AS taxa_mortalidade
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'taxa_mortalidade': 2.83} exp={'mes_cmpt': 1, 'internacoes': 2581, 'obitos': 73.0, 'taxa_mortalidade': 2.83}`
**TTA:** 6588ms

---

### ✅ Q110 — Temporal/Comparativa

**Q:** Trimestre com maior volume de internações por fratura de fêmur (S72) em RJ em 2023

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN mes_cmpt BETWEEN 1 AND 3 THEN 'Q1'
    WHEN mes_cmpt BETWEEN 4 AND 6 THEN 'Q2'
    WHEN mes_cmpt BETWEEN 7 AND 9 THEN 'Q3'
    ELSE 'Q4'
  END AS trimestre,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
GROUP BY trimestre
ORDER BY internacoes DESC
LIMIT 1;
```

**Resultado:** `1 linhas corretas (flexível)`
**TTA:** 7076ms

---

### ✅ Q111 — Temporal/Comparativa

**Q:** Comparativo semestral de internações ortopédicas (1S = jan-jun, 2S = jul-dez) em RJ em 2023

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN mes_cmpt BETWEEN 1 AND 6 THEN '1S'
    ELSE '2S'
  END AS semestre,
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY semestre
ORDER BY semestre;
```

**Resultado:** `2 linhas corretas, ordem livre (flexível)`
**TTA:** 9480ms

---

### ✅ Q112 — Temporal/Comparativa

**Q:** Custo médio mensal por internação por fratura de fêmur (S72) em RJ em 2023

**SQL gerado:**
```sql
SELECT 
  mes_cmpt,
  ROUND(AVG(custo_total), 2) AS custo_medio_mensal
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
GROUP BY 
  mes_cmpt
ORDER BY 
  mes_cmpt;
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 5255ms

---

### ✅ Q113 — Temporal/Comparativa

**Q:** Volume mensal de internações ortopédicas por sexo do paciente em RJ em 2023

**SQL gerado:**
```sql
SELECT
  mes_cmpt,
  sexo_paciente,
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY mes_cmpt, sexo_paciente
ORDER BY mes_cmpt, sexo_paciente;
```

**Resultado:** `24 linhas corretas (flexível)`
**TTA:** 5978ms

---

### ✅ Q114 — Temporal/Comparativa

**Q:** Mês com menor volume de procedimentos ambulatoriais ortopédicos em RJ em 2023

**SQL gerado:**
```sql
SELECT mes_cmpt,
  COUNT(*) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY mes_cmpt
ORDER BY total_procedimentos ASC
LIMIT 1;
```

**Resultado:** `1 linhas corretas (flexível)`
**TTA:** 5369ms

---

### ✅ Q115 — Temporal/Comparativa

**Q:** Volume trimestral de procedimentos ambulatoriais ortopédicos em RJ em 2023

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN mes_cmpt BETWEEN 1 AND 3 THEN 'Q1'
    WHEN mes_cmpt BETWEEN 4 AND 6 THEN 'Q2'
    WHEN mes_cmpt BETWEEN 7 AND 9 THEN 'Q3'
    ELSE 'Q4'
  END AS trimestre,
  COUNT(*) AS volume_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY trimestre
ORDER BY trimestre;
```

**Resultado:** `4 linhas corretas, ordem livre (flexível)`
**TTA:** 7481ms

---

### ✅ Q116 — Temporal/Comparativa

**Q:** Custo mensal de procedimentos ambulatoriais ortopédicos em RJ em 2023

**SQL gerado:**
```sql
SELECT 
  mes_cmpt,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM 
  processed
WHERE 
  sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY 
  mes_cmpt
ORDER BY 
  mes_cmpt;
```

**Resultado:** `12 linhas corretas (exato)`
**TTA:** 9169ms

---

### ✅ Q117 — Temporal/Comparativa

**Q:** Comparativo semestral do custo total de internações ortopédicas (1S = jan-jun, 2S = jul-dez) em RJ em 2023

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN mes_cmpt BETWEEN 1 AND 6 THEN '1S'
    ELSE '2S'
  END AS semestre,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY semestre
ORDER BY semestre;
```

**Resultado:** `2 linhas corretas, ordem livre (exato)`
**TTA:** 6787ms

---

### ✅ Q118 — Temporal/Comparativa

**Q:** Evolução mensal da permanência média em internações por fratura de fêmur (S72) em RJ em 2023

**SQL gerado:**
```sql
SELECT
  mes_cmpt,
  ROUND(AVG(dias_perm), 1) AS permanencia_media_dias
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 7929ms

---

## Failures Detail

- **Q18** (Epidemiológica Complexa): Linha não encontrada: {'faixa_etaria': '0-17', 'total': 3554}
  SQL: `SELECT
  CASE
    WHEN idade_paciente BETWEEN 0 AND 17 THEN '0-17'
    WHEN idade_paciente BETWEEN 18 AND 59 THEN '18-59...`
- **Q19** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'S352', 'total_obitos': 1, 'total_internacoes': 1, 'taxa_mortalidade': 100.0} exp={'cid_principal': 'S062', 'total_internacoes': 40, 'total_obitos': 16.0, 'taxa_mortalidade_pct': 40.0}
  SQL: `SELECT 
  cid_principal,
  COUNT(DISTINCT CASE WHEN morte = 1 THEN n_aih END) AS total_obitos,
  COUNT(DISTINCT n_aih) A...`
- **Q22** (Epidemiológica Complexa): Linha 1: got={'total_internacoes_femur': 7134, 'total_internacoes_rj': 35658} exp={'total_s72': 10668, 's72_idosos': 7134.0, 'pct_idosos': 66.9}
  SQL: `SELECT 
  COUNT(DISTINCT n_aih) AS total_internacoes_femur,
  (SELECT COUNT(DISTINCT n_aih) 
   FROM processed 
   WHERE...`
- **Q23** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'S158', 'total_internacoes': 1, 'permanencia_media_dias': 67.0} exp={'cid_principal': 'T845', 'total_internacoes': 76, 'permanencia_media_dias': 19.4}
  SQL: `SELECT cid_principal,
  COUNT(DISTINCT n_aih) AS total_internacoes,
  ROUND(AVG(dias_perm), 1) AS permanencia_media_dias...`
- **Q26** (Epidemiológica Complexa): Número de linhas: got=5 exp=10
  SQL: `SELECT cid_principal,
  SUM(dias_perm) AS total_dias_internacao
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = '...`
- **Q28** (Epidemiológica Complexa): Linha 1: got={'proporcao': 54.9} exp={'total_internacoes': 35658, 'deslocamento_intermunicipal': 19572.0, 'pct_deslocamento': 54.9}
  SQL: `SELECT 
  ROUND(100.0 * COUNT(CASE WHEN cod_munic_residencia != cod_munic_estabelecimento THEN 1 END) / COUNT(*), 1) AS ...`
- **Q29** (Epidemiológica Complexa): Linha 2: got={'mes_cmpt': 1, 'sistema': 'SIH', 'volume_mensal': 2582} exp={'mes_cmpt': 1, 'sistema': 'SIH', 'total': 2581}
  SQL: `SELECT
  mes_cmpt,
  sistema,
  COUNT(*) AS volume_mensal
FROM processed
WHERE uf_origem = 'RJ'
  AND ano_cmpt = 2023
  ...`
- **Q30** (Epidemiológica Complexa): Linha 1: got={'idade_media': 63.0} exp={'idade_media': 63.0, 'idade_minima': 0.0, 'idade_maxima': 99.0}
  SQL: `SELECT ROUND(AVG(idade_paciente), 1) AS idade_media
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND an...`
- **Q35** (Financeira): Linha 1: got={'custo_total': None} exp={'internacoes_com_opme': 0, 'custo_total_opme': None}
  SQL: `SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ...`
- **Q37** (Financeira): got=396.63 exp=370.15
  SQL: `SELECT ROUND(SUM(COALESCE(custo_total, 0)) / SUM(COALESCE(qt_diarias, 0)), 2) AS custo_medio_por_dia
FROM processed
WHER...`
- **Q38** (Financeira): Linha 1: got={'cnes_estabelecimento': '2290227', 'custo_total': 10961414.37} exp={'cnes_estabelecimento': '2290227', 'internacoes': 5986, 'custo_total': 10961414.37}
  SQL: `SELECT cnes_estabelecimento,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SI...`
- **Q40** (Financeira): Linha 1: got={'pct_val_sh_vs_val_sp': 418.64} exp={'custo_servico_hospitalar': 73139419.49, 'honorarios_profissionais': 17470900.35, 'pct_hospitalar': 80.7}
  SQL: `SELECT 
  ROUND(100.0 * SUM(COALESCE(val_sh, 0)) / SUM(COALESCE(val_sp, 0)), 2) AS pct_val_sh_vs_val_sp
FROM 
  processe...`
- **Q41** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'total_internacoes': 2581, 'pct': 7.2} exp={'mes_cmpt': 1, 'total_internacoes': 2581}
  SQL: `SELECT
  mes_cmpt,
  COUNT(DISTINCT n_aih) AS total_internacoes,
  ROUND(100.0 * COUNT(DISTINCT n_aih) / SUM(COUNT(DISTI...`
- **Q42** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'total_procedimentos': 2678, 'pct': 7.9} exp={'mes_cmpt': 1, 'procedimentos': 2678}
  SQL: `SELECT
  mes_cmpt,
  COUNT(*) AS total_procedimentos,
  ROUND(100.0 * COUNT(*) / SUM(COUNT(*)) OVER (), 1) AS pct
FROM p...`
- **Q45** (Temporal/Comparativa): Número de linhas: got=12 exp=24
  SQL: `SELECT
  mes_cmpt,
  COUNT(DISTINCT CASE WHEN icd_group = 'S00-T98' THEN n_aih END) AS trauma_internacoes,
  COUNT(DISTI...`
- **Q68** (Epidemiológica Simples): got=105 exp=80
  SQL: `SELECT COUNT(DISTINCT cod_munic_residencia) AS total_municipios
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = '...`
- **Q75** (Epidemiológica Complexa): Número de linhas: got=16 exp=6
  SQL: `SELECT 
  icd_group,
  CASE 
    WHEN idade_paciente < 18 THEN '0-17'
    WHEN idade_paciente < 30 THEN '18-29'
    WHEN...`
- **Q76** (Epidemiológica Complexa): Linha 4: got={'cid_principal': 'T847', 'total_internacoes': 71, 'permanencia_media_dias': 12.3} exp={'cid_principal': 'S062', 'internacoes': 40, 'permanencia_media': 12.3}
  SQL: `SELECT cid_principal,
  COUNT(DISTINCT n_aih) AS total_internacoes,
  ROUND(AVG(dias_perm), 1) AS permanencia_media_dias...`
- **Q77** (Epidemiológica Complexa): Linha não encontrada: {'faixa_etaria': '0-17', 'total': 3554}
  SQL: `SELECT
  CASE
    WHEN idade_paciente BETWEEN 0 AND 17 THEN '0-17'
    WHEN idade_paciente BETWEEN 18 AND 39 THEN '18-39...`
- **Q79** (Epidemiológica Complexa): Número de linhas: got=7 exp=3
  SQL: `SELECT
  CASE
    WHEN idade_paciente < 18 THEN '0-17 anos'
    WHEN idade_paciente < 30 THEN '18-29 anos'
    WHEN idad...`
- **Q83** (Epidemiológica Complexa): got=2290227 exp=254.7
  SQL: `SELECT cnes_estabelecimento,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem...`
- **Q84** (Epidemiológica Complexa): Linha não encontrada: {'faixa_permanencia': '1-3 dias', 'total': 13855}
  SQL: `SELECT
  CASE
    WHEN dias_perm BETWEEN 1 AND 3 THEN '1-3 dias'
    WHEN dias_perm BETWEEN 4 AND 7 THEN '4-7 dias'
    ...`
- **Q88** (Financeira): Linha não encontrada: {'sexo_paciente': 'F', 'custo_medio': 2677.23}
  SQL: `SELECT 
  sexo_paciente,
  COUNT(DISTINCT n_aih) AS total_internacoes,
  ROUND(SUM(COALESCE(custo_total, 0)) / COUNT(DIS...`
- **Q93** (Financeira): Número de linhas: got=8 exp=3
  SQL: `SELECT
  CASE
    WHEN idade_paciente < 18 THEN '0-17'
    WHEN idade_paciente < 30 THEN '18-29'
    WHEN idade_paciente...`
- **Q99** (Financeira): Linha 1: got={'cod_procedimento': '0702100048', 'total_procedimentos': 2713, 'custo_total': 8007574.48} exp={'cod_procedimento': '0701010029', 'custo_total': 2349402.8}
  SQL: `SELECT cod_procedimento,
  COUNT(*) AS total_procedimentos,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FRO...`
- **Q109** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'taxa_mortalidade': 2.83} exp={'mes_cmpt': 1, 'internacoes': 2581, 'obitos': 73.0, 'taxa_mortalidade': 2.83}
  SQL: `SELECT
  mes_cmpt,
  ROUND(100.0 * SUM(CASE WHEN morte = 1 THEN 1 ELSE 0 END) / COUNT(DISTINCT n_aih), 2) AS taxa_mortal...`

---
_Gerado por scripts/evaluate_benchmark.py — SUS Data RAG — USF/Mestrado_