# Benchmark Evaluation Report — SUS Data RAG

**Model:** ollama  
**Run date:** 2026-08-23 07:30  
**API:** http://127.0.0.1:8000  

## Summary

| Metric | Value |
|---|---|
| Total queries | 118 |
| Correct (EA numerator) | 77 |
| **Execution Accuracy (EA)** | **65.3%** |
| Wilson IC 95% | [56.3%, 73.2%] |
| Correct via flexible match (alias diferente) | 56 |
| API errors / timeouts | 12 |
| Mean Time-to-Answer | 5.9s |

> **Nota metodológica — Scoring:** A Execution Accuracy avalia se o SQL gerado
> retorna os mesmos **valores** que o gold-standard, independente do nome das colunas.
> Quando o LLM usa um alias diferente mas retorna os dados corretos (match posicional),
> a query é contada como correta e sinalizada com `~`. Esta decisão segue a definição
> de EA em [Lee et al., 2022] — comparação de conjuntos de resultado, não de SQL texto.

## EA by Category

| Category | Correct | Total | EA |
|---|---:|---:|---:|
| Epidemiológica Simples | 25 | 33 | 75.8% |
| Epidemiológica Complexa | 13 | 31 | 41.9% |
| Financeira | 18 | 26 | 69.2% |
| Temporal/Comparativa | 21 | 28 | 75.0% |

## Query Results

### ❌ Q01 — Epidemiológica Simples

**Q:** Total de internações ortopédicas (M00-M99 e S00-S99) em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T99');
```

**Resultado:** `got=3955 exp=35658`
**TTA:** 4995ms

---

### ✅ Q02 — Epidemiológica Simples

**Q:** Total de procedimentos ambulatoriais ortopédicos em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(*) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=33877 exp=33877`
**TTA:** 5956ms

---

### ✅ Q03 — Epidemiológica Simples

**Q:** Total de internações por fratura de fêmur (S72) em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%';
```

**Resultado:** `got=10668 exp=10668`
**TTA:** 4343ms

---

### ❌ Q04 — Epidemiológica Simples

**Q:** Total de internações por osteoartrose (M16, M17) em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M16' OR icd_group = 'M17');
```

**Resultado:** `got=0 exp=348`
**TTA:** 4745ms

---

### ⚠️ Q05 — Epidemiológica Simples

**Q:** Número de óbitos em internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND obito = 'S';
```

**Erro:** Falha DuckDB: Binder Error: Referenced column "obito" not found in FROM clause!
Candidate bindings: "morte", "gestao", "row_id", "cod_munic_estabelecimento", "sistema"

LINE 7:   AND obito = 'S';
              ^

---

### ✅ Q06 — Epidemiológica Simples

**Q:** Total de internações por traumatismos (S00-T98) em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND icd_group = 'S00-T98';
```

**Resultado:** `got=31703 exp=31703`
**TTA:** 4433ms

---

### ✅ Q07 — Epidemiológica Simples

**Q:** Total de internações por doenças osteomusculares (M00-M99) em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND icd_group = 'M00-M99';
```

**Resultado:** `got=3955 exp=3955`
**TTA:** 4424ms

---

### ❌ Q08 — Epidemiológica Simples

**Q:** Número de mulheres internadas por causa ortopédica em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(*) AS total_mulheres_internadas
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND sexo_paciente = 'F';
```

**Resultado:** `got=13286 exp=13285`
**TTA:** 5215ms

---

### ❌ Q09 — Epidemiológica Simples

**Q:** Número de homens internados por causa ortopédica em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(*) AS total_homens_internados
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND sexo_paciente = 'M'
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=22383 exp=22373`
**TTA:** 5181ms

---

### ✅ Q10 — Epidemiológica Simples

**Q:** Número de internações ortopédicas de idosos (60 anos ou mais) em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND idade_paciente >= 60;
```

**Resultado:** `got=12432 exp=12432`
**TTA:** 5375ms

---

### ✅ Q11 — Epidemiológica Simples

**Q:** Número de municípios distintos com internação ortopédica registrada em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT cod_munic_estabelecimento) AS numero_municipios
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=64 exp=64`
**TTA:** 5142ms

---

### ✅ Q12 — Epidemiológica Simples

**Q:** Número de estabelecimentos distintos com internação ortopédica em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT cnes_estabelecimento) AS total_estabelecimentos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=140 exp=140`
**TTA:** 5132ms

---

### ✅ Q13 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com permanência superior a 7 dias em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND dias_perm > 7;
```

**Resultado:** `got=10614 exp=10614`
**TTA:** 5315ms

---

### ❌ Q14 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com uso de UTI em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND val_uti > 0;
```

**Resultado:** `got=5931 exp=1`
**TTA:** 5343ms

---

### ✅ Q15 — Epidemiológica Simples

**Q:** Número de internações por fratura de quadril (S72) em pacientes com 70 anos ou mais em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
  AND idade_paciente >= 70;
```

**Resultado:** `got=5480 exp=5480`
**TTA:** 4684ms

---

### ✅ Q16 — Epidemiológica Complexa

**Q:** Top 10 CIDs ortopédicos por volume de internação em RJ em 2023

**SQL gerado:**
```sql
SELECT cid_principal,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cid_principal
ORDER BY internacoes DESC
LIMIT 10;
```

**Resultado:** `10 linhas corretas (flexível)`
**TTA:** 5699ms

---

### ✅ Q17 — Epidemiológica Complexa

**Q:** Top 5 municípios de RJ com maior número de internações ortopédicas em 2023

**SQL gerado:**
```sql
SELECT cod_munic_estabelecimento,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cod_munic_estabelecimento
ORDER BY internacoes DESC
LIMIT 5;
```

**Resultado:** `5 linhas corretas (flexível)`
**TTA:** 6049ms

---

### ✅ Q18 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por faixa etária (0-17, 18-59, 60+) em RJ em 2023

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN idade_paciente BETWEEN 0 AND 17 THEN '0-17'
    WHEN idade_paciente BETWEEN 18 AND 59 THEN '18-59'
    ELSE '60+'
  END AS faixa_etaria,
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY faixa_etaria
ORDER BY faixa_etaria;
```

**Resultado:** `3 linhas corretas, ordem livre (flexível)`
**TTA:** 7900ms

---

### ⚠️ Q19 — Epidemiológica Complexa

**Q:** Taxa de mortalidade hospitalar por CID ortopédico (top 5) em RJ em 2023

**SQL gerado:**
```sql
SELECT 
  cid_principal,
  COUNT(CASE WHEN obito_hosp = 'S' THEN 1 END) AS mortes,
  COUNT(DISTINCT n_aih) AS internacoes,
  ROUND(100.0 * COUNT(CASE WHEN obito_hosp = 'S' THEN 1 END) / COUNT(DISTINCT n_aih), 2) AS taxa_mortalidade
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY 
  cid_principal
ORDER BY 
  taxa_mortalidade DESC
LIMIT 5;
```

**Erro:** Falha DuckDB: Binder Error: Referenced column "obito_hosp" not found in FROM clause!
Candidate bindings: "cgc_hosp", "val_sp", "gestao_responsavel", "ano_cmpt", "cid_asso"

LINE 3:   COUNT(CASE WHEN obito_hosp = 'S' THEN 1 END) AS mortes,
                          ^

---

### ✅ Q20 — Epidemiológica Complexa

**Q:** Top 10 CIDs ambulatoriais ortopédicos por volume de produção em RJ em 2023

**SQL gerado:**
```sql
SELECT cid_principal,
  COUNT(*) AS volume_producao
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cid_principal
ORDER BY volume_producao DESC
LIMIT 10;
```

**Resultado:** `10 linhas corretas (flexível)`
**TTA:** 5627ms

---

### ❌ Q21 — Epidemiológica Complexa

**Q:** Proporção de internações ortopédicas por sexo em RJ em 2023

**SQL gerado:**
```sql
SELECT 
  sexo_paciente,
  COUNT(*) AS total_internacoes,
  ROUND(100.0 * COUNT(*) / SUM(COUNT(*)) OVER (), 1) AS pct
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY 
  sexo_paciente
ORDER BY 
  total_internacoes DESC;
```

**Resultado:** `Linha não encontrada: {'sexo_paciente': 'M', 'total_internacoes': 22373, 'pct': 62.7}`
**TTA:** 6869ms

---

### ❌ Q22 — Epidemiológica Complexa

**Q:** Número de internações por fratura de fêmur (S72) em idosos (60+) comparado ao total em RJ em 2023

**SQL gerado:**
```sql
SELECT
  SUM(CASE WHEN sexo_paciente = 'M' AND idade_paciente >= 60 THEN 1 ELSE 0 END) AS total_homens_idosos,
  SUM(CASE WHEN sexo_paciente = 'F' AND idade_paciente >= 60 THEN 1 ELSE 0 END) AS total_mulheres_idosos,
  SUM(CASE WHEN sexo_paciente = 'M' THEN 1 ELSE 0 END) AS total_homens,
  SUM(CASE WHEN sexo_paciente = 'F' THEN 1 ELSE 0 END) AS total_mulheres
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%';
```

**Resultado:** `Linha 1: got={'total_homens_idosos': 2262.0, 'total_mulheres_idosos': 4872.0, 'total_homens': 4971.0, 'total_mulheres': 5697.0} exp={'total_s72': 10668, 's72_idosos': 7134.0, 'pct_idosos': 66.9}`
**TTA:** 8592ms

---

### ❌ Q23 — Epidemiológica Complexa

**Q:** Top 5 CIDs ortopédicos com maior permanência média hospitalar em RJ em 2023

**SQL gerado:**
```sql
SELECT cid_principal,
  ROUND(AVG(dias_perm), 1) AS permanencia_media_dias
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cid_principal
ORDER BY permanencia_media_dias DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cid_principal': 'S158', 'permanencia_media_dias': 67.0} exp={'cid_principal': 'T845', 'total_internacoes': 76, 'permanencia_media_dias': 19.4}`
**TTA:** 5984ms

---

### ✅ Q24 — Epidemiológica Complexa

**Q:** Top 5 estabelecimentos com maior volume de internação ortopédica em RJ em 2023

**SQL gerado:**
```sql
SELECT cnes_estabelecimento,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cnes_estabelecimento
ORDER BY internacoes DESC
LIMIT 5;
```

**Resultado:** `5 linhas corretas (flexível)`
**TTA:** 5518ms

---

### ❌ Q25 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por raça/cor do paciente em RJ em 2023

**SQL gerado:**
```sql
SELECT raca_cor_paciente,
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY raca_cor_paciente
ORDER BY total_internacoes DESC;
```

**Resultado:** `Linha não encontrada: {'raca_cor_paciente': '03', 'total': 21334, 'pct': 59.8}`
**TTA:** 5438ms

---

### ⚠️ Q26 — Epidemiológica Complexa

**Q:** CIDs ortopédicos com maior número de dias totais de internação em RJ em 2023

**SQL gerado:**
```sql
SELECT cid_principal,
  SUM(dias_tot_internacao) AS total_dias_internacao
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cid_principal
ORDER BY total_dias_internacao DESC
LIMIT 1;
```

**Erro:** Falha DuckDB: Binder Error: Referenced column "dias_tot_internacao" not found in FROM clause!
Candidate bindings: "dias_perm", "dt_inter", "dt_saida", "raca_cor_paciente", "tipo_financiamento"

LINE 2:   SUM(dias_tot_internacao) AS total_dias_internacao
              ^

---

### ❌ Q27 — Epidemiológica Complexa

**Q:** Top 5 procedimentos ambulatoriais ortopédicos mais realizados em RJ em 2023

**SQL gerado:**
```sql
SELECT cid_principal,
  COUNT(*) AS total_realizacoes
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cid_principal
ORDER BY total_realizacoes DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cid_principal': 'S781', 'total_realizacoes': 6614} exp={'cod_procedimento': '0302050019', 'total_procedimentos': 15319}`
**TTA:** 5570ms

---

### ⚠️ Q28 — Epidemiológica Complexa

**Q:** Proporção de internações ortopédicas com paciente de outro município em RJ em 2023

**SQL gerado:**
```sql
SELECT 
  ROUND(100.0 * COUNT(*) / SUM(COUNT(*)) OVER (), 1) AS pct_internacoes_outro_municipio
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND cod_munic_estabelecimento <> cod_munic_paciente;
```

**Erro:** Falha DuckDB: Binder Error: Referenced column "cod_munic_paciente" not found in FROM clause!
Candidate bindings: "cod_munic_residencia", "cod_munic_estabelecimento", "cod_idade", "cod_procedimento", "etnia_paciente"

LINE 8:   AND cod_munic_estabelecimento <> cod_munic_paciente;
                                           ^

---

### ❌ Q29 — Epidemiológica Complexa

**Q:** Comparativo de volume mensal SIA vs SIH ortopédico em RJ em 2023

**SQL gerado:**
```sql
SELECT
  mes_cmpt,
  COUNT(CASE WHEN sistema = 'SIA' THEN 1 END) AS internacoes_sia,
  COUNT(CASE WHEN sistema = 'SIH' THEN 1 END) AS internacoes_sih
FROM processed
WHERE uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Resultado:** `Número de linhas: got=12 exp=24`
**TTA:** 6092ms

---

### ❌ Q30 — Epidemiológica Complexa

**Q:** Idade média dos pacientes internados por fratura de fêmur (S72) em RJ em 2023

**SQL gerado:**
```sql
SELECT ROUND(AVG(idade_paciente), 1) AS idade_media
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%';
```

**Resultado:** `Linha 1: got={'idade_media': 63.0} exp={'idade_media': 63.0, 'idade_minima': 0.0, 'idade_maxima': 99.0}`
**TTA:** 4356ms

---

### ✅ Q31 — Financeira

**Q:** Custo total de todas as internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=92092851.42 exp=92092851.42`
**TTA:** 5283ms

---

### ✅ Q32 — Financeira

**Q:** Custo médio por internação ortopédica em RJ em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)) / COUNT(DISTINCT n_aih), 2) AS custo_medio_por_internacao
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=2582.67 exp=2581.87`
**TTA:** 5854ms

---

### ✅ Q33 — Financeira

**Q:** Custo total de internações por fratura de fêmur (S72) em RJ em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%';
```

**Resultado:** `got=30987118.46 exp=30987118.46`
**TTA:** 4630ms

---

### ❌ Q34 — Financeira

**Q:** Top 5 CIDs ortopédicos com maior custo total de internação em RJ em 2023

**SQL gerado:**
```sql
SELECT cid_principal,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cid_principal
ORDER BY custo_total DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cid_principal': 'S720', 'custo_total': 12139401.8} exp={'cid_principal': 'S720', 'internacoes': 3541, 'custo_total': 12139401.8}`
**TTA:** 6111ms

---

### ❌ Q35 — Financeira

**Q:** Custo total de internações ortopédicas com uso de OPME (val_ortp > 0) em RJ em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND val_ortp > 0;
```

**Resultado:** `Linha 1: got={'custo_total': None} exp={'internacoes_com_opme': 0, 'custo_total_opme': None}`
**TTA:** 5631ms

---

### ✅ Q36 — Financeira

**Q:** Valor total de honorários profissionais (val_sp) em internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(val_sp, 0)), 2) AS valor_total_honorarios
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=17470900.35 exp=17470900.35`
**TTA:** 5346ms

---

### ✅ Q37 — Financeira

**Q:** Custo médio por dia de internação ortopédica em RJ em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)) / SUM(COALESCE(dias_perm, 0)), 2) AS custo_medio_por_dia
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=370.15 exp=370.15`
**TTA:** 5966ms

---

### ❌ Q38 — Financeira

**Q:** Top 5 estabelecimentos com maior custo total de internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT cnes_estabelecimento,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cnes_estabelecimento
ORDER BY custo_total DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cnes_estabelecimento': '2290227', 'custo_total': 10961414.37} exp={'cnes_estabelecimento': '2290227', 'internacoes': 5986, 'custo_total': 10961414.37}`
**TTA:** 6412ms

---

### ✅ Q39 — Financeira

**Q:** Custo total de procedimentos ambulatoriais ortopédicos em RJ em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=7729430.04 exp=7729430.04`
**TTA:** 5295ms

---

### ⚠️ Q40 — Financeira

**Q:** Proporção custo serviço hospitalar vs honorários em internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT
  ROUND(100.0 * SUM(COALESCE(val_sh, 0)) / SUM(COALESCE(custo_total, 0)), 2) AS pct_val_sh,
  ROUND(100.0 * SUM(COALESCE(val_honorarios, 0)) / SUM(COALESCE(custo_total, 0)), 2) AS pct_val_honorarios
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Erro:** Falha DuckDB: Binder Error: Referenced column "val_honorarios" not found in FROM clause!
Candidate bindings: "val_sh", "val_ortp", "val_uci", "val_uti", "pa_docorig"

LINE 3:   ROUND(100.0 * SUM(COALESCE(val_honorarios, 0)) / SUM(COALESCE(custo_total, 0)), 2)...
                                     ^

---

### ✅ Q41 — Temporal/Comparativa

**Q:** Distribuição mensal de internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT
  mes_cmpt,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 5665ms

---

### ✅ Q42 — Temporal/Comparativa

**Q:** Distribuição mensal de procedimentos ambulatoriais ortopédicos em RJ em 2023

**SQL gerado:**
```sql
SELECT 
  mes_cmpt,
  COUNT(*) AS total_procedimentos
FROM 
  processed
WHERE 
  sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY 
  mes_cmpt
ORDER BY 
  mes_cmpt;
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 5798ms

---

### ✅ Q43 — Temporal/Comparativa

**Q:** Mês com maior número de internações por fratura de fêmur (S72) em RJ em 2023

**SQL gerado:**
```sql
SELECT mes_cmpt,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
GROUP BY mes_cmpt
ORDER BY internacoes DESC
LIMIT 1;
```

**Resultado:** `got=7 exp=7`
**TTA:** 5033ms

---

### ✅ Q44 — Temporal/Comparativa

**Q:** Evolução mensal do custo total de internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT
  mes_cmpt,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Resultado:** `12 linhas corretas (exato)`
**TTA:** 6059ms

---

### ❌ Q45 — Temporal/Comparativa

**Q:** Volume mensal de internações por trauma (S00-T98) vs doenças musculoesqueléticas (M00-M99) em RJ em 2023

**SQL gerado:**
```sql
SELECT
  mes_cmpt,
  COUNT(CASE WHEN icd_group = 'S00-T98' THEN 1 END) AS internacoes_trauma,
  COUNT(CASE WHEN icd_group = 'M00-M99' THEN 1 END) AS internacoes_musculoesqueléticas
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Resultado:** `Número de linhas: got=12 exp=24`
**TTA:** 7640ms

---

### ⚠️ Q46 — Temporal/Comparativa

**Q:** Evolução mensal de óbitos em internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT
  mes_cmpt,
  COUNT(DISTINCT n_aih) AS total_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND obito = 'S'
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Erro:** Falha DuckDB: Binder Error: Referenced column "obito" not found in FROM clause!
Candidate bindings: "morte", "gestao", "row_id", "cod_munic_estabelecimento", "sistema"

LINE 9:   AND obito = 'S'
              ^

---

### ✅ Q47 — Temporal/Comparativa

**Q:** Permanência média por trimestre nas internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN mes_cmpt BETWEEN 1 AND 3 THEN 'Q1'
    WHEN mes_cmpt BETWEEN 4 AND 6 THEN 'Q2'
    WHEN mes_cmpt BETWEEN 7 AND 9 THEN 'Q3'
    ELSE 'Q4'
  END AS trimestre,
  ROUND(AVG(dias_perm), 1) AS permanencia_media_dias
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY trimestre
ORDER BY trimestre;
```

**Resultado:** `4 linhas corretas, ordem livre (exato)`
**TTA:** 7611ms

---

### ✅ Q48 — Temporal/Comparativa

**Q:** Mês com maior custo médio por internação ortopédica em RJ em 2023

**SQL gerado:**
```sql
SELECT mes_cmpt,
  ROUND(AVG(custo_total), 2) AS custo_medio_por_internacao
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY mes_cmpt
ORDER BY custo_medio_por_internacao DESC
LIMIT 1;
```

**Resultado:** `got=12 exp=12`
**TTA:** 5832ms

---

### ✅ Q49 — Temporal/Comparativa

**Q:** Sazonalidade mensal de fraturas de fêmur (S72) em RJ em 2023

**SQL gerado:**
```sql
SELECT
  mes_cmpt,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 4951ms

---

### ✅ Q50 — Temporal/Comparativa

**Q:** Volume mensal de internações ortopédicas de idosos (60 anos ou mais) em RJ em 2023

**SQL gerado:**
```sql
SELECT 
  mes_cmpt,
  COUNT(DISTINCT n_aih) AS internacoes
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND idade_paciente >= 60
GROUP BY 
  mes_cmpt
ORDER BY 
  mes_cmpt;
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 6365ms

---

### ✅ Q51 — Epidemiológica Simples

**Q:** Número de internações por fratura de fêmur (S72) em mulheres em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
  AND sexo_paciente = 'F';
```

**Resultado:** `got=5697 exp=5697`
**TTA:** 4629ms

---

### ✅ Q52 — Epidemiológica Simples

**Q:** Número de internações por fratura de fêmur (S72) em homens em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
  AND sexo_paciente = 'M';
```

**Resultado:** `got=4971 exp=4971`
**TTA:** 4637ms

---

### ✅ Q53 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com permanência de 1 dia ou menos em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND dias_perm <= 1;
```

**Resultado:** `got=7210 exp=7210`
**TTA:** 5227ms

---

### ✅ Q54 — Epidemiológica Simples

**Q:** Número de procedimentos ambulatoriais ortopédicos realizados em mulheres em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(*) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND sexo_paciente = 'F'
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=15625 exp=15625`
**TTA:** 5111ms

---

### ✅ Q55 — Epidemiológica Simples

**Q:** Número de procedimentos ambulatoriais ortopédicos realizados por idosos (60 anos ou mais) em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(*) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND idade_paciente >= 60
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=17399 exp=17399`
**TTA:** 5148ms

---

### ✅ Q56 — Epidemiológica Simples

**Q:** Número de internações por fratura de tíbia e perônio (S82) em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S82%';
```

**Resultado:** `got=4271 exp=4271`
**TTA:** 4305ms

---

### ✅ Q57 — Epidemiológica Simples

**Q:** Número de internações por fratura de antebraço (S52) em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S52%';
```

**Resultado:** `got=2610 exp=2610`
**TTA:** 4321ms

---

### ✅ Q58 — Epidemiológica Simples

**Q:** Número de internações por fratura de ombro e úmero (S42) em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S42%';
```

**Resultado:** `got=1266 exp=1266`
**TTA:** 4313ms

---

### ✅ Q59 — Epidemiológica Simples

**Q:** Número de internações ortopédicas em crianças (menores de 18 anos) em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND idade_paciente < 18;
```

**Resultado:** `got=3554 exp=3554`
**TTA:** 5395ms

---

### ✅ Q60 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com permanência entre 8 e 30 dias em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND dias_perm BETWEEN 8 AND 30;
```

**Resultado:** `got=9782 exp=9782`
**TTA:** 5016ms

---

### ❌ Q61 — Epidemiológica Simples

**Q:** Número de internações por artrose de quadril e joelho (M16 e M17) em idosos (60 anos ou mais) em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M16' OR icd_group = 'M17')
  AND idade_paciente >= 60;
```

**Resultado:** `got=0 exp=244`
**TTA:** 5160ms

---

### ✅ Q62 — Epidemiológica Simples

**Q:** Número de internações por fratura de fêmur (S72) em mulheres com 70 anos ou mais em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
  AND sexo_paciente = 'F'
  AND idade_paciente >= 70;
```

**Resultado:** `got=4000 exp=4000`
**TTA:** 5045ms

---

### ✅ Q63 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com permanência superior a 30 dias em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND dias_perm > 30;
```

**Resultado:** `got=835 exp=835`
**TTA:** 5353ms

---

### ✅ Q64 — Epidemiológica Simples

**Q:** Número de procedimentos ambulatoriais ortopédicos realizados em homens em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(*) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND sexo_paciente = 'M'
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=18252 exp=18252`
**TTA:** 5095ms

---

### ✅ Q65 — Epidemiológica Simples

**Q:** Número de internações por traumatismos (grupo S00-T98) em idosos (60 anos ou mais) em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND icd_group = 'S00-T98'
  AND idade_paciente >= 60;
```

**Resultado:** `got=11281 exp=11281`
**TTA:** 4841ms

---

### ✅ Q66 — Epidemiológica Simples

**Q:** Número de internações por doenças osteomusculares (grupo M00-M99) em mulheres em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND icd_group = 'M00-M99'
  AND sexo_paciente = 'F';
```

**Resultado:** `got=1648 exp=1648`
**TTA:** 4792ms

---

### ⚠️ Q67 — Epidemiológica Simples

**Q:** Número de óbitos em internações por fratura de fêmur (S72) em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
  AND obito = 'S';
```

**Erro:** Falha DuckDB: Binder Error: Referenced column "obito" not found in FROM clause!
Candidate bindings: "morte", "gestao", "row_id", "cod_munic_estabelecimento", "sistema"

LINE 7:   AND obito = 'S';
              ^

---

### ✅ Q68 — Epidemiológica Simples

**Q:** Número de municípios distintos com registro de procedimentos ambulatoriais ortopédicos em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT cod_munic_estabelecimento) AS num_municipios_distintos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=80 exp=80`
**TTA:** 5250ms

---

### ✅ Q69 — Epidemiológica Complexa

**Q:** Top 5 municípios por volume de procedimentos ambulatoriais ortopédicos em RJ em 2023

**SQL gerado:**
```sql
SELECT cod_munic_estabelecimento,
  COUNT(*) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cod_munic_estabelecimento
ORDER BY total_procedimentos DESC
LIMIT 5;
```

**Resultado:** `5 linhas corretas (flexível)`
**TTA:** 5971ms

---

### ❌ Q70 — Epidemiológica Complexa

**Q:** Distribuição de procedimentos ambulatoriais ortopédicos por faixa etária em RJ em 2023

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN idade_paciente BETWEEN 0 AND 17 THEN '0-17'
    WHEN idade_paciente BETWEEN 18 AND 34 THEN '18-34'
    WHEN idade_paciente BETWEEN 35 AND 49 THEN '35-49'
    WHEN idade_paciente BETWEEN 50 AND 64 THEN '50-64'
    ELSE '65+'
  END AS faixa_etaria,
  COUNT(*) AS total
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY faixa_etaria
ORDER BY faixa_etaria;
```

**Resultado:** `Número de linhas: got=5 exp=3`
**TTA:** 8751ms

---

### ⚠️ Q71 — Epidemiológica Complexa

**Q:** Top 5 diagnósticos com maior número de óbitos em internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT cid_principal,
  COUNT(*) AS total_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND obito = 'SIM'
GROUP BY cid_principal
ORDER BY total_obitos DESC
LIMIT 5;
```

**Erro:** Falha DuckDB: Binder Error: Referenced column "obito" not found in FROM clause!
Candidate bindings: "morte", "gestao", "row_id", "cod_munic_estabelecimento", "sistema"

LINE 8:   AND obito = 'SIM'
              ^

---

### ✅ Q72 — Epidemiológica Complexa

**Q:** Permanência hospitalar média por sexo em internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT 
  sexo_paciente,
  ROUND(AVG(dias_perm), 1) AS permanencia_media_dias
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY 
  sexo_paciente
ORDER BY 
  sexo_paciente;
```

**Resultado:** `2 linhas corretas, ordem livre (flexível)`
**TTA:** 6158ms

---

### ✅ Q73 — Epidemiológica Complexa

**Q:** Top 10 estabelecimentos por volume de procedimentos ambulatoriais ortopédicos em RJ em 2023

**SQL gerado:**
```sql
SELECT cnes_estabelecimento,
  COUNT(*) AS procedimentos_ortopédicos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cnes_estabelecimento
ORDER BY procedimentos_ortopédicos DESC
LIMIT 10;
```

**Resultado:** `10 linhas corretas (flexível)`
**TTA:** 6178ms

---

### ⚠️ Q74 — Epidemiológica Complexa

**Q:** Taxa de mortalidade por sexo em internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT 
  sexo_paciente,
  COUNT(DISTINCT CASE WHEN obito_30dias = 'S' THEN n_aih END) AS mortes,
  COUNT(DISTINCT n_aih) AS total_internacoes,
  ROUND(100.0 * COUNT(DISTINCT CASE WHEN obito_30dias = 'S' THEN n_aih END) / COUNT(DISTINCT n_aih), 2) AS taxa_mortalidade
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY sexo_paciente
ORDER BY taxa_mortalidade DESC;
```

**Erro:** Falha DuckDB: Binder Error: Referenced column "obito_30dias" not found in FROM clause!
Candidate bindings: "qt_diarias", "dt_saida", "pa_motsai", "cod_idade", "pa_cbocod"

LINE 3:   COUNT(DISTINCT CASE WHEN obito_30dias = 'S' THEN n_aih END) AS mortes,
                                   ^

---

### ⚠️ Q75 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por grupo diagnóstico (M e S) e faixa etária em RJ em 2023

**SQL gerado:**
```sql
SELECT 
  icd_group,
  faixa_etaria,
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY 
  icd_group, faixa_etaria
ORDER BY 
  icd_group, faixa_etaria;
```

**Erro:** Falha DuckDB: Binder Error: Referenced column "faixa_etaria" not found in FROM clause!
Candidate bindings: "pa_autoriz", "faec_tp", "raca_cor_paciente", "main_icd", "cid_notif"

LINE 13:   icd_group, faixa_etaria
                      ^

---

### ❌ Q76 — Epidemiológica Complexa

**Q:** Top 5 diagnósticos de traumatismo com maior permanência média em RJ em 2023 (mínimo 40 internações)

**SQL gerado:**
```sql
SELECT cid_principal,
  AVG(dias_perm) AS permanencia_media_dias
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND icd_group = 'S00-T98'
GROUP BY cid_principal
HAVING COUNT(DISTINCT n_aih) >= 40
ORDER BY permanencia_media_dias DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cid_principal': 'T845', 'permanencia_media_dias': 19.4078947368} exp={'cid_principal': 'T845', 'internacoes': 76, 'permanencia_media': 19.4}`
**TTA:** 5362ms

---

### ✅ Q77 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por faixa etária (0-17, 18-39, 40-59, 60-79, 80+) em RJ em 2023

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN idade_paciente BETWEEN 0 AND 17 THEN '0-17'
    WHEN idade_paciente BETWEEN 18 AND 39 THEN '18-39'
    WHEN idade_paciente BETWEEN 40 AND 59 THEN '40-59'
    WHEN idade_paciente BETWEEN 60 AND 79 THEN '60-79'
    ELSE '80+'
  END AS faixa_etaria,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY faixa_etaria
ORDER BY faixa_etaria;
```

**Resultado:** `5 linhas corretas, ordem livre (flexível)`
**TTA:** 10687ms

---

### ✅ Q78 — Epidemiológica Complexa

**Q:** Top 5 estabelecimentos com maior volume de internações por fratura de fêmur (S72) em RJ em 2023

**SQL gerado:**
```sql
SELECT cnes_estabelecimento,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
GROUP BY cnes_estabelecimento
ORDER BY internacoes DESC
LIMIT 5;
```

**Resultado:** `5 linhas corretas (exato)`
**TTA:** 5310ms

---

### ❌ Q79 — Epidemiológica Complexa

**Q:** Permanência hospitalar média por faixa etária em internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN idade_paciente < 18 THEN '0-17'
    WHEN idade_paciente < 30 THEN '18-29'
    WHEN idade_paciente < 40 THEN '30-39'
    WHEN idade_paciente < 50 THEN '40-49'
    WHEN idade_paciente < 60 THEN '50-59'
    WHEN idade_paciente < 70 THEN '60-69'
    WHEN idade_paciente < 80 THEN '70-79'
    ELSE '80+'
  END AS faixa_etaria,
  ROUND(AVG(dias_perm), 1) AS permanencia_media_dias
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY faixa_etaria
ORDER BY faixa_etaria;
```

**Resultado:** `Número de linhas: got=8 exp=3`
**TTA:** 11069ms

---

### ✅ Q80 — Epidemiológica Complexa

**Q:** Top 5 diagnósticos de doença osteomuscular (M00-M99) por número de internações em RJ em 2023

**SQL gerado:**
```sql
SELECT cid_principal,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND icd_group = 'M00-M99'
GROUP BY cid_principal
ORDER BY internacoes DESC
LIMIT 5;
```

**Resultado:** `5 linhas corretas (flexível)`
**TTA:** 5121ms

---

### ✅ Q81 — Epidemiológica Complexa

**Q:** Número de internações ortopédicas com permanência entre 3 e 7 dias em RJ em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND dias_perm BETWEEN 3 AND 7;
```

**Resultado:** `got=12572 exp=12572`
**TTA:** 5432ms

---

### ✅ Q82 — Epidemiológica Complexa

**Q:** Top 5 diagnósticos ambulatoriais ortopédicos em idosos (60 anos ou mais) em RJ em 2023

**SQL gerado:**
```sql
SELECT cid_principal,
  COUNT(*) AS total_internacoes
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND idade_paciente >= 60
GROUP BY cid_principal
ORDER BY total_internacoes DESC
LIMIT 5;
```

**Resultado:** `5 linhas corretas (flexível)`
**TTA:** 5970ms

---

### ⚠️ Q83 — Epidemiológica Complexa

**Q:** Média de internações ortopédicas por estabelecimento em RJ em 2023

**SQL gerado:**
```sql
SELECT cnes_estabelecimento,
  ROUND(AVG(COUNT(DISTINCT n_aih)), 1) AS media_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cnes_estabelecimento
ORDER BY media_internacoes DESC;
```

**Erro:** Falha DuckDB: Binder Error: aggregate function calls cannot be nested

LINE 2:   ROUND(AVG(COUNT(DISTINCT n_aih)), 1) AS media_internacoes
                    ^

---

### ❌ Q84 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por faixa de permanência em RJ em 2023

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN dias_perm BETWEEN 0 AND 7 THEN '0-7 dias'
    WHEN dias_perm BETWEEN 8 AND 14 THEN '8-14 dias'
    WHEN dias_perm BETWEEN 15 AND 21 THEN '15-21 dias'
    WHEN dias_perm BETWEEN 22 AND 28 THEN '22-28 dias'
    ELSE '29+ dias'
  END AS faixa_permanencia,
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY faixa_permanencia
ORDER BY faixa_permanencia;
```

**Resultado:** `Número de linhas: got=5 exp=4`
**TTA:** 9555ms

---

### ✅ Q85 — Financeira

**Q:** Custo total de internações ortopédicas de idosos (60 anos ou mais) em RJ em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND idade_paciente >= 60;
```

**Resultado:** `got=37226129.45 exp=37226129.45`
**TTA:** 5709ms

---

### ✅ Q86 — Financeira

**Q:** Custo médio por internação por fratura de fêmur (S72) em RJ em 2023

**SQL gerado:**
```sql
SELECT ROUND(AVG(COALESCE(custo_total, 0)), 2) AS custo_medio_por_internacao
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%';
```

**Resultado:** `got=2904.68 exp=2904.68`
**TTA:** 4823ms

---

### ❌ Q87 — Financeira

**Q:** Custo total de internações ortopédicas com passagem por UTI em RJ em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND val_uti > 0;
```

**Resultado:** `got=37449313.94 exp=18598.76`
**TTA:** 5654ms

---

### ✅ Q88 — Financeira

**Q:** Custo médio por internação ortopédica por sexo do paciente em RJ em 2023

**SQL gerado:**
```sql
SELECT 
  sexo_paciente,
  ROUND(AVG(custo_total), 2) AS custo_medio_por_internacao
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY 
  sexo_paciente
ORDER BY 
  sexo_paciente;
```

**Resultado:** `2 linhas corretas, ordem livre (flexível)`
**TTA:** 6288ms

---

### ✅ Q89 — Financeira

**Q:** Custo total de internações ortopédicas em crianças (menores de 18 anos) em RJ em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND idade_paciente < 18;
```

**Resultado:** `got=7918775.81 exp=7918775.81`
**TTA:** 5700ms

---

### ✅ Q90 — Financeira

**Q:** Custo médio por internação ortopédica com permanência superior a 7 dias em RJ em 2023

**SQL gerado:**
```sql
SELECT ROUND(AVG(custo_total), 2) AS custo_medio
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND dias_perm > 7;
```

**Resultado:** `got=3944.5 exp=3944.5`
**TTA:** 5379ms

---

### ✅ Q91 — Financeira

**Q:** Top 3 municípios com maior custo total em internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT cod_munic_estabelecimento,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cod_munic_estabelecimento
ORDER BY custo_total DESC
LIMIT 3;
```

**Resultado:** `3 linhas corretas (exato)`
**TTA:** 6485ms

---

### ❌ Q92 — Financeira

**Q:** Valor total de serviço hospitalar em internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS valor_total_servico_hospitalar
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=92092851.42 exp=73139419.49`
**TTA:** 5479ms

---

### ❌ Q93 — Financeira

**Q:** Custo médio por internação ortopédica por faixa etária em RJ em 2023

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN idade_paciente < 18 THEN '0-17'
    WHEN idade_paciente < 30 THEN '18-29'
    WHEN idade_paciente < 40 THEN '30-39'
    WHEN idade_paciente < 50 THEN '40-49'
    WHEN idade_paciente < 60 THEN '50-59'
    WHEN idade_paciente < 70 THEN '60-69'
    ELSE '70+'
  END AS faixa_etaria,
  ROUND(AVG(custo_total), 2) AS custo_medio_por_internacao
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY faixa_etaria
ORDER BY faixa_etaria;
```

**Resultado:** `Número de linhas: got=7 exp=3`
**TTA:** 10534ms

---

### ✅ Q94 — Financeira

**Q:** Valor total de UTI em internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(val_uti, 0)), 2) AS valor_uti_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=16618123.58 exp=16618123.58`
**TTA:** 5350ms

---

### ✅ Q95 — Financeira

**Q:** Custo total de internações por doenças osteomusculares (M00-M99) em RJ em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND icd_group = 'M00-M99';
```

**Resultado:** `got=13641307.23 exp=13641307.23`
**TTA:** 4734ms

---

### ✅ Q96 — Financeira

**Q:** Custo total de internações por traumatismos (S00-T98) em RJ em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND icd_group = 'S00-T98';
```

**Resultado:** `got=78451544.19 exp=78451544.19`
**TTA:** 4734ms

---

### ✅ Q97 — Financeira

**Q:** Top 5 diagnósticos com maior custo médio por internação ortopédica em RJ em 2023 (mínimo 40 internações)

**SQL gerado:**
```sql
SELECT cid_principal,
  COUNT(DISTINCT n_aih) AS internacoes,
  ROUND(AVG(custo_total), 2) AS custo_medio
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cid_principal
HAVING COUNT(DISTINCT n_aih) >= 40
ORDER BY custo_medio DESC
LIMIT 5;
```

**Resultado:** `5 linhas corretas (exato)`
**TTA:** 8195ms

---

### ✅ Q98 — Financeira

**Q:** Custo médio por procedimento ambulatorial ortopédico em RJ em 2023

**SQL gerado:**
```sql
SELECT ROUND(AVG(custo_total), 2) AS custo_medio
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=228.16 exp=228.16`
**TTA:** 5097ms

---

### ❌ Q99 — Financeira

**Q:** Top 5 procedimentos ambulatoriais com maior custo total em RJ em 2023

**SQL gerado:**
```sql
SELECT cid_principal,
  COUNT(*) AS total_procedimentos,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
GROUP BY cid_principal
ORDER BY custo_total DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cid_principal': None, 'total_procedimentos': 34349, 'custo_total': 12029717.3} exp={'cod_procedimento': '0701010029', 'custo_total': 2349402.8}`
**TTA:** 5314ms

---

### ✅ Q100 — Financeira

**Q:** Custo total de internações ortopédicas de idosos (60+) com permanência superior a 7 dias em RJ em 2023

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND idade_paciente >= 60
  AND dias_perm > 7;
```

**Resultado:** `got=19953660.54 exp=19953660.54`
**TTA:** 6019ms

---

### ❌ Q101 — Temporal/Comparativa

**Q:** Mês com maior número de óbitos em internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT mes_cmpt,
  COUNT(DISTINCT n_aih) AS total_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY mes_cmpt
ORDER BY total_obitos DESC
LIMIT 1;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 11, 'total_obitos': 3397} exp={'mes_cmpt': 5, 'total_obitos': 88}`
**TTA:** 5828ms

---

### ✅ Q102 — Temporal/Comparativa

**Q:** Volume mensal de internações por fratura de fêmur (S72) em idosos (60 anos ou mais) em RJ em 2023

**SQL gerado:**
```sql
SELECT 
  mes_cmpt,
  COUNT(DISTINCT n_aih) AS internacoes
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
  AND idade_paciente >= 60
GROUP BY 
  mes_cmpt
ORDER BY 
  mes_cmpt;
```

**Resultado:** `12 linhas corretas (exato)`
**TTA:** 6871ms

---

### ✅ Q103 — Temporal/Comparativa

**Q:** Custo mensal de internações ortopédicas de idosos (60 anos ou mais) em RJ em 2023

**SQL gerado:**
```sql
SELECT 
  mes_cmpt,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_mensal
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND idade_paciente >= 60
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY 
  mes_cmpt
ORDER BY 
  mes_cmpt;
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 6874ms

---

### ✅ Q104 — Temporal/Comparativa

**Q:** Mês com maior custo total de internações por fratura de fêmur (S72) em RJ em 2023

**SQL gerado:**
```sql
SELECT mes_cmpt,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
GROUP BY mes_cmpt
ORDER BY custo_total DESC
LIMIT 1;
```

**Resultado:** `1 linhas corretas (exato)`
**TTA:** 5465ms

---

### ✅ Q105 — Temporal/Comparativa

**Q:** Volume mensal de internações por traumatismos (grupo S00-T98) em RJ em 2023

**SQL gerado:**
```sql
SELECT 
  mes_cmpt,
  COUNT(DISTINCT n_aih) AS internacoes
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND icd_group = 'S00-T98'
GROUP BY 
  mes_cmpt
ORDER BY 
  mes_cmpt;
```

**Resultado:** `12 linhas corretas (exato)`
**TTA:** 5399ms

---

### ✅ Q106 — Temporal/Comparativa

**Q:** Volume mensal de procedimentos ambulatoriais ortopédicos por sexo em RJ em 2023

**SQL gerado:**
```sql
SELECT
  mes_cmpt,
  sexo_paciente,
  COUNT(*) AS volume_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY mes_cmpt, sexo_paciente
ORDER BY mes_cmpt, sexo_paciente;
```

**Resultado:** `24 linhas corretas (flexível)`
**TTA:** 6011ms

---

### ✅ Q107 — Temporal/Comparativa

**Q:** Comparativo trimestral de internações ortopédicas por grupo diagnóstico em RJ em 2023

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN mes_cmpt BETWEEN 1 AND 3 THEN 'Q1'
    WHEN mes_cmpt BETWEEN 4 AND 6 THEN 'Q2'
    WHEN mes_cmpt BETWEEN 7 AND 9 THEN 'Q3'
    ELSE 'Q4'
  END AS trimestre,
  icd_group AS grupo_diagnostico,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY trimestre, icd_group
ORDER BY trimestre, grupo_diagnostico;
```

**Resultado:** `8 linhas corretas, ordem livre (flexível)`
**TTA:** 8600ms

---

### ✅ Q108 — Temporal/Comparativa

**Q:** Mês com maior permanência hospitalar média em internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT
  mes_cmpt,
  ROUND(AVG(dias_perm), 1) AS permanencia_media_dias
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY mes_cmpt
ORDER BY permanencia_media_dias DESC
LIMIT 1;
```

**Resultado:** `1 linhas corretas (flexível)`
**TTA:** 6152ms

---

### ⚠️ Q109 — Temporal/Comparativa

**Q:** Evolução mensal da taxa de mortalidade em internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT
  mes_cmpt,
  ROUND(100.0 * SUM(CASE WHEN obito_30_dias = 1 THEN 1 ELSE 0 END) / COUNT(DISTINCT n_aih), 2) AS taxa_mortalidade
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Erro:** Falha DuckDB: Binder Error: Referenced column "obito_30_dias" not found in FROM clause!
Candidate bindings: "qt_diarias", "dt_saida", "cod_idade", "pa_cbocod", "pa_cidcas"

LINE 3:   ROUND(100.0 * SUM(CASE WHEN obito_30_dias = 1 THEN 1 ELSE 0 END) / COUNT(DISTINCT n_aih...
                                      ^

---

### ✅ Q110 — Temporal/Comparativa

**Q:** Trimestre com maior volume de internações por fratura de fêmur (S72) em RJ em 2023

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN mes_cmpt BETWEEN 1 AND 3 THEN 'Q1'
    WHEN mes_cmpt BETWEEN 4 AND 6 THEN 'Q2'
    WHEN mes_cmpt BETWEEN 7 AND 9 THEN 'Q3'
    ELSE 'Q4'
  END AS trimestre,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
GROUP BY trimestre
ORDER BY internacoes DESC
LIMIT 1;
```

**Resultado:** `1 linhas corretas (flexível)`
**TTA:** 7345ms

---

### ❌ Q111 — Temporal/Comparativa

**Q:** Comparativo semestral de internações ortopédicas (1º vs 2º semestre) em RJ em 2023

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN mes_cmpt BETWEEN 1 AND 6 THEN '1º Semestre'
    ELSE '2º Semestre'
  END AS semestre,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY semestre
ORDER BY semestre;
```

**Resultado:** `Linha não encontrada: {'semestre': '1S', 'total': 16855}`
**TTA:** 6765ms

---

### ❌ Q112 — Temporal/Comparativa

**Q:** Custo médio mensal por internação por fratura de fêmur (S72) em RJ em 2023

**SQL gerado:**
```sql
SELECT 
  ROUND(AVG(custo_total), 2) AS custo_medio_mensal
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%';
```

**Resultado:** `Número de linhas: got=1 exp=12`
**TTA:** 4751ms

---

### ✅ Q113 — Temporal/Comparativa

**Q:** Volume mensal de internações ortopédicas por sexo do paciente em RJ em 2023

**SQL gerado:**
```sql
SELECT 
  mes_cmpt,
  sexo_paciente,
  COUNT(DISTINCT n_aih) AS internacoes
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY 
  mes_cmpt, sexo_paciente
ORDER BY 
  mes_cmpt, sexo_paciente;
```

**Resultado:** `24 linhas corretas (flexível)`
**TTA:** 6476ms

---

### ✅ Q114 — Temporal/Comparativa

**Q:** Mês com menor volume de procedimentos ambulatoriais ortopédicos em RJ em 2023

**SQL gerado:**
```sql
SELECT mes_cmpt,
  COUNT(*) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY mes_cmpt
ORDER BY total_procedimentos ASC
LIMIT 1;
```

**Resultado:** `1 linhas corretas (flexível)`
**TTA:** 5652ms

---

### ✅ Q115 — Temporal/Comparativa

**Q:** Volume trimestral de procedimentos ambulatoriais ortopédicos em RJ em 2023

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN mes_cmpt BETWEEN 1 AND 3 THEN 'Q1'
    WHEN mes_cmpt BETWEEN 4 AND 6 THEN 'Q2'
    WHEN mes_cmpt BETWEEN 7 AND 9 THEN 'Q3'
    ELSE 'Q4'
  END AS trimestre,
  COUNT(*) AS volume_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY trimestre
ORDER BY trimestre;
```

**Resultado:** `4 linhas corretas, ordem livre (flexível)`
**TTA:** 7695ms

---

### ✅ Q116 — Temporal/Comparativa

**Q:** Custo mensal de procedimentos ambulatoriais ortopédicos em RJ em 2023

**SQL gerado:**
```sql
SELECT 
  mes_cmpt,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_mensal
FROM 
  processed
WHERE 
  sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY 
  mes_cmpt
ORDER BY 
  mes_cmpt;
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 6451ms

---

### ❌ Q117 — Temporal/Comparativa

**Q:** Comparativo semestral do custo total de internações ortopédicas em RJ em 2023

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN mes_cmpt BETWEEN 1 AND 6 THEN 'Semestre 1'
    ELSE 'Semestre 2'
  END AS semestre,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY semestre
ORDER BY semestre;
```

**Resultado:** `Linha não encontrada: {'semestre': '1S', 'custo_total': 41433762.95}`
**TTA:** 7149ms

---

### ✅ Q118 — Temporal/Comparativa

**Q:** Evolução mensal da permanência média em internações por fratura de fêmur (S72) em RJ em 2023

**SQL gerado:**
```sql
SELECT
  mes_cmpt,
  ROUND(AVG(dias_perm), 1) AS permanencia_media_dias
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 5168ms

---

## Failures Detail

- **Q01** (Epidemiológica Simples): got=3955 exp=35658
  SQL: `SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_...`
- **Q04** (Epidemiológica Simples): got=0 exp=348
  SQL: `SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_...`
- **Q05** (Epidemiológica Simples): Falha DuckDB: Binder Error: Referenced column "obito" not found in FROM clause!
Candidate bindings: "morte", "gestao", "row_id", "cod_munic_estabelecimento", "sistema"

LINE 7:   AND obito = 'S';
              ^
  SQL: `SELECT COUNT(DISTINCT n_aih) AS total_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt ...`
- **Q08** (Epidemiológica Simples): got=13286 exp=13285
  SQL: `SELECT COUNT(*) AS total_mulheres_internadas
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt ...`
- **Q09** (Epidemiológica Simples): got=22383 exp=22373
  SQL: `SELECT COUNT(*) AS total_homens_internados
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = ...`
- **Q14** (Epidemiológica Simples): got=5931 exp=1
  SQL: `SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_...`
- **Q19** (Epidemiológica Complexa): Falha DuckDB: Binder Error: Referenced column "obito_hosp" not found in FROM clause!
Candidate bindings: "cgc_hosp", "val_sp", "gestao_responsavel", "ano_cmpt", "cid_asso"

LINE 3:   COUNT(CASE WHEN obito_hosp = 'S' THEN 1 END) AS mortes,
                          ^
  SQL: `SELECT 
  cid_principal,
  COUNT(CASE WHEN obito_hosp = 'S' THEN 1 END) AS mortes,
  COUNT(DISTINCT n_aih) AS internacoe...`
- **Q21** (Epidemiológica Complexa): Linha não encontrada: {'sexo_paciente': 'M', 'total_internacoes': 22373, 'pct': 62.7}
  SQL: `SELECT 
  sexo_paciente,
  COUNT(*) AS total_internacoes,
  ROUND(100.0 * COUNT(*) / SUM(COUNT(*)) OVER (), 1) AS pct
FR...`
- **Q22** (Epidemiológica Complexa): Linha 1: got={'total_homens_idosos': 2262.0, 'total_mulheres_idosos': 4872.0, 'total_homens': 4971.0, 'total_mulheres': 5697.0} exp={'total_s72': 10668, 's72_idosos': 7134.0, 'pct_idosos': 66.9}
  SQL: `SELECT
  SUM(CASE WHEN sexo_paciente = 'M' AND idade_paciente >= 60 THEN 1 ELSE 0 END) AS total_homens_idosos,
  SUM(CAS...`
- **Q23** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'S158', 'permanencia_media_dias': 67.0} exp={'cid_principal': 'T845', 'total_internacoes': 76, 'permanencia_media_dias': 19.4}
  SQL: `SELECT cid_principal,
  ROUND(AVG(dias_perm), 1) AS permanencia_media_dias
FROM processed
WHERE sistema = 'SIH'
  AND uf...`
- **Q25** (Epidemiológica Complexa): Linha não encontrada: {'raca_cor_paciente': '03', 'total': 21334, 'pct': 59.8}
  SQL: `SELECT raca_cor_paciente,
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_ori...`
- **Q26** (Epidemiológica Complexa): Falha DuckDB: Binder Error: Referenced column "dias_tot_internacao" not found in FROM clause!
Candidate bindings: "dias_perm", "dt_inter", "dt_saida", "raca_cor_paciente", "tipo_financiamento"

LINE 2:   SUM(dias_tot_internacao) AS total_dias_internacao
              ^
  SQL: `SELECT cid_principal,
  SUM(dias_tot_internacao) AS total_dias_internacao
FROM processed
WHERE sistema = 'SIH'
  AND uf_...`
- **Q27** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'S781', 'total_realizacoes': 6614} exp={'cod_procedimento': '0302050019', 'total_procedimentos': 15319}
  SQL: `SELECT cid_principal,
  COUNT(*) AS total_realizacoes
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ...`
- **Q28** (Epidemiológica Complexa): Falha DuckDB: Binder Error: Referenced column "cod_munic_paciente" not found in FROM clause!
Candidate bindings: "cod_munic_residencia", "cod_munic_estabelecimento", "cod_idade", "cod_procedimento", "etnia_paciente"

LINE 8:   AND cod_munic_estabelecimento <> cod_munic_paciente;
                                           ^
  SQL: `SELECT 
  ROUND(100.0 * COUNT(*) / SUM(COUNT(*)) OVER (), 1) AS pct_internacoes_outro_municipio
FROM processed
WHERE sis...`
- **Q29** (Epidemiológica Complexa): Número de linhas: got=12 exp=24
  SQL: `SELECT
  mes_cmpt,
  COUNT(CASE WHEN sistema = 'SIA' THEN 1 END) AS internacoes_sia,
  COUNT(CASE WHEN sistema = 'SIH' T...`
- **Q30** (Epidemiológica Complexa): Linha 1: got={'idade_media': 63.0} exp={'idade_media': 63.0, 'idade_minima': 0.0, 'idade_maxima': 99.0}
  SQL: `SELECT ROUND(AVG(idade_paciente), 1) AS idade_media
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND an...`
- **Q34** (Financeira): Linha 1: got={'cid_principal': 'S720', 'custo_total': 12139401.8} exp={'cid_principal': 'S720', 'internacoes': 3541, 'custo_total': 12139401.8}
  SQL: `SELECT cid_principal,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AN...`
- **Q35** (Financeira): Linha 1: got={'custo_total': None} exp={'internacoes_com_opme': 0, 'custo_total_opme': None}
  SQL: `SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ...`
- **Q38** (Financeira): Linha 1: got={'cnes_estabelecimento': '2290227', 'custo_total': 10961414.37} exp={'cnes_estabelecimento': '2290227', 'internacoes': 5986, 'custo_total': 10961414.37}
  SQL: `SELECT cnes_estabelecimento,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SI...`
- **Q40** (Financeira): Falha DuckDB: Binder Error: Referenced column "val_honorarios" not found in FROM clause!
Candidate bindings: "val_sh", "val_ortp", "val_uci", "val_uti", "pa_docorig"

LINE 3:   ROUND(100.0 * SUM(COALESCE(val_honorarios, 0)) / SUM(COALESCE(custo_total, 0)), 2)...
                                     ^
  SQL: `SELECT
  ROUND(100.0 * SUM(COALESCE(val_sh, 0)) / SUM(COALESCE(custo_total, 0)), 2) AS pct_val_sh,
  ROUND(100.0 * SUM(C...`
- **Q45** (Temporal/Comparativa): Número de linhas: got=12 exp=24
  SQL: `SELECT
  mes_cmpt,
  COUNT(CASE WHEN icd_group = 'S00-T98' THEN 1 END) AS internacoes_trauma,
  COUNT(CASE WHEN icd_grou...`
- **Q46** (Temporal/Comparativa): Falha DuckDB: Binder Error: Referenced column "obito" not found in FROM clause!
Candidate bindings: "morte", "gestao", "row_id", "cod_munic_estabelecimento", "sistema"

LINE 9:   AND obito = 'S'
              ^
  SQL: `SELECT
  mes_cmpt,
  COUNT(DISTINCT n_aih) AS total_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
 ...`
- **Q61** (Epidemiológica Simples): got=0 exp=244
  SQL: `SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_...`
- **Q67** (Epidemiológica Simples): Falha DuckDB: Binder Error: Referenced column "obito" not found in FROM clause!
Candidate bindings: "morte", "gestao", "row_id", "cod_munic_estabelecimento", "sistema"

LINE 7:   AND obito = 'S';
              ^
  SQL: `SELECT COUNT(DISTINCT n_aih) AS total_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt ...`
- **Q70** (Epidemiológica Complexa): Número de linhas: got=5 exp=3
  SQL: `SELECT
  CASE
    WHEN idade_paciente BETWEEN 0 AND 17 THEN '0-17'
    WHEN idade_paciente BETWEEN 18 AND 34 THEN '18-34...`
- **Q71** (Epidemiológica Complexa): Falha DuckDB: Binder Error: Referenced column "obito" not found in FROM clause!
Candidate bindings: "morte", "gestao", "row_id", "cod_munic_estabelecimento", "sistema"

LINE 8:   AND obito = 'SIM'
              ^
  SQL: `SELECT cid_principal,
  COUNT(*) AS total_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_c...`
- **Q74** (Epidemiológica Complexa): Falha DuckDB: Binder Error: Referenced column "obito_30dias" not found in FROM clause!
Candidate bindings: "qt_diarias", "dt_saida", "pa_motsai", "cod_idade", "pa_cbocod"

LINE 3:   COUNT(DISTINCT CASE WHEN obito_30dias = 'S' THEN n_aih END) AS mortes,
                                   ^
  SQL: `SELECT 
  sexo_paciente,
  COUNT(DISTINCT CASE WHEN obito_30dias = 'S' THEN n_aih END) AS mortes,
  COUNT(DISTINCT n_aih...`
- **Q75** (Epidemiológica Complexa): Falha DuckDB: Binder Error: Referenced column "faixa_etaria" not found in FROM clause!
Candidate bindings: "pa_autoriz", "faec_tp", "raca_cor_paciente", "main_icd", "cid_notif"

LINE 13:   icd_group, faixa_etaria
                      ^
  SQL: `SELECT 
  icd_group,
  faixa_etaria,
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
  processed
WHERE 
  sistema = '...`
- **Q76** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'T845', 'permanencia_media_dias': 19.4078947368} exp={'cid_principal': 'T845', 'internacoes': 76, 'permanencia_media': 19.4}
  SQL: `SELECT cid_principal,
  AVG(dias_perm) AS permanencia_media_dias
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = ...`
- **Q79** (Epidemiológica Complexa): Número de linhas: got=8 exp=3
  SQL: `SELECT
  CASE
    WHEN idade_paciente < 18 THEN '0-17'
    WHEN idade_paciente < 30 THEN '18-29'
    WHEN idade_paciente...`
- **Q83** (Epidemiológica Complexa): Falha DuckDB: Binder Error: aggregate function calls cannot be nested

LINE 2:   ROUND(AVG(COUNT(DISTINCT n_aih)), 1) AS media_internacoes
                    ^
  SQL: `SELECT cnes_estabelecimento,
  ROUND(AVG(COUNT(DISTINCT n_aih)), 1) AS media_internacoes
FROM processed
WHERE sistema = ...`
- **Q84** (Epidemiológica Complexa): Número de linhas: got=5 exp=4
  SQL: `SELECT
  CASE
    WHEN dias_perm BETWEEN 0 AND 7 THEN '0-7 dias'
    WHEN dias_perm BETWEEN 8 AND 14 THEN '8-14 dias'
  ...`
- **Q87** (Financeira): got=37449313.94 exp=18598.76
  SQL: `SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ...`
- **Q92** (Financeira): got=92092851.42 exp=73139419.49
  SQL: `SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS valor_total_servico_hospitalar
FROM processed
WHERE sistema = 'SIH'
  ...`
- **Q93** (Financeira): Número de linhas: got=7 exp=3
  SQL: `SELECT
  CASE
    WHEN idade_paciente < 18 THEN '0-17'
    WHEN idade_paciente < 30 THEN '18-29'
    WHEN idade_paciente...`
- **Q99** (Financeira): Linha 1: got={'cid_principal': None, 'total_procedimentos': 34349, 'custo_total': 12029717.3} exp={'cod_procedimento': '0701010029', 'custo_total': 2349402.8}
  SQL: `SELECT cid_principal,
  COUNT(*) AS total_procedimentos,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM p...`
- **Q101** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 11, 'total_obitos': 3397} exp={'mes_cmpt': 5, 'total_obitos': 88}
  SQL: `SELECT mes_cmpt,
  COUNT(DISTINCT n_aih) AS total_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  A...`
- **Q109** (Temporal/Comparativa): Falha DuckDB: Binder Error: Referenced column "obito_30_dias" not found in FROM clause!
Candidate bindings: "qt_diarias", "dt_saida", "cod_idade", "pa_cbocod", "pa_cidcas"

LINE 3:   ROUND(100.0 * SUM(CASE WHEN obito_30_dias = 1 THEN 1 ELSE 0 END) / COUNT(DISTINCT n_aih...
                                      ^
  SQL: `SELECT
  mes_cmpt,
  ROUND(100.0 * SUM(CASE WHEN obito_30_dias = 1 THEN 1 ELSE 0 END) / COUNT(DISTINCT n_aih), 2) AS tax...`
- **Q111** (Temporal/Comparativa): Linha não encontrada: {'semestre': '1S', 'total': 16855}
  SQL: `SELECT
  CASE
    WHEN mes_cmpt BETWEEN 1 AND 6 THEN '1º Semestre'
    ELSE '2º Semestre'
  END AS semestre,
  COUNT(DIS...`
- **Q112** (Temporal/Comparativa): Número de linhas: got=1 exp=12
  SQL: `SELECT 
  ROUND(AVG(custo_total), 2) AS custo_medio_mensal
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = ...`
- **Q117** (Temporal/Comparativa): Linha não encontrada: {'semestre': '1S', 'custo_total': 41433762.95}
  SQL: `SELECT
  CASE
    WHEN mes_cmpt BETWEEN 1 AND 6 THEN 'Semestre 1'
    ELSE 'Semestre 2'
  END AS semestre,
  ROUND(SUM(C...`

---
_Gerado por scripts/evaluate_benchmark.py — SUS Data RAG — USF/Mestrado_