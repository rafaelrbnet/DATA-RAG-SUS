# Benchmark Evaluation Report — SUS Data RAG

**Model:** ollama  
**Run date:** 2026-08-22 23:21  
**API:** http://127.0.0.1:8000  

## Summary

| Metric | Value |
|---|---|
| Total queries | 118 |
| Correct (EA numerator) | 16 |
| **Execution Accuracy (EA)** | **13.6%** |
| Wilson IC 95% | [8.5%, 20.9%] |
| Correct via flexible match (alias diferente) | 10 |
| API errors / timeouts | 2 |
| Mean Time-to-Answer | 6.0s |

> **Nota metodológica — Scoring:** A Execution Accuracy avalia se o SQL gerado
> retorna os mesmos **valores** que o gold-standard, independente do nome das colunas.
> Quando o LLM usa um alias diferente mas retorna os dados corretos (match posicional),
> a query é contada como correta e sinalizada com `~`. Esta decisão segue a definição
> de EA em [Lee et al., 2022] — comparação de conjuntos de resultado, não de SQL texto.

## EA by Category

| Category | Correct | Total | EA |
|---|---:|---:|---:|
| Epidemiológica Simples | 9 | 33 | 27.3% |
| Epidemiológica Complexa | 1 | 31 | 3.2% |
| Financeira | 3 | 26 | 11.5% |
| Temporal/Comparativa | 3 | 28 | 10.7% |

## Query Results

### ✅ Q01 — Epidemiológica Simples

**Q:** Total de internações ortopédicas (M00-M99 e S00-S99) em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=28056 exp=28056`
**TTA:** 11077ms

---

### ❌ Q02 — Epidemiológica Simples

**Q:** Total de procedimentos ambulatoriais ortopédicos em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(*) AS total_procedimentos
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND cod_procedimento LIKE '0408%'
```

**Resultado:** `got=0 exp=27201`
**TTA:** 4218ms

---

### ❌ Q03 — Epidemiológica Simples

**Q:** Total de internações por fratura de fêmur (S72) em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'S72%' OR e.clinical_interpretacao_clinica = 'lesoes e causas traumaticas');
```

**Resultado:** `got=25731 exp=9945`
**TTA:** 5514ms

---

### ✅ Q04 — Epidemiológica Simples

**Q:** Total de internações por osteoartrose (M16, M17) em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M16%' OR cid_principal LIKE 'M17%');
```

**Resultado:** `got=102 exp=102`
**TTA:** 5049ms

---

### ❌ Q05 — Epidemiológica Simples

**Q:** Número de óbitos em internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS num_obitos
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND morte = 1
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=21 exp=965`
**TTA:** 5376ms

---

### ✅ Q06 — Epidemiológica Simples

**Q:** Total de internações por traumatismos (S00-T98) em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'S%' OR icd_group = 'S00-T98');
```

**Resultado:** `got=25731 exp=25731`
**TTA:** 4212ms

---

### ✅ Q07 — Epidemiológica Simples

**Q:** Total de internações por doenças osteomusculares (M00-M99) em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND e.clinical_interpretacao_clinica = 'doencas osteomusculares e do tecido conjuntivo';
```

**Resultado:** `got=2325 exp=2325`
**TTA:** 5445ms

---

### ❌ Q08 — Epidemiológica Simples

**Q:** Número de mulheres internadas por causa ortopédica em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND sexo_paciente = 'F'
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=880 exp=10444`
**TTA:** 5448ms

---

### ❌ Q09 — Epidemiológica Simples

**Q:** Número de homens internados por causa ortopédica em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND sexo_paciente = 'M'
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=1445 exp=17612`
**TTA:** 5444ms

---

### ❌ Q10 — Epidemiológica Simples

**Q:** Número de internações ortopédicas de idosos (60 anos ou mais) em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND idade_paciente >= 60
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=586 exp=10149`
**TTA:** 5502ms

---

### ❌ Q11 — Epidemiológica Simples

**Q:** Número de municípios distintos com internação ortopédica registrada em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT cod_munic_residencia) AS num_municipios
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=102 exp=63`
**TTA:** 5235ms

---

### ❌ Q12 — Epidemiológica Simples

**Q:** Número de estabelecimentos distintos com internação ortopédica em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT cod_munic_estabelecimento) AS num_estabelecimentos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=46 exp=135`
**TTA:** 4429ms

---

### ❌ Q13 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com permanência superior a 7 dias em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND dias_perm > 7
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=734 exp=9244`
**TTA:** 4549ms

---

### ✅ Q14 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com uso de UTI em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
  AND uti_int_to > 0;
```

**Resultado:** `got=0 exp=0`
**TTA:** 5191ms

---

### ❌ Q15 — Epidemiológica Simples

**Q:** Número de internações por fratura de quadril (S72) em pacientes com 70 anos ou mais em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND idade_paciente >= 70
  AND (cid_principal LIKE 'S72%' OR e.clinical_interpretacao_clinica = 'lesoes e causas traumaticas');
```

**Resultado:** `got=6365 exp=5140`
**TTA:** 5912ms

---

### ❌ Q16 — Epidemiológica Complexa

**Q:** Top 10 CIDs ortopédicos por volume de internação em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    cid_principal, 
    COUNT(DISTINCT n_aih) AS internacoes_unicas
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'RJ' 
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cid_principal
ORDER BY 
    internacoes_unicas DESC
LIMIT 10;
```

**Resultado:** `Linha 1: got={'cid_principal': 'M861', 'internacoes_unicas': 182} exp={'cid_principal': 'S720', 'total_internacoes': 3502}`
**TTA:** 6672ms

---

### ❌ Q17 — Epidemiológica Complexa

**Q:** Top 5 municípios de RJ com maior número de internações ortopédicas em 2022

**SQL gerado:**
```sql
SELECT 
    cod_munic_residencia,
    COUNT(DISTINCT n_aih) AS num_internacoes
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'RJ' 
    AND ano_cmpt = 2022 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cod_munic_residencia
ORDER BY 
    num_internacoes DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cod_munic_residencia': '330455', 'num_internacoes': 514} exp={'cod_munic_estabelecimento': '330000', 'internacoes': 9878}`
**TTA:** 6794ms

---

### ❌ Q18 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por faixa etária (0-17, 18-59, 60+) em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    CASE 
        WHEN idade_paciente BETWEEN 0 AND 17 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 59 THEN '18-59'
        WHEN idade_paciente >= 60 THEN '60+'
    END AS faixa_etaria,
    COUNT(DISTINCT n_aih) AS quantidade_internacoes
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    faixa_etaria;
```

**Resultado:** `Linha não encontrada: {'faixa_etaria': '0-17', 'total': 2380}`
**TTA:** 8396ms

---

### ❌ Q19 — Epidemiológica Complexa

**Q:** Taxa de mortalidade hospitalar por CID ortopédico (top 5) em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    cid_principal,
    COUNT(DISTINCT n_aih) AS total_internacoes,
    SUM(morte) AS total_obitos,
    (SUM(morte) * 1.0 / COUNT(DISTINCT n_aih)) * 100 AS taxa_mortalidade
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cid_principal
ORDER BY 
    taxa_mortalidade DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cid_principal': 'M858', 'total_internacoes': 1, 'total_obitos': 1.0, 'taxa_mortalidade': 100.0} exp={'cid_principal': 'T203', 'total_internacoes': 44, 'total_obitos': 26.0, 'taxa_mortalidade_pct': 59.09}`
**TTA:** 8035ms

---

### ❌ Q20 — Epidemiológica Complexa

**Q:** Top 10 CIDs ambulatoriais ortopédicos por volume de produção em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    cid_principal, 
    COUNT(*) AS volume
FROM 
    processed
WHERE 
    sistema = 'SIA' 
    AND uf_origem = 'RJ' 
    AND ano_cmpt = 2022 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cid_principal
ORDER BY 
    volume DESC
LIMIT 10;
```

**Resultado:** `Linha 1: got={'cid_principal': 'M216', 'volume': 1963} exp={'cid_principal': 'S720', 'total_procedimentos': 5570}`
**TTA:** 5142ms

---

### ❌ Q21 — Epidemiológica Complexa

**Q:** Proporção de internações ortopédicas por sexo em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    sexo_paciente,
    COUNT(DISTINCT n_aih) AS internacoes,
    COUNT(DISTINCT n_aih) * 1.0 / SUM(COUNT(DISTINCT n_aih)) OVER () AS proporcao
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    sexo_paciente;
```

**Resultado:** `Linha não encontrada: {'sexo_paciente': 'M', 'total_internacoes': 17612, 'pct': 62.8}`
**TTA:** 6142ms

---

### ❌ Q22 — Epidemiológica Complexa

**Q:** Número de internações por fratura de fêmur (S72) em idosos (60+) comparado ao total em RJ em 2022

**SQL gerado:**
```sql
WITH fratura_femur AS (
    SELECT COUNT(DISTINCT n_aih) AS num_internacoes
    FROM processed
    JOIN enriched e ON processed.row_id = e.row_id
    WHERE sistema = 'SIH'
      AND cid_principal LIKE 'S72%'
      AND idade_paciente >= 60
      AND uf_origem = 'RJ'
      AND ano_cmpt = 2022
),
total_internacoes AS (
    SELECT COUNT(DISTINCT n_aih) AS num_internacoes
    FROM processed
    WHERE sistema = 'SIH'
      AND uf_origem = 'RJ'
      AND ano_cmpt = 2022
)
SELECT 
    ff.num_internacoes AS internacoes_fratura_femur,
    ti.num_internacoes AS total_internacoes
FROM fratura_femur ff, total_internacoes ti;
```

**Resultado:** `Linha 1: got={'internacoes_fratura_femur': 6628, 'total_internacoes': 69787} exp={'total_s72': 9945, 's72_idosos': 6628.0, 'pct_idosos': 66.6}`
**TTA:** 9967ms

---

### ❌ Q23 — Epidemiológica Complexa

**Q:** Top 5 CIDs ortopédicos com maior permanência média hospitalar em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    cid_principal, 
    AVG(dias_perm) AS media_permanencia
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'RJ' 
    AND ano_cmpt = 2022 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cid_principal
ORDER BY 
    media_permanencia DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cid_principal': 'M463', 'media_permanencia': 57.5} exp={'cid_principal': 'T203', 'total_internacoes': 44, 'permanencia_media_dias': 30.1}`
**TTA:** 6449ms

---

### ❌ Q24 — Epidemiológica Complexa

**Q:** Top 5 estabelecimentos com maior volume de internação ortopédica em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    cod_munic_estabelecimento, 
    COUNT(DISTINCT n_aih) AS volume_internacao
FROM 
    processed
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'RJ' 
    AND ano_cmpt = 2022 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cod_munic_estabelecimento
ORDER BY 
    volume_internacao DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cod_munic_estabelecimento': '330000', 'volume_internacao': 1007} exp={'cnes_estabelecimento': '2290227', 'internacoes': 5043}`
**TTA:** 5974ms

---

### ❌ Q25 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por raça/cor do paciente em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    raca_cor_paciente, 
    COUNT(DISTINCT n_aih) AS internacoes_ortopédicas
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'RJ' 
    AND ano_cmpt = 2022 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    raca_cor_paciente;
```

**Resultado:** `Número de linhas: got=5 exp=6`
**TTA:** 6359ms

---

### ❌ Q26 — Epidemiológica Complexa

**Q:** CIDs ortopédicos com maior número de dias totais de internação em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    cid_principal, 
    SUM(qt_diarias) AS total_dias_internacao
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'RJ' 
    AND ano_cmpt = 2022 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cid_principal
ORDER BY 
    total_dias_internacao DESC
LIMIT 1;
```

**Resultado:** `Número de linhas: got=1 exp=10`
**TTA:** 6634ms

---

### ❌ Q27 — Epidemiológica Complexa

**Q:** Top 5 procedimentos ambulatoriais ortopédicos mais realizados em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    cod_procedimento, 
    COUNT(*) AS quantidade_realizada
FROM 
    processed
WHERE 
    sistema = 'SIA' 
    AND uf_origem = 'RJ' 
    AND ano_cmpt = 2022 
    AND cod_procedimento LIKE '0408%'
GROUP BY 
    cod_procedimento
ORDER BY 
    quantidade_realizada DESC
LIMIT 5;
```

**Resultado:** `Número de linhas: got=0 exp=5`
**TTA:** 5080ms

---

### ⚠️ Q28 — Epidemiológica Complexa

**Q:** Proporção de internações ortopédicas com paciente de outro município em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    SUM(CASE WHEN e.clinical_deslocamento_territorial = 'deslocamento interestadual' THEN 1 ELSE 0 END) AS internacoes_deslocamento_interestadual,
    COUNT(DISTINCT p.n_aih) AS total_internacoes
FROM 
    processed p
JOIN 
    enriched e ON p.row_id = e.row_id
WHERE 
    p.sistema = 'SIH'
    AND p.uf_origem = 'RJ'
    AND p.ano_cmpt = 2022
    AND (p.cid_principal LIKE 'M%' OR p.icd_group = 'M00-M99')
GROUP BY 
    total_internacoes
HAVING 
    total_internacoes > 0
```

**Erro:** Falha DuckDB: Binder Error: GROUP BY clause cannot contain aggregates!

LINE 3:     COUNT(DISTINCT p.n_aih) AS total_internacoes
            ^

---

### ❌ Q29 — Epidemiológica Complexa

**Q:** Comparativo de volume mensal SIA vs SIH ortopédico em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    sistema,
    mes_cmpt,
    COUNT(*) AS volume_mensal
FROM 
    processed
WHERE 
    uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    sistema, mes_cmpt
ORDER BY 
    sistema, mes_cmpt;
```

**Resultado:** `Linha 1: got={'sistema': 'SIA', 'mes_cmpt': 1, 'volume_mensal': 208} exp={'mes_cmpt': 1, 'sistema': 'SIA', 'total': 1647}`
**TTA:** 5035ms

---

### ❌ Q30 — Epidemiológica Complexa

**Q:** Idade média dos pacientes internados por fratura de fêmur (S72) em RJ em 2022

**SQL gerado:**
```sql
SELECT AVG(idade_paciente) AS idade_media
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'S72%' OR e.clinical_interpretacao_clinica = 'lesoes e causas traumaticas');
```

**Resultado:** `Linha 1: got={'idade_media': 48.9025300222} exp={'idade_media': 63.3, 'idade_minima': 0.0, 'idade_maxima': 99.0}`
**TTA:** 5397ms

---

### ❌ Q31 — Financeira

**Q:** Custo total de todas as internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT SUM(custo_total) AS total_custo
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=8405202.739999998 exp=70893354.22`
**TTA:** 4097ms

---

### ❌ Q32 — Financeira

**Q:** Custo médio por internação ortopédica em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    AVG(COALESCE(custo_total, 0)) AS custo_medio_por_internacao
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=3596.5779803166 exp=2525.77`
**TTA:** 4799ms

---

### ❌ Q33 — Financeira

**Q:** Custo total de internações por fratura de fêmur (S72) em RJ em 2022

**SQL gerado:**
```sql
SELECT SUM(COALESCE(custo_total, 0)) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'S72%' OR icd_group = 'S00-T98');
```

**Resultado:** `got=62488151.48000009 exp=26337280.38`
**TTA:** 4454ms

---

### ❌ Q34 — Financeira

**Q:** Top 5 CIDs ortopédicos com maior custo total de internação em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    cid_principal, 
    SUM(custo_total) AS total_custo
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'RJ' 
    AND ano_cmpt = 2022 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cid_principal
ORDER BY 
    total_custo DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cid_principal': 'M511', 'total_custo': 1137816.14} exp={'cid_principal': 'S720', 'internacoes': 3502, 'custo_total': 10821184.25}`
**TTA:** 6372ms

---

### ❌ Q35 — Financeira

**Q:** Custo total de internações ortopédicas com uso de OPME (val_ortp > 0) em RJ em 2022

**SQL gerado:**
```sql
SELECT SUM(COALESCE(val_ortp, 0)) AS custo_total_ortopedia
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND val_ortp > 0;
```

**Resultado:** `Linha 1: got={'custo_total_ortopedia': None} exp={'internacoes_com_opme': 0, 'custo_total_opme': None}`
**TTA:** 4025ms

---

### ❌ Q36 — Financeira

**Q:** Valor total de honorários profissionais (val_sp) em internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT COALESCE(SUM(val_sp), 0) AS total_honorarios
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=1467550.3299999994 exp=13402482.02`
**TTA:** 5252ms

---

### ❌ Q37 — Financeira

**Q:** Custo médio por dia de internação ortopédica em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    AVG(COALESCE(val_uti, 0)) AS custo_medio_por_dia
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND sistema = 'SIH'
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=371.8023020967 exp=321.98`
**TTA:** 5710ms

---

### ❌ Q38 — Financeira

**Q:** Top 5 estabelecimentos com maior custo total de internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    cod_munic_estabelecimento, 
    SUM(custo_total) AS total_custo
FROM 
    processed
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'RJ' 
    AND ano_cmpt = 2022 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cod_munic_estabelecimento
ORDER BY 
    total_custo DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cod_munic_estabelecimento': '330000', 'total_custo': 3740518.5499999993} exp={'cnes_estabelecimento': '2290227', 'internacoes': 5043, 'custo_total': 9418220.5}`
**TTA:** 5788ms

---

### ❌ Q39 — Financeira

**Q:** Custo total de procedimentos ambulatoriais ortopédicos em RJ em 2022

**SQL gerado:**
```sql
SELECT SUM(COALESCE(custo_total, 0)) AS total_custo
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=867233.2599999987 exp=4149578.04`
**TTA:** 4375ms

---

### ❌ Q40 — Financeira

**Q:** Proporção custo serviço hospitalar vs honorários em internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    SUM(COALESCE(val_sh, 0)) / SUM(COALESCE(val_sp, 0)) AS proporcao_sh_sp
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `Linha 1: got={'proporcao_sh_sp': 4.727369323} exp={'custo_servico_hospitalar': 57490872.2, 'honorarios_profissionais': 13402482.02, 'pct_hospitalar': 81.1}`
**TTA:** 6037ms

---

### ❌ Q41 — Temporal/Comparativa

**Q:** Distribuição mensal de internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    COUNT(DISTINCT n_aih) AS internacoes_ortopédicas
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'internacoes_ortopédicas': 121} exp={'mes_cmpt': 1, 'total_internacoes': 1998}`
**TTA:** 6405ms

---

### ❌ Q42 — Temporal/Comparativa

**Q:** Distribuição mensal de procedimentos ambulatoriais ortopédicos em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    COUNT(*) AS quantidade_procedimentos
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIA' 
    AND uf_origem = 'RJ' 
    AND ano_cmpt = 2022 
    AND cod_procedimento LIKE '0408%'
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `Número de linhas: got=0 exp=12`
**TTA:** 5446ms

---

### ✅ Q43 — Temporal/Comparativa

**Q:** Mês com maior número de internações por fratura de fêmur (S72) em RJ em 2022

**SQL gerado:**
```sql
SELECT mes_cmpt, COUNT(DISTINCT n_aih) AS num_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND cid_principal LIKE 'S72%'
GROUP BY mes_cmpt
ORDER BY num_internacoes DESC
LIMIT 1;
```

**Resultado:** `got=10 exp=10`
**TTA:** 5533ms

---

### ❌ Q44 — Temporal/Comparativa

**Q:** Evolução mensal do custo total de internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    SUM(COALESCE(custo_total, 0)) AS custo_total
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'custo_total': 515324.59} exp={'mes_cmpt': 1, 'custo_total': 4528720.12}`
**TTA:** 6399ms

---

### ❌ Q45 — Temporal/Comparativa

**Q:** Volume mensal de internações por trauma (S00-T98) vs doenças musculoesqueléticas (M00-M99) em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    COUNT(DISTINCT CASE WHEN icd_group = 'S00-T98' THEN n_aih END) AS trauma_internacoes,
    COUNT(DISTINCT CASE WHEN icd_group = 'M00-M99' THEN n_aih END) AS musculoesqueletica_internacoes
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `Número de linhas: got=12 exp=24`
**TTA:** 7475ms

---

### ❌ Q46 — Temporal/Comparativa

**Q:** Evolução mensal de óbitos em internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    COUNT(DISTINCT n_aih) AS obitos
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND morte = 1
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `Número de linhas: got=11 exp=12`
**TTA:** 6557ms

---

### ❌ Q47 — Temporal/Comparativa

**Q:** Permanência média por trimestre nas internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    AVG(dias_perm) AS media_permanencia,
    mes_cmpt
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `Número de linhas: got=12 exp=4`
**TTA:** 6161ms

---

### ❌ Q48 — Temporal/Comparativa

**Q:** Mês com maior custo médio por internação ortopédica em RJ em 2022

**SQL gerado:**
```sql
SELECT mes_cmpt, AVG(custo_total) AS custo_medio
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY mes_cmpt
ORDER BY custo_medio DESC
LIMIT 1;
```

**Resultado:** `got=6 exp=11`
**TTA:** 5883ms

---

### ❌ Q49 — Temporal/Comparativa

**Q:** Sazonalidade mensal de fraturas de fêmur (S72) em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    COUNT(*) AS count_fraturas_femur
FROM 
    processed
WHERE 
    sistema = 'SIA' 
    AND uf_origem = 'RJ' 
    AND ano_cmpt = 2022 
    AND cid_principal LIKE 'S72%'
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'count_fraturas_femur': 739} exp={'mes_cmpt': 1, 'total_fraturas': 770}`
**TTA:** 4765ms

---

### ❌ Q50 — Temporal/Comparativa

**Q:** Volume mensal de internações ortopédicas de idosos (60 anos ou mais) em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND idade_paciente >= 60
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'volume_internacoes': 26} exp={'mes_cmpt': 1, 'total_internacoes': 731}`
**TTA:** 6697ms

---

### ❌ Q51 — Epidemiológica Simples

**Q:** Número de internações por fratura de fêmur (S72) em mulheres em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND sexo_paciente = 'F'
  AND (cid_principal LIKE 'S72%' OR e.clinical_interpretacao_clinica = 'lesoes e causas traumaticas')
```

**Resultado:** `got=9564 exp=5436`
**TTA:** 5909ms

---

### ❌ Q52 — Epidemiológica Simples

**Q:** Número de internações por fratura de fêmur (S72) em homens em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND sexo_paciente = 'M'
  AND (cid_principal LIKE 'S72%' OR e.clinical_interpretacao_clinica = 'lesoes e causas traumaticas');
```

**Resultado:** `got=16167 exp=4509`
**TTA:** 5918ms

---

### ❌ Q53 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com permanência de 1 dia ou menos em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND dias_perm <= 1
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=373 exp=5619`
**TTA:** 4575ms

---

### ❌ Q54 — Epidemiológica Simples

**Q:** Número de procedimentos ambulatoriais ortopédicos realizados em mulheres em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(*) AS numero_procedimentos
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE processed.sistema = 'SIA'
  AND processed.uf_origem = 'RJ'
  AND processed.ano_cmpt = 2022
  AND processed.sexo_paciente = 'F'
  AND (processed.cid_principal LIKE 'M%' OR e.clinical_interpretacao_clinica = 'doencas osteomusculares e do tecido conjuntivo')
  AND processed.cod_procedimento LIKE '0408%';
```

**Resultado:** `got=0 exp=12838`
**TTA:** 6479ms

---

### ❌ Q55 — Epidemiológica Simples

**Q:** Número de procedimentos ambulatoriais ortopédicos realizados por idosos (60 anos ou mais) em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(*) AS numero_procedimentos
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND idade_paciente >= 60
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
  AND cod_procedimento LIKE '0408%';
```

**Resultado:** `got=0 exp=13298`
**TTA:** 5568ms

---

### ❌ Q56 — Epidemiológica Simples

**Q:** Número de internações por fratura de tíbia e perônio (S82) em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'S82%' OR e.clinical_interpretacao_clinica = 'lesoes e causas traumaticas');
```

**Resultado:** `got=25731 exp=2991`
**TTA:** 5551ms

---

### ❌ Q57 — Epidemiológica Simples

**Q:** Número de internações por fratura de antebraço (S52) em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'S52%' OR e.clinical_interpretacao_clinica = 'lesoes e causas traumaticas')
```

**Resultado:** `got=25731 exp=1447`
**TTA:** 5538ms

---

### ❌ Q58 — Epidemiológica Simples

**Q:** Número de internações por fratura de ombro e úmero (S42) em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'S42%' OR e.clinical_interpretacao_clinica = 'lesoes e causas traumaticas');
```

**Resultado:** `got=25731 exp=765`
**TTA:** 5577ms

---

### ❌ Q59 — Epidemiológica Simples

**Q:** Número de internações ortopédicas em crianças (menores de 18 anos) em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND idade_paciente < 18
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=358 exp=2380`
**TTA:** 5535ms

---

### ❌ Q60 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com permanência entre 8 e 30 dias em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND dias_perm BETWEEN 8 AND 30
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=604 exp=8235`
**TTA:** 5617ms

---

### ✅ Q61 — Epidemiológica Simples

**Q:** Número de internações por artrose de quadril e joelho (M16 e M17) em idosos (60 anos ou mais) em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M16%' OR cid_principal LIKE 'M17%')
  AND idade_paciente >= 60;
```

**Resultado:** `got=62 exp=62`
**TTA:** 5493ms

---

### ❌ Q62 — Epidemiológica Simples

**Q:** Número de internações por fratura de fêmur (S72) em mulheres com 70 anos ou mais em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND idade_paciente >= 70
  AND sexo_paciente = 'F'
  AND (cid_principal LIKE 'S72%' OR icd_group = 'S00-T98');
```

**Resultado:** `got=4487 exp=3841`
**TTA:** 5976ms

---

### ❌ Q63 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com permanência superior a 30 dias em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND dias_perm > 30
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=131 exp=1010`
**TTA:** 4630ms

---

### ❌ Q64 — Epidemiológica Simples

**Q:** Número de procedimentos ambulatoriais ortopédicos realizados em homens em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(*) AS numero_procedimentos
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE processed.sistema = 'SIA'
  AND processed.uf_origem = 'RJ'
  AND processed.ano_cmpt = 2022
  AND processed.sexo_paciente = 'M'
  AND (processed.cid_principal LIKE 'M%' OR e.clinical_interpretacao_clinica = 'doencas osteomusculares e do tecido conjuntivo')
  AND processed.cod_procedimento LIKE '0408%';
```

**Resultado:** `got=0 exp=14363`
**TTA:** 6484ms

---

### ✅ Q65 — Epidemiológica Simples

**Q:** Número de internações por traumatismos (grupo S00-T98) em idosos (60 anos ou mais) em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND idade_paciente >= 60
  AND (cid_principal LIKE 'S%' OR icd_group = 'S00-T98');
```

**Resultado:** `got=9563 exp=9563`
**TTA:** 5539ms

---

### ✅ Q66 — Epidemiológica Simples

**Q:** Número de internações por doenças osteomusculares (grupo M00-M99) em mulheres em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND sexo_paciente = 'F'
  AND icd_group = 'M00-M99';
```

**Resultado:** `got=880 exp=880`
**TTA:** 5153ms

---

### ✅ Q67 — Epidemiológica Simples

**Q:** Número de óbitos em internações por fratura de fêmur (S72) em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_obitos
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND cid_principal LIKE 'S72%'
  AND morte = 1;
```

**Resultado:** `got=425 exp=425`
**TTA:** 4938ms

---

### ❌ Q68 — Epidemiológica Simples

**Q:** Número de municípios distintos com registro de procedimentos ambulatoriais ortopédicos em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT cod_munic_residencia) AS num_municipios_distintos
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND cod_procedimento LIKE '0408%'
```

**Resultado:** `got=0 exp=74`
**TTA:** 4695ms

---

### ❌ Q69 — Epidemiológica Complexa

**Q:** Top 5 municípios por volume de procedimentos ambulatoriais ortopédicos em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    cod_munic_residencia,
    COUNT(*) AS volume_procedimentos
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIA' 
    AND uf_origem = 'RJ' 
    AND ano_cmpt = 2022 
    AND cod_procedimento LIKE '0408%'
GROUP BY 
    cod_munic_residencia
ORDER BY 
    volume_procedimentos DESC
LIMIT 5;
```

**Resultado:** `Número de linhas: got=0 exp=5`
**TTA:** 5921ms

---

### ❌ Q70 — Epidemiológica Complexa

**Q:** Distribuição de procedimentos ambulatoriais ortopédicos por faixa etária em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    FLOOR(idade_paciente / 10) * 10 AS faixa_etaria,
    COUNT(*) AS quantidade_procedimentos
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIA' 
    AND uf_origem = 'RJ' 
    AND ano_cmpt = 2022 
    AND cod_procedimento LIKE '0408%'
GROUP BY 
    faixa_etaria
ORDER BY 
    faixa_etaria;
```

**Resultado:** `Número de linhas: got=0 exp=3`
**TTA:** 6214ms

---

### ❌ Q71 — Epidemiológica Complexa

**Q:** Top 5 diagnósticos com maior número de óbitos em internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    cid_principal, 
    COUNT(*) AS num_obitos
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'RJ' 
    AND ano_cmpt = 2022 
    AND morte = 1 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cid_principal
ORDER BY 
    num_obitos DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cid_principal': 'M869', 'num_obitos': 5} exp={'cid_principal': 'S720', 'total_internacoes': 3502, 'total_obitos': 187.0}`
**TTA:** 6563ms

---

### ❌ Q72 — Epidemiológica Complexa

**Q:** Permanência hospitalar média por sexo em internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    sexo_paciente,
    AVG(dias_perm) AS media_permanencia
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    sexo_paciente;
```

**Resultado:** `Linha não encontrada: {'sexo_paciente': 'F', 'permanencia_media': 8.6}`
**TTA:** 5837ms

---

### ❌ Q73 — Epidemiológica Complexa

**Q:** Top 10 estabelecimentos por volume de procedimentos ambulatoriais ortopédicos em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    cod_munic_estabelecimento, 
    COUNT(*) AS volume_procedimentos
FROM 
    processed
WHERE 
    sistema = 'SIA' 
    AND uf_origem = 'RJ' 
    AND ano_cmpt = 2022 
    AND cod_procedimento LIKE '0408%'
GROUP BY 
    cod_munic_estabelecimento
ORDER BY 
    volume_procedimentos DESC
LIMIT 10;
```

**Resultado:** `Número de linhas: got=0 exp=10`
**TTA:** 5432ms

---

### ❌ Q74 — Epidemiológica Complexa

**Q:** Taxa de mortalidade por sexo em internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    sexo_paciente,
    SUM(morte) AS total_obitos,
    COUNT(DISTINCT n_aih) AS total_internacoes,
    (SUM(morte) * 1.0 / COUNT(DISTINCT n_aih)) AS taxa_mortalidade
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    sexo_paciente;
```

**Resultado:** `Linha não encontrada: {'sexo_paciente': 'F', 'total_obitos': 412.0, 'taxa_mortalidade': 3.94}`
**TTA:** 7393ms

---

### ❌ Q75 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por grupo diagnóstico (M e S) e faixa etária em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    icd_group,
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente < 30 THEN '18-29'
        WHEN idade_paciente < 40 THEN '30-39'
        WHEN idade_paciente < 50 THEN '40-49'
        WHEN idade_paciente < 60 THEN '50-59'
        WHEN idade_paciente < 70 THEN '60-69'
        WHEN idade_paciente < 80 THEN '70-79'
        ELSE '80+'
    END AS faixa_etaria,
    COUNT(DISTINCT n_aih) AS internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99' OR cid_principal LIKE 'S%' OR icd_group = 'S00-T98')
GROUP BY 
    icd_group, faixa_etaria
ORDER BY 
    icd_group, faixa_etaria;
```

**Resultado:** `Número de linhas: got=16 exp=6`
**TTA:** 11808ms

---

### ❌ Q76 — Epidemiológica Complexa

**Q:** Top 5 diagnósticos de traumatismo com maior permanência média em RJ em 2022 (mínimo 40 internações)

**SQL gerado:**
```sql
SELECT 
    cid_principal,
    AVG(dias_perm) AS media_permanencia,
    COUNT(DISTINCT n_aih) AS internacoes
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'RJ' 
    AND ano_cmpt = 2022 
    AND (cid_principal LIKE 'S%' OR icd_group = 'S00-T98')
GROUP BY 
    cid_principal
HAVING 
    COUNT(DISTINCT n_aih) >= 40
ORDER BY 
    media_permanencia DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cid_principal': 'T203', 'media_permanencia': 30.0681818182, 'internacoes': 44} exp={'cid_principal': 'T203', 'internacoes': 44, 'permanencia_media': 30.1}`
**TTA:** 7595ms

---

### ❌ Q77 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por faixa etária (0-17, 18-39, 40-59, 60-79, 80+) em RJ em 2022

**SQL gerado:**
```sql
SELECT 
  CASE 
    WHEN idade_paciente BETWEEN 0 AND 17 THEN '0-17'
    WHEN idade_paciente BETWEEN 18 AND 39 THEN '18-39'
    WHEN idade_paciente BETWEEN 40 AND 59 THEN '40-59'
    WHEN idade_paciente BETWEEN 60 AND 79 THEN '60-79'
    WHEN idade_paciente >= 80 THEN '80+'
  END AS faixa_etaria,
  COUNT(DISTINCT n_aih) AS quantidade_internacoes
FROM 
  processed
JOIN 
  enriched e ON processed.row_id = e.row_id
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
  faixa_etaria
ORDER BY 
  faixa_etaria;
```

**Resultado:** `Linha não encontrada: {'faixa_etaria': '0-17', 'total': 2380}`
**TTA:** 10435ms

---

### ❌ Q78 — Epidemiológica Complexa

**Q:** Top 5 estabelecimentos com maior volume de internações por fratura de fêmur (S72) em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    cod_munic_estabelecimento, 
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'RJ' 
    AND ano_cmpt = 2022 
    AND (cid_principal LIKE 'S72%' OR icd_group = 'S00-T98')
GROUP BY 
    cod_munic_estabelecimento
ORDER BY 
    volume_internacoes DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cod_munic_estabelecimento': '330000', 'volume_internacoes': 8871} exp={'cnes_estabelecimento': '2290227', 'internacoes': 788}`
**TTA:** 7045ms

---

### ❌ Q79 — Epidemiológica Complexa

**Q:** Permanência hospitalar média por faixa etária em internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente < 30 THEN '18-29'
        WHEN idade_paciente < 40 THEN '30-39'
        WHEN idade_paciente < 50 THEN '40-49'
        WHEN idade_paciente < 60 THEN '50-59'
        WHEN idade_paciente < 70 THEN '60-69'
        ELSE '70+'
    END AS faixa_etaria,
    AVG(dias_perm) AS media_permanencia
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    faixa_etaria
ORDER BY 
    faixa_etaria;
```

**Resultado:** `Número de linhas: got=7 exp=3`
**TTA:** 10735ms

---

### ✅ Q80 — Epidemiológica Complexa

**Q:** Top 5 diagnósticos de doença osteomuscular (M00-M99) por número de internações em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    cid_principal, 
    COUNT(DISTINCT n_aih) AS num_internacoes
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'RJ' 
    AND ano_cmpt = 2022 
    AND icd_group = 'M00-M99'
GROUP BY 
    cid_principal
ORDER BY 
    num_internacoes DESC
LIMIT 5;
```

**Resultado:** `5 linhas corretas (flexível)`
**TTA:** 6257ms

---

### ❌ Q81 — Epidemiológica Complexa

**Q:** Número de internações ortopédicas com permanência entre 3 e 7 dias em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND dias_perm BETWEEN 3 AND 7
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=717 exp=9617`
**TTA:** 4702ms

---

### ❌ Q82 — Epidemiológica Complexa

**Q:** Top 5 diagnósticos ambulatoriais ortopédicos em idosos (60 anos ou mais) em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    cid_principal, 
    COUNT(*) AS quantidade
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIA' 
    AND uf_origem = 'RJ' 
    AND ano_cmpt = 2022 
    AND idade_paciente >= 60 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cid_principal
ORDER BY 
    quantidade DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cid_principal': 'M216', 'quantidade': 729} exp={'cid_principal': 'S720', 'total': 3056}`
**TTA:** 6486ms

---

### ⚠️ Q83 — Epidemiológica Complexa

**Q:** Média de internações ortopédicas por estabelecimento em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    cod_munic_estabelecimento,
    AVG(COUNT(DISTINCT n_aih)) AS media_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'RJ' 
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cod_munic_estabelecimento;
```

**Erro:** Falha DuckDB: Binder Error: aggregate function calls cannot be nested

LINE 3:     AVG(COUNT(DISTINCT n_aih)) AS media_internacoes
                ^

---

### ❌ Q84 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por faixa de permanência em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    CASE 
        WHEN dias_perm < 5 THEN '0-4 dias'
        WHEN dias_perm < 10 THEN '5-9 dias'
        WHEN dias_perm < 15 THEN '10-14 dias'
        WHEN dias_perm < 20 THEN '15-19 dias'
        WHEN dias_perm < 25 THEN '20-24 dias'
        ELSE '25+ dias'
    END AS faixa_permanencia,
    COUNT(DISTINCT n_aih) AS quantidade_internacoes
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    faixa_permanencia
ORDER BY 
    faixa_permanencia;
```

**Resultado:** `Número de linhas: got=6 exp=4`
**TTA:** 10175ms

---

### ❌ Q85 — Financeira

**Q:** Custo total de internações ortopédicas de idosos (60 anos ou mais) em RJ em 2022

**SQL gerado:**
```sql
SELECT SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND idade_paciente >= 60
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=2115629.9599999995 exp=28396358.5`
**TTA:** 4899ms

---

### ❌ Q86 — Financeira

**Q:** Custo médio por internação por fratura de fêmur (S72) em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    AVG(custo_total) AS custo_medio_por_internacao
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'S72%' OR icd_group = 'S00-T98')
```

**Resultado:** `got=2428.516244219 exp=2648.29`
**TTA:** 5558ms

---

### ✅ Q87 — Financeira

**Q:** Custo total de internações ortopédicas com passagem por UTI em RJ em 2022

**SQL gerado:**
```sql
SELECT SUM(COALESCE(val_uti, 0)) AS custo_total_uti
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
  AND uti_int_to > 0;
```

**Resultado:** `got=None exp=None`
**TTA:** 5426ms

---

### ❌ Q88 — Financeira

**Q:** Custo médio por internação ortopédica por sexo do paciente em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    sexo_paciente,
    AVG(custo_total) AS custo_medio_por_internacao
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    sexo_paciente;
```

**Resultado:** `Linha não encontrada: {'sexo_paciente': 'F', 'custo_medio': 2634.6}`
**TTA:** 5989ms

---

### ❌ Q89 — Financeira

**Q:** Custo total de internações ortopédicas em crianças (menores de 18 anos) em RJ em 2022

**SQL gerado:**
```sql
SELECT SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND idade_paciente < 18
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=1995445.8099999996 exp=5562026.72`
**TTA:** 5763ms

---

### ❌ Q90 — Financeira

**Q:** Custo médio por internação ortopédica com permanência superior a 7 dias em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    AVG(custo_total) AS custo_medio_por_internacao
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND dias_perm > 7
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=4934.2820643432 exp=3699.42`
**TTA:** 5805ms

---

### ❌ Q91 — Financeira

**Q:** Top 3 municípios com maior custo total em internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    cod_munic_residencia, 
    SUM(custo_total) AS total_custo
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'RJ' 
    AND ano_cmpt = 2022 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cod_munic_residencia
ORDER BY 
    total_custo DESC
LIMIT 3;
```

**Resultado:** `Linha 1: got={'cod_munic_residencia': '330455', 'total_custo': 1993166.9300000002} exp={'cod_munic_estabelecimento': '330000', 'custo_total': 26271214.76}`
**TTA:** 6676ms

---

### ❌ Q92 — Financeira

**Q:** Valor total de serviço hospitalar em internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT SUM(val_sh) AS total_servico_hospitalar
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=6937652.409999998 exp=57490872.2`
**TTA:** 5042ms

---

### ❌ Q93 — Financeira

**Q:** Custo médio por internação ortopédica por faixa etária em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    FLOOR(idade_paciente / 10) * 10 AS faixa_etaria,
    AVG(custo_total) AS custo_medio_por_internacao
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    faixa_etaria
ORDER BY 
    faixa_etaria;
```

**Resultado:** `Número de linhas: got=10 exp=3`
**TTA:** 7070ms

---

### ❌ Q94 — Financeira

**Q:** Valor total de UTI em internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT SUM(COALESCE(val_uti, 0)) AS total_uti
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=868901.9800000001 exp=13056994.92`
**TTA:** 4364ms

---

### ✅ Q95 — Financeira

**Q:** Custo total de internações por doenças osteomusculares (M00-M99) em RJ em 2022

**SQL gerado:**
```sql
SELECT SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND e.clinical_interpretacao_clinica = 'doencas osteomusculares e do tecido conjuntivo';
```

**Resultado:** `got=8405202.739999998 exp=8405202.74`
**TTA:** 5721ms

---

### ✅ Q96 — Financeira

**Q:** Custo total de internações por traumatismos (S00-T98) em RJ em 2022

**SQL gerado:**
```sql
SELECT SUM(COALESCE(custo_total, 0)) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'S%' OR icd_group = 'S00-T98');
```

**Resultado:** `got=62488151.48000009 exp=62488151.48`
**TTA:** 4373ms

---

### ❌ Q97 — Financeira

**Q:** Top 5 diagnósticos com maior custo médio por internação ortopédica em RJ em 2022 (mínimo 40 internações)

**SQL gerado:**
```sql
SELECT 
    cid_principal, 
    AVG(custo_total) AS custo_medio, 
    COUNT(DISTINCT n_aih) AS internacoes
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'RJ' 
    AND ano_cmpt = 2022 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cid_principal
HAVING 
    COUNT(DISTINCT n_aih) >= 40
ORDER BY 
    custo_medio DESC
LIMIT 5;
```

**Resultado:** `Linha 2: got={'cid_principal': 'M511', 'custo_medio': 8820.2801550388, 'internacoes': 129} exp={'cid_principal': 'S068', 'internacoes': 55, 'custo_medio': 8934.42}`
**TTA:** 7669ms

---

### ❌ Q98 — Financeira

**Q:** Custo médio por procedimento ambulatorial ortopédico em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    cod_procedimento,
    AVG(COALESCE(custo_total, 0)) AS custo_medio
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIA' 
    AND uf_origem = 'RJ' 
    AND ano_cmpt = 2022 
    AND cod_procedimento LIKE '0408%'
GROUP BY 
    cod_procedimento;
```

**Resultado:** `Resultado vazio`
**TTA:** 5604ms

---

### ❌ Q99 — Financeira

**Q:** Top 5 procedimentos ambulatoriais com maior custo total em RJ em 2022

**SQL gerado:**
```sql
SELECT cod_procedimento, SUM(custo_total) AS total_custo
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
GROUP BY cod_procedimento
ORDER BY total_custo DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cod_procedimento': '0702100048', 'total_custo': 9146187.890000049} exp={'cod_procedimento': '0701020369', 'custo_total': 665532.0}`
**TTA:** 4127ms

---

### ❌ Q100 — Financeira

**Q:** Custo total de internações ortopédicas de idosos (60+) com permanência superior a 7 dias em RJ em 2022

**SQL gerado:**
```sql
SELECT SUM(COALESCE(custo_total, 0)) AS custo_total_ortopedia_idosos
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND idade_paciente >= 60
  AND dias_perm > 7
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=1013984.57 exp=16205065.06`
**TTA:** 6224ms

---

### ❌ Q101 — Temporal/Comparativa

**Q:** Mês com maior número de óbitos em internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT mes_cmpt, COUNT(DISTINCT n_aih) AS num_obitos
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND morte = 1
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY mes_cmpt
ORDER BY num_obitos DESC
LIMIT 1;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 11, 'num_obitos': 4} exp={'mes_cmpt': 1, 'total_obitos': 104}`
**TTA:** 6259ms

---

### ❌ Q102 — Temporal/Comparativa

**Q:** Volume mensal de internações por fratura de fêmur (S72) em idosos (60 anos ou mais) em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND idade_paciente >= 60
    AND (cid_principal LIKE 'S72%' OR e.clinical_interpretacao_clinica = 'lesoes e causas traumaticas')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'volume_internacoes': 705} exp={'mes_cmpt': 1, 'internacoes': 505}`
**TTA:** 7109ms

---

### ❌ Q103 — Temporal/Comparativa

**Q:** Custo mensal de internações ortopédicas de idosos (60 anos ou mais) em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    SUM(COALESCE(custo_total, 0)) AS custo_total
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND idade_paciente >= 60
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'custo_total': 82431.4} exp={'mes_cmpt': 1, 'custo_total': 1749439.39}`
**TTA:** 6803ms

---

### ✅ Q104 — Temporal/Comparativa

**Q:** Mês com maior custo total de internações por fratura de fêmur (S72) em RJ em 2022

**SQL gerado:**
```sql
SELECT mes_cmpt, SUM(custo_total) AS total_custo
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND cid_principal LIKE 'S72%'
GROUP BY mes_cmpt
ORDER BY total_custo DESC
LIMIT 1;
```

**Resultado:** `1 linhas corretas (flexível)`
**TTA:** 5338ms

---

### ✅ Q105 — Temporal/Comparativa

**Q:** Volume mensal de internações por traumatismos (grupo S00-T98) em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'S%' OR icd_group = 'S00-T98')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 6295ms

---

### ❌ Q106 — Temporal/Comparativa

**Q:** Volume mensal de procedimentos ambulatoriais ortopédicos por sexo em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    sexo_paciente,
    COUNT(*) AS volume_procedimentos
FROM 
    processed
WHERE 
    sistema = 'SIA' 
    AND uf_origem = 'RJ' 
    AND ano_cmpt = 2022 
    AND cod_procedimento LIKE '0408%'
GROUP BY 
    mes_cmpt, sexo_paciente
ORDER BY 
    mes_cmpt, sexo_paciente;
```

**Resultado:** `Número de linhas: got=0 exp=24`
**TTA:** 5318ms

---

### ❌ Q107 — Temporal/Comparativa

**Q:** Comparativo trimestral de internações ortopédicas por grupo diagnóstico em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    icd_group,
    COUNT(DISTINCT n_aih) AS num_internacoes
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt, icd_group
ORDER BY 
    mes_cmpt, icd_group;
```

**Resultado:** `Número de linhas: got=12 exp=8`
**TTA:** 6787ms

---

### ❌ Q108 — Temporal/Comparativa

**Q:** Mês com maior permanência hospitalar média em internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT mes_cmpt, AVG(dias_perm) AS media_permanencia
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND cid_principal LIKE 'M%'
  AND sistema = 'SIH'
GROUP BY mes_cmpt
ORDER BY media_permanencia DESC
LIMIT 1;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 3, 'media_permanencia': 11.5039370079} exp={'mes_cmpt': 1, 'permanencia_media': 9.9}`
**TTA:** 5326ms

---

### ❌ Q109 — Temporal/Comparativa

**Q:** Evolução mensal da taxa de mortalidade em internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    COUNT(DISTINCT CASE WHEN morte = 1 THEN n_aih END) / COUNT(DISTINCT n_aih) AS taxa_mortalidade
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'taxa_mortalidade': 0.0165289256} exp={'mes_cmpt': 1, 'internacoes': 1998, 'obitos': 104.0, 'taxa_mortalidade': 5.21}`
**TTA:** 7000ms

---

### ❌ Q110 — Temporal/Comparativa

**Q:** Trimestre com maior volume de internações por fratura de fêmur (S72) em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND cid_principal LIKE 'S72%'
GROUP BY 
    mes_cmpt
ORDER BY 
    volume_internacoes DESC
LIMIT 1;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 10, 'volume_internacoes': 941} exp={'trimestre': 'Q3', 'total': 2707}`
**TTA:** 6051ms

---

### ❌ Q111 — Temporal/Comparativa

**Q:** Comparativo semestral de internações ortopédicas (1º vs 2º semestre) em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    COUNT(DISTINCT n_aih) AS num_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `Número de linhas: got=12 exp=2`
**TTA:** 5317ms

---

### ❌ Q112 — Temporal/Comparativa

**Q:** Custo médio mensal por internação por fratura de fêmur (S72) em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    AVG(custo_total) AS custo_medio_mensal
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND cid_principal LIKE 'S72%'
```

**Resultado:** `Número de linhas: got=1 exp=12`
**TTA:** 4973ms

---

### ❌ Q113 — Temporal/Comparativa

**Q:** Volume mensal de internações ortopédicas por sexo do paciente em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    sexo_paciente,
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt, sexo_paciente
ORDER BY 
    mes_cmpt, sexo_paciente;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'sexo_paciente': 'F', 'volume_internacoes': 53} exp={'mes_cmpt': 1, 'sexo_paciente': 'F', 'total': 815}`
**TTA:** 6799ms

---

### ❌ Q114 — Temporal/Comparativa

**Q:** Mês com menor volume de procedimentos ambulatoriais ortopédicos em RJ em 2022

**SQL gerado:**
```sql
SELECT mes_cmpt
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY mes_cmpt
ORDER BY COUNT(*) ASC
LIMIT 1;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 4} exp={'mes_cmpt': 1, 'total': 1647}`
**TTA:** 5359ms

---

### ❌ Q115 — Temporal/Comparativa

**Q:** Volume trimestral de procedimentos ambulatoriais ortopédicos em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    COUNT(*) AS volume_procedimentos
FROM 
    processed
WHERE 
    sistema = 'SIA' 
    AND uf_origem = 'RJ' 
    AND ano_cmpt = 2022 
    AND cod_procedimento LIKE '0408%'
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `Número de linhas: got=0 exp=4`
**TTA:** 4804ms

---

### ❌ Q116 — Temporal/Comparativa

**Q:** Custo mensal de procedimentos ambulatoriais ortopédicos em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    SUM(custo_total) AS custo_total
FROM 
    processed
WHERE 
    sistema = 'SIA' 
    AND uf_origem = 'RJ' 
    AND ano_cmpt = 2022 
    AND cod_procedimento LIKE '0408%'
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `Número de linhas: got=0 exp=12`
**TTA:** 4884ms

---

### ❌ Q117 — Temporal/Comparativa

**Q:** Comparativo semestral do custo total de internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    SUM(COALESCE(custo_total, 0)) AS custo_total
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `Número de linhas: got=12 exp=2`
**TTA:** 5440ms

---

### ❌ Q118 — Temporal/Comparativa

**Q:** Evolução mensal da permanência média em internações por fratura de fêmur (S72) em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    AVG(dias_perm) AS media_permanencia
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'S72%' OR e.clinical_interpretacao_clinica = 'lesoes e causas traumaticas')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'media_permanencia': 9.8391049547} exp={'mes_cmpt': 1, 'permanencia_media': 10.8}`
**TTA:** 6596ms

---

## Failures Detail

- **Q02** (Epidemiológica Simples): got=0 exp=27201
  SQL: `SELECT COUNT(*) AS total_procedimentos
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SI...`
- **Q03** (Epidemiológica Simples): got=25731 exp=9945
  SQL: `SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE si...`
- **Q05** (Epidemiológica Simples): got=21 exp=965
  SQL: `SELECT COUNT(DISTINCT n_aih) AS num_obitos
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema =...`
- **Q08** (Epidemiológica Simples): got=880 exp=10444
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE s...`
- **Q09** (Epidemiológica Simples): got=1445 exp=17612
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE s...`
- **Q10** (Epidemiológica Simples): got=586 exp=10149
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE s...`
- **Q11** (Epidemiológica Simples): got=102 exp=63
  SQL: `SELECT COUNT(DISTINCT cod_munic_residencia) AS num_municipios
FROM processed
JOIN enriched e ON processed.row_id = e.row...`
- **Q12** (Epidemiológica Simples): got=46 exp=135
  SQL: `SELECT COUNT(DISTINCT cod_munic_estabelecimento) AS num_estabelecimentos
FROM processed
WHERE sistema = 'SIH'
  AND uf_o...`
- **Q13** (Epidemiológica Simples): got=734 exp=9244
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano...`
- **Q15** (Epidemiológica Simples): got=6365 exp=5140
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE s...`
- **Q16** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'M861', 'internacoes_unicas': 182} exp={'cid_principal': 'S720', 'total_internacoes': 3502}
  SQL: `SELECT 
    cid_principal, 
    COUNT(DISTINCT n_aih) AS internacoes_unicas
FROM 
    processed
JOIN 
    enriched e ON ...`
- **Q17** (Epidemiológica Complexa): Linha 1: got={'cod_munic_residencia': '330455', 'num_internacoes': 514} exp={'cod_munic_estabelecimento': '330000', 'internacoes': 9878}
  SQL: `SELECT 
    cod_munic_residencia,
    COUNT(DISTINCT n_aih) AS num_internacoes
FROM 
    processed
JOIN 
    enriched e ...`
- **Q18** (Epidemiológica Complexa): Linha não encontrada: {'faixa_etaria': '0-17', 'total': 2380}
  SQL: `SELECT 
    CASE 
        WHEN idade_paciente BETWEEN 0 AND 17 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 59...`
- **Q19** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'M858', 'total_internacoes': 1, 'total_obitos': 1.0, 'taxa_mortalidade': 100.0} exp={'cid_principal': 'T203', 'total_internacoes': 44, 'total_obitos': 26.0, 'taxa_mortalidade_pct': 59.09}
  SQL: `SELECT 
    cid_principal,
    COUNT(DISTINCT n_aih) AS total_internacoes,
    SUM(morte) AS total_obitos,
    (SUM(mort...`
- **Q20** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'M216', 'volume': 1963} exp={'cid_principal': 'S720', 'total_procedimentos': 5570}
  SQL: `SELECT 
    cid_principal, 
    COUNT(*) AS volume
FROM 
    processed
WHERE 
    sistema = 'SIA' 
    AND uf_origem = '...`
- **Q21** (Epidemiológica Complexa): Linha não encontrada: {'sexo_paciente': 'M', 'total_internacoes': 17612, 'pct': 62.8}
  SQL: `SELECT 
    sexo_paciente,
    COUNT(DISTINCT n_aih) AS internacoes,
    COUNT(DISTINCT n_aih) * 1.0 / SUM(COUNT(DISTINC...`
- **Q22** (Epidemiológica Complexa): Linha 1: got={'internacoes_fratura_femur': 6628, 'total_internacoes': 69787} exp={'total_s72': 9945, 's72_idosos': 6628.0, 'pct_idosos': 66.6}
  SQL: `WITH fratura_femur AS (
    SELECT COUNT(DISTINCT n_aih) AS num_internacoes
    FROM processed
    JOIN enriched e ON pr...`
- **Q23** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'M463', 'media_permanencia': 57.5} exp={'cid_principal': 'T203', 'total_internacoes': 44, 'permanencia_media_dias': 30.1}
  SQL: `SELECT 
    cid_principal, 
    AVG(dias_perm) AS media_permanencia
FROM 
    processed
JOIN 
    enriched e ON processe...`
- **Q24** (Epidemiológica Complexa): Linha 1: got={'cod_munic_estabelecimento': '330000', 'volume_internacao': 1007} exp={'cnes_estabelecimento': '2290227', 'internacoes': 5043}
  SQL: `SELECT 
    cod_munic_estabelecimento, 
    COUNT(DISTINCT n_aih) AS volume_internacao
FROM 
    processed
WHERE 
    si...`
- **Q25** (Epidemiológica Complexa): Número de linhas: got=5 exp=6
  SQL: `SELECT 
    raca_cor_paciente, 
    COUNT(DISTINCT n_aih) AS internacoes_ortopédicas
FROM 
    processed
JOIN 
    enric...`
- **Q26** (Epidemiológica Complexa): Número de linhas: got=1 exp=10
  SQL: `SELECT 
    cid_principal, 
    SUM(qt_diarias) AS total_dias_internacao
FROM 
    processed
JOIN 
    enriched e ON pro...`
- **Q27** (Epidemiológica Complexa): Número de linhas: got=0 exp=5
  SQL: `SELECT 
    cod_procedimento, 
    COUNT(*) AS quantidade_realizada
FROM 
    processed
WHERE 
    sistema = 'SIA' 
    ...`
- **Q28** (Epidemiológica Complexa): Falha DuckDB: Binder Error: GROUP BY clause cannot contain aggregates!

LINE 3:     COUNT(DISTINCT p.n_aih) AS total_internacoes
            ^
  SQL: `SELECT 
    SUM(CASE WHEN e.clinical_deslocamento_territorial = 'deslocamento interestadual' THEN 1 ELSE 0 END) AS inter...`
- **Q29** (Epidemiológica Complexa): Linha 1: got={'sistema': 'SIA', 'mes_cmpt': 1, 'volume_mensal': 208} exp={'mes_cmpt': 1, 'sistema': 'SIA', 'total': 1647}
  SQL: `SELECT 
    sistema,
    mes_cmpt,
    COUNT(*) AS volume_mensal
FROM 
    processed
WHERE 
    uf_origem = 'RJ'
    AND...`
- **Q30** (Epidemiológica Complexa): Linha 1: got={'idade_media': 48.9025300222} exp={'idade_media': 63.3, 'idade_minima': 0.0, 'idade_maxima': 99.0}
  SQL: `SELECT AVG(idade_paciente) AS idade_media
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = ...`
- **Q31** (Financeira): got=8405202.739999998 exp=70893354.22
  SQL: `SELECT SUM(custo_total) AS total_custo
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022...`
- **Q32** (Financeira): got=3596.5779803166 exp=2525.77
  SQL: `SELECT 
    AVG(COALESCE(custo_total, 0)) AS custo_medio_por_internacao
FROM 
    processed
WHERE 
    sistema = 'SIH'
 ...`
- **Q33** (Financeira): got=62488151.48000009 exp=26337280.38
  SQL: `SELECT SUM(COALESCE(custo_total, 0)) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND an...`
- **Q34** (Financeira): Linha 1: got={'cid_principal': 'M511', 'total_custo': 1137816.14} exp={'cid_principal': 'S720', 'internacoes': 3502, 'custo_total': 10821184.25}
  SQL: `SELECT 
    cid_principal, 
    SUM(custo_total) AS total_custo
FROM 
    processed
JOIN 
    enriched e ON processed.ro...`
- **Q35** (Financeira): Linha 1: got={'custo_total_ortopedia': None} exp={'internacoes_com_opme': 0, 'custo_total_opme': None}
  SQL: `SELECT SUM(COALESCE(val_ortp, 0)) AS custo_total_ortopedia
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
 ...`
- **Q36** (Financeira): got=1467550.3299999994 exp=13402482.02
  SQL: `SELECT COALESCE(SUM(val_sp), 0) AS total_honorarios
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE ...`
- **Q37** (Financeira): got=371.8023020967 exp=321.98
  SQL: `SELECT 
    AVG(COALESCE(val_uti, 0)) AS custo_medio_por_dia
FROM 
    processed
JOIN 
    enriched e ON processed.row_i...`
- **Q38** (Financeira): Linha 1: got={'cod_munic_estabelecimento': '330000', 'total_custo': 3740518.5499999993} exp={'cnes_estabelecimento': '2290227', 'internacoes': 5043, 'custo_total': 9418220.5}
  SQL: `SELECT 
    cod_munic_estabelecimento, 
    SUM(custo_total) AS total_custo
FROM 
    processed
WHERE 
    sistema = 'SI...`
- **Q39** (Financeira): got=867233.2599999987 exp=4149578.04
  SQL: `SELECT SUM(COALESCE(custo_total, 0)) AS total_custo
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND an...`
- **Q40** (Financeira): Linha 1: got={'proporcao_sh_sp': 4.727369323} exp={'custo_servico_hospitalar': 57490872.2, 'honorarios_profissionais': 13402482.02, 'pct_hospitalar': 81.1}
  SQL: `SELECT 
    SUM(COALESCE(val_sh, 0)) / SUM(COALESCE(val_sp, 0)) AS proporcao_sh_sp
FROM 
    processed
JOIN 
    enriche...`
- **Q41** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'internacoes_ortopédicas': 121} exp={'mes_cmpt': 1, 'total_internacoes': 1998}
  SQL: `SELECT 
    mes_cmpt,
    COUNT(DISTINCT n_aih) AS internacoes_ortopédicas
FROM 
    processed
JOIN 
    enriched e ON p...`
- **Q42** (Temporal/Comparativa): Número de linhas: got=0 exp=12
  SQL: `SELECT 
    mes_cmpt,
    COUNT(*) AS quantidade_procedimentos
FROM 
    processed
JOIN 
    enriched e ON processed.row...`
- **Q44** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'custo_total': 515324.59} exp={'mes_cmpt': 1, 'custo_total': 4528720.12}
  SQL: `SELECT 
    mes_cmpt,
    SUM(COALESCE(custo_total, 0)) AS custo_total
FROM 
    processed
JOIN 
    enriched e ON proce...`
- **Q45** (Temporal/Comparativa): Número de linhas: got=12 exp=24
  SQL: `SELECT 
    mes_cmpt,
    COUNT(DISTINCT CASE WHEN icd_group = 'S00-T98' THEN n_aih END) AS trauma_internacoes,
    COUN...`
- **Q46** (Temporal/Comparativa): Número de linhas: got=11 exp=12
  SQL: `SELECT 
    mes_cmpt,
    COUNT(DISTINCT n_aih) AS obitos
FROM 
    processed
JOIN 
    enriched e ON processed.row_id =...`
- **Q47** (Temporal/Comparativa): Número de linhas: got=12 exp=4
  SQL: `SELECT 
    AVG(dias_perm) AS media_permanencia,
    mes_cmpt
FROM 
    processed
JOIN 
    enriched e ON processed.row_...`
- **Q48** (Temporal/Comparativa): got=6 exp=11
  SQL: `SELECT mes_cmpt, AVG(custo_total) AS custo_medio
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sis...`
- **Q49** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'count_fraturas_femur': 739} exp={'mes_cmpt': 1, 'total_fraturas': 770}
  SQL: `SELECT 
    mes_cmpt,
    COUNT(*) AS count_fraturas_femur
FROM 
    processed
WHERE 
    sistema = 'SIA' 
    AND uf_or...`
- **Q50** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'volume_internacoes': 26} exp={'mes_cmpt': 1, 'total_internacoes': 731}
  SQL: `SELECT 
    mes_cmpt,
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
JOIN 
    enriched e ON proces...`
- **Q51** (Epidemiológica Simples): got=9564 exp=5436
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE s...`
- **Q52** (Epidemiológica Simples): got=16167 exp=4509
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE s...`
- **Q53** (Epidemiológica Simples): got=373 exp=5619
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano...`
- **Q54** (Epidemiológica Simples): got=0 exp=12838
  SQL: `SELECT COUNT(*) AS numero_procedimentos
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE processed.si...`
- **Q55** (Epidemiológica Simples): got=0 exp=13298
  SQL: `SELECT COUNT(*) AS numero_procedimentos
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'S...`
- **Q56** (Epidemiológica Simples): got=25731 exp=2991
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE s...`
- **Q57** (Epidemiológica Simples): got=25731 exp=1447
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE s...`
- **Q58** (Epidemiológica Simples): got=25731 exp=765
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE s...`
- **Q59** (Epidemiológica Simples): got=358 exp=2380
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE s...`
- **Q60** (Epidemiológica Simples): got=604 exp=8235
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE s...`
- **Q62** (Epidemiológica Simples): got=4487 exp=3841
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE s...`
- **Q63** (Epidemiológica Simples): got=131 exp=1010
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano...`
- **Q64** (Epidemiológica Simples): got=0 exp=14363
  SQL: `SELECT COUNT(*) AS numero_procedimentos
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE processed.si...`
- **Q68** (Epidemiológica Simples): got=0 exp=74
  SQL: `SELECT COUNT(DISTINCT cod_munic_residencia) AS num_municipios_distintos
FROM processed
JOIN enriched e ON processed.row_...`
- **Q69** (Epidemiológica Complexa): Número de linhas: got=0 exp=5
  SQL: `SELECT 
    cod_munic_residencia,
    COUNT(*) AS volume_procedimentos
FROM 
    processed
JOIN 
    enriched e ON proce...`
- **Q70** (Epidemiológica Complexa): Número de linhas: got=0 exp=3
  SQL: `SELECT 
    FLOOR(idade_paciente / 10) * 10 AS faixa_etaria,
    COUNT(*) AS quantidade_procedimentos
FROM 
    processe...`
- **Q71** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'M869', 'num_obitos': 5} exp={'cid_principal': 'S720', 'total_internacoes': 3502, 'total_obitos': 187.0}
  SQL: `SELECT 
    cid_principal, 
    COUNT(*) AS num_obitos
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e....`
- **Q72** (Epidemiológica Complexa): Linha não encontrada: {'sexo_paciente': 'F', 'permanencia_media': 8.6}
  SQL: `SELECT 
    sexo_paciente,
    AVG(dias_perm) AS media_permanencia
FROM 
    processed
JOIN 
    enriched e ON processed...`
- **Q73** (Epidemiológica Complexa): Número de linhas: got=0 exp=10
  SQL: `SELECT 
    cod_munic_estabelecimento, 
    COUNT(*) AS volume_procedimentos
FROM 
    processed
WHERE 
    sistema = 'S...`
- **Q74** (Epidemiológica Complexa): Linha não encontrada: {'sexo_paciente': 'F', 'total_obitos': 412.0, 'taxa_mortalidade': 3.94}
  SQL: `SELECT 
    sexo_paciente,
    SUM(morte) AS total_obitos,
    COUNT(DISTINCT n_aih) AS total_internacoes,
    (SUM(mort...`
- **Q75** (Epidemiológica Complexa): Número de linhas: got=16 exp=6
  SQL: `SELECT 
    icd_group,
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente < 30 THEN '18-...`
- **Q76** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'T203', 'media_permanencia': 30.0681818182, 'internacoes': 44} exp={'cid_principal': 'T203', 'internacoes': 44, 'permanencia_media': 30.1}
  SQL: `SELECT 
    cid_principal,
    AVG(dias_perm) AS media_permanencia,
    COUNT(DISTINCT n_aih) AS internacoes
FROM 
    p...`
- **Q77** (Epidemiológica Complexa): Linha não encontrada: {'faixa_etaria': '0-17', 'total': 2380}
  SQL: `SELECT 
  CASE 
    WHEN idade_paciente BETWEEN 0 AND 17 THEN '0-17'
    WHEN idade_paciente BETWEEN 18 AND 39 THEN '18-...`
- **Q78** (Epidemiológica Complexa): Linha 1: got={'cod_munic_estabelecimento': '330000', 'volume_internacoes': 8871} exp={'cnes_estabelecimento': '2290227', 'internacoes': 788}
  SQL: `SELECT 
    cod_munic_estabelecimento, 
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
JOIN 
    en...`
- **Q79** (Epidemiológica Complexa): Número de linhas: got=7 exp=3
  SQL: `SELECT 
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente < 30 THEN '18-29'
        WHE...`
- **Q81** (Epidemiológica Complexa): got=717 exp=9617
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano...`
- **Q82** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'M216', 'quantidade': 729} exp={'cid_principal': 'S720', 'total': 3056}
  SQL: `SELECT 
    cid_principal, 
    COUNT(*) AS quantidade
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e....`
- **Q83** (Epidemiológica Complexa): Falha DuckDB: Binder Error: aggregate function calls cannot be nested

LINE 3:     AVG(COUNT(DISTINCT n_aih)) AS media_internacoes
                ^
  SQL: `SELECT 
    cod_munic_estabelecimento,
    AVG(COUNT(DISTINCT n_aih)) AS media_internacoes
FROM 
    processed
WHERE 
  ...`
- **Q84** (Epidemiológica Complexa): Número de linhas: got=6 exp=4
  SQL: `SELECT 
    CASE 
        WHEN dias_perm < 5 THEN '0-4 dias'
        WHEN dias_perm < 10 THEN '5-9 dias'
        WHEN di...`
- **Q85** (Financeira): got=2115629.9599999995 exp=28396358.5
  SQL: `SELECT SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = '...`
- **Q86** (Financeira): got=2428.516244219 exp=2648.29
  SQL: `SELECT 
    AVG(custo_total) AS custo_medio_por_internacao
FROM 
    processed
JOIN 
    enriched e ON processed.row_id ...`
- **Q88** (Financeira): Linha não encontrada: {'sexo_paciente': 'F', 'custo_medio': 2634.6}
  SQL: `SELECT 
    sexo_paciente,
    AVG(custo_total) AS custo_medio_por_internacao
FROM 
    processed
JOIN 
    enriched e O...`
- **Q89** (Financeira): got=1995445.8099999996 exp=5562026.72
  SQL: `SELECT SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.r...`
- **Q90** (Financeira): got=4934.2820643432 exp=3699.42
  SQL: `SELECT 
    AVG(custo_total) AS custo_medio_por_internacao
FROM 
    processed
JOIN 
    enriched e ON processed.row_id ...`
- **Q91** (Financeira): Linha 1: got={'cod_munic_residencia': '330455', 'total_custo': 1993166.9300000002} exp={'cod_munic_estabelecimento': '330000', 'custo_total': 26271214.76}
  SQL: `SELECT 
    cod_munic_residencia, 
    SUM(custo_total) AS total_custo
FROM 
    processed
JOIN 
    enriched e ON proce...`
- **Q92** (Financeira): got=6937652.409999998 exp=57490872.2
  SQL: `SELECT SUM(val_sh) AS total_servico_hospitalar
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE siste...`
- **Q93** (Financeira): Número de linhas: got=10 exp=3
  SQL: `SELECT 
    FLOOR(idade_paciente / 10) * 10 AS faixa_etaria,
    AVG(custo_total) AS custo_medio_por_internacao
FROM 
  ...`
- **Q94** (Financeira): got=868901.9800000001 exp=13056994.92
  SQL: `SELECT SUM(COALESCE(val_uti, 0)) AS total_uti
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt...`
- **Q97** (Financeira): Linha 2: got={'cid_principal': 'M511', 'custo_medio': 8820.2801550388, 'internacoes': 129} exp={'cid_principal': 'S068', 'internacoes': 55, 'custo_medio': 8934.42}
  SQL: `SELECT 
    cid_principal, 
    AVG(custo_total) AS custo_medio, 
    COUNT(DISTINCT n_aih) AS internacoes
FROM 
    pro...`
- **Q98** (Financeira): Resultado vazio
  SQL: `SELECT 
    cod_procedimento,
    AVG(COALESCE(custo_total, 0)) AS custo_medio
FROM 
    processed
JOIN 
    enriched e ...`
- **Q99** (Financeira): Linha 1: got={'cod_procedimento': '0702100048', 'total_custo': 9146187.890000049} exp={'cod_procedimento': '0701020369', 'custo_total': 665532.0}
  SQL: `SELECT cod_procedimento, SUM(custo_total) AS total_custo
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  A...`
- **Q100** (Financeira): got=1013984.57 exp=16205065.06
  SQL: `SELECT SUM(COALESCE(custo_total, 0)) AS custo_total_ortopedia_idosos
FROM processed
JOIN enriched e ON processed.row_id ...`
- **Q101** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 11, 'num_obitos': 4} exp={'mes_cmpt': 1, 'total_obitos': 104}
  SQL: `SELECT mes_cmpt, COUNT(DISTINCT n_aih) AS num_obitos
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE...`
- **Q102** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'volume_internacoes': 705} exp={'mes_cmpt': 1, 'internacoes': 505}
  SQL: `SELECT 
    mes_cmpt,
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
JOIN 
    enriched e ON proces...`
- **Q103** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'custo_total': 82431.4} exp={'mes_cmpt': 1, 'custo_total': 1749439.39}
  SQL: `SELECT 
    mes_cmpt,
    SUM(COALESCE(custo_total, 0)) AS custo_total
FROM 
    processed
JOIN 
    enriched e ON proce...`
- **Q106** (Temporal/Comparativa): Número de linhas: got=0 exp=24
  SQL: `SELECT 
    mes_cmpt,
    sexo_paciente,
    COUNT(*) AS volume_procedimentos
FROM 
    processed
WHERE 
    sistema = '...`
- **Q107** (Temporal/Comparativa): Número de linhas: got=12 exp=8
  SQL: `SELECT 
    mes_cmpt,
    icd_group,
    COUNT(DISTINCT n_aih) AS num_internacoes
FROM 
    processed
JOIN 
    enriched...`
- **Q108** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 3, 'media_permanencia': 11.5039370079} exp={'mes_cmpt': 1, 'permanencia_media': 9.9}
  SQL: `SELECT mes_cmpt, AVG(dias_perm) AS media_permanencia
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE...`
- **Q109** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'taxa_mortalidade': 0.0165289256} exp={'mes_cmpt': 1, 'internacoes': 1998, 'obitos': 104.0, 'taxa_mortalidade': 5.21}
  SQL: `SELECT 
    mes_cmpt,
    COUNT(DISTINCT CASE WHEN morte = 1 THEN n_aih END) / COUNT(DISTINCT n_aih) AS taxa_mortalidade...`
- **Q110** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 10, 'volume_internacoes': 941} exp={'trimestre': 'Q3', 'total': 2707}
  SQL: `SELECT 
    mes_cmpt,
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
JOIN 
    enriched e ON proces...`
- **Q111** (Temporal/Comparativa): Número de linhas: got=12 exp=2
  SQL: `SELECT 
    mes_cmpt,
    COUNT(DISTINCT n_aih) AS num_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AN...`
- **Q112** (Temporal/Comparativa): Número de linhas: got=1 exp=12
  SQL: `SELECT 
    AVG(custo_total) AS custo_medio_mensal
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_...`
- **Q113** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'sexo_paciente': 'F', 'volume_internacoes': 53} exp={'mes_cmpt': 1, 'sexo_paciente': 'F', 'total': 815}
  SQL: `SELECT 
    mes_cmpt,
    sexo_paciente,
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
JOIN 
    e...`
- **Q114** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 4} exp={'mes_cmpt': 1, 'total': 1647}
  SQL: `SELECT mes_cmpt
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIA'
  AND uf_origem = 'R...`
- **Q115** (Temporal/Comparativa): Número de linhas: got=0 exp=4
  SQL: `SELECT 
    mes_cmpt,
    COUNT(*) AS volume_procedimentos
FROM 
    processed
WHERE 
    sistema = 'SIA' 
    AND uf_or...`
- **Q116** (Temporal/Comparativa): Número de linhas: got=0 exp=12
  SQL: `SELECT 
    mes_cmpt,
    SUM(custo_total) AS custo_total
FROM 
    processed
WHERE 
    sistema = 'SIA' 
    AND uf_ori...`
- **Q117** (Temporal/Comparativa): Número de linhas: got=12 exp=2
  SQL: `SELECT 
    mes_cmpt,
    SUM(COALESCE(custo_total, 0)) AS custo_total
FROM 
    processed
WHERE 
    sistema = 'SIH'
  ...`
- **Q118** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'media_permanencia': 9.8391049547} exp={'mes_cmpt': 1, 'permanencia_media': 10.8}
  SQL: `SELECT 
    mes_cmpt,
    AVG(dias_perm) AS media_permanencia
FROM 
    processed
JOIN 
    enriched e ON processed.row_...`

---
_Gerado por scripts/evaluate_benchmark.py — SUS Data RAG — USF/Mestrado_