# Benchmark Evaluation Report — SUS Data RAG

**Model:** ollama  
**Run date:** 2026-08-22 23:01  
**API:** http://127.0.0.1:8000  

## Summary

| Metric | Value |
|---|---|
| Total queries | 118 |
| Correct (EA numerator) | 16 |
| **Execution Accuracy (EA)** | **13.6%** |
| Wilson IC 95% | [8.5%, 20.9%] |
| Correct via flexible match (alias diferente) | 11 |
| API errors / timeouts | 1 |
| Mean Time-to-Answer | 6.2s |

> **Nota metodológica — Scoring:** A Execution Accuracy avalia se o SQL gerado
> retorna os mesmos **valores** que o gold-standard, independente do nome das colunas.
> Quando o LLM usa um alias diferente mas retorna os dados corretos (match posicional),
> a query é contada como correta e sinalizada com `~`. Esta decisão segue a definição
> de EA em [Lee et al., 2022] — comparação de conjuntos de resultado, não de SQL texto.

## EA by Category

| Category | Correct | Total | EA |
|---|---:|---:|---:|
| Epidemiológica Simples | 10 | 33 | 30.3% |
| Epidemiológica Complexa | 1 | 31 | 3.2% |
| Financeira | 3 | 26 | 11.5% |
| Temporal/Comparativa | 2 | 28 | 7.1% |

## Query Results

### ✅ Q01 — Epidemiológica Simples

**Q:** Total de internações ortopédicas (M00-M99 e S00-S99) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=69487 exp=69487`
**TTA:** 5360ms

---

### ❌ Q02 — Epidemiológica Simples

**Q:** Total de procedimentos ambulatoriais ortopédicos em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(*) AS total_procedimentos
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
  AND clinical_interpretacao_clinica = 'doencas osteomusculares e do tecido conjuntivo';
```

**Resultado:** `got=11834 exp=105364`
**TTA:** 5932ms

---

### ❌ Q03 — Epidemiológica Simples

**Q:** Total de internações por fratura de fêmur (S72) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'S72%' OR e.clinical_interpretacao_clinica = 'lesoes e causas traumaticas');
```

**Resultado:** `got=62607 exp=30272`
**TTA:** 5541ms

---

### ✅ Q04 — Epidemiológica Simples

**Q:** Total de internações por osteoartrose (M16, M17) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'M16%' OR cid_principal LIKE 'M17%');
```

**Resultado:** `got=165 exp=165`
**TTA:** 5067ms

---

### ❌ Q05 — Epidemiológica Simples

**Q:** Número de óbitos em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS num_obitos
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND morte = 1
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=81 exp=2274`
**TTA:** 5369ms

---

### ✅ Q06 — Epidemiológica Simples

**Q:** Total de internações por traumatismos (S00-T98) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'S%' OR icd_group = 'S00-T98');
```

**Resultado:** `got=62607 exp=62607`
**TTA:** 5115ms

---

### ✅ Q07 — Epidemiológica Simples

**Q:** Total de internações por doenças osteomusculares (M00-M99) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND e.clinical_interpretacao_clinica = 'doencas osteomusculares e do tecido conjuntivo';
```

**Resultado:** `got=6880 exp=6880`
**TTA:** 5455ms

---

### ❌ Q08 — Epidemiológica Simples

**Q:** Número de mulheres internadas por causa ortopédica em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND sexo_paciente = 'F'
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=2554 exp=27027`
**TTA:** 5444ms

---

### ⚠️ Q09 — Epidemiológica Simples

**Q:** Número de homens internados por causa ortopédica em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT e.n_aih) AS numero_internacoes
FROM processed p
JOIN enriched e ON p.row_id = e.row_id
WHERE p.sistema = 'SIH'
  AND p.uf_origem = 'SP'
  AND p.ano_cmpt = 2023
  AND p.sexo_paciente = 'M'
  AND (p.cid_principal LIKE 'M%' OR p.icd_group = 'M00-M99');
```

**Erro:** Falha DuckDB: Binder Error: Values list "e" does not have a column named "n_aih"

LINE 1: SELECT COUNT(DISTINCT e.n_aih) AS numero_internacoes
                              ^

---

### ❌ Q10 — Epidemiológica Simples

**Q:** Número de internações ortopédicas de idosos (60 anos ou mais) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND idade_paciente >= 60
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=1912 exp=28165`
**TTA:** 5501ms

---

### ❌ Q11 — Epidemiológica Simples

**Q:** Número de municípios distintos com internação ortopédica registrada em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT cod_munic_residencia) AS num_municipios_distintos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=508 exp=215`
**TTA:** 4463ms

---

### ❌ Q12 — Epidemiológica Simples

**Q:** Número de estabelecimentos distintos com internação ortopédica em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT cod_munic_estabelecimento) AS num_estabelecimentos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=166 exp=385`
**TTA:** 4409ms

---

### ❌ Q13 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com permanência superior a 7 dias em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND dias_perm > 7
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=1521 exp=17778`
**TTA:** 4542ms

---

### ❌ Q14 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com uso de UTI em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND sistema = 'SIH'
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
  AND uti_int_to > 0;
```

**Resultado:** `got=0 exp=1`
**TTA:** 5197ms

---

### ❌ Q15 — Epidemiológica Simples

**Q:** Número de internações por fratura de quadril (S72) em pacientes com 70 anos ou mais em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND idade_paciente >= 70
  AND (cid_principal LIKE 'S72%' OR e.clinical_interpretacao_clinica = 'lesoes e causas traumaticas');
```

**Resultado:** `got=18446 exp=15626`
**TTA:** 5930ms

---

### ❌ Q16 — Epidemiológica Complexa

**Q:** Top 10 CIDs ortopédicos por volume de internação em SP em 2023

**SQL gerado:**
```sql
SELECT 
    cid_principal, 
    COUNT(DISTINCT n_aih) AS internacoes_unicas
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2023 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cid_principal
ORDER BY 
    internacoes_unicas DESC
LIMIT 10;
```

**Resultado:** `Linha 1: got={'cid_principal': 'M861', 'internacoes_unicas': 957} exp={'cid_principal': 'S720', 'total_internacoes': 9059}`
**TTA:** 6659ms

---

### ❌ Q17 — Epidemiológica Complexa

**Q:** Top 5 municípios de SP com maior número de internações ortopédicas em 2023

**SQL gerado:**
```sql
SELECT 
    cod_munic_residencia, 
    COUNT(DISTINCT n_aih) AS num_internacoes
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2023 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cod_munic_residencia
ORDER BY 
    num_internacoes DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cod_munic_residencia': '355030', 'num_internacoes': 1240} exp={'cod_munic_estabelecimento': '350000', 'internacoes': 32140}`
**TTA:** 6840ms

---

### ❌ Q18 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por faixa etária (0-17, 18-59, 60+) em SP em 2023

**SQL gerado:**
```sql
SELECT 
    CASE 
        WHEN idade_paciente BETWEEN 0 AND 17 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 59 THEN '18-59'
        ELSE '60+'
    END AS faixa_etaria,
    COUNT(DISTINCT n_aih) AS quantidade_internacoes
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    faixa_etaria
ORDER BY 
    faixa_etaria;
```

**Resultado:** `Linha não encontrada: {'faixa_etaria': '0-17', 'total': 4040}`
**TTA:** 8432ms

---

### ❌ Q19 — Epidemiológica Complexa

**Q:** Taxa de mortalidade hospitalar por CID ortopédico (top 5) em SP em 2023

**SQL gerado:**
```sql
SELECT 
    cid_principal,
    COUNT(DISTINCT n_aih) AS total_internacoes,
    SUM(morte) AS total_obitos,
    (SUM(morte) * 1.0 / COUNT(DISTINCT n_aih)) * 100 AS taxa_mortalidade
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cid_principal
ORDER BY 
    taxa_mortalidade DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cid_principal': 'M463', 'total_internacoes': 6, 'total_obitos': 2.0, 'taxa_mortalidade': 33.3333333333} exp={'cid_principal': 'S062', 'total_internacoes': 58, 'total_obitos': 20.0, 'taxa_mortalidade_pct': 34.48}`
**TTA:** 8027ms

---

### ❌ Q20 — Epidemiológica Complexa

**Q:** Top 10 CIDs ambulatoriais ortopédicos por volume de produção em SP em 2023

**SQL gerado:**
```sql
SELECT 
    cid_principal, 
    COUNT(*) AS volume
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIA' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2023 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cid_principal
ORDER BY 
    volume DESC
LIMIT 10;
```

**Resultado:** `Linha 1: got={'cid_principal': 'M216', 'volume': 5349} exp={'cid_principal': 'S720', 'total_procedimentos': 17325}`
**TTA:** 6104ms

---

### ❌ Q21 — Epidemiológica Complexa

**Q:** Proporção de internações ortopédicas por sexo em SP em 2023

**SQL gerado:**
```sql
SELECT 
    sexo_paciente,
    COUNT(DISTINCT n_aih) AS internacoes,
    COUNT(DISTINCT n_aih) * 1.0 / SUM(COUNT(DISTINCT n_aih)) OVER () AS proporcao
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    sexo_paciente;
```

**Resultado:** `Linha não encontrada: {'sexo_paciente': 'M', 'total_internacoes': 42460, 'pct': 61.1}`
**TTA:** 6111ms

---

### ❌ Q22 — Epidemiológica Complexa

**Q:** Número de internações por fratura de fêmur (S72) em idosos (60+) comparado ao total em SP em 2023

**SQL gerado:**
```sql
WITH fratura_femur AS (
    SELECT COUNT(DISTINCT n_aih) AS num_internacoes_femur
    FROM processed
    JOIN enriched e ON processed.row_id = e.row_id
    WHERE sistema = 'SIH'
      AND uf_origem = 'SP'
      AND ano_cmpt = 2023
      AND idade_paciente >= 60
      AND (cid_principal LIKE 'S72%' OR e.clinical_interpretacao_clinica = 'lesoes e causas traumaticas')
),
total_internacoes AS (
    SELECT COUNT(DISTINCT n_aih) AS num_internacoes_total
    FROM processed
    WHERE sistema = 'SIH'
      AND uf_origem = 'SP'
      AND ano_cmpt = 2023
)
SELECT 
    (SELECT num_internacoes_femur FROM fratura_femur) AS num_internacoes_femur,
    (SELECT num_internacoes_total FROM total_internacoes) AS num_internacoes_total,
    (SELECT num_internacoes_femur FROM fratura_femur) * 100.0 / (SELECT num_internacoes_total FROM total_internacoes) AS percentual_femur
```

**Resultado:** `Linha 1: got={'num_internacoes_femur': 26253, 'num_internacoes_total': 209808, 'percentual_femur': 12.5128689087} exp={'total_s72': 30272, 's72_idosos': 19598.0, 'pct_idosos': 64.7}`
**TTA:** 16072ms

---

### ❌ Q23 — Epidemiológica Complexa

**Q:** Top 5 CIDs ortopédicos com maior permanência média hospitalar em SP em 2023

**SQL gerado:**
```sql
SELECT 
    cid_principal, 
    AVG(dias_perm) AS media_permanencia
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2023 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cid_principal
ORDER BY 
    media_permanencia DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cid_principal': 'M412', 'media_permanencia': 60.0} exp={'cid_principal': 'S272', 'total_internacoes': 49, 'permanencia_media_dias': 14.3}`
**TTA:** 6411ms

---

### ❌ Q24 — Epidemiológica Complexa

**Q:** Top 5 estabelecimentos com maior volume de internação ortopédica em SP em 2023

**SQL gerado:**
```sql
SELECT 
    cod_munic_estabelecimento, 
    COUNT(DISTINCT n_aih) AS volume_internacao
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2023 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cod_munic_estabelecimento
ORDER BY 
    volume_internacao DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cod_munic_estabelecimento': '350000', 'volume_internacao': 3097} exp={'cnes_estabelecimento': '2078015', 'internacoes': 2257}`
**TTA:** 6908ms

---

### ❌ Q25 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por raça/cor do paciente em SP em 2023

**SQL gerado:**
```sql
SELECT 
    raca_cor_paciente, 
    COUNT(DISTINCT n_aih) AS num_internacoes
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2023 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    raca_cor_paciente;
```

**Resultado:** `Linha não encontrada: {'raca_cor_paciente': '01', 'total': 42626, 'pct': 61.3}`
**TTA:** 6204ms

---

### ❌ Q26 — Epidemiológica Complexa

**Q:** CIDs ortopédicos com maior número de dias totais de internação em SP em 2023

**SQL gerado:**
```sql
SELECT 
    cid_principal, 
    SUM(qt_diarias) AS total_dias_internacao
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2023 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cid_principal
ORDER BY 
    total_dias_internacao DESC
LIMIT 1;
```

**Resultado:** `Número de linhas: got=1 exp=10`
**TTA:** 6605ms

---

### ❌ Q27 — Epidemiológica Complexa

**Q:** Top 5 procedimentos ambulatoriais ortopédicos mais realizados em SP em 2023

**SQL gerado:**
```sql
SELECT 
    cod_procedimento, 
    COUNT(*) AS quantidade_realizada
FROM 
    processed
WHERE 
    sistema = 'SIA' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2023 
    AND cod_procedimento LIKE '0408%'
GROUP BY 
    cod_procedimento
ORDER BY 
    quantidade_realizada DESC
LIMIT 5;
```

**Resultado:** `Número de linhas: got=0 exp=5`
**TTA:** 5024ms

---

### ❌ Q28 — Epidemiológica Complexa

**Q:** Proporção de internações ortopédicas com paciente de outro município em SP em 2023

**SQL gerado:**
```sql
WITH orthopedic_internations AS (
    SELECT
        p.n_aih,
        p.cod_munic_residencia,
        p.cod_munic_estabelecimento
    FROM
        processed p
    JOIN
        enriched e ON p.row_id = e.row_id
    WHERE
        p.sistema = 'SIH'
        AND p.uf_origem = 'SP'
        AND p.ano_cmpt = 2023
        AND (p.cid_principal LIKE 'M%' OR p.icd_group = 'M00-M99')
        AND p.cod_munic_residencia <> p.cod_munic_estabelecimento
)
SELECT
    COUNT(DISTINCT oi.n_aih) AS total_internations,
    SUM(CASE WHEN oi.cod_munic_residencia <> oi.cod_munic_estabelecimento THEN 1 ELSE 0 END) AS internations_with_other_municipality,
    (SUM(CASE WHEN oi.cod_munic_residencia <> oi.cod_munic_estabelecimento THEN 1 ELSE 0 END) * 1.0 / COUNT(DISTINCT oi.n_aih)) AS proportion
FROM
    orthopedic_internations oi;
```

**Resultado:** `Linha 1: got={'total_internations': 3729, 'internations_with_other_municipality': 3739.0, 'proportion': 1.0026816841} exp={'total_internacoes': 69487, 'deslocamento_intermunicipal': 39611.0, 'pct_deslocamento': 57.0}`
**TTA:** 11995ms

---

### ❌ Q29 — Epidemiológica Complexa

**Q:** Comparativo de volume mensal SIA vs SIH ortopédico em SP em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    sistema,
    COUNT(*) AS volume
FROM 
    processed
WHERE 
    uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt, sistema
ORDER BY 
    mes_cmpt, sistema;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'sistema': 'SIA', 'volume': 605} exp={'mes_cmpt': 1, 'sistema': 'SIA', 'total': 8415}`
**TTA:** 4896ms

---

### ❌ Q30 — Epidemiológica Complexa

**Q:** Idade média dos pacientes internados por fratura de fêmur (S72) em SP em 2023

**SQL gerado:**
```sql
SELECT AVG(idade_paciente) AS idade_media
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE cid_principal LIKE 'S72%'
  AND sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023;
```

**Resultado:** `Linha 1: got={'idade_media': 62.3639245732} exp={'idade_media': 62.4, 'idade_minima': 0.0, 'idade_maxima': 99.0}`
**TTA:** 4471ms

---

### ❌ Q31 — Financeira

**Q:** Custo total de todas as internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=15790831.06 exp=187220821.79`
**TTA:** 4447ms

---

### ❌ Q32 — Financeira

**Q:** Custo médio por internação ortopédica em SP em 2023

**SQL gerado:**
```sql
SELECT 
    COALESCE(SUM(custo_total), 0) / COUNT(DISTINCT n_aih) AS custo_medio_por_internacao
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=2295.1789331395 exp=2693.13`
**TTA:** 6079ms

---

### ❌ Q33 — Financeira

**Q:** Custo total de internações por fratura de fêmur (S72) em SP em 2023

**SQL gerado:**
```sql
SELECT SUM(COALESCE(custo_total, 0)) AS custo_total
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'S72%' OR e.clinical_interpretacao_clinica = 'lesoes e causas traumaticas');
```

**Resultado:** `got=171429990.73000062 exp=82692089.44`
**TTA:** 5647ms

---

### ❌ Q34 — Financeira

**Q:** Top 5 CIDs ortopédicos com maior custo total de internação em SP em 2023

**SQL gerado:**
```sql
SELECT 
    cid_principal, 
    SUM(custo_total) AS total_custo
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2023 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cid_principal
ORDER BY 
    total_custo DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cid_principal': 'M511', 'total_custo': 1119270.5000000002} exp={'cid_principal': 'S720', 'internacoes': 9059, 'custo_total': 30400546.73}`
**TTA:** 6314ms

---

### ❌ Q35 — Financeira

**Q:** Custo total de internações ortopédicas com uso de OPME (val_ortp > 0) em SP em 2023

**SQL gerado:**
```sql
SELECT SUM(COALESCE(val_ortp, 0)) AS custo_total_ortopedia
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND val_ortp > 0;
```

**Resultado:** `Linha 1: got={'custo_total_ortopedia': None} exp={'internacoes_com_opme': 0, 'custo_total_opme': None}`
**TTA:** 3986ms

---

### ❌ Q36 — Financeira

**Q:** Valor total de honorários profissionais (val_sp) em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT SUM(COALESCE(val_sp, 0)) AS total_honorarios
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=3059022.389999998 exp=32810269.18`
**TTA:** 4345ms

---

### ❌ Q37 — Financeira

**Q:** Custo médio por dia de internação ortopédica em SP em 2023

**SQL gerado:**
```sql
SELECT 
    COALESCE(SUM(custo_total) / SUM(qt_diarias), 0) AS custo_medio_por_dia
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=404.4575344501 exp=441.21`
**TTA:** 5952ms

---

### ❌ Q38 — Financeira

**Q:** Top 5 estabelecimentos com maior custo total de internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT 
    cod_munic_estabelecimento, 
    SUM(custo_total) AS total_custo
FROM 
    processed
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2023 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cod_munic_estabelecimento
ORDER BY 
    total_custo DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cod_munic_estabelecimento': '350000', 'total_custo': 7641321.079999998} exp={'cnes_estabelecimento': '2078015', 'internacoes': 2257, 'custo_total': 11295149.42}`
**TTA:** 5786ms

---

### ❌ Q39 — Financeira

**Q:** Custo total de procedimentos ambulatoriais ortopédicos em SP em 2023

**SQL gerado:**
```sql
SELECT SUM(COALESCE(custo_total, 0)) AS total_custo
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=2109063.400000005 exp=9092221.75`
**TTA:** 4341ms

---

### ❌ Q40 — Financeira

**Q:** Proporção custo serviço hospitalar vs honorários em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT 
    COALESCE(SUM(val_sh), 0) AS total_val_sh,
    COALESCE(SUM(val_sp), 0) AS total_val_sp,
    (COALESCE(SUM(val_sh), 0) / COALESCE(SUM(val_sp), 0)) AS proporcao_val_sh_val_sp
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
    AND e.clinical_interpretacao_clinica = 'doencas osteomusculares e do tecido conjuntivo';
```

**Resultado:** `Linha 1: got={'total_val_sh': 12531360.409999998, 'total_val_sp': 3059022.3899999997, 'proporcao_val_sh_val_sp': 4.0965245795} exp={'custo_servico_hospitalar': 153782049.46, 'honorarios_profissionais': 32810269.18, 'pct_hospitalar': 82.4}`
**TTA:** 8691ms

---

### ❌ Q41 — Temporal/Comparativa

**Q:** Distribuição mensal de internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    COUNT(DISTINCT n_aih) AS internacoes_ortopédicas
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'internacoes_ortopédicas': 507} exp={'mes_cmpt': 1, 'total_internacoes': 5667}`
**TTA:** 6375ms

---

### ❌ Q42 — Temporal/Comparativa

**Q:** Distribuição mensal de procedimentos ambulatoriais ortopédicos em SP em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt, 
    COUNT(*) AS quantidade_procedimentos
FROM 
    processed
WHERE 
    sistema = 'SIA' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2023 
    AND cod_procedimento LIKE '0408%'
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `Número de linhas: got=0 exp=12`
**TTA:** 4792ms

---

### ✅ Q43 — Temporal/Comparativa

**Q:** Mês com maior número de internações por fratura de fêmur (S72) em SP em 2023

**SQL gerado:**
```sql
SELECT mes_cmpt, COUNT(DISTINCT n_aih) AS num_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
GROUP BY mes_cmpt
ORDER BY num_internacoes DESC
LIMIT 1;
```

**Resultado:** `got=8 exp=8`
**TTA:** 5499ms

---

### ❌ Q44 — Temporal/Comparativa

**Q:** Evolução mensal do custo total de internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    SUM(COALESCE(custo_total, 0)) AS custo_total
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'custo_total': 1163311.8100000008} exp={'mes_cmpt': 1, 'custo_total': 16004871.37}`
**TTA:** 6375ms

---

### ❌ Q45 — Temporal/Comparativa

**Q:** Volume mensal de internações por trauma (S00-T98) vs doenças musculoesqueléticas (M00-M99) em SP em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    COUNT(DISTINCT CASE WHEN icd_group = 'S00-T98' THEN n_aih END) AS trauma_internacoes,
    COUNT(DISTINCT CASE WHEN icd_group = 'M00-M99' THEN n_aih END) AS musculoesqueletica_internacoes
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `Número de linhas: got=12 exp=24`
**TTA:** 7447ms

---

### ❌ Q46 — Temporal/Comparativa

**Q:** Evolução mensal de óbitos em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    COUNT(DISTINCT n_aih) AS total_obitos
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND morte = 1
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'total_obitos': 6} exp={'mes_cmpt': 1, 'total_obitos': 198}`
**TTA:** 6533ms

---

### ❌ Q47 — Temporal/Comparativa

**Q:** Permanência média por trimestre nas internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT 
    AVG(dias_perm) AS media_permanencia,
    competencia_ano_mes / 100 AS trimestre
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2023 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    trimestre
ORDER BY 
    trimestre;
```

**Resultado:** `Número de linhas: got=12 exp=4`
**TTA:** 6511ms

---

### ❌ Q48 — Temporal/Comparativa

**Q:** Mês com maior custo médio por internação ortopédica em SP em 2023

**SQL gerado:**
```sql
SELECT mes_cmpt, AVG(custo_total) AS media_custo
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY mes_cmpt
ORDER BY media_custo DESC
LIMIT 1;
```

**Resultado:** `got=8 exp=1`
**TTA:** 5770ms

---

### ❌ Q49 — Temporal/Comparativa

**Q:** Sazonalidade mensal de fraturas de fêmur (S72) em SP em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    COUNT(*) AS count_s72
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND cid_principal LIKE 'S72%'
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'count_s72': 6619} exp={'mes_cmpt': 1, 'total_fraturas': 2467}`
**TTA:** 5220ms

---

### ❌ Q50 — Temporal/Comparativa

**Q:** Volume mensal de internações ortopédicas de idosos (60 anos ou mais) em SP em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND idade_paciente >= 60
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'volume_internacoes': 147} exp={'mes_cmpt': 1, 'total_internacoes': 2318}`
**TTA:** 6655ms

---

### ❌ Q51 — Epidemiológica Simples

**Q:** Número de internações por fratura de fêmur (S72) em mulheres em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND sexo_paciente = 'F'
  AND (cid_principal LIKE 'S72%' OR e.clinical_interpretacao_clinica = 'lesoes e causas traumaticas');
```

**Resultado:** `got=24473 exp=15639`
**TTA:** 5887ms

---

### ❌ Q52 — Epidemiológica Simples

**Q:** Número de internações por fratura de fêmur (S72) em homens em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND sexo_paciente = 'M'
  AND (cid_principal LIKE 'S72%' OR icd_group = 'S00-T98')
```

**Resultado:** `got=38134 exp=14633`
**TTA:** 5537ms

---

### ❌ Q53 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com permanência de 1 dia ou menos em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
  AND dias_perm <= 1;
```

**Resultado:** `got=2388 exp=14718`
**TTA:** 5416ms

---

### ❌ Q54 — Epidemiológica Simples

**Q:** Número de procedimentos ambulatoriais ortopédicos realizados em mulheres em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(*) AS numero_procedimentos
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND sexo_paciente = 'F'
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
  AND cod_procedimento LIKE '0408%';
```

**Resultado:** `got=0 exp=47792`
**TTA:** 5464ms

---

### ❌ Q55 — Epidemiológica Simples

**Q:** Número de procedimentos ambulatoriais ortopédicos realizados por idosos (60 anos ou mais) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(*) AS numero_procedimentos
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND idade_paciente >= 60
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
  AND clinical_interpretacao_clinica = 'doencas osteomusculares e do tecido conjuntivo';
```

**Resultado:** `got=4515 exp=55711`
**TTA:** 6359ms

---

### ❌ Q56 — Epidemiológica Simples

**Q:** Número de internações por fratura de tíbia e perônio (S82) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'S82%' OR e.clinical_interpretacao_clinica = 'lesoes e causas traumaticas');
```

**Resultado:** `got=62607 exp=6827`
**TTA:** 5536ms

---

### ❌ Q57 — Epidemiológica Simples

**Q:** Número de internações por fratura de antebraço (S52) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'S52%' OR e.clinical_interpretacao_clinica = 'lesoes e causas traumaticas');
```

**Resultado:** `got=62607 exp=2264`
**TTA:** 5517ms

---

### ✅ Q58 — Epidemiológica Simples

**Q:** Número de internações por fratura de ombro e úmero (S42) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'S42%' OR e.clinical_event_narrative LIKE '%fratura de ombro e úmero%');
```

**Resultado:** `got=1391 exp=1391`
**TTA:** 6240ms

---

### ❌ Q59 — Epidemiológica Simples

**Q:** Número de internações ortopédicas em crianças (menores de 18 anos) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND idade_paciente < 18
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=559 exp=4040`
**TTA:** 5497ms

---

### ❌ Q60 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com permanência entre 8 e 30 dias em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS num_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND dias_perm BETWEEN 8 AND 30
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=1349 exp=16748`
**TTA:** 4679ms

---

### ✅ Q61 — Epidemiológica Simples

**Q:** Número de internações por artrose de quadril e joelho (M16 e M17) em idosos (60 anos ou mais) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'M16%' OR cid_principal LIKE 'M17%')
  AND idade_paciente >= 60;
```

**Resultado:** `got=100 exp=100`
**TTA:** 5476ms

---

### ✅ Q62 — Epidemiológica Simples

**Q:** Número de internações por fratura de fêmur (S72) em mulheres com 70 anos ou mais em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND idade_paciente >= 70
  AND sexo_paciente = 'F'
  AND (cid_principal LIKE 'S72%' OR main_icd LIKE 'S72%');
```

**Resultado:** `got=11171 exp=11171`
**TTA:** 5878ms

---

### ❌ Q63 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com permanência superior a 30 dias em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND dias_perm > 30
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=177 exp=1037`
**TTA:** 5462ms

---

### ❌ Q64 — Epidemiológica Simples

**Q:** Número de procedimentos ambulatoriais ortopédicos realizados em homens em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(*) AS num_procedimentos
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND sexo_paciente = 'M'
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
  AND cod_procedimento LIKE '0408%';
```

**Resultado:** `got=0 exp=57572`
**TTA:** 5471ms

---

### ✅ Q65 — Epidemiológica Simples

**Q:** Número de internações por traumatismos (grupo S00-T98) em idosos (60 anos ou mais) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND idade_paciente >= 60
  AND icd_group = 'S00-T98';
```

**Resultado:** `got=26253 exp=26253`
**TTA:** 5182ms

---

### ✅ Q66 — Epidemiológica Simples

**Q:** Número de internações por doenças osteomusculares (grupo M00-M99) em mulheres em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND sexo_paciente = 'F'
  AND icd_group = 'M00-M99';
```

**Resultado:** `got=2554 exp=2554`
**TTA:** 5137ms

---

### ✅ Q67 — Epidemiológica Simples

**Q:** Número de óbitos em internações por fratura de fêmur (S72) em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_obitos
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cid_principal LIKE 'S72%'
  AND morte = 1;
```

**Resultado:** `got=1250 exp=1250`
**TTA:** 4912ms

---

### ❌ Q68 — Epidemiológica Simples

**Q:** Número de municípios distintos com registro de procedimentos ambulatoriais ortopédicos em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT cod_munic_residencia) AS num_municipios
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cod_procedimento LIKE '0408%';
```

**Resultado:** `got=0 exp=400`
**TTA:** 4580ms

---

### ❌ Q69 — Epidemiológica Complexa

**Q:** Top 5 municípios por volume de procedimentos ambulatoriais ortopédicos em SP em 2023

**SQL gerado:**
```sql
SELECT 
    cod_munic_residencia,
    COUNT(*) AS volume_procedimentos
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIA' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2023 
    AND cod_procedimento LIKE '0408%'
GROUP BY 
    cod_munic_residencia
ORDER BY 
    volume_procedimentos DESC
LIMIT 5;
```

**Resultado:** `Número de linhas: got=0 exp=5`
**TTA:** 5896ms

---

### ❌ Q70 — Epidemiológica Complexa

**Q:** Distribuição de procedimentos ambulatoriais ortopédicos por faixa etária em SP em 2023

**SQL gerado:**
```sql
SELECT 
    FLOOR(idade_paciente / 10) * 10 AS faixa_etaria,
    COUNT(*) AS quantidade_procedimentos
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIA' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2023 
    AND cod_procedimento LIKE '0408%'
GROUP BY 
    faixa_etaria
ORDER BY 
    faixa_etaria;
```

**Resultado:** `Número de linhas: got=0 exp=3`
**TTA:** 6171ms

---

### ❌ Q71 — Epidemiológica Complexa

**Q:** Top 5 diagnósticos com maior número de óbitos em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT 
    cid_principal, 
    COUNT(*) AS num_obitos
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2023 
    AND morte = 1 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cid_principal
ORDER BY 
    num_obitos DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cid_principal': 'M869', 'num_obitos': 17} exp={'cid_principal': 'S720', 'total_internacoes': 9059, 'total_obitos': 478.0}`
**TTA:** 6523ms

---

### ❌ Q72 — Epidemiológica Complexa

**Q:** Permanência hospitalar média por sexo em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT 
    sexo_paciente,
    AVG(dias_perm) AS media_dias_perm
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    sexo_paciente;
```

**Resultado:** `Linha não encontrada: {'sexo_paciente': 'F', 'permanencia_media': 6.3}`
**TTA:** 5818ms

---

### ❌ Q73 — Epidemiológica Complexa

**Q:** Top 10 estabelecimentos por volume de procedimentos ambulatoriais ortopédicos em SP em 2023

**SQL gerado:**
```sql
SELECT 
    cod_munic_estabelecimento, 
    COUNT(*) AS volume_procedimentos
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIA' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2023 
    AND cod_procedimento LIKE '0408%'
GROUP BY 
    cod_munic_estabelecimento
ORDER BY 
    volume_procedimentos DESC
LIMIT 10;
```

**Resultado:** `Número de linhas: got=0 exp=10`
**TTA:** 6067ms

---

### ❌ Q74 — Epidemiológica Complexa

**Q:** Taxa de mortalidade por sexo em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT 
    sexo_paciente,
    SUM(CASE WHEN morte = 1 THEN 1 ELSE 0 END) AS total_obitos,
    COUNT(DISTINCT n_aih) AS total_internacoes,
    (SUM(CASE WHEN morte = 1 THEN 1 ELSE 0 END) * 1.0 / COUNT(DISTINCT n_aih)) AS taxa_mortalidade
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    sexo_paciente;
```

**Resultado:** `Linha não encontrada: {'sexo_paciente': 'F', 'total_obitos': 1050.0, 'taxa_mortalidade': 3.89}`
**TTA:** 8386ms

---

### ❌ Q75 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por grupo diagnóstico (M e S) e faixa etária em SP em 2023

**SQL gerado:**
```sql
SELECT 
    icd_group,
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente < 30 THEN '18-29'
        WHEN idade_paciente < 40 THEN '30-39'
        WHEN idade_paciente < 50 THEN '40-49'
        WHEN idade_paciente < 60 THEN '50-59'
        WHEN idade_paciente < 70 THEN '60-69'
        WHEN idade_paciente < 80 THEN '70-79'
        ELSE '80+'
    END AS faixa_etaria,
    COUNT(DISTINCT n_aih) AS quantidade_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR cid_principal LIKE 'S%')
GROUP BY 
    icd_group, faixa_etaria
ORDER BY 
    icd_group, faixa_etaria;
```

**Resultado:** `Número de linhas: got=16 exp=6`
**TTA:** 10867ms

---

### ❌ Q76 — Epidemiológica Complexa

**Q:** Top 5 diagnósticos de traumatismo com maior permanência média em SP em 2023 (mínimo 40 internações)

**SQL gerado:**
```sql
SELECT 
    cid_principal, 
    AVG(dias_perm) AS media_permanencia, 
    COUNT(DISTINCT n_aih) AS quantidade_internacoes
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2023 
    AND (cid_principal LIKE 'S%' OR icd_group = 'S00-T98')
GROUP BY 
    cid_principal
HAVING 
    COUNT(DISTINCT n_aih) >= 40
ORDER BY 
    media_permanencia DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cid_principal': 'S272', 'media_permanencia': 14.2653061224, 'quantidade_internacoes': 49} exp={'cid_principal': 'S272', 'internacoes': 49, 'permanencia_media': 14.3}`
**TTA:** 7732ms

---

### ❌ Q77 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por faixa etária (0-17, 18-39, 40-59, 60-79, 80+) em SP em 2023

**SQL gerado:**
```sql
SELECT 
  CASE 
    WHEN idade_paciente BETWEEN 0 AND 17 THEN '0-17'
    WHEN idade_paciente BETWEEN 18 AND 39 THEN '18-39'
    WHEN idade_paciente BETWEEN 40 AND 59 THEN '40-59'
    WHEN idade_paciente BETWEEN 60 AND 79 THEN '60-79'
    WHEN idade_paciente >= 80 THEN '80+'
  END AS faixa_etaria,
  COUNT(DISTINCT n_aih) AS quantidade_internacoes
FROM 
  processed
JOIN 
  enriched e ON processed.row_id = e.row_id
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
  faixa_etaria
ORDER BY 
  faixa_etaria;
```

**Resultado:** `Linha não encontrada: {'faixa_etaria': '0-17', 'total': 4040}`
**TTA:** 10396ms

---

### ❌ Q78 — Epidemiológica Complexa

**Q:** Top 5 estabelecimentos com maior volume de internações por fratura de fêmur (S72) em SP em 2023

**SQL gerado:**
```sql
SELECT 
    cod_munic_estabelecimento, 
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2023 
    AND cid_principal LIKE 'S72%'
GROUP BY 
    cod_munic_estabelecimento
ORDER BY 
    volume_internacoes DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cod_munic_estabelecimento': '350000', 'volume_internacoes': 12849} exp={'cnes_estabelecimento': '2091399', 'internacoes': 826}`
**TTA:** 6443ms

---

### ❌ Q79 — Epidemiológica Complexa

**Q:** Permanência hospitalar média por faixa etária em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT 
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente < 30 THEN '18-29'
        WHEN idade_paciente < 40 THEN '30-39'
        WHEN idade_paciente < 50 THEN '40-49'
        WHEN idade_paciente < 60 THEN '50-59'
        WHEN idade_paciente < 70 THEN '60-69'
        WHEN idade_paciente < 80 THEN '70-79'
        ELSE '80+'
    END AS faixa_etaria,
    AVG(dias_perm) AS media_permanencia
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    faixa_etaria
ORDER BY 
    faixa_etaria;
```

**Resultado:** `Número de linhas: got=8 exp=3`
**TTA:** 11374ms

---

### ✅ Q80 — Epidemiológica Complexa

**Q:** Top 5 diagnósticos de doença osteomuscular (M00-M99) por número de internações em SP em 2023

**SQL gerado:**
```sql
SELECT 
    cid_principal, 
    COUNT(DISTINCT n_aih) AS num_internacoes
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2023 
    AND icd_group = 'M00-M99'
GROUP BY 
    cid_principal
ORDER BY 
    num_internacoes DESC
LIMIT 5;
```

**Resultado:** `5 linhas corretas (flexível)`
**TTA:** 6209ms

---

### ❌ Q81 — Epidemiológica Complexa

**Q:** Número de internações ortopédicas com permanência entre 3 e 7 dias em SP em 2023

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND dias_perm BETWEEN 3 AND 7
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=1769 exp=27009`
**TTA:** 5534ms

---

### ❌ Q82 — Epidemiológica Complexa

**Q:** Top 5 diagnósticos ambulatoriais ortopédicos em idosos (60 anos ou mais) em SP em 2023

**SQL gerado:**
```sql
SELECT 
    cid_principal, 
    COUNT(*) AS quantidade
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIA' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2023 
    AND idade_paciente >= 60 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cid_principal
ORDER BY 
    quantidade DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cid_principal': 'M216', 'quantidade': 1846} exp={'cid_principal': 'S720', 'total': 11745}`
**TTA:** 6442ms

---

### ❌ Q83 — Epidemiológica Complexa

**Q:** Média de internações ortopédicas por estabelecimento em SP em 2023

**SQL gerado:**
```sql
SELECT 
    cod_munic_estabelecimento,
    COUNT(DISTINCT n_aih) AS num_internacoes
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cod_munic_estabelecimento;
```

**Resultado:** `got=350410 exp=180.5`
**TTA:** 6242ms

---

### ❌ Q84 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por faixa de permanência em SP em 2023

**SQL gerado:**
```sql
SELECT 
    CASE 
        WHEN dias_perm < 3 THEN '0-2 dias'
        WHEN dias_perm < 7 THEN '3-6 dias'
        WHEN dias_perm < 14 THEN '7-13 dias'
        WHEN dias_perm < 30 THEN '14-29 dias'
        ELSE '30+ dias'
    END AS faixa_permanencia,
    COUNT(DISTINCT n_aih) AS quantidade_internacoes
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    faixa_permanencia
ORDER BY 
    faixa_permanencia;
```

**Resultado:** `Número de linhas: got=5 exp=4`
**TTA:** 9416ms

---

### ❌ Q85 — Financeira

**Q:** Custo total de internações ortopédicas de idosos (60 anos ou mais) em SP em 2023

**SQL gerado:**
```sql
SELECT SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND idade_paciente >= 60
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=5568723.949999998 exp=83044181.8`
**TTA:** 4869ms

---

### ✅ Q86 — Financeira

**Q:** Custo médio por internação por fratura de fêmur (S72) em SP em 2023

**SQL gerado:**
```sql
SELECT 
    AVG(custo_total) AS custo_medio_por_internacao
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'S72%' OR e.clinical_interpretacao_clinica = 'lesoes e causas traumaticas')
```

**Resultado:** `got=2737.7984976683 exp=2730.82`
**TTA:** 5894ms

---

### ❌ Q87 — Financeira

**Q:** Custo total de internações ortopédicas com passagem por UTI em SP em 2023

**SQL gerado:**
```sql
SELECT SUM(COALESCE(val_uti, 0)) AS custo_total_uti
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
  AND uti_int_to > 0;
```

**Resultado:** `got=None exp=6792.17`
**TTA:** 5404ms

---

### ❌ Q88 — Financeira

**Q:** Custo médio por internação ortopédica por sexo do paciente em SP em 2023

**SQL gerado:**
```sql
SELECT 
    sexo_paciente,
    AVG(custo_total) AS custo_medio_internacao
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    sexo_paciente;
```

**Resultado:** `Linha não encontrada: {'sexo_paciente': 'F', 'custo_medio': 2710.26}`
**TTA:** 4964ms

---

### ❌ Q89 — Financeira

**Q:** Custo total de internações ortopédicas em crianças (menores de 18 anos) em SP em 2023

**SQL gerado:**
```sql
SELECT SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND idade_paciente < 18
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=938048.9300000002 exp=7933442.51`
**TTA:** 5730ms

---

### ❌ Q90 — Financeira

**Q:** Custo médio por internação ortopédica com permanência superior a 7 dias em SP em 2023

**SQL gerado:**
```sql
SELECT 
    AVG(custo_total) AS custo_medio_por_internacao
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND dias_perm > 7
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=3390.9169584955 exp=4390.48`
**TTA:** 5761ms

---

### ❌ Q91 — Financeira

**Q:** Top 3 municípios com maior custo total em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT 
    cod_munic_residencia,
    SUM(custo_total) AS total_custo
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2023 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cod_munic_residencia
ORDER BY 
    total_custo DESC
LIMIT 3;
```

**Resultado:** `Linha 1: got={'cod_munic_residencia': '355030', 'total_custo': 2802662.5899999994} exp={'cod_munic_estabelecimento': '350000', 'custo_total': 98503323.97}`
**TTA:** 6608ms

---

### ❌ Q92 — Financeira

**Q:** Valor total de serviço hospitalar em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT SUM(COALESCE(val_sh, 0)) AS valor_total_servico_hospitalar
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=12531360.41 exp=153782049.46`
**TTA:** 5353ms

---

### ❌ Q93 — Financeira

**Q:** Custo médio por internação ortopédica por faixa etária em SP em 2023

**SQL gerado:**
```sql
SELECT 
    FLOOR(idade_paciente / 10) * 10 AS faixa_etaria,
    AVG(custo_total) AS custo_medio_por_internacao
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    faixa_etaria
ORDER BY 
    faixa_etaria;
```

**Resultado:** `Número de linhas: got=10 exp=3`
**TTA:** 7037ms

---

### ❌ Q94 — Financeira

**Q:** Valor total de UTI em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT SUM(COALESCE(val_uti, 0)) AS total_uti
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=1778326.4 exp=40878597.03`
**TTA:** 5205ms

---

### ✅ Q95 — Financeira

**Q:** Custo total de internações por doenças osteomusculares (M00-M99) em SP em 2023

**SQL gerado:**
```sql
SELECT SUM(COALESCE(custo_total, 0)) AS custo_total
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND e.clinical_interpretacao_clinica = 'doencas osteomusculares e do tecido conjuntivo';
```

**Resultado:** `got=15790831.059999995 exp=15790831.06`
**TTA:** 5581ms

---

### ✅ Q96 — Financeira

**Q:** Custo total de internações por traumatismos (S00-T98) em SP em 2023

**SQL gerado:**
```sql
SELECT SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'S%' OR icd_group = 'S00-T98');
```

**Resultado:** `got=171429990.73000062 exp=171429990.73`
**TTA:** 5326ms

---

### ❌ Q97 — Financeira

**Q:** Top 5 diagnósticos com maior custo médio por internação ortopédica em SP em 2023 (mínimo 40 internações)

**SQL gerado:**
```sql
WITH filtered_data AS (
    SELECT
        cid_principal,
        AVG(custo_total) AS avg_cost
    FROM
        processed
    JOIN
        enriched e ON processed.row_id = e.row_id
    WHERE
        sistema = 'SIH'
        AND uf_origem = 'SP'
        AND ano_cmpt = 2023
        AND cid_principal LIKE 'M%' -- Filtro de CIDs ortopédicos musculoesqueléticos
        AND n_aih IN (
            SELECT
                n_aih
            FROM
                processed
            WHERE
                sistema = 'SIH'
                AND uf_origem = 'SP'
                AND ano_cmpt = 2023
                AND cid_principal LIKE 'M%'
            GROUP BY
                n_aih
            HAVING
                COUNT(*) >= 40
        )
    GROUP BY
        cid_principal
)
SELECT
    cid_principal,
    avg_cost
FROM
    filtered_data
ORDER BY
    avg_cost DESC
LIMIT 5;
```

**Resultado:** `Número de linhas: got=0 exp=5`
**TTA:** 10774ms

---

### ❌ Q98 — Financeira

**Q:** Custo médio por procedimento ambulatorial ortopédico em SP em 2023

**SQL gerado:**
```sql
SELECT 
    cod_procedimento,
    AVG(custo_total) AS custo_medio
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIA' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2023 
    AND cod_procedimento LIKE '0408%'
GROUP BY 
    cod_procedimento;
```

**Resultado:** `Resultado vazio`
**TTA:** 5308ms

---

### ❌ Q99 — Financeira

**Q:** Top 5 procedimentos ambulatoriais com maior custo total em SP em 2023

**SQL gerado:**
```sql
SELECT 
    cod_procedimento, 
    SUM(custo_total) AS total_custo
FROM 
    processed
WHERE 
    sistema = 'SIA' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2023
GROUP BY 
    cod_procedimento
ORDER BY 
    total_custo DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cod_procedimento': '0702100048', 'total_custo': 45189222.96000072} exp={'cod_procedimento': '0701020369', 'custo_total': 1261008.0}`
**TTA:** 4657ms

---

### ❌ Q100 — Financeira

**Q:** Custo total de internações ortopédicas de idosos (60+) com permanência superior a 7 dias em SP em 2023

**SQL gerado:**
```sql
SELECT SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND idade_paciente >= 60
  AND dias_perm > 7
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99');
```

**Resultado:** `got=2241104.49 exp=37989386.14`
**TTA:** 6063ms

---

### ❌ Q101 — Temporal/Comparativa

**Q:** Mês com maior número de óbitos em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT mes_cmpt, COUNT(DISTINCT n_aih) AS num_obitos
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND morte = 1
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY mes_cmpt
ORDER BY num_obitos DESC
LIMIT 1;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 9, 'num_obitos': 12} exp={'mes_cmpt': 11, 'total_obitos': 209}`
**TTA:** 6191ms

---

### ❌ Q102 — Temporal/Comparativa

**Q:** Volume mensal de internações por fratura de fêmur (S72) em idosos (60 anos ou mais) em SP em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND idade_paciente >= 60
    AND (cid_principal LIKE 'S72%' OR e.clinical_interpretacao_clinica = 'lesoes e causas traumaticas')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'volume_internacoes': 2171} exp={'mes_cmpt': 1, 'internacoes': 1597}`
**TTA:** 7077ms

---

### ❌ Q103 — Temporal/Comparativa

**Q:** Custo mensal de internações ortopédicas de idosos (60 anos ou mais) em SP em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    SUM(COALESCE(custo_total, 0)) AS custo_total
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND idade_paciente >= 60
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'custo_total': 494055.7599999998} exp={'mes_cmpt': 1, 'custo_total': 7166029.19}`
**TTA:** 6781ms

---

### ❌ Q104 — Temporal/Comparativa

**Q:** Mês com maior custo total de internações por fratura de fêmur (S72) em SP em 2023

**SQL gerado:**
```sql
SELECT mes_cmpt, SUM(custo_total) AS total_custo
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'S72%' OR e.clinical_interpretacao_clinica = 'lesoes e causas traumaticas')
GROUP BY mes_cmpt
ORDER BY total_custo DESC
LIMIT 1;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 12, 'total_custo': 15716790.150000032} exp={'mes_cmpt': 8, 'custo_total': 7745411.5}`
**TTA:** 6211ms

---

### ✅ Q105 — Temporal/Comparativa

**Q:** Volume mensal de internações por traumatismos (grupo S00-T98) em SP em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND e.clinical_interpretacao_clinica = 'lesoes e causas traumaticas'
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 6328ms

---

### ❌ Q106 — Temporal/Comparativa

**Q:** Volume mensal de procedimentos ambulatoriais ortopédicos por sexo em SP em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    sexo_paciente,
    COUNT(*) AS volume_procedimentos
FROM 
    processed
WHERE 
    sistema = 'SIA' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2023 
    AND cod_procedimento LIKE '0408%'
GROUP BY 
    mes_cmpt, sexo_paciente
ORDER BY 
    mes_cmpt, sexo_paciente;
```

**Resultado:** `Número de linhas: got=0 exp=24`
**TTA:** 5275ms

---

### ❌ Q107 — Temporal/Comparativa

**Q:** Comparativo trimestral de internações ortopédicas por grupo diagnóstico em SP em 2023

**SQL gerado:**
```sql
SELECT
    icd_group,
    TRUNC(competencia_ano_mes, -2) AS trimestre,
    COUNT(DISTINCT n_aih) AS internacoes_unicas
FROM
    processed
JOIN
    enriched e ON processed.row_id = e.row_id
WHERE
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY
    icd_group, trimestre
ORDER BY
    icd_group, trimestre;
```

**Resultado:** `Número de linhas: got=1 exp=8`
**TTA:** 7198ms

---

### ❌ Q108 — Temporal/Comparativa

**Q:** Mês com maior permanência hospitalar média em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT mes_cmpt, AVG(dias_perm) AS media_permanencia
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY mes_cmpt
ORDER BY media_permanencia DESC
LIMIT 1;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 11, 'media_permanencia': 6.4865900383} exp={'mes_cmpt': 11, 'permanencia_media': 6.4}`
**TTA:** 5848ms

---

### ❌ Q109 — Temporal/Comparativa

**Q:** Evolução mensal da taxa de mortalidade em internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    COUNT(DISTINCT CASE WHEN morte = 1 THEN n_aih END) / COUNT(DISTINCT n_aih) AS taxa_mortalidade
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2023 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'taxa_mortalidade': 0.0118343195} exp={'mes_cmpt': 1, 'internacoes': 5667, 'obitos': 198.0, 'taxa_mortalidade': 3.49}`
**TTA:** 7043ms

---

### ❌ Q110 — Temporal/Comparativa

**Q:** Trimestre com maior volume de internações por fratura de fêmur (S72) em SP em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'S72%' OR e.clinical_event_narrative LIKE '%fratura de fêmur%')
GROUP BY 
    mes_cmpt
ORDER BY 
    volume_internacoes DESC
LIMIT 1;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 8, 'volume_internacoes': 2816} exp={'trimestre': 'Q3', 'total': 7947}`
**TTA:** 7507ms

---

### ❌ Q111 — Temporal/Comparativa

**Q:** Comparativo semestral de internações ortopédicas (1º vs 2º semestre) em SP em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    COUNT(DISTINCT n_aih) AS num_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2023 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `Número de linhas: got=12 exp=2`
**TTA:** 5371ms

---

### ❌ Q112 — Temporal/Comparativa

**Q:** Custo médio mensal por internação por fratura de fêmur (S72) em SP em 2023

**SQL gerado:**
```sql
SELECT 
    AVG(custo_total) AS custo_medio_mensal,
    mes_cmpt
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2023 
    AND cid_principal LIKE 'S72%'
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `Linha 1: got={'custo_medio_mensal': 2724.4534211593, 'mes_cmpt': 1} exp={'mes_cmpt': 1, 'custo_medio': 2724.45}`
**TTA:** 5873ms

---

### ❌ Q113 — Temporal/Comparativa

**Q:** Volume mensal de internações ortopédicas por sexo do paciente em SP em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    sexo_paciente,
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt, sexo_paciente
ORDER BY 
    mes_cmpt, sexo_paciente;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'sexo_paciente': 'F', 'volume_internacoes': 207} exp={'mes_cmpt': 1, 'sexo_paciente': 'F', 'total': 2219}`
**TTA:** 6772ms

---

### ❌ Q114 — Temporal/Comparativa

**Q:** Mês com menor volume de procedimentos ambulatoriais ortopédicos em SP em 2023

**SQL gerado:**
```sql
SELECT mes_cmpt
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cod_procedimento LIKE '0408%'
GROUP BY mes_cmpt
ORDER BY COUNT(*) ASC
LIMIT 1;
```

**Resultado:** `Número de linhas: got=0 exp=1`
**TTA:** 4087ms

---

### ❌ Q115 — Temporal/Comparativa

**Q:** Volume trimestral de procedimentos ambulatoriais ortopédicos em SP em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt, 
    COUNT(*) AS volume_procedimentos
FROM 
    processed
WHERE 
    sistema = 'SIA' 
    AND uf_origem = 'SP' 
    AND ano_cmpt = 2023 
    AND cod_procedimento LIKE '0408%'
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `Número de linhas: got=0 exp=4`
**TTA:** 4804ms

---

### ❌ Q116 — Temporal/Comparativa

**Q:** Custo mensal de procedimentos ambulatoriais ortopédicos em SP em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    SUM(COALESCE(custo_total, 0)) AS custo_mensal
FROM 
    processed
WHERE 
    sistema = 'SIA'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND cod_procedimento LIKE '0408%'
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `Número de linhas: got=0 exp=12`
**TTA:** 5116ms

---

### ❌ Q117 — Temporal/Comparativa

**Q:** Comparativo semestral do custo total de internações ortopédicas em SP em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    SUM(COALESCE(custo_total, 0)) AS custo_total
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `Número de linhas: got=12 exp=2`
**TTA:** 5386ms

---

### ❌ Q118 — Temporal/Comparativa

**Q:** Evolução mensal da permanência média em internações por fratura de fêmur (S72) em SP em 2023

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    AVG(dias_perm) AS media_permanencia
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'SP'
    AND ano_cmpt = 2023
    AND (cid_principal LIKE 'S72%' OR e.clinical_interpretacao_clinica = 'lesoes e causas traumaticas')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'media_permanencia': 6.2184108527} exp={'mes_cmpt': 1, 'permanencia_media': 6.6}`
**TTA:** 6563ms

---

## Failures Detail

- **Q02** (Epidemiológica Simples): got=11834 exp=105364
  SQL: `SELECT COUNT(*) AS total_procedimentos
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SI...`
- **Q03** (Epidemiológica Simples): got=62607 exp=30272
  SQL: `SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE si...`
- **Q05** (Epidemiológica Simples): got=81 exp=2274
  SQL: `SELECT COUNT(DISTINCT n_aih) AS num_obitos
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema =...`
- **Q08** (Epidemiológica Simples): got=2554 exp=27027
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE s...`
- **Q09** (Epidemiológica Simples): Falha DuckDB: Binder Error: Values list "e" does not have a column named "n_aih"

LINE 1: SELECT COUNT(DISTINCT e.n_aih) AS numero_internacoes
                              ^
  SQL: `SELECT COUNT(DISTINCT e.n_aih) AS numero_internacoes
FROM processed p
JOIN enriched e ON p.row_id = e.row_id
WHERE p.sis...`
- **Q10** (Epidemiológica Simples): got=1912 exp=28165
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE s...`
- **Q11** (Epidemiológica Simples): got=508 exp=215
  SQL: `SELECT COUNT(DISTINCT cod_munic_residencia) AS num_municipios_distintos
FROM processed
WHERE sistema = 'SIH'
  AND uf_or...`
- **Q12** (Epidemiológica Simples): got=166 exp=385
  SQL: `SELECT COUNT(DISTINCT cod_munic_estabelecimento) AS num_estabelecimentos
FROM processed
WHERE sistema = 'SIH'
  AND uf_o...`
- **Q13** (Epidemiológica Simples): got=1521 exp=17778
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano...`
- **Q14** (Epidemiológica Simples): got=0 exp=1
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE u...`
- **Q15** (Epidemiológica Simples): got=18446 exp=15626
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE s...`
- **Q16** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'M861', 'internacoes_unicas': 957} exp={'cid_principal': 'S720', 'total_internacoes': 9059}
  SQL: `SELECT 
    cid_principal, 
    COUNT(DISTINCT n_aih) AS internacoes_unicas
FROM 
    processed
JOIN 
    enriched e ON ...`
- **Q17** (Epidemiológica Complexa): Linha 1: got={'cod_munic_residencia': '355030', 'num_internacoes': 1240} exp={'cod_munic_estabelecimento': '350000', 'internacoes': 32140}
  SQL: `SELECT 
    cod_munic_residencia, 
    COUNT(DISTINCT n_aih) AS num_internacoes
FROM 
    processed
JOIN 
    enriched e...`
- **Q18** (Epidemiológica Complexa): Linha não encontrada: {'faixa_etaria': '0-17', 'total': 4040}
  SQL: `SELECT 
    CASE 
        WHEN idade_paciente BETWEEN 0 AND 17 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 59...`
- **Q19** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'M463', 'total_internacoes': 6, 'total_obitos': 2.0, 'taxa_mortalidade': 33.3333333333} exp={'cid_principal': 'S062', 'total_internacoes': 58, 'total_obitos': 20.0, 'taxa_mortalidade_pct': 34.48}
  SQL: `SELECT 
    cid_principal,
    COUNT(DISTINCT n_aih) AS total_internacoes,
    SUM(morte) AS total_obitos,
    (SUM(mort...`
- **Q20** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'M216', 'volume': 5349} exp={'cid_principal': 'S720', 'total_procedimentos': 17325}
  SQL: `SELECT 
    cid_principal, 
    COUNT(*) AS volume
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_...`
- **Q21** (Epidemiológica Complexa): Linha não encontrada: {'sexo_paciente': 'M', 'total_internacoes': 42460, 'pct': 61.1}
  SQL: `SELECT 
    sexo_paciente,
    COUNT(DISTINCT n_aih) AS internacoes,
    COUNT(DISTINCT n_aih) * 1.0 / SUM(COUNT(DISTINC...`
- **Q22** (Epidemiológica Complexa): Linha 1: got={'num_internacoes_femur': 26253, 'num_internacoes_total': 209808, 'percentual_femur': 12.5128689087} exp={'total_s72': 30272, 's72_idosos': 19598.0, 'pct_idosos': 64.7}
  SQL: `WITH fratura_femur AS (
    SELECT COUNT(DISTINCT n_aih) AS num_internacoes_femur
    FROM processed
    JOIN enriched e...`
- **Q23** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'M412', 'media_permanencia': 60.0} exp={'cid_principal': 'S272', 'total_internacoes': 49, 'permanencia_media_dias': 14.3}
  SQL: `SELECT 
    cid_principal, 
    AVG(dias_perm) AS media_permanencia
FROM 
    processed
JOIN 
    enriched e ON processe...`
- **Q24** (Epidemiológica Complexa): Linha 1: got={'cod_munic_estabelecimento': '350000', 'volume_internacao': 3097} exp={'cnes_estabelecimento': '2078015', 'internacoes': 2257}
  SQL: `SELECT 
    cod_munic_estabelecimento, 
    COUNT(DISTINCT n_aih) AS volume_internacao
FROM 
    processed
JOIN 
    enr...`
- **Q25** (Epidemiológica Complexa): Linha não encontrada: {'raca_cor_paciente': '01', 'total': 42626, 'pct': 61.3}
  SQL: `SELECT 
    raca_cor_paciente, 
    COUNT(DISTINCT n_aih) AS num_internacoes
FROM 
    processed
JOIN 
    enriched e ON...`
- **Q26** (Epidemiológica Complexa): Número de linhas: got=1 exp=10
  SQL: `SELECT 
    cid_principal, 
    SUM(qt_diarias) AS total_dias_internacao
FROM 
    processed
JOIN 
    enriched e ON pro...`
- **Q27** (Epidemiológica Complexa): Número de linhas: got=0 exp=5
  SQL: `SELECT 
    cod_procedimento, 
    COUNT(*) AS quantidade_realizada
FROM 
    processed
WHERE 
    sistema = 'SIA' 
    ...`
- **Q28** (Epidemiológica Complexa): Linha 1: got={'total_internations': 3729, 'internations_with_other_municipality': 3739.0, 'proportion': 1.0026816841} exp={'total_internacoes': 69487, 'deslocamento_intermunicipal': 39611.0, 'pct_deslocamento': 57.0}
  SQL: `WITH orthopedic_internations AS (
    SELECT
        p.n_aih,
        p.cod_munic_residencia,
        p.cod_munic_estabe...`
- **Q29** (Epidemiológica Complexa): Linha 1: got={'mes_cmpt': 1, 'sistema': 'SIA', 'volume': 605} exp={'mes_cmpt': 1, 'sistema': 'SIA', 'total': 8415}
  SQL: `SELECT 
    mes_cmpt,
    sistema,
    COUNT(*) AS volume
FROM 
    processed
WHERE 
    uf_origem = 'SP'
    AND ano_cm...`
- **Q30** (Epidemiológica Complexa): Linha 1: got={'idade_media': 62.3639245732} exp={'idade_media': 62.4, 'idade_minima': 0.0, 'idade_maxima': 99.0}
  SQL: `SELECT AVG(idade_paciente) AS idade_media
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE cid_princi...`
- **Q31** (Financeira): got=15790831.06 exp=187220821.79
  SQL: `SELECT SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = '...`
- **Q32** (Financeira): got=2295.1789331395 exp=2693.13
  SQL: `SELECT 
    COALESCE(SUM(custo_total), 0) / COUNT(DISTINCT n_aih) AS custo_medio_por_internacao
FROM 
    processed
JOIN...`
- **Q33** (Financeira): got=171429990.73000062 exp=82692089.44
  SQL: `SELECT SUM(COALESCE(custo_total, 0)) AS custo_total
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE ...`
- **Q34** (Financeira): Linha 1: got={'cid_principal': 'M511', 'total_custo': 1119270.5000000002} exp={'cid_principal': 'S720', 'internacoes': 9059, 'custo_total': 30400546.73}
  SQL: `SELECT 
    cid_principal, 
    SUM(custo_total) AS total_custo
FROM 
    processed
JOIN 
    enriched e ON processed.ro...`
- **Q35** (Financeira): Linha 1: got={'custo_total_ortopedia': None} exp={'internacoes_com_opme': 0, 'custo_total_opme': None}
  SQL: `SELECT SUM(COALESCE(val_ortp, 0)) AS custo_total_ortopedia
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
 ...`
- **Q36** (Financeira): got=3059022.389999998 exp=32810269.18
  SQL: `SELECT SUM(COALESCE(val_sp, 0)) AS total_honorarios
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND an...`
- **Q37** (Financeira): got=404.4575344501 exp=441.21
  SQL: `SELECT 
    COALESCE(SUM(custo_total) / SUM(qt_diarias), 0) AS custo_medio_por_dia
FROM 
    processed
JOIN 
    enriche...`
- **Q38** (Financeira): Linha 1: got={'cod_munic_estabelecimento': '350000', 'total_custo': 7641321.079999998} exp={'cnes_estabelecimento': '2078015', 'internacoes': 2257, 'custo_total': 11295149.42}
  SQL: `SELECT 
    cod_munic_estabelecimento, 
    SUM(custo_total) AS total_custo
FROM 
    processed
WHERE 
    sistema = 'SI...`
- **Q39** (Financeira): got=2109063.400000005 exp=9092221.75
  SQL: `SELECT SUM(COALESCE(custo_total, 0)) AS total_custo
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND an...`
- **Q40** (Financeira): Linha 1: got={'total_val_sh': 12531360.409999998, 'total_val_sp': 3059022.3899999997, 'proporcao_val_sh_val_sp': 4.0965245795} exp={'custo_servico_hospitalar': 153782049.46, 'honorarios_profissionais': 32810269.18, 'pct_hospitalar': 82.4}
  SQL: `SELECT 
    COALESCE(SUM(val_sh), 0) AS total_val_sh,
    COALESCE(SUM(val_sp), 0) AS total_val_sp,
    (COALESCE(SUM(va...`
- **Q41** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'internacoes_ortopédicas': 507} exp={'mes_cmpt': 1, 'total_internacoes': 5667}
  SQL: `SELECT 
    mes_cmpt,
    COUNT(DISTINCT n_aih) AS internacoes_ortopédicas
FROM 
    processed
JOIN 
    enriched e ON p...`
- **Q42** (Temporal/Comparativa): Número de linhas: got=0 exp=12
  SQL: `SELECT 
    mes_cmpt, 
    COUNT(*) AS quantidade_procedimentos
FROM 
    processed
WHERE 
    sistema = 'SIA' 
    AND ...`
- **Q44** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'custo_total': 1163311.8100000008} exp={'mes_cmpt': 1, 'custo_total': 16004871.37}
  SQL: `SELECT 
    mes_cmpt,
    SUM(COALESCE(custo_total, 0)) AS custo_total
FROM 
    processed
JOIN 
    enriched e ON proce...`
- **Q45** (Temporal/Comparativa): Número de linhas: got=12 exp=24
  SQL: `SELECT 
    mes_cmpt,
    COUNT(DISTINCT CASE WHEN icd_group = 'S00-T98' THEN n_aih END) AS trauma_internacoes,
    COUN...`
- **Q46** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'total_obitos': 6} exp={'mes_cmpt': 1, 'total_obitos': 198}
  SQL: `SELECT 
    mes_cmpt,
    COUNT(DISTINCT n_aih) AS total_obitos
FROM 
    processed
JOIN 
    enriched e ON processed.ro...`
- **Q47** (Temporal/Comparativa): Número de linhas: got=12 exp=4
  SQL: `SELECT 
    AVG(dias_perm) AS media_permanencia,
    competencia_ano_mes / 100 AS trimestre
FROM 
    processed
JOIN 
  ...`
- **Q48** (Temporal/Comparativa): got=8 exp=1
  SQL: `SELECT mes_cmpt, AVG(custo_total) AS media_custo
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sis...`
- **Q49** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'count_s72': 6619} exp={'mes_cmpt': 1, 'total_fraturas': 2467}
  SQL: `SELECT 
    mes_cmpt,
    COUNT(*) AS count_s72
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e.row_id
...`
- **Q50** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'volume_internacoes': 147} exp={'mes_cmpt': 1, 'total_internacoes': 2318}
  SQL: `SELECT 
    mes_cmpt,
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
JOIN 
    enriched e ON proces...`
- **Q51** (Epidemiológica Simples): got=24473 exp=15639
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE s...`
- **Q52** (Epidemiológica Simples): got=38134 exp=14633
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE s...`
- **Q53** (Epidemiológica Simples): got=2388 exp=14718
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE s...`
- **Q54** (Epidemiológica Simples): got=0 exp=47792
  SQL: `SELECT COUNT(*) AS numero_procedimentos
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'S...`
- **Q55** (Epidemiológica Simples): got=4515 exp=55711
  SQL: `SELECT COUNT(*) AS numero_procedimentos
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'S...`
- **Q56** (Epidemiológica Simples): got=62607 exp=6827
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE s...`
- **Q57** (Epidemiológica Simples): got=62607 exp=2264
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE s...`
- **Q59** (Epidemiológica Simples): got=559 exp=4040
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE s...`
- **Q60** (Epidemiológica Simples): got=1349 exp=16748
  SQL: `SELECT COUNT(DISTINCT n_aih) AS num_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'SP'
  AND ano_cm...`
- **Q63** (Epidemiológica Simples): got=177 exp=1037
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE s...`
- **Q64** (Epidemiológica Simples): got=0 exp=57572
  SQL: `SELECT COUNT(*) AS num_procedimentos
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIA'...`
- **Q68** (Epidemiológica Simples): got=0 exp=400
  SQL: `SELECT COUNT(DISTINCT cod_munic_residencia) AS num_municipios
FROM processed
JOIN enriched e ON processed.row_id = e.row...`
- **Q69** (Epidemiológica Complexa): Número de linhas: got=0 exp=5
  SQL: `SELECT 
    cod_munic_residencia,
    COUNT(*) AS volume_procedimentos
FROM 
    processed
JOIN 
    enriched e ON proce...`
- **Q70** (Epidemiológica Complexa): Número de linhas: got=0 exp=3
  SQL: `SELECT 
    FLOOR(idade_paciente / 10) * 10 AS faixa_etaria,
    COUNT(*) AS quantidade_procedimentos
FROM 
    processe...`
- **Q71** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'M869', 'num_obitos': 17} exp={'cid_principal': 'S720', 'total_internacoes': 9059, 'total_obitos': 478.0}
  SQL: `SELECT 
    cid_principal, 
    COUNT(*) AS num_obitos
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e....`
- **Q72** (Epidemiológica Complexa): Linha não encontrada: {'sexo_paciente': 'F', 'permanencia_media': 6.3}
  SQL: `SELECT 
    sexo_paciente,
    AVG(dias_perm) AS media_dias_perm
FROM 
    processed
JOIN 
    enriched e ON processed.r...`
- **Q73** (Epidemiológica Complexa): Número de linhas: got=0 exp=10
  SQL: `SELECT 
    cod_munic_estabelecimento, 
    COUNT(*) AS volume_procedimentos
FROM 
    processed
JOIN 
    enriched e ON...`
- **Q74** (Epidemiológica Complexa): Linha não encontrada: {'sexo_paciente': 'F', 'total_obitos': 1050.0, 'taxa_mortalidade': 3.89}
  SQL: `SELECT 
    sexo_paciente,
    SUM(CASE WHEN morte = 1 THEN 1 ELSE 0 END) AS total_obitos,
    COUNT(DISTINCT n_aih) AS ...`
- **Q75** (Epidemiológica Complexa): Número de linhas: got=16 exp=6
  SQL: `SELECT 
    icd_group,
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente < 30 THEN '18-...`
- **Q76** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'S272', 'media_permanencia': 14.2653061224, 'quantidade_internacoes': 49} exp={'cid_principal': 'S272', 'internacoes': 49, 'permanencia_media': 14.3}
  SQL: `SELECT 
    cid_principal, 
    AVG(dias_perm) AS media_permanencia, 
    COUNT(DISTINCT n_aih) AS quantidade_internacoe...`
- **Q77** (Epidemiológica Complexa): Linha não encontrada: {'faixa_etaria': '0-17', 'total': 4040}
  SQL: `SELECT 
  CASE 
    WHEN idade_paciente BETWEEN 0 AND 17 THEN '0-17'
    WHEN idade_paciente BETWEEN 18 AND 39 THEN '18-...`
- **Q78** (Epidemiológica Complexa): Linha 1: got={'cod_munic_estabelecimento': '350000', 'volume_internacoes': 12849} exp={'cnes_estabelecimento': '2091399', 'internacoes': 826}
  SQL: `SELECT 
    cod_munic_estabelecimento, 
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
JOIN 
    en...`
- **Q79** (Epidemiológica Complexa): Número de linhas: got=8 exp=3
  SQL: `SELECT 
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente < 30 THEN '18-29'
        WHE...`
- **Q81** (Epidemiológica Complexa): got=1769 exp=27009
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE s...`
- **Q82** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'M216', 'quantidade': 1846} exp={'cid_principal': 'S720', 'total': 11745}
  SQL: `SELECT 
    cid_principal, 
    COUNT(*) AS quantidade
FROM 
    processed
JOIN 
    enriched e ON processed.row_id = e....`
- **Q83** (Epidemiológica Complexa): got=350410 exp=180.5
  SQL: `SELECT 
    cod_munic_estabelecimento,
    COUNT(DISTINCT n_aih) AS num_internacoes
FROM 
    processed
JOIN 
    enrich...`
- **Q84** (Epidemiológica Complexa): Número de linhas: got=5 exp=4
  SQL: `SELECT 
    CASE 
        WHEN dias_perm < 3 THEN '0-2 dias'
        WHEN dias_perm < 7 THEN '3-6 dias'
        WHEN dia...`
- **Q85** (Financeira): got=5568723.949999998 exp=83044181.8
  SQL: `SELECT SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = '...`
- **Q87** (Financeira): got=None exp=6792.17
  SQL: `SELECT SUM(COALESCE(val_uti, 0)) AS custo_total_uti
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE ...`
- **Q88** (Financeira): Linha não encontrada: {'sexo_paciente': 'F', 'custo_medio': 2710.26}
  SQL: `SELECT 
    sexo_paciente,
    AVG(custo_total) AS custo_medio_internacao
FROM 
    processed
WHERE 
    sistema = 'SIH'...`
- **Q89** (Financeira): got=938048.9300000002 exp=7933442.51
  SQL: `SELECT SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.r...`
- **Q90** (Financeira): got=3390.9169584955 exp=4390.48
  SQL: `SELECT 
    AVG(custo_total) AS custo_medio_por_internacao
FROM 
    processed
JOIN 
    enriched e ON processed.row_id ...`
- **Q91** (Financeira): Linha 1: got={'cod_munic_residencia': '355030', 'total_custo': 2802662.5899999994} exp={'cod_munic_estabelecimento': '350000', 'custo_total': 98503323.97}
  SQL: `SELECT 
    cod_munic_residencia,
    SUM(custo_total) AS total_custo
FROM 
    processed
JOIN 
    enriched e ON proces...`
- **Q92** (Financeira): got=12531360.41 exp=153782049.46
  SQL: `SELECT SUM(COALESCE(val_sh, 0)) AS valor_total_servico_hospitalar
FROM processed
JOIN enriched e ON processed.row_id = e...`
- **Q93** (Financeira): Número de linhas: got=10 exp=3
  SQL: `SELECT 
    FLOOR(idade_paciente / 10) * 10 AS faixa_etaria,
    AVG(custo_total) AS custo_medio_por_internacao
FROM 
  ...`
- **Q94** (Financeira): got=1778326.4 exp=40878597.03
  SQL: `SELECT SUM(COALESCE(val_uti, 0)) AS total_uti
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistem...`
- **Q97** (Financeira): Número de linhas: got=0 exp=5
  SQL: `WITH filtered_data AS (
    SELECT
        cid_principal,
        AVG(custo_total) AS avg_cost
    FROM
        processe...`
- **Q98** (Financeira): Resultado vazio
  SQL: `SELECT 
    cod_procedimento,
    AVG(custo_total) AS custo_medio
FROM 
    processed
JOIN 
    enriched e ON processed....`
- **Q99** (Financeira): Linha 1: got={'cod_procedimento': '0702100048', 'total_custo': 45189222.96000072} exp={'cod_procedimento': '0701020369', 'custo_total': 1261008.0}
  SQL: `SELECT 
    cod_procedimento, 
    SUM(custo_total) AS total_custo
FROM 
    processed
WHERE 
    sistema = 'SIA' 
    A...`
- **Q100** (Financeira): got=2241104.49 exp=37989386.14
  SQL: `SELECT SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.r...`
- **Q101** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 9, 'num_obitos': 12} exp={'mes_cmpt': 11, 'total_obitos': 209}
  SQL: `SELECT mes_cmpt, COUNT(DISTINCT n_aih) AS num_obitos
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE...`
- **Q102** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'volume_internacoes': 2171} exp={'mes_cmpt': 1, 'internacoes': 1597}
  SQL: `SELECT 
    mes_cmpt,
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
JOIN 
    enriched e ON proces...`
- **Q103** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'custo_total': 494055.7599999998} exp={'mes_cmpt': 1, 'custo_total': 7166029.19}
  SQL: `SELECT 
    mes_cmpt,
    SUM(COALESCE(custo_total, 0)) AS custo_total
FROM 
    processed
JOIN 
    enriched e ON proce...`
- **Q104** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 12, 'total_custo': 15716790.150000032} exp={'mes_cmpt': 8, 'custo_total': 7745411.5}
  SQL: `SELECT mes_cmpt, SUM(custo_total) AS total_custo
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sis...`
- **Q106** (Temporal/Comparativa): Número de linhas: got=0 exp=24
  SQL: `SELECT 
    mes_cmpt,
    sexo_paciente,
    COUNT(*) AS volume_procedimentos
FROM 
    processed
WHERE 
    sistema = '...`
- **Q107** (Temporal/Comparativa): Número de linhas: got=1 exp=8
  SQL: `SELECT
    icd_group,
    TRUNC(competencia_ano_mes, -2) AS trimestre,
    COUNT(DISTINCT n_aih) AS internacoes_unicas
F...`
- **Q108** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 11, 'media_permanencia': 6.4865900383} exp={'mes_cmpt': 11, 'permanencia_media': 6.4}
  SQL: `SELECT mes_cmpt, AVG(dias_perm) AS media_permanencia
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE...`
- **Q109** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'taxa_mortalidade': 0.0118343195} exp={'mes_cmpt': 1, 'internacoes': 5667, 'obitos': 198.0, 'taxa_mortalidade': 3.49}
  SQL: `SELECT 
    mes_cmpt,
    COUNT(DISTINCT CASE WHEN morte = 1 THEN n_aih END) / COUNT(DISTINCT n_aih) AS taxa_mortalidade...`
- **Q110** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 8, 'volume_internacoes': 2816} exp={'trimestre': 'Q3', 'total': 7947}
  SQL: `SELECT 
    mes_cmpt,
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
JOIN 
    enriched e ON proces...`
- **Q111** (Temporal/Comparativa): Número de linhas: got=12 exp=2
  SQL: `SELECT 
    mes_cmpt,
    COUNT(DISTINCT n_aih) AS num_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH' 
    A...`
- **Q112** (Temporal/Comparativa): Linha 1: got={'custo_medio_mensal': 2724.4534211593, 'mes_cmpt': 1} exp={'mes_cmpt': 1, 'custo_medio': 2724.45}
  SQL: `SELECT 
    AVG(custo_total) AS custo_medio_mensal,
    mes_cmpt
FROM 
    processed
JOIN 
    enriched e ON processed.r...`
- **Q113** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'sexo_paciente': 'F', 'volume_internacoes': 207} exp={'mes_cmpt': 1, 'sexo_paciente': 'F', 'total': 2219}
  SQL: `SELECT 
    mes_cmpt,
    sexo_paciente,
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
JOIN 
    e...`
- **Q114** (Temporal/Comparativa): Número de linhas: got=0 exp=1
  SQL: `SELECT mes_cmpt
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'SP'
  AND ano_cmpt = 2023
  AND cod_procedimento...`
- **Q115** (Temporal/Comparativa): Número de linhas: got=0 exp=4
  SQL: `SELECT 
    mes_cmpt, 
    COUNT(*) AS volume_procedimentos
FROM 
    processed
WHERE 
    sistema = 'SIA' 
    AND uf_o...`
- **Q116** (Temporal/Comparativa): Número de linhas: got=0 exp=12
  SQL: `SELECT 
    mes_cmpt,
    SUM(COALESCE(custo_total, 0)) AS custo_mensal
FROM 
    processed
WHERE 
    sistema = 'SIA'
 ...`
- **Q117** (Temporal/Comparativa): Número de linhas: got=12 exp=2
  SQL: `SELECT 
    mes_cmpt,
    SUM(COALESCE(custo_total, 0)) AS custo_total
FROM 
    processed
WHERE 
    sistema = 'SIH'
  ...`
- **Q118** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'media_permanencia': 6.2184108527} exp={'mes_cmpt': 1, 'permanencia_media': 6.6}
  SQL: `SELECT 
    mes_cmpt,
    AVG(dias_perm) AS media_permanencia
FROM 
    processed
JOIN 
    enriched e ON processed.row_...`

---
_Gerado por scripts/evaluate_benchmark.py — SUS Data RAG — USF/Mestrado_