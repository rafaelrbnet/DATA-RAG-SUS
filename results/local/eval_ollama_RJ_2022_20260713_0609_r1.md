# Benchmark Evaluation Report — SUS Data RAG

**Model:** ollama  
**Run date:** 2026-07-13 06:09  
**API:** http://127.0.0.1:8000  

## Summary

| Metric | Value |
|---|---|
| Total queries | 118 |
| Correct (EA numerator) | 87 |
| **Execution Accuracy (EA)** | **73.7%** |
| Wilson IC 95% | [65.1%, 80.8%] |
| Correct via flexible match (alias diferente) | 63 |
| API errors / timeouts | 0 |
| Mean Time-to-Answer | 9.3s |

> **Nota metodológica — Scoring:** A Execution Accuracy avalia se o SQL gerado
> retorna os mesmos **valores** que o gold-standard, independente do nome das colunas.
> Quando o LLM usa um alias diferente mas retorna os dados corretos (match posicional),
> a query é contada como correta e sinalizada com `~`. Esta decisão segue a definição
> de EA em [Lee et al., 2022] — comparação de conjuntos de resultado, não de SQL texto.

## EA by Category

| Category | Correct | Total | EA |
|---|---:|---:|---:|
| Epidemiológica Simples | 32 | 33 | 97.0% |
| Epidemiológica Complexa | 14 | 31 | 45.2% |
| Financeira | 20 | 26 | 76.9% |
| Temporal/Comparativa | 21 | 28 | 75.0% |

## Query Results

### ✅ Q01 — Epidemiológica Simples

**Q:** Total de internações ortopédicas (M00-M99 e S00-S99) em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=28056 exp=28056`
**TTA:** 7868ms

---

### ✅ Q02 — Epidemiológica Simples

**Q:** Total de procedimentos ambulatoriais ortopédicos em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(*) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=27201 exp=27201`
**TTA:** 6840ms

---

### ✅ Q03 — Epidemiológica Simples

**Q:** Total de internações por fratura de fêmur (S72) em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND cid_principal LIKE 'S72%';
```

**Resultado:** `got=9945 exp=9945`
**TTA:** 6082ms

---

### ✅ Q04 — Epidemiológica Simples

**Q:** Total de internações por osteoartrose (M16, M17) em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M16%' OR cid_principal LIKE 'M17%');
```

**Resultado:** `got=102 exp=102`
**TTA:** 6921ms

---

### ✅ Q05 — Epidemiológica Simples

**Q:** Número de óbitos em internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND morte = 1;
```

**Resultado:** `got=965 exp=965`
**TTA:** 8027ms

---

### ✅ Q06 — Epidemiológica Simples

**Q:** Total de internações por traumatismos (S00-T98) em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND icd_group = 'S00-T98';
```

**Resultado:** `got=25731 exp=25731`
**TTA:** 6282ms

---

### ✅ Q07 — Epidemiológica Simples

**Q:** Total de internações por doenças osteomusculares (M00-M99) em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND icd_group = 'M00-M99';
```

**Resultado:** `got=2325 exp=2325`
**TTA:** 6253ms

---

### ✅ Q08 — Epidemiológica Simples

**Q:** Número de mulheres internadas por causa ortopédica em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND sexo_paciente = 'F'
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=10444 exp=10444`
**TTA:** 7933ms

---

### ✅ Q09 — Epidemiológica Simples

**Q:** Número de homens internados por causa ortopédica em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND sexo_paciente = 'M';
```

**Resultado:** `got=17612 exp=17612`
**TTA:** 7944ms

---

### ✅ Q10 — Epidemiológica Simples

**Q:** Número de internações ortopédicas de idosos (60 anos ou mais) em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes_idosos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND idade_paciente >= 60;
```

**Resultado:** `got=10149 exp=10149`
**TTA:** 8248ms

---

### ✅ Q11 — Epidemiológica Simples

**Q:** Número de municípios distintos com internação ortopédica registrada em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT cod_munic_estabelecimento) AS total_municipios
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=63 exp=63`
**TTA:** 7640ms

---

### ✅ Q12 — Epidemiológica Simples

**Q:** Número de estabelecimentos distintos com internação ortopédica em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT cnes_estabelecimento) AS total_estabelecimentos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=135 exp=135`
**TTA:** 7587ms

---

### ✅ Q13 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com permanência superior a 7 dias em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND dias_perm > 7;
```

**Resultado:** `got=9244 exp=9244`
**TTA:** 7805ms

---

### ✅ Q14 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com uso de UTI em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes_com_uti
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND uti_int_to > 0;
```

**Resultado:** `got=0 exp=0`
**TTA:** 8195ms

---

### ✅ Q15 — Epidemiológica Simples

**Q:** Número de internações por fratura de quadril (S72) em pacientes com 70 anos ou mais em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND cid_principal LIKE 'S72%'
  AND idade_paciente >= 70;
```

**Resultado:** `got=5140 exp=5140`
**TTA:** 7531ms

---

### ✅ Q16 — Epidemiológica Complexa

**Q:** Top 10 CIDs ortopédicos por volume de internação em RJ em 2022

**SQL gerado:**
```sql
SELECT 
  cid_principal,
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY 
  cid_principal
ORDER BY 
  total_internacoes DESC
LIMIT 10;
```

**Resultado:** `10 linhas corretas (flexível)`
**TTA:** 10631ms

---

### ✅ Q17 — Epidemiológica Complexa

**Q:** Top 5 municípios de RJ com maior número de internações ortopédicas em 2022

**SQL gerado:**
```sql
SELECT cod_munic_estabelecimento,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cod_munic_estabelecimento
ORDER BY internacoes DESC
LIMIT 5;
```

**Resultado:** `5 linhas corretas (flexível)`
**TTA:** 10224ms

---

### ❌ Q18 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por faixa etária (0-17, 18-59, 60+) em RJ em 2022

**SQL gerado:**
```sql
SELECT 
  CASE 
    WHEN idade_paciente >= 0 AND idade_paciente <= 17 THEN '0-17'
    WHEN idade_paciente >= 18 AND idade_paciente <= 59 THEN '18-59'
    ELSE '60+'
  END AS faixa_etaria,
  COUNT(DISTINCT n_aih) AS total_internacoes,
  ROUND(100.0 * COUNT(DISTINCT n_aih) / SUM(COUNT(DISTINCT n_aih)) OVER (), 1) AS pct
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY faixa_etaria
ORDER BY total_internacoes DESC;
```

**Resultado:** `Linha não encontrada: {'faixa_etaria': '0-17', 'total': 2380}`
**TTA:** 16660ms

---

### ❌ Q19 — Epidemiológica Complexa

**Q:** Taxa de mortalidade hospitalar por CID ortopédico (top 5) em RJ em 2022

**SQL gerado:**
```sql
SELECT 
  cid_principal,
  COUNT(CASE WHEN morte = 1 THEN n_aih END) AS total_obitos,
  COUNT(DISTINCT n_aih) AS total_internacoes,
  ROUND(100.0 * COUNT(CASE WHEN morte = 1 THEN n_aih END) / COUNT(DISTINCT n_aih), 2) AS taxa_mortalidade
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY 
  cid_principal
ORDER BY 
  taxa_mortalidade DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cid_principal': 'S497', 'total_obitos': 1, 'total_internacoes': 1, 'taxa_mortalidade': 100.0} exp={'cid_principal': 'T203', 'total_internacoes': 44, 'total_obitos': 26.0, 'taxa_mortalidade_pct': 59.09}`
**TTA:** 14379ms

---

### ✅ Q20 — Epidemiológica Complexa

**Q:** Top 10 CIDs ambulatoriais ortopédicos por volume de produção em RJ em 2022

**SQL gerado:**
```sql
SELECT 
  cid_principal,
  COUNT(*) AS total_producao
FROM 
  processed
WHERE 
  sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY 
  cid_principal
ORDER BY 
  total_producao DESC
LIMIT 10;
```

**Resultado:** `10 linhas corretas (flexível)`
**TTA:** 9153ms

---

### ✅ Q21 — Epidemiológica Complexa

**Q:** Proporção de internações ortopédicas por sexo em RJ em 2022

**SQL gerado:**
```sql
SELECT 
  sexo_paciente,
  COUNT(DISTINCT n_aih) AS total_internacoes,
  ROUND(100.0 * COUNT(DISTINCT n_aih) / SUM(COUNT(DISTINCT n_aih)) OVER (), 1) AS pct
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY sexo_paciente
ORDER BY total_internacoes DESC;
```

**Resultado:** `2 linhas corretas, ordem livre (flexível)`
**TTA:** 11581ms

---

### ❌ Q22 — Epidemiológica Complexa

**Q:** Número de internações por fratura de fêmur (S72) em idosos (60+) comparado ao total em RJ em 2022

**SQL gerado:**
```sql
SELECT 
  COUNT(DISTINCT n_aih) AS total_internacoes_femur,
  ROUND(100.0 * COUNT(DISTINCT n_aih) / SUM(COUNT(DISTINCT n_aih)) OVER (), 1) AS pct
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND cid_principal LIKE 'S72%'
  AND idade_paciente >= 60;
```

**Resultado:** `Linha 1: got={'total_internacoes_femur': 6628, 'pct': 100.0} exp={'total_s72': 9945, 's72_idosos': 6628.0, 'pct_idosos': 66.6}`
**TTA:** 9748ms

---

### ❌ Q23 — Epidemiológica Complexa

**Q:** Top 5 CIDs ortopédicos com maior permanência média hospitalar em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    cid_principal,
    ROUND(AVG(dias_perm), 1) AS permanencia_media_dias
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY 
    cid_principal
ORDER BY 
    permanencia_media_dias DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cid_principal': 'T142', 'permanencia_media_dias': 59.0} exp={'cid_principal': 'T203', 'total_internacoes': 44, 'permanencia_media_dias': 30.1}`
**TTA:** 9840ms

---

### ✅ Q24 — Epidemiológica Complexa

**Q:** Top 5 estabelecimentos com maior volume de internação ortopédica em RJ em 2022

**SQL gerado:**
```sql
SELECT cnes_estabelecimento,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cnes_estabelecimento
ORDER BY internacoes DESC
LIMIT 5;
```

**Resultado:** `5 linhas corretas (flexível)`
**TTA:** 9067ms

---

### ✅ Q25 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por raça/cor do paciente em RJ em 2022

**SQL gerado:**
```sql
SELECT raca_cor_paciente,
  COUNT(DISTINCT n_aih) AS total_internacoes,
  ROUND(100.0 * COUNT(DISTINCT n_aih) / SUM(COUNT(DISTINCT n_aih)) OVER (), 1) AS pct
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY raca_cor_paciente
ORDER BY total_internacoes DESC;
```

**Resultado:** `6 linhas corretas, ordem livre (flexível)`
**TTA:** 11653ms

---

### ❌ Q26 — Epidemiológica Complexa

**Q:** CIDs ortopédicos com maior número de dias totais de internação em RJ em 2022

**SQL gerado:**
```sql
SELECT cid_principal,
       SUM(dias_perm) AS total_dias_internacao
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cid_principal
ORDER BY total_dias_internacao DESC;
```

**Resultado:** `Número de linhas: got=742 exp=10`
**TTA:** 8520ms

---

### ✅ Q27 — Epidemiológica Complexa

**Q:** Top 5 procedimentos ambulatoriais ortopédicos mais realizados em RJ em 2022

**SQL gerado:**
```sql
SELECT cod_procedimento,
  COUNT(*) AS total_realizados
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cod_procedimento
ORDER BY total_realizados DESC
LIMIT 5;
```

**Resultado:** `5 linhas corretas (flexível)`
**TTA:** 8432ms

---

### ❌ Q28 — Epidemiológica Complexa

**Q:** Proporção de internações ortopédicas com paciente de outro município em RJ em 2022

**SQL gerado:**
```sql
SELECT 
  ROUND(100.0 * COUNT(CASE WHEN cod_munic_residencia != cod_munic_estabelecimento THEN n_aih END) / COUNT(DISTINCT n_aih), 1) AS proporcao_deslocamento
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `Linha 1: got={'proporcao_deslocamento': 50.6} exp={'total_internacoes': 28056, 'deslocamento_intermunicipal': 14192.0, 'pct_deslocamento': 50.6}`
**TTA:** 10181ms

---

### ❌ Q29 — Epidemiológica Complexa

**Q:** Comparativo de volume mensal SIA vs SIH ortopédico em RJ em 2022

**SQL gerado:**
```sql
SELECT 
  mes_cmpt,
  COUNT(*) AS total_ortopedico,
  sistema
FROM processed
WHERE uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY mes_cmpt, sistema
ORDER BY mes_cmpt, sistema;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'total_ortopedico': 1647, 'sistema': 'SIA'} exp={'mes_cmpt': 1, 'sistema': 'SIA', 'total': 1647}`
**TTA:** 8181ms

---

### ❌ Q30 — Epidemiológica Complexa

**Q:** Idade média dos pacientes internados por fratura de fêmur (S72) em RJ em 2022

**SQL gerado:**
```sql
SELECT ROUND(AVG(idade_paciente), 1) AS idade_media
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND cid_principal LIKE 'S72%';
```

**Resultado:** `Linha 1: got={'idade_media': 63.3} exp={'idade_media': 63.3, 'idade_minima': 0.0, 'idade_maxima': 99.0}`
**TTA:** 5977ms

---

### ✅ Q31 — Financeira

**Q:** Custo total de todas as internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=70893354.22 exp=70893354.22`
**TTA:** 7853ms

---

### ✅ Q32 — Financeira

**Q:** Custo médio por internação ortopédica em RJ em 2022

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)) / COUNT(DISTINCT n_aih), 2) AS custo_medio_por_internacao
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=2526.85 exp=2525.77`
**TTA:** 8854ms

---

### ✅ Q33 — Financeira

**Q:** Custo total de internações por fratura de fêmur (S72) em RJ em 2022

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND cid_principal LIKE 'S72%';
```

**Resultado:** `got=26337280.38 exp=26337280.38`
**TTA:** 6523ms

---

### ❌ Q34 — Financeira

**Q:** Top 5 CIDs ortopédicos com maior custo total de internação em RJ em 2022

**SQL gerado:**
```sql
SELECT cid_principal,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cid_principal
ORDER BY custo_total DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cid_principal': 'S720', 'custo_total': 10821184.25} exp={'cid_principal': 'S720', 'internacoes': 3502, 'custo_total': 10821184.25}`
**TTA:** 9314ms

---

### ❌ Q35 — Financeira

**Q:** Custo total de internações ortopédicas com uso de OPME (val_ortp > 0) em RJ em 2022

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND val_ortp > 0;
```

**Resultado:** `Linha 1: got={'custo_total': None} exp={'internacoes_com_opme': 0, 'custo_total_opme': None}`
**TTA:** 8582ms

---

### ✅ Q36 — Financeira

**Q:** Valor total de honorários profissionais (val_sp) em internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(val_sp, 0)), 2) AS valor_total_honorarios
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=13402482.02 exp=13402482.02`
**TTA:** 7944ms

---

### ✅ Q37 — Financeira

**Q:** Custo médio por dia de internação ortopédica em RJ em 2022

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)) / SUM(dias_perm), 2) AS custo_medio_por_dia
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=321.98 exp=321.98`
**TTA:** 8656ms

---

### ❌ Q38 — Financeira

**Q:** Top 5 estabelecimentos com maior custo total de internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT cnes_estabelecimento,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cnes_estabelecimento
ORDER BY custo_total DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cnes_estabelecimento': '2290227', 'custo_total': 9418220.5} exp={'cnes_estabelecimento': '2290227', 'internacoes': 5043, 'custo_total': 9418220.5}`
**TTA:** 9980ms

---

### ✅ Q39 — Financeira

**Q:** Custo total de procedimentos ambulatoriais ortopédicos em RJ em 2022

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=4149578.04 exp=4149578.04`
**TTA:** 7840ms

---

### ❌ Q40 — Financeira

**Q:** Proporção custo serviço hospitalar vs honorários em internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT 
  ROUND(100.0 * SUM(COALESCE(val_sh, 0)) / SUM(COALESCE(custo_total, 0)), 2) AS pct_val_sh,
  ROUND(100.0 * SUM(COALESCE(val_sp, 0)) / SUM(COALESCE(custo_total, 0)), 2) AS pct_val_sp
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `Linha 1: got={'pct_val_sh': 81.09, 'pct_val_sp': 18.91} exp={'custo_servico_hospitalar': 57490872.2, 'honorarios_profissionais': 13402482.02, 'pct_hospitalar': 81.1}`
**TTA:** 12404ms

---

### ❌ Q41 — Temporal/Comparativa

**Q:** Distribuição mensal de internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT 
  mes_cmpt,
  COUNT(DISTINCT n_aih) AS total_internacoes,
  ROUND(100.0 * COUNT(DISTINCT n_aih) / SUM(COUNT(DISTINCT n_aih)) OVER (), 1) AS pct
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'total_internacoes': 1998, 'pct': 7.1} exp={'mes_cmpt': 1, 'total_internacoes': 1998}`
**TTA:** 11348ms

---

### ❌ Q42 — Temporal/Comparativa

**Q:** Distribuição mensal de procedimentos ambulatoriais ortopédicos em RJ em 2022

**SQL gerado:**
```sql
SELECT 
  mes_cmpt,
  COUNT(*) AS total_procedimentos,
  ROUND(100.0 * COUNT(*) / SUM(COUNT(*)) OVER (), 1) AS pct
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'total_procedimentos': 1647, 'pct': 6.1} exp={'mes_cmpt': 1, 'procedimentos': 1647}`
**TTA:** 10194ms

---

### ✅ Q43 — Temporal/Comparativa

**Q:** Mês com maior número de internações por fratura de fêmur (S72) em RJ em 2022

**SQL gerado:**
```sql
SELECT mes_cmpt,
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND cid_principal LIKE 'S72%'
GROUP BY mes_cmpt
ORDER BY total_internacoes DESC
LIMIT 1;
```

**Resultado:** `got=10 exp=10`
**TTA:** 7685ms

---

### ✅ Q44 — Temporal/Comparativa

**Q:** Evolução mensal do custo total de internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT 
  mes_cmpt,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Resultado:** `12 linhas corretas (exato)`
**TTA:** 9330ms

---

### ❌ Q45 — Temporal/Comparativa

**Q:** Volume mensal de internações por trauma (S00-T98) vs doenças musculoesqueléticas (M00-M99) em RJ em 2022

**SQL gerado:**
```sql
SELECT 
  mes_cmpt,
  COUNT(DISTINCT CASE WHEN icd_group = 'S00-T98' THEN n_aih END) AS internacoes_trauma,
  COUNT(DISTINCT CASE WHEN icd_group = 'M00-M99' THEN n_aih END) AS internacoes_musculoesqueletica
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Resultado:** `Número de linhas: got=12 exp=24`
**TTA:** 10555ms

---

### ✅ Q46 — Temporal/Comparativa

**Q:** Evolução mensal de óbitos em internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT 
  mes_cmpt,
  COUNT(DISTINCT CASE WHEN morte = 1 THEN n_aih END) AS total_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Resultado:** `12 linhas corretas (exato)`
**TTA:** 9274ms

---

### ✅ Q47 — Temporal/Comparativa

**Q:** Permanência média por trimestre nas internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN mes_cmpt BETWEEN 1 AND 3 THEN 'Q1'
    WHEN mes_cmpt BETWEEN 4 AND 6 THEN 'Q2'
    WHEN mes_cmpt BETWEEN 7 AND 9 THEN 'Q3'
    ELSE 'Q4'
  END AS trimestre,
  ROUND(AVG(dias_perm), 1) AS permanencia_media_dias
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY trimestre
ORDER BY trimestre;
```

**Resultado:** `4 linhas corretas, ordem livre (exato)`
**TTA:** 13142ms

---

### ✅ Q48 — Temporal/Comparativa

**Q:** Mês com maior custo médio por internação ortopédica em RJ em 2022

**SQL gerado:**
```sql
SELECT mes_cmpt,
  ROUND(AVG(custo_total), 2) AS custo_medio_por_internacao
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY mes_cmpt
ORDER BY custo_medio_por_internacao DESC
LIMIT 1;
```

**Resultado:** `got=11 exp=11`
**TTA:** 9756ms

---

### ✅ Q49 — Temporal/Comparativa

**Q:** Sazonalidade mensal de fraturas de fêmur (S72) em RJ em 2022

**SQL gerado:**
```sql
SELECT 
  mes_cmpt,
  COUNT(*) AS total_fraturas_femur
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND cid_principal LIKE 'S72%'
GROUP BY 
  mes_cmpt
ORDER BY 
  mes_cmpt;
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 7633ms

---

### ✅ Q50 — Temporal/Comparativa

**Q:** Volume mensal de internações ortopédicas de idosos (60 anos ou mais) em RJ em 2022

**SQL gerado:**
```sql
SELECT 
  mes_cmpt,
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND idade_paciente >= 60
GROUP BY 
  mes_cmpt
ORDER BY 
  mes_cmpt;
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 10070ms

---

### ✅ Q51 — Epidemiológica Simples

**Q:** Número de internações por fratura de fêmur (S72) em mulheres em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND cid_principal LIKE 'S72%'
  AND sexo_paciente = 'F';
```

**Resultado:** `got=5436 exp=5436`
**TTA:** 6614ms

---

### ✅ Q52 — Epidemiológica Simples

**Q:** Número de internações por fratura de fêmur (S72) em homens em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND cid_principal LIKE 'S72%'
  AND sexo_paciente = 'M';
```

**Resultado:** `got=4509 exp=4509`
**TTA:** 6575ms

---

### ✅ Q53 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com permanência de 1 dia ou menos em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND dias_perm <= 1;
```

**Resultado:** `got=5619 exp=5619`
**TTA:** 7859ms

---

### ✅ Q54 — Epidemiológica Simples

**Q:** Número de procedimentos ambulatoriais ortopédicos realizados em mulheres em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(*) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND sexo_paciente = 'F'
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=12838 exp=12838`
**TTA:** 7506ms

---

### ✅ Q55 — Epidemiológica Simples

**Q:** Número de procedimentos ambulatoriais ortopédicos realizados por idosos (60 anos ou mais) em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(*) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND idade_paciente >= 60
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=13298 exp=13298`
**TTA:** 7555ms

---

### ✅ Q56 — Epidemiológica Simples

**Q:** Número de internações por fratura de tíbia e perônio (S82) em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND cid_principal LIKE 'S82%';
```

**Resultado:** `got=2991 exp=2991`
**TTA:** 6016ms

---

### ✅ Q57 — Epidemiológica Simples

**Q:** Número de internações por fratura de antebraço (S52) em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND cid_principal LIKE 'S52%';
```

**Resultado:** `got=1447 exp=1447`
**TTA:** 5951ms

---

### ✅ Q58 — Epidemiológica Simples

**Q:** Número de internações por fratura de ombro e úmero (S42) em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND cid_principal LIKE 'S42%';
```

**Resultado:** `got=765 exp=765`
**TTA:** 5985ms

---

### ✅ Q59 — Epidemiológica Simples

**Q:** Número de internações ortopédicas em crianças (menores de 18 anos) em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND idade_paciente < 18
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=2380 exp=2380`
**TTA:** 7997ms

---

### ✅ Q60 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com permanência entre 8 e 30 dias em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND dias_perm BETWEEN 8 AND 30;
```

**Resultado:** `got=8235 exp=8235`
**TTA:** 8155ms

---

### ✅ Q61 — Epidemiológica Simples

**Q:** Número de internações por artrose de quadril e joelho (M16 e M17) em idosos (60 anos ou mais) em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M16%' OR cid_principal LIKE 'M17%')
  AND idade_paciente >= 60;
```

**Resultado:** `got=62 exp=62`
**TTA:** 7596ms

---

### ✅ Q62 — Epidemiológica Simples

**Q:** Número de internações por fratura de fêmur (S72) em mulheres com 70 anos ou mais em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND cid_principal LIKE 'S72%'
  AND sexo_paciente = 'F'
  AND idade_paciente >= 70;
```

**Resultado:** `got=3841 exp=3841`
**TTA:** 7363ms

---

### ✅ Q63 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com permanência superior a 30 dias em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND dias_perm > 30;
```

**Resultado:** `got=1010 exp=1010`
**TTA:** 7940ms

---

### ✅ Q64 — Epidemiológica Simples

**Q:** Número de procedimentos ambulatoriais ortopédicos realizados em homens em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(*) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND sexo_paciente = 'M'
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=14363 exp=14363`
**TTA:** 7525ms

---

### ✅ Q65 — Epidemiológica Simples

**Q:** Número de internações por traumatismos (grupo S00-T98) em idosos (60 anos ou mais) em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND icd_group = 'S00-T98'
  AND idade_paciente >= 60;
```

**Resultado:** `got=9563 exp=9563`
**TTA:** 6963ms

---

### ✅ Q66 — Epidemiológica Simples

**Q:** Número de internações por doenças osteomusculares (grupo M00-M99) em mulheres em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND icd_group = 'M00-M99'
  AND sexo_paciente = 'F';
```

**Resultado:** `got=880 exp=880`
**TTA:** 6900ms

---

### ✅ Q67 — Epidemiológica Simples

**Q:** Número de óbitos em internações por fratura de fêmur (S72) em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND cid_principal LIKE 'S72%'
  AND morte = 1;
```

**Resultado:** `got=425 exp=425`
**TTA:** 6420ms

---

### ❌ Q68 — Epidemiológica Simples

**Q:** Número de municípios distintos com registro de procedimentos ambulatoriais ortopédicos em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT cod_munic_residencia) AS total_municipios
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=100 exp=74`
**TTA:** 7485ms

---

### ❌ Q69 — Epidemiológica Complexa

**Q:** Top 5 municípios por volume de procedimentos ambulatoriais ortopédicos em RJ em 2022

**SQL gerado:**
```sql
SELECT cod_munic_residencia,
  COUNT(*) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cod_munic_residencia
ORDER BY total_procedimentos DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cod_munic_residencia': '330455', 'total_procedimentos': 6779} exp={'cod_munic_estabelecimento': '330455', 'total': 7104}`
**TTA:** 9024ms

---

### ❌ Q70 — Epidemiológica Complexa

**Q:** Distribuição de procedimentos ambulatoriais ortopédicos por faixa etária em RJ em 2022

**SQL gerado:**
```sql
SELECT 
  CASE 
    WHEN idade_paciente < 18 THEN '0-17'
    WHEN idade_paciente BETWEEN 18 AND 35 THEN '18-35'
    WHEN idade_paciente BETWEEN 36 AND 59 THEN '36-59'
    ELSE '60+'
  END AS faixa_etaria,
  COUNT(*) AS total,
  ROUND(100.0 * COUNT(*) / SUM(COUNT(*)) OVER (), 1) AS pct
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY faixa_etaria
ORDER BY total DESC;
```

**Resultado:** `Número de linhas: got=4 exp=3`
**TTA:** 15435ms

---

### ❌ Q71 — Epidemiológica Complexa

**Q:** Top 5 diagnósticos com maior número de óbitos em internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    cid_principal,
    COUNT(*) AS total_obitos
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
    AND morte = 1
GROUP BY 
    cid_principal
ORDER BY 
    total_obitos DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cid_principal': 'S720', 'total_obitos': 187} exp={'cid_principal': 'S720', 'total_internacoes': 3502, 'total_obitos': 187.0}`
**TTA:** 9653ms

---

### ✅ Q72 — Epidemiológica Complexa

**Q:** Permanência hospitalar média por sexo em internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT 
  sexo_paciente,
  ROUND(AVG(dias_perm), 1) AS permanencia_media_dias
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY sexo_paciente;
```

**Resultado:** `2 linhas corretas, ordem livre (flexível)`
**TTA:** 8493ms

---

### ✅ Q73 — Epidemiológica Complexa

**Q:** Top 10 estabelecimentos por volume de procedimentos ambulatoriais ortopédicos em RJ em 2022

**SQL gerado:**
```sql
SELECT cnes_estabelecimento,
  COUNT(*) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cnes_estabelecimento
ORDER BY total_procedimentos DESC
LIMIT 10;
```

**Resultado:** `10 linhas corretas (flexível)`
**TTA:** 9133ms

---

### ✅ Q74 — Epidemiológica Complexa

**Q:** Taxa de mortalidade por sexo em internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT 
  sexo_paciente,
  COUNT(CASE WHEN morte = 1 THEN n_aih END) AS total_obitos,
  COUNT(DISTINCT n_aih) AS total_internacoes,
  ROUND(100.0 * COUNT(CASE WHEN morte = 1 THEN n_aih END) / COUNT(DISTINCT n_aih), 2) AS taxa_mortalidade
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY 
  sexo_paciente;
```

**Resultado:** `2 linhas corretas, ordem livre (exato)`
**TTA:** 13590ms

---

### ❌ Q75 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por grupo diagnóstico (M e S) e faixa etária em RJ em 2022

**SQL gerado:**
```sql
SELECT 
  icd_group,
  CASE 
    WHEN idade_paciente < 18 THEN '0-17'
    WHEN idade_paciente BETWEEN 18 AND 34 THEN '18-34'
    WHEN idade_paciente BETWEEN 35 AND 49 THEN '35-49'
    WHEN idade_paciente BETWEEN 50 AND 64 THEN '50-64'
    ELSE '65+'
  END AS faixa_etaria,
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY icd_group, faixa_etaria
ORDER BY total_internacoes DESC;
```

**Resultado:** `Número de linhas: got=10 exp=6`
**TTA:** 16735ms

---

### ❌ Q76 — Epidemiológica Complexa

**Q:** Top 5 diagnósticos de traumatismo com maior permanência média em RJ em 2022 (mínimo 40 internações)

**SQL gerado:**
```sql
SELECT 
  cid_principal,
  ROUND(AVG(dias_perm), 1) AS permanencia_media_dias,
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND icd_group = 'S00-T98'
GROUP BY cid_principal
HAVING COUNT(DISTINCT n_aih) >= 40
ORDER BY permanencia_media_dias DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cid_principal': 'T203', 'permanencia_media_dias': 30.1, 'total_internacoes': 44} exp={'cid_principal': 'T203', 'internacoes': 44, 'permanencia_media': 30.1}`
**TTA:** 10648ms

---

### ❌ Q77 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por faixa etária (0-17, 18-39, 40-59, 60-79, 80+) em RJ em 2022

**SQL gerado:**
```sql
SELECT 
  CASE
    WHEN idade_paciente >= 0 AND idade_paciente <= 17 THEN '0-17'
    WHEN idade_paciente >= 18 AND idade_paciente <= 39 THEN '18-39'
    WHEN idade_paciente >= 40 AND idade_paciente <= 59 THEN '40-59'
    WHEN idade_paciente >= 60 AND idade_paciente <= 79 THEN '60-79'
    ELSE '80+'
  END AS faixa_etaria,
  COUNT(DISTINCT n_aih) AS internacoes,
  ROUND(100.0 * COUNT(DISTINCT n_aih) / SUM(COUNT(DISTINCT n_aih)) OVER (), 1) AS pct
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY faixa_etaria
ORDER BY internacoes DESC;
```

**Resultado:** `Linha não encontrada: {'faixa_etaria': '0-17', 'total': 2380}`
**TTA:** 19985ms

---

### ✅ Q78 — Epidemiológica Complexa

**Q:** Top 5 estabelecimentos com maior volume de internações por fratura de fêmur (S72) em RJ em 2022

**SQL gerado:**
```sql
SELECT cnes_estabelecimento,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND cid_principal LIKE 'S72%'
GROUP BY cnes_estabelecimento
ORDER BY internacoes DESC
LIMIT 5;
```

**Resultado:** `5 linhas corretas (exato)`
**TTA:** 7881ms

---

### ❌ Q79 — Epidemiológica Complexa

**Q:** Permanência hospitalar média por faixa etária em internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN idade_paciente < 18 THEN '0-17'
    WHEN idade_paciente BETWEEN 18 AND 34 THEN '18-34'
    WHEN idade_paciente BETWEEN 35 AND 49 THEN '35-49'
    WHEN idade_paciente BETWEEN 50 AND 64 THEN '50-64'
    ELSE '65+'
  END AS faixa_etaria,
  ROUND(AVG(dias_perm), 1) AS permanencia_media_dias
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY faixa_etaria
ORDER BY faixa_etaria;
```

**Resultado:** `Número de linhas: got=5 exp=3`
**TTA:** 16375ms

---

### ✅ Q80 — Epidemiológica Complexa

**Q:** Top 5 diagnósticos de doença osteomuscular (M00-M99) por número de internações em RJ em 2022

**SQL gerado:**
```sql
SELECT cid_principal,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND icd_group = 'M00-M99'
GROUP BY cid_principal
ORDER BY internacoes DESC
LIMIT 5;
```

**Resultado:** `5 linhas corretas (flexível)`
**TTA:** 7616ms

---

### ✅ Q81 — Epidemiológica Complexa

**Q:** Número de internações ortopédicas com permanência entre 3 e 7 dias em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND dias_perm BETWEEN 3 AND 7;
```

**Resultado:** `got=9617 exp=9617`
**TTA:** 8163ms

---

### ✅ Q82 — Epidemiológica Complexa

**Q:** Top 5 diagnósticos ambulatoriais ortopédicos em idosos (60 anos ou mais) em RJ em 2022

**SQL gerado:**
```sql
SELECT cid_principal,
       COUNT(*) AS total_internacoes
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND idade_paciente >= 60
GROUP BY cid_principal
ORDER BY total_internacoes DESC
LIMIT 5;
```

**Resultado:** `5 linhas corretas (flexível)`
**TTA:** 9324ms

---

### ❌ Q83 — Epidemiológica Complexa

**Q:** Média de internações ortopédicas por estabelecimento em RJ em 2022

**SQL gerado:**
```sql
SELECT 
  cnes_estabelecimento,
  ROUND(AVG(dias_perm), 1) AS media_internacoes_dias
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cnes_estabelecimento
ORDER BY media_internacoes_dias DESC;
```

**Resultado:** `got=0012513 exp=207.8`
**TTA:** 9816ms

---

### ❌ Q84 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por faixa de permanência em RJ em 2022

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN dias_perm BETWEEN 1 AND 3 THEN '1-3 dias'
    WHEN dias_perm BETWEEN 4 AND 7 THEN '4-7 dias'
    WHEN dias_perm BETWEEN 8 AND 14 THEN '8-14 dias'
    ELSE 'Mais de 14 dias'
  END AS faixa_permanencia,
  COUNT(DISTINCT n_aih) AS total_internacoes,
  ROUND(100.0 * COUNT(DISTINCT n_aih) / SUM(COUNT(DISTINCT n_aih)) OVER (), 1) AS pct
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY faixa_permanencia
ORDER BY total_internacoes DESC;
```

**Resultado:** `Linha não encontrada: {'faixa_permanencia': '1-3 dias', 'total': 10343}`
**TTA:** 17162ms

---

### ✅ Q85 — Financeira

**Q:** Custo total de internações ortopédicas de idosos (60 anos ou mais) em RJ em 2022

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND idade_paciente >= 60;
```

**Resultado:** `got=28396358.5 exp=28396358.5`
**TTA:** 8571ms

---

### ✅ Q86 — Financeira

**Q:** Custo médio por internação por fratura de fêmur (S72) em RJ em 2022

**SQL gerado:**
```sql
SELECT ROUND(AVG(COALESCE(custo_total, 0)), 2) AS custo_medio_por_internacao
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND cid_principal LIKE 'S72%';
```

**Resultado:** `got=2648.29 exp=2648.29`
**TTA:** 6909ms

---

### ✅ Q87 — Financeira

**Q:** Custo total de internações ortopédicas com passagem por UTI em RJ em 2022

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND uti_int_to > 0;
```

**Resultado:** `got=None exp=None`
**TTA:** 8581ms

---

### ✅ Q88 — Financeira

**Q:** Custo médio por internação ortopédica por sexo do paciente em RJ em 2022

**SQL gerado:**
```sql
SELECT 
  sexo_paciente,
  ROUND(AVG(custo_total), 2) AS custo_medio_por_internacao
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY sexo_paciente;
```

**Resultado:** `2 linhas corretas, ordem livre (flexível)`
**TTA:** 8672ms

---

### ✅ Q89 — Financeira

**Q:** Custo total de internações ortopédicas em crianças (menores de 18 anos) em RJ em 2022

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND idade_paciente < 18
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=5562026.72 exp=5562026.72`
**TTA:** 8598ms

---

### ✅ Q90 — Financeira

**Q:** Custo médio por internação ortopédica com permanência superior a 7 dias em RJ em 2022

**SQL gerado:**
```sql
SELECT ROUND(AVG(COALESCE(custo_total, 0)), 2) AS custo_medio_por_internacao
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND dias_perm > 7;
```

**Resultado:** `got=3699.42 exp=3699.42`
**TTA:** 8910ms

---

### ✅ Q91 — Financeira

**Q:** Top 3 municípios com maior custo total em internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT cod_munic_estabelecimento,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY cod_munic_estabelecimento
ORDER BY custo_total DESC
LIMIT 3;
```

**Resultado:** `3 linhas corretas (exato)`
**TTA:** 10186ms

---

### ✅ Q92 — Financeira

**Q:** Valor total de serviço hospitalar em internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(val_sh, 0)), 2) AS valor_total_servico_hospitalar
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=57490872.2 exp=57490872.2`
**TTA:** 8080ms

---

### ❌ Q93 — Financeira

**Q:** Custo médio por internação ortopédica por faixa etária em RJ em 2022

**SQL gerado:**
```sql
SELECT 
  CASE 
    WHEN idade_paciente < 18 THEN 'Menor de 18 anos'
    WHEN idade_paciente BETWEEN 18 AND 35 THEN '18 a 35 anos'
    WHEN idade_paciente BETWEEN 36 AND 50 THEN '36 a 50 anos'
    WHEN idade_paciente BETWEEN 51 AND 65 THEN '51 a 65 anos'
    ELSE '65 anos ou mais'
  END AS faixa_etaria,
  ROUND(AVG(COALESCE(custo_total, 0)), 2) AS custo_medio_por_internacao
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY faixa_etaria
ORDER BY faixa_etaria;
```

**Resultado:** `Número de linhas: got=5 exp=3`
**TTA:** 17712ms

---

### ✅ Q94 — Financeira

**Q:** Valor total de UTI em internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(val_uti, 0)), 2) AS valor_total_uti
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=13056994.92 exp=13056994.92`
**TTA:** 7926ms

---

### ✅ Q95 — Financeira

**Q:** Custo total de internações por doenças osteomusculares (M00-M99) em RJ em 2022

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND icd_group = 'M00-M99';
```

**Resultado:** `got=8405202.74 exp=8405202.74`
**TTA:** 6882ms

---

### ✅ Q96 — Financeira

**Q:** Custo total de internações por traumatismos (S00-T98) em RJ em 2022

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND icd_group = 'S00-T98';
```

**Resultado:** `got=62488151.48 exp=62488151.48`
**TTA:** 6807ms

---

### ✅ Q97 — Financeira

**Q:** Top 5 diagnósticos com maior custo médio por internação ortopédica em RJ em 2022 (mínimo 40 internações)

**SQL gerado:**
```sql
SELECT 
    cid_principal,
    COUNT(DISTINCT n_aih) AS total_internacoes,
    ROUND(AVG(custo_total), 2) AS custo_medio_por_internacao
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY 
    cid_principal
HAVING 
    total_internacoes >= 40
ORDER BY 
    custo_medio_por_internacao DESC
LIMIT 5;
```

**Resultado:** `5 linhas corretas (flexível)`
**TTA:** 12516ms

---

### ✅ Q98 — Financeira

**Q:** Custo médio por procedimento ambulatorial ortopédico em RJ em 2022

**SQL gerado:**
```sql
SELECT ROUND(AVG(COALESCE(custo_total, 0)), 2) AS custo_medio_procedimento
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98');
```

**Resultado:** `got=152.55 exp=152.55`
**TTA:** 8165ms

---

### ❌ Q99 — Financeira

**Q:** Top 5 procedimentos ambulatoriais com maior custo total em RJ em 2022

**SQL gerado:**
```sql
SELECT cod_procedimento,
  COUNT(*) AS quantidade,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
GROUP BY cod_procedimento
ORDER BY custo_total DESC
LIMIT 5;
```

**Resultado:** `Linha 1: got={'cod_procedimento': '0702100048', 'quantidade': 3114, 'custo_total': 9146187.89} exp={'cod_procedimento': '0701020369', 'custo_total': 665532.0}`
**TTA:** 7999ms

---

### ✅ Q100 — Financeira

**Q:** Custo total de internações ortopédicas de idosos (60+) com permanência superior a 7 dias em RJ em 2022

**SQL gerado:**
```sql
SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND idade_paciente >= 60
  AND dias_perm > 7;
```

**Resultado:** `got=16205065.06 exp=16205065.06`
**TTA:** 9302ms

---

### ✅ Q101 — Temporal/Comparativa

**Q:** Mês com maior número de óbitos em internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT mes_cmpt,
       COUNT(*) AS total_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND morte = 1
GROUP BY mes_cmpt
ORDER BY total_obitos DESC
LIMIT 1;
```

**Resultado:** `1 linhas corretas (exato)`
**TTA:** 8968ms

---

### ✅ Q102 — Temporal/Comparativa

**Q:** Volume mensal de internações por fratura de fêmur (S72) em idosos (60 anos ou mais) em RJ em 2022

**SQL gerado:**
```sql
SELECT 
  mes_cmpt,
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND cid_principal LIKE 'S72%'
  AND idade_paciente >= 60
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 8116ms

---

### ✅ Q103 — Temporal/Comparativa

**Q:** Custo mensal de internações ortopédicas de idosos (60 anos ou mais) em RJ em 2022

**SQL gerado:**
```sql
SELECT 
  mes_cmpt,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_mensal
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
  AND idade_paciente >= 60
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 10206ms

---

### ✅ Q104 — Temporal/Comparativa

**Q:** Mês com maior custo total de internações por fratura de fêmur (S72) em RJ em 2022

**SQL gerado:**
```sql
SELECT mes_cmpt,
       ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND cid_principal LIKE 'S72%'
GROUP BY mes_cmpt
ORDER BY custo_total DESC
LIMIT 1;
```

**Resultado:** `1 linhas corretas (exato)`
**TTA:** 8141ms

---

### ✅ Q105 — Temporal/Comparativa

**Q:** Volume mensal de internações por traumatismos (grupo S00-T98) em RJ em 2022

**SQL gerado:**
```sql
SELECT 
  mes_cmpt,
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND icd_group = 'S00-T98'
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 7604ms

---

### ❌ Q106 — Temporal/Comparativa

**Q:** Volume mensal de procedimentos ambulatoriais ortopédicos por sexo em RJ em 2022

**SQL gerado:**
```sql
SELECT 
  mes_cmpt,
  sexo_paciente,
  COUNT(*) AS total_procedimentos,
  ROUND(100.0 * COUNT(*) / SUM(COUNT(*)) OVER (PARTITION BY mes_cmpt), 1) AS pct
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY mes_cmpt, sexo_paciente
ORDER BY mes_cmpt, sexo_paciente;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'sexo_paciente': 'F', 'total_procedimentos': 739, 'pct': 44.9} exp={'mes_cmpt': 1, 'sexo_paciente': 'F', 'total': 739}`
**TTA:** 12689ms

---

### ✅ Q107 — Temporal/Comparativa

**Q:** Comparativo trimestral de internações ortopédicas por grupo diagnóstico em RJ em 2022

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN mes_cmpt BETWEEN 1 AND 3 THEN 'Q1'
    WHEN mes_cmpt BETWEEN 4 AND 6 THEN 'Q2'
    WHEN mes_cmpt BETWEEN 7 AND 9 THEN 'Q3'
    ELSE 'Q4'
  END AS trimestre,
  icd_group,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY trimestre, icd_group
ORDER BY trimestre, internacoes DESC;
```

**Resultado:** `8 linhas corretas, ordem livre (flexível)`
**TTA:** 15335ms

---

### ✅ Q108 — Temporal/Comparativa

**Q:** Mês com maior permanência hospitalar média em internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT mes_cmpt,
       ROUND(AVG(dias_perm), 1) AS permanencia_media_dias
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY mes_cmpt
ORDER BY permanencia_media_dias DESC
LIMIT 1;
```

**Resultado:** `1 linhas corretas (flexível)`
**TTA:** 10449ms

---

### ❌ Q109 — Temporal/Comparativa

**Q:** Evolução mensal da taxa de mortalidade em internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT 
  mes_cmpt,
  ROUND(100.0 * SUM(CASE WHEN morte = 1 THEN 1 ELSE 0 END) / COUNT(*), 2) AS taxa_mortalidade
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'taxa_mortalidade': 5.21} exp={'mes_cmpt': 1, 'internacoes': 1998, 'obitos': 104.0, 'taxa_mortalidade': 5.21}`
**TTA:** 10640ms

---

### ✅ Q110 — Temporal/Comparativa

**Q:** Trimestre com maior volume de internações por fratura de fêmur (S72) em RJ em 2022

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN mes_cmpt BETWEEN 1 AND 3 THEN 'Q1'
    WHEN mes_cmpt BETWEEN 4 AND 6 THEN 'Q2'
    WHEN mes_cmpt BETWEEN 7 AND 9 THEN 'Q3'
    ELSE 'Q4'
  END AS trimestre,
  COUNT(DISTINCT n_aih) AS internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND cid_principal LIKE 'S72%'
GROUP BY trimestre
ORDER BY internacoes DESC
LIMIT 1;
```

**Resultado:** `1 linhas corretas (flexível)`
**TTA:** 11902ms

---

### ❌ Q111 — Temporal/Comparativa

**Q:** Comparativo semestral de internações ortopédicas (1º vs 2º semestre) em RJ em 2022

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN mes_cmpt BETWEEN 1 AND 6 THEN '1º Semestre'
    ELSE '2º Semestre'
  END AS semestre,
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY semestre
ORDER BY semestre;
```

**Resultado:** `Linha não encontrada: {'semestre': '1S', 'total': 12212}`
**TTA:** 10882ms

---

### ✅ Q112 — Temporal/Comparativa

**Q:** Custo médio mensal por internação por fratura de fêmur (S72) em RJ em 2022

**SQL gerado:**
```sql
SELECT 
  mes_cmpt,
  ROUND(AVG(custo_total), 2) AS custo_medio_mensal
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND cid_principal LIKE 'S72%'
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 7675ms

---

### ✅ Q113 — Temporal/Comparativa

**Q:** Volume mensal de internações ortopédicas por sexo do paciente em RJ em 2022

**SQL gerado:**
```sql
SELECT 
  mes_cmpt,
  sexo_paciente,
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY mes_cmpt, sexo_paciente
ORDER BY mes_cmpt, sexo_paciente;
```

**Resultado:** `24 linhas corretas (flexível)`
**TTA:** 9677ms

---

### ✅ Q114 — Temporal/Comparativa

**Q:** Mês com menor volume de procedimentos ambulatoriais ortopédicos em RJ em 2022

**SQL gerado:**
```sql
SELECT mes_cmpt,
       COUNT(*) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY mes_cmpt
ORDER BY total_procedimentos ASC
LIMIT 1;
```

**Resultado:** `1 linhas corretas (flexível)`
**TTA:** 8575ms

---

### ✅ Q115 — Temporal/Comparativa

**Q:** Volume trimestral de procedimentos ambulatoriais ortopédicos em RJ em 2022

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN mes_cmpt BETWEEN 1 AND 3 THEN 'Q1'
    WHEN mes_cmpt BETWEEN 4 AND 6 THEN 'Q2'
    WHEN mes_cmpt BETWEEN 7 AND 9 THEN 'Q3'
    ELSE 'Q4'
  END AS trimestre,
  COUNT(*) AS volume_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY trimestre
ORDER BY trimestre;
```

**Resultado:** `4 linhas corretas, ordem livre (flexível)`
**TTA:** 12477ms

---

### ✅ Q116 — Temporal/Comparativa

**Q:** Custo mensal de procedimentos ambulatoriais ortopédicos em RJ em 2022

**SQL gerado:**
```sql
SELECT 
  mes_cmpt,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_mensal
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 9407ms

---

### ❌ Q117 — Temporal/Comparativa

**Q:** Comparativo semestral do custo total de internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT
  CASE
    WHEN mes_cmpt BETWEEN 1 AND 6 THEN 'Semestre 1'
    ELSE 'Semestre 2'
  END AS semestre,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY semestre
ORDER BY semestre;
```

**Resultado:** `Linha não encontrada: {'semestre': '1S', 'custo_total': 29834491.27}`
**TTA:** 11408ms

---

### ✅ Q118 — Temporal/Comparativa

**Q:** Evolução mensal da permanência média em internações por fratura de fêmur (S72) em RJ em 2022

**SQL gerado:**
```sql
SELECT 
  mes_cmpt,
  ROUND(AVG(dias_perm), 1) AS permanencia_media_dias
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND cid_principal LIKE 'S72%'
GROUP BY mes_cmpt
ORDER BY mes_cmpt;
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 7535ms

---

## Failures Detail

- **Q18** (Epidemiológica Complexa): Linha não encontrada: {'faixa_etaria': '0-17', 'total': 2380}
  SQL: `SELECT 
  CASE 
    WHEN idade_paciente >= 0 AND idade_paciente <= 17 THEN '0-17'
    WHEN idade_paciente >= 18 AND idad...`
- **Q19** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'S497', 'total_obitos': 1, 'total_internacoes': 1, 'taxa_mortalidade': 100.0} exp={'cid_principal': 'T203', 'total_internacoes': 44, 'total_obitos': 26.0, 'taxa_mortalidade_pct': 59.09}
  SQL: `SELECT 
  cid_principal,
  COUNT(CASE WHEN morte = 1 THEN n_aih END) AS total_obitos,
  COUNT(DISTINCT n_aih) AS total_i...`
- **Q22** (Epidemiológica Complexa): Linha 1: got={'total_internacoes_femur': 6628, 'pct': 100.0} exp={'total_s72': 9945, 's72_idosos': 6628.0, 'pct_idosos': 66.6}
  SQL: `SELECT 
  COUNT(DISTINCT n_aih) AS total_internacoes_femur,
  ROUND(100.0 * COUNT(DISTINCT n_aih) / SUM(COUNT(DISTINCT n...`
- **Q23** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'T142', 'permanencia_media_dias': 59.0} exp={'cid_principal': 'T203', 'total_internacoes': 44, 'permanencia_media_dias': 30.1}
  SQL: `SELECT 
    cid_principal,
    ROUND(AVG(dias_perm), 1) AS permanencia_media_dias
FROM 
    processed
WHERE 
    sistema...`
- **Q26** (Epidemiológica Complexa): Número de linhas: got=742 exp=10
  SQL: `SELECT cid_principal,
       SUM(dias_perm) AS total_dias_internacao
FROM processed
WHERE sistema = 'SIH'
  AND uf_orige...`
- **Q28** (Epidemiológica Complexa): Linha 1: got={'proporcao_deslocamento': 50.6} exp={'total_internacoes': 28056, 'deslocamento_intermunicipal': 14192.0, 'pct_deslocamento': 50.6}
  SQL: `SELECT 
  ROUND(100.0 * COUNT(CASE WHEN cod_munic_residencia != cod_munic_estabelecimento THEN n_aih END) / COUNT(DISTIN...`
- **Q29** (Epidemiológica Complexa): Linha 1: got={'mes_cmpt': 1, 'total_ortopedico': 1647, 'sistema': 'SIA'} exp={'mes_cmpt': 1, 'sistema': 'SIA', 'total': 1647}
  SQL: `SELECT 
  mes_cmpt,
  COUNT(*) AS total_ortopedico,
  sistema
FROM processed
WHERE uf_origem = 'RJ'
  AND ano_cmpt = 202...`
- **Q30** (Epidemiológica Complexa): Linha 1: got={'idade_media': 63.3} exp={'idade_media': 63.3, 'idade_minima': 0.0, 'idade_maxima': 99.0}
  SQL: `SELECT ROUND(AVG(idade_paciente), 1) AS idade_media
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND an...`
- **Q34** (Financeira): Linha 1: got={'cid_principal': 'S720', 'custo_total': 10821184.25} exp={'cid_principal': 'S720', 'internacoes': 3502, 'custo_total': 10821184.25}
  SQL: `SELECT cid_principal,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AN...`
- **Q35** (Financeira): Linha 1: got={'custo_total': None} exp={'internacoes_com_opme': 0, 'custo_total_opme': None}
  SQL: `SELECT ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ...`
- **Q38** (Financeira): Linha 1: got={'cnes_estabelecimento': '2290227', 'custo_total': 9418220.5} exp={'cnes_estabelecimento': '2290227', 'internacoes': 5043, 'custo_total': 9418220.5}
  SQL: `SELECT cnes_estabelecimento,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM processed
WHERE sistema = 'SI...`
- **Q40** (Financeira): Linha 1: got={'pct_val_sh': 81.09, 'pct_val_sp': 18.91} exp={'custo_servico_hospitalar': 57490872.2, 'honorarios_profissionais': 13402482.02, 'pct_hospitalar': 81.1}
  SQL: `SELECT 
  ROUND(100.0 * SUM(COALESCE(val_sh, 0)) / SUM(COALESCE(custo_total, 0)), 2) AS pct_val_sh,
  ROUND(100.0 * SUM(...`
- **Q41** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'total_internacoes': 1998, 'pct': 7.1} exp={'mes_cmpt': 1, 'total_internacoes': 1998}
  SQL: `SELECT 
  mes_cmpt,
  COUNT(DISTINCT n_aih) AS total_internacoes,
  ROUND(100.0 * COUNT(DISTINCT n_aih) / SUM(COUNT(DIST...`
- **Q42** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'total_procedimentos': 1647, 'pct': 6.1} exp={'mes_cmpt': 1, 'procedimentos': 1647}
  SQL: `SELECT 
  mes_cmpt,
  COUNT(*) AS total_procedimentos,
  ROUND(100.0 * COUNT(*) / SUM(COUNT(*)) OVER (), 1) AS pct
FROM ...`
- **Q45** (Temporal/Comparativa): Número de linhas: got=12 exp=24
  SQL: `SELECT 
  mes_cmpt,
  COUNT(DISTINCT CASE WHEN icd_group = 'S00-T98' THEN n_aih END) AS internacoes_trauma,
  COUNT(DIST...`
- **Q68** (Epidemiológica Simples): got=100 exp=74
  SQL: `SELECT COUNT(DISTINCT cod_munic_residencia) AS total_municipios
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = '...`
- **Q69** (Epidemiológica Complexa): Linha 1: got={'cod_munic_residencia': '330455', 'total_procedimentos': 6779} exp={'cod_munic_estabelecimento': '330455', 'total': 7104}
  SQL: `SELECT cod_munic_residencia,
  COUNT(*) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'R...`
- **Q70** (Epidemiológica Complexa): Número de linhas: got=4 exp=3
  SQL: `SELECT 
  CASE 
    WHEN idade_paciente < 18 THEN '0-17'
    WHEN idade_paciente BETWEEN 18 AND 35 THEN '18-35'
    WHEN...`
- **Q71** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'S720', 'total_obitos': 187} exp={'cid_principal': 'S720', 'total_internacoes': 3502, 'total_obitos': 187.0}
  SQL: `SELECT 
    cid_principal,
    COUNT(*) AS total_obitos
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem...`
- **Q75** (Epidemiológica Complexa): Número de linhas: got=10 exp=6
  SQL: `SELECT 
  icd_group,
  CASE 
    WHEN idade_paciente < 18 THEN '0-17'
    WHEN idade_paciente BETWEEN 18 AND 34 THEN '18...`
- **Q76** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'T203', 'permanencia_media_dias': 30.1, 'total_internacoes': 44} exp={'cid_principal': 'T203', 'internacoes': 44, 'permanencia_media': 30.1}
  SQL: `SELECT 
  cid_principal,
  ROUND(AVG(dias_perm), 1) AS permanencia_media_dias,
  COUNT(DISTINCT n_aih) AS total_internac...`
- **Q77** (Epidemiológica Complexa): Linha não encontrada: {'faixa_etaria': '0-17', 'total': 2380}
  SQL: `SELECT 
  CASE
    WHEN idade_paciente >= 0 AND idade_paciente <= 17 THEN '0-17'
    WHEN idade_paciente >= 18 AND idade...`
- **Q79** (Epidemiológica Complexa): Número de linhas: got=5 exp=3
  SQL: `SELECT
  CASE
    WHEN idade_paciente < 18 THEN '0-17'
    WHEN idade_paciente BETWEEN 18 AND 34 THEN '18-34'
    WHEN i...`
- **Q83** (Epidemiológica Complexa): got=0012513 exp=207.8
  SQL: `SELECT 
  cnes_estabelecimento,
  ROUND(AVG(dias_perm), 1) AS media_internacoes_dias
FROM processed
WHERE sistema = 'SIH...`
- **Q84** (Epidemiológica Complexa): Linha não encontrada: {'faixa_permanencia': '1-3 dias', 'total': 10343}
  SQL: `SELECT
  CASE
    WHEN dias_perm BETWEEN 1 AND 3 THEN '1-3 dias'
    WHEN dias_perm BETWEEN 4 AND 7 THEN '4-7 dias'
    ...`
- **Q93** (Financeira): Número de linhas: got=5 exp=3
  SQL: `SELECT 
  CASE 
    WHEN idade_paciente < 18 THEN 'Menor de 18 anos'
    WHEN idade_paciente BETWEEN 18 AND 35 THEN '18 ...`
- **Q99** (Financeira): Linha 1: got={'cod_procedimento': '0702100048', 'quantidade': 3114, 'custo_total': 9146187.89} exp={'cod_procedimento': '0701020369', 'custo_total': 665532.0}
  SQL: `SELECT cod_procedimento,
  COUNT(*) AS quantidade,
  ROUND(SUM(COALESCE(custo_total, 0)), 2) AS custo_total
FROM process...`
- **Q106** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'sexo_paciente': 'F', 'total_procedimentos': 739, 'pct': 44.9} exp={'mes_cmpt': 1, 'sexo_paciente': 'F', 'total': 739}
  SQL: `SELECT 
  mes_cmpt,
  sexo_paciente,
  COUNT(*) AS total_procedimentos,
  ROUND(100.0 * COUNT(*) / SUM(COUNT(*)) OVER (P...`
- **Q109** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'taxa_mortalidade': 5.21} exp={'mes_cmpt': 1, 'internacoes': 1998, 'obitos': 104.0, 'taxa_mortalidade': 5.21}
  SQL: `SELECT 
  mes_cmpt,
  ROUND(100.0 * SUM(CASE WHEN morte = 1 THEN 1 ELSE 0 END) / COUNT(*), 2) AS taxa_mortalidade
FROM p...`
- **Q111** (Temporal/Comparativa): Linha não encontrada: {'semestre': '1S', 'total': 12212}
  SQL: `SELECT
  CASE
    WHEN mes_cmpt BETWEEN 1 AND 6 THEN '1º Semestre'
    ELSE '2º Semestre'
  END AS semestre,
  COUNT(DIS...`
- **Q117** (Temporal/Comparativa): Linha não encontrada: {'semestre': '1S', 'custo_total': 29834491.27}
  SQL: `SELECT
  CASE
    WHEN mes_cmpt BETWEEN 1 AND 6 THEN 'Semestre 1'
    ELSE 'Semestre 2'
  END AS semestre,
  ROUND(SUM(C...`

---
_Gerado por scripts/evaluate_benchmark.py — SUS Data RAG — USF/Mestrado_