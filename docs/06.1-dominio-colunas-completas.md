# 06.1 - Domínio e Normalização de Colunas (Modelo Canônico Consolidado)

Documento de normalização aplicado no `src/data/transform.py` para a camada `data/processed/**/*.parquet`.

## 1. Regras Globais

- **Normalização de nomes (antes da transformação):** coalesce de colunas duplicadas após normalização para snake_case e aliases legados.
- **Tratamento textual padrão:** `trim`; `""`, `"nan"`, `"None"`, `"<NA>"` -> `null`.
- **Tipagem canônica:** por coluna (`boolean`, `Int64`, `Float64`, `string`) conforme tabela abaixo.
- **Modelo de saída:** parquet unificado por `ano/uf/mês` (SIA + SIH no mesmo arquivo).
- **Observação de domínio:** `sistema` é o discriminador de origem (`SIA`/`SIH`). Já `opm_flag` e `fisio_flag` são marcadores canônicos de integração/curadoria analítica (não campos nativos padronizados dos dicionários públicos SIA/SIH).

Aliases legados aplicados:
- `year_comp -> ano_cmpt`
- `month_comp -> mes_cmpt`
- `system -> sistema`
- `uf -> uf_origem`
- `municip_res -> munic_res`
- `municip_mov -> munic_mov`
- `munResStatus/Tipo/Nome/Uf/Lat/Lon/Alt/Area -> mun_res_*`
- `pa_uf_mun -> pa_ufmun`
- `pa_mun_pcn -> pa_munpcn`

## 2. Regras por Coluna (Total Canônico + Técnico `107`)

| # | Coluna | Origem | Tipo Canônico | Regra de Normalização |
|---:|---|---|---|---|
| 0 | `row_id` | Técnica (pipeline) | `string` | Identificador técnico estável por linha para vincular `data/processed` e `data/enriched`. Gerado no `transform.py` a partir de `source_file` + `source_row`, com hash SHA-1 em hexadecimal (40 caracteres). |
| 1 | `idade_paciente` | Derivada (`pa_idade` no SIA, `idade` no SIH) | `Float64` | Tipo canônico `Float64`: limpeza textual (`trim`, `nan/None/<NA>` -> `null`) e conversão numérica com `coerce`. |
| 2 | `sexo_paciente` | Derivada (`pa_sexo` no SIA, `sexo` no SIH) | `string` | Normalização de domínio: `1/M/MASCULINO -> M`; `3/F/FEMININO -> F`; `0/9/vazio/inválido -> null`. Saída em `string`. |
| 3 | `raca_cor_paciente` | Derivada (`pa_racacor` no SIA, `raca_cor` no SIH) | `string` | Normalização de domínio para código oficial: `01` (Branca), `02` (Preta), `03` (Parda), `04` (Amarela), `05` (Indígena), `99` (Sem Informação). `0/9/vazio/inválido -> 99`. |
| 4 | `etnia_paciente` | Derivada (`pa_etnia` no SIA, `etnia` no SIH) | `string` | Código de etnia indígena unificado (domínio IBGE/FUNAI, 4 dígitos). Normalização canônica: remover ruído e manter 4 dígitos (`zfill` quando necessário); `0000`, vazios e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. Regra de negócio: só é válido quando `raca_cor_paciente = 05` (Indígena); caso contrário, `null`. |
| 5 | `cod_munic_residencia` | Derivada (`pa_munpcn` no SIA, `munic_res` no SIH) | `string` | Código IBGE de 6 dígitos (sem DV): remove espaços e não dígitos; se vier com 7+, trunca para 6; `000000`, `999999`, vazio e nulos técnicos -> `null`. |
| 6 | `cod_munic_estabelecimento` | Derivada (`pa_ufmun` no SIA, `uf_zi` no SIH) | `string` | Código de 6 dígitos: remove espaços e não dígitos; trunca para 6; `000000`, `999999`, vazio e nulos técnicos -> `null`. No SIH, valores `XX0000` podem indicar UF/gestão estadual (município físico depende de cruzamento com CNES). |
| 7 | `cnes_estabelecimento` | Derivada (`pa_coduni` no SIA, `cnes` no SIH) | `string` | CNES em 7 dígitos: remove espaços e não dígitos; mantém 7 últimos dígitos e aplica `zfill(7)`; `0000000`, `9999999`, vazio e nulos técnicos -> `null`. |
| 8 | `cod_procedimento` | Derivada (`pa_proc_id` no SIA, `proc_rea` no SIH) | `string` | Código SIGTAP em 10 dígitos: remove espaços e não dígitos; mantém 10 últimos dígitos e aplica `zfill(10)`; `0000000000`, vazio e nulos técnicos -> `null`. |
| 9 | `cid_principal` | Derivada (`pa_cidpri` no SIA, `diag_princ` no SIH) | `string` | CID-10 canônico: `trim`, `uppercase`, remoção de ponto (`.`) e caracteres não alfanuméricos; mantém formato válido `^[A-Z][0-9]{2}[0-9A-Z]?$` (3-4 caracteres); `0000`, `000`, `-`, vazio e nulos técnicos -> `null`. |
| 10 | `custo_total` | Derivada (`pa_valapr` no SIA, `val_tot` no SIH) | `Float64` | Conversão numérica com `errors='coerce'` após limpeza (`trim`, `nan/None/<NA>` -> `null`); suporta vírgula decimal (`150,50 -> 150.50`) e padrão misto (`1.234,56 -> 1234.56`); `0.00` é mantido como valor válido. |
| 11 | `competencia_ano_mes` | Derivada (`pa_cmp` no SIA, `ano_cmpt + mes_cmpt` no SIH) | `Int64` | Derivada e normalizada para `Int64 (AAAAMM)`: no SIA, usa `pa_cmp` (somente dígitos); no SIH, calcula `ano_cmpt*100 + mes_cmpt` (se `ano_cmpt` vier com 2 dígitos, aplica `+2000`); valida mês `01..12`; se inválido/nulo, usa fallback da partição (`ano/mês`). |
| 12 | `ano_cmpt` | Comum | `Int64` | Ano de competência canônico (`AAAA`): `trim`, coerção numérica com `errors='coerce'`, fallback por extração de dígitos; se vier com 2 dígitos (`23`), aplica `+2000` -> `2023`; valores fora de `2000..2099` (incluindo `0`) e inválidos -> `null`. |
| 13 | `mes_cmpt` | Comum | `Int64` | Mês de competência canônico (`1..12`): `trim` + coerção numérica; valores fora da faixa (`0`, `13`, `99`, inválidos) -> `null`; fallback opcional pelo `competencia_ano_mes` (extrai posições MM de `AAAAMM`). |
| 14 | `sistema` | Comum | `string` | Rótulo de origem canônico: `trim` + `uppercase`; mapeia `SIA/PA/BI/AD -> SIA` e `SIH/RD/RJ/ER -> SIH`; valores fora do padrão e nulos técnicos -> `null` (com fallback por evidência de colunas SIA/SIH quando possível). Campo usado para segregar produção ambulatorial vs hospitalar. |
| 15 | `uf_origem` | Comum | `string` | UF canônica (2 letras): `trim` + `uppercase`; aceita sigla direta (`SP`) e código numérico IBGE (`35 -> SP`), inclusive quando vier embutido em código municipal; mantém apenas as 27 UFs válidas; inválidos (`99`, `XX`, vazio, nulos técnicos) -> `null`; fallback via prefixo de `cod_munic_residencia`. |
| 16 | `main_icd` | Comum | `string` | CID-10 canônico: `trim`, `uppercase`, remoção de ponto (`.`) e caracteres não alfanuméricos; mantém formato válido `^[A-Z][0-9]{2}[0-9A-Z]?$` (3-4 caracteres); `0000`, `000`, `9999`, `999`, `-`, vazio e nulos técnicos -> `null`. |
| 17 | `icd_group` | Comum | `string` | Agrupamento CID-10 canônico: `trim` + `uppercase`; mantém valores válidos no formato faixa (`A00-B99`) ou `CAPITULO <ROMANO>`; se ausente/inválido, deriva a faixa pelo primeiro caractere de `main_icd` (ex.: `J -> J00-J99`); sem correspondência válida -> `null`. |
| 18 | `opm_flag` | Comum | `boolean` | Marcador canônico para itens OPM (Órteses/Próteses/Materiais Especiais) em camada analítica. Regra estrita: `1` (ou `1.0`) -> `true`; `0` (ou `0.0`) -> `false`; qualquer outro valor -> `null`. |
| 19 | `fisio_flag` | Comum | `boolean` | Marcador canônico para identificar produção de fisioterapia na camada analítica (não é coluna padronizada dos layouts públicos SIA/SIH). Regra estrita: `1` (ou `1.0`) -> `true`; `0` (ou `0.0`) -> `false`; qualquer outro valor -> `null`. |
| 20 | `mun_res_status` | Comum | `string` | Status de validade/consistência do município de residência (`cod_munic_residencia`) em registros SIA e SIH (ex.: código IBGE válido, incompatibilidade com UF, preenchimento genérico para casos específicos). Tipo canônico `string`: aplicar `trim`; valores vazios e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 21 | `mun_res_tipo` | Comum | `string` | Classificação/tipologia do município de residência no contexto SIA/SIH (suporte à regionalização assistencial e regras de gestão/faturamento entre entes). Tipo canônico `string` para preservar códigos textuais; aplicar `trim`; valores vazios e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 22 | `mun_res_nome` | Comum | `string` | Nome do município de residência do paciente em registros SIA (produção ambulatorial) e SIH (internação/AIH). Tipo canônico `string`: aplicar `trim`; valores vazios e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 23 | `mun_res_uf` | Comum | `string` | Identificação da UF de residência do paciente (contexto SIA/SIH), compatível com codificação geográfica IBGE usada no município de residência. Tipo canônico `string`: aplicar `trim`; valores vazios e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 24 | `mun_res_lat` | Comum | `Float64` | Latitude do município de residência (campo derivado para análise geoespacial em bases tratadas, não nativo dos layouts brutos BPA/APAC/AIH). Tipo canônico `Float64`: limpeza textual (`trim`, `nan/None/<NA>` -> `null`) e conversão numérica com `coerce` (`to_numeric(errors='coerce')`). |
| 25 | `mun_res_lon` | Comum | `Float64` | Longitude do município de residência (campo derivado para análise geoespacial a partir do código IBGE; centroide municipal, sem coordenada individual do paciente). Tipo canônico `Float64`: limpeza textual (`trim`, `nan/None/<NA>` -> `null`) e conversão numérica com `coerce` (`to_numeric(errors='coerce')`). |
| 26 | `mun_res_alt` | Comum | `Float64` | Altitude do município de residência (campo geoespacial derivado em base tratada, associado ao município IBGE; não representa “município alterado”). Tipo canônico `Float64`: limpeza textual (`trim`, `nan/None/<NA>` -> `null`) e conversão numérica com `coerce` (`to_numeric(errors='coerce')`). |
| 27 | `mun_res_area` | Comum | `Float64` | Área territorial do município de residência (campo geoespacial derivado em base tratada, associado ao município IBGE; não representa classificação urbano/rural). Tipo canônico `Float64`: limpeza textual (`trim`, `nan/None/<NA>` -> `null`) e conversão numérica com `coerce` (`to_numeric(errors='coerce')`). |
| 28 | `mun_res_zona` | Comum | `string` | Zona de residência do município em código canônico: `1` (urbana), `2` (rural), `0/9` e inválidos -> `null`. Tipo canônico `string`: aplicar `trim`; valores vazios e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 29 | `pa_cmp` | SIA | `string` | Competência da produção/atendimento ambulatorial no SIA (referência temporal de faturamento), usualmente no formato `AAAAMM` (ex.: `202305`). Tipo canônico `string`: aplicar `trim`; valores vazios e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 30 | `pa_mvm` | SIA | `string` | Mês/ano de processamento (movimentação) da produção ambulatorial no SIA, usualmente no formato `AAAAMM` (ex.: `202401`). Permite distinguir competência de execução vs competência de processamento/faturamento. Tipo canônico `string`: aplicar `trim`; valores vazios e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 31 | `idademin` | SIA | `Float64` | Limite mínimo de idade permitido para o procedimento conforme regras SIGTAP (validação de consistência entre idade do paciente e procedimento faturado no SIA). Tipo canônico `Float64`: limpeza textual (`trim`, `nan/None/<NA>` -> `null`) e conversão numérica com `coerce` (`to_numeric(errors='coerce')`). |
| 32 | `idademax` | SIA | `Float64` | Limite máximo de idade permitido para o procedimento conforme regras SIGTAP (validação de consistência no processamento SIA). Em tabelas de referência, `999` pode representar ausência de limite superior. Tipo canônico `Float64`: limpeza textual (`trim`, `nan/None/<NA>` -> `null`) e conversão numérica com `coerce` (`to_numeric(errors='coerce')`). |
| 33 | `pa_ufdif` | SIA | `string` | Indicador técnico de divergência entre UF de residência do paciente e UF do estabelecimento de atendimento (“invasão estadual”). Normalização canônica: `1/sim/true -> "1"`, `0/não/false -> "0"`, `9/99/ignorado/inválido -> null`; fallback por comparação da UF (2 primeiros dígitos) entre `cod_munic_residencia` e `cod_munic_estabelecimento`. Tipo canônico `string`: aplicar `trim`; nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 34 | `pa_mndif` | SIA | `string` | Indicador técnico de divergência entre município de residência do paciente e município do estabelecimento de atendimento (“invasão municipal”). Normalização canônica: `1/sim/true -> "1"`, `0/não/false -> "0"`, `9/99/ignorado/inválido -> null`; fallback por comparação entre `cod_munic_residencia` e `cod_munic_estabelecimento`. Tipo canônico `string`: aplicar `trim`; nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 35 | `pa_cnpjcpf` | SIA | `string` | Identificação do estabelecimento executante via CNPJ. Normalização canônica: remover pontuação/caracteres não numéricos, manter CNPJ com 14 dígitos (`zfill` quando necessário) e sem máscara; vazios, inválidos técnicos e `00000000000000` -> `null`. |
| 36 | `pa_cnpjmnt` | SIA | `string` | CNPJ da mantenedora do estabelecimento. Normalização canônica: remover pontuação/caracteres não numéricos, manter 14 dígitos sem máscara (`zfill` quando necessário). Valor `00000000000000` é preservado para representar ausência de mantenedora vinculada; vazios e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 37 | `pa_nat_jur` | SIA | `string` | Natureza jurídica do estabelecimento (domínio CONCLA/IBGE). Normalização canônica: remover máscara/caracteres não numéricos (ex.: `101-5 -> 1015`) e manter código de 4 dígitos; vazios, códigos incompletos e inválidos técnicos (`0000`, `9999`, `nan`, `None`, `<NA>`) -> `null`. |
| 38 | `pa_cnsmed` | SIA | `string` | CNS do profissional executante. Normalização canônica: remover caracteres não numéricos e manter 15 dígitos (`zfill` quando necessário); valores válidos normalmente iniciam em `1`, `2`, `7`, `8` ou `9`. O sentinel `000000000000000` é preservado para casos permitidos de consolidação (ex.: BPA-C); vazios, inválidos e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 39 | `pa_cbocod` | SIA | `string` | Código CBO do profissional executante (padrão CBO 2002). Normalização canônica: remover caracteres não numéricos, manter 6 dígitos com zero à esquerda (`zfill`) e sem máscara; vazios, `000000` e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 40 | `nome_proced` | SIA | `string` | Descrição textual do procedimento (domínio SIGTAP), vinculada ao código de procedimento. Normalização canônica: `trim` e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`, com limite defensivo de até 250 caracteres. |
| 41 | `pa_grupo` | SIA | `string` | Grupo SIGTAP do procedimento (2 primeiros dígitos do código de procedimento de 10 dígitos). Normalização canônica: remover caracteres não numéricos e manter 2 dígitos; `00/99`, vazios e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`; fallback pelos 2 primeiros dígitos de `cod_procedimento`. |
| 42 | `pa_subgru` | SIA | `string` | Subgrupo SIGTAP do procedimento (3º e 4º dígitos do código de procedimento de 10 dígitos). Normalização canônica: remover caracteres não numéricos e manter 2 dígitos; `00/99`, vazios e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`; fallback pelos dígitos 3-4 de `cod_procedimento`. |
| 43 | `pa_cidsec` | SIA | `string` | Diagnóstico secundário (CID-10) associado ao atendimento/procedimento. Normalização canônica de CID: `trim`, `uppercase`, remoção de ponto (`.`) e caracteres não alfanuméricos; mantém formato válido `^[A-Z][0-9]{2}[0-9A-Z]?$` (3-4 caracteres). Vazios, inválidos e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 44 | `pa_cidcas` | SIA | `string` | Diagnóstico de causa associada/externa (CID-10), usado para detalhar origem do agravo (ex.: acidentes/violências) em conjunto com o diagnóstico principal. Normalização canônica de CID: `trim`, `uppercase`, remoção de ponto (`.`) e caracteres não alfanuméricos; mantém formato válido `^[A-Z][0-9]{2}[0-9A-Z]?$` (3-4 caracteres). Vazios, inválidos e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 45 | `pa_qtdpro` | SIA | `Int64` | Quantidade produzida do procedimento no registro ambulatorial. Normalização canônica: converter para inteiro não-negativo (`Int64`), com limpeza textual (`trim`) e coerção numérica; valores inválidos, negativos e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 46 | `pa_qtdapr` | SIA | `Int64` | Quantidade aprovada do procedimento após críticas/validações do processamento SIA (valor aceito para faturamento/estatística). Normalização canônica: converter para inteiro não-negativo (`Int64`), com limpeza textual (`trim`) e coerção numérica; valores inválidos, negativos e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. Valor `0` é mantido para casos de glosa/rejeição. |
| 47 | `pa_pmdf` | SIA | `Int64` | Procedimento Máximo Diário por Faturamento (PMDF), limite quantitativo do procedimento por dia/competência de faturamento. Normalização canônica: inteiro positivo `1..9999`; `999/9999` mantidos como códigos de alto limite; `0`, negativos, inválidos e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 48 | `pa_valpro` | SIA | `Float64` | Valor total da produção informada (pré-glosa), derivado de quantidade produzida × valor unitário SIGTAP. Normalização monetária canônica: `trim`, suporte a vírgula/ponto decimal (inclusive padrão misto `1.234,56`), conversão numérica com `coerce` para `Float64`; valores negativos, inválidos e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 49 | `pa_vl_cf` | SIA | `Float64` | Valor de Complemento Federal aprovado para o procedimento no SIA. Normalização monetária canônica: `trim`, suporte a vírgula/ponto decimal (inclusive padrão misto `1.234,56`), conversão numérica com `coerce` para `Float64`; valores negativos, inválidos e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 50 | `pa_vl_cl` | SIA | `Float64` | Valor de Complemento Local aprovado para o procedimento (aporte do gestor estadual/municipal). Normalização monetária canônica: `trim`, suporte a vírgula/ponto decimal (inclusive padrão misto `1.234,56`), conversão numérica com `coerce` para `Float64`; valores negativos, inválidos e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 51 | `pa_vl_inc` | SIA | `Float64` | Valor de Incremento aprovado para o procedimento (parcela adicional de incentivo vinculada a regras/programas). Normalização monetária canônica: `trim`, suporte a vírgula/ponto decimal (inclusive padrão misto `1.234,56`), conversão numérica com `coerce` para `Float64`; valores negativos, inválidos e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 52 | `nu_pa_tot` | SIA | `Float64` | Valor total aprovado do registro ambulatorial (consolidação financeira), usualmente composto por `PA_VALAPR + PA_VL_INC + PA_VL_CF + PA_VL_CL` conforme regras do processamento SIA. Normalização monetária canônica: `trim`, suporte a vírgula/ponto decimal (inclusive padrão misto `1.234,56`), conversão numérica com `coerce` para `Float64`; valores negativos, inválidos e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 53 | `nu_vpa_tot` | SIA | `Float64` | Tipo canônico `Float64`: limpeza textual (`trim`, `nan/None/<NA>` -> `null`) e conversão numérica com `coerce`. |
| 54 | `pa_docorig` | SIA | `string` | Documento de origem da produção ambulatorial. Normalização canônica: `trim` + `uppercase`, mantendo domínio principal `C` (BPA-C), `I` (BPA-I), `A` (APAC) e `R` (RAAS); valores fora do domínio e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 55 | `pa_autoriz` | SIA | `string` | Número da autorização/APAC. Normalização canônica: remover caracteres não numéricos e manter 13 dígitos (`zfill` quando necessário). Regra condicional: quando `pa_docorig = 'A'`, exige valor válido (não `0000000000000`); para outras origens, pode permanecer nulo/zeros sentinela. Valores vazios e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 56 | `pa_catend` | SIA | `string` | Caráter de atendimento do procedimento. Normalização canônica: `trim`, remoção de caracteres não numéricos e código em 2 dígitos (`zfill`), mantendo domínio `01..06` (`01` eletivo, `02` urgência, etc.); rótulos textuais comuns (`ELETIVO`, `URGÊNCIA`) são mapeados. Valores fora do domínio e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 57 | `pa_motsai` | SIA | `string` | Motivo de saída/desfecho do atendimento. Normalização canônica: `trim`, remoção de caracteres não numéricos e código em 2 dígitos (`zfill`), mantendo domínio principal `11`, `12`, `14`, `15`, `16`, `21`, `31`, `41`, `51` (alta, permanência, transferência, óbito, abandono); rótulos textuais equivalentes são mapeados. Valores fora do domínio e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 58 | `pa_indica` | SIA | `string` | Indicador de processamento do registro no SIA. Normalização canônica: domínio `0` (aprovado), `1` (advertência), `2` (rejeitado/glosado), com `trim` e remoção de ruído textual; rótulos equivalentes são mapeados. Valores fora do domínio e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 59 | `pa_tpfin` | SIA | `string` | Tipo de financiamento do procedimento no SIA. Normalização canônica: `trim`, remoção de ruído e código em 2 dígitos (`zfill`), mantendo domínio principal `01`, `02`, `04`, `05`, `06`, `07` (inclui FAEC `05`); rótulos textuais equivalentes são mapeados. Valores fora do domínio e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 60 | `pa_subfin` | SIA | `string` | Subtipo de financiamento do procedimento no SIA (detalhamento do `pa_tpfin`). Normalização canônica: `trim`, remoção de ruído e código de 4 dígitos (`zfill`), com `0000` permitido (sem detalhamento). Quando `pa_tpfin` está preenchido, valida compatibilidade por prefixo (2 primeiros dígitos de `pa_subfin` devem casar com `pa_tpfin`), exceto `0000`. Valores fora da regra e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 61 | `pa_gestao` | SIA | `string` | Esfera administrativa de gestão do estabelecimento/processamento. Normalização canônica: `trim` + `uppercase`, mantendo domínio `M` (municipal), `E` (estadual), `D` (dupla); rótulos textuais equivalentes são mapeados. Valores fora do domínio e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 62 | `n_aih` | SIH | `string` | Número da AIH (identificador único da internação). Normalização canônica: remover caracteres não numéricos, manter 13 dígitos como texto (`zfill`) para preservar zeros à esquerda e validar estrutura básica (UF + ano + tipo + sequencial+DV). Valores inválidos e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 63 | `cgc_hosp` | SIH | `string` | Identificador jurídico do estabelecimento/mantenedora (CNPJ/CGC histórico). Normalização canônica: remover pontuação/caracteres não numéricos, manter 14 dígitos sem máscara (`zfill` quando necessário). Valores vazios, inválidos técnicos e `00000000000000` -> `null`. |
| 64 | `cnpj_mant` | SIH | `string` | CNPJ da entidade mantenedora do estabelecimento (responsável contratual/financeira; em layouts antigos pode vir como `cgc_mant`). Normalização canônica: remover pontuação/caracteres não numéricos, manter 14 dígitos sem máscara (`zfill` quando necessário). Valores vazios, inválidos técnicos e `00000000000000` -> `null`. |
| 65 | `munic_mov` | SIH | `string` | Município de movimentação da internação (onde o hospital executante está localizado/onde a produção foi processada). Normalização canônica: remover espaços e não dígitos, manter código IBGE de 6 dígitos (sem DV) truncando quando vier com 7+ dígitos. Valores inválidos técnicos (`000000`, `999999`, vazio, `nan`, `None`, `<NA>`) -> `null`. |
| 66 | `cod_idade` | SIH | `string` | Unidade de medida da idade no SIH (`idade`): domínio canônico `2` (dias), `3` (meses), `4` (anos), com aceite legado de `1` (horas) quando presente em bases históricas. Normalização canônica: `trim`, manter 1 dígito válido e descartar demais valores; nulos técnicos (`nan`, `None`, `<NA>`) e fora do domínio -> `null`. |
| 67 | `nasc` | SIH | `string` | Data de nascimento do paciente no SIH (`NASC`, podendo vir como `DT_NASC` em alguns layouts). Normalização canônica: remover ruído e manter `AAAAMMDD` quando data válida (faixa de ano 1900..ano corrente); em bases mascaradas por privacidade, aceitar `AAAA` (somente ano). Valores inválidos, vazios e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 68 | `cep` | SIH | `string` | CEP de residência do paciente no SIH. Normalização canônica: remover caracteres não numéricos e manter CEP completo com 8 dígitos (sem traço); em bases públicas/mascaradas, aceita CEP parcial com 5 dígitos. Valores inválidos, `00000000`/`00000`, vazios e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 69 | `nacional` | SIH | `string` | Nacionalidade do paciente no SIH. Normalização canônica para código de 3 dígitos: `010` (brasileiro), `020` (naturalizado), `030` (estrangeiro); suporta forma curta (`01`, `02`, `03`) mapeando para o padrão de 3 dígitos. `999` (ignorado), valores fora do domínio, vazios e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 70 | `diag_secun` | SIH | `string` | Diagnóstico secundário da internação (CID-10). Normalização canônica de CID: `trim`, `uppercase`, remoção de ponto (`.`) e caracteres não alfanuméricos; mantém formato válido `^[A-Z][0-9]{2}[0-9A-Z]?$` (3-4 caracteres, ex.: `E10`, `I109`). Vazios, inválidos e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 71 | `diagsec1` | SIH | `string` | Primeiro diagnóstico secundário adicional da AIH (também pode vir como `diag_sec1` em layouts/extrações). Normalização canônica de CID: `trim`, `uppercase`, remoção de ponto (`.`) e caracteres não alfanuméricos; mantém formato válido `^[A-Z][0-9]{2}[0-9A-Z]?$` (3-4 caracteres, ex.: `A00`, `E119`). Vazios, inválidos e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 72 | `diagsec2` | SIH | `string` | Segundo diagnóstico secundário adicional da AIH (também pode vir como `diag_sec2` em layouts/extrações). Normalização canônica de CID: `trim`, `uppercase`, remoção de ponto (`.`) e caracteres não alfanuméricos; mantém formato válido `^[A-Z][0-9]{2}[0-9A-Z]?$` (3-4 caracteres). Vazios, inválidos e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 73 | `diagsec3` | SIH | `string` | Terceiro diagnóstico secundário adicional da AIH (também pode vir como `diag_sec3` em layouts/extrações). Normalização canônica de CID: `trim`, `uppercase`, remoção de ponto (`.`) e caracteres não alfanuméricos; mantém formato válido `^[A-Z][0-9]{2}[0-9A-Z]?$` (3-4 caracteres). Vazios, inválidos e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 74 | `diagsec4` | SIH | `string` | Quarto diagnóstico secundário adicional da AIH (também pode vir como `diag_sec4` em layouts/extrações). Normalização canônica de CID: `trim`, `uppercase`, remoção de ponto (`.`) e caracteres não alfanuméricos; mantém formato válido `^[A-Z][0-9]{2}[0-9A-Z]?$` (3-4 caracteres). Vazios, inválidos e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 75 | `diagsec5` | SIH | `string` | Quinto diagnóstico secundário adicional da AIH (também pode vir como `diag_sec5` em layouts/extrações). Normalização canônica de CID: `trim`, `uppercase`, remoção de ponto (`.`) e caracteres não alfanuméricos; mantém formato válido `^[A-Z][0-9]{2}[0-9A-Z]?$` (3-4 caracteres). Vazios, inválidos e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 76 | `diagsec6` | SIH | `string` | Sexto diagnóstico secundário adicional da AIH (também pode vir como `diag_sec6` em layouts/extrações). Normalização canônica de CID: `trim`, `uppercase`, remoção de ponto (`.`) e caracteres não alfanuméricos; mantém formato válido `^[A-Z][0-9]{2}[0-9A-Z]?$` (3-4 caracteres). Vazios, inválidos e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 77 | `diagsec7` | SIH | `string` | Sétimo diagnóstico secundário adicional da AIH (também pode vir como `diag_sec7` em layouts/extrações). Normalização canônica de CID: `trim`, `uppercase`, remoção de ponto (`.`) e caracteres não alfanuméricos; mantém formato válido `^[A-Z][0-9]{2}[0-9A-Z]?$` (3-4 caracteres). Vazios, inválidos e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 78 | `diagsec8` | SIH | `string` | Oitavo diagnóstico secundário adicional da AIH (também pode vir como `diag_sec8` em layouts/extrações). Normalização canônica de CID: `trim`, `uppercase`, remoção de ponto (`.`) e caracteres não alfanuméricos; mantém formato válido `^[A-Z][0-9]{2}[0-9A-Z]?$` (3-4 caracteres). Vazios, inválidos e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 79 | `diagsec9` | SIH | `string` | Nono diagnóstico secundário adicional da AIH (também pode vir como `diag_sec9` em layouts/extrações). Normalização canônica de CID: `trim`, `uppercase`, remoção de ponto (`.`) e caracteres não alfanuméricos; mantém formato válido `^[A-Z][0-9]{2}[0-9A-Z]?$` (3-4 caracteres). Vazios, inválidos e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 80 | `cid_morte` | SIH | `string` | Causa básica do óbito na internação (também pode vir como `cid_obito` em alguns dicionários/layouts). Normalização canônica de CID: `trim`, `uppercase`, remoção de ponto (`.`) e caracteres não alfanuméricos; mantém formato válido `^[A-Z][0-9]{2}[0-9A-Z]?$` (3-4 caracteres, ex.: `A00`, `I219`). Vazios, inválidos e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. Regra de consistência de negócio: quando o indicador/motivo de saída representar óbito, `cid_morte` deve estar preenchido. |
| 81 | `cid_notif` | SIH | `string` | Diagnóstico de notificação compulsória identificado na internação (também pode vir como `cid_not` em alguns dicionários/layouts). Normalização canônica de CID: `trim`, `uppercase`, remoção de ponto (`.`) e caracteres não alfanuméricos; mantém formato válido `^[A-Z][0-9]{2}[0-9A-Z]?$` (3-4 caracteres, ex.: `A30`, `A90X`). Vazios, inválidos e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 82 | `cid_asso` | SIH | `string` | Causa associada/externa da internação (também pode vir como `cid_causa_asso` em alguns dicionários/layouts). Normalização canônica: `trim`, `uppercase`, remoção de ponto (`.`) e caracteres não alfanuméricos, mantendo CID-10 válido do Capítulo XX (`V01` a `Y98`) no formato `^[VWXY][0-9]{2}[0-9A-Z]?$` (3-4 caracteres, ex.: `V299`, `W19`). Vazios, inválidos e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 83 | `cid_princ` | SIH | `string` | Diagnóstico principal da internação no SIH (também pode vir como `diag_princ` em arquivos RD). Normalização canônica de CID: `trim`, `uppercase`, remoção de ponto (`.`) e caracteres não alfanuméricos; mantém formato válido `^[A-Z][0-9]{2}[0-9A-Z]?$` (3-4 caracteres, ex.: `A00`, `J189`). Vazios, inválidos e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 84 | `proc_solic` | SIH | `string` | Procedimento solicitado na AIH (intenção clínica inicial), para comparação com o procedimento realizado. Normalização canônica: remover caracteres não numéricos e manter código SIGTAP de 10 dígitos (`zfill` quando necessário). Valores inválidos técnicos (`0000000000`), vazios e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 85 | `uti_mes_to` | SIH | `Int64` | Total de diárias de UTI na competência/mês da AIH (uso mensal em tabulação, distinto do total acumulado da internação). Normalização canônica: inteiro não-negativo (`Int64`) no intervalo `0..999`, com limpeza textual (`trim`) e coerção numérica; inválidos e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 86 | `uti_int_to` | SIH | `Int64` | Total de diárias de UTI no registro (uso predominante no SIH, podendo aparecer em integrações específicas do SIA). Normalização canônica: inteiro não-negativo (`Int64`) no intervalo `0..999`, com limpeza textual (`trim`) e coerção numérica; inválidos e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 87 | `qt_diarias` | SIH | `Int64` | Quantidade de diárias da internação (base para hotelaria/TMP no SIH). Normalização canônica: inteiro não-negativo (`Int64`) no intervalo `0..999`, com limpeza textual (`trim`) e coerção numérica; valores inválidos e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 88 | `dias_perm` | SIH | `Int64` | Dias de permanência real da internação (diferença cronológica entre `dt_saida` e `dt_inter`, distinto de `qt_diarias` de faturamento). Normalização canônica: inteiro não-negativo (`Int64`) no intervalo `0..999`, com limpeza textual (`trim`) e coerção numérica; fallback de derivação `dt_saida - dt_inter` quando o campo vier nulo/inválido; valores inválidos e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 89 | `val_sh` | SIH | `Float64` | Valor do Serviço Hospitalar aprovado na AIH (parcela institucional/hospitalar). Normalização monetária canônica: `trim`, suporte a vírgula/ponto decimal (inclusive padrão misto `1.234,56`), conversão numérica com `coerce` para `Float64`; valores negativos, inválidos e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 90 | `val_sp` | SIH | `Float64` | Valor do Serviço Profissional aprovado na AIH (honorários dos profissionais). Normalização monetária canônica: `trim`, suporte a vírgula/ponto decimal (inclusive padrão misto `1.234,56`), conversão numérica com `coerce` para `Float64`; valores negativos, inválidos e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 91 | `val_sadt` | SIH | `Float64` | Valor de Serviços Auxiliares de Diagnose e Terapia (SADT) aprovado na AIH. Normalização monetária canônica: `trim`, suporte a vírgula/ponto decimal (inclusive padrão misto `1.234,56`), conversão numérica com `coerce` para `Float64`; valores negativos, inválidos e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 92 | `val_ortp` | SIH | `Float64` | Valor de Órteses, Próteses e Materiais Especiais (OPME) aprovado na AIH (também chamado `val_opm` em alguns contextos). Normalização monetária canônica: `trim`, suporte a vírgula/ponto decimal (inclusive padrão misto `1.234,56`), conversão numérica com `coerce` para `Float64`; valores negativos, inválidos e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 93 | `val_uti` | SIH | `Float64` | Valor total aprovado de diárias de UTI na AIH (assistência intensiva). Normalização monetária canônica: `trim`, suporte a vírgula/ponto decimal (inclusive padrão misto `1.234,56`), conversão numérica com `coerce` para `Float64`; valores negativos, inválidos e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 94 | `val_uci` | SIH | `Float64` | Valor total aprovado de diárias em Unidade de Cuidados Intermediários (UCI) na AIH. Normalização monetária canônica: `trim`, suporte a vírgula/ponto decimal (inclusive padrão misto `1.234,56`), conversão numérica com `coerce` para `Float64`; valores negativos, inválidos e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 95 | `val_sangue` | SIH | `Float64` | Valor aprovado de hemocomponentes e hemoderivados na AIH (hemoterapia). Normalização monetária canônica: `trim`, suporte a vírgula/ponto decimal (inclusive padrão misto `1.234,56`), conversão numérica com `coerce` para `Float64`; valores negativos, inválidos e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 96 | `val_acomp` | SIH | `Float64` | Valor aprovado de diárias de acompanhante na AIH (também referido como `val_diar_acomp` em alguns contextos). Normalização monetária canônica: `trim`, suporte a vírgula/ponto decimal (inclusive padrão misto `1.234,56`), conversão numérica com `coerce` para `Float64`; valores negativos, inválidos e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 97 | `dt_inter` | SIH | `string` | Data de internação no SIH. Normalização canônica: limpeza textual e validação de data, com saída em string `AAAAMMDD` (aceita entradas legadas parseáveis e converte para o padrão). Valores inválidos, vazios e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 98 | `dt_saida` | SIH | `string` | Data de saída/encerramento da internação no SIH (alta, óbito ou transferência). Normalização canônica: limpeza textual e validação de data, com saída em string `AAAAMMDD` (aceita entradas legadas parseáveis e converte para o padrão). Valores inválidos, vazios e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 99 | `morte` | SIH | `Int64` | Indicador binário de óbito na internação. Normalização canônica: `1` quando houve óbito e `0` quando não houve; aceita entradas textuais equivalentes (`SIM/NÃO`, `ÓBITO`) e aplica fallback pelo motivo de saída/cobrança (`41`, `42`, `43` -> `1`). Valores inválidos e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 100 | `cobranca` | SIH | `string` | Tipo de cobrança/status da AIH no ciclo de faturamento. Normalização canônica: `trim`, remoção de ruído e código em 2 dígitos (`zfill`), mantendo domínio operacional SIH (ex.: `11` alta, `21` permanência, `31` transferência, `41/42/43` óbito, `51` encerramento por mudança de procedimento). Aceita rótulos textuais equivalentes (alta, permanência, transferência, óbito, encerramento). Valores fora do domínio e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 101 | `gestao` | SIH | `string` | Esfera administrativa responsável pela AIH no SIH. Normalização canônica: `trim` + `uppercase`, mantendo domínio `M` (gestão municipal), `E` (gestão estadual), `D` (gestão dupla); rótulos textuais equivalentes são mapeados. Valores fora do domínio e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 102 | `financ` | SIH | `string` | Bloco de financiamento da AIH (`FINANC`/`TP_FINANC`). Normalização canônica: `trim`, remoção de ruído e código em 2 dígitos (`zfill`), mantendo domínio principal `01` (Atenção Básica), `04` (MAC), `05` (FAEC), `07` (Incentivos); rótulos textuais equivalentes (`MAC`, `MÉDIA E ALTA COMPLEXIDADE`, `FAEC`, `INCENTIVOS`) são mapeados. Valores fora do domínio e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 103 | `faec_tp` | SIH | `string` | Subtipo do financiamento FAEC na AIH (`FAEC_TP`/`FAEC_TP_FIN`). Normalização canônica: `trim`, remoção de ruído e extração numérica; aceita código em layout de 6 dígitos (preferencial, ex.: `050001`, `050002`, `050003`, `050005`) e 4 dígitos (layout reduzido). Rótulos textuais equivalentes (`TRANSPLANTES`, `NEFROLOGIA`, `CIRURGIAS ELETIVAS`, `TRIAGEM NEONATAL`) são mapeados. Valores sentinela (`0000`, `000000`), fora do padrão FAEC (`05...`) e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. Regra de consistência: manter valor apenas quando `financ = 05` (FAEC); para outros blocos, `null`. |
| 104 | `aud_just` | SIH | `string` | Justificativa da auditoria da AIH (`AUD_JUST`/`JUST_AUD`) para liberação, exceção ou glosa no SIH. Normalização canônica: `trim` e preservação textual; aceita códigos curtos (1-2 caracteres) e texto de observação (layout expandido), com limite defensivo de até 40 caracteres. Valores sentinela (`0`, `00`, `000`, `NA/N/A`, `NÃO SE APLICA`, `SEM JUSTIFICATIVA`) e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 105 | `sis_just` | SIH | `string` | Justificativa automática do sistema para críticas/liberação no SIH (`SIS_JUST`/`JUST_SIS`). Normalização canônica: `trim`; quando numérico curto, padroniza para código de 3 dígitos (`zfill`, ex.: `1 -> 001`, `15 -> 015`, `999` mantido); quando não numérico, preserva texto (layout expandido) com limite defensivo de até 40 caracteres. Sentinelas (`0`, `00`, `000`, `NA/N/A`, `NÃO SE APLICA`, `SEM CRÍTICA`, `SEM JUSTIFICATIVA`) e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. |
| 106 | `sequencia` | SIH | `string` | Sequência técnica da AIH (`SEQUENCIA`/`SEQ_AIH`) para ordenar múltiplos lançamentos vinculados ao mesmo `n_aih`. Normalização canônica: `trim`, remoção de ruído e extração numérica com saída em 3 dígitos (`zfill`), domínio `001..999`; valor `000`, inválidos e nulos técnicos (`nan`, `None`, `<NA>`) -> `null`. Regra analítica: para contagem de internações únicas, preferir a linha mestre (`sequencia = 001`). |
