# Benchmark Evaluation Report — SUS Data RAG

**Model:** openai  
**Run date:** 2026-08-22 23:56  
**API:** http://127.0.0.1:8000  

## Summary

| Metric | Value |
|---|---|
| Total queries | 118 |
| Correct (EA numerator) | 31 |
| **Execution Accuracy (EA)** | **26.3%** |
| Wilson IC 95% | [19.2%, 34.9%] |
| Correct via flexible match (alias diferente) | 23 |
| API errors / timeouts | 0 |
| Mean Time-to-Answer | 3.6s |

> **Nota metodológica — Scoring:** A Execution Accuracy avalia se o SQL gerado
> retorna os mesmos **valores** que o gold-standard, independente do nome das colunas.
> Quando o LLM usa um alias diferente mas retorna os dados corretos (match posicional),
> a query é contada como correta e sinalizada com `~`. Esta decisão segue a definição
> de EA em [Lee et al., 2022] — comparação de conjuntos de resultado, não de SQL texto.

## EA by Category

| Category | Correct | Total | EA |
|---|---:|---:|---:|
| Epidemiológica Simples | 17 | 33 | 51.5% |
| Epidemiológica Complexa | 2 | 31 | 6.5% |
| Financeira | 5 | 26 | 19.2% |
| Temporal/Comparativa | 7 | 28 | 25.0% |

## Query Results

### ✅ Q01 — Epidemiológica Simples

**Q:** Total de internações ortopédicas (M00-M99 e S00-S99) em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
```

**Resultado:** `got=28056 exp=28056`
**TTA:** 3656ms

---

### ❌ Q02 — Epidemiológica Simples

**Q:** Total de procedimentos ambulatoriais ortopédicos em RJ em 2022

**SQL gerado:**
```sql
SELECT SUM(pa_qtdapr) AS total_procedimentos_ambulatoriais_ortopedicos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=3387.0 exp=27201`
**TTA:** 1971ms

---

### ✅ Q03 — Epidemiológica Simples

**Q:** Total de internações por fratura de fêmur (S72) em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND cid_principal LIKE 'S72%'
```

**Resultado:** `got=9945 exp=9945`
**TTA:** 1830ms

---

### ✅ Q04 — Epidemiológica Simples

**Q:** Total de internações por osteoartrose (M16, M17) em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M16%' OR cid_principal LIKE 'M17%')
```

**Resultado:** `got=102 exp=102`
**TTA:** 2352ms

---

### ❌ Q05 — Epidemiológica Simples

**Q:** Número de óbitos em internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(*) AS numero_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND morte = 1
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=21 exp=965`
**TTA:** 1812ms

---

### ✅ Q06 — Epidemiológica Simples

**Q:** Total de internações por traumatismos (S00-T98) em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND icd_group = 'S00-T98'
```

**Resultado:** `got=25731 exp=25731`
**TTA:** 1946ms

---

### ✅ Q07 — Epidemiológica Simples

**Q:** Total de internações por doenças osteomusculares (M00-M99) em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS total_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND icd_group = 'M00-M99'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
```

**Resultado:** `got=2325 exp=2325`
**TTA:** 1711ms

---

### ❌ Q08 — Epidemiológica Simples

**Q:** Número de mulheres internadas por causa ortopédica em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND sexo_paciente = 'F'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=880 exp=10444`
**TTA:** 2145ms

---

### ❌ Q09 — Epidemiológica Simples

**Q:** Número de homens internados por causa ortopédica em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS num_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND sexo_paciente = 'M'
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=1445 exp=17612`
**TTA:** 1945ms

---

### ❌ Q10 — Epidemiológica Simples

**Q:** Número de internações ortopédicas de idosos (60 anos ou mais) em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND idade_paciente >= 60
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=586 exp=10149`
**TTA:** 2294ms

---

### ❌ Q11 — Epidemiológica Simples

**Q:** Número de municípios distintos com internação ortopédica registrada em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT cod_munic_estabelecimento)
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=46 exp=63`
**TTA:** 1988ms

---

### ❌ Q12 — Epidemiológica Simples

**Q:** Número de estabelecimentos distintos com internação ortopédica em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT cnes_estabelecimento) 
FROM processed 
WHERE sistema = 'SIH' 
  AND uf_origem = 'RJ' 
  AND ano_cmpt = 2022 
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=100 exp=135`
**TTA:** 2103ms

---

### ❌ Q13 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com permanência superior a 7 dias em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) 
FROM processed 
WHERE sistema = 'SIH' 
  AND uf_origem = 'RJ' 
  AND ano_cmpt = 2022 
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99') 
  AND dias_perm > 7
```

**Resultado:** `got=734 exp=9244`
**TTA:** 1893ms

---

### ✅ Q14 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com uso de UTI em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS num_internacoes_ortopedicas_uti
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
  AND uti_int_to > 0
```

**Resultado:** `got=0 exp=0`
**TTA:** 2051ms

---

### ✅ Q15 — Epidemiológica Simples

**Q:** Número de internações por fratura de quadril (S72) em pacientes com 70 anos ou mais em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS num_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND idade_paciente >= 70
  AND cid_principal LIKE 'S72%'
```

**Resultado:** `got=5140 exp=5140`
**TTA:** 2127ms

---

### ❌ Q16 — Epidemiológica Complexa

**Q:** Top 10 CIDs ortopédicos por volume de internação em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    cid_principal, 
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cid_principal
ORDER BY 
    volume_internacoes DESC
LIMIT 10
```

**Resultado:** `Linha 1: got={'cid_principal': 'M861', 'volume_internacoes': 182} exp={'cid_principal': 'S720', 'total_internacoes': 3502}`
**TTA:** 2169ms

---

### ❌ Q17 — Epidemiológica Complexa

**Q:** Top 5 municípios de RJ com maior número de internações ortopédicas em 2022

**SQL gerado:**
```sql
SELECT 
    p.cod_munic_estabelecimento, 
    COUNT(DISTINCT p.n_aih) AS num_internacoes
FROM 
    processed p
JOIN 
    enriched e ON p.row_id = e.row_id
WHERE 
    p.sistema = 'SIH'
    AND p.uf_origem = 'RJ'
    AND p.ano_cmpt = 2022
    AND (p.cid_principal LIKE 'M%' OR p.icd_group = 'M00-M99')
GROUP BY 
    p.cod_munic_estabelecimento
ORDER BY 
    num_internacoes DESC
LIMIT 5
```

**Resultado:** `Linha 1: got={'cod_munic_estabelecimento': '330000', 'num_internacoes': 1007} exp={'cod_munic_estabelecimento': '330000', 'internacoes': 9878}`
**TTA:** 2934ms

---

### ❌ Q18 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por faixa etária (0-17, 18-59, 60+) em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 59 THEN '18-59'
        ELSE '60+'
    END AS faixa_etaria,
    COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    faixa_etaria
ORDER BY 
    faixa_etaria
```

**Resultado:** `Linha não encontrada: {'faixa_etaria': '0-17', 'total': 2380}`
**TTA:** 2351ms

---

### ❌ Q19 — Epidemiológica Complexa

**Q:** Taxa de mortalidade hospitalar por CID ortopédico (top 5) em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    p.cid_principal, 
    COUNT(DISTINCT p.n_aih) AS total_internacoes, 
    SUM(p.morte) AS total_obitos, 
    (SUM(p.morte) * 100.0 / COUNT(DISTINCT p.n_aih)) AS taxa_mortalidade
FROM 
    processed p
JOIN 
    enriched e ON p.row_id = e.row_id
WHERE 
    p.sistema = 'SIH' 
    AND p.uf_origem = 'RJ' 
    AND p.ano_cmpt = 2022 
    AND (p.cid_principal LIKE 'M%' OR p.icd_group = 'M00-M99')
GROUP BY 
    p.cid_principal
ORDER BY 
    taxa_mortalidade DESC
LIMIT 5
```

**Resultado:** `Linha 1: got={'cid_principal': 'M858', 'total_internacoes': 1, 'total_obitos': 1.0, 'taxa_mortalidade': 100.0} exp={'cid_principal': 'T203', 'total_internacoes': 44, 'total_obitos': 26.0, 'taxa_mortalidade_pct': 59.09}`
**TTA:** 3218ms

---

### ❌ Q20 — Epidemiológica Complexa

**Q:** Top 10 CIDs ambulatoriais ortopédicos por volume de produção em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    cid_principal, 
    COUNT(*) AS volume_producao
FROM 
    processed
WHERE 
    sistema = 'SIA'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cid_principal
ORDER BY 
    volume_producao DESC
LIMIT 10
```

**Resultado:** `Linha 1: got={'cid_principal': 'M216', 'volume_producao': 1963} exp={'cid_principal': 'S720', 'total_procedimentos': 5570}`
**TTA:** 2046ms

---

### ❌ Q21 — Epidemiológica Complexa

**Q:** Proporção de internações ortopédicas por sexo em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    sexo_paciente, 
    COUNT(DISTINCT n_aih) AS total_internacoes,
    COUNT(DISTINCT n_aih) * 1.0 / SUM(COUNT(DISTINCT n_aih)) OVER () AS proporcao
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    sexo_paciente
```

**Resultado:** `Linha não encontrada: {'sexo_paciente': 'M', 'total_internacoes': 17612, 'pct': 62.8}`
**TTA:** 2272ms

---

### ❌ Q22 — Epidemiológica Complexa

**Q:** Número de internações por fratura de fêmur (S72) em idosos (60+) comparado ao total em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    COUNT(DISTINCT CASE WHEN idade_paciente >= 60 AND cid_principal LIKE 'S72%' THEN n_aih END) AS fraturas_femur_idosos,
    COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND cid_principal LIKE 'S72%'
```

**Resultado:** `Linha 1: got={'fraturas_femur_idosos': 6628, 'total_internacoes': 9945} exp={'total_s72': 9945, 's72_idosos': 6628.0, 'pct_idosos': 66.6}`
**TTA:** 2346ms

---

### ❌ Q23 — Epidemiológica Complexa

**Q:** Top 5 CIDs ortopédicos com maior permanência média hospitalar em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    cid_principal, 
    AVG(dias_perm) AS permanencia_media
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cid_principal
ORDER BY 
    permanencia_media DESC
LIMIT 5
```

**Resultado:** `Linha 1: got={'cid_principal': 'M463', 'permanencia_media': 57.5} exp={'cid_principal': 'T203', 'total_internacoes': 44, 'permanencia_media_dias': 30.1}`
**TTA:** 2116ms

---

### ❌ Q24 — Epidemiológica Complexa

**Q:** Top 5 estabelecimentos com maior volume de internação ortopédica em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    p.cnes_estabelecimento, 
    COUNT(DISTINCT p.n_aih) AS volume_internacoes
FROM 
    processed p
JOIN 
    enriched e ON p.row_id = e.row_id
WHERE 
    p.sistema = 'SIH'
    AND p.uf_origem = 'RJ'
    AND p.ano_cmpt = 2022
    AND (p.cid_principal LIKE 'M%' OR p.icd_group = 'M00-M99')
GROUP BY 
    p.cnes_estabelecimento
ORDER BY 
    volume_internacoes DESC
LIMIT 5
```

**Resultado:** `Linha 1: got={'cnes_estabelecimento': '2273276', 'volume_internacoes': 326} exp={'cnes_estabelecimento': '2290227', 'internacoes': 5043}`
**TTA:** 3171ms

---

### ❌ Q25 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por raça/cor do paciente em RJ em 2022

**SQL gerado:**
```sql
SELECT 
  raca_cor_paciente, 
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
  raca_cor_paciente
ORDER BY 
  total_internacoes DESC
```

**Resultado:** `Número de linhas: got=5 exp=6`
**TTA:** 2181ms

---

### ❌ Q26 — Epidemiológica Complexa

**Q:** CIDs ortopédicos com maior número de dias totais de internação em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    cid_principal, 
    SUM(dias_perm) AS total_dias_internacao
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cid_principal
ORDER BY 
    total_dias_internacao DESC
```

**Resultado:** `Número de linhas: got=202 exp=10`
**TTA:** 1970ms

---

### ❌ Q27 — Epidemiológica Complexa

**Q:** Top 5 procedimentos ambulatoriais ortopédicos mais realizados em RJ em 2022

**SQL gerado:**
```sql
SELECT cod_procedimento, SUM(pa_qtdapr) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY cod_procedimento
ORDER BY total_procedimentos DESC
LIMIT 5
```

**Resultado:** `Linha 1: got={'cod_procedimento': '0701010053', 'total_procedimentos': 973.0} exp={'cod_procedimento': '0302050019', 'total_procedimentos': 15646}`
**TTA:** 1852ms

---

### ❌ Q28 — Epidemiológica Complexa

**Q:** Proporção de internações ortopédicas com paciente de outro município em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    COUNT(DISTINCT p.n_aih) AS total_internacoes,
    SUM(CASE WHEN e.clinical_deslocamento_territorial = 'deslocamento intermunicipal' THEN 1 ELSE 0 END) AS deslocamento_intermunicipal,
    SUM(CASE WHEN e.clinical_deslocamento_territorial = 'deslocamento intermunicipal' THEN 1 ELSE 0 END) * 1.0 / COUNT(DISTINCT p.n_aih) AS proporcao_deslocamento_intermunicipal
FROM 
    processed p
JOIN 
    enriched e ON p.row_id = e.row_id
WHERE 
    p.sistema = 'SIH'
    AND p.uf_origem = 'RJ'
    AND p.ano_cmpt = 2022
    AND (p.cid_principal LIKE 'M%' OR p.icd_group = 'M00-M99')
    AND e.clinical_interpretacao_clinica = 'doencas osteomusculares e do tecido conjuntivo'
```

**Resultado:** `Linha 1: got={'total_internacoes': 2325, 'deslocamento_intermunicipal': 1439.0, 'proporcao_deslocamento_intermunicipal': 0.6189247312} exp={'total_internacoes': 28056, 'deslocamento_intermunicipal': 14192.0, 'pct_deslocamento': 50.6}`
**TTA:** 3408ms

---

### ❌ Q29 — Epidemiológica Complexa

**Q:** Comparativo de volume mensal SIA vs SIH ortopédico em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    ano_cmpt,
    mes_cmpt,
    sistema,
    COUNT(*) AS volume
FROM 
    processed
WHERE 
    uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    ano_cmpt,
    mes_cmpt,
    sistema
ORDER BY 
    ano_cmpt,
    mes_cmpt,
    sistema;
```

**Resultado:** `Linha 1: got={'ano_cmpt': 2022, 'mes_cmpt': 1, 'sistema': 'SIA', 'volume': 208} exp={'mes_cmpt': 1, 'sistema': 'SIA', 'total': 1647}`
**TTA:** 2047ms

---

### ❌ Q30 — Epidemiológica Complexa

**Q:** Idade média dos pacientes internados por fratura de fêmur (S72) em RJ em 2022

**SQL gerado:**
```sql
SELECT AVG(idade_paciente) AS idade_media
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND cid_principal LIKE 'S72%'
```

**Resultado:** `Linha 1: got={'idade_media': 63.3186525892} exp={'idade_media': 63.3, 'idade_minima': 0.0, 'idade_maxima': 99.0}`
**TTA:** 1789ms

---

### ❌ Q31 — Financeira

**Q:** Custo total de todas as internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_ortopedicas
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=8405202.74 exp=70893354.22`
**TTA:** 2627ms

---

### ❌ Q32 — Financeira

**Q:** Custo médio por internação ortopédica em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    COALESCE(SUM(custo_total) / COUNT(DISTINCT n_aih), 0) AS custo_medio_por_internacao
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=3615.1409634409 exp=2525.77`
**TTA:** 4299ms

---

### ✅ Q33 — Financeira

**Q:** Custo total de internações por fratura de fêmur (S72) em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_fratura_femur
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND cid_principal LIKE 'S72%'
```

**Resultado:** `got=26337280.380000006 exp=26337280.38`
**TTA:** 3906ms

---

### ❌ Q34 — Financeira

**Q:** Top 5 CIDs ortopédicos com maior custo total de internação em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    cid_principal, 
    SUM(custo_total) AS total_custo
FROM 
    processed
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'RJ' 
    AND ano_cmpt = 2022 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    cid_principal
ORDER BY 
    total_custo DESC
LIMIT 5
```

**Resultado:** `Linha 1: got={'cid_principal': 'M511', 'total_custo': 1137816.14} exp={'cid_principal': 'S720', 'internacoes': 3502, 'custo_total': 10821184.25}`
**TTA:** 4292ms

---

### ❌ Q35 — Financeira

**Q:** Custo total de internações ortopédicas com uso de OPME (val_ortp > 0) em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_ortopedicas_opme
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
    AND val_ortp > 0
```

**Resultado:** `Linha 1: got={'custo_total_ortopedicas_opme': None} exp={'internacoes_com_opme': 0, 'custo_total_opme': None}`
**TTA:** 4065ms

---

### ❌ Q36 — Financeira

**Q:** Valor total de honorários profissionais (val_sp) em internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    COALESCE(SUM(val_sp), 0) AS total_val_sp
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=1467550.33 exp=13402482.02`
**TTA:** 3894ms

---

### ❌ Q37 — Financeira

**Q:** Custo médio por dia de internação ortopédica em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    SUM(custo_total) / SUM(dias_perm) AS custo_medio_por_dia
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
    AND dias_perm > 0
```

**Resultado:** `got=409.1374498356 exp=321.98`
**TTA:** 4352ms

---

### ❌ Q38 — Financeira

**Q:** Top 5 estabelecimentos com maior custo total de internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    p.cnes_estabelecimento, 
    SUM(p.custo_total) AS total_custo
FROM 
    processed p
JOIN 
    enriched e ON p.row_id = e.row_id
WHERE 
    p.sistema = 'SIH' 
    AND p.uf_origem = 'RJ' 
    AND p.ano_cmpt = 2022
    AND (p.cid_principal LIKE 'M%' OR p.icd_group = 'M00-M99')
GROUP BY 
    p.cnes_estabelecimento
ORDER BY 
    total_custo DESC
LIMIT 5
```

**Resultado:** `Linha 1: got={'cnes_estabelecimento': '7185081', 'total_custo': 1877775.91} exp={'cnes_estabelecimento': '2290227', 'internacoes': 5043, 'custo_total': 9418220.5}`
**TTA:** 4631ms

---

### ❌ Q39 — Financeira

**Q:** Custo total de procedimentos ambulatoriais ortopédicos em RJ em 2022

**SQL gerado:**
```sql
SELECT COALESCE(SUM(custo_total), 0) AS custo_total_ambulatorial_ortopedico
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=867233.2599999988 exp=4149578.04`
**TTA:** 3365ms

---

### ❌ Q40 — Financeira

**Q:** Proporção custo serviço hospitalar vs honorários em internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    SUM(COALESCE(val_sh, 0)) / NULLIF(SUM(COALESCE(val_sp, 0)), 0) AS proporcao_custo_sh_vs_honorarios
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `Linha 1: got={'proporcao_custo_sh_vs_honorarios': 4.727369323} exp={'custo_servico_hospitalar': 57490872.2, 'honorarios_profissionais': 13402482.02, 'pct_hospitalar': 81.1}`
**TTA:** 4287ms

---

### ❌ Q41 — Temporal/Comparativa

**Q:** Distribuição mensal de internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt, 
    COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'total_internacoes': 121} exp={'mes_cmpt': 1, 'total_internacoes': 1998}`
**TTA:** 4069ms

---

### ❌ Q42 — Temporal/Comparativa

**Q:** Distribuição mensal de procedimentos ambulatoriais ortopédicos em RJ em 2022

**SQL gerado:**
```sql
SELECT 
  mes_cmpt, 
  COUNT(*) AS total_procedimentos
FROM 
  processed
WHERE 
  sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
  mes_cmpt
ORDER BY 
  mes_cmpt
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'total_procedimentos': 208} exp={'mes_cmpt': 1, 'procedimentos': 1647}`
**TTA:** 4008ms

---

### ✅ Q43 — Temporal/Comparativa

**Q:** Mês com maior número de internações por fratura de fêmur (S72) em RJ em 2022

**SQL gerado:**
```sql
SELECT mes_cmpt, COUNT(DISTINCT n_aih) AS num_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND cid_principal LIKE 'S72%'
GROUP BY mes_cmpt
ORDER BY num_internacoes DESC
LIMIT 1
```

**Resultado:** `got=10 exp=10`
**TTA:** 3977ms

---

### ❌ Q44 — Temporal/Comparativa

**Q:** Evolução mensal do custo total de internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt, 
    SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'custo_total_internacoes': 515324.59} exp={'mes_cmpt': 1, 'custo_total': 4528720.12}`
**TTA:** 4183ms

---

### ❌ Q45 — Temporal/Comparativa

**Q:** Volume mensal de internações por trauma (S00-T98) vs doenças musculoesqueléticas (M00-M99) em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    SUM(CASE WHEN icd_group = 'S00-T98' THEN 1 ELSE 0 END) AS trauma_internacoes,
    SUM(CASE WHEN icd_group = 'M00-M99' THEN 1 ELSE 0 END) AS musculo_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt
```

**Resultado:** `Número de linhas: got=12 exp=24`
**TTA:** 4554ms

---

### ❌ Q46 — Temporal/Comparativa

**Q:** Evolução mensal de óbitos em internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt, 
    COUNT(DISTINCT n_aih) AS total_obitos
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND morte = 1
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt
```

**Resultado:** `Número de linhas: got=11 exp=12`
**TTA:** 4226ms

---

### ❌ Q47 — Temporal/Comparativa

**Q:** Permanência média por trimestre nas internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    CEIL(mes_cmpt / 3.0) AS trimestre,
    AVG(dias_perm) AS permanencia_media
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    trimestre
ORDER BY 
    trimestre
```

**Resultado:** `Linha não encontrada: {'trimestre': 'Q1', 'permanencia_media_dias': 8.8}`
**TTA:** 4145ms

---

### ❌ Q48 — Temporal/Comparativa

**Q:** Mês com maior custo médio por internação ortopédica em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt, 
    AVG(custo_total) AS custo_medio
FROM 
    processed
WHERE 
    sistema = 'SIH' 
    AND uf_origem = 'RJ' 
    AND ano_cmpt = 2022 
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    custo_medio DESC
LIMIT 1
```

**Resultado:** `got=6 exp=11`
**TTA:** 3704ms

---

### ✅ Q49 — Temporal/Comparativa

**Q:** Sazonalidade mensal de fraturas de fêmur (S72) em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt, 
    COUNT(DISTINCT n_aih) AS total_fraturas_femur
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND cid_principal LIKE 'S72%'
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 4171ms

---

### ❌ Q50 — Temporal/Comparativa

**Q:** Volume mensal de internações ortopédicas de idosos (60 anos ou mais) em RJ em 2022

**SQL gerado:**
```sql
SELECT 
  mes_cmpt, 
  COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND idade_paciente >= 60
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
  mes_cmpt
ORDER BY 
  mes_cmpt
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'volume_internacoes': 26} exp={'mes_cmpt': 1, 'total_internacoes': 731}`
**TTA:** 4098ms

---

### ✅ Q51 — Epidemiológica Simples

**Q:** Número de internações por fratura de fêmur (S72) em mulheres em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND cid_principal LIKE 'S72%'
  AND sexo_paciente = 'F'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
```

**Resultado:** `got=5436 exp=5436`
**TTA:** 3883ms

---

### ✅ Q52 — Epidemiológica Simples

**Q:** Número de internações por fratura de fêmur (S72) em homens em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND cid_principal LIKE 'S72%'
  AND sexo_paciente = 'M'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
```

**Resultado:** `got=4509 exp=4509`
**TTA:** 4019ms

---

### ❌ Q53 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com permanência de 1 dia ou menos em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS num_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND dias_perm <= 1
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=373 exp=5619`
**TTA:** 4306ms

---

### ❌ Q54 — Epidemiológica Simples

**Q:** Número de procedimentos ambulatoriais ortopédicos realizados em mulheres em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(*)
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND sexo_paciente = 'F'
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=1721 exp=12838`
**TTA:** 3835ms

---

### ❌ Q55 — Epidemiológica Simples

**Q:** Número de procedimentos ambulatoriais ortopédicos realizados por idosos (60 anos ou mais) em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(*)
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND idade_paciente >= 60
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=1228 exp=13298`
**TTA:** 3889ms

---

### ✅ Q56 — Epidemiológica Simples

**Q:** Número de internações por fratura de tíbia e perônio (S82) em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND cid_principal LIKE 'S82%'
```

**Resultado:** `got=2991 exp=2991`
**TTA:** 4350ms

---

### ✅ Q57 — Epidemiológica Simples

**Q:** Número de internações por fratura de antebraço (S52) em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS num_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND cid_principal LIKE 'S52%'
```

**Resultado:** `got=1447 exp=1447`
**TTA:** 3776ms

---

### ✅ Q58 — Epidemiológica Simples

**Q:** Número de internações por fratura de ombro e úmero (S42) em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND cid_principal LIKE 'S42%'
```

**Resultado:** `got=765 exp=765`
**TTA:** 4001ms

---

### ❌ Q59 — Epidemiológica Simples

**Q:** Número de internações ortopédicas em crianças (menores de 18 anos) em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND idade_paciente < 18
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=358 exp=2380`
**TTA:** 4167ms

---

### ❌ Q60 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com permanência entre 8 e 30 dias em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) 
FROM processed 
WHERE sistema = 'SIH' 
  AND uf_origem = 'RJ' 
  AND ano_cmpt = 2022 
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99') 
  AND dias_perm BETWEEN 8 AND 30
```

**Resultado:** `got=604 exp=8235`
**TTA:** 4230ms

---

### ✅ Q61 — Epidemiológica Simples

**Q:** Número de internações por artrose de quadril e joelho (M16 e M17) em idosos (60 anos ou mais) em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND idade_paciente >= 60
  AND (cid_principal LIKE 'M16%' OR cid_principal LIKE 'M17%')
```

**Resultado:** `got=62 exp=62`
**TTA:** 4470ms

---

### ✅ Q62 — Epidemiológica Simples

**Q:** Número de internações por fratura de fêmur (S72) em mulheres com 70 anos ou mais em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND cid_principal LIKE 'S72%'
  AND sexo_paciente = 'F'
  AND idade_paciente >= 70
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
```

**Resultado:** `got=3841 exp=3841`
**TTA:** 3852ms

---

### ❌ Q63 — Epidemiológica Simples

**Q:** Número de internações ortopédicas com permanência superior a 30 dias em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) 
FROM processed 
WHERE sistema = 'SIH' 
  AND uf_origem = 'RJ' 
  AND ano_cmpt = 2022 
  AND dias_perm > 30 
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=131 exp=1010`
**TTA:** 4783ms

---

### ❌ Q64 — Epidemiológica Simples

**Q:** Número de procedimentos ambulatoriais ortopédicos realizados em homens em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(*)
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND sexo_paciente = 'M'
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=1465 exp=14363`
**TTA:** 3153ms

---

### ✅ Q65 — Epidemiológica Simples

**Q:** Número de internações por traumatismos (grupo S00-T98) em idosos (60 anos ou mais) em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND icd_group = 'S00-T98'
  AND idade_paciente >= 60
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
```

**Resultado:** `got=9563 exp=9563`
**TTA:** 4189ms

---

### ✅ Q66 — Epidemiológica Simples

**Q:** Número de internações por doenças osteomusculares (grupo M00-M99) em mulheres em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
JOIN enriched e ON processed.row_id = e.row_id
WHERE sistema = 'SIH'
  AND e.clinical_interpretacao_clinica = 'doencas osteomusculares e do tecido conjuntivo'
  AND sexo_paciente = 'F'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
```

**Resultado:** `got=880 exp=880`
**TTA:** 4766ms

---

### ✅ Q67 — Epidemiológica Simples

**Q:** Número de óbitos em internações por fratura de fêmur (S72) em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(*) AS numero_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND cid_principal LIKE 'S72%'
  AND morte = 1
```

**Resultado:** `got=425 exp=425`
**TTA:** 3640ms

---

### ❌ Q68 — Epidemiológica Simples

**Q:** Número de municípios distintos com registro de procedimentos ambulatoriais ortopédicos em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT cod_munic_estabelecimento) 
FROM processed 
WHERE sistema = 'SIA' 
  AND uf_origem = 'RJ' 
  AND ano_cmpt = 2022 
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=19 exp=74`
**TTA:** 4031ms

---

### ❌ Q69 — Epidemiológica Complexa

**Q:** Top 5 municípios por volume de procedimentos ambulatoriais ortopédicos em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    p.cod_munic_estabelecimento, 
    COUNT(*) AS volume_procedimentos
FROM 
    processed p
WHERE 
    p.sistema = 'SIA'
    AND p.uf_origem = 'RJ'
    AND p.ano_cmpt = 2022
    AND (p.cid_principal LIKE 'M%' OR p.icd_group = 'M00-M99')
GROUP BY 
    p.cod_munic_estabelecimento
ORDER BY 
    volume_procedimentos DESC
LIMIT 5
```

**Resultado:** `Linha 1: got={'cod_munic_estabelecimento': '330330', 'volume_procedimentos': 1831} exp={'cod_munic_estabelecimento': '330455', 'total': 7104}`
**TTA:** 5100ms

---

### ❌ Q70 — Epidemiológica Complexa

**Q:** Distribuição de procedimentos ambulatoriais ortopédicos por faixa etária em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 34 THEN '18-34'
        WHEN idade_paciente BETWEEN 35 AND 49 THEN '35-49'
        WHEN idade_paciente BETWEEN 50 AND 64 THEN '50-64'
        ELSE '65+' 
    END AS faixa_etaria,
    COUNT(*) AS quantidade_procedimentos
FROM 
    processed
WHERE 
    sistema = 'SIA'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    faixa_etaria
ORDER BY 
    faixa_etaria
```

**Resultado:** `Número de linhas: got=5 exp=3`
**TTA:** 4007ms

---

### ❌ Q71 — Epidemiológica Complexa

**Q:** Top 5 diagnósticos com maior número de óbitos em internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    p.cid_principal, 
    COUNT(*) AS numero_obitos
FROM 
    processed p
JOIN 
    enriched e ON p.row_id = e.row_id
WHERE 
    p.sistema = 'SIH'
    AND p.uf_origem = 'RJ'
    AND p.ano_cmpt = 2022
    AND p.morte = 1
    AND (p.cid_principal LIKE 'M%' OR p.icd_group = 'M00-M99')
GROUP BY 
    p.cid_principal
ORDER BY 
    numero_obitos DESC
LIMIT 5
```

**Resultado:** `Linha 1: got={'cid_principal': 'M869', 'numero_obitos': 5} exp={'cid_principal': 'S720', 'total_internacoes': 3502, 'total_obitos': 187.0}`
**TTA:** 4451ms

---

### ❌ Q72 — Epidemiológica Complexa

**Q:** Permanência hospitalar média por sexo em internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    sexo_paciente, 
    AVG(dias_perm) AS permanencia_media
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    sexo_paciente
```

**Resultado:** `Linha não encontrada: {'sexo_paciente': 'F', 'permanencia_media': 8.6}`
**TTA:** 2964ms

---

### ❌ Q73 — Epidemiológica Complexa

**Q:** Top 10 estabelecimentos por volume de procedimentos ambulatoriais ortopédicos em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    p.cnes_estabelecimento, 
    COUNT(*) AS volume_procedimentos
FROM 
    processed p
WHERE 
    p.sistema = 'SIA'
    AND p.uf_origem = 'RJ'
    AND p.ano_cmpt = 2022
    AND (p.cid_principal LIKE 'M%' OR p.icd_group = 'M00-M99')
GROUP BY 
    p.cnes_estabelecimento
ORDER BY 
    volume_procedimentos DESC
LIMIT 10
```

**Resultado:** `Linha 1: got={'cnes_estabelecimento': '2272997', 'volume_procedimentos': 1053} exp={'cnes_estabelecimento': '2270048', 'total': 4745}`
**TTA:** 4210ms

---

### ❌ Q74 — Epidemiológica Complexa

**Q:** Taxa de mortalidade por sexo em internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    sexo_paciente,
    SUM(CASE WHEN morte = 1 THEN 1 ELSE 0 END) * 100.0 / COUNT(DISTINCT n_aih) AS taxa_mortalidade
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    sexo_paciente
```

**Resultado:** `Linha não encontrada: {'sexo_paciente': 'F', 'total_obitos': 412.0, 'taxa_mortalidade': 3.94}`
**TTA:** 4335ms

---

### ❌ Q75 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por grupo diagnóstico (M e S) e faixa etária em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 34 THEN '18-34'
        WHEN idade_paciente BETWEEN 35 AND 49 THEN '35-49'
        WHEN idade_paciente BETWEEN 50 AND 64 THEN '50-64'
        ELSE '65+' 
    END AS faixa_etaria,
    icd_group,
    COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (icd_group = 'M00-M99' OR icd_group = 'S00-T98')
GROUP BY 
    faixa_etaria, icd_group
ORDER BY 
    faixa_etaria, icd_group;
```

**Resultado:** `Número de linhas: got=10 exp=6`
**TTA:** 4778ms

---

### ❌ Q76 — Epidemiológica Complexa

**Q:** Top 5 diagnósticos de traumatismo com maior permanência média em RJ em 2022 (mínimo 40 internações)

**SQL gerado:**
```sql
SELECT 
    p.cid_principal, 
    COUNT(DISTINCT p.n_aih) AS num_internacoes,
    AVG(p.dias_perm) AS permanencia_media
FROM 
    processed p
JOIN 
    enriched e ON p.row_id = e.row_id
WHERE 
    p.sistema = 'SIH'
    AND p.uf_origem = 'RJ'
    AND p.ano_cmpt = 2022
    AND e.clinical_interpretacao_clinica = 'lesoes e causas traumaticas'
GROUP BY 
    p.cid_principal
HAVING 
    COUNT(DISTINCT p.n_aih) >= 40
ORDER BY 
    permanencia_media DESC
LIMIT 5
```

**Resultado:** `Linha 5: got={'cid_principal': 'S365', 'num_internacoes': 62, 'permanencia_media': 12.1451612903} exp={'cid_principal': 'S065', 'internacoes': 295, 'permanencia_media': 12.1}`
**TTA:** 4621ms

---

### ❌ Q77 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por faixa etária (0-17, 18-39, 40-59, 60-79, 80+) em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    CASE 
        WHEN idade_paciente BETWEEN 0 AND 17 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 39 THEN '18-39'
        WHEN idade_paciente BETWEEN 40 AND 59 THEN '40-59'
        WHEN idade_paciente BETWEEN 60 AND 79 THEN '60-79'
        ELSE '80+'
    END AS faixa_etaria,
    COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    faixa_etaria
ORDER BY 
    faixa_etaria
```

**Resultado:** `Linha não encontrada: {'faixa_etaria': '0-17', 'total': 2380}`
**TTA:** 4056ms

---

### ✅ Q78 — Epidemiológica Complexa

**Q:** Top 5 estabelecimentos com maior volume de internações por fratura de fêmur (S72) em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    p.cnes_estabelecimento, 
    COUNT(DISTINCT p.n_aih) AS volume_internacoes
FROM 
    processed p
WHERE 
    p.sistema = 'SIH'
    AND p.uf_origem = 'RJ'
    AND p.ano_cmpt = 2022
    AND p.cid_principal LIKE 'S72%'
GROUP BY 
    p.cnes_estabelecimento
ORDER BY 
    volume_internacoes DESC
LIMIT 5
```

**Resultado:** `5 linhas corretas (flexível)`
**TTA:** 3915ms

---

### ❌ Q79 — Epidemiológica Complexa

**Q:** Permanência hospitalar média por faixa etária em internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 34 THEN '18-34'
        WHEN idade_paciente BETWEEN 35 AND 49 THEN '35-49'
        WHEN idade_paciente BETWEEN 50 AND 64 THEN '50-64'
        ELSE '65+' 
    END AS faixa_etaria,
    AVG(dias_perm) AS permanencia_media
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    faixa_etaria
ORDER BY 
    faixa_etaria
```

**Resultado:** `Número de linhas: got=5 exp=3`
**TTA:** 4447ms

---

### ✅ Q80 — Epidemiológica Complexa

**Q:** Top 5 diagnósticos de doença osteomuscular (M00-M99) por número de internações em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    main_icd, 
    COUNT(DISTINCT n_aih) AS num_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND icd_group = 'M00-M99'
GROUP BY 
    main_icd
ORDER BY 
    num_internacoes DESC
LIMIT 5
```

**Resultado:** `5 linhas corretas (flexível)`
**TTA:** 3995ms

---

### ❌ Q81 — Epidemiológica Complexa

**Q:** Número de internações ortopédicas com permanência entre 3 e 7 dias em RJ em 2022

**SQL gerado:**
```sql
SELECT COUNT(DISTINCT n_aih) AS num_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
  AND dias_perm BETWEEN 3 AND 7
```

**Resultado:** `got=717 exp=9617`
**TTA:** 3238ms

---

### ❌ Q82 — Epidemiológica Complexa

**Q:** Top 5 diagnósticos ambulatoriais ortopédicos em idosos (60 anos ou mais) em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    main_icd AS diagnostico,
    COUNT(*) AS total
FROM 
    processed
WHERE 
    sistema = 'SIA'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND idade_paciente >= 60
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    main_icd
ORDER BY 
    total DESC
LIMIT 5
```

**Resultado:** `Linha 1: got={'diagnostico': 'M216', 'total': 729} exp={'cid_principal': 'S720', 'total': 3056}`
**TTA:** 4661ms

---

### ❌ Q83 — Epidemiológica Complexa

**Q:** Média de internações ortopédicas por estabelecimento em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    cnes_estabelecimento,
    AVG(internacoes) AS media_internacoes
FROM (
    SELECT 
        cnes_estabelecimento,
        COUNT(DISTINCT n_aih) AS internacoes
    FROM 
        processed
    WHERE 
        sistema = 'SIH'
        AND uf_origem = 'RJ'
        AND ano_cmpt = 2022
        AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
    GROUP BY 
        cnes_estabelecimento
) AS subquery
GROUP BY 
    cnes_estabelecimento
```

**Resultado:** `got=2704595 exp=207.8`
**TTA:** 4088ms

---

### ❌ Q84 — Epidemiológica Complexa

**Q:** Distribuição de internações ortopédicas por faixa de permanência em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    CASE 
        WHEN dias_perm <= 3 THEN '1-3 dias'
        WHEN dias_perm BETWEEN 4 AND 7 THEN '4-7 dias'
        WHEN dias_perm BETWEEN 8 AND 14 THEN '8-14 dias'
        WHEN dias_perm BETWEEN 15 AND 30 THEN '15-30 dias'
        ELSE 'Mais de 30 dias'
    END AS faixa_permanencia,
    COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    faixa_permanencia
ORDER BY 
    faixa_permanencia
```

**Resultado:** `Número de linhas: got=5 exp=4`
**TTA:** 5073ms

---

### ❌ Q85 — Financeira

**Q:** Custo total de internações ortopédicas de idosos (60 anos ou mais) em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes_ortopedicas_idosos
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND idade_paciente >= 60
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=2115629.96 exp=28396358.5`
**TTA:** 3610ms

---

### ✅ Q86 — Financeira

**Q:** Custo médio por internação por fratura de fêmur (S72) em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    COALESCE(SUM(custo_total) / COUNT(DISTINCT n_aih), 0) AS custo_medio_por_internacao
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND cid_principal LIKE 'S72%'
```

**Resultado:** `got=2648.293653092 exp=2648.29`
**TTA:** 3554ms

---

### ✅ Q87 — Financeira

**Q:** Custo total de internações ortopédicas com passagem por UTI em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_ortopedicas_uti
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND icd_group = 'M00-M99'
    AND uti_int_to > 0
```

**Resultado:** `got=None exp=None`
**TTA:** 3919ms

---

### ❌ Q88 — Financeira

**Q:** Custo médio por internação ortopédica por sexo do paciente em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    sexo_paciente, 
    AVG(custo_total) AS custo_medio
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    sexo_paciente
```

**Resultado:** `Linha não encontrada: {'sexo_paciente': 'F', 'custo_medio': 2634.6}`
**TTA:** 4255ms

---

### ❌ Q89 — Financeira

**Q:** Custo total de internações ortopédicas em crianças (menores de 18 anos) em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes_ortopedicas
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND idade_paciente < 18
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=1995445.81 exp=5562026.72`
**TTA:** 4197ms

---

### ❌ Q90 — Financeira

**Q:** Custo médio por internação ortopédica com permanência superior a 7 dias em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    COALESCE(SUM(custo_total) / COUNT(DISTINCT n_aih), 0) AS custo_medio
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND dias_perm > 7
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=5014.9515258856 exp=3699.42`
**TTA:** 4081ms

---

### ❌ Q91 — Financeira

**Q:** Top 3 municípios com maior custo total em internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    p.cod_munic_estabelecimento, 
    SUM(p.custo_total) AS total_custo
FROM 
    processed p
JOIN 
    enriched e ON p.row_id = e.row_id
WHERE 
    p.sistema = 'SIH'
    AND p.uf_origem = 'RJ'
    AND p.ano_cmpt = 2022
    AND (p.cid_principal LIKE 'M%' OR p.icd_group = 'M00-M99')
GROUP BY 
    p.cod_munic_estabelecimento
ORDER BY 
    total_custo DESC
LIMIT 3
```

**Resultado:** `Linha 1: got={'cod_munic_estabelecimento': '330000', 'total_custo': 3740518.5500000003} exp={'cod_munic_estabelecimento': '330000', 'custo_total': 26271214.76}`
**TTA:** 4806ms

---

### ❌ Q92 — Financeira

**Q:** Valor total de serviço hospitalar em internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT COALESCE(SUM(val_sh), 0) AS total_valor_servico_hospitalar
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=6937652.41 exp=57490872.2`
**TTA:** 3221ms

---

### ❌ Q93 — Financeira

**Q:** Custo médio por internação ortopédica por faixa etária em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 34 THEN '18-34'
        WHEN idade_paciente BETWEEN 35 AND 49 THEN '35-49'
        WHEN idade_paciente BETWEEN 50 AND 64 THEN '50-64'
        ELSE '65+' 
    END AS faixa_etaria,
    COALESCE(SUM(custo_total) / COUNT(DISTINCT n_aih), 0) AS custo_medio_por_internacao
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    faixa_etaria
ORDER BY 
    faixa_etaria
```

**Resultado:** `Número de linhas: got=5 exp=3`
**TTA:** 5029ms

---

### ❌ Q94 — Financeira

**Q:** Valor total de UTI em internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    COALESCE(SUM(val_uti), 0) AS total_valor_uti
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=868901.98 exp=13056994.92`
**TTA:** 3461ms

---

### ✅ Q95 — Financeira

**Q:** Custo total de internações por doenças osteomusculares (M00-M99) em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND icd_group = 'M00-M99'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
```

**Resultado:** `got=8405202.74 exp=8405202.74`
**TTA:** 4222ms

---

### ✅ Q96 — Financeira

**Q:** Custo total de internações por traumatismos (S00-T98) em RJ em 2022

**SQL gerado:**
```sql
SELECT SUM(COALESCE(custo_total, 0)) AS total_custo_traumatismos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND icd_group = 'S00-T98'
```

**Resultado:** `got=62488151.480000086 exp=62488151.48`
**TTA:** 3640ms

---

### ❌ Q97 — Financeira

**Q:** Top 5 diagnósticos com maior custo médio por internação ortopédica em RJ em 2022 (mínimo 40 internações)

**SQL gerado:**
```sql
SELECT 
    p.cid_principal, 
    COUNT(DISTINCT p.n_aih) AS num_internacoes,
    SUM(p.custo_total) / COUNT(DISTINCT p.n_aih) AS custo_medio
FROM 
    processed p
WHERE 
    p.sistema = 'SIH'
    AND p.uf_origem = 'RJ'
    AND p.ano_cmpt = 2022
    AND (p.cid_principal LIKE 'M%' OR p.icd_group = 'M00-M99')
GROUP BY 
    p.cid_principal
HAVING 
    COUNT(DISTINCT p.n_aih) >= 40
ORDER BY 
    custo_medio DESC
LIMIT 5
```

**Resultado:** `Linha 2: got={'cid_principal': 'M511', 'num_internacoes': 129, 'custo_medio': 8820.2801550388} exp={'cid_principal': 'S068', 'internacoes': 55, 'custo_medio': 8934.42}`
**TTA:** 4951ms

---

### ❌ Q98 — Financeira

**Q:** Custo médio por procedimento ambulatorial ortopédico em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    AVG(custo_total) AS custo_medio
FROM 
    processed
WHERE 
    sistema = 'SIA'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=272.2012743252 exp=152.55`
**TTA:** 3553ms

---

### ❌ Q99 — Financeira

**Q:** Top 5 procedimentos ambulatoriais com maior custo total em RJ em 2022

**SQL gerado:**
```sql
SELECT cod_procedimento, SUM(custo_total) AS total_custo
FROM processed
WHERE sistema = 'SIA' AND uf_origem = 'RJ' AND ano_cmpt = 2022
GROUP BY cod_procedimento
ORDER BY total_custo DESC
LIMIT 5
```

**Resultado:** `Linha 1: got={'cod_procedimento': '0702100048', 'total_custo': 9146187.890000049} exp={'cod_procedimento': '0701020369', 'custo_total': 665532.0}`
**TTA:** 3775ms

---

### ❌ Q100 — Financeira

**Q:** Custo total de internações ortopédicas de idosos (60+) com permanência superior a 7 dias em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND idade_paciente >= 60
    AND dias_perm > 7
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
```

**Resultado:** `got=1013984.5700000001 exp=16205065.06`
**TTA:** 4683ms

---

### ❌ Q101 — Temporal/Comparativa

**Q:** Mês com maior número de óbitos em internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT mes_cmpt, COUNT(*) AS num_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND morte = 1
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY mes_cmpt
ORDER BY num_obitos DESC
LIMIT 1
```

**Resultado:** `Linha 1: got={'mes_cmpt': 11, 'num_obitos': 4} exp={'mes_cmpt': 1, 'total_obitos': 104}`
**TTA:** 4086ms

---

### ✅ Q102 — Temporal/Comparativa

**Q:** Volume mensal de internações por fratura de fêmur (S72) em idosos (60 anos ou mais) em RJ em 2022

**SQL gerado:**
```sql
SELECT 
  mes_cmpt, 
  COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND idade_paciente >= 60
  AND cid_principal LIKE 'S72%'
GROUP BY 
  mes_cmpt
ORDER BY 
  mes_cmpt
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 4066ms

---

### ❌ Q103 — Temporal/Comparativa

**Q:** Custo mensal de internações ortopédicas de idosos (60 anos ou mais) em RJ em 2022

**SQL gerado:**
```sql
SELECT 
  mes_cmpt, 
  SUM(COALESCE(custo_total, 0)) AS custo_total_mensal
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND idade_paciente >= 60
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
  mes_cmpt
ORDER BY 
  mes_cmpt
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'custo_total_mensal': 82431.4} exp={'mes_cmpt': 1, 'custo_total': 1749439.39}`
**TTA:** 4275ms

---

### ✅ Q104 — Temporal/Comparativa

**Q:** Mês com maior custo total de internações por fratura de fêmur (S72) em RJ em 2022

**SQL gerado:**
```sql
SELECT mes_cmpt, SUM(custo_total) AS total_custo
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND cid_principal LIKE 'S72%'
GROUP BY mes_cmpt
ORDER BY total_custo DESC
LIMIT 1
```

**Resultado:** `1 linhas corretas (flexível)`
**TTA:** 3481ms

---

### ✅ Q105 — Temporal/Comparativa

**Q:** Volume mensal de internações por traumatismos (grupo S00-T98) em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt, 
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND icd_group = 'S00-T98'
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 4366ms

---

### ❌ Q106 — Temporal/Comparativa

**Q:** Volume mensal de procedimentos ambulatoriais ortopédicos por sexo em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt, 
    sexo_paciente, 
    SUM(pa_qtdapr) AS total_procedimentos
FROM 
    processed
WHERE 
    sistema = 'SIA'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt, 
    sexo_paciente
ORDER BY 
    mes_cmpt, 
    sexo_paciente;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'sexo_paciente': 'F', 'total_procedimentos': 136.0} exp={'mes_cmpt': 1, 'sexo_paciente': 'F', 'total': 739}`
**TTA:** 5488ms

---

### ❌ Q107 — Temporal/Comparativa

**Q:** Comparativo trimestral de internações ortopédicas por grupo diagnóstico em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    CASE 
        WHEN mes_cmpt IN (1, 2, 3) THEN '1º Trimestre'
        WHEN mes_cmpt IN (4, 5, 6) THEN '2º Trimestre'
        WHEN mes_cmpt IN (7, 8, 9) THEN '3º Trimestre'
        WHEN mes_cmpt IN (10, 11, 12) THEN '4º Trimestre'
    END AS trimestre,
    icd_group,
    COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    trimestre, icd_group
ORDER BY 
    trimestre, icd_group;
```

**Resultado:** `Número de linhas: got=4 exp=8`
**TTA:** 3617ms

---

### ❌ Q108 — Temporal/Comparativa

**Q:** Mês com maior permanência hospitalar média em internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT mes_cmpt, AVG(dias_perm) AS media_permanencia
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY mes_cmpt
ORDER BY media_permanencia DESC
LIMIT 1
```

**Resultado:** `Linha 1: got={'mes_cmpt': 3, 'media_permanencia': 11.5039370079} exp={'mes_cmpt': 1, 'permanencia_media': 9.9}`
**TTA:** 3541ms

---

### ❌ Q109 — Temporal/Comparativa

**Q:** Evolução mensal da taxa de mortalidade em internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    COUNT(DISTINCT CASE WHEN morte = 1 THEN n_aih END) * 1.0 / COUNT(DISTINCT n_aih) AS taxa_mortalidade
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'taxa_mortalidade': 0.0165289256} exp={'mes_cmpt': 1, 'internacoes': 1998, 'obitos': 104.0, 'taxa_mortalidade': 5.21}`
**TTA:** 4575ms

---

### ❌ Q110 — Temporal/Comparativa

**Q:** Trimestre com maior volume de internações por fratura de fêmur (S72) em RJ em 2022

**SQL gerado:**
```sql
SELECT 
  CEIL(mes_cmpt / 3.0) AS trimestre,
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND cid_principal LIKE 'S72%'
GROUP BY 
  trimestre
ORDER BY 
  total_internacoes DESC
LIMIT 1
```

**Resultado:** `Linha 1: got={'trimestre': 3.0, 'total_internacoes': 2707} exp={'trimestre': 'Q3', 'total': 2707}`
**TTA:** 3856ms

---

### ❌ Q111 — Temporal/Comparativa

**Q:** Comparativo semestral de internações ortopédicas (1º vs 2º semestre) em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    CASE 
        WHEN mes_cmpt BETWEEN 1 AND 6 THEN '1º Semestre'
        ELSE '2º Semestre'
    END AS semestre,
    COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    semestre
ORDER BY 
    semestre
```

**Resultado:** `Linha não encontrada: {'semestre': '1S', 'total': 12212}`
**TTA:** 4132ms

---

### ✅ Q112 — Temporal/Comparativa

**Q:** Custo médio mensal por internação por fratura de fêmur (S72) em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    COALESCE(SUM(custo_total) / COUNT(DISTINCT n_aih), 0) AS custo_medio_mensal
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND cid_principal LIKE 'S72%'
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt
```

**Resultado:** `12 linhas corretas (flexível)`
**TTA:** 3835ms

---

### ❌ Q113 — Temporal/Comparativa

**Q:** Volume mensal de internações ortopédicas por sexo do paciente em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt, 
    sexo_paciente, 
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt, 
    sexo_paciente
ORDER BY 
    mes_cmpt, 
    sexo_paciente;
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'sexo_paciente': 'F', 'volume_internacoes': 53} exp={'mes_cmpt': 1, 'sexo_paciente': 'F', 'total': 815}`
**TTA:** 4058ms

---

### ❌ Q114 — Temporal/Comparativa

**Q:** Mês com menor volume de procedimentos ambulatoriais ortopédicos em RJ em 2022

**SQL gerado:**
```sql
SELECT mes_cmpt, SUM(pa_qtdapr) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY mes_cmpt
ORDER BY total_procedimentos ASC
LIMIT 1
```

**Resultado:** `Linha 1: got={'mes_cmpt': 4, 'total_procedimentos': 217.0} exp={'mes_cmpt': 1, 'total': 1647}`
**TTA:** 4017ms

---

### ❌ Q115 — Temporal/Comparativa

**Q:** Volume trimestral de procedimentos ambulatoriais ortopédicos em RJ em 2022

**SQL gerado:**
```sql
SELECT 
  CEIL(mes_cmpt / 3.0) AS trimestre,
  COUNT(*) AS volume_procedimentos
FROM 
  processed
WHERE 
  sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
  trimestre
ORDER BY 
  trimestre
```

**Resultado:** `Linha não encontrada: {'trimestre': 'Q1', 'total': 5399}`
**TTA:** 4563ms

---

### ❌ Q116 — Temporal/Comparativa

**Q:** Custo mensal de procedimentos ambulatoriais ortopédicos em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt, 
    SUM(COALESCE(custo_total, 0)) AS custo_total_mensal
FROM 
    processed
WHERE 
    sistema = 'SIA'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt
```

**Resultado:** `Linha 1: got={'mes_cmpt': 1, 'custo_total_mensal': 55130.4499999999} exp={'mes_cmpt': 1, 'custo_total': 284703.64}`
**TTA:** 3990ms

---

### ❌ Q117 — Temporal/Comparativa

**Q:** Comparativo semestral do custo total de internações ortopédicas em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    CASE 
        WHEN mes_cmpt BETWEEN 1 AND 6 THEN '1º Semestre'
        ELSE '2º Semestre'
    END AS semestre,
    SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND (cid_principal LIKE 'M%' OR icd_group = 'M00-M99')
GROUP BY 
    semestre
ORDER BY 
    semestre
```

**Resultado:** `Linha não encontrada: {'semestre': '1S', 'custo_total': 29834491.27}`
**TTA:** 3922ms

---

### ✅ Q118 — Temporal/Comparativa

**Q:** Evolução mensal da permanência média em internações por fratura de fêmur (S72) em RJ em 2022

**SQL gerado:**
```sql
SELECT 
    mes_cmpt,
    AVG(dias_perm) AS permanencia_media
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = 'RJ'
    AND ano_cmpt = 2022
    AND cid_principal LIKE 'S72%'
GROUP BY 
    mes_cmpt
ORDER BY 
    mes_cmpt
```

**Resultado:** `12 linhas corretas (exato)`
**TTA:** 4057ms

---

## Failures Detail

- **Q02** (Epidemiológica Simples): got=3387.0 exp=27201
  SQL: `SELECT SUM(pa_qtdapr) AS total_procedimentos_ambulatoriais_ortopedicos
FROM processed
WHERE sistema = 'SIA'
  AND uf_ori...`
- **Q05** (Epidemiológica Simples): got=21 exp=965
  SQL: `SELECT COUNT(*) AS numero_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND...`
- **Q08** (Epidemiológica Simples): got=880 exp=10444
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND sexo_paciente = 'F'
  AND ...`
- **Q09** (Epidemiológica Simples): got=1445 exp=17612
  SQL: `SELECT COUNT(DISTINCT n_aih) AS num_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cm...`
- **Q10** (Epidemiológica Simples): got=586 exp=10149
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano...`
- **Q11** (Epidemiológica Simples): got=46 exp=63
  SQL: `SELECT COUNT(DISTINCT cod_munic_estabelecimento)
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_c...`
- **Q12** (Epidemiológica Simples): got=100 exp=135
  SQL: `SELECT COUNT(DISTINCT cnes_estabelecimento) 
FROM processed 
WHERE sistema = 'SIH' 
  AND uf_origem = 'RJ' 
  AND ano_cm...`
- **Q13** (Epidemiológica Simples): got=734 exp=9244
  SQL: `SELECT COUNT(DISTINCT n_aih) 
FROM processed 
WHERE sistema = 'SIH' 
  AND uf_origem = 'RJ' 
  AND ano_cmpt = 2022 
  AN...`
- **Q16** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'M861', 'volume_internacoes': 182} exp={'cid_principal': 'S720', 'total_internacoes': 3502}
  SQL: `SELECT 
    cid_principal, 
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
WHERE 
    sistema = 'SI...`
- **Q17** (Epidemiológica Complexa): Linha 1: got={'cod_munic_estabelecimento': '330000', 'num_internacoes': 1007} exp={'cod_munic_estabelecimento': '330000', 'internacoes': 9878}
  SQL: `SELECT 
    p.cod_munic_estabelecimento, 
    COUNT(DISTINCT p.n_aih) AS num_internacoes
FROM 
    processed p
JOIN 
   ...`
- **Q18** (Epidemiológica Complexa): Linha não encontrada: {'faixa_etaria': '0-17', 'total': 2380}
  SQL: `SELECT 
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 59 THEN '18-59...`
- **Q19** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'M858', 'total_internacoes': 1, 'total_obitos': 1.0, 'taxa_mortalidade': 100.0} exp={'cid_principal': 'T203', 'total_internacoes': 44, 'total_obitos': 26.0, 'taxa_mortalidade_pct': 59.09}
  SQL: `SELECT 
    p.cid_principal, 
    COUNT(DISTINCT p.n_aih) AS total_internacoes, 
    SUM(p.morte) AS total_obitos, 
    ...`
- **Q20** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'M216', 'volume_producao': 1963} exp={'cid_principal': 'S720', 'total_procedimentos': 5570}
  SQL: `SELECT 
    cid_principal, 
    COUNT(*) AS volume_producao
FROM 
    processed
WHERE 
    sistema = 'SIA'
    AND uf_or...`
- **Q21** (Epidemiológica Complexa): Linha não encontrada: {'sexo_paciente': 'M', 'total_internacoes': 17612, 'pct': 62.8}
  SQL: `SELECT 
    sexo_paciente, 
    COUNT(DISTINCT n_aih) AS total_internacoes,
    COUNT(DISTINCT n_aih) * 1.0 / SUM(COUNT(...`
- **Q22** (Epidemiológica Complexa): Linha 1: got={'fraturas_femur_idosos': 6628, 'total_internacoes': 9945} exp={'total_s72': 9945, 's72_idosos': 6628.0, 'pct_idosos': 66.6}
  SQL: `SELECT 
    COUNT(DISTINCT CASE WHEN idade_paciente >= 60 AND cid_principal LIKE 'S72%' THEN n_aih END) AS fraturas_femu...`
- **Q23** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'M463', 'permanencia_media': 57.5} exp={'cid_principal': 'T203', 'total_internacoes': 44, 'permanencia_media_dias': 30.1}
  SQL: `SELECT 
    cid_principal, 
    AVG(dias_perm) AS permanencia_media
FROM 
    processed
WHERE 
    sistema = 'SIH'
    A...`
- **Q24** (Epidemiológica Complexa): Linha 1: got={'cnes_estabelecimento': '2273276', 'volume_internacoes': 326} exp={'cnes_estabelecimento': '2290227', 'internacoes': 5043}
  SQL: `SELECT 
    p.cnes_estabelecimento, 
    COUNT(DISTINCT p.n_aih) AS volume_internacoes
FROM 
    processed p
JOIN 
    e...`
- **Q25** (Epidemiológica Complexa): Número de linhas: got=5 exp=6
  SQL: `SELECT 
  raca_cor_paciente, 
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
  processed
WHERE 
  sistema = 'SIH'
  ...`
- **Q26** (Epidemiológica Complexa): Número de linhas: got=202 exp=10
  SQL: `SELECT 
    cid_principal, 
    SUM(dias_perm) AS total_dias_internacao
FROM 
    processed
WHERE 
    sistema = 'SIH'
 ...`
- **Q27** (Epidemiológica Complexa): Linha 1: got={'cod_procedimento': '0701010053', 'total_procedimentos': 973.0} exp={'cod_procedimento': '0302050019', 'total_procedimentos': 15646}
  SQL: `SELECT cod_procedimento, SUM(pa_qtdapr) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'R...`
- **Q28** (Epidemiológica Complexa): Linha 1: got={'total_internacoes': 2325, 'deslocamento_intermunicipal': 1439.0, 'proporcao_deslocamento_intermunicipal': 0.6189247312} exp={'total_internacoes': 28056, 'deslocamento_intermunicipal': 14192.0, 'pct_deslocamento': 50.6}
  SQL: `SELECT 
    COUNT(DISTINCT p.n_aih) AS total_internacoes,
    SUM(CASE WHEN e.clinical_deslocamento_territorial = 'deslo...`
- **Q29** (Epidemiológica Complexa): Linha 1: got={'ano_cmpt': 2022, 'mes_cmpt': 1, 'sistema': 'SIA', 'volume': 208} exp={'mes_cmpt': 1, 'sistema': 'SIA', 'total': 1647}
  SQL: `SELECT 
    ano_cmpt,
    mes_cmpt,
    sistema,
    COUNT(*) AS volume
FROM 
    processed
WHERE 
    uf_origem = 'RJ'
...`
- **Q30** (Epidemiológica Complexa): Linha 1: got={'idade_media': 63.3186525892} exp={'idade_media': 63.3, 'idade_minima': 0.0, 'idade_maxima': 99.0}
  SQL: `SELECT AVG(idade_paciente) AS idade_media
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2...`
- **Q31** (Financeira): got=8405202.74 exp=70893354.22
  SQL: `SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_ortopedicas
FROM 
    processed
WHERE 
    sistema = 'SIH'
    ...`
- **Q32** (Financeira): got=3615.1409634409 exp=2525.77
  SQL: `SELECT 
    COALESCE(SUM(custo_total) / COUNT(DISTINCT n_aih), 0) AS custo_medio_por_internacao
FROM 
    processed
WHER...`
- **Q34** (Financeira): Linha 1: got={'cid_principal': 'M511', 'total_custo': 1137816.14} exp={'cid_principal': 'S720', 'internacoes': 3502, 'custo_total': 10821184.25}
  SQL: `SELECT 
    cid_principal, 
    SUM(custo_total) AS total_custo
FROM 
    processed
WHERE 
    sistema = 'SIH' 
    AND ...`
- **Q35** (Financeira): Linha 1: got={'custo_total_ortopedicas_opme': None} exp={'internacoes_com_opme': 0, 'custo_total_opme': None}
  SQL: `SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_ortopedicas_opme
FROM 
    processed
WHERE 
    sistema = 'SIH'...`
- **Q36** (Financeira): got=1467550.33 exp=13402482.02
  SQL: `SELECT 
    COALESCE(SUM(val_sp), 0) AS total_val_sp
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_origem = ...`
- **Q37** (Financeira): got=409.1374498356 exp=321.98
  SQL: `SELECT 
    SUM(custo_total) / SUM(dias_perm) AS custo_medio_por_dia
FROM 
    processed
WHERE 
    sistema = 'SIH'
    ...`
- **Q38** (Financeira): Linha 1: got={'cnes_estabelecimento': '7185081', 'total_custo': 1877775.91} exp={'cnes_estabelecimento': '2290227', 'internacoes': 5043, 'custo_total': 9418220.5}
  SQL: `SELECT 
    p.cnes_estabelecimento, 
    SUM(p.custo_total) AS total_custo
FROM 
    processed p
JOIN 
    enriched e ON...`
- **Q39** (Financeira): got=867233.2599999988 exp=4149578.04
  SQL: `SELECT COALESCE(SUM(custo_total), 0) AS custo_total_ambulatorial_ortopedico
FROM processed
WHERE sistema = 'SIA'
  AND u...`
- **Q40** (Financeira): Linha 1: got={'proporcao_custo_sh_vs_honorarios': 4.727369323} exp={'custo_servico_hospitalar': 57490872.2, 'honorarios_profissionais': 13402482.02, 'pct_hospitalar': 81.1}
  SQL: `SELECT 
    SUM(COALESCE(val_sh, 0)) / NULLIF(SUM(COALESCE(val_sp, 0)), 0) AS proporcao_custo_sh_vs_honorarios
FROM 
   ...`
- **Q41** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'total_internacoes': 121} exp={'mes_cmpt': 1, 'total_internacoes': 1998}
  SQL: `SELECT 
    mes_cmpt, 
    COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
   ...`
- **Q42** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'total_procedimentos': 208} exp={'mes_cmpt': 1, 'procedimentos': 1647}
  SQL: `SELECT 
  mes_cmpt, 
  COUNT(*) AS total_procedimentos
FROM 
  processed
WHERE 
  sistema = 'SIA'
  AND uf_origem = 'RJ'...`
- **Q44** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'custo_total_internacoes': 515324.59} exp={'mes_cmpt': 1, 'custo_total': 4528720.12}
  SQL: `SELECT 
    mes_cmpt, 
    SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes
FROM 
    processed
WHERE 
    siste...`
- **Q45** (Temporal/Comparativa): Número de linhas: got=12 exp=24
  SQL: `SELECT 
    mes_cmpt,
    SUM(CASE WHEN icd_group = 'S00-T98' THEN 1 ELSE 0 END) AS trauma_internacoes,
    SUM(CASE WHE...`
- **Q46** (Temporal/Comparativa): Número de linhas: got=11 exp=12
  SQL: `SELECT 
    mes_cmpt, 
    COUNT(DISTINCT n_aih) AS total_obitos
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND ...`
- **Q47** (Temporal/Comparativa): Linha não encontrada: {'trimestre': 'Q1', 'permanencia_media_dias': 8.8}
  SQL: `SELECT 
    CEIL(mes_cmpt / 3.0) AS trimestre,
    AVG(dias_perm) AS permanencia_media
FROM 
    processed
WHERE 
    si...`
- **Q48** (Temporal/Comparativa): got=6 exp=11
  SQL: `SELECT 
    mes_cmpt, 
    AVG(custo_total) AS custo_medio
FROM 
    processed
WHERE 
    sistema = 'SIH' 
    AND uf_or...`
- **Q50** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'volume_internacoes': 26} exp={'mes_cmpt': 1, 'total_internacoes': 731}
  SQL: `SELECT 
  mes_cmpt, 
  COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
  processed
WHERE 
  sistema = 'SIH'
  AND uf_o...`
- **Q53** (Epidemiológica Simples): got=373 exp=5619
  SQL: `SELECT COUNT(DISTINCT n_aih) AS num_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cm...`
- **Q54** (Epidemiológica Simples): got=1721 exp=12838
  SQL: `SELECT COUNT(*)
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND sexo_paciente = ...`
- **Q55** (Epidemiológica Simples): got=1228 exp=13298
  SQL: `SELECT COUNT(*)
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND idade_paciente >...`
- **Q59** (Epidemiológica Simples): got=358 exp=2380
  SQL: `SELECT COUNT(DISTINCT n_aih) AS numero_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano...`
- **Q60** (Epidemiológica Simples): got=604 exp=8235
  SQL: `SELECT COUNT(DISTINCT n_aih) 
FROM processed 
WHERE sistema = 'SIH' 
  AND uf_origem = 'RJ' 
  AND ano_cmpt = 2022 
  AN...`
- **Q63** (Epidemiológica Simples): got=131 exp=1010
  SQL: `SELECT COUNT(DISTINCT n_aih) 
FROM processed 
WHERE sistema = 'SIH' 
  AND uf_origem = 'RJ' 
  AND ano_cmpt = 2022 
  AN...`
- **Q64** (Epidemiológica Simples): got=1465 exp=14363
  SQL: `SELECT COUNT(*)
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 2022
  AND sexo_paciente = ...`
- **Q68** (Epidemiológica Simples): got=19 exp=74
  SQL: `SELECT COUNT(DISTINCT cod_munic_estabelecimento) 
FROM processed 
WHERE sistema = 'SIA' 
  AND uf_origem = 'RJ' 
  AND a...`
- **Q69** (Epidemiológica Complexa): Linha 1: got={'cod_munic_estabelecimento': '330330', 'volume_procedimentos': 1831} exp={'cod_munic_estabelecimento': '330455', 'total': 7104}
  SQL: `SELECT 
    p.cod_munic_estabelecimento, 
    COUNT(*) AS volume_procedimentos
FROM 
    processed p
WHERE 
    p.sistem...`
- **Q70** (Epidemiológica Complexa): Número de linhas: got=5 exp=3
  SQL: `SELECT 
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 34 THEN '18-34...`
- **Q71** (Epidemiológica Complexa): Linha 1: got={'cid_principal': 'M869', 'numero_obitos': 5} exp={'cid_principal': 'S720', 'total_internacoes': 3502, 'total_obitos': 187.0}
  SQL: `SELECT 
    p.cid_principal, 
    COUNT(*) AS numero_obitos
FROM 
    processed p
JOIN 
    enriched e ON p.row_id = e.r...`
- **Q72** (Epidemiológica Complexa): Linha não encontrada: {'sexo_paciente': 'F', 'permanencia_media': 8.6}
  SQL: `SELECT 
    sexo_paciente, 
    AVG(dias_perm) AS permanencia_media
FROM 
    processed
WHERE 
    sistema = 'SIH'
    A...`
- **Q73** (Epidemiológica Complexa): Linha 1: got={'cnes_estabelecimento': '2272997', 'volume_procedimentos': 1053} exp={'cnes_estabelecimento': '2270048', 'total': 4745}
  SQL: `SELECT 
    p.cnes_estabelecimento, 
    COUNT(*) AS volume_procedimentos
FROM 
    processed p
WHERE 
    p.sistema = '...`
- **Q74** (Epidemiológica Complexa): Linha não encontrada: {'sexo_paciente': 'F', 'total_obitos': 412.0, 'taxa_mortalidade': 3.94}
  SQL: `SELECT 
    sexo_paciente,
    SUM(CASE WHEN morte = 1 THEN 1 ELSE 0 END) * 100.0 / COUNT(DISTINCT n_aih) AS taxa_mortal...`
- **Q75** (Epidemiológica Complexa): Número de linhas: got=10 exp=6
  SQL: `SELECT 
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 34 THEN '18-34...`
- **Q76** (Epidemiológica Complexa): Linha 5: got={'cid_principal': 'S365', 'num_internacoes': 62, 'permanencia_media': 12.1451612903} exp={'cid_principal': 'S065', 'internacoes': 295, 'permanencia_media': 12.1}
  SQL: `SELECT 
    p.cid_principal, 
    COUNT(DISTINCT p.n_aih) AS num_internacoes,
    AVG(p.dias_perm) AS permanencia_media
...`
- **Q77** (Epidemiológica Complexa): Linha não encontrada: {'faixa_etaria': '0-17', 'total': 2380}
  SQL: `SELECT 
    CASE 
        WHEN idade_paciente BETWEEN 0 AND 17 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 39...`
- **Q79** (Epidemiológica Complexa): Número de linhas: got=5 exp=3
  SQL: `SELECT 
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 34 THEN '18-34...`
- **Q81** (Epidemiológica Complexa): got=717 exp=9617
  SQL: `SELECT COUNT(DISTINCT n_aih) AS num_internacoes
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cm...`
- **Q82** (Epidemiológica Complexa): Linha 1: got={'diagnostico': 'M216', 'total': 729} exp={'cid_principal': 'S720', 'total': 3056}
  SQL: `SELECT 
    main_icd AS diagnostico,
    COUNT(*) AS total
FROM 
    processed
WHERE 
    sistema = 'SIA'
    AND uf_ori...`
- **Q83** (Epidemiológica Complexa): got=2704595 exp=207.8
  SQL: `SELECT 
    cnes_estabelecimento,
    AVG(internacoes) AS media_internacoes
FROM (
    SELECT 
        cnes_estabelecime...`
- **Q84** (Epidemiológica Complexa): Número de linhas: got=5 exp=4
  SQL: `SELECT 
    CASE 
        WHEN dias_perm <= 3 THEN '1-3 dias'
        WHEN dias_perm BETWEEN 4 AND 7 THEN '4-7 dias'
   ...`
- **Q85** (Financeira): got=2115629.96 exp=28396358.5
  SQL: `SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes_ortopedicas_idosos
FROM 
    processed
WHERE 
    s...`
- **Q88** (Financeira): Linha não encontrada: {'sexo_paciente': 'F', 'custo_medio': 2634.6}
  SQL: `SELECT 
    sexo_paciente, 
    AVG(custo_total) AS custo_medio
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND u...`
- **Q89** (Financeira): got=1995445.81 exp=5562026.72
  SQL: `SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes_ortopedicas
FROM 
    processed
WHERE 
    sistema ...`
- **Q90** (Financeira): got=5014.9515258856 exp=3699.42
  SQL: `SELECT 
    COALESCE(SUM(custo_total) / COUNT(DISTINCT n_aih), 0) AS custo_medio
FROM 
    processed
WHERE 
    sistema ...`
- **Q91** (Financeira): Linha 1: got={'cod_munic_estabelecimento': '330000', 'total_custo': 3740518.5500000003} exp={'cod_munic_estabelecimento': '330000', 'custo_total': 26271214.76}
  SQL: `SELECT 
    p.cod_munic_estabelecimento, 
    SUM(p.custo_total) AS total_custo
FROM 
    processed p
JOIN 
    enriched...`
- **Q92** (Financeira): got=6937652.41 exp=57490872.2
  SQL: `SELECT COALESCE(SUM(val_sh), 0) AS total_valor_servico_hospitalar
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem =...`
- **Q93** (Financeira): Número de linhas: got=5 exp=3
  SQL: `SELECT 
    CASE 
        WHEN idade_paciente < 18 THEN '0-17'
        WHEN idade_paciente BETWEEN 18 AND 34 THEN '18-34...`
- **Q94** (Financeira): got=868901.98 exp=13056994.92
  SQL: `SELECT 
    COALESCE(SUM(val_uti), 0) AS total_valor_uti
FROM 
    processed
WHERE 
    sistema = 'SIH'
    AND uf_orige...`
- **Q97** (Financeira): Linha 2: got={'cid_principal': 'M511', 'num_internacoes': 129, 'custo_medio': 8820.2801550388} exp={'cid_principal': 'S068', 'internacoes': 55, 'custo_medio': 8934.42}
  SQL: `SELECT 
    p.cid_principal, 
    COUNT(DISTINCT p.n_aih) AS num_internacoes,
    SUM(p.custo_total) / COUNT(DISTINCT p....`
- **Q98** (Financeira): got=272.2012743252 exp=152.55
  SQL: `SELECT 
    AVG(custo_total) AS custo_medio
FROM 
    processed
WHERE 
    sistema = 'SIA'
    AND uf_origem = 'RJ'
    ...`
- **Q99** (Financeira): Linha 1: got={'cod_procedimento': '0702100048', 'total_custo': 9146187.890000049} exp={'cod_procedimento': '0701020369', 'custo_total': 665532.0}
  SQL: `SELECT cod_procedimento, SUM(custo_total) AS total_custo
FROM processed
WHERE sistema = 'SIA' AND uf_origem = 'RJ' AND a...`
- **Q100** (Financeira): got=1013984.5700000001 exp=16205065.06
  SQL: `SELECT 
    SUM(COALESCE(custo_total, 0)) AS custo_total_internacoes
FROM 
    processed
WHERE 
    sistema = 'SIH'
    ...`
- **Q101** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 11, 'num_obitos': 4} exp={'mes_cmpt': 1, 'total_obitos': 104}
  SQL: `SELECT mes_cmpt, COUNT(*) AS num_obitos
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND ano_cmpt = 202...`
- **Q103** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'custo_total_mensal': 82431.4} exp={'mes_cmpt': 1, 'custo_total': 1749439.39}
  SQL: `SELECT 
  mes_cmpt, 
  SUM(COALESCE(custo_total, 0)) AS custo_total_mensal
FROM 
  processed
WHERE 
  sistema = 'SIH'
  ...`
- **Q106** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'sexo_paciente': 'F', 'total_procedimentos': 136.0} exp={'mes_cmpt': 1, 'sexo_paciente': 'F', 'total': 739}
  SQL: `SELECT 
    mes_cmpt, 
    sexo_paciente, 
    SUM(pa_qtdapr) AS total_procedimentos
FROM 
    processed
WHERE 
    sist...`
- **Q107** (Temporal/Comparativa): Número de linhas: got=4 exp=8
  SQL: `SELECT 
    CASE 
        WHEN mes_cmpt IN (1, 2, 3) THEN '1º Trimestre'
        WHEN mes_cmpt IN (4, 5, 6) THEN '2º Tri...`
- **Q108** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 3, 'media_permanencia': 11.5039370079} exp={'mes_cmpt': 1, 'permanencia_media': 9.9}
  SQL: `SELECT mes_cmpt, AVG(dias_perm) AS media_permanencia
FROM processed
WHERE sistema = 'SIH'
  AND uf_origem = 'RJ'
  AND a...`
- **Q109** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'taxa_mortalidade': 0.0165289256} exp={'mes_cmpt': 1, 'internacoes': 1998, 'obitos': 104.0, 'taxa_mortalidade': 5.21}
  SQL: `SELECT 
    mes_cmpt,
    COUNT(DISTINCT CASE WHEN morte = 1 THEN n_aih END) * 1.0 / COUNT(DISTINCT n_aih) AS taxa_morta...`
- **Q110** (Temporal/Comparativa): Linha 1: got={'trimestre': 3.0, 'total_internacoes': 2707} exp={'trimestre': 'Q3', 'total': 2707}
  SQL: `SELECT 
  CEIL(mes_cmpt / 3.0) AS trimestre,
  COUNT(DISTINCT n_aih) AS total_internacoes
FROM 
  processed
WHERE 
  sis...`
- **Q111** (Temporal/Comparativa): Linha não encontrada: {'semestre': '1S', 'total': 12212}
  SQL: `SELECT 
    CASE 
        WHEN mes_cmpt BETWEEN 1 AND 6 THEN '1º Semestre'
        ELSE '2º Semestre'
    END AS semestr...`
- **Q113** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'sexo_paciente': 'F', 'volume_internacoes': 53} exp={'mes_cmpt': 1, 'sexo_paciente': 'F', 'total': 815}
  SQL: `SELECT 
    mes_cmpt, 
    sexo_paciente, 
    COUNT(DISTINCT n_aih) AS volume_internacoes
FROM 
    processed
WHERE 
  ...`
- **Q114** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 4, 'total_procedimentos': 217.0} exp={'mes_cmpt': 1, 'total': 1647}
  SQL: `SELECT mes_cmpt, SUM(pa_qtdapr) AS total_procedimentos
FROM processed
WHERE sistema = 'SIA'
  AND uf_origem = 'RJ'
  AND...`
- **Q115** (Temporal/Comparativa): Linha não encontrada: {'trimestre': 'Q1', 'total': 5399}
  SQL: `SELECT 
  CEIL(mes_cmpt / 3.0) AS trimestre,
  COUNT(*) AS volume_procedimentos
FROM 
  processed
WHERE 
  sistema = 'SI...`
- **Q116** (Temporal/Comparativa): Linha 1: got={'mes_cmpt': 1, 'custo_total_mensal': 55130.4499999999} exp={'mes_cmpt': 1, 'custo_total': 284703.64}
  SQL: `SELECT 
    mes_cmpt, 
    SUM(COALESCE(custo_total, 0)) AS custo_total_mensal
FROM 
    processed
WHERE 
    sistema = ...`
- **Q117** (Temporal/Comparativa): Linha não encontrada: {'semestre': '1S', 'custo_total': 29834491.27}
  SQL: `SELECT 
    CASE 
        WHEN mes_cmpt BETWEEN 1 AND 6 THEN '1º Semestre'
        ELSE '2º Semestre'
    END AS semestr...`

---
_Gerado por scripts/evaluate_benchmark.py — SUS Data RAG — USF/Mestrado_